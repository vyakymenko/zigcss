# ZigCSS Development Status

Last updated: 2026-07-31

## Execution context

- Goal: complete the approved roadmap in `DEVELOPMENT_PLAN.md`.
- Branch: `vale/zigcss-recovery`
- Base commit: `2d2c0d90208610e1e7a623185372ddeab01fc44c` (`main` and `origin/main` at autonomous start)
- Verified execution model: `gpt-5.6-sol`, xhigh reasoning, as explicitly changed and approved by the project owner on 2026-07-27.
- Worktree: isolated Codex worktree `/Users/vyakymenko/.codex/worktrees/9302/zigcss`; the user's main checkout is out of scope.
- Inherited changes preserved: the approved `DEVELOPMENT_PLAN.md` was untracked at autonomous start, was committed as the authoritative roadmap, and has since changed only through recorded owner-approved amendments and implementation evidence.
- External actions: on 2026-07-13 the operator explicitly authorized automatic non-force pushes of each clean green checkpoint to `origin` at the same current `vale/*` branch, then explicitly authorized merging that branch to `origin/main` without a pull request. On 2026-07-14 the operator authorized completing the remaining external gates; workflow-based GitHub Pages was enabled and its existing deployment workflow was dispatched on exact integrated `main`. On 2026-07-15 the operator explicitly resumed the remaining unproven work, including published packages and release artifacts. On 2026-07-16 the operator instructed continuation after the failed public `v0.4.0-rc.1` tag could no longer be moved safely, so the release advanced to `0.4.0-rc.2` instead of rewriting public history. Its guarded run also failed closed before publication, so continued autonomous remediation advanced to the published `0.4.0-rc.3`; neither immutable failed tag will be moved. On 2026-07-16 the operator explicitly funded full canonical SCSS, indented Sass, Less, and Stylus integration and instructed that it be added to autonomous development. On 2026-07-17 the operator authorized running the prepared release, but before any tag was created superseded that direction with a self-contained native/zero-production-dependency requirement. The `0.5.0-rc.1` publication was cancelled with no tag, release, attestation, package, or artifact created; ADR-013 and Milestone 10 now govern the product. Automatic green checkpoint push and non-force main integration remain authorized. On 2026-07-27 the owner separately authorized the first fully graduated native release only after every `NATIVE-009` gate passes: one unused immutable tag may invoke the existing fail-closed workflow for a GitHub prerelease and npm `next` publication. The authorization excludes `0.5.0-rc.1`, moved/reused tags, npm `latest`, bypassed gates, and Homebrew, editor-extension, container, documentation, or service publication.

## CI security remediation

- Work package: `SEC-ADVISORY-2026-07-28`
- State: `VERIFIED`
- Implementation checkpoint: `873ad8a3a86997339e3491731d08bed749b91028`
- Trigger and reproduction: integrated-main Build run `30300618455` passed all five native platform jobs but failed the Test Suite production-audit gate with four high findings. The root graph resolved `stylus@0.64.0 -> glob@10.5.0 -> minimatch@9.0.9 -> brace-expansion@2.1.2`, which became vulnerable under `GHSA-mh99-v99m-4gvg`. After that first failure was removed, the sequential gate exposed `react-router@7.18.1` under `GHSA-qwww-vcr4-c8h2` in the documentation graph and the same brace-expansion advisory through `vscode-languageclient@9.0.1` in the extension graph. Full development audits additionally exposed compatible patch updates for `fast-uri` under `GHSA-v2hh-gcrm-f6hx` and `tar` under `GHSA-r292-9mhp-454m`.
- Change: the root manifest now owns one exact fail-closed `brace-expansion@5.0.8` override, the lock resolves its reviewed Stylus edge to `balanced-match@4.0.4`, and dependency policy tests reject a missing, extra, vulnerable, or detached override. Generated runtime SPDX/notices and the exact Stylus closure fingerprint bind those new bytes. The website moved to React/React DOM 19.2.7 and the only patched React Router release, 8.3.0, with an exact Node 22.22.0 build contract. The extension moved to `vscode-languageclient@10.1.0`, VS Code `^1.91.0`, Node16 package-export resolution, current runtime notices/licenses, and its supported LSP dependency graph. Compatible lock-only updates select `fast-uri@3.1.4` and `tar@7.5.22`.
- Decision: npm's breaking forced downgrade was rejected because it would violate the canonical Stylus 0.64.0 oracle. The React Router advisory reaches unstable RSC paths that the static site does not use, but no vulnerability exception or weakened audit gate was admitted. Upstream-supported React Router and Language Client migrations were chosen over unsupported transitive substitution; engine and editor minimums advanced with their runtime requirements.
- Evidence: the test-first dependency baseline failed 2/5 exactly on the absent reviewed override. Final clean lifecycle-disabled installs reproduce all three locks; full and production audits report zero findings for root, documentation, and VS Code. Dependency policy passes 5/5 plus check, package policy 6/6 plus check over 86 runtime dependencies, and workflow policy 5/5 plus check. Canonical provider/reference/product coverage passes 151/151, including all 326 Stylus successes and 20 integration errors. Exact Node 22.22.0 passes the website's 153/153 tests and production build/bundle budget; documentation integrity and capability status each pass 7/7 plus check. The extension passes 13/13, compiles, bundles, and produces its exact five-file/147,011-byte VSIX. Zig 0.15.2 passes focused native 408/408 and full compiler 903/903 under the isolated macOS 15.4 SDK selector. JavaScript/JSON syntax, generated metadata, formatting/diff, and clean-worktree checks pass.
- Blockers and external state: none for this remediation. The autonomous runner remains paused until the integrated checkpoint is pushed and hosted Build closes green. No tag, release, package publication, deployment, or release interlock changed.

## Current work

- Milestone: Milestone 10 self-contained native stylesheet frontends, in parallel with Milestone 8 controlled benchmark evidence
- Work package: `NSASS-011` native Sass evaluation
- State: `IN_PROGRESS`
- Active phase: the two-hundred-seventy-first native Sass semantic checkpoint, `NSASS-011-LOCAL-USE-THIRTY-FIFTH-MODULE-REEXPORTED-CALLABLE-CONFIGURATION-OWNERSHIP-FOUNDATION`, is `VERIFIED` at local implementation checkpoint `56e5b3fd327988340a46191f7c6f198d8c509bce`. Fresh Dart Sass 1.101.0 in-memory importer probes establish that function and mixin references retain exact behavior through thirty-fifth- and thirty-sixth-module configuration in SCSS and indented Sass: invocation and application inside and after the configured modules, original-owner live state, identical compressed CSS, and zero warnings all match. The additive native fixture retains the prior thirty-fourth-module regression and separately admits only the scheduled thirty-fifth-module hop across direct and enumerated public variables, nested aggregates, module-returned values, invocation/application inside and after the thirty-fifth module, default/custom canonical aliases, original-owner identity and state, deterministic dependency order, maps, and SCSS/indented-Sass parity. Native Zig centralizes the boundary as a maximum configurable re-export depth of thirty-three: the original owner identity enters at depth zero; the middle through thirty-fourth re-exports enter at depths one through thirty-three and may configure the third through thirty-fifth modules respectively; and the thirty-fifth re-export enters at depth thirty-four and remains rejected when configuring a thirty-sixth module. Every accepted aggregate is deep-owned by its destination while the stable packed original owner/callable identity remains unchanged, so calls, mixin application, inspection, content acceptance, aliases, equality, and mutable state cross the new hop without retaining evaluator-arena pointers. Callable arguments and cross-module `@content` retain their stricter depth-zero/owner-only remappers. The test-first focused Debug gate stopped at 639/642 with exactly three failures: the thirty-fifth-module positive and exhaustive-allocation paths reached the former maximum-depth-thirty-two rejection, while the advanced no-partial diagnostic still pointed to `thirty-fourth.$exported` rather than the new thirty-sixth-module boundary. Exact thirty-sixth-module failure now owns one diagnostic at `thirty-fifth.$exported`, leaves generated position zero, and emits no CSS; exhaustive-allocation coverage exercises the accepted thirty-fifth-module path and every allocation failure passes. Final focused native Debug/ReleaseSafe each pass 23/23 steps with 642/642 tests; full compiler Debug/ReleaseSafe each pass 60/60 steps with 1137/1137 tests; and evaluator-test-root plus preprocessor-root no-emission cross-compiles pass for Windows x64 GNU and Linux ARM64 GNU in both modes. Native contract 12/12 plus check adds only `local-use-thirty-fifth-module-reexported-callable-configuration-ownership-foundation` while retaining private native Sass rows, production unreachability, zero target package/optional/runtime dependencies, and the closed release interlock. Complete provider/reference/product matrices pass 153/153; preprocessor package policy passes 6/6 plus check over 22 runtime sources, 28 archive files, 86 locked dependencies, and 5 targets; capability-status policy passes 7/7 plus check; autonomous-runner selftest passes 59/59; and repository-wide Zig formatting, changed JavaScript syntax, all 36 tracked JSON files, and diff checks pass. The bounded sibling search confirms that thirty-sixth-and-later native configuration, raw caller callables, another-parent identities, callable arguments and `@content` transport, transitive modules, `@forward`, legacy imports, `meta.load-css()` execution, broader resolution, production routing, public capability claims, native graduation, version, release interlock, tag, artifact/package publication, deployment, and every external system remain unchanged.
- Quality closure: `QUAL-001` is verified at `3eced86`; repository-wide Zig formatting debt is zero and the final local validation matrix is green
- Next eligible package: admit only the next thirty-sixth-module hop already measured against Dart Sass 1.101.0 as the smallest durable `NSASS-011-LOCAL-USE-THIRTY-SIXTH-MODULE-REEXPORTED-CALLABLE-CONFIGURATION-OWNERSHIP-FOUNDATION`, with direct/enumerated and nested values, invocation/application inside and after the thirty-sixth module, canonical/default/custom aliases, live owner state, SCSS/indented-Sass parity, exact provenance depth, strict diagnostics, limits, and every allocation failure. Retain thirty-seventh-and-later configuration, raw caller callables, another-parent identities, callable arguments and `@content` transport, transitive modules, `@forward`, legacy imports, `meta.load-css()`, broader resolution, and native graduation as separate measured slices. Provider removal and the package-level zero-dependency cutover remain gated on native graduation rather than deleting the exact development oracles early. Independently, `BENCH-007` still requires a dedicated non-emulated Linux x64 runner with labels `[self-hosted, linux, x64, zigcss-benchmark-v1]`; its absence must not pause eligible native frontend work.
- Runtime control: model identity, single-agent execution, isolated-worktree confinement, automatic supervisor-owned green-checkpoint pushes, the owner-bounded post-`NATIVE-009` GitHub-prerelease/npm-`next` authority, and every native graduation/release interlock remain unchanged.

## Prior current-work evidence (thirty-fourth-module callable configuration checkpoint)

- Milestone: Milestone 10 self-contained native stylesheet frontends, in parallel with Milestone 8 controlled benchmark evidence
- Work package: `NSASS-011` native Sass evaluation
- State: `IN_PROGRESS`
- Active phase: the two-hundred-seventieth native Sass semantic checkpoint, `NSASS-011-LOCAL-USE-THIRTY-FOURTH-MODULE-REEXPORTED-CALLABLE-CONFIGURATION-OWNERSHIP-FOUNDATION`, is `VERIFIED` at local implementation checkpoint `d1b53ac52704337ca5a5fc61f7395f2f433b3f8c`. Fresh Dart Sass 1.101.0 in-memory importer probes establish that function and mixin references retain exact behavior through thirty-fourth- and thirty-fifth-module configuration in SCSS and indented Sass: invocation and application inside and after the configured modules, original-owner live state, identical compressed CSS, and zero warnings all match. The additive native fixture retains the prior thirty-third-module regression and separately admits only the scheduled thirty-fourth-module hop across direct and enumerated public variables, nested aggregates, module-returned values, invocation/application inside and after the thirty-fourth module, default/custom canonical aliases, original-owner identity and state, deterministic dependency order, maps, and SCSS/indented-Sass parity. Native Zig centralizes the boundary as a maximum configurable re-export depth of thirty-two: the original owner identity enters at depth zero; the middle through thirty-third re-exports enter at depths one through thirty-two and may configure the third through thirty-fourth modules respectively; and the thirty-fourth re-export enters at depth thirty-three and remains rejected when configuring a thirty-fifth module. Every accepted aggregate is deep-owned by its destination while the stable packed original owner/callable identity remains unchanged, so calls, mixin application, inspection, content acceptance, aliases, equality, and mutable state cross the new hop without retaining evaluator-arena pointers. Callable arguments and cross-module `@content` retain their stricter depth-zero/owner-only remappers. The test-first focused Debug gate stopped at 638/641 with exactly three failures: the thirty-fourth-module positive and exhaustive-allocation paths reached the former maximum-depth-thirty-one rejection, while the advanced no-partial diagnostic still pointed to `thirty-third.$exported` rather than the new thirty-fifth-module boundary. Exact thirty-fifth-module failure now owns one diagnostic at `thirty-fourth.$exported`, leaves generated position zero, and emits no CSS; exhaustive-allocation coverage exercises the accepted thirty-fourth-module path and every allocation failure passes. Final focused native Debug/ReleaseSafe each pass 23/23 steps with 641/641 tests; full compiler Debug/ReleaseSafe each pass 60/60 steps with 1136/1136 tests; and evaluator-test-root plus preprocessor-root no-emission cross-compiles pass for Windows x64 GNU and Linux ARM64 GNU in both modes. Native contract 12/12 plus check adds only `local-use-thirty-fourth-module-reexported-callable-configuration-ownership-foundation` while retaining private native Sass rows, production unreachability, zero target package/optional/runtime dependencies, and the closed release interlock. Complete provider/reference/product matrices pass 153/153; preprocessor package policy passes 6/6 plus check over 22 runtime sources, 28 archive files, 86 locked dependencies, and 5 targets; capability-status policy passes 7/7 plus check; autonomous-runner selftest passes 59/59; and repository-wide Zig formatting, changed JavaScript syntax, all 36 tracked JSON files, and diff checks pass. The bounded sibling search confirms that thirty-fifth-and-later native configuration, raw caller callables, another-parent identities, callable arguments and `@content` transport, transitive modules, `@forward`, legacy imports, `meta.load-css()` execution, broader resolution, production routing, public capability claims, native graduation, version, release interlock, tag, artifact/package publication, deployment, and every external system remain unchanged.
- Quality closure: `QUAL-001` is verified at `3eced86`; repository-wide Zig formatting debt is zero and the final local validation matrix is green
- Next eligible package: admit only the next thirty-fifth-module hop already measured against Dart Sass 1.101.0 as the smallest durable `NSASS-011-LOCAL-USE-THIRTY-FIFTH-MODULE-REEXPORTED-CALLABLE-CONFIGURATION-OWNERSHIP-FOUNDATION`, with direct/enumerated and nested values, invocation/application inside and after the thirty-fifth module, canonical/default/custom aliases, live owner state, SCSS/indented-Sass parity, exact provenance depth, strict diagnostics, limits, and every allocation failure. Retain thirty-sixth-and-later configuration, raw caller callables, another-parent identities, callable arguments and `@content` transport, transitive modules, `@forward`, legacy imports, `meta.load-css()`, broader resolution, and native graduation as separate measured slices. Provider removal and the package-level zero-dependency cutover remain gated on native graduation rather than deleting the exact development oracles early. Independently, `BENCH-007` still requires a dedicated non-emulated Linux x64 runner with labels `[self-hosted, linux, x64, zigcss-benchmark-v1]`; its absence must not pause eligible native frontend work.
- Runtime control: model identity, single-agent execution, isolated-worktree confinement, automatic supervisor-owned green-checkpoint pushes, the owner-bounded post-`NATIVE-009` GitHub-prerelease/npm-`next` authority, and every native graduation/release interlock remain unchanged.

## Prior current-work evidence (thirty-third-module callable configuration checkpoint)

- Milestone: Milestone 10 self-contained native stylesheet frontends, in parallel with Milestone 8 controlled benchmark evidence
- Work package: `NSASS-011` native Sass evaluation
- State: `IN_PROGRESS`
- Active phase: the two-hundred-sixty-ninth native Sass semantic checkpoint, `NSASS-011-LOCAL-USE-THIRTY-THIRD-MODULE-REEXPORTED-CALLABLE-CONFIGURATION-OWNERSHIP-FOUNDATION`, is `VERIFIED` at local implementation checkpoint `304648d3538567589411911dc64611f474003e7e`. Fresh Dart Sass 1.101.0 in-memory importer probes establish that function and mixin references retain exact behavior through thirty-third- and thirty-fourth-module configuration in SCSS and indented Sass: invocation and application inside and after the configured modules, original-owner live state, identical compressed CSS, and zero warnings all match. The additive native fixture retains the prior thirty-second-module regression and separately admits only the scheduled thirty-third-module hop across direct and enumerated public variables, nested aggregates, module-returned values, invocation/application inside and after the thirty-third module, default/custom canonical aliases, original-owner identity and state, deterministic dependency order, maps, and SCSS/indented-Sass parity. Native Zig centralizes the boundary as a maximum configurable re-export depth of thirty-one: the original owner identity enters at depth zero; the middle through thirty-second re-exports enter at depths one through thirty-one and may configure the third through thirty-third modules respectively; and the thirty-third re-export enters at depth thirty-two and remains rejected when configuring a thirty-fourth module. Every accepted aggregate is deep-owned by its destination while the stable packed original owner/callable identity remains unchanged, so calls, mixin application, inspection, content acceptance, aliases, equality, and mutable state cross the new hop without retaining evaluator-arena pointers. Callable arguments and cross-module `@content` retain their stricter depth-zero/owner-only remappers. The test-first focused Debug gate stopped at 637/640 with exactly three failures: the thirty-third-module positive and exhaustive-allocation paths reached the former maximum-depth-thirty rejection, while the advanced no-partial diagnostic still pointed to `thirty-second.$exported` rather than the new thirty-fourth-module boundary. Exact thirty-fourth-module failure now owns one diagnostic at `thirty-third.$exported`, leaves generated position zero, and emits no CSS; exhaustive-allocation coverage exercises the accepted thirty-third-module path and every allocation failure passes. Final focused native Debug/ReleaseSafe each pass 23/23 steps with 640/640 tests; full compiler Debug/ReleaseSafe each pass 60/60 steps with 1135/1135 tests; and evaluator-test-root plus preprocessor-root no-emission cross-compiles pass for Windows x64 GNU and Linux ARM64 GNU in both modes. Native contract 12/12 plus check adds only `local-use-thirty-third-module-reexported-callable-configuration-ownership-foundation` while retaining private native Sass rows, production unreachability, zero target package/optional/runtime dependencies, and the closed release interlock. Complete provider/reference/product matrices pass 153/153; preprocessor package policy passes 6/6 plus check over 22 runtime sources, 28 archive files, 86 locked dependencies, and 5 targets; capability-status policy passes 7/7 plus check; autonomous-runner selftest passes 59/59; and repository-wide Zig formatting, changed JavaScript syntax, all 36 tracked JSON files, and diff checks pass. The bounded sibling search confirms that thirty-fourth-and-later native configuration, raw caller callables, another-parent identities, callable arguments and `@content` transport, transitive modules, `@forward`, legacy imports, `meta.load-css()` execution, broader resolution, production routing, public capability claims, native graduation, version, release interlock, tag, artifact/package publication, deployment, and every external system remain unchanged.
- Quality closure: `QUAL-001` is verified at `3eced86`; repository-wide Zig formatting debt is zero and the final local validation matrix is green
- Next eligible package: admit only the next thirty-fourth-module hop already measured against Dart Sass 1.101.0 as the smallest durable `NSASS-011-LOCAL-USE-THIRTY-FOURTH-MODULE-REEXPORTED-CALLABLE-CONFIGURATION-OWNERSHIP-FOUNDATION`, with direct/enumerated and nested values, invocation/application inside and after the thirty-fourth module, canonical/default/custom aliases, live owner state, SCSS/indented-Sass parity, exact provenance depth, strict diagnostics, limits, and every allocation failure. Retain thirty-fifth-and-later configuration, raw caller callables, another-parent identities, callable arguments and `@content` transport, transitive modules, `@forward`, legacy imports, `meta.load-css()`, broader resolution, and native graduation as separate measured slices. Provider removal and the package-level zero-dependency cutover remain gated on native graduation rather than deleting the exact development oracles early. Independently, `BENCH-007` still requires a dedicated non-emulated Linux x64 runner with labels `[self-hosted, linux, x64, zigcss-benchmark-v1]`; its absence must not pause eligible native frontend work.
- Runtime control: model identity, single-agent execution, isolated-worktree confinement, automatic supervisor-owned green-checkpoint pushes, the owner-bounded post-`NATIVE-009` GitHub-prerelease/npm-`next` authority, and every native graduation/release interlock remain unchanged.

## Prior current-work evidence (thirty-second-module callable configuration checkpoint)

- Milestone: Milestone 10 self-contained native stylesheet frontends, in parallel with Milestone 8 controlled benchmark evidence
- Work package: `NSASS-011` native Sass evaluation
- State: `IN_PROGRESS`
- Active phase: the two-hundred-sixty-eighth native Sass semantic checkpoint, `NSASS-011-LOCAL-USE-THIRTY-SECOND-MODULE-REEXPORTED-CALLABLE-CONFIGURATION-OWNERSHIP-FOUNDATION`, is `VERIFIED` at local implementation checkpoint `7229cefa359f9cd1feb99ae50535b51998d7ccec`. Fresh Dart Sass 1.101.0 in-memory importer probes establish that function and mixin references retain exact behavior through thirty-second- and thirty-third-module configuration in SCSS and indented Sass: invocation and application inside and after the configured modules, original-owner live state, identical compressed CSS, and zero warnings all match. The additive native fixture retains the prior thirty-first-module regression and separately admits only the scheduled thirty-second-module hop across direct and enumerated public variables, nested aggregates, module-returned values, invocation/application inside and after the thirty-second module, default/custom canonical aliases, original-owner identity and state, deterministic dependency order, maps, and SCSS/indented-Sass parity. Native Zig centralizes the boundary as a maximum configurable re-export depth of thirty: the original owner identity enters at depth zero; the middle through thirty-first re-exports enter at depths one through thirty and may configure the third through thirty-second modules respectively; and the thirty-second re-export enters at depth thirty-one and remains rejected when configuring a thirty-third module. Every accepted aggregate is deep-owned by its destination while the stable packed original owner/callable identity remains unchanged, so calls, mixin application, inspection, content acceptance, aliases, equality, and mutable state cross the new hop without retaining evaluator-arena pointers. Callable arguments and cross-module `@content` retain their stricter depth-zero/owner-only remappers. The test-first focused Debug gate stopped at 636/639 with exactly three failures: the thirty-second-module positive and exhaustive-allocation paths reached the former maximum-depth-twenty-nine rejection, while the advanced no-partial diagnostic still pointed to `thirty-first.$exported` rather than the new thirty-third-module boundary. Exact thirty-third-module failure now owns one diagnostic at `thirty-second.$exported`, leaves generated position zero, and emits no CSS; exhaustive-allocation coverage exercises the accepted thirty-second-module path and every allocation failure passes. Final focused native Debug/ReleaseSafe each pass 23/23 steps with 639/639 tests; full compiler Debug/ReleaseSafe each pass 60/60 steps with 1134/1134 tests; and evaluator-test-root plus preprocessor-root no-emission cross-compiles pass for Windows x64 GNU and Linux ARM64 GNU in both modes. Native contract 12/12 plus check adds only `local-use-thirty-second-module-reexported-callable-configuration-ownership-foundation` while retaining private native Sass rows, production unreachability, zero target package/optional/runtime dependencies, and the closed release interlock. Complete provider/reference/product matrices pass 153/153; preprocessor package policy passes 6/6 plus check over 22 runtime sources, 28 archive files, 86 locked dependencies, and 5 targets; capability-status policy passes 7/7 plus check; autonomous-runner selftest passes 59/59; and repository-wide Zig formatting, changed JavaScript syntax, all 36 tracked JSON files, and diff checks pass. The bounded sibling search confirms that thirty-third-and-later native configuration, raw caller callables, another-parent identities, callable arguments and `@content` transport, transitive modules, `@forward`, legacy imports, `meta.load-css()` execution, broader resolution, production routing, public capability claims, native graduation, version, release interlock, tag, artifact/package publication, deployment, and every external system remain unchanged.
- Quality closure: `QUAL-001` is verified at `3eced86`; repository-wide Zig formatting debt is zero and the final local validation matrix is green
- Next eligible package: admit only the next thirty-third-module hop already measured against Dart Sass 1.101.0 as the smallest durable `NSASS-011-LOCAL-USE-THIRTY-THIRD-MODULE-REEXPORTED-CALLABLE-CONFIGURATION-OWNERSHIP-FOUNDATION`, with direct/enumerated and nested values, invocation/application inside and after the thirty-third module, canonical/default/custom aliases, live owner state, SCSS/indented-Sass parity, exact provenance depth, strict diagnostics, limits, and every allocation failure. Retain thirty-fourth-and-later configuration, raw caller callables, another-parent identities, callable arguments and `@content` transport, transitive modules, `@forward`, legacy imports, `meta.load-css()`, broader resolution, and native graduation as separate measured slices. Provider removal and the package-level zero-dependency cutover remain gated on native graduation rather than deleting the exact development oracles early. Independently, `BENCH-007` still requires a dedicated non-emulated Linux x64 runner with labels `[self-hosted, linux, x64, zigcss-benchmark-v1]`; its absence must not pause eligible native frontend work.
- Runtime control: model identity, single-agent execution, isolated-worktree confinement, automatic supervisor-owned green-checkpoint pushes, the owner-bounded post-`NATIVE-009` GitHub-prerelease/npm-`next` authority, and every native graduation/release interlock remain unchanged.

## Prior current-work evidence (thirty-first-module callable configuration checkpoint)

- Milestone: Milestone 10 self-contained native stylesheet frontends, in parallel with Milestone 8 controlled benchmark evidence
- Work package: `NSASS-011` native Sass evaluation
- State: `IN_PROGRESS`
- Active phase: the two-hundred-sixty-seventh native Sass semantic checkpoint, `NSASS-011-LOCAL-USE-THIRTY-FIRST-MODULE-REEXPORTED-CALLABLE-CONFIGURATION-OWNERSHIP-FOUNDATION`, is `VERIFIED` at local implementation checkpoint `7bbe368c3f0875d5d9ffd92727c3e78710574399`. Fresh Dart Sass 1.101.0 in-memory importer probes establish that function and mixin references retain exact behavior through thirty-first- and thirty-second-module configuration in SCSS and indented Sass: invocation and application inside and after the configured modules, default and custom canonical aliases, original-owner live state, identical compressed CSS, and zero warnings all match. The additive native fixture retains the prior thirtieth-module regression and separately admits only the scheduled thirty-first-module hop across direct and enumerated public variables, nested aggregates, module-returned values, invocation/application inside and after the thirty-first module, default/custom canonical aliases, original-owner identity and state, deterministic dependency order, maps, and SCSS/indented-Sass parity. Native Zig centralizes the boundary as a maximum configurable re-export depth of twenty-nine: the original owner identity enters at depth zero; the middle through thirtieth re-exports enter at depths one through twenty-nine and may configure the third through thirty-first modules respectively; and the thirty-first re-export enters at depth thirty and remains rejected when configuring a thirty-second module. Every accepted aggregate is deep-owned by its destination while the stable packed original owner/callable identity remains unchanged, so calls, mixin application, inspection, content acceptance, aliases, equality, and mutable state cross the new hop without retaining evaluator-arena pointers. Callable arguments and cross-module `@content` retain their stricter depth-zero/owner-only remappers. The test-first focused Debug gate stopped at 635/638 with exactly three failures: the thirty-first-module positive and exhaustive-allocation paths reached the former maximum-depth-twenty-eight rejection, while the advanced no-partial diagnostic still pointed to `thirtieth.$exported` rather than the new thirty-second-module boundary. Exact thirty-second-module failure now owns one diagnostic at `thirty-first.$exported`, leaves generated position zero, and emits no CSS; exhaustive-allocation coverage exercises the accepted thirty-first-module path and every allocation failure passes. Final focused native Debug/ReleaseSafe each pass 23/23 steps with 638/638 tests; full compiler Debug/ReleaseSafe each pass 60/60 steps with 1133/1133 tests; and evaluator-test-root plus preprocessor-root no-emission cross-compiles pass for Windows x64 GNU and Linux ARM64 GNU in both modes. Native contract 12/12 plus check adds only `local-use-thirty-first-module-reexported-callable-configuration-ownership-foundation` while retaining private native Sass rows, production unreachability, zero target package/optional/runtime dependencies, and the closed release interlock. Complete provider/reference/product matrices pass 153/153; preprocessor package policy passes 6/6 plus check over 22 runtime sources, 28 archive files, 86 locked dependencies, and 5 targets; capability-status policy passes 7/7 plus check; autonomous-runner selftest passes 59/59; and repository-wide Zig formatting, changed JavaScript syntax, all 36 tracked JSON files, and diff checks pass. The bounded sibling search confirms that thirty-second-and-later native configuration, raw caller callables, another-parent identities, callable arguments and `@content` transport, transitive modules, `@forward`, legacy imports, `meta.load-css()` execution, broader resolution, production routing, public capability claims, native graduation, version, release interlock, tag, artifact/package publication, deployment, and every external system remain unchanged.
- Quality closure: `QUAL-001` is verified at `3eced86`; repository-wide Zig formatting debt is zero and the final local validation matrix is green
- Next eligible package: admit only the next thirty-second-module hop already measured against Dart Sass 1.101.0 as the smallest durable `NSASS-011-LOCAL-USE-THIRTY-SECOND-MODULE-REEXPORTED-CALLABLE-CONFIGURATION-OWNERSHIP-FOUNDATION`, with direct/enumerated and nested values, invocation/application inside and after the thirty-second module, canonical/default/custom aliases, live owner state, SCSS/indented-Sass parity, exact provenance depth, strict diagnostics, limits, and every allocation failure. Retain thirty-third-and-later configuration, raw caller callables, another-parent identities, callable arguments and `@content` transport, transitive modules, `@forward`, legacy imports, `meta.load-css()`, broader resolution, and native graduation as separate measured slices. Provider removal and the package-level zero-dependency cutover remain gated on native graduation rather than deleting the exact development oracles early. Independently, `BENCH-007` still requires a dedicated non-emulated Linux x64 runner with labels `[self-hosted, linux, x64, zigcss-benchmark-v1]`; its absence must not pause eligible native frontend work.
- Runtime control: model identity, single-agent execution, isolated-worktree confinement, automatic supervisor-owned green-checkpoint pushes, the owner-bounded post-`NATIVE-009` GitHub-prerelease/npm-`next` authority, and every native graduation/release interlock remain unchanged.

## Prior current-work evidence (thirtieth-module callable configuration checkpoint)

- Milestone: Milestone 10 self-contained native stylesheet frontends, in parallel with Milestone 8 controlled benchmark evidence
- Work package: `NSASS-011` native Sass evaluation
- State: `IN_PROGRESS`
- Active phase: the two-hundred-sixty-sixth native Sass semantic checkpoint, `NSASS-011-LOCAL-USE-THIRTIETH-MODULE-REEXPORTED-CALLABLE-CONFIGURATION-OWNERSHIP-FOUNDATION`, is `VERIFIED` at local implementation checkpoint `ef1540270ea26391d45b999765df9f0f8b466b3f`. Fresh Dart Sass 1.101.0 in-memory importer probes establish that function and mixin references retain exact behavior through thirtieth- and thirty-first-module configuration in SCSS and indented Sass: invocation and application inside and after the configured modules, original-owner live state, identical compressed CSS, and zero warnings all match. The additive native fixture retains the prior twenty-ninth-module regression and separately admits only the scheduled thirtieth-module hop across direct and enumerated public variables, nested aggregates, module-returned values, invocation/application inside and after the thirtieth module, default/custom canonical aliases, original-owner identity and state, deterministic dependency order, maps, and SCSS/indented-Sass parity. Native Zig centralizes the boundary as a maximum configurable re-export depth of twenty-eight: the original owner identity enters at depth zero; the middle through twenty-ninth re-exports enter at depths one through twenty-eight and may configure the third through thirtieth modules respectively; and the thirtieth re-export enters at depth twenty-nine and remains rejected when configuring a thirty-first module. Every accepted aggregate is deep-owned by its destination while the stable packed original owner/callable identity remains unchanged, so calls, mixin application, inspection, content acceptance, aliases, equality, and mutable state cross the new hop without retaining evaluator-arena pointers. Callable arguments and cross-module `@content` retain their stricter depth-zero/owner-only remappers. The corrected test-first focused Debug gate stopped at 634/637 with exactly three failures: the thirtieth-module positive and exhaustive-allocation paths reached the former maximum-depth-twenty-seven rejection, while the advanced no-partial diagnostic still pointed to `twenty-ninth.$exported` rather than the new thirty-first-module boundary. Exact thirty-first-module failure now owns one diagnostic at `thirtieth.$exported`, leaves generated position zero, and emits no CSS; exhaustive-allocation coverage exercises the accepted thirtieth-module path and every allocation failure passes. Final focused native Debug/ReleaseSafe each pass 23/23 steps with 637/637 tests; full compiler Debug/ReleaseSafe each pass 60/60 steps with 1132/1132 tests; and evaluator-test-root plus preprocessor-root no-emission cross-compiles pass for Windows x64 GNU and Linux ARM64 GNU in both modes. Native contract 12/12 plus check adds only `local-use-thirtieth-module-reexported-callable-configuration-ownership-foundation` while retaining private native Sass rows, production unreachability, zero target package/optional/runtime dependencies, and the closed release interlock. Complete provider/reference/product matrices pass 153/153; preprocessor package policy passes 6/6 plus check over 22 runtime sources, 28 archive files, 86 locked dependencies, and 5 targets; capability-status policy passes 7/7 plus check; autonomous-runner selftest passes 59/59; and repository-wide Zig formatting, changed JavaScript syntax, all 36 tracked JSON files, and diff checks pass. The bounded sibling search confirms that thirty-first-and-later native configuration, raw caller callables, another-parent identities, callable arguments and `@content` transport, transitive modules, `@forward`, legacy imports, `meta.load-css()` execution, broader resolution, production routing, public capability claims, native graduation, version, release interlock, tag, artifact/package publication, deployment, and every external system remain unchanged.
- Quality closure: `QUAL-001` is verified at `3eced86`; repository-wide Zig formatting debt is zero and the final local validation matrix is green
- Next eligible package: admit only the next thirty-first-module hop already measured against Dart Sass 1.101.0 as the smallest durable `NSASS-011-LOCAL-USE-THIRTY-FIRST-MODULE-REEXPORTED-CALLABLE-CONFIGURATION-OWNERSHIP-FOUNDATION`, with direct/enumerated and nested values, invocation/application inside and after the thirty-first module, canonical/default/custom aliases, live owner state, SCSS/indented-Sass parity, exact provenance depth, strict diagnostics, limits, and every allocation failure. Retain thirty-second-and-later configuration, raw caller callables, another-parent identities, callable arguments and `@content` transport, transitive modules, `@forward`, legacy imports, `meta.load-css()`, broader resolution, and native graduation as separate measured slices. Provider removal and the package-level zero-dependency cutover remain gated on native graduation rather than deleting the exact development oracles early. Independently, `BENCH-007` still requires a dedicated non-emulated Linux x64 runner with labels `[self-hosted, linux, x64, zigcss-benchmark-v1]`; its absence must not pause eligible native frontend work.
- Runtime control: model identity, single-agent execution, isolated-worktree confinement, automatic supervisor-owned green-checkpoint pushes, the owner-bounded post-`NATIVE-009` GitHub-prerelease/npm-`next` authority, and every native graduation/release interlock remain unchanged.

## Prior current-work evidence (twenty-ninth-module callable configuration checkpoint)

- Milestone: Milestone 10 self-contained native stylesheet frontends, in parallel with Milestone 8 controlled benchmark evidence
- Work package: `NSASS-011` native Sass evaluation
- State: `IN_PROGRESS`
- Active phase: the two-hundred-sixty-fifth native Sass semantic checkpoint, `NSASS-011-LOCAL-USE-TWENTY-NINTH-MODULE-REEXPORTED-CALLABLE-CONFIGURATION-OWNERSHIP-FOUNDATION`, is `VERIFIED` at local implementation checkpoint `dcbfe4ea3af82ebd7f63f3916dd61e69a3302be1`. The prior fresh Dart Sass 1.101.0 in-memory importer probes established exact behavior for the admitted twenty-ninth-module configuration in SCSS and indented Sass: function invocation and mixin application inside and after the configured module, original-owner live state, identical compressed CSS, and zero warnings all match. The additive native fixture retains the prior twenty-eighth-module regression and separately admits only the scheduled twenty-ninth-module hop across direct and enumerated public variables, nested aggregates, module-returned values, invocation/application inside and after the twenty-ninth module, default/custom canonical aliases, original-owner identity and state, deterministic dependency order, maps, and SCSS/indented-Sass parity. Native Zig centralizes the boundary as a maximum configurable re-export depth of twenty-seven: the original owner identity enters at depth zero; the middle through twenty-eighth re-exports enter at depths one through twenty-seven and may configure the third through twenty-ninth modules respectively; and the twenty-ninth re-export enters at depth twenty-eight and remains rejected when configuring a thirtieth module. Every accepted aggregate is deep-owned by its destination while the stable packed original owner/callable identity remains unchanged, so calls, mixin application, inspection, content acceptance, aliases, equality, and mutable state cross the new hop without retaining evaluator-arena pointers. Callable arguments and cross-module `@content` retain their stricter depth-zero/owner-only remappers. The test-first focused Debug gate stopped at 633/636 with exactly three failures: the twenty-ninth-module positive and exhaustive-allocation paths reached the former maximum-depth-twenty-six rejection, while the advanced no-partial diagnostic still pointed to `twenty-eighth.$exported` rather than the new thirtieth-module boundary. Exact thirtieth-module failure now owns one diagnostic at `twenty-ninth.$exported`, leaves generated position zero, and emits no CSS; exhaustive-allocation coverage exercises the accepted twenty-ninth-module path and every allocation failure passes. Final focused native Debug/ReleaseSafe each pass 23/23 steps with 636/636 tests; full compiler Debug/ReleaseSafe each pass 60/60 steps with 1131/1131 tests; and evaluator-test-root plus preprocessor-root no-emission cross-compiles pass for Windows x64 GNU and Linux ARM64 GNU in both modes. Native contract 12/12 plus check adds only `local-use-twenty-ninth-module-reexported-callable-configuration-ownership-foundation` while retaining private native Sass rows, production unreachability, zero target package/optional/runtime dependencies, and the closed release interlock. Complete provider/reference/product matrices pass 153/153; preprocessor package policy passes 6/6 plus check over 22 runtime sources, 28 archive files, 86 locked dependencies, and 5 targets; capability-status policy passes 7/7 plus check; autonomous-runner selftest passes 59/59; and repository-wide Zig formatting, changed JavaScript syntax, all 36 tracked JSON files, and diff checks pass. The bounded sibling search confirms that thirtieth-and-later native configuration, raw caller callables, another-parent identities, callable arguments and `@content` transport, transitive modules, `@forward`, legacy imports, `meta.load-css()` execution, broader resolution, production routing, public capability claims, native graduation, version, release interlock, tag, artifact/package publication, deployment, and every external system remain unchanged.
- Quality closure: `QUAL-001` is verified at `3eced86`; repository-wide Zig formatting debt is zero and the final local validation matrix is green
- Next eligible package: measure the exact Dart Sass 1.101.0 thirtieth-module hop and admit only the smallest durable `NSASS-011-LOCAL-USE-THIRTIETH-MODULE-REEXPORTED-CALLABLE-CONFIGURATION-OWNERSHIP-FOUNDATION` if executable evidence supports it, with direct/enumerated and nested values, invocation/application inside and after the thirtieth module, canonical/default/custom aliases, live owner state, SCSS/indented-Sass parity, exact provenance depth, strict diagnostics, limits, and every allocation failure. Retain thirty-first-and-later configuration, raw caller callables, another-parent identities, callable arguments and `@content` transport, transitive modules, `@forward`, legacy imports, `meta.load-css()`, broader resolution, and native graduation as separate measured slices. Provider removal and the package-level zero-dependency cutover remain gated on native graduation rather than deleting the exact development oracles early. Independently, `BENCH-007` still requires a dedicated non-emulated Linux x64 runner with labels `[self-hosted, linux, x64, zigcss-benchmark-v1]`; its absence must not pause eligible native frontend work.
- Runtime control: model identity, single-agent execution, isolated-worktree confinement, automatic supervisor-owned green-checkpoint pushes, the owner-bounded post-`NATIVE-009` GitHub-prerelease/npm-`next` authority, and every native graduation/release interlock remain unchanged.

## Prior current-work evidence (twenty-eighth-module callable configuration checkpoint)

- Milestone: Milestone 10 self-contained native stylesheet frontends, in parallel with Milestone 8 controlled benchmark evidence
- Work package: `NSASS-011` native Sass evaluation
- State: `IN_PROGRESS`
- Active phase: the two-hundred-sixty-fourth native Sass semantic checkpoint, `NSASS-011-LOCAL-USE-TWENTY-EIGHTH-MODULE-REEXPORTED-CALLABLE-CONFIGURATION-OWNERSHIP-FOUNDATION`, is `VERIFIED` at local implementation checkpoint `4a7034893a38d9e4fd550696bae674eb17635ce8`. Fresh Dart Sass 1.101.0 in-memory importer probes establish that function and mixin references retain exact behavior through twenty-eighth- and twenty-ninth-module configuration in SCSS and indented Sass: invocation and application inside and after the configured modules, original-owner live state, identical compressed CSS, and zero warnings all match. The additive native fixture retains the prior twenty-seventh-module regression and separately admits only the scheduled twenty-eighth-module hop across direct and enumerated public variables, nested aggregates, module-returned values, invocation/application inside and after the twenty-eighth module, default/custom canonical aliases, original-owner identity and state, deterministic dependency order, maps, and SCSS/indented-Sass parity. Native Zig centralizes the boundary as a maximum configurable re-export depth of twenty-six: the original owner identity enters at depth zero; the middle through twenty-seventh re-exports enter at depths one through twenty-six and may configure the third through twenty-eighth modules respectively; and the twenty-eighth re-export enters at depth twenty-seven and remains rejected when configuring a twenty-ninth module. Every accepted aggregate is deep-owned by its destination while the stable packed original owner/callable identity remains unchanged, so calls, mixin application, inspection, content acceptance, aliases, equality, and mutable state cross the new hop without retaining evaluator-arena pointers. Callable arguments and cross-module `@content` retain their stricter depth-zero/owner-only remappers. The test-first focused Debug gate stopped at 632/635 with exactly three failures: the twenty-eighth-module positive and exhaustive-allocation paths reached the former maximum-depth-twenty-five rejection, while the advanced no-partial diagnostic still pointed to `twenty-seventh.$exported` rather than the new twenty-ninth-module boundary. Exact twenty-ninth-module failure now owns one diagnostic at `twenty-eighth.$exported`, leaves generated position zero, and emits no CSS; exhaustive-allocation coverage exercises the accepted twenty-eighth-module path and every allocation failure passes. Final focused native Debug/ReleaseSafe each pass 23/23 steps with 635/635 tests; full compiler Debug/ReleaseSafe each pass 60/60 steps with 1130/1130 tests; and evaluator-test-root plus preprocessor-root no-emission cross-compiles pass for Windows x64 GNU and Linux ARM64 GNU in both modes. Native contract 12/12 plus check adds only `local-use-twenty-eighth-module-reexported-callable-configuration-ownership-foundation` while retaining private native Sass rows, production unreachability, zero target package/optional/runtime dependencies, and the closed release interlock. Complete provider/reference/product matrices pass 153/153; preprocessor package policy passes 6/6 plus check over 22 runtime sources, 28 archive files, 86 locked dependencies, and 5 targets; capability-status policy passes 7/7 plus check; autonomous-runner selftest passes 59/59; and repository-wide Zig formatting, changed JavaScript syntax, all 36 tracked JSON files, and diff checks pass. The bounded sibling search confirms that twenty-ninth-and-later native configuration, raw caller callables, another-parent identities, callable arguments and `@content` transport, transitive modules, `@forward`, legacy imports, `meta.load-css()` execution, broader resolution, production routing, public capability claims, native graduation, version, release interlock, tag, artifact/package publication, deployment, and every external system remain unchanged.
- Quality closure: `QUAL-001` is verified at `3eced86`; repository-wide Zig formatting debt is zero and the final local validation matrix is green
- Next eligible package: admit only the next twenty-ninth-module hop already measured against Dart Sass 1.101.0 as the smallest durable `NSASS-011-LOCAL-USE-TWENTY-NINTH-MODULE-REEXPORTED-CALLABLE-CONFIGURATION-OWNERSHIP-FOUNDATION`, with direct/enumerated and nested values, invocation/application inside and after the twenty-ninth module, canonical/default/custom aliases, live owner state, SCSS/indented-Sass parity, exact provenance depth, strict diagnostics, limits, and every allocation failure. Retain thirtieth-and-later configuration, raw caller callables, another-parent identities, callable arguments and `@content` transport, transitive modules, `@forward`, legacy imports, `meta.load-css()`, broader resolution, and native graduation as separate measured slices. Provider removal and the package-level zero-dependency cutover remain gated on native graduation rather than deleting the exact development oracles early. Independently, `BENCH-007` still requires a dedicated non-emulated Linux x64 runner with labels `[self-hosted, linux, x64, zigcss-benchmark-v1]`; its absence must not pause eligible native frontend work.
- Runtime control: model identity, single-agent execution, isolated-worktree confinement, automatic supervisor-owned green-checkpoint pushes, the owner-bounded post-`NATIVE-009` GitHub-prerelease/npm-`next` authority, and every native graduation/release interlock remain unchanged.

## Prior current-work evidence (twenty-seventh-module callable configuration checkpoint)

- Milestone: Milestone 10 self-contained native stylesheet frontends, in parallel with Milestone 8 controlled benchmark evidence
- Work package: `NSASS-011` native Sass evaluation
- State: `IN_PROGRESS`
- Active phase: the two-hundred-sixty-third native Sass semantic checkpoint, `NSASS-011-LOCAL-USE-TWENTY-SEVENTH-MODULE-REEXPORTED-CALLABLE-CONFIGURATION-OWNERSHIP-FOUNDATION`, is `VERIFIED` at local implementation checkpoint `3d013790ecec5306dd7107e002a64b55b7d12759`. Fresh Dart Sass 1.101.0 in-memory importer probes establish that function and mixin references retain exact behavior through twenty-seventh- and twenty-eighth-module configuration in SCSS and indented Sass: invocation and application inside and after the configured modules, original-owner live state, identical compressed CSS, and zero warnings all match. The additive native fixture retains the prior twenty-sixth-module regression and separately admits only the scheduled twenty-seventh-module hop across direct and enumerated public variables, nested aggregates, module-returned values, invocation/application inside and after the twenty-seventh module, default/custom canonical aliases, original-owner identity and state, deterministic dependency order, maps, and SCSS/indented-Sass parity. Native Zig centralizes the boundary as a maximum configurable re-export depth of twenty-five: the original owner identity enters at depth zero; the middle through twenty-sixth re-exports enter at depths one through twenty-five and may configure the third through twenty-seventh modules respectively; and the twenty-seventh re-export enters at depth twenty-six and remains rejected when configuring a twenty-eighth module. Every accepted aggregate is deep-owned by its destination while the stable packed original owner/callable identity remains unchanged, so calls, mixin application, inspection, content acceptance, aliases, equality, and mutable state cross the new hop without retaining evaluator-arena pointers. Callable arguments and cross-module `@content` retain their stricter depth-zero/owner-only remappers. The test-first focused Debug gate stopped at 631/634 with exactly three failures: the twenty-seventh-module positive and exhaustive-allocation paths reached the former maximum-depth-twenty-four rejection, while the advanced no-partial diagnostic still pointed to `twenty-sixth.$exported` rather than the new twenty-eighth-module boundary. Exact twenty-eighth-module failure now owns one diagnostic at `twenty-seventh.$exported`, leaves generated position zero, and emits no CSS; exhaustive-allocation coverage exercises the accepted twenty-seventh-module path and every allocation failure passes. Final focused native Debug/ReleaseSafe each pass 23/23 steps with 634/634 tests; full compiler Debug/ReleaseSafe each pass 60/60 steps with 1129/1129 tests; and evaluator-test-root plus preprocessor-root no-emission cross-compiles pass for Windows x64 GNU and Linux ARM64 GNU in both modes. Native contract 12/12 plus check adds only `local-use-twenty-seventh-module-reexported-callable-configuration-ownership-foundation` while retaining private native Sass rows, production unreachability, zero target package/optional/runtime dependencies, and the closed release interlock. Complete provider/reference/product matrices pass 153/153; preprocessor package policy passes 6/6 plus check over 22 runtime sources, 28 archive files, 86 locked dependencies, and 5 targets; capability-status policy passes 7/7 plus check; autonomous-runner selftest passes 59/59; and repository-wide Zig formatting, changed JavaScript syntax, all 36 tracked JSON files, and diff checks pass. The bounded sibling search confirms that twenty-eighth-and-later native configuration, raw caller callables, another-parent identities, callable arguments and `@content` transport, transitive modules, `@forward`, legacy imports, `meta.load-css()` execution, broader resolution, production routing, public capability claims, native graduation, version, release interlock, tag, artifact/package publication, deployment, and every external system remain unchanged.
- Quality closure: `QUAL-001` is verified at `3eced86`; repository-wide Zig formatting debt is zero and the final local validation matrix is green
- Next eligible package: admit only the next twenty-eighth-module hop already measured against Dart Sass 1.101.0 as the smallest durable `NSASS-011-LOCAL-USE-TWENTY-EIGHTH-MODULE-REEXPORTED-CALLABLE-CONFIGURATION-OWNERSHIP-FOUNDATION`, with direct/enumerated and nested values, invocation/application inside and after the twenty-eighth module, canonical/default/custom aliases, live owner state, SCSS/indented-Sass parity, exact provenance depth, strict diagnostics, limits, and every allocation failure. Retain twenty-ninth-and-later configuration, raw caller callables, another-parent identities, callable arguments and `@content` transport, transitive modules, `@forward`, legacy imports, `meta.load-css()`, broader resolution, and native graduation as separate measured slices. Provider removal and the package-level zero-dependency cutover remain gated on native graduation rather than deleting the exact development oracles early. Independently, `BENCH-007` still requires a dedicated non-emulated Linux x64 runner with labels `[self-hosted, linux, x64, zigcss-benchmark-v1]`; its absence must not pause eligible native frontend work.
- Runtime control: model identity, single-agent execution, isolated-worktree confinement, automatic supervisor-owned green-checkpoint pushes, the owner-bounded post-`NATIVE-009` GitHub-prerelease/npm-`next` authority, and every native graduation/release interlock remain unchanged.

## Prior current-work evidence (twenty-sixth-module callable configuration checkpoint)

- Milestone: Milestone 10 self-contained native stylesheet frontends, in parallel with Milestone 8 controlled benchmark evidence
- Work package: `NSASS-011` native Sass evaluation
- State: `IN_PROGRESS`
- Active phase: the two-hundred-sixty-second native Sass semantic checkpoint, `NSASS-011-LOCAL-USE-TWENTY-SIXTH-MODULE-REEXPORTED-CALLABLE-CONFIGURATION-OWNERSHIP-FOUNDATION`, is `VERIFIED` at local implementation checkpoint `2322aec3622bdf47294e20dc5b30679245855421`. Fresh Dart Sass 1.101.0 in-memory importer probes establish that function and mixin references retain exact behavior through twenty-sixth- and twenty-seventh-module configuration in SCSS and indented Sass: invocation and application inside and after the configured modules, original-owner live state, identical compressed CSS, and zero warnings all match. The additive native fixture retains the prior twenty-fifth-module regression and separately admits only the scheduled twenty-sixth-module hop across direct and enumerated public variables, nested aggregates, module-returned values, invocation/application inside and after the twenty-sixth module, default/custom canonical aliases, original-owner identity and state, deterministic dependency order, maps, and SCSS/indented-Sass parity. Native Zig centralizes the boundary as a maximum configurable re-export depth of twenty-four: the original owner identity enters at depth zero; the middle through twenty-fifth re-exports enter at depths one through twenty-four and may configure the third through twenty-sixth modules respectively; and the twenty-sixth re-export enters at depth twenty-five and remains rejected when configuring a twenty-seventh module. Every accepted aggregate is deep-owned by its destination while the stable packed original owner/callable identity remains unchanged, so calls, mixin application, inspection, content acceptance, aliases, equality, and mutable state cross the new hop without retaining evaluator-arena pointers. Callable arguments and cross-module `@content` retain their stricter depth-zero/owner-only remappers. The test-first focused Debug gate stopped at 630/633 with exactly three failures: the twenty-sixth-module positive and exhaustive-allocation paths reached the former maximum-depth-twenty-three rejection, while the advanced no-partial diagnostic still pointed to `twenty-fifth.$exported` rather than the new twenty-seventh-module boundary. Exact twenty-seventh-module failure now owns one diagnostic at `twenty-sixth.$exported`, leaves generated position zero, and emits no CSS; exhaustive-allocation coverage exercises the accepted twenty-sixth-module path and every allocation failure passes. Final focused native Debug/ReleaseSafe each pass 23/23 steps with 633/633 tests; full compiler Debug/ReleaseSafe each pass 60/60 steps with 1128/1128 tests; and evaluator-test-root plus preprocessor-root no-emission cross-compiles pass for Windows x64 GNU and Linux ARM64 GNU in both modes. Native contract 12/12 plus check adds only `local-use-twenty-sixth-module-reexported-callable-configuration-ownership-foundation` while retaining private native Sass rows, production unreachability, zero target package/optional/runtime dependencies, and the closed release interlock. Complete provider/reference/product matrices pass 153/153; preprocessor package policy passes 6/6 plus check over 22 runtime sources, 28 archive files, 86 locked dependencies, and 5 targets; capability-status policy passes 7/7 plus check; autonomous-runner selftest passes 59/59; and repository-wide Zig formatting, changed JavaScript syntax, all 36 tracked JSON files, and diff checks pass. The bounded sibling search confirms that twenty-seventh-and-later native configuration, raw caller callables, another-parent identities, callable arguments and `@content` transport, transitive modules, `@forward`, legacy imports, `meta.load-css()` execution, broader resolution, production routing, public capability claims, native graduation, version, release interlock, tag, artifact/package publication, deployment, and every external system remain unchanged.
- Quality closure: `QUAL-001` is verified at `3eced86`; repository-wide Zig formatting debt is zero and the final local validation matrix is green
- Next eligible package: admit only the next twenty-seventh-module hop already measured against Dart Sass 1.101.0 as the smallest durable `NSASS-011-LOCAL-USE-TWENTY-SEVENTH-MODULE-REEXPORTED-CALLABLE-CONFIGURATION-OWNERSHIP-FOUNDATION`, with direct/enumerated and nested values, invocation/application inside and after the twenty-seventh module, canonical/default/custom aliases, live owner state, SCSS/indented-Sass parity, exact provenance depth, strict diagnostics, limits, and every allocation failure. Retain twenty-eighth-and-later configuration, raw caller callables, another-parent identities, callable arguments and `@content` transport, transitive modules, `@forward`, legacy imports, `meta.load-css()`, broader resolution, and native graduation as separate measured slices. Provider removal and the package-level zero-dependency cutover remain gated on native graduation rather than deleting the exact development oracles early. Independently, `BENCH-007` still requires a dedicated non-emulated Linux x64 runner with labels `[self-hosted, linux, x64, zigcss-benchmark-v1]`; its absence must not pause eligible native frontend work.
- Runtime control: model identity, single-agent execution, isolated-worktree confinement, automatic supervisor-owned green-checkpoint pushes, the owner-bounded post-`NATIVE-009` GitHub-prerelease/npm-`next` authority, and every native graduation/release interlock remain unchanged.

## Prior current-work evidence (twenty-fifth-module callable configuration checkpoint)

- Milestone: Milestone 10 self-contained native stylesheet frontends, in parallel with Milestone 8 controlled benchmark evidence
- Work package: `NSASS-011` native Sass evaluation
- State: `IN_PROGRESS`
- Active phase: the two-hundred-sixty-first native Sass semantic checkpoint, `NSASS-011-LOCAL-USE-TWENTY-FIFTH-MODULE-REEXPORTED-CALLABLE-CONFIGURATION-OWNERSHIP-FOUNDATION`, is `VERIFIED` at local implementation checkpoint `ff92cdcef768b77cbc186e09b6e0eabbabe6e475`. Fresh Dart Sass 1.101.0 in-memory importer probes establish that function and mixin references retain exact behavior through twenty-fifth- and twenty-sixth-module configuration in SCSS and indented Sass: invocation and application inside and after the configured modules, original-owner live state, identical compressed CSS, and zero warnings all match. The additive native fixture retains the prior twenty-fourth-module regression and separately admits only the scheduled twenty-fifth-module hop across direct and enumerated public variables, nested aggregates, module-returned values, invocation/application inside and after the twenty-fifth module, default/custom canonical aliases, original-owner identity and state, deterministic dependency order, maps, and SCSS/indented-Sass parity. Native Zig centralizes the boundary as a maximum configurable re-export depth of twenty-three: the original owner identity enters at depth zero; the middle through twenty-fourth re-exports enter at depths one through twenty-three and may configure the third through twenty-fifth modules respectively; and the twenty-fifth re-export enters at depth twenty-four and remains rejected when configuring a twenty-sixth module. Every accepted aggregate is deep-owned by its destination while the stable packed original owner/callable identity remains unchanged, so calls, mixin application, inspection, content acceptance, aliases, equality, and mutable state cross the new hop without retaining evaluator-arena pointers. Callable arguments and cross-module `@content` retain their stricter depth-zero/owner-only remappers. The test-first focused Debug gate stopped at 629/632 with exactly three failures: the twenty-fifth-module positive and exhaustive-allocation paths reached the former maximum-depth-twenty-two rejection, while the advanced no-partial diagnostic still pointed to `twenty-fourth.$exported` rather than the new twenty-sixth-module boundary. Exact twenty-sixth-module failure now owns one diagnostic at `twenty-fifth.$exported`, leaves generated position zero, and emits no CSS; exhaustive-allocation coverage exercises the accepted twenty-fifth-module path and every allocation failure passes. Final focused native Debug/ReleaseSafe each pass 23/23 steps with 632/632 tests; full compiler Debug/ReleaseSafe each pass 60/60 steps with 1127/1127 tests; and evaluator-test-root plus preprocessor-root no-emission cross-compiles pass for Windows x64 GNU and Linux ARM64 GNU in both modes. Native contract 12/12 plus check adds only `local-use-twenty-fifth-module-reexported-callable-configuration-ownership-foundation` while retaining private native Sass rows, production unreachability, zero target package/optional/runtime dependencies, and the closed release interlock. Complete provider/reference/product matrices pass 153/153; preprocessor package policy passes 6/6 plus check over 22 runtime sources, 28 archive files, 86 locked dependencies, and 5 targets; capability-status policy passes 7/7 plus check; autonomous-runner selftest passes 59/59; and repository-wide Zig formatting, changed JavaScript syntax, all 36 tracked JSON files, and diff checks pass. The bounded sibling search confirms that twenty-sixth-and-later native configuration, raw caller callables, another-parent identities, callable arguments and `@content` transport, transitive modules, `@forward`, legacy imports, `meta.load-css()` execution, broader resolution, production routing, public capability claims, native graduation, version, release interlock, tag, artifact/package publication, deployment, and every external system remain unchanged.
- Quality closure: `QUAL-001` is verified at `3eced86`; repository-wide Zig formatting debt is zero and the final local validation matrix is green
- Next eligible package: admit only the next twenty-sixth-module hop already measured against Dart Sass 1.101.0 as the smallest durable `NSASS-011-LOCAL-USE-TWENTY-SIXTH-MODULE-REEXPORTED-CALLABLE-CONFIGURATION-OWNERSHIP-FOUNDATION`, with direct/enumerated and nested values, invocation/application inside and after the twenty-sixth module, canonical/default/custom aliases, live owner state, SCSS/indented-Sass parity, exact provenance depth, strict diagnostics, limits, and every allocation failure. Retain twenty-seventh-and-later configuration, raw caller callables, another-parent identities, callable arguments and `@content` transport, transitive modules, `@forward`, legacy imports, `meta.load-css()`, broader resolution, and native graduation as separate measured slices. Provider removal and the package-level zero-dependency cutover remain gated on native graduation rather than deleting the exact development oracles early. Independently, `BENCH-007` still requires a dedicated non-emulated Linux x64 runner with labels `[self-hosted, linux, x64, zigcss-benchmark-v1]`; its absence must not pause eligible native frontend work.
- Runtime control: model identity, single-agent execution, isolated-worktree confinement, automatic supervisor-owned green-checkpoint pushes, the owner-bounded post-`NATIVE-009` GitHub-prerelease/npm-`next` authority, and every native graduation/release interlock remain unchanged.

## Prior current-work evidence (twenty-fourth-module callable configuration checkpoint)

- Milestone: Milestone 10 self-contained native stylesheet frontends, in parallel with Milestone 8 controlled benchmark evidence
- Work package: `NSASS-011` native Sass evaluation
- State: `IN_PROGRESS`
- Active phase: the two-hundred-sixtieth native Sass semantic checkpoint, `NSASS-011-LOCAL-USE-TWENTY-FOURTH-MODULE-REEXPORTED-CALLABLE-CONFIGURATION-OWNERSHIP-FOUNDATION`, is `VERIFIED` at local implementation checkpoint `eb04bc207e5027ad2e58f5405b3df08a20ca1ead`. Fresh Dart Sass 1.101.0 in-memory importer probes establish that function and mixin references retain exact behavior through twenty-fourth- and twenty-fifth-module configuration in SCSS and indented Sass: invocation and application inside and after the configured modules, original-owner live state, identical compressed CSS, and zero warnings all match. The additive native fixture retains the prior twenty-third-module regression and separately admits only the scheduled twenty-fourth-module hop across direct and enumerated public variables, nested aggregates, module-returned values, invocation/application inside and after the twenty-fourth module, default/custom canonical aliases, original-owner identity and state, deterministic dependency order, maps, and SCSS/indented-Sass parity. Native Zig centralizes the boundary as a maximum configurable re-export depth of twenty-two: the original owner identity enters at depth zero; the middle through twenty-third re-exports enter at depths one through twenty-two and may configure the third through twenty-fourth modules respectively; and the twenty-fourth re-export enters at depth twenty-three and remains rejected when configuring a twenty-fifth module. Every accepted aggregate is deep-owned by its destination while the stable packed original owner/callable identity remains unchanged, so calls, mixin application, inspection, content acceptance, aliases, equality, and mutable state cross the new hop without retaining evaluator-arena pointers. Callable arguments and cross-module `@content` retain their stricter depth-zero/owner-only remappers. The test-first focused Debug gate stopped at 628/631 with exactly three failures: the twenty-fourth-module positive and exhaustive-allocation paths reached the former maximum-depth-twenty-one rejection, while the advanced no-partial diagnostic still pointed to `twenty-third.$exported` rather than the new twenty-fifth-module boundary. Exact twenty-fifth-module failure now owns one diagnostic at `twenty-fourth.$exported`, leaves generated position zero, and emits no CSS; exhaustive-allocation coverage exercises the accepted twenty-fourth-module path and every allocation failure passes. Final focused native Debug/ReleaseSafe each pass 23/23 steps with 631/631 tests; full compiler Debug/ReleaseSafe each pass 60/60 steps with 1126/1126 tests; and evaluator-test-root plus preprocessor-root no-emission cross-compiles pass for Windows x64 GNU and Linux ARM64 GNU in both modes. Native contract 12/12 plus check adds only `local-use-twenty-fourth-module-reexported-callable-configuration-ownership-foundation` while retaining private native Sass rows, production unreachability, zero target package/optional/runtime dependencies, and the closed release interlock. Complete provider/reference/product matrices pass 153/153; preprocessor package policy passes 6/6 plus check over 22 runtime sources, 28 archive files, 86 locked dependencies, and 5 targets; capability-status policy passes 7/7 plus check; autonomous-runner selftest passes 59/59; and repository-wide Zig formatting, changed JavaScript syntax, all 36 tracked JSON files, and diff checks pass. The bounded sibling search confirms that twenty-fifth-and-later native configuration, raw caller callables, another-parent identities, callable arguments and `@content` transport, transitive modules, `@forward`, legacy imports, `meta.load-css()` execution, broader resolution, production routing, public capability claims, native graduation, version, release interlock, tag, artifact/package publication, deployment, and every external system remain unchanged.
- Quality closure: `QUAL-001` is verified at `3eced86`; repository-wide Zig formatting debt is zero and the final local validation matrix is green
- Next eligible package: admit only the next twenty-fifth-module hop already measured against Dart Sass 1.101.0 as the smallest durable `NSASS-011-LOCAL-USE-TWENTY-FIFTH-MODULE-REEXPORTED-CALLABLE-CONFIGURATION-OWNERSHIP-FOUNDATION`, with direct/enumerated and nested values, invocation/application inside and after the twenty-fifth module, canonical/default/custom aliases, live owner state, SCSS/indented-Sass parity, exact provenance depth, strict diagnostics, limits, and every allocation failure. Retain twenty-sixth-and-later configuration, raw caller callables, another-parent identities, callable arguments and `@content` transport, transitive modules, `@forward`, legacy imports, `meta.load-css()`, broader resolution, and native graduation as separate measured slices. Provider removal and the package-level zero-dependency cutover remain gated on native graduation rather than deleting the exact development oracles early. Independently, `BENCH-007` still requires a dedicated non-emulated Linux x64 runner with labels `[self-hosted, linux, x64, zigcss-benchmark-v1]`; its absence must not pause eligible native frontend work.
- Runtime control: model identity, single-agent execution, isolated-worktree confinement, automatic supervisor-owned green-checkpoint pushes, the owner-bounded post-`NATIVE-009` GitHub-prerelease/npm-`next` authority, and every native graduation/release interlock remain unchanged.

## Prior current-work evidence (twenty-third-module callable configuration checkpoint)

- Milestone: Milestone 10 self-contained native stylesheet frontends, in parallel with Milestone 8 controlled benchmark evidence
- Work package: `NSASS-011` native Sass evaluation
- State: `IN_PROGRESS`
- Active phase: the two-hundred-fifty-ninth native Sass semantic checkpoint, `NSASS-011-LOCAL-USE-TWENTY-THIRD-MODULE-REEXPORTED-CALLABLE-CONFIGURATION-OWNERSHIP-FOUNDATION`, is `VERIFIED` at local implementation checkpoint `fe4588b07ccc8c7915d3025db221e69bd8dd3007`. Fresh Dart Sass 1.101.0 in-memory importer probes establish that function and mixin references retain exact behavior through twenty-third- and twenty-fourth-module configuration in SCSS and indented Sass: invocation and application inside and after the configured modules, original-owner live state, identical compressed CSS, and zero warnings all match. The additive native fixture retains the prior twenty-second-module regression and separately admits only the scheduled twenty-third-module hop across direct and enumerated public variables, nested aggregates, module-returned values, invocation/application inside and after the twenty-third module, default/custom canonical aliases, original-owner identity and state, deterministic dependency order, maps, and SCSS/indented-Sass parity. Native Zig centralizes the boundary as a maximum configurable re-export depth of twenty-one: the original owner identity enters at depth zero; the middle through twenty-second re-exports enter at depths one through twenty-one and may configure the third through twenty-third modules respectively; and the twenty-third re-export enters at depth twenty-two and remains rejected when configuring a twenty-fourth module. Every accepted aggregate is deep-owned by its destination while the stable packed original owner/callable identity remains unchanged, so calls, mixin application, inspection, content acceptance, aliases, equality, and mutable state cross the new hop without retaining evaluator-arena pointers. Callable arguments and cross-module `@content` retain their stricter depth-zero/owner-only remappers. The test-first focused Debug gate stopped at 627/630 with exactly three failures: the twenty-third-module positive and exhaustive-allocation paths reached the former maximum-depth-twenty rejection, while the advanced no-partial diagnostic still pointed to `twenty-second.$exported` rather than the new twenty-fourth-module boundary. Exact twenty-fourth-module failure now owns one diagnostic at `twenty-third.$exported`, leaves generated position zero, and emits no CSS; exhaustive-allocation coverage exercises the accepted twenty-third-module path and every allocation failure passes. Final focused native Debug/ReleaseSafe each pass 23/23 steps with 630/630 tests; full compiler Debug/ReleaseSafe each pass 60/60 steps with 1125/1125 tests; and evaluator-test-root plus preprocessor-root no-emission cross-compiles pass for Windows x64 GNU and Linux ARM64 GNU in both modes. Native contract 12/12 plus check adds only `local-use-twenty-third-module-reexported-callable-configuration-ownership-foundation` while retaining private native Sass rows, production unreachability, zero target package/optional/runtime dependencies, and the closed release interlock. Complete provider/reference/product matrices pass 153/153; preprocessor package policy passes 6/6 plus check over 22 runtime sources, 28 archive files, 86 locked dependencies, and 5 targets; capability-status policy passes 7/7 plus check; autonomous-runner selftest passes 59/59; and repository-wide Zig formatting, changed JavaScript syntax, all 36 tracked JSON files, and diff checks pass. The bounded sibling search confirms that twenty-fourth-and-later native configuration, raw caller callables, another-parent identities, callable arguments and `@content` transport, transitive modules, `@forward`, legacy imports, `meta.load-css()` execution, broader resolution, production routing, public capability claims, native graduation, version, release interlock, tag, artifact/package publication, deployment, and every external system remain unchanged.
- Quality closure: `QUAL-001` is verified at `3eced86`; repository-wide Zig formatting debt is zero and the final local validation matrix is green
- Next eligible package: admit only the next twenty-fourth-module hop already measured against Dart Sass 1.101.0 as the smallest durable `NSASS-011-LOCAL-USE-TWENTY-FOURTH-MODULE-REEXPORTED-CALLABLE-CONFIGURATION-OWNERSHIP-FOUNDATION`, with direct/enumerated and nested values, invocation/application inside and after the twenty-fourth module, canonical/default/custom aliases, live owner state, SCSS/indented-Sass parity, exact provenance depth, strict diagnostics, limits, and every allocation failure. Retain twenty-fifth-and-later configuration, raw caller callables, another-parent identities, callable arguments and `@content` transport, transitive modules, `@forward`, legacy imports, `meta.load-css()`, broader resolution, and native graduation as separate measured slices. Provider removal and the package-level zero-dependency cutover remain gated on native graduation rather than deleting the exact development oracles early. Independently, `BENCH-007` still requires a dedicated non-emulated Linux x64 runner with labels `[self-hosted, linux, x64, zigcss-benchmark-v1]`; its absence must not pause eligible native frontend work.
- Runtime control: model identity, single-agent execution, isolated-worktree confinement, automatic supervisor-owned green-checkpoint pushes, the owner-bounded post-`NATIVE-009` GitHub-prerelease/npm-`next` authority, and every native graduation/release interlock remain unchanged.

## Prior current-work evidence (twenty-second-module callable configuration checkpoint)

- Milestone: Milestone 10 self-contained native stylesheet frontends, in parallel with Milestone 8 controlled benchmark evidence
- Work package: `NSASS-011` native Sass evaluation
- State: `IN_PROGRESS`
- Active phase: the two-hundred-fifty-eighth native Sass semantic checkpoint, `NSASS-011-LOCAL-USE-TWENTY-SECOND-MODULE-REEXPORTED-CALLABLE-CONFIGURATION-OWNERSHIP-FOUNDATION`, is `VERIFIED` at local implementation checkpoint `df7d1066d4042878ad2b2b10aba5becf790735ff`. Fresh Dart Sass 1.101.0 in-memory importer probes establish that function and mixin references retain exact behavior through twenty-second- and twenty-third-module configuration in SCSS and indented Sass: invocation and application inside and after the configured modules, original-owner live state, identical compressed CSS, and zero warnings all match. The additive native fixture retains the prior twenty-first-module regression and separately admits only the scheduled twenty-second-module hop across direct and enumerated public variables, nested aggregates, module-returned values, invocation/application inside and after the twenty-second module, default/custom canonical aliases, original-owner identity and state, deterministic dependency order, maps, and SCSS/indented-Sass parity. Native Zig centralizes the boundary as a maximum configurable re-export depth of twenty: the original owner identity enters at depth zero; the middle through twenty-first re-exports enter at depths one through twenty and may configure the third through twenty-second modules respectively; and the twenty-second re-export enters at depth twenty-one and remains rejected when configuring a twenty-third module. Every accepted aggregate is deep-owned by its destination while the stable packed original owner/callable identity remains unchanged, so calls, mixin application, inspection, content acceptance, aliases, equality, and mutable state cross the new hop without retaining evaluator-arena pointers. Callable arguments and cross-module `@content` retain their stricter depth-zero/owner-only remappers. The test-first focused Debug gate stopped at 626/629 with exactly three failures: the twenty-second-module positive and exhaustive-allocation paths reached the former maximum-depth-nineteen rejection, while the advanced no-partial diagnostic still pointed to `twenty-first.$exported` rather than the new twenty-third-module boundary. Exact twenty-third-module failure now owns one diagnostic at `twenty-second.$exported`, leaves generated position zero, and emits no CSS; exhaustive-allocation coverage exercises the accepted twenty-second-module path and every allocation failure passes. Final focused native Debug/ReleaseSafe each pass 23/23 steps with 629/629 tests; full compiler Debug/ReleaseSafe each pass 60/60 steps with 1124/1124 tests; and evaluator-test-root plus preprocessor-root no-emission cross-compiles pass for Windows x64 GNU and Linux ARM64 GNU in both modes. Native contract 12/12 plus check adds only `local-use-twenty-second-module-reexported-callable-configuration-ownership-foundation` while retaining private native Sass rows, production unreachability, zero target package/optional/runtime dependencies, and the closed release interlock. Complete provider/reference/product matrices pass 153/153; preprocessor package policy passes 6/6 plus check over 22 runtime sources, 28 archive files, 86 locked dependencies, and 5 targets; capability-status policy passes 7/7 plus check; autonomous-runner selftest passes 59/59; and repository-wide Zig formatting, changed JavaScript syntax, all 36 tracked JSON files, and diff checks pass. The bounded sibling search confirms that twenty-third-and-later native configuration, raw caller callables, another-parent identities, callable arguments and `@content` transport, transitive modules, `@forward`, legacy imports, `meta.load-css()` execution, broader resolution, production routing, public capability claims, native graduation, version, release interlock, tag, artifact/package publication, deployment, and every external system remain unchanged.
- Quality closure: `QUAL-001` is verified at `3eced86`; repository-wide Zig formatting debt is zero and the final local validation matrix is green
- Next eligible package: admit only the next twenty-third-module hop already measured against Dart Sass 1.101.0 as the smallest durable `NSASS-011-LOCAL-USE-TWENTY-THIRD-MODULE-REEXPORTED-CALLABLE-CONFIGURATION-OWNERSHIP-FOUNDATION`, with direct/enumerated and nested values, invocation/application inside and after the twenty-third module, canonical/default/custom aliases, live owner state, SCSS/indented-Sass parity, exact provenance depth, strict diagnostics, limits, and every allocation failure. Retain twenty-fourth-and-later configuration, raw caller callables, another-parent identities, callable arguments and `@content` transport, transitive modules, `@forward`, legacy imports, `meta.load-css()`, broader resolution, and native graduation as separate measured slices. Provider removal and the package-level zero-dependency cutover remain gated on native graduation rather than deleting the exact development oracles early. Independently, `BENCH-007` still requires a dedicated non-emulated Linux x64 runner with labels `[self-hosted, linux, x64, zigcss-benchmark-v1]`; its absence must not pause eligible native frontend work.
- Runtime control: model identity, single-agent execution, isolated-worktree confinement, automatic supervisor-owned green-checkpoint pushes, the owner-bounded post-`NATIVE-009` GitHub-prerelease/npm-`next` authority, and every native graduation/release interlock remain unchanged.

## Prior current-work evidence (twenty-first-module callable configuration checkpoint)

- Milestone: Milestone 10 self-contained native stylesheet frontends, in parallel with Milestone 8 controlled benchmark evidence
- Work package: `NSASS-011` native Sass evaluation
- State: `IN_PROGRESS`
- Active phase: the two-hundred-fifty-seventh native Sass semantic checkpoint, `NSASS-011-LOCAL-USE-TWENTY-FIRST-MODULE-REEXPORTED-CALLABLE-CONFIGURATION-OWNERSHIP-FOUNDATION`, is `VERIFIED` at local implementation checkpoint `c9e51c083d16021497d5d8b5401237bf551dcfb3`. Fresh Dart Sass 1.101.0 in-memory importer probes establish that function and mixin references retain exact behavior through twenty-first- and twenty-second-module configuration in SCSS and indented Sass: invocation and application inside and after the configured modules, original-owner live state, identical compressed CSS, and zero warnings all match. The additive native fixture retains the prior twentieth-module regression and separately admits only the scheduled twenty-first-module hop across direct and enumerated public variables, nested aggregates, module-returned values, invocation/application inside and after the twenty-first module, default/custom canonical aliases, original-owner identity and state, deterministic dependency order, maps, and SCSS/indented-Sass parity. Native Zig centralizes the boundary as a maximum configurable re-export depth of nineteen: the original owner identity enters at depth zero; the middle through twentieth re-exports enter at depths one through nineteen and may configure the third through twenty-first modules respectively; and the twenty-first re-export enters at depth twenty and remains rejected when configuring a twenty-second module. Every accepted aggregate is deep-owned by its destination while the stable packed original owner/callable identity remains unchanged, so calls, mixin application, inspection, content acceptance, aliases, equality, and mutable state cross the new hop without retaining evaluator-arena pointers. Callable arguments and cross-module `@content` retain their stricter depth-zero/owner-only remappers. The test-first focused Debug gate stopped at 625/628 with exactly three failures: the twenty-first-module positive and exhaustive-allocation paths reached the former maximum-depth-eighteen rejection, while the advanced no-partial diagnostic still pointed to `twentieth.$exported` rather than the new twenty-second-module boundary. Exact twenty-second-module failure now owns one diagnostic at `twenty-first.$exported`, leaves generated position zero, and emits no CSS; exhaustive-allocation coverage exercises the accepted twenty-first-module path and every allocation failure passes. Final focused native Debug/ReleaseSafe each pass 23/23 steps with 628/628 tests; full compiler Debug/ReleaseSafe each pass 60/60 steps with 1123/1123 tests; and evaluator-test-root plus preprocessor-root no-emission cross-compiles pass for Windows x64 GNU and Linux ARM64 GNU in both modes. Native contract 12/12 plus check adds only `local-use-twenty-first-module-reexported-callable-configuration-ownership-foundation` while retaining private native Sass rows, production unreachability, zero target package/optional/runtime dependencies, and the closed release interlock. Complete provider/reference/product matrices pass 153/153; preprocessor package policy passes 6/6 plus check over 22 runtime sources, 28 archive files, 86 locked dependencies, and 5 targets; capability-status policy passes 7/7 plus check; autonomous-runner selftest passes 59/59; and repository-wide Zig formatting, changed JavaScript syntax, all 36 tracked JSON files, and diff checks pass. The bounded sibling search confirms that twenty-second-and-later native configuration, raw caller callables, another-parent identities, callable arguments and `@content` transport, transitive modules, `@forward`, legacy imports, `meta.load-css()` execution, broader resolution, production routing, public capability claims, native graduation, version, release interlock, tag, artifact/package publication, deployment, and every external system remain unchanged.
- Quality closure: `QUAL-001` is verified at `3eced86`; repository-wide Zig formatting debt is zero and the final local validation matrix is green
- Next eligible package: admit only the next twenty-second-module hop already measured against Dart Sass 1.101.0 as the smallest durable `NSASS-011-LOCAL-USE-TWENTY-SECOND-MODULE-REEXPORTED-CALLABLE-CONFIGURATION-OWNERSHIP-FOUNDATION`, with direct/enumerated and nested values, invocation/application inside and after the twenty-second module, canonical/default/custom aliases, live owner state, SCSS/indented-Sass parity, exact provenance depth, strict diagnostics, limits, and every allocation failure. Retain twenty-third-and-later configuration, raw caller callables, another-parent identities, callable arguments and `@content` transport, transitive modules, `@forward`, legacy imports, `meta.load-css()`, broader resolution, and native graduation as separate measured slices. Provider removal and the package-level zero-dependency cutover remain gated on native graduation rather than deleting the exact development oracles early. Independently, `BENCH-007` still requires a dedicated non-emulated Linux x64 runner with labels `[self-hosted, linux, x64, zigcss-benchmark-v1]`; its absence must not pause eligible native frontend work.
- Runtime control: model identity, single-agent execution, isolated-worktree confinement, automatic supervisor-owned green-checkpoint pushes, the owner-bounded post-`NATIVE-009` GitHub-prerelease/npm-`next` authority, and every native graduation/release interlock remain unchanged.

## Prior current-work evidence (twentieth-module callable configuration checkpoint)

- Milestone: Milestone 10 self-contained native stylesheet frontends, in parallel with Milestone 8 controlled benchmark evidence
- Work package: `NSASS-011` native Sass evaluation
- State: `IN_PROGRESS`
- Active phase: the two-hundred-fifty-sixth native Sass semantic checkpoint, `NSASS-011-LOCAL-USE-TWENTIETH-MODULE-REEXPORTED-CALLABLE-CONFIGURATION-OWNERSHIP-FOUNDATION`, is `VERIFIED` at local implementation checkpoint `4388a40bfbc987b49c5659ac7bff22d5ffaa4a59`. Fresh Dart Sass 1.101.0 in-memory importer probes establish that function and mixin references retain exact behavior through twentieth- and twenty-first-module configuration in SCSS and indented Sass: invocation and application inside and after the configured modules, original-owner live state, identical compressed CSS, and zero warnings all match. The additive native fixture retains the prior nineteenth-module regression and separately admits only the scheduled twentieth-module hop across direct and enumerated public variables, nested aggregates, module-returned values, invocation/application inside and after the twentieth module, default/custom canonical aliases, original-owner identity and state, deterministic dependency order, maps, and SCSS/indented-Sass parity. Native Zig centralizes the boundary as a maximum configurable re-export depth of eighteen: the original owner identity enters at depth zero; the middle through nineteenth re-exports enter at depths one through eighteen and may configure the third through twentieth modules respectively; and the twentieth re-export enters at depth nineteen and remains rejected when configuring a twenty-first module. Every accepted aggregate is deep-owned by its destination while the stable packed original owner/callable identity remains unchanged, so calls, mixin application, inspection, content acceptance, aliases, equality, and mutable state cross the new hop without retaining evaluator-arena pointers. Callable arguments and cross-module `@content` retain their stricter depth-zero/owner-only remappers. The test-first focused Debug gate stopped at 624/627 with exactly three failures: the twentieth-module positive and exhaustive-allocation paths reached the former maximum-depth-seventeen rejection, while the advanced no-partial diagnostic still pointed to `nineteenth.$exported` rather than the new twenty-first-module boundary. Exact twenty-first-module failure now owns one diagnostic at `twentieth.$exported`, leaves generated position zero, and emits no CSS; exhaustive-allocation coverage exercises the accepted twentieth-module path and every allocation failure passes. Final focused native Debug/ReleaseSafe each pass 23/23 steps with 627/627 tests; full compiler Debug/ReleaseSafe each pass 60/60 steps with 1122/1122 tests; and evaluator-test-root plus preprocessor-root no-emission cross-compiles pass for Windows x64 GNU and Linux ARM64 GNU in both modes. Native contract 12/12 plus check adds only `local-use-twentieth-module-reexported-callable-configuration-ownership-foundation` while retaining private native Sass rows, production unreachability, zero target package/optional/runtime dependencies, and the closed release interlock. Complete provider/reference/product matrices pass 153/153; preprocessor package policy passes 6/6 plus check over 22 runtime sources, 28 archive files, 86 locked dependencies, and 5 targets; capability-status policy passes 7/7 plus check; autonomous-runner selftest passes 59/59; and repository-wide Zig formatting, changed JavaScript syntax, all 36 tracked JSON files, and diff checks pass. The bounded sibling search confirms that twenty-first-and-later native configuration, raw caller callables, another-parent identities, callable arguments and `@content` transport, transitive modules, `@forward`, legacy imports, `meta.load-css()` execution, broader resolution, production routing, public capability claims, native graduation, version, release interlock, tag, artifact/package publication, deployment, and every external system remain unchanged.
- Quality closure: `QUAL-001` is verified at `3eced86`; repository-wide Zig formatting debt is zero and the final local validation matrix is green
- Next eligible package: admit only the next twenty-first-module hop already measured against Dart Sass 1.101.0 as the smallest durable `NSASS-011-LOCAL-USE-TWENTY-FIRST-MODULE-REEXPORTED-CALLABLE-CONFIGURATION-OWNERSHIP-FOUNDATION`, with direct/enumerated and nested values, invocation/application inside and after the twenty-first module, canonical/default/custom aliases, live owner state, SCSS/indented-Sass parity, exact provenance depth, strict diagnostics, limits, and every allocation failure. Retain twenty-second-and-later configuration, raw caller callables, another-parent identities, callable arguments and `@content` transport, transitive modules, `@forward`, legacy imports, `meta.load-css()`, broader resolution, and native graduation as separate measured slices. Provider removal and the package-level zero-dependency cutover remain gated on native graduation rather than deleting the exact development oracles early. Independently, `BENCH-007` still requires a dedicated non-emulated Linux x64 runner with labels `[self-hosted, linux, x64, zigcss-benchmark-v1]`; its absence must not pause eligible native frontend work.
- Runtime control: model identity, single-agent execution, isolated-worktree confinement, automatic supervisor-owned green-checkpoint pushes, the owner-bounded post-`NATIVE-009` GitHub-prerelease/npm-`next` authority, and every native graduation/release interlock remain unchanged.

## Prior current-work evidence (nineteenth-module callable configuration checkpoint)

- Milestone: Milestone 10 self-contained native stylesheet frontends, in parallel with Milestone 8 controlled benchmark evidence
- Work package: `NSASS-011` native Sass evaluation
- State: `IN_PROGRESS`
- Active phase: the two-hundred-fifty-fifth native Sass semantic checkpoint, `NSASS-011-LOCAL-USE-NINETEENTH-MODULE-REEXPORTED-CALLABLE-CONFIGURATION-OWNERSHIP-FOUNDATION`, is `VERIFIED` at local implementation checkpoint `986176d081e9afad873d2ae04c1234faba2738c3`. Fresh Dart Sass 1.101.0 in-memory importer probes establish that function and mixin references retain exact behavior through nineteenth- and twentieth-module configuration in SCSS and indented Sass: invocation and application inside the terminal module, original-owner live state, identical compressed CSS, and zero warnings all match. The additive native fixture retains the prior eighteenth-module regression and separately admits only the scheduled nineteenth-module hop across direct and enumerated public variables, nested aggregates, module-returned values, invocation/application inside and after the nineteenth module, default/custom canonical aliases, original-owner identity and state, deterministic dependency order, maps, and SCSS/indented-Sass parity. Native Zig centralizes the boundary as a maximum configurable re-export depth of seventeen: the original owner identity enters at depth zero; the middle through eighteenth re-exports enter at depths one through seventeen and may configure the third through nineteenth modules respectively; and the nineteenth re-export enters at depth eighteen and remains rejected when configuring a twentieth module. Every accepted aggregate is deep-owned by its destination while the stable packed original owner/callable identity remains unchanged, so calls, mixin application, inspection, content acceptance, aliases, equality, and mutable state cross the new hop without retaining evaluator-arena pointers. Callable arguments and cross-module `@content` retain their stricter depth-zero/owner-only remappers. The test-first focused Debug gate stopped at 623/626 with exactly three failures: the nineteenth-module positive and exhaustive-allocation paths reached the former maximum-depth-sixteen rejection, while the advanced no-partial diagnostic still pointed to `eighteenth.$exported` rather than the new twentieth-module boundary. Exact twentieth-module failure now owns one diagnostic at `nineteenth.$exported`, leaves generated position zero, and emits no CSS; exhaustive-allocation coverage exercises the accepted nineteenth-module path and every allocation failure passes. Final focused native Debug/ReleaseSafe each pass 23/23 steps with 626/626 tests; full compiler Debug/ReleaseSafe each pass 60/60 steps with 1121/1121 tests; and evaluator-test-root plus preprocessor-root no-emission cross-compiles pass for Windows x64 GNU and Linux ARM64 GNU in both modes. Native contract 12/12 plus check adds only `local-use-nineteenth-module-reexported-callable-configuration-ownership-foundation` while retaining private native Sass rows, production unreachability, zero target package/optional/runtime dependencies, and the closed release interlock. Complete provider/reference/product matrices pass 153/153; preprocessor package policy passes 6/6 plus check over 22 runtime sources, 28 archive files, 86 locked dependencies, and 5 targets; capability-status policy passes 7/7 plus check; autonomous-runner selftest passes 59/59; and repository-wide Zig formatting, changed JavaScript syntax, all 36 tracked JSON files, and diff checks pass. The bounded sibling search confirms that twentieth-and-later native configuration, raw caller callables, another-parent identities, callable arguments and `@content` transport, transitive modules, `@forward`, legacy imports, `meta.load-css()` execution, broader resolution, production routing, public capability claims, native graduation, version, release interlock, tag, artifact/package publication, deployment, and every external system remain unchanged.
- Quality closure: `QUAL-001` is verified at `3eced86`; repository-wide Zig formatting debt is zero and the final local validation matrix is green
- Next eligible package: admit only the next twentieth-module hop already measured against Dart Sass 1.101.0 as the smallest durable `NSASS-011-LOCAL-USE-TWENTIETH-MODULE-REEXPORTED-CALLABLE-CONFIGURATION-OWNERSHIP-FOUNDATION`, with direct/enumerated and nested values, invocation/application inside and after the twentieth module, canonical/default/custom aliases, live owner state, SCSS/indented-Sass parity, exact provenance depth, strict diagnostics, limits, and every allocation failure. Retain twenty-first-and-later configuration, raw caller callables, another-parent identities, callable arguments and `@content` transport, transitive modules, `@forward`, legacy imports, `meta.load-css()`, broader resolution, and native graduation as separate measured slices. Provider removal and the package-level zero-dependency cutover remain gated on native graduation rather than deleting the exact development oracles early. Independently, `BENCH-007` still requires a dedicated non-emulated Linux x64 runner with labels `[self-hosted, linux, x64, zigcss-benchmark-v1]`; its absence must not pause eligible native frontend work.
- Runtime control: model identity, single-agent execution, isolated-worktree confinement, automatic supervisor-owned green-checkpoint pushes, the owner-bounded post-`NATIVE-009` GitHub-prerelease/npm-`next` authority, and every native graduation/release interlock remain unchanged.

## Prior current-work evidence (eighteenth-module callable configuration checkpoint)

- Milestone: Milestone 10 self-contained native stylesheet frontends, in parallel with Milestone 8 controlled benchmark evidence
- Work package: `NSASS-011` native Sass evaluation
- State: `IN_PROGRESS`
- Active phase: the two-hundred-fifty-fourth native Sass semantic checkpoint, `NSASS-011-LOCAL-USE-EIGHTEENTH-MODULE-REEXPORTED-CALLABLE-CONFIGURATION-OWNERSHIP-FOUNDATION`, is `VERIFIED` at local implementation checkpoint `bc8d944946acafbad9a860ceab9e424945d274cd`. Fresh Dart Sass 1.101.0 in-memory importer probes establish that function and mixin references retain exact behavior through eighteenth- and nineteenth-module configuration in SCSS and indented Sass: invocation and application inside the terminal module, original-owner live state, identical compressed CSS, and zero warnings all match. The additive native fixture retains the prior seventeenth-module regression and separately admits only the scheduled eighteenth-module hop across direct and enumerated public variables, nested aggregates, module-returned values, invocation/application inside and after the eighteenth module, default/custom canonical aliases, original-owner identity and state, deterministic dependency order, maps, and SCSS/indented-Sass parity. Native Zig centralizes the boundary as a maximum configurable re-export depth of sixteen: the original owner identity enters at depth zero; the middle through seventeenth re-exports enter at depths one through sixteen and may configure the third through eighteenth modules respectively; and the eighteenth re-export enters at depth seventeen and remains rejected when configuring a nineteenth module. Every accepted aggregate is deep-owned by its destination while the stable packed original owner/callable identity remains unchanged, so calls, mixin application, inspection, content acceptance, aliases, equality, and mutable state cross the new hop without retaining evaluator-arena pointers. Callable arguments and cross-module `@content` retain their stricter depth-zero/owner-only remappers. The test-first focused Debug gate stopped at 622/625 with exactly three failures: the eighteenth-module positive and exhaustive-allocation paths reached the former maximum-depth-fifteen rejection, while the advanced no-partial diagnostic still pointed to `seventeenth.$exported` rather than the new nineteenth-module boundary. Exact nineteenth-module failure now owns one diagnostic at `eighteenth.$exported`, leaves generated position zero, and emits no CSS; exhaustive-allocation coverage exercises the accepted eighteenth-module path and every allocation failure passes. Final focused native Debug/ReleaseSafe each pass 23/23 steps with 625/625 tests; full compiler Debug/ReleaseSafe each pass 60/60 steps with 1120/1120 tests; and evaluator-test-root plus preprocessor-root no-emission cross-compiles pass for Windows x64 GNU and Linux ARM64 GNU in both modes. Native contract 12/12 plus check adds only `local-use-eighteenth-module-reexported-callable-configuration-ownership-foundation` while retaining private native Sass rows, production unreachability, zero target package/optional/runtime dependencies, and the closed release interlock. Complete provider/reference/product matrices pass 153/153; preprocessor package policy passes 6/6 plus check over 22 runtime sources, 28 archive files, 86 locked dependencies, and 5 targets; capability-status policy passes 7/7 plus check; autonomous-runner selftest passes 59/59; and repository-wide Zig formatting, changed JavaScript syntax, all 36 tracked JSON files, and diff checks pass. The bounded sibling search confirms that nineteenth-and-later native configuration, raw caller callables, another-parent identities, callable arguments and `@content` transport, transitive modules, `@forward`, legacy imports, `meta.load-css()` execution, broader resolution, production routing, public capability claims, native graduation, version, release interlock, tag, artifact/package publication, deployment, and every external system remain unchanged.
- Quality closure: `QUAL-001` is verified at `3eced86`; repository-wide Zig formatting debt is zero and the final local validation matrix is green
- Next eligible package: admit only the next nineteenth-module hop already measured against Dart Sass 1.101.0 as the smallest durable `NSASS-011-LOCAL-USE-NINETEENTH-MODULE-REEXPORTED-CALLABLE-CONFIGURATION-OWNERSHIP-FOUNDATION`, with direct/enumerated and nested values, invocation/application inside and after the nineteenth module, canonical/default/custom aliases, live owner state, SCSS/indented-Sass parity, exact provenance depth, strict diagnostics, limits, and every allocation failure. Retain twentieth-and-later configuration, raw caller callables, another-parent identities, callable arguments and `@content` transport, transitive modules, `@forward`, legacy imports, `meta.load-css()`, broader resolution, and native graduation as separate measured slices. Provider removal and the package-level zero-dependency cutover remain gated on native graduation rather than deleting the exact development oracles early. Independently, `BENCH-007` still requires a dedicated non-emulated Linux x64 runner with labels `[self-hosted, linux, x64, zigcss-benchmark-v1]`; its absence must not pause eligible native frontend work.
- Runtime control: model identity, single-agent execution, isolated-worktree confinement, automatic supervisor-owned green-checkpoint pushes, the owner-bounded post-`NATIVE-009` GitHub-prerelease/npm-`next` authority, and every native graduation/release interlock remain unchanged.

## Prior current-work evidence (seventeenth-module callable configuration checkpoint)

- Milestone: Milestone 10 self-contained native stylesheet frontends, in parallel with Milestone 8 controlled benchmark evidence
- Work package: `NSASS-011` native Sass evaluation
- State: `IN_PROGRESS`
- Active phase: the two-hundred-fifty-third native Sass semantic checkpoint, `NSASS-011-LOCAL-USE-SEVENTEENTH-MODULE-REEXPORTED-CALLABLE-CONFIGURATION-OWNERSHIP-FOUNDATION`, is `VERIFIED` at local implementation checkpoint `460c130f0459b185ae84c243f8cc0776dc2b39ec`. Fresh Dart Sass 1.101.0 in-memory importer probes establish that function and mixin references retain exact behavior through seventeenth- and eighteenth-module configuration in SCSS and indented Sass: invocation and application inside and after the configured modules, original-owner live state, identical compressed CSS, and zero warnings all match. The additive native fixture retains the prior sixteenth-module regression and separately admits only the scheduled seventeenth-module hop across direct and enumerated public variables, nested aggregates, module-returned values, invocation/application inside and after the seventeenth module, default/custom canonical aliases, original-owner identity and state, deterministic dependency order, maps, and SCSS/indented-Sass parity. Native Zig centralizes the boundary as a maximum configurable re-export depth of fifteen: the original owner identity enters at depth zero; the middle through sixteenth re-exports enter at depths one through fifteen and may configure the third through seventeenth modules respectively; and the seventeenth re-export enters at depth sixteen and remains rejected when configuring an eighteenth module. Every accepted aggregate is deep-owned by its destination while the stable packed original owner/callable identity remains unchanged, so calls, mixin application, inspection, content acceptance, aliases, equality, and mutable state cross the new hop without retaining evaluator-arena pointers. Callable arguments and cross-module `@content` retain their stricter depth-zero/owner-only remappers. The test-first focused Debug gate stopped at 621/624 with exactly three failures: the seventeenth-module positive and exhaustive-allocation paths reached the former maximum-depth-fourteen rejection, while the advanced no-partial diagnostic still pointed to `sixteenth.$exported` rather than the new eighteenth-module boundary. Exact eighteenth-module failure now owns one diagnostic at `seventeenth.$exported`, leaves generated position zero, and emits no CSS; exhaustive-allocation coverage exercises the accepted seventeenth-module path and every allocation failure passes. Final focused native Debug/ReleaseSafe each pass 23/23 steps with 624/624 tests; full compiler Debug/ReleaseSafe each pass 60/60 steps with 1119/1119 tests; and evaluator-test-root plus preprocessor-root no-emission cross-compiles pass for Windows x64 GNU and Linux ARM64 GNU in both modes. Native contract 12/12 plus check adds only `local-use-seventeenth-module-reexported-callable-configuration-ownership-foundation` while retaining private native Sass rows, production unreachability, zero target package/optional/runtime dependencies, and the closed release interlock. Complete provider/reference/product matrices pass 153/153; preprocessor package policy passes 6/6 plus check over 22 runtime sources, 28 archive files, 86 locked dependencies, and 5 targets; capability-status policy passes 7/7 plus check; autonomous-runner selftest passes 59/59; and repository-wide Zig formatting, changed JavaScript syntax, all 36 tracked JSON files, and diff checks pass. The bounded sibling search confirms that eighteenth-and-later native configuration, raw caller callables, another-parent identities, callable arguments and `@content` transport, transitive modules, `@forward`, legacy imports, `meta.load-css()` execution, broader resolution, production routing, public capability claims, native graduation, version, release interlock, tag, artifact/package publication, deployment, and every external system remain unchanged.
- Quality closure: `QUAL-001` is verified at `3eced86`; repository-wide Zig formatting debt is zero and the final local validation matrix is green
- Next eligible package: admit only the next eighteenth-module hop already measured against Dart Sass 1.101.0 as the smallest durable `NSASS-011-LOCAL-USE-EIGHTEENTH-MODULE-REEXPORTED-CALLABLE-CONFIGURATION-OWNERSHIP-FOUNDATION`, with direct/enumerated and nested values, invocation/application inside and after the eighteenth module, canonical/default/custom aliases, live owner state, SCSS/indented-Sass parity, exact provenance depth, strict diagnostics, limits, and every allocation failure. Retain nineteenth-and-later configuration, raw caller callables, another-parent identities, callable arguments and `@content` transport, transitive modules, `@forward`, legacy imports, `meta.load-css()`, broader resolution, and native graduation as separate measured slices. Provider removal and the package-level zero-dependency cutover remain gated on native graduation rather than deleting the exact development oracles early. Independently, `BENCH-007` still requires a dedicated non-emulated Linux x64 runner with labels `[self-hosted, linux, x64, zigcss-benchmark-v1]`; its absence must not pause eligible native frontend work.
- Runtime control: model identity, single-agent execution, isolated-worktree confinement, automatic supervisor-owned green-checkpoint pushes, the owner-bounded post-`NATIVE-009` GitHub-prerelease/npm-`next` authority, and every native graduation/release interlock remain unchanged.

## Prior current-work evidence (sixteenth-module callable configuration checkpoint)

- Milestone: Milestone 10 self-contained native stylesheet frontends, in parallel with Milestone 8 controlled benchmark evidence
- Work package: `NSASS-011` native Sass evaluation
- State: `IN_PROGRESS`
- Active phase: the two-hundred-fifty-second native Sass semantic checkpoint, `NSASS-011-LOCAL-USE-SIXTEENTH-MODULE-REEXPORTED-CALLABLE-CONFIGURATION-OWNERSHIP-FOUNDATION`, is `VERIFIED` at local implementation checkpoint `3555679d81979e5c61eeefb8c80589e63eca7ea5`. Fresh Dart Sass 1.101.0 in-memory importer probes establish that function and mixin references retain exact behavior through sixteenth- and seventeenth-module configuration in SCSS and indented Sass: invocation and application inside and after the configured modules, original-owner identity and live state, identical compressed CSS, and zero warnings all match. The additive native fixture retains the prior fifteenth-module regression and separately admits only the scheduled sixteenth-module hop across direct and enumerated public variables, nested aggregates, module-returned values, invocation/application inside and after the sixteenth module, default/custom canonical aliases, original-owner identity and state, deterministic dependency order, maps, and SCSS/indented-Sass parity. Native Zig centralizes the boundary as a maximum configurable re-export depth of fourteen: the original owner identity enters at depth zero; the middle through fifteenth re-exports enter at depths one through fourteen and may configure the third through sixteenth modules respectively; and the sixteenth re-export enters at depth fifteen and remains rejected when configuring a seventeenth module. Every accepted aggregate is deep-owned by its destination while the stable packed original owner/callable identity remains unchanged, so calls, mixin application, inspection, content acceptance, aliases, equality, and mutable state cross the new hop without retaining evaluator-arena pointers. Callable arguments and cross-module `@content` retain their stricter depth-zero/owner-only remappers. The test-first focused Debug gate stopped at 620/623 with exactly three failures: the sixteenth-module positive and exhaustive-allocation paths reached the former maximum-depth-thirteen rejection, while the advanced no-partial diagnostic still pointed to `fifteenth.$exported` rather than the new seventeenth-module boundary. Exact seventeenth-module failure now owns one diagnostic at `sixteenth.$exported`, leaves generated position zero, and emits no CSS; exhaustive-allocation coverage exercises the accepted sixteenth-module path and every allocation failure passes. Final focused native Debug/ReleaseSafe each pass 23/23 steps with 623/623 tests; full compiler Debug/ReleaseSafe each pass 60/60 steps with 1118/1118 tests; and evaluator-test-root plus preprocessor-root no-emission cross-compiles pass for Windows x64 GNU and Linux ARM64 GNU in both modes. Native contract 12/12 plus check adds only `local-use-sixteenth-module-reexported-callable-configuration-ownership-foundation` while retaining private native Sass rows, production unreachability, zero target package/optional/runtime dependencies, and the closed release interlock. Complete provider/reference/product matrices pass 153/153; preprocessor package policy passes 6/6 plus check over 22 runtime sources, 28 archive files, 86 locked dependencies, and 5 targets; capability-status policy passes 7/7 plus check; autonomous-runner selftest passes 59/59; and repository-wide Zig formatting, changed JavaScript syntax, all 36 tracked JSON files, and diff checks pass. The bounded sibling search confirms that seventeenth-and-later native configuration, raw caller callables, another-parent identities, callable arguments and `@content` transport, transitive modules, `@forward`, legacy imports, `meta.load-css()` execution, broader resolution, production routing, public capability claims, native graduation, version, release interlock, tag, artifact/package publication, deployment, and every external system remain unchanged.
- Quality closure: `QUAL-001` is verified at `3eced86`; repository-wide Zig formatting debt is zero and the final local validation matrix is green
- Next eligible package: admit only the next seventeenth-module hop already measured against Dart Sass 1.101.0 as the smallest durable `NSASS-011-LOCAL-USE-SEVENTEENTH-MODULE-REEXPORTED-CALLABLE-CONFIGURATION-OWNERSHIP-FOUNDATION`, with direct/enumerated and nested values, invocation/application inside and after the seventeenth module, canonical/default/custom aliases, live owner state, SCSS/indented-Sass parity, exact provenance depth, strict diagnostics, limits, and every allocation failure. Retain eighteenth-and-later configuration, raw caller callables, another-parent identities, callable arguments and `@content` transport, transitive modules, `@forward`, legacy imports, `meta.load-css()`, broader resolution, and native graduation as separate measured slices. Provider removal and the package-level zero-dependency cutover remain gated on native graduation rather than deleting the exact development oracles early. Independently, `BENCH-007` still requires a dedicated non-emulated Linux x64 runner with labels `[self-hosted, linux, x64, zigcss-benchmark-v1]`; its absence must not pause eligible native frontend work.
- Runtime control: model identity, single-agent execution, isolated-worktree confinement, automatic supervisor-owned green-checkpoint pushes, the owner-bounded post-`NATIVE-009` GitHub-prerelease/npm-`next` authority, and every native graduation/release interlock remain unchanged.

## Prior current-work evidence (fifteenth-module callable configuration checkpoint)

- Milestone: Milestone 10 self-contained native stylesheet frontends, in parallel with Milestone 8 controlled benchmark evidence
- Work package: `NSASS-011` native Sass evaluation
- State: `IN_PROGRESS`
- Active phase: the two-hundred-fifty-first native Sass semantic checkpoint, `NSASS-011-LOCAL-USE-FIFTEENTH-MODULE-REEXPORTED-CALLABLE-CONFIGURATION-OWNERSHIP-FOUNDATION`, is `VERIFIED` at local implementation checkpoint `0bd18400fb3459c8d86b17df483a0147aba86df2`. Fresh Dart Sass 1.101.0 probes establish that function and mixin references retain exact behavior through fifteenth- and sixteenth-module configuration in SCSS and indented Sass: invocation and application inside and after the configured modules, original-owner live state, exact compressed CSS, and zero warnings all match. The strengthened native fixture retains the prior fourteenth-module regression and separately admits only the scheduled fifteenth-module hop across direct and enumerated public variables, nested aggregates, module-returned values, invocation/application inside and after the fifteenth module, default/custom canonical aliases, original-owner identity and state, deterministic dependency order, maps, and SCSS/indented-Sass parity. Native Zig centralizes the boundary as a maximum configurable re-export depth of thirteen: the original owner identity enters at depth zero; the middle through fourteenth re-exports enter at depths one through thirteen and may configure the third through fifteenth modules respectively; and the fifteenth re-export enters at depth fourteen and remains rejected when configuring a sixteenth module. Every accepted aggregate is deep-owned by its destination while the stable packed original owner/callable identity remains unchanged, so calls, mixin application, inspection, content acceptance, aliases, equality, and mutable state cross the new hop without retaining evaluator-arena pointers. Callable arguments and cross-module `@content` retain their stricter depth-zero/owner-only remappers. The test-first focused Debug gate stopped at 618/621 with exactly three failures: the fifteenth-module positive and exhaustive-allocation paths reached the former maximum-depth-twelve rejection, while the advanced no-partial diagnostic still pointed to `fourteenth.$exported` rather than the new sixteenth-module boundary. Exact sixteenth-module failure now owns one diagnostic at `fifteenth.$exported`, leaves generated position zero, and emits no CSS; exhaustive-allocation coverage exercises the accepted fifteenth-module path and every allocation failure passes. Final focused native Debug/ReleaseSafe each pass 23/23 steps with 622/622 tests; full compiler Debug/ReleaseSafe each pass 60/60 steps with 1117/1117 tests; and evaluator-test-root plus preprocessor-root no-emission cross-compiles pass for Windows x64 GNU and Linux ARM64 GNU in both modes. Native contract 12/12 plus check adds only `local-use-fifteenth-module-reexported-callable-configuration-ownership-foundation` while retaining private native Sass rows, production unreachability, zero target package/optional/runtime dependencies, and the closed release interlock. Complete provider/reference/product matrices pass 153/153; preprocessor package policy passes 6/6 plus check over 22 runtime sources, 28 archive files, 86 locked dependencies, and 5 targets; capability-status policy passes 7/7 plus check; autonomous-runner selftest passes 59/59; and repository-wide Zig formatting, changed JavaScript syntax, all 36 tracked JSON files, and diff checks pass. The bounded sibling search confirms that sixteenth-and-later configuration, raw caller callables, another-parent identities, callable arguments and `@content` transport, transitive modules, `@forward`, legacy imports, `meta.load-css()` execution, broader resolution, production routing, public capability claims, native graduation, version, release interlock, tag, artifact/package publication, deployment, and every external system remain unchanged.
- Quality closure: `QUAL-001` is verified at `3eced86`; repository-wide Zig formatting debt is zero and the final local validation matrix is green
- Next eligible package: measure and admit only the next sixteenth-module hop as the smallest durable `NSASS-011-LOCAL-USE-SIXTEENTH-MODULE-REEXPORTED-CALLABLE-CONFIGURATION-OWNERSHIP-FOUNDATION`, with direct/enumerated and nested values, invocation/application inside and after the sixteenth module, canonical/default/custom aliases, live owner state, SCSS/indented-Sass parity, exact provenance depth, strict diagnostics, limits, and every allocation failure. Retain seventeenth-and-later configuration, raw caller callables, another-parent identities, callable arguments and `@content` transport, transitive modules, `@forward`, legacy imports, `meta.load-css()`, broader resolution, and native graduation as separate measured slices. Provider removal and the package-level zero-dependency cutover remain gated on native graduation rather than deleting the exact development oracles early. Independently, `BENCH-007` still requires a dedicated non-emulated Linux x64 runner with labels `[self-hosted, linux, x64, zigcss-benchmark-v1]`; its absence must not pause eligible native frontend work.
- Runtime control: model identity, single-agent execution, isolated-worktree confinement, automatic supervisor-owned green-checkpoint pushes, the owner-bounded post-`NATIVE-009` GitHub-prerelease/npm-`next` authority, and every native graduation/release interlock remain unchanged.

## Prior current-work evidence (fourteenth-module callable configuration checkpoint)

- Milestone: Milestone 10 self-contained native stylesheet frontends, in parallel with Milestone 8 controlled benchmark evidence
- Work package: `NSASS-011` native Sass evaluation
- State: `IN_PROGRESS`
- Active phase: the two-hundred-fiftieth native Sass semantic checkpoint, `NSASS-011-LOCAL-USE-FOURTEENTH-MODULE-REEXPORTED-CALLABLE-CONFIGURATION-OWNERSHIP-FOUNDATION`, is `VERIFIED` at local implementation checkpoint `1cd606bada383c14649cf9faa8359fbefa135d1e`. Fresh Dart Sass 1.101.0 probes establish that function and mixin references retain exact behavior through fourteenth- and fifteenth-module configuration in SCSS and indented Sass: invocation and application inside and after the configured modules, original-owner live state, exact compressed CSS, and zero warnings all match. The strengthened native fixture admits only the scheduled fourteenth-module hop across direct and enumerated public variables, nested aggregates, module-returned values, invocation/application inside and after the fourteenth module, default/custom canonical aliases, original-owner identity and state, deterministic dependency order, maps, and SCSS/indented-Sass parity. Native Zig centralizes the boundary as a maximum configurable re-export depth of twelve: the original owner identity enters at depth zero; the middle through thirteenth re-exports enter at depths one through twelve and may configure the third through fourteenth modules respectively; and the fourteenth re-export enters at depth thirteen and remains rejected when configuring a fifteenth module. Every accepted aggregate is deep-owned by its destination while the stable packed original owner/callable identity remains unchanged, so calls, mixin application, inspection, content acceptance, aliases, equality, and mutable state cross the new hop without retaining evaluator-arena pointers. Callable arguments and cross-module `@content` retain their stricter depth-zero/owner-only remappers. The test-first focused Debug gate stopped at 618/621 with exactly three failures: the fourteenth-module positive and exhaustive-allocation paths reached the former maximum-depth-eleven rejection, while the advanced no-partial diagnostic still pointed to `thirteenth.$exported` rather than the new fifteenth-module boundary. Exact fifteenth-module failure now owns one diagnostic at `fourteenth.$exported`, leaves generated position zero, and emits no CSS; exhaustive-allocation coverage exercises the accepted fourteenth-module path and every allocation failure passes. Final focused native Debug/ReleaseSafe each pass 23/23 steps with 621/621 tests; full compiler Debug/ReleaseSafe each pass 60/60 steps with 1116/1116 tests; and evaluator-test-root plus preprocessor-root no-emission cross-compiles pass for Windows x64 GNU and Linux ARM64 GNU in both modes. Native contract 12/12 plus check adds only `local-use-fourteenth-module-reexported-callable-configuration-ownership-foundation` while retaining private native Sass rows, production unreachability, zero target package/optional/runtime dependencies, and the closed release interlock. Complete provider/reference/product matrices pass 153/153; preprocessor package policy passes 6/6 plus check over 22 runtime sources, 28 archive files, 86 locked dependencies, and 5 targets; capability-status policy passes 7/7 plus check; autonomous-runner selftest passes 59/59; and repository-wide Zig formatting, changed JavaScript syntax, all 36 tracked JSON files, and diff checks pass. The bounded sibling search confirms that fifteenth-and-later configuration, raw caller callables, another-parent identities, callable arguments and `@content` transport, transitive modules, `@forward`, legacy imports, `meta.load-css()` execution, broader resolution, production routing, public capability claims, native graduation, version, release interlock, tag, artifact/package publication, deployment, and every external system remain unchanged.
- Quality closure: `QUAL-001` is verified at `3eced86`; repository-wide Zig formatting debt is zero and the final local validation matrix is green
- Next eligible package: measure and admit only the next fifteenth-module hop as the smallest durable `NSASS-011-LOCAL-USE-FIFTEENTH-MODULE-REEXPORTED-CALLABLE-CONFIGURATION-OWNERSHIP-FOUNDATION`, with direct/enumerated and nested values, invocation/application inside and after the fifteenth module, canonical/default/custom aliases, live owner state, SCSS/indented-Sass parity, exact provenance depth, strict diagnostics, limits, and every allocation failure. Retain sixteenth-and-later configuration, raw caller callables, another-parent identities, callable arguments and `@content` transport, transitive modules, `@forward`, legacy imports, `meta.load-css()`, broader resolution, and native graduation as separate measured slices. Provider removal and the package-level zero-dependency cutover remain gated on native graduation rather than deleting the exact development oracles early. Independently, `BENCH-007` still requires a dedicated non-emulated Linux x64 runner with labels `[self-hosted, linux, x64, zigcss-benchmark-v1]`; its absence must not pause eligible native frontend work.
- Runtime control: model identity, single-agent execution, isolated-worktree confinement, automatic supervisor-owned green-checkpoint pushes, the owner-bounded post-`NATIVE-009` GitHub-prerelease/npm-`next` authority, and every native graduation/release interlock remain unchanged.

## Prior current-work evidence (thirteenth-module callable configuration checkpoint)

- Milestone: Milestone 10 self-contained native stylesheet frontends, in parallel with Milestone 8 controlled benchmark evidence
- Work package: `NSASS-011` native Sass evaluation
- State: `IN_PROGRESS`
- Active phase: the two-hundred-forty-ninth native Sass semantic checkpoint, `NSASS-011-LOCAL-USE-THIRTEENTH-MODULE-REEXPORTED-CALLABLE-CONFIGURATION-OWNERSHIP-FOUNDATION`, is `VERIFIED` at local implementation checkpoint `cf52f4f6e6793f17096076633f7b6278cb74169a`. Fresh Dart Sass 1.101.0 probes establish that function and mixin references retain exact behavior through thirteenth- and fourteenth-module configuration in SCSS and indented Sass: invocation and application inside and after the configured modules, original-owner live state, exact compressed CSS, and zero warnings all match. The strengthened native fixture admits only the scheduled thirteenth-module hop across direct and enumerated public variables, nested aggregates, module-returned values, invocation/application inside and after the thirteenth module, default/custom canonical aliases, original-owner identity and state, deterministic dependency order, maps, and SCSS/indented-Sass parity. Native Zig centralizes the boundary as a maximum configurable re-export depth of eleven: the original owner identity enters at depth zero; the middle through twelfth re-exports enter at depths one through eleven and may configure the third through thirteenth modules respectively; and the thirteenth re-export enters at depth twelve and remains rejected when configuring a fourteenth module. Every accepted aggregate is deep-owned by its destination while the stable packed original owner/callable identity remains unchanged, so calls, mixin application, inspection, content acceptance, aliases, equality, and mutable state cross the new hop without retaining evaluator-arena pointers. Callable arguments and cross-module `@content` retain their stricter depth-zero/owner-only remappers. The test-first focused Debug gate stopped at 618/620 with exactly two failures: the thirteenth-module positive path reached the former maximum-depth-ten rejection, while the advanced no-partial diagnostic still pointed to `twelfth.$exported` rather than the new fourteenth-module boundary. Exact fourteenth-module failure now owns one diagnostic at `thirteenth.$exported`, leaves generated position zero, and emits no CSS; exhaustive-allocation coverage exercises the accepted thirteenth-module path and every allocation failure passes. Final focused native Debug/ReleaseSafe each pass 23/23 steps with 620/620 tests; full compiler Debug/ReleaseSafe each pass 60/60 steps with 1115/1115 tests; and evaluator-test-root plus preprocessor-root no-emission cross-compiles pass for Windows x64 GNU and Linux ARM64 GNU in both modes. Native contract 12/12 plus check adds only `local-use-thirteenth-module-reexported-callable-configuration-ownership-foundation` while retaining private native Sass rows, production unreachability, zero target package/optional/runtime dependencies, and the closed release interlock. Complete provider/reference/product matrices pass 153/153; preprocessor package policy passes 6/6 plus check over 22 runtime sources, 28 archive files, 86 locked dependencies, and 5 targets; capability-status policy passes 7/7 plus check; autonomous-runner selftest passes 59/59; and repository-wide Zig formatting, changed JavaScript syntax, all 36 tracked JSON files, and diff checks pass. The bounded sibling search confirms that fourteenth-and-later configuration, raw caller callables, another-parent identities, callable arguments and `@content` transport, transitive modules, `@forward`, legacy imports, `meta.load-css()` execution, broader resolution, production routing, public capability claims, native graduation, version, release interlock, tag, artifact/package publication, deployment, and every external system remain unchanged.
- Quality closure: `QUAL-001` is verified at `3eced86`; repository-wide Zig formatting debt is zero and the final local validation matrix is green
- Next eligible package: measure and admit only the next fourteenth-module hop as the smallest durable `NSASS-011-LOCAL-USE-FOURTEENTH-MODULE-REEXPORTED-CALLABLE-CONFIGURATION-OWNERSHIP-FOUNDATION`, with direct/enumerated and nested values, invocation/application inside and after the fourteenth module, canonical/default/custom aliases, live owner state, SCSS/indented-Sass parity, exact provenance depth, strict diagnostics, limits, and every allocation failure. Retain fifteenth-and-later configuration, raw caller callables, another-parent identities, callable arguments and `@content` transport, transitive modules, `@forward`, legacy imports, `meta.load-css()`, broader resolution, and native graduation as separate measured slices. Provider removal and the package-level zero-dependency cutover remain gated on native graduation rather than deleting the exact development oracles early. Independently, `BENCH-007` still requires a dedicated non-emulated Linux x64 runner with labels `[self-hosted, linux, x64, zigcss-benchmark-v1]`; its absence must not pause eligible native frontend work.
- Runtime control: model identity, single-agent execution, isolated-worktree confinement, automatic supervisor-owned green-checkpoint pushes, the owner-bounded post-`NATIVE-009` GitHub-prerelease/npm-`next` authority, and every native graduation/release interlock remain unchanged.

## Prior current-work evidence (twelfth-module callable configuration checkpoint)

- Milestone: Milestone 10 self-contained native stylesheet frontends, in parallel with Milestone 8 controlled benchmark evidence
- Work package: `NSASS-011` native Sass evaluation
- State: `IN_PROGRESS`
- Active phase: the two-hundred-forty-eighth native Sass semantic checkpoint, `NSASS-011-LOCAL-USE-TWELFTH-MODULE-REEXPORTED-CALLABLE-CONFIGURATION-OWNERSHIP-FOUNDATION`, is `VERIFIED` at local implementation checkpoint `314f5f3569dbd32b2d7db92ac7732124e350945a`. Fresh Dart Sass 1.101.0 probes establish that function and mixin references retain exact behavior through twelfth- and thirteenth-module configuration in SCSS and indented Sass: invocation and application inside and after the configured modules, original-owner live state, exact compressed CSS, and zero warnings all match. The strengthened native fixture admits only the scheduled twelfth-module hop across direct and enumerated public variables, nested aggregates, module-returned values, invocation/application inside and after the twelfth module, default/custom canonical aliases, original-owner identity and state, deterministic dependency order, maps, and SCSS/indented-Sass parity. Native Zig centralizes the boundary as a maximum configurable re-export depth of ten: the original owner identity enters at depth zero; the middle, third, fourth, fifth, sixth, seventh, eighth, ninth, tenth, and eleventh re-exports enter at depths one through ten and may configure the third through twelfth modules respectively; and the twelfth re-export enters at depth eleven and remains rejected when configuring a thirteenth module. Every accepted aggregate is deep-owned by its destination while the stable packed original owner/callable identity remains unchanged, so calls, mixin application, inspection, content acceptance, aliases, equality, and mutable state cross the new hop without retaining evaluator-arena pointers. Callable arguments and cross-module `@content` retain their stricter depth-zero/owner-only remappers. The test-first focused Debug gate stopped at 616/619 with exactly three failures: the twelfth-module positive and exhaustive-allocation paths reached the former maximum-depth-nine rejection, while the no-partial diagnostic still pointed to the twelfth rather than thirteenth module. Exact thirteenth-module failure now owns one diagnostic at `twelfth.$exported`, leaves generated position zero, and emits no CSS; exhaustive-allocation coverage exercises the accepted twelfth-module path and every allocation failure passes. Final focused native Debug/ReleaseSafe each pass 23/23 steps with 619/619 tests; full compiler Debug/ReleaseSafe each pass 60/60 steps with 1114/1114 tests; and evaluator-test-root plus preprocessor-root no-emission cross-compiles pass for Windows x64 GNU and Linux ARM64 GNU in both modes. Native contract 12/12 plus check adds only `local-use-twelfth-module-reexported-callable-configuration-ownership-foundation` while retaining private native Sass rows, production unreachability, zero target package/optional/runtime dependencies, and the closed release interlock. Complete provider/reference/product matrices pass 153/153; preprocessor package policy passes 6/6 plus check over 22 runtime sources, 28 archive files, 86 locked dependencies, and 5 targets; capability-status policy passes 7/7 plus check; autonomous-runner selftest passes 59/59; and repository-wide Zig formatting, changed JavaScript syntax, all 36 tracked JSON files, and diff checks pass. The bounded sibling search confirms that thirteenth-and-later configuration, raw caller callables, another-parent identities, callable arguments and `@content` transport, transitive modules, `@forward`, legacy imports, `meta.load-css()` execution, broader resolution, production routing, public capability claims, native graduation, version, release interlock, tag, artifact/package publication, deployment, and every external system remain unchanged.
- Quality closure: `QUAL-001` is verified at `3eced86`; repository-wide Zig formatting debt is zero and the final local validation matrix is green
- Next eligible package: measure and admit only the next thirteenth-module hop as the smallest durable `NSASS-011-LOCAL-USE-THIRTEENTH-MODULE-REEXPORTED-CALLABLE-CONFIGURATION-OWNERSHIP-FOUNDATION`, with direct/enumerated and nested values, invocation/application inside and after the thirteenth module, canonical/default/custom aliases, live owner state, SCSS/indented-Sass parity, exact provenance depth, strict diagnostics, limits, and every allocation failure. Retain fourteenth-and-later configuration, raw caller callables, another-parent identities, callable arguments and `@content` transport, transitive modules, `@forward`, legacy imports, `meta.load-css()`, broader resolution, and native graduation as separate measured slices. Provider removal and the package-level zero-dependency cutover remain gated on native graduation rather than deleting the exact development oracles early. Independently, `BENCH-007` still requires a dedicated non-emulated Linux x64 runner with labels `[self-hosted, linux, x64, zigcss-benchmark-v1]`; its absence must not pause eligible native frontend work.
- Runtime control: model identity, single-agent execution, isolated-worktree confinement, automatic supervisor-owned green-checkpoint pushes, the owner-bounded post-`NATIVE-009` GitHub-prerelease/npm-`next` authority, and every native graduation/release interlock remain unchanged.

## Prior current-work evidence (eleventh-module callable configuration checkpoint)

- Milestone: Milestone 10 self-contained native stylesheet frontends, in parallel with Milestone 8 controlled benchmark evidence
- Work package: `NSASS-011` native Sass evaluation
- State: `IN_PROGRESS`
- Active phase: the two-hundred-forty-seventh native Sass semantic checkpoint, `NSASS-011-LOCAL-USE-ELEVENTH-MODULE-REEXPORTED-CALLABLE-CONFIGURATION-OWNERSHIP-FOUNDATION`, is `VERIFIED` at local implementation checkpoint `b5d795236721139aec24de955bef593798b1c9bf`. Fresh Dart Sass 1.101.0 probes establish that function references retain exact behavior through eleventh- and twelfth-module configuration in SCSS and indented Sass: invocation inside and after the configured modules, original-owner live state, exact compressed CSS, and zero warnings all match. The strengthened native fixture admits only the scheduled eleventh-module hop across direct and enumerated public variables, nested aggregates, module-returned values, invocation/application inside and after the eleventh module, default/custom canonical aliases, original-owner identity and state, deterministic dependency order, maps, and SCSS/indented-Sass parity. Native Zig centralizes the boundary as a maximum configurable re-export depth of nine: the original owner identity enters at depth zero; the middle, third, fourth, fifth, sixth, seventh, eighth, ninth, and tenth re-exports enter at depths one through nine and may configure the third through eleventh modules respectively; and the eleventh re-export enters at depth ten and remains rejected when configuring a twelfth module. Every accepted aggregate is deep-owned by its destination while the stable packed original owner/callable identity remains unchanged, so calls, mixin application, inspection, content acceptance, aliases, equality, and mutable state cross the new hop without retaining evaluator-arena pointers. Callable arguments and cross-module `@content` retain their stricter depth-zero/owner-only remappers. The test-first focused Debug gate stopped at 615/618 with exactly three failures: the eleventh-module positive and exhaustive-allocation paths reached the former maximum-depth-eight rejection, while the no-partial diagnostic still pointed to the eleventh rather than twelfth module. Exact twelfth-module failure now owns one diagnostic at `eleventh.$exported`, leaves generated position zero, and emits no CSS; exhaustive-allocation coverage exercises the accepted eleventh-module path and every allocation failure passes. Final focused native Debug/ReleaseSafe each pass 23/23 steps with 618/618 tests; full compiler Debug/ReleaseSafe each pass 60/60 steps with 1113/1113 tests; and evaluator-test-root plus preprocessor-root no-emission cross-compiles pass for Windows x64 GNU and Linux ARM64 GNU in both modes. Native contract 12/12 plus check adds only `local-use-eleventh-module-reexported-callable-configuration-ownership-foundation` while retaining private native Sass rows, production unreachability, zero target package/optional/runtime dependencies, and the closed release interlock. Complete provider/reference/product matrices pass 153/153; preprocessor package policy passes 6/6 plus check over 22 runtime sources, 28 archive files, 86 locked dependencies, and 5 targets; capability-status policy passes 7/7 plus check; autonomous-runner selftest passes 59/59; and repository-wide Zig formatting, changed JavaScript syntax, all 36 tracked JSON files, and diff checks pass. The bounded sibling search confirms that twelfth-and-later configuration, raw caller callables, another-parent identities, callable arguments and `@content` transport, transitive modules, `@forward`, legacy imports, `meta.load-css()` execution, broader resolution, production routing, public capability claims, native graduation, version, release interlock, tag, artifact/package publication, deployment, and every external system remain unchanged.
- Quality closure: `QUAL-001` is verified at `3eced86`; repository-wide Zig formatting debt is zero and the final local validation matrix is green
- Next eligible package: measure and admit only the next twelfth-module hop as the smallest durable `NSASS-011-LOCAL-USE-TWELFTH-MODULE-REEXPORTED-CALLABLE-CONFIGURATION-OWNERSHIP-FOUNDATION`, with direct/enumerated and nested values, invocation/application inside and after the twelfth module, canonical/default/custom aliases, live owner state, SCSS/indented-Sass parity, exact provenance depth, strict diagnostics, limits, and every allocation failure. Retain thirteenth-and-later configuration, raw caller callables, another-parent identities, callable arguments and `@content` transport, transitive modules, `@forward`, legacy imports, `meta.load-css()`, broader resolution, and native graduation as separate measured slices. Provider removal and the package-level zero-dependency cutover remain gated on native graduation rather than deleting the exact development oracles early. Independently, `BENCH-007` still requires a dedicated non-emulated Linux x64 runner with labels `[self-hosted, linux, x64, zigcss-benchmark-v1]`; its absence must not pause eligible native frontend work.
- Runtime control: model identity, single-agent execution, isolated-worktree confinement, automatic supervisor-owned green-checkpoint pushes, the owner-bounded post-`NATIVE-009` GitHub-prerelease/npm-`next` authority, and every native graduation/release interlock remain unchanged.

## Prior current-work evidence (tenth-module callable configuration checkpoint)

- Milestone: Milestone 10 self-contained native stylesheet frontends, in parallel with Milestone 8 controlled benchmark evidence
- Work package: `NSASS-011` native Sass evaluation
- State: `IN_PROGRESS`
- Active phase: the two-hundred-forty-sixth native Sass semantic checkpoint, `NSASS-011-LOCAL-USE-TENTH-MODULE-REEXPORTED-CALLABLE-CONFIGURATION-OWNERSHIP-FOUNDATION`, is `VERIFIED` at local implementation checkpoint `ae0890e589099dc53afbcc7efa42920d2473d34f`. Fresh Dart Sass 1.101.0 probes establish that function references retain exact behavior through tenth- and eleventh-module configuration in SCSS and indented Sass: invocation inside and after the configured modules, original-owner live state, exact compressed CSS, and zero warnings all match. The strengthened native fixture admits only the scheduled tenth-module hop across direct and enumerated public variables, nested aggregates, module-returned values, invocation/application inside and after the tenth module, default/custom canonical aliases, original-owner identity and state, deterministic dependency order, maps, and SCSS/indented-Sass parity. Native Zig centralizes the boundary as a maximum configurable re-export depth of eight: the original owner identity enters at depth zero; the middle, third, fourth, fifth, sixth, seventh, eighth, and ninth re-exports enter at depths one through eight and may configure the third through tenth modules respectively; and the tenth re-export enters at depth nine and remains rejected when configuring an eleventh module. Every accepted aggregate is deep-owned by its destination while the stable packed original owner/callable identity remains unchanged, so calls, mixin application, inspection, content acceptance, aliases, equality, and mutable state cross the new hop without retaining evaluator-arena pointers. Callable arguments and cross-module `@content` retain their stricter depth-zero/owner-only remappers. The test-first focused Debug gate stopped at 614/617 with exactly three failures: the tenth-module positive and exhaustive-allocation paths reached the former maximum-depth-seven rejection, while the no-partial diagnostic still pointed to the tenth rather than eleventh module. Exact eleventh-module failure now owns one diagnostic at `tenth.$exported`, leaves generated position zero, and emits no CSS; exhaustive-allocation coverage exercises the accepted tenth-module path and every allocation failure passes. Final focused native Debug/ReleaseSafe each pass 23/23 steps with 617/617 tests; full compiler Debug/ReleaseSafe each pass 60/60 steps with 1112/1112 tests; and evaluator-test-root plus preprocessor-root no-emission cross-compiles pass for Windows x64 GNU and Linux ARM64 GNU in both modes. Native contract 12/12 plus check adds only `local-use-tenth-module-reexported-callable-configuration-ownership-foundation` while retaining private native Sass rows, production unreachability, zero target package/optional/runtime dependencies, and the closed release interlock. Complete provider/reference/product matrices pass 153/153; preprocessor package policy passes 6/6 plus check over 22 runtime sources, 28 archive files, 86 locked dependencies, and 5 targets; capability-status policy passes 7/7 plus check; autonomous-runner selftest passes 59/59; and repository-wide Zig formatting, changed JavaScript syntax, all 36 tracked JSON files, and diff checks pass. The bounded sibling search confirms that eleventh-and-later configuration, raw caller callables, another-parent identities, callable arguments and `@content` transport, transitive modules, `@forward`, legacy imports, `meta.load-css()` execution, broader resolution, production routing, public capability claims, native graduation, version, release interlock, tag, artifact/package publication, deployment, and every external system remain unchanged.
- Quality closure: `QUAL-001` is verified at `3eced86`; repository-wide Zig formatting debt is zero and the final local validation matrix is green
- Next eligible package: measure and admit only the next eleventh-module hop as the smallest durable `NSASS-011-LOCAL-USE-ELEVENTH-MODULE-REEXPORTED-CALLABLE-CONFIGURATION-OWNERSHIP-FOUNDATION`, with direct/enumerated and nested values, invocation/application inside and after the eleventh module, canonical/default/custom aliases, live owner state, SCSS/indented-Sass parity, exact provenance depth, strict diagnostics, limits, and every allocation failure. Retain twelfth-and-later configuration, raw caller callables, another-parent identities, callable arguments and `@content` transport, transitive modules, `@forward`, legacy imports, `meta.load-css()`, broader resolution, and native graduation as separate measured slices. Provider removal and the package-level zero-dependency cutover remain gated on native graduation rather than deleting the exact development oracles early. Independently, `BENCH-007` still requires a dedicated non-emulated Linux x64 runner with labels `[self-hosted, linux, x64, zigcss-benchmark-v1]`; its absence must not pause eligible native frontend work.
- Runtime control: model identity, single-agent execution, isolated-worktree confinement, automatic supervisor-owned green-checkpoint pushes, the owner-bounded post-`NATIVE-009` GitHub-prerelease/npm-`next` authority, and every native graduation/release interlock remain unchanged.

## Prior current-work evidence (ninth-module callable configuration checkpoint)

- Milestone: Milestone 10 self-contained native stylesheet frontends, in parallel with Milestone 8 controlled benchmark evidence
- Work package: `NSASS-011` native Sass evaluation
- State: `IN_PROGRESS`
- Active phase: the two-hundred-forty-fifth native Sass semantic checkpoint, `NSASS-011-LOCAL-USE-NINTH-MODULE-REEXPORTED-CALLABLE-CONFIGURATION-OWNERSHIP-FOUNDATION`, is `VERIFIED` at local implementation checkpoint `9c8d8e9107c9a73f5cf7e177aad4f4ce715ca7e7`. Fresh Dart Sass 1.101.0 probes establish that function references retain exact behavior through ninth- and tenth-module configuration in SCSS and indented Sass: invocation inside and after the configured module, original-owner live state, exact compressed CSS, and zero warnings all match. The strengthened native fixture admits only the scheduled ninth-module hop across direct and enumerated public variables, nested aggregates, module-returned values, invocation/application inside and after the ninth module, default/custom canonical aliases, original-owner identity and state, deterministic dependency order, maps, and SCSS/indented-Sass parity. Native Zig centralizes the boundary as a maximum configurable re-export depth of seven: the original owner identity enters at depth zero; the middle, third, fourth, fifth, sixth, seventh, and eighth re-exports enter at depths one through seven and may configure the third through ninth modules respectively; and the ninth re-export enters at depth eight and remains rejected when configuring a tenth module. Every accepted aggregate is deep-owned by its destination while the stable packed original owner/callable identity remains unchanged, so calls, mixin application, inspection, content acceptance, aliases, equality, and mutable state cross the new hop without retaining evaluator-arena pointers. Callable arguments and cross-module `@content` retain their stricter depth-zero/owner-only remappers. The test-first focused Debug gate stopped at 613/616 with exactly three failures: the ninth-module positive and exhaustive-allocation paths reached the former maximum-depth-six rejection, while the no-partial diagnostic still pointed to the ninth rather than tenth module. Exact tenth-module failure now owns one diagnostic at `ninth.$exported`, leaves generated position zero, and emits no CSS; exhaustive-allocation coverage exercises the accepted ninth-module path and every allocation failure passes. Final focused native Debug/ReleaseSafe each pass 23/23 steps with 616/616 tests; full compiler Debug/ReleaseSafe each pass 60/60 steps with 1111/1111 tests; and evaluator-test-root plus preprocessor-root no-emission cross-compiles pass for Windows x64 GNU and Linux ARM64 GNU in both modes. Native contract 12/12 plus check adds only `local-use-ninth-module-reexported-callable-configuration-ownership-foundation` while retaining private native Sass rows, production unreachability, zero target package/optional/runtime dependencies, and the closed release interlock. Complete provider/reference/product matrices pass 153/153; preprocessor package policy passes 6/6 plus check over 22 runtime sources, 28 archive files, 86 locked dependencies, and 5 targets; capability-status policy passes 7/7 plus check; autonomous-runner selftest passes 59/59; and repository-wide Zig formatting, changed JavaScript syntax, all 36 tracked JSON files, and diff checks pass. The bounded sibling search confirms that tenth-and-later configuration, raw caller callables, another-parent identities, callable arguments and `@content` transport, transitive modules, `@forward`, legacy imports, `meta.load-css()` execution, broader resolution, production routing, public capability claims, native graduation, version, release interlock, tag, artifact/package publication, deployment, and every external system remain unchanged.
- Quality closure: `QUAL-001` is verified at `3eced86`; repository-wide Zig formatting debt is zero and the final local validation matrix is green
- Next eligible package: measure and admit only the next tenth-module hop as the smallest durable `NSASS-011-LOCAL-USE-TENTH-MODULE-REEXPORTED-CALLABLE-CONFIGURATION-OWNERSHIP-FOUNDATION`, with direct/enumerated and nested values, invocation/application inside and after the tenth module, canonical/default/custom aliases, live owner state, SCSS/indented-Sass parity, exact provenance depth, strict diagnostics, limits, and every allocation failure. Retain eleventh-and-later configuration, raw caller callables, another-parent identities, callable arguments and `@content` transport, transitive modules, `@forward`, legacy imports, `meta.load-css()`, broader resolution, and native graduation as separate measured slices. Provider removal and the package-level zero-dependency cutover remain gated on native graduation rather than deleting the exact development oracles early. Independently, `BENCH-007` still requires a dedicated non-emulated Linux x64 runner with labels `[self-hosted, linux, x64, zigcss-benchmark-v1]`; its absence must not pause eligible native frontend work.
- Runtime control: model identity, single-agent execution, isolated-worktree confinement, automatic supervisor-owned green-checkpoint pushes, the owner-bounded post-`NATIVE-009` GitHub-prerelease/npm-`next` authority, and every native graduation/release interlock remain unchanged.

## Prior current-work evidence (eighth-module callable configuration checkpoint)

- Milestone: Milestone 10 self-contained native stylesheet frontends, in parallel with Milestone 8 controlled benchmark evidence
- Work package: `NSASS-011` native Sass evaluation
- State: `IN_PROGRESS`
- Active phase: the two-hundred-forty-fourth native Sass semantic checkpoint, `NSASS-011-LOCAL-USE-EIGHTH-MODULE-REEXPORTED-CALLABLE-CONFIGURATION-OWNERSHIP-FOUNDATION`, is `VERIFIED` at local implementation checkpoint `be22a893da17a670d77a2823c172bd1ef28d3848`. A fresh Dart Sass 1.101.0 probe establishes that function references retain exact behavior through eighth-module configuration in SCSS and indented Sass: invocation inside the configured module, invocation after the final module, original-owner identity and live state, exact compressed CSS, and zero warnings all match. The strengthened native fixture admits only the scheduled eighth-module hop across direct and enumerated public variables, nested aggregates, module-returned values, invocation/application inside and after the eighth module, default/custom canonical aliases, original-owner identity and state, deterministic dependency order, maps, and SCSS/indented-Sass parity. Native Zig centralizes the boundary as a maximum configurable re-export depth of six: the original owner identity enters at depth zero; the middle, third, fourth, fifth, sixth, and seventh re-exports enter at depths one through six and may configure the third through eighth modules respectively; and the eighth re-export enters at depth seven and remains rejected when configuring a ninth module. Every accepted aggregate is deep-owned by its destination while the stable packed original owner/callable identity remains unchanged, so calls, mixin application, inspection, content acceptance, aliases, equality, and mutable state cross the new hop without retaining evaluator-arena pointers. Callable arguments and cross-module `@content` retain their stricter depth-zero/owner-only remappers. The test-first focused Debug gate stopped at 612/615 with exactly three failures: the eighth-module positive and exhaustive-allocation paths reached the former maximum-depth-five rejection, while the no-partial diagnostic still pointed to the eighth rather than ninth module. Exact ninth-module failure now owns one diagnostic at `eighth.$exported`, leaves generated position zero, and emits no CSS; exhaustive-allocation coverage exercises the accepted eighth-module path and every allocation failure passes. Final focused native Debug/ReleaseSafe each pass 23/23 steps with 615/615 tests; full compiler Debug/ReleaseSafe each pass 60/60 steps with 1110/1110 tests; and evaluator-test-root plus preprocessor-root no-emission cross-compiles pass for Windows x64 GNU and Linux ARM64 GNU in both modes. Native contract 12/12 plus check adds only `local-use-eighth-module-reexported-callable-configuration-ownership-foundation` while retaining private native Sass rows, production unreachability, zero target package/optional/runtime dependencies, and the closed release interlock. Complete provider/reference/product matrices pass 153/153; preprocessor package policy passes 6/6 plus check over 22 runtime sources, 28 archive files, 86 locked dependencies, and 5 targets; capability-status policy passes 7/7 plus check; autonomous-runner selftest passes 59/59; and repository-wide Zig formatting, changed JavaScript syntax, all 36 tracked JSON files, and diff checks pass. The bounded sibling search confirms that ninth-and-later configuration, raw caller callables, another-parent identities, callable arguments and `@content` transport, transitive modules, `@forward`, legacy imports, `meta.load-css()` execution, broader resolution, production routing, public capability claims, native graduation, version, release interlock, tag, artifact/package publication, deployment, and every external system remain unchanged.
- Quality closure: `QUAL-001` is verified at `3eced86`; repository-wide Zig formatting debt is zero and the final local validation matrix is green
- Next eligible package: measure and admit only the next ninth-module hop as the smallest durable `NSASS-011-LOCAL-USE-NINTH-MODULE-REEXPORTED-CALLABLE-CONFIGURATION-OWNERSHIP-FOUNDATION`, with direct/enumerated and nested values, invocation/application inside and after the ninth module, canonical/default/custom aliases, live owner state, SCSS/indented-Sass parity, exact provenance depth, strict diagnostics, limits, and every allocation failure. Retain tenth-and-later configuration, raw caller callables, another-parent identities, callable arguments and `@content` transport, transitive modules, `@forward`, legacy imports, `meta.load-css()`, broader resolution, and native graduation as separate measured slices. Provider removal and the package-level zero-dependency cutover remain gated on native graduation rather than deleting the exact development oracles early. Independently, `BENCH-007` still requires a dedicated non-emulated Linux x64 runner with labels `[self-hosted, linux, x64, zigcss-benchmark-v1]`; its absence must not pause eligible native frontend work.
- Runtime control: model identity, single-agent execution, isolated-worktree confinement, automatic supervisor-owned green-checkpoint pushes, the owner-bounded post-`NATIVE-009` GitHub-prerelease/npm-`next` authority, and every native graduation/release interlock remain unchanged.

## Prior current-work evidence (seventh-module callable configuration checkpoint)

- Milestone: Milestone 10 self-contained native stylesheet frontends, in parallel with Milestone 8 controlled benchmark evidence
- Work package: `NSASS-011` native Sass evaluation
- State: `IN_PROGRESS`
- Active phase: the two-hundred-forty-third native Sass semantic checkpoint, `NSASS-011-LOCAL-USE-SEVENTH-MODULE-REEXPORTED-CALLABLE-CONFIGURATION-OWNERSHIP-FOUNDATION`, is `VERIFIED` at local implementation checkpoint `b5c214a929f11ae70db12054bb5c0ba0f6873da7`. A fresh Dart Sass 1.101.0 probe establishes that function references retain exact behavior through seventh- and eighth-module configuration in SCSS and indented Sass: invocation inside the configured modules, invocation after the final module, original-owner identity and live state, exact compressed CSS, and zero warnings all match. The strengthened native fixture admits only the scheduled seventh-module hop across direct and enumerated public variables, nested aggregates, module-returned values, invocation/application inside and after the seventh module, default/custom canonical aliases, original-owner identity and state, deterministic dependency order, maps, and SCSS/indented-Sass parity. Native Zig centralizes the boundary as a maximum configurable re-export depth of five: the original owner identity enters at depth zero; the middle, third, fourth, fifth, and sixth re-exports enter at depths one through five and may configure the third through seventh modules respectively; and the seventh re-export enters at depth six and remains rejected when configuring an eighth module. Every accepted aggregate is deep-owned by its destination while the stable packed original owner/callable identity remains unchanged, so calls, mixin application, inspection, content acceptance, aliases, equality, and mutable state cross the new hop without retaining evaluator-arena pointers. Callable arguments and cross-module `@content` retain their stricter depth-zero/owner-only remappers. The test-first focused Debug gate stopped at 611/614 with exactly three failures: the seventh-module positive and exhaustive-allocation paths reached the former maximum-depth-four rejection, while the no-partial diagnostic still pointed to the seventh rather than eighth module. Exact eighth-module failure now owns one diagnostic at `seventh.$exported`, leaves generated position zero, and emits no CSS; exhaustive-allocation coverage exercises the accepted seventh-module path and every allocation failure passes. Final focused native Debug/ReleaseSafe each pass 23/23 steps with 614/614 tests; full compiler Debug/ReleaseSafe each pass 60/60 steps with 1109/1109 tests; and evaluator-test-root plus preprocessor-root no-emission cross-compiles pass for Windows x64 GNU and Linux ARM64 GNU in both modes. Native contract 12/12 plus check adds only `local-use-seventh-module-reexported-callable-configuration-ownership-foundation` while retaining private native Sass rows, production unreachability, zero target package/optional/runtime dependencies, and the closed release interlock. Complete provider/reference/product matrices pass 153/153; preprocessor package policy passes 6/6 plus check over 22 runtime sources, 28 archive files, 86 locked dependencies, and 5 targets; capability-status policy passes 7/7 plus check; autonomous-runner selftest passes 59/59; and repository-wide Zig formatting, changed JavaScript syntax, all 36 tracked JSON files, and diff checks pass. The bounded sibling search confirms that eighth-and-later configuration, raw caller callables, another-parent identities, callable arguments and `@content` transport, transitive modules, `@forward`, legacy imports, `meta.load-css()` execution, broader resolution, production routing, public capability claims, native graduation, version, release interlock, tag, artifact/package publication, deployment, and every external system remain unchanged.
- Quality closure: `QUAL-001` is verified at `3eced86`; repository-wide Zig formatting debt is zero and the final local validation matrix is green
- Next eligible package: admit only the next eighth-module hop already measured against Dart Sass 1.101.0 as the smallest durable `NSASS-011-LOCAL-USE-EIGHTH-MODULE-REEXPORTED-CALLABLE-CONFIGURATION-OWNERSHIP-FOUNDATION`, with direct/enumerated and nested values, invocation/application inside and after the eighth module, canonical/default/custom aliases, live owner state, SCSS/indented-Sass parity, exact provenance depth, strict diagnostics, limits, and every allocation failure. Retain ninth-and-later configuration, raw caller callables, another-parent identities, callable arguments and `@content` transport, transitive modules, `@forward`, legacy imports, `meta.load-css()`, broader resolution, and native graduation as separate measured slices. Provider removal and the package-level zero-dependency cutover remain gated on native graduation rather than deleting the exact development oracles early. Independently, `BENCH-007` still requires a dedicated non-emulated Linux x64 runner with labels `[self-hosted, linux, x64, zigcss-benchmark-v1]`; its absence must not pause eligible native frontend work.
- Runtime control: model identity, single-agent execution, isolated-worktree confinement, automatic supervisor-owned green-checkpoint pushes, the owner-bounded post-`NATIVE-009` GitHub-prerelease/npm-`next` authority, and every native graduation/release interlock remain unchanged.

## Prior current-work evidence (sixth-module callable configuration checkpoint)

- Milestone: Milestone 10 self-contained native stylesheet frontends, in parallel with Milestone 8 controlled benchmark evidence
- Work package: `NSASS-011` native Sass evaluation
- State: `IN_PROGRESS`
- Active phase: the two-hundred-forty-second native Sass semantic checkpoint, `NSASS-011-LOCAL-USE-SIXTH-MODULE-REEXPORTED-CALLABLE-CONFIGURATION-OWNERSHIP-FOUNDATION`, is `VERIFIED` at local implementation checkpoint `6d7e4c21f9f0950f3bce3f81069d1ed02e3b718f`. A fresh Dart Sass 1.101.0 probe establishes that function references retain exact behavior through sixth- and seventh-module configuration in SCSS and indented Sass: invocation inside the configured modules, invocation after the final module, original-owner identity and live state, exact compressed CSS, and zero warnings all match. The strengthened native fixture admits only the scheduled sixth-module hop across direct and enumerated public variables, nested aggregates, module-returned values, invocation/application inside and after the sixth module, default/custom canonical aliases, original-owner identity and state, deterministic dependency order, maps, and SCSS/indented-Sass parity. Native Zig centralizes the boundary as a maximum configurable re-export depth of four: the original owner identity enters at depth zero; the middle, third, fourth, and fifth re-exports enter at depths one through four and may configure the third through sixth modules respectively; and the sixth re-export enters at depth five and remains rejected when configuring a seventh module. Every accepted aggregate is deep-owned by its destination while the stable packed original owner/callable identity remains unchanged, so calls, mixin application, inspection, content acceptance, aliases, equality, and mutable state cross the new hop without retaining evaluator-arena pointers. Callable arguments and cross-module `@content` retain their stricter depth-zero/owner-only remappers. The test-first focused Debug gate stopped at 610/613 with exactly three failures: the sixth-module positive and exhaustive-allocation paths reached the former maximum-depth-three rejection, while the no-partial diagnostic still pointed to the sixth rather than seventh module. Exact seventh-module failure now owns one diagnostic at `sixth.$exported`, leaves generated position zero, and emits no CSS; exhaustive-allocation coverage exercises the accepted sixth-module path and every allocation failure passes. Final focused native Debug/ReleaseSafe each pass 23/23 steps with 613/613 tests; full compiler Debug/ReleaseSafe each pass 60/60 steps with 1108/1108 tests; and evaluator-test-root plus preprocessor-root no-emission cross-compiles pass for Windows x64 GNU and Linux ARM64 GNU in both modes. Native contract 12/12 plus check adds only `local-use-sixth-module-reexported-callable-configuration-ownership-foundation` while retaining private native Sass rows, production unreachability, zero target package/optional/runtime dependencies, and the closed release interlock. Complete provider/reference/product matrices pass 153/153; preprocessor package policy passes 6/6 plus check over 22 runtime sources, 28 archive files, 86 locked dependencies, and 5 targets; capability-status policy passes 7/7 plus check; autonomous-runner selftest passes 59/59; and repository-wide Zig formatting, changed JavaScript syntax, all 36 tracked JSON files, and diff checks pass. The bounded sibling search confirms that seventh-and-later configuration, raw caller callables, another-parent identities, callable arguments and `@content` transport, transitive modules, `@forward`, legacy imports, `meta.load-css()` execution, broader resolution, production routing, public capability claims, native graduation, version, release interlock, tag, artifact/package publication, deployment, and every external system remain unchanged.
- Quality closure: `QUAL-001` is verified at `3eced86`; repository-wide Zig formatting debt is zero and the final local validation matrix is green
- Next eligible package: admit only the next seventh-module hop already measured against Dart Sass 1.101.0 as the smallest durable `NSASS-011-LOCAL-USE-SEVENTH-MODULE-REEXPORTED-CALLABLE-CONFIGURATION-OWNERSHIP-FOUNDATION`, with direct/enumerated and nested values, invocation/application inside and after the seventh module, canonical/default/custom aliases, live owner state, SCSS/indented-Sass parity, exact provenance depth, strict diagnostics, limits, and every allocation failure. Retain eighth-and-later configuration, raw caller callables, another-parent identities, callable arguments and `@content` transport, transitive modules, `@forward`, legacy imports, `meta.load-css()`, broader resolution, and native graduation as separate measured slices. Provider removal and the package-level zero-dependency cutover remain gated on native graduation rather than deleting the exact development oracles early. Independently, `BENCH-007` still requires a dedicated non-emulated Linux x64 runner with labels `[self-hosted, linux, x64, zigcss-benchmark-v1]`; its absence must not pause eligible native frontend work.
- Runtime control: model identity, single-agent execution, isolated-worktree confinement, automatic supervisor-owned green-checkpoint pushes, the owner-bounded post-`NATIVE-009` GitHub-prerelease/npm-`next` authority, and every native graduation/release interlock remain unchanged.

## Prior current-work evidence (fifth-module callable configuration checkpoint)

- Milestone: Milestone 10 self-contained native stylesheet frontends, in parallel with Milestone 8 controlled benchmark evidence
- Work package: `NSASS-011` native Sass evaluation
- State: `IN_PROGRESS`
- Active phase: the two-hundred-forty-first native Sass semantic checkpoint, `NSASS-011-LOCAL-USE-FIFTH-MODULE-REEXPORTED-CALLABLE-CONFIGURATION-OWNERSHIP-FOUNDATION`, is `VERIFIED` at local implementation checkpoint `fa83d74af2e35e5c2fa83b5d9fa9e3bcc5b0b35d`. A fresh Dart Sass 1.101.0 probe establishes that function references retain exact behavior through fifth- and sixth-module configuration in SCSS and indented Sass: invocation inside both configured modules, invocation after the sixth module, original-owner identity and live state, exact compressed CSS, and zero warnings all match. The strengthened native fixture admits only the scheduled fifth-module hop across direct and enumerated public variables, nested aggregates, module-returned values, invocation/application inside and after the fifth module, default/custom canonical aliases, original-owner identity and state, deterministic dependency order, maps, and SCSS/indented-Sass parity. Native Zig centralizes the boundary as a maximum configurable re-export depth of three: the original owner identity enters at depth zero; the middle, third, and fourth re-exports enter at depths one, two, and three and may configure the third, fourth, and fifth modules respectively; and the fifth re-export enters at depth four and remains rejected when configuring a sixth module. Every accepted aggregate is deep-owned by its destination while the stable packed original owner/callable identity remains unchanged, so calls, mixin application, inspection, content acceptance, aliases, equality, and mutable state cross the new hop without retaining evaluator-arena pointers. Callable arguments and cross-module `@content` retain their stricter depth-zero/owner-only remappers. The test-first focused Debug gate stopped at 609/612 with exactly three failures: the fifth-module positive and exhaustive-allocation paths reached the former maximum-depth-two rejection, while the no-partial diagnostic still pointed to the fifth rather than sixth module. Exact sixth-module failure now owns one diagnostic at `fifth.$exported`, leaves generated position zero, and emits no CSS; exhaustive-allocation coverage exercises the accepted fifth-module path and every allocation failure passes. Final focused native Debug/ReleaseSafe each pass 23/23 steps with 612/612 tests; full compiler Debug/ReleaseSafe each pass 60/60 steps with 1107/1107 tests; and evaluator-test-root plus preprocessor-root no-emission cross-compiles pass for Windows x64 GNU and Linux ARM64 GNU in both modes. Native contract 12/12 plus check adds only `local-use-fifth-module-reexported-callable-configuration-ownership-foundation` while retaining private native Sass rows, production unreachability, zero target package/optional/runtime dependencies, and the closed release interlock. Complete provider/reference/product matrices pass 153/153; preprocessor package policy passes 6/6 plus check over 22 runtime sources, 28 archive files, 86 locked dependencies, and 5 targets; capability-status policy passes 7/7 plus check; autonomous-runner selftest passes 59/59; and repository-wide Zig formatting, changed JavaScript syntax, all 36 tracked JSON files, and diff checks pass. The bounded sibling search confirms that sixth-and-later configuration, raw caller callables, another-parent identities, callable arguments and `@content` transport, transitive modules, `@forward`, legacy imports, `meta.load-css()` execution, broader resolution, production routing, public capability claims, native graduation, version, release interlock, tag, artifact/package publication, deployment, and every external system remain unchanged.
- Quality closure: `QUAL-001` is verified at `3eced86`; repository-wide Zig formatting debt is zero and the final local validation matrix is green
- Next eligible package: measure and admit only the next sixth-module hop as the smallest durable `NSASS-011-LOCAL-USE-SIXTH-MODULE-REEXPORTED-CALLABLE-CONFIGURATION-OWNERSHIP-FOUNDATION`, with direct/enumerated and nested values, invocation/application inside and after the sixth module, canonical/default/custom aliases, live owner state, SCSS/indented-Sass parity, exact provenance depth, strict diagnostics, limits, and every allocation failure. Retain seventh-and-later configuration, raw caller callables, another-parent identities, callable arguments and `@content` transport, transitive modules, `@forward`, legacy imports, `meta.load-css()`, broader resolution, and native graduation as separate measured slices. Provider removal and the package-level zero-dependency cutover remain gated on native graduation rather than deleting the exact development oracles early. Independently, `BENCH-007` still requires a dedicated non-emulated Linux x64 runner with labels `[self-hosted, linux, x64, zigcss-benchmark-v1]`; its absence must not pause eligible native frontend work.
- Runtime control: model identity, single-agent execution, isolated-worktree confinement, automatic supervisor-owned green-checkpoint pushes, the owner-bounded post-`NATIVE-009` GitHub-prerelease/npm-`next` authority, and every native graduation/release interlock remain unchanged.

## Prior current-work evidence (recursive callable configuration checkpoint)

- Milestone: Milestone 10 self-contained native stylesheet frontends, in parallel with Milestone 8 controlled benchmark evidence
- Work package: `NSASS-011` native Sass evaluation
- State: `IN_PROGRESS`
- Active phase: the two-hundred-fortieth native Sass semantic checkpoint, `NSASS-011-LOCAL-USE-RECURSIVE-REEXPORTED-CALLABLE-CONFIGURATION-OWNERSHIP-FOUNDATION`, is `VERIFIED` at local implementation checkpoint `f174916fb2c516e454148200ec20ca8575da0cd8`. A fresh Dart Sass 1.101.0 probe establishes that function and mixin references retain exact behavior through recursive fourth- and fifth-module configuration in SCSS and indented Sass: default/custom canonical aliases, invocation and application inside and after the configured modules, original-owner equality and live state, dependency order, exact compressed CSS, and zero warnings all match. The strengthened native fixture admits only the scheduled fourth-module hop across direct and enumerated public variables, deeply nested aggregates, module-returned values, invocation/application inside and after the fourth module, default/custom canonical aliases, original-owner identity and state, dependency order, maps, and SCSS/indented-Sass parity. Native Zig centralizes the boundary as a maximum configurable re-export depth of two: the original owner identity enters at depth zero, the middle and third re-exports enter at depths one and two and may configure the third and fourth modules respectively, and the fourth re-export enters at depth three and remains rejected when configuring a fifth module. Every accepted aggregate is deep-owned by its destination while the stable packed original owner/callable identity remains unchanged, so calls, mixin application, inspection, content acceptance, aliases, equality, and mutable state cross the new hop without retaining evaluator-arena pointers. Callable arguments and cross-module `@content` retain their stricter depth-zero/owner-only remappers. The test-first focused Debug gate stopped at 608/611 with exactly three failures: the fourth-module positive and exhaustive-allocation paths reached the former depth-two rejection, while the no-partial diagnostic still pointed to the fourth rather than fifth module. Exact fifth-module failure now owns one diagnostic at `fourth.$exported`, leaves generated position zero, and emits no CSS; exhaustive-allocation coverage exercises the accepted fourth-module path and every allocation failure passes. Final focused native Debug/ReleaseSafe each pass 23/23 steps with 611/611 tests; full compiler Debug/ReleaseSafe each pass 60/60 steps with 1106/1106 tests; and evaluator-test-root plus preprocessor-root no-emission cross-compiles pass for Windows x64 GNU and Linux ARM64 GNU in both modes. Native contract 12/12 plus check adds only `local-use-recursive-reexported-callable-configuration-ownership-foundation` while retaining private native Sass rows, production unreachability, zero target package/optional/runtime dependencies, and the closed release interlock. Complete provider/reference/product matrices pass 153/153; preprocessor package policy passes 6/6 plus check over 22 runtime sources, 28 archive files, 86 locked dependencies, and 5 targets; capability-status policy passes 7/7 plus check; autonomous-runner selftest passes 59/59; and repository-wide Zig formatting, changed JavaScript syntax, all 36 tracked JSON files, and diff checks pass. The bounded sibling search confirms that fifth-and-later configuration, raw caller callables, another-parent identities, callable arguments and `@content` transport, transitive modules, `@forward`, legacy imports, `meta.load-css()` execution, broader resolution, production routing, public capability claims, native graduation, version, release interlock, tag, artifact/package publication, deployment, and every external system remain unchanged.
- Quality closure: `QUAL-001` is verified at `3eced86`; repository-wide Zig formatting debt is zero and the final local validation matrix is green
- Next eligible package: measure and admit only the next fifth-module hop as the smallest durable `NSASS-011-LOCAL-USE-FIFTH-MODULE-REEXPORTED-CALLABLE-CONFIGURATION-OWNERSHIP-FOUNDATION`, with direct/enumerated and nested values, invocation/application inside and after the fifth module, canonical/default/custom aliases, live owner state, SCSS/indented-Sass parity, exact provenance depth, strict diagnostics, limits, and every allocation failure. Retain sixth-and-later configuration, raw caller callables, another-parent identities, callable arguments and `@content` transport, transitive modules, `@forward`, legacy imports, `meta.load-css()`, broader resolution, and native graduation as separate measured slices. Provider removal and the package-level zero-dependency cutover remain gated on native graduation rather than deleting the exact development oracles early. Independently, `BENCH-007` still requires a dedicated non-emulated Linux x64 runner with labels `[self-hosted, linux, x64, zigcss-benchmark-v1]`; its absence must not pause eligible native frontend work.
- Runtime control: model identity, single-agent execution, isolated-worktree confinement, automatic supervisor-owned green-checkpoint pushes, the owner-bounded post-`NATIVE-009` GitHub-prerelease/npm-`next` authority, and every native graduation/release interlock remain unchanged.

## Prior current-work evidence (reexported callable configuration checkpoint)

- Milestone: Milestone 10 self-contained native stylesheet frontends, in parallel with Milestone 8 controlled benchmark evidence
- Work package: `NSASS-011` native Sass evaluation
- State: `IN_PROGRESS`
- Active phase: the two-hundred-thirty-ninth native Sass semantic checkpoint, `NSASS-011-LOCAL-USE-REEXPORTED-CALLABLE-CONFIGURATION-OWNERSHIP-FOUNDATION`, is `VERIFIED` at local implementation checkpoint `7eaae26499cdcec71689e77283ff7af202bb9669`. Fresh Dart Sass 1.101.0 probes establish configuring a later third sibling from function and mixin identities re-exported by the configured middle module: direct and enumerated public variables, nested aggregates, invocation/application inside and after the third module, middle and third default/custom canonical aliases, identity equality with the original owner, live owner state, dependency order, exact compressed CSS, zero warnings, and SCSS/indented-Sass parity. Separate bounded provider probes confirm that Dart Sass accepts the configuration chain through fourth and fifth modules; this checkpoint intentionally admits only the scheduled third-sibling hop. Native Zig replaces the former boolean provenance bit with a bounded non-semantic re-export depth. The original owner export enters the retained parent at depth zero, the configured middle re-export enters at depth one and may configure the third module, and the third re-export enters at depth two and remains rejected when configuring a fourth module. Every accepted aggregate is deep-owned by the destination while the stable packed original owner/callable identity remains unchanged, so invocation, application, inspection, content acceptance, aliases, equality, and mutable owner state resolve through the exact owner without retaining evaluator-arena pointers. Callable arguments and cross-module `@content` retain their stricter depth-zero/owner-only remappers. The test-first focused Debug gate stopped at 609/611 with the new positive path reaching the former unsupported boundary and the recursive diagnostic moving from the third to the fourth module. Exact fourth-module failure still owns one diagnostic and emits no partial CSS; strengthened exhaustive-allocation coverage exercises the accepted third-module path and every allocation failure passes. Final focused native Debug/ReleaseSafe each pass 23/23 steps with 611/611 tests; full compiler Debug/ReleaseSafe each pass 60/60 steps with 1106/1106 tests; and evaluator-root plus preprocessor-root no-emission cross-compiles pass for Windows x64 GNU and Linux ARM64 GNU in both modes. Native contract 12/12 plus check adds only `local-use-reexported-callable-configuration-ownership-foundation` while retaining private native Sass rows, production unreachability, zero target package/optional/runtime dependencies, and the closed release interlock. Complete provider/reference/product matrices pass 153/153; preprocessor package policy passes 6/6 plus check over 22 runtime sources, 28 archive files, 86 locked dependencies, and 5 targets; capability-status policy passes 7/7 plus check; autonomous-runner selftest passes 59/59; and repository-wide Zig formatting, changed JavaScript syntax, all 36 tracked JSON files, and diff checks pass. The bounded sibling search confirms that recursive fourth-module configuration, raw caller callables, another-parent identities, callable arguments and `@content` transport, transitive modules, `@forward`, legacy imports, `meta.load-css()` execution, broader resolution, production routing, public capability claims, native graduation, version, release interlock, tag, artifact/package publication, deployment, and every external system remain unchanged.
- Quality closure: `QUAL-001` is verified at `3eced86`; repository-wide Zig formatting debt is zero and the final local validation matrix is green
- Next eligible package: measure and admit only the next recursive fourth-module hop as the smallest durable `NSASS-011-LOCAL-USE-RECURSIVE-REEXPORTED-CALLABLE-CONFIGURATION-OWNERSHIP-FOUNDATION`, with direct/enumerated and nested values, invocation/application inside and after the fourth module, canonical/default/custom aliases, live owner state, SCSS/indented-Sass parity, exact provenance depth, strict diagnostics, limits, and every allocation failure. Retain fifth-and-later configuration, raw caller callables, another-parent identities, callable arguments and `@content` transport, transitive modules, `@forward`, legacy imports, `meta.load-css()`, broader resolution, and native graduation as separate measured slices. Provider removal and the package-level zero-dependency cutover remain gated on native graduation rather than deleting the exact development oracles early. Independently, `BENCH-007` still requires a dedicated non-emulated Linux x64 runner with labels `[self-hosted, linux, x64, zigcss-benchmark-v1]`; its absence must not pause eligible native frontend work.
- Runtime control: model identity, single-agent execution, isolated-worktree confinement, automatic supervisor-owned green-checkpoint pushes, the owner-bounded post-`NATIVE-009` GitHub-prerelease/npm-`next` authority, and every native graduation/release interlock remain unchanged.

## Prior current-work evidence (configured callable re-export checkpoint)

- Milestone: Milestone 10 self-contained native stylesheet frontends, in parallel with Milestone 8 controlled benchmark evidence
- Work package: `NSASS-011` native Sass evaluation
- State: `IN_PROGRESS`
- Active phase: the two-hundred-thirty-eighth native Sass semantic checkpoint, `NSASS-011-LOCAL-USE-CONFIGURED-CALLABLE-REEXPORT-OWNERSHIP-FOUNDATION`, is `VERIFIED` at local implementation checkpoint `5b1a29f2c079c3e3470da5d62f6c96d82778c2d9`. Fresh Dart Sass 1.101.0 probes establish one-hop configured function and mixin re-exports through direct and enumerated public variables, nested aggregates, module function results, default/custom canonical aliases, direct/reflected invocation after return, mixin application with caller content, original/re-export identity equality, live owner state, dependency order, exact compressed CSS, zero warnings, and SCSS/indented-Sass parity. Native Zig now deep-owns the complete exported aggregate into the retained parent while preserving stable packed owner/callable identity and recording non-semantic re-export provenance without retaining an evaluator-arena pointer. Calls, application, inspection, content acceptance, aliases, and Sass equality resolve through the exact original retained owner; provenance is ignored for canonical callable equality but rejected by module-configuration and callable-argument remappers. A second remapper keeps cross-module `@content` at its prior owner-only boundary. Therefore feeding an already re-exported callable into a third module fails with one exact owned diagnostic and no partial CSS, while raw caller callables, another-parent identities, transitive modules, and broader callable transport remain closed. The test-first focused gate stopped at 608/609 on the former recursive re-export rejection. Strengthened exhaustive-allocation coverage now exercises direct/enumerated configured aggregates, invocation, inspection, equality, and mixin application, and every allocation failure passes. Final focused native Debug/ReleaseSafe each pass 23/23 steps with 610/610 tests; full compiler Debug/ReleaseSafe each pass 60/60 steps with 1105/1105 tests; and evaluator-root plus preprocessor-root no-emission cross-compiles pass for Windows x64 GNU and Linux ARM64 GNU in both modes. Native contract 12/12 plus check adds only `local-use-configured-callable-reexport-ownership-foundation` while retaining private native Sass rows, production unreachability, zero target package/optional/runtime dependencies, and the closed release interlock. Complete provider/reference/product matrices pass 153/153; preprocessor package policy passes 6/6 plus check over 22 runtime sources, 28 archive files, 86 locked dependencies, and 5 targets; capability-status policy passes 7/7 plus check; autonomous-runner selftest passes 59/59; and repository-wide Zig formatting, changed JavaScript syntax, all 36 tracked JSON files, and diff checks pass. The bounded sibling search confirms that re-exported-callable configuration chaining, callable arguments and `@content` transport, transitive modules, `@forward`, legacy imports, `meta.load-css()` execution, broader resolution, production routing, public capability claims, native graduation, version, release interlock, tag, artifact/package publication, deployment, and every external system remain unchanged.
- Quality closure: `QUAL-001` is verified at `3eced86`; repository-wide Zig formatting debt is zero and the final local validation matrix is green
- Next eligible package: measure the exact Dart Sass 1.101.0 boundary for using an already re-exported callable to configure a later third sibling and select only the smallest durable `NSASS-011-LOCAL-USE-REEXPORTED-CALLABLE-CONFIGURATION-OWNERSHIP-FOUNDATION` supported by executable evidence, including direct/enumerated and nested values, invocation/application inside and after the third module, canonical/default/custom aliases, live owner state, SCSS/indented-Sass parity, exact provenance depth, strict diagnostics, limits, and every allocation failure. Admit at most the explicitly measured retained-parent hop without arena-pointer retention; keep recursively configuring a fourth module, raw caller callables, another-parent identities, callable arguments and `@content` transport, transitive modules, `@forward`, legacy imports, `meta.load-css()`, broader resolution, and native graduation separate. Provider removal and the package-level zero-dependency cutover remain gated on native graduation rather than deleting the exact development oracles early. Independently, `BENCH-007` still requires a dedicated non-emulated Linux x64 runner with labels `[self-hosted, linux, x64, zigcss-benchmark-v1]`; its absence must not pause eligible native frontend work.
- Runtime control: model identity, single-agent execution, isolated-worktree confinement, automatic supervisor-owned green-checkpoint pushes, the owner-bounded post-`NATIVE-009` GitHub-prerelease/npm-`next` authority, and every native graduation/release interlock remain unchanged.

## Prior current-work evidence (cross-module callable configuration checkpoint)

- Milestone: Milestone 10 self-contained native stylesheet frontends, in parallel with Milestone 8 controlled benchmark evidence
- Work package: `NSASS-011` native Sass evaluation
- State: `IN_PROGRESS`
- Active phase: the two-hundred-thirty-seventh native Sass semantic checkpoint, `NSASS-011-LOCAL-USE-CROSS-MODULE-CALLABLE-CONFIGURATION-OWNERSHIP-FOUNDATION`, is `VERIFIED` at local implementation checkpoint `1814902701e8e735f71dabc32cb61aa8fa9ca283`. Fresh Dart Sass 1.101.0 probes establish one-hop `@use ... with (...)` configuration containing function and mixin references from an already retained sibling module, nested lists/maps, direct and reflected invocation inside the configured module, mixin application with target-owned and caller-owned content, type/inspection/content predicates, default/custom namespace aliases, one-time canonical identity, live owner state, SCSS and indented Sass, exact compressed CSS, zero warnings, and dependency order. Native Zig now admits only packed local-module callable identities whose callable exists in the exact retained parent and whose canonical owner index precedes the configured module. Bounded remapping deep-owns each enclosing configured aggregate into the target evaluator while retaining the stable parent-owned identity; direct and reflected function invocation, mixin application, inspection, content acceptance, aliases, and shared mutable owner state resolve through that exact parent. Raw caller callables, another-parent identities, later/nonexistent owners, and recursively re-exported local callables remain fail-closed with owned diagnostics and no partial CSS. The test-first focused gate stopped at the former blanket local-callable configuration rejection. The strengthened exhaustive-allocation regression then exposed a real partial-initialization cleanup defect in shared dependency-edge cloning; the fix releases each initialized edge payload before freeing the one complete allocation, and every allocation failure now passes. Final focused native Debug/ReleaseSafe each pass 23/23 steps with 608/608 tests; full compiler Debug/ReleaseSafe each pass 60/60 steps with 1103/1103 tests; and evaluator-root plus preprocessor-root no-emission cross-compiles pass for Windows x64 GNU and Linux ARM64 GNU in both modes. Native contract 12/12 plus check adds only `local-use-cross-module-callable-configuration-ownership-foundation` while retaining private native Sass rows, production unreachability, zero target package/optional/runtime dependencies, and the closed release interlock. Complete provider/reference/product matrices pass 153/153; preprocessor package policy passes 6/6 plus check over 22 runtime sources, 28 archive files, 86 locked dependencies, and 5 targets; capability-status policy passes 7/7 plus check; autonomous-runner selftest passes 59/59; and repository-wide Zig formatting, changed JavaScript syntax, all 36 tracked JSON files, and diff checks pass. The bounded sibling search confirms that configured-callable re-exports, transitive modules, `@forward`, legacy imports, `meta.load-css()` execution, broader resolution, production routing, public capability claims, native graduation, version, release interlock, tag, artifact/package publication, deployment, and every external system remain unchanged.
- Quality closure: `QUAL-001` is verified at `3eced86`; repository-wide Zig formatting debt is zero and the final local validation matrix is green
- Next eligible package: measure the exact Dart Sass 1.101.0 one-hop configured-callable re-export boundary and select only the smallest durable `NSASS-011-LOCAL-USE-CONFIGURED-CALLABLE-REEXPORT-OWNERSHIP-FOUNDATION` supported by executable evidence, including direct and enumerated public variables, nested aggregates, invocation/application after return, canonical/default/custom aliases, live owner state, SCSS/indented-Sass parity, strict diagnostics, limits, and every allocation failure. Admit only identities that can be translated back to the configured module's exact retained parent without arena-pointer retention; keep using an already re-exported callable to configure a third module, raw caller callables, callables owned by another parent, transitive modules, `@forward`, legacy imports, `meta.load-css()`, broader resolution, and native graduation separate. Provider removal and the package-level zero-dependency cutover remain gated on native graduation rather than deleting the exact development oracles early. Independently, `BENCH-007` still requires a dedicated non-emulated Linux x64 runner with labels `[self-hosted, linux, x64, zigcss-benchmark-v1]`; its absence must not pause eligible native frontend work.
- Runtime control: model identity, single-agent execution, isolated-worktree confinement, automatic supervisor-owned green-checkpoint pushes, the owner-bounded post-`NATIVE-009` GitHub-prerelease/npm-`next` authority, and every native graduation/release interlock remain unchanged.

## Prior current-work evidence (built-in callable configuration checkpoint)

- Milestone: Milestone 10 self-contained native stylesheet frontends, in parallel with Milestone 8 controlled benchmark evidence
- Work package: `NSASS-011` native Sass evaluation
- State: `IN_PROGRESS`
- Active phase: the two-hundred-thirty-sixth native Sass semantic checkpoint, `NSASS-011-LOCAL-USE-BUILTIN-CALLABLE-CONFIGURATION-OWNERSHIP-FOUNDATION`, is `VERIFIED` at local implementation checkpoint `b2a035d0b6f2d7f1e41ab6552c33507a8fb324ea`. Fresh Dart Sass 1.101.0 probes establish one-hop `@use ... with (...)` configuration containing legacy-global and module built-in function references, built-in `meta.apply`/`meta.load-css` mixin references, nested lists/maps, direct type/inspection/content predicates, reflected function and mixin invocation, default/custom namespace aliases, one-time canonical module identity, exact legacy warnings, SCSS and indented Sass, exact compressed CSS, and dependency order. Native Zig now recursively classifies configured values and admits only `builtin_function` and `builtin_mixin` identities whose meaning is global and independent of both evaluator arenas. The existing bounded value-store clone deep-owns every surrounding aggregate into the retained module before evaluation; direct/reflected use, later unconfigured aliases, and returned configured values preserve the same stable identity. Caller user callables remain blocked by canonical `@use` placement, and local-module user function/mixin identities—including nested values—fail with the exact unsupported-feature boundary and no partial CSS. Value count/depth/owned-byte/collection, operation, callable, argument, diagnostic, and every allocation-failure ceiling remain terminal. The test-first focused Debug gate stopped at 604/607: both built-in callable paths reached the former blanket rejection, while the caller-user fixture correctly exposed the earlier canonical placement gate and was retained with that exact error classification. Final focused native Debug/ReleaseSafe each pass 23/23 steps with 607/607 tests; full compiler Debug/ReleaseSafe each pass 60/60 steps with 1102/1102 tests; and evaluator-root plus preprocessor-root no-emission cross-compiles pass for Windows x64 GNU and Linux ARM64 GNU in both modes. Native contract 12/12 plus check adds only `local-use-built-in-callable-configuration-ownership-foundation` while retaining private native Sass rows, production unreachability, zero target package/optional/runtime dependencies, and the closed release interlock. Complete provider/reference/product matrices pass 153/153; preprocessor package policy passes 6/6 plus check over 22 runtime sources, 28 archive files, 86 locked dependencies, and 5 targets; capability-status policy passes 7/7 plus check; autonomous-runner selftest passes 59/59; and repository-wide Zig formatting, changed JavaScript syntax, all 36 tracked JSON files, and diff checks pass. The bounded sibling search confirms that local-module user callable configuration, transitive modules, `@forward`, legacy imports, `meta.load-css()` execution, broader resolution, production routing, public capability claims, native graduation, version, release interlock, tag, artifact/package publication, deployment, and every external system remain unchanged.
- Quality closure: `QUAL-001` is verified at `3eced86`; repository-wide Zig formatting debt is zero and the final local validation matrix is green
- Next eligible package: measure the exact Dart Sass 1.101.0 one-hop cross-module user-callable configuration boundary and select only the smallest durable `NSASS-011-LOCAL-USE-CROSS-MODULE-CALLABLE-CONFIGURATION-OWNERSHIP-FOUNDATION` supported by executable evidence, including function/mixin references from an already retained sibling module, nested aggregates, direct/reflected use inside the configured module, canonical/default/custom aliases, one-time configuration, SCSS/indented-Sass parity, exact owner translation, strict diagnostics, limits, and every allocation failure. Admit only packed callable identities whose canonical module owner belongs to the configured evaluator's exact retained parent; keep raw caller user callables, callables owned by another parent, recursive re-exports, transitive modules, `@forward`, legacy imports, `meta.load-css()`, broader resolution, and native graduation separate. Provider removal and the package-level zero-dependency cutover remain gated on native graduation rather than deleting the exact development oracles early. Independently, `BENCH-007` still requires a dedicated non-emulated Linux x64 runner with labels `[self-hosted, linux, x64, zigcss-benchmark-v1]`; its absence must not pause eligible native frontend work.
- Runtime control: model identity, single-agent execution, isolated-worktree confinement, automatic supervisor-owned green-checkpoint pushes, the owner-bounded post-`NATIVE-009` GitHub-prerelease/npm-`next` authority, and every native graduation/release interlock remain unchanged.

## Prior current-work evidence (local configuration checkpoint)

- Milestone: Milestone 10 self-contained native stylesheet frontends, in parallel with Milestone 8 controlled benchmark evidence
- Work package: `NSASS-011` native Sass evaluation
- State: `IN_PROGRESS`
- Active phase: the two-hundred-thirty-fifth native Sass semantic checkpoint, `NSASS-011-LOCAL-USE-CONFIGURATION-FOUNDATION`, is `VERIFIED` at local implementation checkpoint `6c543caf827ac6b749f1d960ad7503f3bc3b3bb8`. Fresh Dart Sass 1.101.0 probes establish one-hop local `@use ... with (...)` behavior for public `!default` variables, default/custom namespaces, canonical URL aliases, caller-scope evaluation before module loading, nested values, null/default interaction, first-load CSS order, one-time configuration, repeated configuration, unknown/non-default/private/duplicate variables, malformed lists, SCSS and indented Sass, exact compressed CSS, dependency identity, and warning/error behavior. Native Zig now parses a bounded named configuration list, evaluates every value exactly once in caller source order, rejects callable-bearing values, validates only public top-level module variables declared with `!default`, deep-owns the admitted values into the retained module store, and injects them before the module's single evaluation. Configured null retains canonical `!default` fallback behavior; canonical aliases share one configured module and dependency; later unconfigured aliases are allowed; and any later configuration attempt fails. Private, unknown, non-default, duplicate/normalized-duplicate, built-in-module, callable, positional, empty, malformed, or over-limit configuration fails with owned diagnostics and no partial CSS. The test-first focused Debug regression stopped at 603/606 on the former malformed-`@use` boundary. Final focused native Debug/ReleaseSafe each pass 23/23 steps with 606/606 tests; full compiler Debug/ReleaseSafe each pass 60/60 steps with 1101/1101 tests; and evaluator-root plus preprocessor-root no-emission cross-compiles pass for Windows x64 GNU and Linux ARM64 GNU in both modes. Native contract 12/12 plus check adds only `local-use-configuration-foundation` while retaining private native Sass rows, production unreachability, zero target package/optional/runtime dependencies, and the closed release interlock. Complete provider/reference/product matrices pass 153/153; preprocessor package policy passes 6/6 plus check over 22 runtime sources, 28 archive files, 86 locked dependencies, and 5 targets; capability-status policy passes 7/7 plus check; autonomous-runner selftest passes 59/59; and repository-wide Zig formatting, changed JavaScript syntax, all 36 tracked JSON files, and diff checks pass. The bounded sibling search confirms that callable-valued configuration beyond the explicit rejection, transitive modules, `@forward`, legacy imports, `meta.load-css()`, broader resolution, production routing, public capability claims, native graduation, version, release interlock, tag, artifact/package publication, deployment, and every external system remain unchanged.
- Quality closure: `QUAL-001` is verified at `3eced86`; repository-wide Zig formatting debt is zero and the final local validation matrix is green
- Next eligible package: measure the exact Dart Sass 1.101.0 one-hop built-in callable configuration boundary and select only the smallest durable `NSASS-011-LOCAL-USE-BUILTIN-CALLABLE-CONFIGURATION-OWNERSHIP-FOUNDATION` supported by executable evidence, including global and module function/mixin references, nested aggregates, direct and reflected use inside the configured module, default/custom namespace aliases, one-time configuration, SCSS/indented-Sass parity, strict diagnostics, value/depth/count/collection/operation/callable limits, and every allocation failure. Admit only globally owned built-in identities whose lifetime and meaning remain independent of both evaluator arenas; keep caller user callables, other-local-module callables, recursively re-exported callables, transitive modules, `@forward`, legacy imports, `meta.load-css()`, broader resolution, and native graduation separate. Provider removal and the package-level zero-dependency cutover remain gated on native graduation rather than deleting the exact development oracles early. Independently, `BENCH-007` still requires a dedicated non-emulated Linux x64 runner with labels `[self-hosted, linux, x64, zigcss-benchmark-v1]`; its absence must not pause eligible native frontend work.
- Runtime control: model identity, single-agent execution, isolated-worktree confinement, automatic supervisor-owned green-checkpoint pushes, the owner-bounded post-`NATIVE-009` GitHub-prerelease/npm-`next` authority, and every native graduation/release interlock remain unchanged.

## Prior current-work evidence (local callable result ownership checkpoint)

- Milestone: Milestone 10 self-contained native stylesheet frontends, in parallel with Milestone 8 controlled benchmark evidence
- Work package: `NSASS-011` native Sass evaluation
- State: `IN_PROGRESS`
- Active phase: the two-hundred-thirty-fourth native Sass semantic checkpoint, `NSASS-011-LOCAL-USE-CALLABLE-RESULT-OWNERSHIP-FOUNDATION`, is `VERIFIED` at local implementation checkpoint `05efba1fc0030c6494d7249f53d2b5f688cf4c2d`. Fresh Dart Sass 1.101.0 probes establish one-hop module results containing local and built-in function/mixin references, nested aggregates, direct and reflected calls, safe argument passthrough, canonical URL/namespace aliases, invocation/application after return, SCSS and indented Sass, exact compressed CSS, and zero warnings. Native Zig now deep-owns those results into the caller value store through the existing bounded export remapper: retained-module user function/mixin identities become stable packed canonical-module/callable owners, built-in identities remain global, and nested collections preserve those translations without retaining an arena pointer. Already-packed local-module identities remain an explicit unsupported recursive re-export boundary, and any result whose owner cannot be mapped fails with one exact owned diagnostic and no partial CSS. Direct qualified calls and `meta.call()` share the same module invocation/result path; value/depth/count/collection/temporary-byte/operation/callable ceilings and every allocation failure remain terminal. The test-first focused Debug regression stopped at the former blanket callable-result rejection. Final focused native Debug/ReleaseSafe each pass 23/23 steps with 603/603 tests; full compiler Debug/ReleaseSafe each pass 60/60 steps with 1098/1098 tests; and evaluator-root plus preprocessor-root no-emission cross-compiles pass for Windows x64 GNU and Linux ARM64 GNU in both modes. Native contract 12/12 plus check adds only `local-use-callable-result-ownership-foundation` while retaining private native Sass rows, production unreachability, zero target package/optional/runtime dependencies, and the closed release interlock. Complete provider/reference/product matrices pass 153/153; preprocessor package policy passes 6/6 plus check over 22 runtime sources, 28 archive files, 86 locked dependencies, and 5 targets; autonomous-runner selftest passes 59/59; and repository-wide Zig formatting, changed JavaScript syntax, all 36 tracked JSON files, and diff checks pass. The bounded sibling search confirms that configured/transitive modules, recursive callable re-exports, `@forward`, legacy imports, `meta.load-css()`, broader resolution, production routing, public capability claims, native graduation, version, release interlock, tag, artifact/package publication, deployment, and every external system remain unchanged.
- Quality closure: `QUAL-001` is verified at `3eced86`; repository-wide Zig formatting debt is zero and the final local validation matrix is green
- Next eligible package: measure the exact Dart Sass 1.101.0 one-hop local `@use ... with (...)` boundary and select only the smallest durable `NSASS-011-LOCAL-USE-CONFIGURATION-FOUNDATION` supported by executable evidence, including configured public `!default` variables, default/custom namespaces, canonical URL identity, evaluation and source order, one-time configuration and reconfiguration behavior, SCSS/indented-Sass parity, strict diagnostics, limits, and allocation failure. Do not admit private, unknown, non-default, repeated, caller-owned callable, other-module-owned callable, or recursively re-exported configuration unless its canonical ownership and provider semantics remain unambiguous; keep `@forward`, legacy imports, `meta.load-css()`, transitive modules, broader resolution, native graduation, and every unsafe configuration form separate. Provider removal and the package-level zero-dependency cutover remain gated on native graduation rather than deleting the exact development oracles early. Independently, `BENCH-007` still requires a dedicated non-emulated Linux x64 runner with labels `[self-hosted, linux, x64, zigcss-benchmark-v1]`; its absence must not pause eligible native frontend work.
- Runtime control: model identity, single-agent execution, isolated-worktree confinement, automatic supervisor-owned green-checkpoint pushes, the owner-bounded post-`NATIVE-009` GitHub-prerelease/npm-`next` authority, and every native graduation/release interlock remain unchanged.

## Prior current-work evidence (local callable argument ownership checkpoint)

- Milestone: Milestone 10 self-contained native stylesheet frontends, in parallel with Milestone 8 controlled benchmark evidence
- Work package: `NSASS-011` native Sass evaluation
- State: `IN_PROGRESS`
- Active phase: the two-hundred-thirty-third native Sass semantic checkpoint, `NSASS-011-LOCAL-USE-CALLABLE-ARGUMENT-OWNERSHIP-FOUNDATION`, is `VERIFIED` at local implementation checkpoint `baf420c03e3a0245e8ae12a7edf92041b5c64ee4`. Fresh Dart Sass 1.101.0 probes establish one-hop callable-valued module arguments for target-module local function/mixin references, built-in references, caller-owned references, other-module references, nested aggregates, positional/named/map-splat binding, direct and reflected functions/mixins, module-to-caller `@content` round trips, SCSS and indented Sass, exact compressed CSS, and zero warnings. Native Zig now admits only the identities whose owner remains unambiguous: built-ins retain their global identity, while a callable packed for the exact destination module is recursively translated back to that module's user-function/mixin identity. The retained module records its exact parent engine and canonical parent module index so caller-content arguments recursively reverse that translation without retaining an arena pointer. Caller-owned and other-module callables, including nested values, fail with one exact owned unsupported-feature diagnostic and no CSS; callable-valued results retain their separate explicit rejection. Positional, keyword, and splat-keyword cloning share the bounded remapper, and value/depth/count/collection/operation/callable ceilings plus every allocation failure remain terminal. The test-first focused Debug gate stopped at 600/601 on the former blanket callable-argument rejection. Final focused native Debug/ReleaseSafe each pass 23/23 steps with 602/602 tests; full compiler Debug/ReleaseSafe each pass 60/60 steps with 1097/1097 tests; and evaluator-root plus preprocessor-root no-emission cross-compiles pass for Windows x64 GNU and Linux ARM64 GNU in both modes. Native contract 12/12 plus check adds only `local-use-callable-argument-ownership-foundation` while retaining private native Sass rows, production unreachability, zero target package/optional/runtime dependencies, and the closed release interlock. Complete provider/reference/product matrices pass 153/153; preprocessor package policy passes 6/6 plus check over 22 runtime sources, 28 archive files, 86 locked dependencies, and 5 targets; autonomous-runner selftest passes 59/59; and repository-wide Zig formatting, changed JavaScript syntax, all 36 tracked JSON files, and diff checks pass. The bounded sibling search confirms that callable-valued module results, `@forward`, legacy imports, `meta.load-css()`, transitive/configured modules, broader resolution, production routing, public capability claims, native graduation, version, release interlock, tag, artifact/package publication, deployment, and every external system remain unchanged.
- Quality closure: `QUAL-001` is verified at `3eced86`; repository-wide Zig formatting debt is zero and the final local validation matrix is green
- Next eligible package: measure the exact Dart Sass 1.101.0 one-hop callable-valued module-result boundary and select only the smallest durable `NSASS-011-LOCAL-USE-CALLABLE-RESULT-OWNERSHIP-FOUNDATION` supported by executable evidence, including module-owned local and built-in function/mixin references, nested aggregates, direct/reflected function calls, safe argument passthrough, canonical URL/namespace aliases, invocation/application after return, SCSS/indented-Sass parity, owner translation into the caller arena, strict diagnostics, limits, and allocation failure. Do not admit caller-owned, other-module-owned, or recursively re-exported callable data unless its canonical owner remains unambiguous; keep `@forward`, legacy imports, `meta.load-css()`, transitive/configured modules, broader resolution, native graduation, and every unsafe callable form separate. Provider removal and the package-level zero-dependency cutover remain gated on native graduation rather than deleting the exact development oracles early. Independently, `BENCH-007` still requires a dedicated non-emulated Linux x64 runner with labels `[self-hosted, linux, x64, zigcss-benchmark-v1]`; its absence must not pause eligible native frontend work.
- Runtime control: model identity, single-agent execution, isolated-worktree confinement, automatic supervisor-owned green-checkpoint pushes, the owner-bounded post-`NATIVE-009` GitHub-prerelease/npm-`next` authority, and every native graduation/release interlock remain unchanged.

## Prior current-work evidence (local callable variable ownership checkpoint)

- Milestone: Milestone 10 self-contained native stylesheet frontends, in parallel with Milestone 8 controlled benchmark evidence
- Work package: `NSASS-011` native Sass evaluation
- State: `IN_PROGRESS`
- Active phase: the two-hundred-thirty-second native Sass semantic checkpoint, `NSASS-011-LOCAL-USE-CALLABLE-VARIABLE-OWNERSHIP-FOUNDATION`, is `VERIFIED` at local implementation checkpoint `fa3bbedd6be0ef3aea1b6943187c687f74b6bf34`. Fresh Dart Sass 1.101.0 probes establish one-hop callable-valued public module variables for local and built-in function/mixin references, direct qualified reads and `meta.module-variables()` maps, nested list/map containment, canonical URL and namespace aliases, reassignment snapshots, default/custom namespaces, invocation/application, type/inspection/equality, SCSS and indented Sass, exact compressed CSS, and zero warnings; retained variable-enumeration evidence keeps private members absent. Native Zig now deep-owns those values into the caller store while recursively translating module-owned user function/mixin identities to their canonical packed module/callable owner and preserving built-in identities. Direct reads and enumeration maps share that bounded translation, aliases compare equal, nested callables remain invocable/applicable, and earlier maps remain immutable snapshots after later module-variable reassignment. Retained local modules may load only the closed built-in `sass:*` set needed to construct built-in references; local transitive modules remain rejected with the existing exact diagnostic. Already-translated local-module identities cannot be re-exported, so unsafe transitive ownership stays fail-closed, and caller-to-module callable arguments/results retain their existing explicit rejection. Value/depth/count/collection/temporary-byte/operation/callable limits and every allocation failure remain terminal. The test-first focused Debug gate stopped at 599/600 on the former local-module built-in-use boundary. Final focused native Debug/ReleaseSafe each pass 23/23 steps with 600/600 tests; full compiler Debug/ReleaseSafe each pass 60/60 steps with 1095/1095 tests; and evaluator-root plus preprocessor-root no-emission cross-compiles pass for Windows x64 GNU and Linux ARM64 GNU in both modes. Native contract 12/12 plus check adds only `local-use-callable-variable-ownership-foundation` while retaining private native Sass rows, production unreachability, zero target package/optional/runtime dependencies, and the closed release interlock. Complete provider/reference/product matrices pass 153/153; preprocessor package policy passes 6/6 plus check over 22 runtime sources, 28 archive files, 86 locked dependencies, and 5 targets; autonomous-runner selftest passes 59/59; and repository-wide Zig formatting, changed JavaScript syntax, tracked JSON parsing, and diff checks pass. The bounded sibling search confirms that callable-valued module arguments/results, `@forward`, legacy imports, `meta.load-css()`, transitive/configured modules, broader resolution, production routing, public capability claims, native graduation, version, release interlock, tag, artifact/package publication, deployment, and every external system remain unchanged.
- Quality closure: `QUAL-001` is verified at `3eced86`; repository-wide Zig formatting debt is zero and the final local validation matrix is green
- Next eligible package: measure the exact Dart Sass 1.101.0 one-hop callable-valued module-argument boundary and select only the smallest durable `NSASS-011-LOCAL-USE-CALLABLE-ARGUMENT-OWNERSHIP-FOUNDATION` supported by executable evidence, including module-owned local function/mixin references, built-in references, caller-owned user references, nested aggregates, direct/reflected calls and includes, owner translation in both directions, SCSS/indented-Sass parity, strict diagnostics, limits, and allocation failure. Do not admit caller-owned or nested callable data unless its canonical owner can remain unambiguous across evaluator arenas; keep callable-valued results, `@forward`, legacy imports, `meta.load-css()`, transitive/configured modules, broader resolution, native graduation, and every unsafe callable form separate. Provider removal and the package-level zero-dependency cutover remain gated on native graduation rather than deleting the exact development oracles early. Independently, `BENCH-007` still requires a dedicated non-emulated Linux x64 runner with labels `[self-hosted, linux, x64, zigcss-benchmark-v1]`; its absence must not pause eligible native frontend work.
- Runtime control: model identity, single-agent execution, isolated-worktree confinement, automatic supervisor-owned green-checkpoint pushes, the owner-bounded post-`NATIVE-009` GitHub-prerelease/npm-`next` authority, and every native graduation/release interlock remain unchanged.

## Prior current-work evidence (local variable enumeration checkpoint)

- Milestone: Milestone 10 self-contained native stylesheet frontends, in parallel with Milestone 8 controlled benchmark evidence
- Work package: `NSASS-011` native Sass evaluation
- State: `IN_PROGRESS`
- Active phase: the two-hundred-thirty-first native Sass semantic checkpoint, `NSASS-011-LOCAL-USE-VARIABLE-ENUMERATION-FOUNDATION`, is `VERIFIED` at local implementation checkpoint `5aa506e1037251e92be601992aec67dfeabb1e59`. Fresh Dart Sass 1.101.0 probes establish one-hop local-module variable enumeration through `meta.module-variables()` for public/private members, final reassignment, canonical first-definition order, underscore/hyphen name normalization, default/custom namespaces, canonical URL aliases, direct and reflected invocation, SCSS and indented Sass, exact nested list/map/string values, snapshot behavior across later live module mutation, exact compressed CSS, zero warnings, and exact unknown-namespace failures. Native Zig now resolves only an already-loaded local module target, walks its existing public variable inventory in canonical order, reads each final live module-owned value at call time, and deep-owns the completed map in the caller value store without mutating module state or retaining a pointer into the module arena. Canonical aliases produce equal maps before mutation, private variables remain absent, earlier maps remain immutable snapshots after a module function mutates global state, and later enumeration observes the new value. Callable-bearing values retain the existing explicit unsupported boundary rather than crossing evaluator arenas. Direct and reflected unknown namespaces own the enclosing call and fail without partial CSS; argument, collection, temporary-byte, operation, callable, and allocation ceilings remain fail-closed. The test-first focused Debug gate stopped at 596/599 on the former local-reflection rejection across the positive, collection-limit, and allocation paths. Final focused native Debug/ReleaseSafe each pass 23/23 steps with 599/599 tests; full compiler Debug/ReleaseSafe each pass 60/60 steps with 1094/1094 tests; and both evaluator-root and preprocessor-root no-emission cross-compiles pass for Windows x64 GNU and Linux ARM64 GNU in both modes. Native contract 12/12 plus check adds only `local-use-variable-enumeration-foundation` while retaining private native Sass rows, production unreachability, zero target package/optional/runtime dependencies, and the closed release interlock. Complete provider/reference/product matrices pass 153/153, including product 30/30; preprocessor package policy passes 6/6 plus check over 22 runtime sources, 28 archive files, 86 locked dependencies, and 5 targets; autonomous-runner selftest passes 59/59; and repository-wide Zig formatting, changed JavaScript syntax, tracked JSON parsing, and diff checks pass. The bounded sibling search confirms that callable-valued module variables/exports/arguments/results, dynamic/transitive/configured loading, `@forward`, legacy imports, `meta.load-css()`, broader resolution, production routing, public capability claims, native graduation, version, release interlock, tag, artifact/package publication, deployment, and every external system remain unchanged.
- Quality closure: `QUAL-001` is verified at `3eced86`; repository-wide Zig formatting debt is zero and the final local validation matrix is green
- Next eligible package: measure the exact Dart Sass 1.101.0 one-hop callable-valued module-variable boundary and select only the smallest durable `NSASS-011-LOCAL-USE-CALLABLE-VARIABLE-OWNERSHIP-FOUNDATION` supported by executable evidence, including local and built-in function/mixin references, direct qualified reads and `meta.module-variables()` maps, nested list/map containment, reassignment snapshots, default/custom namespace identity, invocation/application behavior, SCSS/indented-Sass parity, cross-evaluator identity and ownership, strict diagnostics, limits, and allocation failure. Do not admit a callable unless its canonical owner can remain unambiguous across arenas; keep `@forward`, legacy imports, `meta.load-css()`, transitive/configured modules, broader resolution, native graduation, and any still-unsafe callable form separate. Provider removal and the package-level zero-dependency cutover remain gated on native graduation rather than deleting the exact development oracles early. Independently, `BENCH-007` still requires a dedicated non-emulated Linux x64 runner with labels `[self-hosted, linux, x64, zigcss-benchmark-v1]`; its absence must not pause eligible native frontend work.
- Runtime control: model identity, single-agent execution, isolated-worktree confinement, automatic supervisor-owned green-checkpoint pushes, the owner-bounded post-`NATIVE-009` GitHub-prerelease/npm-`next` authority, and every native graduation/release interlock remain unchanged.

## Prior current-work evidence (local mixin enumeration checkpoint)

- Milestone: Milestone 10 self-contained native stylesheet frontends, in parallel with Milestone 8 controlled benchmark evidence
- Work package: `NSASS-011` native Sass evaluation
- State: `IN_PROGRESS`
- Active phase: the two-hundred-thirtieth native Sass semantic checkpoint, `NSASS-011-LOCAL-USE-MIXIN-ENUMERATION-FOUNDATION`, is `VERIFIED` at local implementation checkpoint `8ae034c595bf8ceffec1ba93ee7b8d67e051851e`. Fresh Dart Sass 1.101.0 probes establish one-hop local-module mixin enumeration through `meta.module-mixins()` for public/private members, final redefinitions, canonical first-definition order, underscore/hyphen name normalization, contentful/contentless references, default/custom namespaces, canonical URL aliases, direct and reflected invocation, SCSS and indented Sass, exact compressed CSS, zero warnings, and exact unknown-namespace failures. Native Zig now resolves only an already-loaded local module target, enumerates public root mixins in canonical first-definition order, binds every retained key to the final module-owned callable identity, and deep-owns the resulting map in the caller value store without evaluating a mixin, cloning a callable across arenas, or mutating module state. Canonical aliases produce equal maps, private members remain absent, and the returned references retain content acceptance and invoke through the already-verified bounded cross-evaluator path. Direct and reflected unknown namespaces own the enclosing call and fail without partial CSS; argument, collection, temporary-byte, operation, callable, and allocation ceilings remain fail-closed. The test-first focused Debug gate stopped at 594/597 on the former local-reflection rejection. The first implementation replay reached 596/597 and exposed only a collection-limit fixture whose one public mixin correctly fit the configured one-item ceiling; the fixture was strengthened to two public mixins and retained the exact limit rather than weakening the gate. Final focused native Debug/ReleaseSafe each pass 23/23 steps with 597/597 tests; full compiler Debug/ReleaseSafe each pass 60/60 steps with 1092/1092 tests; and preprocessor-root no-emission cross-compiles pass for Windows x64 GNU and Linux ARM64 GNU in both modes. Native contract 12/12 plus check adds only `local-use-mixin-enumeration-foundation` while retaining private native Sass rows, production unreachability, zero target package/optional/runtime dependencies, and the closed release interlock. Complete provider/reference/product matrices pass 153/153, including product 30/30; preprocessor package policy passes 6/6 plus check over 22 runtime sources, 28 archive files, 86 locked dependencies, and 5 targets; autonomous-runner selftest passes 59/59; and repository-wide Zig formatting, changed JavaScript syntax, tracked JSON parsing, and diff checks pass. The bounded sibling search confirms that local module variable enumeration, callable-valued module exports/variables/arguments/results beyond the returned mixin map, dynamic/transitive/configured loading, imports/forwards, production routing, public capability claims, native graduation, version, release interlock, tag, artifact/package publication, deployment, and every external system remain unchanged.
- Quality closure: `QUAL-001` is verified at `3eced86`; repository-wide Zig formatting debt is zero and the final local validation matrix is green
- Next eligible package: measure the exact Dart Sass 1.101.0 one-hop local-module variable-enumeration boundary through `meta.module-variables()` and implement only the smallest durable `NSASS-011-LOCAL-USE-VARIABLE-ENUMERATION-FOUNDATION` supported by executable evidence, including public/private and reassigned members, canonical definition order and underscore/hyphen names, default/custom namespace identity, direct and reflected enumeration, SCSS/indented-Sass behavior, exact unknown-module diagnostics, collection/argument limits, and deep caller ownership of non-callable values. Keep callable-valued module variables/exports/arguments/results, `@forward`, legacy imports, `meta.load-css()`, transitive/configured modules, broader resolution, and native graduation separate. Provider removal and the package-level zero-dependency cutover remain gated on native graduation rather than deleting the exact development oracles early. Independently, `BENCH-007` still requires a dedicated non-emulated Linux x64 runner with labels `[self-hosted, linux, x64, zigcss-benchmark-v1]`; its absence must not pause eligible native frontend work.
- Runtime control: model identity, single-agent execution, isolated-worktree confinement, automatic supervisor-owned green-checkpoint pushes, the owner-bounded post-`NATIVE-009` GitHub-prerelease/npm-`next` authority, and every native graduation/release interlock remain unchanged.

## Prior current-work evidence (local function enumeration checkpoint)

- Milestone: Milestone 10 self-contained native stylesheet frontends, in parallel with Milestone 8 controlled benchmark evidence
- Work package: `NSASS-011` native Sass evaluation
- State: `IN_PROGRESS`
- Active phase: the two-hundred-twenty-ninth native Sass semantic checkpoint, `NSASS-011-LOCAL-USE-FUNCTION-ENUMERATION-FOUNDATION`, is `VERIFIED` at local implementation checkpoint `42899a39d2abfe463157302f22b0cc947112bc25`. Fresh Dart Sass 1.101.0 probes establish one-hop local-module function enumeration through `meta.module-functions()` for public/private members, final redefinitions, canonical first-definition order, underscore/hyphen name normalization, default/custom namespaces, canonical URL aliases, direct and reflected invocation, SCSS and indented Sass, exact compressed CSS, zero warnings, and exact unknown-namespace failures. Native Zig now resolves only an already-loaded local module target, enumerates public root functions in canonical first-definition order, binds every retained key to the final module-owned callable identity, and deep-owns the resulting map in the caller value store without evaluating a function, cloning a callable across arenas, or mutating module state. Canonical aliases produce equal maps, private members remain absent, and the returned references invoke through the already-verified bounded cross-evaluator path. Direct and reflected unknown namespaces own the enclosing call and fail without partial CSS; argument, collection, temporary-byte, operation, callable, and allocation ceilings remain fail-closed. The test-first focused Debug gate stopped at 592/595 on the former local-reflection rejection. The first implementation replay reached 594/595 and exposed only a collection-limit fixture whose one public function correctly fit the configured one-item ceiling; the fixture was strengthened to two public functions and retained the exact limit rather than weakening the gate. Final focused native Debug/ReleaseSafe each pass 23/23 steps with 595/595 tests; full compiler Debug/ReleaseSafe each pass 60/60 steps with 1090/1090 tests; and preprocessor-root no-emission cross-compiles pass for Windows x64 GNU and Linux ARM64 GNU in both modes. Native contract 12/12 plus check adds only `local-use-function-enumeration-foundation` while retaining private native Sass rows, production unreachability, zero target package/optional/runtime dependencies, and the closed release interlock. Complete provider/reference/product matrices pass 153/153, including product 30/30; preprocessor package policy passes 6/6 plus check over 22 runtime sources, 28 archive files, 86 locked dependencies, and 5 targets; autonomous-runner selftest passes 59/59; and repository-wide Zig formatting, changed JavaScript syntax, tracked JSON parsing, and diff checks pass. The bounded sibling search confirms that local module mixin enumeration and variable reflection, callable-valued module exports/variables/arguments/results beyond the returned function map, dynamic/transitive/configured loading, imports/forwards, production routing, public capability claims, native graduation, version, release interlock, tag, artifact/package publication, deployment, and every external system remain unchanged.
- Quality closure: `QUAL-001` is verified at `3eced86`; repository-wide Zig formatting debt is zero and the final local validation matrix is green
- Next eligible package: measure the exact Dart Sass 1.101.0 one-hop local-module mixin-enumeration boundary through `meta.module-mixins()` and implement only the smallest durable `NSASS-011-LOCAL-USE-MIXIN-ENUMERATION-FOUNDATION` supported by executable evidence, including public/private and redefined members, canonical definition order and underscore/hyphen names, contentful/contentless callable identity, default/custom namespace identity, direct and reflected enumeration, SCSS/indented-Sass behavior, exact unknown-module diagnostics, collection/argument limits, and allocation ownership. Keep local variable reflection, callable-valued module exports/variables/arguments/results beyond the returned mixin map, `@forward`, legacy imports, `meta.load-css()`, transitive/configured modules, broader resolution, and native graduation separate. Provider removal and the package-level zero-dependency cutover remain gated on native graduation rather than deleting the exact development oracles early. Independently, `BENCH-007` still requires a dedicated non-emulated Linux x64 runner with labels `[self-hosted, linux, x64, zigcss-benchmark-v1]`; its absence must not pause eligible native frontend work.
- Runtime control: model identity, single-agent execution, isolated-worktree confinement, automatic supervisor-owned green-checkpoint pushes, the owner-bounded post-`NATIVE-009` GitHub-prerelease/npm-`next` authority, and every native graduation/release interlock remain unchanged.

## Prior current-work evidence (local mixin content inspection checkpoint)

- Milestone: Milestone 10 self-contained native stylesheet frontends, in parallel with Milestone 8 controlled benchmark evidence
- Work package: `NSASS-011` native Sass evaluation
- State: `IN_PROGRESS`
- Active phase: the two-hundred-twenty-eighth native Sass semantic checkpoint, `NSASS-011-LOCAL-USE-MIXIN-CONTENT-INSPECTION-FOUNDATION`, is `VERIFIED` at local implementation checkpoint `9a63d80ad7a20f490b25fa8c49a977e241c2e3f0`. Fresh Dart Sass 1.101.0 probes establish one-hop local-module mixin content inspection through `meta.accepts-content()` for public contentful and contentless mixins, final redefinitions, default/custom/unprefixed namespaces, canonical URL aliases, direct and reflected predicate invocation, SCSS and indented Sass, exact compressed CSS, zero warnings, and exact private, missing, unknown-namespace, ambiguous, non-mixin, and non-callable errors. Native Zig now decodes only local mixin-reference identities, validates the retained canonical module and final mixin indices, and reads the module-owned immutable `accepts_content` fact without cloning values, evaluating module code, or changing module state. Direct and reflected predicates return the same caller-owned boolean, aliases preserve identity, and last-definition semantics remain stable. Ambiguous lookup owns the inner `meta.get-mixin()` span and exact native diagnostic without partial CSS; expanded argument limits remain fail-closed; and the local-use allocation harness exhausts the new direct inspection path. The test-first focused Debug gate stopped at 591/593 on the former local-mixin-reference rejection. The first implementation replay reached 592/593 and exposed only the existing injected-allocation reclassification for an added reflected built-in function lookup; both nested and pre-bound variants reproduced that separate boundary, so the allocation fixture was narrowed to the new direct local-reference path while reflected inspection remains covered by positive SCSS and Sass tests. Final focused native Debug/ReleaseSafe each pass 23/23 steps with 593/593 tests; full compiler Debug/ReleaseSafe each pass 60/60 steps with 1088/1088 tests; and preprocessor-root no-emission cross-compiles pass for Windows x64 GNU and Linux ARM64 GNU in both modes. Native contract 12/12 plus check adds only `local-use-mixin-content-inspection-foundation` while retaining private native Sass rows, production unreachability, zero target package/optional/runtime dependencies, and the closed release interlock. Complete provider/reference/product matrices pass 153/153, including product 30/30; preprocessor package policy passes 6/6 plus check over 22 runtime sources, 28 archive files, 86 locked dependencies, and 5 targets; autonomous-runner selftest passes 59/59; and repository-wide Zig formatting, changed JavaScript syntax, tracked JSON parsing, and diff checks pass. The bounded sibling search confirms that local module function/mixin enumeration and variable reflection, callable-valued module exports/variables/arguments/results, dynamic/transitive/configured loading, imports/forwards, production routing, public capability claims, native graduation, version, release interlock, tag, artifact/package publication, deployment, and every external system remain unchanged.
- Quality closure: `QUAL-001` is verified at `3eced86`; repository-wide Zig formatting debt is zero and the final local validation matrix is green
- Next eligible package: measure the exact Dart Sass 1.101.0 one-hop local-module function-enumeration boundary through `meta.module-functions()` and implement only the smallest durable `NSASS-011-LOCAL-USE-FUNCTION-ENUMERATION-FOUNDATION` supported by executable evidence, including public/private and redefined members, canonical definition order and underscore/hyphen names, default/custom namespace identity, direct and reflected enumeration, SCSS/indented-Sass behavior, exact unknown-module diagnostics, collection/argument limits, and allocation ownership. Keep local mixin and variable enumeration, callable-valued module exports/variables/arguments/results beyond the returned function map, `@forward`, legacy imports, `meta.load-css()`, transitive/configured modules, broader resolution, and native graduation separate. Provider removal and the package-level zero-dependency cutover remain gated on native graduation rather than deleting the exact development oracles early. Independently, `BENCH-007` still requires a dedicated non-emulated Linux x64 runner with labels `[self-hosted, linux, x64, zigcss-benchmark-v1]`; its absence must not pause eligible native frontend work.
- Runtime control: model identity, single-agent execution, isolated-worktree confinement, automatic supervisor-owned green-checkpoint pushes, the owner-bounded post-`NATIVE-009` GitHub-prerelease/npm-`next` authority, and every native graduation/release interlock remain unchanged.

## Prior current-work evidence (local mixin reference application checkpoint)

- Milestone: Milestone 10 self-contained native stylesheet frontends, in parallel with Milestone 8 controlled benchmark evidence
- Work package: `NSASS-011` native Sass evaluation
- State: `IN_PROGRESS`
- Active phase: the two-hundred-twenty-seventh native Sass semantic checkpoint, `NSASS-011-LOCAL-USE-MIXIN-REFERENCE-APPLICATION-FOUNDATION`, is `VERIFIED` at local implementation checkpoint `e0763bd91bcfe396a2d454a1e75f3e570cd206c2`. Fresh Dart Sass 1.101.0 probes establish one-hop local-module mixin-reference application through `meta.apply()` for default, custom, and unprefixed namespaces; canonical URL aliases; final redefinitions; positional and named arguments; fixed-arity list and map splats; provider-compatible later-map override; mutable module state and lexical defaults; caller-owned parameterized `@content`; SCSS and indented Sass; exact compressed CSS; zero warnings; and exact missing, extra, unknown, duplicate, non-mixin, and unexpected-content errors. Native Zig now decodes only local mixin-reference identities, clones each already-evaluated non-callable argument into the retained module evaluator, and routes application through the same module-owned mixin binder/evaluator and caller-owned content bridge as direct local-module includes. Module state remains live across aliases and reflected applications, redefinitions retain last-definition semantics, and caller content continues to execute in the caller document and lexical scope. Callable-valued cross-arena arguments remain rejected, invalid binding owns the root `meta.apply()` span without partial CSS, expanded argument limits remain fail-closed, and the existing local-use allocation harness now exercises reflected application with caller content. The test-first focused Debug gate stopped at 587/591 on the former local-mixin-reference rejection. The first implementation replay reached 589/591 and exposed only a fixture that exceeded the existing rest-argument serialization boundary and generic function wording in the shared binder; the fixture was narrowed to the measured fixed-arity boundary and diagnostics were contextualized for functions versus mixins without broadening either surface. Final focused native Debug/ReleaseSafe each pass 23/23 steps with 591/591 tests; full compiler Debug/ReleaseSafe each pass 60/60 steps with 1086/1086 tests; and preprocessor-root no-emission cross-compiles pass for Windows x64 GNU and Linux ARM64 GNU in both modes. Native contract 12/12 plus check adds only `local-use-mixin-reference-application-foundation` while retaining private native Sass rows, production unreachability, zero target package/optional/runtime dependencies, and the closed release interlock. Complete provider/reference/product matrices pass 153/153, including product 30/30; preprocessor package policy passes 6/6 plus check over 22 runtime sources, 28 archive files, 86 locked dependencies, and 5 targets; autonomous-runner selftest passes 59/59; and repository-wide Zig formatting, changed JavaScript syntax, tracked JSON parsing, and diff checks pass. The bounded sibling search confirms that local mixin content inspection, module function/mixin enumeration and variable reflection, callable-valued module exports/variables/arguments/results, dynamic/transitive/configured loading, imports/forwards, production routing, public capability claims, native graduation, version, release interlock, tag, artifact/package publication, deployment, and every external system remain unchanged.
- Quality closure: `QUAL-001` is verified at `3eced86`; repository-wide Zig formatting debt is zero and the final local validation matrix is green
- Next eligible package: measure the exact Dart Sass 1.101.0 one-hop local-module mixin-reference content-inspection boundary through `meta.accepts-content()` and implement only the smallest durable `NSASS-011-LOCAL-USE-MIXIN-CONTENT-INSPECTION-FOUNDATION` supported by executable evidence, including public/redefined contentful and contentless mixins, default/custom/unprefixed reference identity, direct and reflected predicate invocation, SCSS/indented-Sass behavior, exact invalid or ambiguous lookup diagnostics, limits, and allocation ownership. Keep local function/mixin enumeration, callable-valued module exports and cross-arena arguments/results, `@forward`, legacy imports, `meta.load-css()`, transitive/configured modules, broader resolution, and native graduation separate. Provider removal and the package-level zero-dependency cutover remain gated on native graduation rather than deleting the exact development oracles early. Independently, `BENCH-007` still requires a dedicated non-emulated Linux x64 runner with labels `[self-hosted, linux, x64, zigcss-benchmark-v1]`; its absence must not pause eligible native frontend work.
- Runtime control: model identity, single-agent execution, isolated-worktree confinement, automatic supervisor-owned green-checkpoint pushes, the owner-bounded post-`NATIVE-009` GitHub-prerelease/npm-`next` authority, and every native graduation/release interlock remain unchanged.

## Prior current-work evidence (local function reference checkpoint)

- Milestone: Milestone 10 self-contained native stylesheet frontends, in parallel with Milestone 8 controlled benchmark evidence
- Work package: `NSASS-011` native Sass evaluation
- State: `IN_PROGRESS`
- Active phase: the two-hundred-twenty-sixth native Sass semantic checkpoint, `NSASS-011-LOCAL-USE-FUNCTION-REFERENCE-INVOCATION-FOUNDATION`, is `VERIFIED` at local implementation checkpoint `ad4780ebb8e2cedf4a575fdabb1999d434d03fb5`. Fresh Dart Sass 1.101.0 probes establish one-hop local-module function-reference invocation through `meta.call()` for default, custom, and unprefixed namespaces; canonical URL aliases; final redefinitions; positional and named arguments; list, map, and mixed direct/map splats; provider-compatible later-map override; mutable module state and lexical defaults; nested map/list/string return ownership; SCSS and indented Sass; exact compressed CSS; zero warnings; and exact missing, extra, unknown, duplicate, non-function, and mixin-as-function errors. Native Zig now decodes only local function-reference identities and routes them through the same bounded cross-evaluator argument clone, module-owned user-function binder/evaluator, callable-result guard, and deep clone-back path as direct local-module calls. Module state remains live across aliases and reflected calls, redefinitions retain last-definition semantics, and caller-owned aggregate results remain valid after the module invocation returns. Callable-valued cross-arena arguments and results remain rejected, invalid binding owns the root `meta.call()` span without partial CSS, expanded argument limits remain fail-closed, and the existing local-use allocation harness now exercises reflected invocation. The initial test-first focused Debug gate stopped at 584/588: two fixtures initially reached the unchanged transitive-module guard and were narrowed back to the intended one-hop boundary, while diagnostic and allocation paths reached the former reflected-invocation rejection. Final focused native Debug/ReleaseSafe each pass 23/23 steps with 588/588 tests; full compiler Debug/ReleaseSafe each pass 60/60 steps with 1083/1083 tests; and preprocessor-root no-emission cross-compiles pass for Windows x64 GNU and Linux ARM64 GNU in both modes. Native contract 12/12 plus check adds only `local-use-function-reference-invocation-foundation` while retaining private native Sass rows, production unreachability, zero target package/optional/runtime dependencies, and the closed release interlock. Complete provider/reference/product matrices pass 153/153, including product 30/30; preprocessor package policy passes 6/6 plus check over 22 runtime sources, 28 archive files, 86 locked dependencies, and 5 targets; autonomous-runner selftest passes 59/59; and repository-wide Zig formatting, changed JavaScript syntax, tracked JSON parsing, and diff checks pass. The bounded sibling search confirms that local mixin application and content inspection, module function/mixin enumeration and variable reflection, callable-valued module exports/variables/arguments/results, dynamic/transitive/configured loading, imports/forwards, production routing, public capability claims, native graduation, version, release interlock, tag, artifact/package publication, deployment, and every external system remain unchanged.
- Quality closure: `QUAL-001` is verified at `3eced86`; repository-wide Zig formatting debt is zero and the final local validation matrix is green
- Next eligible package: measure the exact Dart Sass 1.101.0 one-hop local-module first-class mixin-reference application boundary through `meta.apply()` and implement only the smallest durable `NSASS-011-LOCAL-USE-MIXIN-REFERENCE-APPLICATION-FOUNDATION` supported by executable evidence, including default/custom/unprefixed reference identity, positional/named/list/map-splat binding, module lexical state and redefinitions, caller-owned `@content` behavior, SCSS/indented-Sass behavior, exact invalid-application diagnostics, limits, and allocation ownership. Keep local callable content inspection, function/mixin enumeration, callable-valued module exports and cross-arena arguments/results, `@forward`, legacy imports, `meta.load-css()`, transitive/configured modules, broader resolution, and native graduation separate. Provider removal and the package-level zero-dependency cutover remain gated on native graduation rather than deleting the exact development oracles early. Independently, `BENCH-007` still requires a dedicated non-emulated Linux x64 runner with labels `[self-hosted, linux, x64, zigcss-benchmark-v1]`; its absence must not pause eligible native frontend work.
- Runtime control: model identity, single-agent execution, isolated-worktree confinement, automatic supervisor-owned green-checkpoint pushes, the owner-bounded post-`NATIVE-009` GitHub-prerelease/npm-`next` authority, and every native graduation/release interlock remain unchanged.

## Prior current-work evidence (local callable reference checkpoint)

- Milestone: Milestone 10 self-contained native stylesheet frontends, in parallel with Milestone 8 controlled benchmark evidence
- Work package: `NSASS-011` native Sass evaluation
- State: `IN_PROGRESS`
- Active phase: the two-hundred-twenty-fifth native Sass semantic checkpoint, `NSASS-011-LOCAL-USE-CALLABLE-REFERENCE-FOUNDATION`, is `VERIFIED` at local implementation checkpoint `ad01fe5d7c66c63144b0166c870f2b4b5cc243a5`. Fresh Dart Sass 1.101.0 probes establish one-hop local-module first-class function and mixin reference construction for public, private, missing, and redefined members; default, custom, and unprefixed namespaces; canonical identity across URL and namespace aliases; underscore/hyphen normalization; type, inspection, and equality; SCSS and indented Sass; and exact missing, unknown-namespace, competing-local, and built-in/local star-import errors. Native Zig now gives local function and mixin references distinct packed callable identities over the canonical module and final callable definition, constructs them only for public top-level members, deduplicates repeated aliases of one module, and detects every distinct unprefixed provider module before returning a reference. `meta.type-of()`, `meta.inspect()`, and equality expose stable provider-compatible behavior without cloning the callable across evaluator arenas. Private and missing references, unknown namespaces, competing local modules, and built-in/local collisions fail without CSS. Reference invocation through `meta.call()`, mixin application through `meta.apply()`, content inspection, module enumeration, callable-valued exports and cross-arena arguments/results, and every broader reflection path remain explicitly unavailable. The test-first focused Debug gate stopped at 581/585 on the former local-reference boundaries; the bounded sibling audit then added a provider-measured built-in/local collision regression that stopped at 584/585 before the lookup was corrected to accumulate candidates by canonical module identity. Final focused native Debug/ReleaseSafe each pass 23/23 steps with 585/585 tests; full compiler Debug/ReleaseSafe each pass 60/60 steps with 1080/1080 tests; and preprocessor-root no-emission cross-compiles pass for Windows x64 GNU and Linux ARM64 GNU in both modes. Native contract 12/12 plus check adds only `local-use-callable-reference-foundation` while retaining private native Sass rows, production unreachability, zero target package/optional/runtime dependencies, and the closed release interlock. Complete provider/reference/product matrices pass 153/153, the product matrix passes 30/30 again after sibling closure, preprocessor package policy passes 6/6 plus check over 22 runtime sources, 28 archive files, 86 locked dependencies, and 5 targets, autonomous-runner selftest passes 59/59, and repository-wide Zig formatting, changed JavaScript syntax, tracked JSON parsing, and diff checks pass. The bounded sibling search confirms that callable-valued module exports/variables/arguments/results remain rejected by the shared recursive callable guard; module function/mixin enumeration and variable reflection retain their local-module unsupported boundary; and dynamic/transitive/configured loading, imports/forwards, production routing, public capability claims, native graduation, version, release interlock, tag, artifact/package publication, deployment, and every external system remain unchanged.
- Quality closure: `QUAL-001` is verified at `3eced86`; repository-wide Zig formatting debt is zero and the final local validation matrix is green
- Next eligible package: measure the exact Dart Sass 1.101.0 one-hop local-module first-class function-reference invocation boundary through `meta.call()` and implement only the smallest durable `NSASS-011-LOCAL-USE-FUNCTION-REFERENCE-INVOCATION-FOUNDATION` supported by executable evidence, including default/custom/unprefixed reference identity, positional/named/list/map-splat binding, module lexical state and redefinitions, return-value ownership, SCSS/indented-Sass behavior, exact invalid-call diagnostics, limits, and allocation ownership. Keep first-class mixin application and content behavior, function/mixin enumeration, callable-valued module exports and cross-arena arguments/results, `@forward`, legacy imports, `meta.load-css()`, transitive/configured modules, broader resolution, and native graduation separate. Provider removal and the package-level zero-dependency cutover remain gated on native graduation rather than deleting the exact development oracles early. Independently, `BENCH-007` still requires a dedicated non-emulated Linux x64 runner with labels `[self-hosted, linux, x64, zigcss-benchmark-v1]`; its absence must not pause eligible native frontend work.
- Runtime control: model identity, single-agent execution, isolated-worktree confinement, automatic supervisor-owned green-checkpoint pushes, the owner-bounded post-`NATIVE-009` GitHub-prerelease/npm-`next` authority, and every native graduation/release interlock remain unchanged.

## Prior current-work evidence (local callable existence checkpoint)

- Milestone: Milestone 10 self-contained native stylesheet frontends, in parallel with Milestone 8 controlled benchmark evidence
- Work package: `NSASS-011` native Sass evaluation
- State: `IN_PROGRESS`
- Active phase: the two-hundred-twenty-fourth native Sass semantic checkpoint, `NSASS-011-LOCAL-USE-CALLABLE-EXISTENCE-FOUNDATION`, is `VERIFIED` at local implementation checkpoint `bf1ab47dc985d12c23adefa87602b861883a7b9b`. Fresh Dart Sass 1.101.0 probes establish public, private, missing, and redefined one-hop module function/mixin existence through default, custom, and unprefixed namespaces; exact underscore/hyphen and case behavior; nullable module lookup; distinct unknown-namespace and competing-unprefixed-module errors; reflected predicate invocation; and SCSS/indented-Sass parity. Native Zig now resolves only `meta.function-exists()` and `meta.mixin-exists()` module targets against retained local module engines, exposes only public top-level callable presence, and detects distinct unprefixed module ambiguity while treating aliases of one canonical module as one identity. Direct and reflected predicates share the same bounded path. Private and absent members return false; unknown namespaces and ambiguous star imports fail without CSS. Existing indented-Sass `as *` parsing remains fail-closed and separate, while default/custom indented namespaces and SCSS unprefixed lookup pass. Local `get-function()`/`get-mixin()` references, module enumeration, variable reflection, and every broader user-module reflection path retain the existing unsupported-feature boundary. The test-first focused Debug gate stopped at 578/582 on the former local-module rejection and false-result boundaries. Final focused native Debug/ReleaseSafe each pass 23/23 steps with 582/582 tests; full compiler Debug/ReleaseSafe each pass 60/60 steps with 1077/1077 tests; and preprocessor-root no-emission cross-compiles pass for Windows x64 GNU and Linux ARM64 GNU in both modes. Native contract 12/12 plus check adds only `local-use-callable-existence-foundation` while retaining private native Sass rows, production unreachability, zero target package/optional/runtime dependencies, and the closed release interlock. Complete provider/reference/product matrices pass 153/153, preprocessor package policy passes 6/6 plus check over 22 runtime sources, 28 archive files, 86 locked dependencies, and 5 targets, autonomous-runner selftest passes 59/59, and repository-wide Zig formatting, changed JavaScript syntax, tracked JSON parsing, and diff checks pass. The bounded sibling search confirms that first-class local callable references/invocation/application, local function/mixin enumeration, callable-valued cross-arena exports/arguments/results, dynamic/transitive/configured loading, imports/forwards, production routing, public capability claims, native graduation, version, release interlock, tag, artifact/package publication, deployment, and every external system remain unchanged.
- Quality closure: `QUAL-001` is verified at `3eced86`; repository-wide Zig formatting debt is zero and the final local validation matrix is green
- Next eligible package: measure the exact Dart Sass 1.101.0 one-hop local-module first-class callable-reference construction boundary and implement only the smallest durable `NSASS-011-LOCAL-USE-CALLABLE-REFERENCE-FOUNDATION` supported by executable evidence, including public/private and redefined functions/mixins, default/custom/unprefixed namespace identity, type/inspection/equality across canonical aliases, SCSS/indented-Sass behavior, exact missing/ambiguous errors, diagnostics, limits, and allocation ownership. Keep local reference invocation/application, function/mixin enumeration, callable-valued module exports and cross-arena arguments/results, `@forward`, legacy imports, `meta.load-css()`, transitive/configured modules, broader resolution, and native graduation separate. Provider removal and the package-level zero-dependency cutover remain gated on native graduation rather than deleting the exact development oracles early. Independently, `BENCH-007` still requires a dedicated non-emulated Linux x64 runner with labels `[self-hosted, linux, x64, zigcss-benchmark-v1]`; its absence must not pause eligible native frontend work.
- Runtime control: model identity, single-agent execution, isolated-worktree confinement, automatic supervisor-owned green-checkpoint pushes, the owner-bounded post-`NATIVE-009` GitHub-prerelease/npm-`next` authority, and every native graduation/release interlock remain unchanged.

## Prior current-work evidence (local callable member checkpoint)

- Milestone: Milestone 10 self-contained native stylesheet frontends, in parallel with Milestone 8 controlled benchmark evidence
- Work package: `NSASS-011` native Sass evaluation
- State: `IN_PROGRESS`
- Active phase: the two-hundred-twenty-third native Sass semantic checkpoint, `NSASS-011-LOCAL-USE-CALLABLE-MEMBER-FOUNDATION`, is `VERIFIED` at local implementation checkpoint `3905f7cba035491f66be6f0fa4f353bf590c5299`. Fresh Dart Sass 1.101.0 probes establish public/private one-hop module functions and mixins, default/custom/unprefixed namespaces, module-owned lexical variables and defaults, positional/named binding, caller-owned `@content` binding, canonical once-only state across aliases, last-definition-wins callable replacement, first-load CSS order, arithmetic/nested calls, SCSS/indented-Sass cross-loading, exact invalid/private/ambiguous access, and callable limits. Native Zig now retains each loaded module's parsed document, evaluator, environment, and value arena behind the canonical URL cache; gives top-level callables a stable root-scope owner; resolves only public top-level qualified or unprefixed functions and mixins; clones non-callable arguments/results across evaluator arenas; bridges content arguments back into the caller document and lexical scope; preserves live module-global variable state; and evaluates qualified local calls and variables inside arithmetic. Root declarations shadow unprefixed module callables, distinct unprefixed modules remain ambiguous, aliases of one canonical module share state, and redefinitions select the last declaration. Private, missing, ambiguous, malformed-binding, unexpected-content, callable-valued cross-arena, over-limit, diagnostic, transaction, and allocation paths fail without partial CSS. The test-first focused Debug gate stopped at 575/577 on the former local-module function and mixin boundaries. Final focused native Debug/ReleaseSafe each pass 23/23 steps with 579/579 tests; full compiler Debug/ReleaseSafe each pass 60/60 steps with 1074/1074 tests; and preprocessor-root no-emission cross-compiles pass for Windows x64 GNU and Linux ARM64 GNU in both modes. Native contract 12/12 plus check adds only `local-use-callable-member-foundation` while retaining private native Sass rows, production unreachability, zero target package/optional/runtime dependencies, and the closed release interlock. Complete provider/reference/product matrices pass 153/153, preprocessor package policy passes 6/6 plus check over 22 runtime sources, 28 archive files, 86 locked dependencies, and 5 targets, autonomous-runner selftest passes 59/59, and repository-wide Zig formatting, changed JavaScript syntax, tracked JSON parsing, and diff checks pass. The bounded sibling search confirms that local callable values, first-class references, module enumeration, dynamic/transitive/configured loading, imports/forwards, production routing, public capability claims, native graduation, version, release interlock, tag, artifact/package publication, deployment, and every external system remain unchanged.
- Quality closure: `QUAL-001` is verified at `3eced86`; repository-wide Zig formatting debt is zero and the final local validation matrix is green
- Next eligible package: measure the exact Dart Sass 1.101.0 one-hop local-module callable-existence boundary and implement only the smallest durable `NSASS-011-LOCAL-USE-CALLABLE-EXISTENCE-FOUNDATION` supported by executable evidence, including public/private and redefined functions/mixins, default/custom/unprefixed namespace identity, SCSS/indented-Sass behavior, exact false/error distinctions, diagnostics, limits, and allocation ownership. Keep first-class local `get-function()`/`get-mixin()` references and their invocation/application, module function/mixin enumeration, callable-valued variables/arguments/results, `@forward`, legacy imports, `meta.load-css()`, transitive/configured modules, broader resolution, and native graduation separate. Provider removal and the package-level zero-dependency cutover remain gated on native graduation rather than deleting the exact development oracles early. Independently, `BENCH-007` still requires a dedicated non-emulated Linux x64 runner with labels `[self-hosted, linux, x64, zigcss-benchmark-v1]`; its absence must not pause eligible native frontend work.
- Runtime control: model identity, single-agent execution, isolated-worktree confinement, automatic supervisor-owned green-checkpoint pushes, the owner-bounded post-`NATIVE-009` GitHub-prerelease/npm-`next` authority, and every native graduation/release interlock remain unchanged.

## Prior current-work evidence (local use loading checkpoint)

- Milestone: Milestone 10 self-contained native stylesheet frontends, in parallel with Milestone 8 controlled benchmark evidence
- Work package: `NSASS-011` native Sass evaluation
- State: `IN_PROGRESS`
- Active phase: the two-hundred-twenty-second native Sass semantic checkpoint, `NSASS-011-LOCAL-USE-SINGLE-MODULE-LOADING-FOUNDATION`, is `VERIFIED` at local implementation checkpoint `8613575032f728b0150821eb2552d8e335221d4d`. Fresh Dart Sass 1.101.0 probes establish one-hop local `@use` behavior for explicit and partial SCSS/Sass URLs, default/custom/unprefixed namespaces, first-load CSS ordering, canonical once-only evaluation across aliases, public/private variable visibility, SCSS/indented-Sass cross-loading, exact dependency identity, duplicate namespace and late-use syntax, self-cycles, missing files, and full/partial ambiguity. A sibling probe confirms that underscore-bearing default namespaces retain their exact spelling. Native Zig now derives only sibling `.scss`/`.sass` candidates, delegates every open and confinement decision to the bounded resolver session, adds loaded bytes to the shared source table, parses and evaluates the selected module in an isolated environment, clones public non-callable variable values into a canonical-URL module cache, emits module CSS only on first load, and exposes qualified or unprefixed variables without widening built-in-module behavior. Missing, ambiguous, escaping, cyclic, private, transitive, over-limit, parser-failing, and callable-bearing cases abort the transaction without partial CSS; `@forward`, legacy `@import`, `meta.load-css()`, configuration, transitive graphs, user-module callables/reflection, and broader resolution remain explicitly unavailable. The test-first focused Debug gate stopped at 571/574 on the former generic unsupported boundary. Final focused native Debug/ReleaseSafe each pass 23/23 steps with 575/575 tests; full compiler Debug/ReleaseSafe each pass 60/60 steps with 1070/1070 tests; and preprocessor-root no-emission cross-compiles pass for Windows x64 GNU and Linux ARM64 GNU in both modes. Native contract 12/12 plus check adds only `local-use-single-module-loading-foundation` while retaining private native Sass rows, production unreachability, zero target package/optional/runtime dependencies, and the closed release interlock. Complete provider/reference/product matrices pass 153/153, preprocessor package policy passes 6/6 plus check over 22 runtime sources, 28 archive files, 86 locked dependencies, and 5 targets, autonomous-runner selftest passes 59/59, and repository-wide Zig formatting, changed JavaScript syntax, tracked JSON parsing, and diff checks pass. The bounded sibling search confirms that local module targets are ignored or rejected by built-in function, mixin, and reflection paths and that production routing, public capability claims, native graduation, version, release interlock, tag, artifact/package publication, deployment, and every external system remain unchanged.
- Quality closure: `QUAL-001` is verified at `3eced86`; repository-wide Zig formatting debt is zero and the final local validation matrix is green
- Next eligible package: measure the exact Dart Sass 1.101.0 one-hop local-module callable-member boundary across public/private functions and mixins, default/custom/unprefixed namespaces, lexical module scope, argument/content binding, first-load ordering, SCSS/indented-Sass interaction, diagnostics, limits, and allocation ownership, and implement only the smallest durable `NSASS-011-LOCAL-USE-CALLABLE-MEMBER-FOUNDATION` supported by executable evidence. Keep callable reflection and enumeration, `@forward`, legacy imports, `meta.load-css()`, transitive/configured modules, broader resolution, and native graduation separate. Provider removal and the package-level zero-dependency cutover remain gated on native graduation rather than deleting the exact development oracles early. Independently, `BENCH-007` still requires a dedicated non-emulated Linux x64 runner with labels `[self-hosted, linux, x64, zigcss-benchmark-v1]`; its absence must not pause eligible native frontend work.
- Runtime control: model identity, single-agent execution, isolated-worktree confinement, automatic supervisor-owned green-checkpoint pushes, the owner-bounded post-`NATIVE-009` GitHub-prerelease/npm-`next` authority, and every native graduation/release interlock remain unchanged.

## Prior current-work evidence (string unique-id deterministic compatibility checkpoint)

- Milestone: Milestone 10 self-contained native stylesheet frontends, in parallel with Milestone 8 controlled benchmark evidence
- Work package: `NSASS-011` native Sass evaluation
- State: `IN_PROGRESS`
- Active phase: the two-hundred-twenty-first native Sass semantic checkpoint, `NSASS-011-STRING-UNIQUE-ID-DETERMINISTIC-COMPATIBILITY-DECISION`, is `VERIFIED` at local implementation checkpoint `a8fce613ef0376b7a1c12f00c535fb1742fa44ef`. Fresh Dart Sass 1.101.0 probes establish that `sass:string.unique-id()` returns an unquoted string matching `u[0-9a-z]{6}`, advances process-global randomized state across repeated calls and compilations, and produces different bytes across fresh processes and concurrent compilations. Default, aliased, and unprefixed module access; direct, list-splat, empty-map-splat, and reflected calls; SCSS and indented Sass; string typing; quoted interpolation; and exact arity behavior are executable provider evidence. The legacy global `unique-id()` and global reflection expose the same nondeterministic built-in plus the source-owned `global-builtin` deprecation. Because reproducing those bytes would violate ZigCSS's deterministic-output contract, native Zig now recognizes every loaded `sass:string` and legacy-global unique-id identity and fails closed with the exact dedicated diagnostic `native Sass string.unique-id() is unavailable because Dart Sass 1.101.0 produces nondeterministic output`; legacy direct and reflected calls first emit the already-owned global-built-in warning. No value or CSS is exposed. The regression covers default/alias/unprefixed module calls, legacy global calls, list/map splats, module/global reflection, repeated identical compilations, SCSS/indented Sass, exact severity/code/message/span/order, generated position zero, failed transaction completion, function-argument limits, and exhaustive allocation failure. The test-first native contract gate stopped at 6/12 on the absent capability and the focused Debug gate stopped at 21/23 steps with 570/571 tests on the former generic boundary. Final focused native Debug/ReleaseSafe each pass 23/23 steps with 571/571 tests; full compiler Debug/ReleaseSafe each pass 60/60 steps with 1066/1066 tests; and preprocessor-root no-emission cross-compiles pass for Windows x64 GNU and Linux ARM64 GNU in both modes. Native contract 12/12 plus check adds only `built-in-string-unique-id-deterministic-rejection` while retaining private native Sass rows, production unreachability, zero target package/optional/runtime dependencies, and the closed release interlock. Complete provider/reference/product matrices pass 153/153, preprocessor package policy passes 6/6 plus check over 22 runtime sources, 28 archive files, 86 locked dependencies, and 5 targets, autonomous-runner selftest passes 59/59, and repository-wide Zig formatting, changed JavaScript syntax, tracked JSON parsing, and diff checks pass. A sibling search confirms that qualified, unprefixed, legacy-global, splatted, and reflected callable surfaces share the explicit rejection while indented-Sass unprefixed-module parsing retains its existing fail-closed syntax boundary. User-module reflection, local/dynamic loading, imports/forwards, production routing, public capability claims, native graduation, version, release interlock, tag, artifact/package publication, deployment, and every external system remain unchanged.
- Quality closure: `QUAL-001` is verified at `3eced86`; repository-wide Zig formatting debt is zero and the final local validation matrix is green
- Next eligible package: measure the exact Dart Sass 1.101.0 one-hop local `@use` foundation for a sibling stylesheet module, including explicit and partial URLs, default/custom/unprefixed namespaces, source ordering and once-only evaluation, member visibility, SCSS/indented-Sass interaction, dependency identity, duplicate/cycle/missing/ambiguous failures, path and module limits, diagnostics, and allocation ownership, and implement only the smallest bounded provider-free `NSASS-011-LOCAL-USE-SINGLE-MODULE-LOADING-FOUNDATION` slice supported by the native source/resolver contract. Keep `@forward`, legacy imports, `meta.load-css()`, transitive/configured modules, user-module reflection, and broader dependency/source-map behavior separate until that foundation is executable. Provider removal and the package-level zero-dependency cutover remain gated on native graduation rather than deleting the exact development oracles early. Independently, `BENCH-007` still requires a dedicated non-emulated Linux x64 runner with labels `[self-hosted, linux, x64, zigcss-benchmark-v1]`; its absence must not pause eligible native frontend work.
- Runtime control: model identity, single-agent execution, isolated-worktree confinement, automatic supervisor-owned green-checkpoint pushes, the owner-bounded post-`NATIVE-009` GitHub-prerelease/npm-`next` authority, and every native graduation/release interlock remain unchanged.

## Prior current-work evidence (legacy alpha modern-color rejection checkpoint)

- Milestone: Milestone 10 self-contained native stylesheet frontends, in parallel with Milestone 8 controlled benchmark evidence
- Work package: `NSASS-011` native Sass evaluation
- State: `IN_PROGRESS`
- Active phase: the two-hundred-twentieth native Sass semantic checkpoint, `NSASS-011-COLOR-LEGACY-ALPHA-MODERN-COLOR-REJECTION-SEMANTICS`, is `VERIFIED` at local implementation checkpoint `59fe32f05974c0823ae0ee74811168f0861e339b`. Fresh Dart Sass 1.101.0 probes establish a uniform 312-case rejection matrix across the 13 modern-color spellings `lab`, `lch`, `oklab`, `oklch`, `color(srgb)`, `color(srgb-linear)`, `color(display-p3)`, `color(a98-rgb)`, `color(prophoto-rgb)`, `color(rec2020)`, `color(xyz-d50)`, `color(xyz-d65)`, and the `xyz` alias; legacy `opacify()`/`fade-in()`/`transparentize()`/`fade-out()`; ordinary direct, direct-splat, and reflected calls; and SCSS plus indented Sass. Every provider case emits one global-built-in deprecation before failing with the function-specific legacy-colors-only error, emits no function-specific color deprecation, and produces no CSS. Native Zig now routes the non-legacy-color branch of the shared legacy-alpha guard through the existing normalized global-built-in warning before returning the exact function-specific provider error; the guard still admits no modern transformation and covers every direct, splatted, and reflected surface. The exhaustive native regression owns all 312 combinations, exact warning/error order and identities, exact call spans, generated position zero, failed transaction completion with no partial CSS, a one-diagnostic limit failure after the owned warning, and exhaustive allocation failure on a representative indented-Sass splat. The test-first Debug gate stopped at 21/23 steps with 569/570 tests because the former branch emitted only the error. The first green replay exposed only the test harness's swallowed allocator-injected out-of-memory result, which was corrected to preserve allocation ownership. Final focused native Debug/ReleaseSafe each pass 23/23 steps with 570/570 tests; full compiler Debug/ReleaseSafe each pass 60/60 steps with 1065/1065 tests; and preprocessor-root no-emission cross-compiles pass for Windows x64 GNU and Linux ARM64 GNU in both modes. Native contract 12/12 plus check adds only `built-in-color-legacy-alpha-modern-color-rejection-semantics` while retaining unavailable modern-color transforms, private native Sass rows, production unreachability, zero target package/optional/runtime dependencies, and the closed release interlock. Complete provider/reference/product matrices pass 151/151, preprocessor package policy passes 6/6 plus check over 22 runtime sources, 28 archive files, 86 locked dependencies, and 5 targets, autonomous-runner selftest passes 59/59, and repository-wide Zig formatting, changed JavaScript syntax, tracked JSON parsing, and diff checks pass. A sibling search confirms that the shared legacy-alpha guard owns all ordinary direct, direct-splat, and reflected legacy-alpha entry points while unrelated transforms retain independent color boundaries. User-module reflection, dynamic loading, imports/forwards, nondeterministic `sass:string.unique-id()`, production routing, public capability claims, native graduation, version, release interlock, tag, artifact/package publication, deployment, and every external system remain unchanged.
- Quality closure: `QUAL-001` is verified at `3eced86`; repository-wide Zig formatting debt is zero and the final local validation matrix is green
- Next eligible package: measure the exact Dart Sass 1.101.0 `sass:string.unique-id()` contract across ordinary direct, splatted, and reflected calls, repeated calls and compilations, SCSS/indented-Sass behavior, string typing and interpolation, sequential and parallel determinism, limits, diagnostics, and allocation ownership, and make the smallest explicit `NSASS-011-STRING-UNIQUE-ID-DETERMINISTIC-COMPATIBILITY-DECISION`. Implement a native slice only if the provider contract can be represented without violating ZigCSS determinism; otherwise retain the function as unavailable and ledger the fail-closed compatibility decision. Do not infer user-module reflection, dynamic loading, imports/forwards, broader function semantics, or diagnostic normalization beyond executable evidence. Provider removal and the package-level zero-dependency cutover remain gated on native graduation rather than deleting the exact development oracles early. Independently, `BENCH-007` still requires a dedicated non-emulated Linux x64 runner with labels `[self-hosted, linux, x64, zigcss-benchmark-v1]`; its absence must not pause eligible native frontend work.
- Runtime control: model identity, single-agent execution, isolated-worktree confinement, automatic supervisor-owned green-checkpoint pushes, the owner-bounded post-`NATIVE-009` GitHub-prerelease/npm-`next` authority, and every native graduation/release interlock remain unchanged.

## Prior current-work evidence (legacy alpha compound-unit checkpoint)

- Milestone: Milestone 10 self-contained native stylesheet frontends, in parallel with Milestone 8 controlled benchmark evidence
- Work package: `NSASS-011` native Sass evaluation
- State: `IN_PROGRESS`
- Active phase: the two-hundred-nineteenth native Sass semantic checkpoint, `NSASS-011-COLOR-LEGACY-ALPHA-COMPOUND-UNIT-SEMANTICS`, is `VERIFIED` at local implementation checkpoint `027ddbfb113c6ce5f26f1d83904474c3cc562ec6`. Fresh Dart Sass 1.101.0 probes establish the complete selected amount boundary for legacy `opacify()`/`fade-in()`/`transparentize()`/`fade-out()` across ordinary direct, direct-splat, and reflected calls: completed numeric unit algebra is admitted regardless of remaining simple, denominator, product, percentage-product, or custom compound units; compatible-unit cancellation and conversion retain their evaluated scalar; zero and one are accepted; alpha results clamp at the color boundary; and finite scalar values within Dart Sass's fuzzy `1e-11` equality tolerance are clamped into the closed zero-to-one range while values outside that tolerance, larger magnitudes, negative magnitudes, non-numbers, and deferred calculations fail closed. Native Zig now routes ordinary direct calls through the same bounded legacy-alpha amount kernel already used by direct splats and reflection, admits every completed numeric unit inventory without allocating or serializing it, preserves finite/range rejection, applies the existing provider-compatible fuzzy equality before clamping, and emits the exact global-built-in plus function-specific warning pair for each successful ordinary direct call as well as splatted/reflected calls. Four stale per-function negative inventories no longer misclassify the newly admitted compound amount, while the fixed-keyword aggregate now owns the four exact warning pairs. The test-first Debug gate stopped at 21/23 steps with 568/569 tests exactly at the former `colorNumber()` compound-unit rejection. The first green replay exposed only those four stale negative classifications and the direct-warning expectation, and the next replay exposed only the fixed-keyword aggregate warning count; all were resolved without weakening any failure boundary. Final focused native Debug/ReleaseSafe each pass 23/23 steps with 569/569 tests; full compiler Debug/ReleaseSafe each pass 60/60 steps with 1064/1064 tests; and preprocessor-root no-emission cross-compiles pass for Windows x64 GNU and Linux ARM64 GNU in both modes. Native contract 12/12 plus check adds only `built-in-color-legacy-alpha-compound-unit-semantics` while retaining private native Sass rows, production unreachability, zero target package/optional/runtime dependencies, and the closed release interlock. Complete provider/reference/product matrices pass 151/151, preprocessor package policy passes 6/6 plus check over 22 runtime sources, 28 archive files, 86 locked dependencies, and 5 targets, autonomous-runner selftest passes 59/59, and repository-wide Zig formatting, tracked JSON parsing, and diff checks pass. A sibling Dart Sass probe confirms that modern-space inputs to these legacy alpha functions fail closed with the provider's explicit legacy-color-only error; the existing native color-space guard and retained direct/reflected negative inventories remain fail-closed, so modern-color rejection diagnostics stay separate from this amount slice. User-module reflection, dynamic loading, imports/forwards, nondeterministic `sass:string.unique-id()`, production routing, public capability claims, native graduation, version, release interlock, tag, artifact/package publication, deployment, and every external system remain unchanged.
- Quality closure: `QUAL-001` is verified at `3eced86`; repository-wide Zig formatting debt is zero and the final local validation matrix is green
- Next eligible package: measure the complete Dart Sass 1.101.0 modern-color rejection boundary for the already-admitted legacy-alpha functions across every modern color space, ordinary direct, direct-splat, and reflected `opacify()`/`fade-in()`/`transparentize()`/`fade-out()`, SCSS/indented-Sass behavior, exact error and warning ownership, no-partial-CSS guarantees, limits, and allocation failures, and implement only the smallest closed deterministic `NSASS-011-COLOR-LEGACY-ALPHA-MODERN-COLOR-REJECTION-SEMANTICS` slice if executable evidence exposes a native compatibility gap. Keep modern color transformation support unavailable, and keep nondeterministic `sass:string.unique-id()` separate pending its own deterministic compatibility decision. Do not infer user-module reflection, dynamic loading, imports/forwards, broader function semantics, or diagnostic normalization beyond executable evidence. Provider removal and the package-level zero-dependency cutover remain gated on native graduation rather than deleting the exact development oracles early. Independently, `BENCH-007` still requires a dedicated non-emulated Linux x64 runner with labels `[self-hosted, linux, x64, zigcss-benchmark-v1]`; its absence must not pause eligible native frontend work.
- Runtime control: model identity, single-agent execution, isolated-worktree confinement, automatic supervisor-owned green-checkpoint pushes, the owner-bounded post-`NATIVE-009` GitHub-prerelease/npm-`next` authority, and every native graduation/release interlock remain unchanged.

## Prior current-work evidence (legacy alpha missing-channel checkpoint)

- Milestone: Milestone 10 self-contained native stylesheet frontends, in parallel with Milestone 8 controlled benchmark evidence
- Work package: `NSASS-011` native Sass evaluation
- State: `IN_PROGRESS`
- Active phase: the two-hundred-eighteenth native Sass semantic checkpoint, `NSASS-011-COLOR-LEGACY-ALPHA-MISSING-CHANNEL-SEMANTICS`, is `VERIFIED` at local implementation checkpoint `8845c604e0bbedbc0e544d5489346b1a445f2ad6`. Fresh Dart Sass 1.101.0 probes establish the complete selected missing-channel boundary for legacy `opacify()`/`fade-in()`/`transparentize()`/`fade-out()` across direct and reflected calls, RGB/HSL/HWB channel absence, missing alpha, SCSS, indented Sass, exact compressed CSS, warning identity, and rejected comma syntax. Each missing legacy channel is replaced by zero before the alpha transform, the missing mask is cleared, and missing alpha starts from zero before clamped addition or subtraction. Native Zig now admits `none` only in whitespace/slash legacy constructors, preserves the authored legacy color space while replacing missing channels, uses one bounded legacy-alpha kernel across ordinary direct, direct-splat, and reflected calls, and emits the exact global-built-in plus function-specific warning pair for every newly admitted source call. Non-missing HSL/HWB behavior retains its already-verified RGB conversion. Comma-form missing channels and alpha remain invalid. Other legacy transforms, keyword color transforms, IE hex serialization, and legacy `color.is-powerless()` remain fail-closed for missing legacy channels, while previously admitted modern missing-channel queries remain unchanged. The test-first Debug gate stopped at 21/23 steps with 566/567 tests at the former constructor type boundary; the first broad replay then exposed the sibling surfaces now closed by retained negative regressions. Final focused native Debug/ReleaseSafe each pass 23/23 steps with 568/568 tests; full compiler Debug/ReleaseSafe each pass 60/60 steps with 1063/1063 tests; and preprocessor-root no-emission cross-compiles pass for Windows x64 GNU and Linux ARM64 GNU in both modes. Native contract 12/12 plus check adds only `built-in-color-legacy-alpha-missing-channel-semantics` while retaining private native Sass rows, production unreachability, zero target package/optional/runtime dependencies, and the closed release interlock. Complete provider/reference/product matrices pass 151/151, preprocessor package policy passes 6/6 plus check over 22 runtime sources, 28 archive files, 86 locked dependencies, and 5 targets, autonomous-runner selftest passes 59/59, and repository-wide Zig formatting, JavaScript/JSON syntax/parsing, and diff checks pass. Compound-unit alpha amounts, modern-color transformation semantics, user-module reflection, dynamic loading, imports/forwards, nondeterministic `sass:string.unique-id()`, production routing, public capability claims, native graduation, version, release interlock, tag, artifact/package publication, deployment, and every external system remain unchanged.
- Quality closure: `QUAL-001` is verified at `3eced86`; repository-wide Zig formatting debt is zero and the final local validation matrix is green
- Next eligible package: measure the complete Dart Sass 1.101.0 compound-unit amount boundary for the already-admitted legacy-alpha functions across direct and reflected `opacify()`/`fade-in()`/`transparentize()`/`fade-out()`, unit algebra, clamping, SCSS/indented-Sass behavior, warning identity, limits, and failure ownership, and implement only the smallest closed deterministic `NSASS-011-COLOR-LEGACY-ALPHA-COMPOUND-UNIT-SEMANTICS` slice if executable evidence supports it. Keep modern-color transforms and nondeterministic `sass:string.unique-id()` separate and unavailable pending their own exact compatibility decisions. Do not infer user-module reflection, dynamic loading, imports/forwards, broader function semantics, or diagnostic normalization beyond executable evidence. Provider removal and the package-level zero-dependency cutover remain gated on native graduation rather than deleting the exact development oracles early. Independently, `BENCH-007` still requires a dedicated non-emulated Linux x64 runner with labels `[self-hosted, linux, x64, zigcss-benchmark-v1]`; its absence must not pause eligible native frontend work.
- Runtime control: model identity, single-agent execution, isolated-worktree confinement, automatic supervisor-owned green-checkpoint pushes, the owner-bounded post-`NATIVE-009` GitHub-prerelease/npm-`next` authority, and every native graduation/release interlock remain unchanged.

## Prior current-work evidence (deprecation source deduplication checkpoint)

- Milestone: Milestone 10 self-contained native stylesheet frontends, in parallel with Milestone 8 controlled benchmark evidence
- Work package: `NSASS-011` native Sass evaluation
- State: `IN_PROGRESS`
- Active phase: the two-hundred-seventeenth native Sass semantic checkpoint, `NSASS-011-DIAGNOSTIC-DEPRECATION-SOURCE-DEDUPLICATION`, is `VERIFIED` at local implementation checkpoint `27dfd2d7be6e9bcebb08434f482a1ff1336d28fe`. Fresh Dart Sass 1.101.0 probes establish the transaction-level source-deduplication boundary across repeated direct legacy calls in loops, user-function bodies invoked repeatedly, reflected static and dynamic calls, legacy-global `length()`, repeated legacy `if()`, multiple textual call sites, SCSS, and indented Sass. One exact diagnostic code/source/span/message tuple emits once per compilation; distinct source locations and distinct warning messages remain independently observable, including three different warnings from one dynamic reflected call span. Native Zig now owns a bounded deprecation-key inventory in the evaluator transaction, refines built-in and legacy-`if()` spans from source-backed expression slices, and routes every admitted compatibility warning through one exact deduplication policy. Legacy-`if()` and misplaced-rest first-five throttling and the provider-compatible omitted-warning summary remain intact after deduplication. The new semantic limit defaults to 1,000 unique deprecations, rejects zero or values above the hard 65,536 ceiling, and fails closed before CSS when exhausted. Exact diagnostic budgets now count only emitted unique warnings. Existing direct legacy `grayscale()` and reflected meta content-existence expectations were corrected from 10 to 9 and 2 to 1 warnings after fresh provider probes proved repeated function-body and repeated call-site warnings are deduplicated. The test-first filtered Debug gate failed 0/1 with 29 warnings against the then-expected 14 at the former per-execution boundary; the hardened final regression covers 15 unique warnings including legacy `if()`. Final focused native Debug/ReleaseSafe each pass 23/23 steps with 566/566 tests; full compiler Debug/ReleaseSafe each pass 60/60 steps with 1061/1061 tests; and preprocessor-root no-emission cross-compiles pass for Windows x64 GNU and Linux ARM64 GNU in both modes. Native contract 12/12 plus check adds only `diagnostic-deprecation-source-deduplication` while retaining private native Sass rows, production unreachability, zero target package/optional/runtime dependencies, and the closed release interlock. Complete provider/reference/product matrices pass 151/151, preprocessor package policy passes 6/6 plus check over 22 runtime sources, 28 archive files, 86 locked dependencies, and 5 targets, autonomous-runner selftest passes 59/59, and repository-wide Zig formatting, JavaScript/JSON syntax/parsing, and diff checks pass. Sibling SCSS and indented-Sass probes establish a distinct missing-channel legacy-alpha boundary: Dart Sass coerces selected missing legacy RGB/HSL/HWB channels for `opacify()`/`fade-in()`/`transparentize()`/`fade-out()`, preserves the exact global/color warning pair, and produces deterministic legacy colors, while missing alpha is separately clamped by the transform. Those semantics remain fail-closed here pending their own complete compatibility slice. Modern-color semantics, user-module reflection, dynamic loading, imports/forwards, nondeterministic `sass:string.unique-id()`, production routing, public capability claims, native graduation, version, release interlock, tag, artifact/package publication, deployment, and every external system remain unchanged.
- Quality closure: `QUAL-001` is verified at `3eced86`; repository-wide Zig formatting debt is zero and the final local validation matrix is green
- Next eligible package: measure the complete Dart Sass 1.101.0 missing-channel boundary for the already-admitted legacy-alpha functions across direct and reflected `opacify()`/`fade-in()`/`transparentize()`/`fade-out()`, legacy RGB/HSL/HWB channel absence, missing alpha, SCSS/indented-Sass behavior, warning identity, limits, and failure ownership, and implement only the smallest closed deterministic `NSASS-011-COLOR-LEGACY-ALPHA-MISSING-CHANNEL-SEMANTICS` slice if executable evidence supports it. Keep modern-color transforms and nondeterministic `sass:string.unique-id()` separate and unavailable pending their own exact compatibility decisions. Do not infer user-module reflection, dynamic loading, imports/forwards, broader function semantics, or diagnostic normalization beyond executable evidence. Provider removal and the package-level zero-dependency cutover remain gated on native graduation rather than deleting the exact development oracles early. Independently, `BENCH-007` still requires a dedicated non-emulated Linux x64 runner with labels `[self-hosted, linux, x64, zigcss-benchmark-v1]`; its absence must not pause eligible native frontend work.
- Runtime control: model identity, single-agent execution, isolated-worktree confinement, automatic supervisor-owned green-checkpoint pushes, the owner-bounded post-`NATIVE-009` GitHub-prerelease/npm-`next` authority, and every native graduation/release interlock remain unchanged.

## Prior current-work evidence (legacy color fade-out checkpoint)

- Milestone: Milestone 10 self-contained native stylesheet frontends, in parallel with Milestone 8 controlled benchmark evidence
- Work package: `NSASS-011` native Sass evaluation
- State: `IN_PROGRESS`
- Active phase: the two-hundred-sixteenth native Sass semantic checkpoint, `NSASS-011-COLOR-LEGACY-FADE-OUT-DIRECT-SPLAT-EXPANSION`, is `VERIFIED` at local implementation checkpoint `4f35ca9fbb2aa7d8b62845704142a85f4d7fc317`. Fresh Dart Sass 1.101.0 probes establish the admitted legacy-global direct `fade-out()` boundary: positional-list, keyword-map, bracketed-list, and positional/keyword argument-list splats; provider-compatible later-map override of an earlier named color; positional/map duplicate rejection; once-only source order; SCSS and indented Sass; legacy RGB/HSL/HWB, transparent, opaque, alpha-clamping, zero/full amounts, and color typing; exact CSS; and one global-built-in plus one `fade-out()` deprecation per successful source call. Native Zig now detects a syntactic splat only on legacy-global direct `fade-out()` and routes that call through the already-verified evaluated-splat expansion, canonical bounded binder, global callable identity, and shared legacy-alpha kernel used by reflected invocation. Ordinary legacy-global calls and module-owned direct calls retain their prior paths. Empty, short, and overfull splats, unknown and non-string map keys, positional/map duplicates, invalid values, deferred calculations, negative/over-range amounts, missing-channel constructors, modern colors, expanded argument limits, and every allocation failure expose no CSS. The provider accepts selected compound-unit and missing-channel inputs, but the native boundary remains deliberately narrower and fail-closed rather than broadening beyond the established reflected legacy-alpha semantics. The test-first Debug evaluator gate stopped at 21/23 steps with 563/565 tests at the former `SplatUnsupported` boundary and the former unsupported negative classification. Final focused native Debug/ReleaseSafe each pass 23/23 steps with 565/565 tests; full compiler Debug/ReleaseSafe each pass 60/60 steps with 1060/1060 tests; and root no-emission cross-compiles pass for Windows x64 GNU and Linux ARM64 GNU in both modes. Native contract 12/12 plus check adds only `built-in-color-legacy-fade-out-direct-splat-expansion` while retaining private native Sass rows, production unreachability, zero target package/optional/runtime dependencies, and the closed release interlock. Complete provider/reference/product matrices pass 151/151, preprocessor package policy passes 6/6 plus check over 22 runtime sources, 28 archive files, 86 locked dependencies, and 5 targets, autonomous-runner selftest passes 59/59, and repository-wide Zig formatting, JavaScript/JSON syntax/parsing, and diff checks pass. A sibling SCSS and indented-Sass probe establishes the next distinct diagnostic boundary: three executions of one direct legacy `fade-out()` source location emit exact CSS but only one global-built-in and one color-functions warning per location. Missing-channel and modern-color semantics, user-module reflection, dynamic loading, imports/forwards, nondeterministic `sass:string.unique-id()`, production routing, public capability claims, native graduation, version, release interlock, tag, artifact/package publication, deployment, and every external system remain unchanged.
- Quality closure: `QUAL-001` is verified at `3eced86`; repository-wide Zig formatting debt is zero and the final local validation matrix is green
- Next eligible package: measure the complete Dart Sass 1.101.0 source-location deprecation deduplication boundary across already-admitted legacy-global call paths and implement only the smallest architecture-consistent `NSASS-011-DIAGNOSTIC-DEPRECATION-SOURCE-DEDUPLICATION` slice if executable evidence supports one shared transaction-level policy. Preserve distinct deprecation types, source spans, warning throttles, SCSS/indented-Sass behavior, deterministic repeated/loop/function evaluation, and allocation/limit ownership. Keep missing-channel and modern-color semantics and nondeterministic `sass:string.unique-id()` separate and unavailable pending their own exact compatibility decisions. Do not infer user-module reflection, dynamic loading, imports/forwards, broader function semantics, or diagnostic normalization beyond executable evidence. Provider removal and the package-level zero-dependency cutover remain gated on native graduation rather than deleting the exact development oracles early. Independently, `BENCH-007` still requires a dedicated non-emulated Linux x64 runner with labels `[self-hosted, linux, x64, zigcss-benchmark-v1]`; its absence must not pause eligible native frontend work.
- Runtime control: model identity, single-agent execution, isolated-worktree confinement, automatic supervisor-owned green-checkpoint pushes, the owner-bounded post-`NATIVE-009` GitHub-prerelease/npm-`next` authority, and every native graduation/release interlock remain unchanged.

## Prior current-work evidence (legacy color transparentize checkpoint)

- Milestone: Milestone 10 self-contained native stylesheet frontends, in parallel with Milestone 8 controlled benchmark evidence
- Work package: `NSASS-011` native Sass evaluation
- State: `IN_PROGRESS`
- Active phase: the two-hundred-fifteenth native Sass semantic checkpoint, `NSASS-011-COLOR-LEGACY-TRANSPARENTIZE-DIRECT-SPLAT-EXPANSION`, is `VERIFIED` at local implementation checkpoint `07eab662807631ae5648d526a1ca86c99c205510`. Fresh Dart Sass 1.101.0 probes establish the admitted legacy-global direct `transparentize()` boundary: positional-list, keyword-map, bracketed-list, and positional/keyword argument-list splats; provider-compatible later-map override of an earlier named color; positional/map duplicate rejection; once-only source order; SCSS and indented Sass; legacy RGB/HSL/HWB, transparent, opaque, alpha-clamping, zero/full amounts, and color typing; exact CSS; and one global-built-in plus one `transparentize()` deprecation per successful source call. Native Zig now detects a syntactic splat only on legacy-global direct `transparentize()` and routes that call through the already-verified evaluated-splat expansion, canonical bounded binder, global callable identity, and shared legacy-alpha kernel used by reflected invocation. Ordinary legacy-global calls, module-owned direct calls, and direct `fade-out()` calls retain their prior paths. Empty, short, and overfull splats, unknown and non-string map keys, positional/map duplicates, invalid values, deferred calculations, negative/over-range amounts, missing-channel constructors, modern colors, expanded argument limits, and every allocation failure expose no CSS. The provider accepts selected compound-unit and missing-channel inputs, but the native boundary remains deliberately narrower and fail-closed rather than broadening beyond the established reflected legacy-alpha semantics. The test-first Debug evaluator gate stopped at 21/23 steps with 562/564 tests at the former `SplatUnsupported` boundary. Final focused native Debug/ReleaseSafe each pass 23/23 steps with 564/564 tests; full compiler Debug/ReleaseSafe each pass 60/60 steps with 1059/1059 tests; and preprocessor-root no-emission cross-compiles pass for Windows x64 GNU and Linux ARM64 GNU in both modes. Native contract 12/12 plus check adds only `built-in-color-legacy-transparentize-direct-splat-expansion` while retaining private native Sass rows, production unreachability, zero target package/optional/runtime dependencies, and the closed release interlock. Complete provider/reference/product matrices pass 151/151, preprocessor package policy passes 6/6 plus check over 22 runtime sources, 28 archive files, 86 locked dependencies, and 5 targets, autonomous-runner selftest passes 59/59, and repository-wide Zig formatting, JavaScript/JSON syntax/parsing, and diff checks pass. Sibling probes designate legacy-global direct `fade-out()` splats next: list, map, and later-map override results are deterministic legacy colors with the same two warning classes per successful source call. Repeated-call warning deduplication, missing-channel and modern-color semantics, user-module reflection, dynamic loading, imports/forwards, nondeterministic `sass:string.unique-id()`, production routing, public capability claims, native graduation, version, release interlock, tag, artifact/package publication, deployment, and every external system remain unchanged.
- Quality closure: `QUAL-001` is verified at `3eced86`; repository-wide Zig formatting debt is zero and the final local validation matrix is green
- Next eligible package: measure the complete Dart Sass 1.101.0 legacy-global direct `fade-out()` splat boundary and implement only the smallest closed deterministic `NSASS-011-COLOR-LEGACY-FADE-OUT-DIRECT-SPLAT-EXPANSION` slice if executable evidence supports it, preserving both legacy warning classes. Keep repeated-call warning deduplication, missing-channel and modern-color semantics, and nondeterministic `sass:string.unique-id()` separate and unavailable pending their own exact compatibility decisions. Do not infer user-module reflection, dynamic loading, imports/forwards, broader function semantics, or normalized diagnostics beyond executable evidence. Provider removal and the package-level zero-dependency cutover remain gated on native graduation rather than deleting the exact development oracles early. Independently, `BENCH-007` still requires a dedicated non-emulated Linux x64 runner with labels `[self-hosted, linux, x64, zigcss-benchmark-v1]`; its absence must not pause eligible native frontend work.
- Runtime control: model identity, single-agent execution, isolated-worktree confinement, automatic supervisor-owned green-checkpoint pushes, the owner-bounded post-`NATIVE-009` GitHub-prerelease/npm-`next` authority, and every native graduation/release interlock remain unchanged.

## Prior current-work evidence (legacy color fade in checkpoint)

- Milestone: Milestone 10 self-contained native stylesheet frontends, in parallel with Milestone 8 controlled benchmark evidence
- Work package: `NSASS-011` native Sass evaluation
- State: `IN_PROGRESS`
- Active phase: the two-hundred-fourteenth native Sass semantic checkpoint, `NSASS-011-COLOR-LEGACY-FADE-IN-DIRECT-SPLAT-EXPANSION`, is `VERIFIED` at local implementation checkpoint `19027afbf8524c0116c6b445fe04d0a11454f32b`. Fresh Dart Sass 1.101.0 probes establish the admitted legacy-global direct `fade-in()` boundary: positional-list, keyword-map, bracketed-list, and positional/keyword argument-list splats; provider-compatible later-map override of an earlier named color; positional/map duplicate rejection; once-only source order; SCSS and indented Sass; legacy RGB/HSL/HWB, transparent, opaque, alpha-clamping, zero/full amounts, and color typing; exact CSS; and one global-built-in plus one `fade-in()` deprecation per successful source call. Native Zig now detects a syntactic splat only on legacy-global direct `fade-in()` and routes that call through the already-verified evaluated-splat expansion, canonical bounded binder, global callable identity, and shared legacy-alpha kernel used by reflected invocation. Ordinary legacy-global calls, module-owned direct calls, and direct `transparentize()`/`fade-out()` calls retain their prior paths. Empty, short, and overfull splats, unknown and non-string map keys, positional/map duplicates, invalid values, deferred calculations, negative/over-range amounts, missing-channel constructors, modern colors, expanded argument limits, and every allocation failure expose no CSS. The provider accepts selected compound-unit and missing-channel inputs, but the native boundary remains deliberately narrower and fail-closed rather than broadening beyond the established reflected legacy-alpha semantics. The test-first Debug evaluator gate stopped at 21/23 steps with 561/563 tests at the former `SplatUnsupported` boundary. Final focused native Debug/ReleaseSafe each pass 23/23 steps with 563/563 tests; full compiler Debug/ReleaseSafe each pass 60/60 steps with 1058/1058 tests; and evaluator no-emission cross-compiles pass for Windows x64 GNU and Linux ARM64 GNU in both modes. Native contract 12/12 plus check adds only `built-in-color-legacy-fade-in-direct-splat-expansion` while retaining private native Sass rows, production unreachability, zero target package/optional/runtime dependencies, and the closed release interlock. Complete provider/reference/product matrices pass 151/151, preprocessor package policy passes 6/6 plus check over 22 runtime sources, 28 archive files, 86 locked dependencies, and 5 targets, autonomous-runner selftest passes 59/59, and repository-wide Zig formatting, JavaScript/JSON syntax/parsing, and diff checks pass. Sibling probes designate legacy-global direct `transparentize()` splats next: list, map, and later-map override results are deterministic legacy colors with the same two warning classes per successful source call. Direct `fade-out()` splats, repeated-call warning deduplication, missing-channel and modern-color semantics, user-module reflection, dynamic loading, imports/forwards, nondeterministic `sass:string.unique-id()`, production routing, public capability claims, native graduation, version, release interlock, tag, artifact/package publication, deployment, and every external system remain unchanged.
- Quality closure: `QUAL-001` is verified at `3eced86`; repository-wide Zig formatting debt is zero and the final local validation matrix is green
- Next eligible package: measure the complete Dart Sass 1.101.0 legacy-global direct `transparentize()` splat boundary and implement only the smallest closed deterministic `NSASS-011-COLOR-LEGACY-TRANSPARENTIZE-DIRECT-SPLAT-EXPANSION` slice if executable evidence supports it, preserving both legacy warning classes. Keep direct `fade-out()` splats, repeated-call warning deduplication, missing-channel and modern-color semantics, and nondeterministic `sass:string.unique-id()` separate and unavailable pending their own exact compatibility decisions. Do not infer user-module reflection, dynamic loading, imports/forwards, broader function semantics, or normalized diagnostics beyond executable evidence. Provider removal and the package-level zero-dependency cutover remain gated on native graduation rather than deleting the exact development oracles early. Independently, `BENCH-007` still requires a dedicated non-emulated Linux x64 runner with labels `[self-hosted, linux, x64, zigcss-benchmark-v1]`; its absence must not pause eligible native frontend work.
- Runtime control: model identity, single-agent execution, isolated-worktree confinement, automatic supervisor-owned green-checkpoint pushes, the owner-bounded post-`NATIVE-009` GitHub-prerelease/npm-`next` authority, and every native graduation/release interlock remain unchanged.

## Prior current-work evidence (legacy color opacify checkpoint)

- Milestone: Milestone 10 self-contained native stylesheet frontends, in parallel with Milestone 8 controlled benchmark evidence
- Work package: `NSASS-011` native Sass evaluation
- State: `IN_PROGRESS`
- Active phase: the two-hundred-thirteenth native Sass semantic checkpoint, `NSASS-011-COLOR-LEGACY-OPACIFY-DIRECT-SPLAT-EXPANSION`, is `VERIFIED` at local implementation checkpoint `573031f0d93030d767551369b91907dc73c331d2`. Fresh Dart Sass 1.101.0 probes establish the admitted legacy-global direct `opacify()` boundary: positional-list, keyword-map, bracketed-list, and positional/keyword argument-list splats; provider-compatible later-map override of an earlier named color; positional/map duplicate rejection; once-only source order; SCSS and indented Sass; legacy RGB/HSL/HWB, transparent, opaque, alpha-clamping, zero/full amounts, and color typing; exact CSS; and one global-built-in plus one `opacify()` deprecation per successful source call. Native Zig now detects a syntactic splat only on legacy-global direct `opacify()` and routes that call through the already-verified evaluated-splat expansion, canonical bounded binder, global callable identity, and shared legacy-alpha kernel used by reflected invocation. Ordinary legacy-global calls, module-owned direct calls, and direct `fade-in()`/`transparentize()`/`fade-out()` calls retain their prior paths. Empty, short, and overfull splats, unknown and non-string map keys, positional/map duplicates, invalid values, deferred calculations, negative/over-range amounts, missing-channel constructors, modern colors, expanded argument limits, and every allocation failure expose no CSS. The provider accepts selected compound-unit and missing-channel inputs, but the native boundary remains deliberately narrower and fail-closed rather than broadening beyond the established reflected legacy-alpha semantics. The test-first Debug evaluator gate stopped at 21/23 steps with 560/562 tests at the former `SplatUnsupported` boundary. Final focused native Debug/ReleaseSafe each pass 23/23 steps with 562/562 tests; full compiler Debug/ReleaseSafe each pass 60/60 steps with 1057/1057 tests; and evaluator no-emission cross-compiles pass for Windows x64 GNU and Linux ARM64 GNU in both modes. Native contract 12/12 plus check adds only `built-in-color-legacy-opacify-direct-splat-expansion` while retaining private native Sass rows, production unreachability, zero target package/optional/runtime dependencies, and the closed release interlock. Complete provider/reference/product matrices pass 151/151, preprocessor package policy passes 6/6 plus check over 22 runtime sources, 28 archive files, 86 locked dependencies, and 5 targets, autonomous-runner selftest passes 59/59, and repository-wide Zig formatting, JavaScript/JSON syntax/parsing, and diff checks pass. Sibling probes designate legacy-global direct `fade-in()` splats next: list, map, and later-map override results are deterministic legacy colors with the same two warning classes per successful source call. Direct `transparentize()`/`fade-out()` splats, repeated-call warning deduplication, missing-channel and modern-color semantics, user-module reflection, dynamic loading, imports/forwards, nondeterministic `sass:string.unique-id()`, production routing, public capability claims, native graduation, version, release interlock, tag, artifact/package publication, deployment, and every external system remain unchanged.
- Quality closure: `QUAL-001` is verified at `3eced86`; repository-wide Zig formatting debt is zero and the final local validation matrix is green
- Next eligible package: measure the complete Dart Sass 1.101.0 legacy-global direct `fade-in()` splat boundary and implement only the smallest closed deterministic `NSASS-011-COLOR-LEGACY-FADE-IN-DIRECT-SPLAT-EXPANSION` slice if executable evidence supports it, preserving both legacy warning classes. Keep direct `transparentize()`/`fade-out()` splats, repeated-call warning deduplication, missing-channel and modern-color semantics, and nondeterministic `sass:string.unique-id()` separate and unavailable pending their own exact compatibility decisions. Do not infer user-module reflection, dynamic loading, imports/forwards, broader function semantics, or normalized diagnostics beyond executable evidence. Provider removal and the package-level zero-dependency cutover remain gated on native graduation rather than deleting the exact development oracles early. Independently, `BENCH-007` still requires a dedicated non-emulated Linux x64 runner with labels `[self-hosted, linux, x64, zigcss-benchmark-v1]`; its absence must not pause eligible native frontend work.
- Runtime control: model identity, single-agent execution, isolated-worktree confinement, automatic supervisor-owned green-checkpoint pushes, the owner-bounded post-`NATIVE-009` GitHub-prerelease/npm-`next` authority, and every native graduation/release interlock remain unchanged.

## Prior current-work evidence (legacy color invert checkpoint)

- Milestone: Milestone 10 self-contained native stylesheet frontends, in parallel with Milestone 8 controlled benchmark evidence
- Work package: `NSASS-011` native Sass evaluation
- State: `IN_PROGRESS`
- Active phase: the two-hundred-twelfth native Sass semantic checkpoint, `NSASS-011-COLOR-LEGACY-INVERT-DIRECT-SPLAT-EXPANSION`, is `VERIFIED` at local implementation checkpoint `eedc79b437a15b6a4fb44f5114bfba663f7c3039`. Fresh Dart Sass 1.101.0 probes establish the admitted legacy-global direct `invert()` boundary: comma-list, keyword-map, scalar, bracketed-list, and positional/keyword argument-list splats; provider-compatible later-map override of an earlier named binding; positional/map duplicate rejection; once-only source order; SCSS and indented Sass; legacy RGB/HSL/HWB, transparent, and alpha behavior; exact CSS; color and CSS-filter typing; one global-built-in deprecation per successful color source call; and warning-free numeric, unit-bearing, deferred, and calculated CSS filters. Native Zig now detects a syntactic splat only on legacy-global direct `invert()` and routes that call through the already-verified evaluated-splat expansion, canonical bounded binder, global callable identity, and shared invert kernel used by reflected invocation. Ordinary legacy-global calls and module-owned direct calls retain their prior paths. Empty and overfull splats, unknown and non-string map keys, positional/map duplicates, invalid values, unitless or unit-bearing weights, optional interpolation spaces, direct missing-channel constructor forms, modern colors, expanded argument limits, and every allocation failure expose no CSS. The provider accepts selected unit coercions, an optional space, and selected missing-channel inputs, but the native boundary remains deliberately narrower and fail-closed rather than broadening beyond the established reflected legacy-color semantics. The test-first Debug evaluator gate stopped at 21/23 steps with 560/561 tests at the former `SplatUnsupported` boundary. Final focused native Debug/ReleaseSafe each pass 23/23 steps with 561/561 tests; full compiler Debug/ReleaseSafe each pass 60/60 steps with 1056/1056 tests; and evaluator no-emission cross-compiles pass for Windows x64 GNU and Linux ARM64 GNU in both modes. Native contract 12/12 plus check adds only `built-in-color-legacy-invert-direct-splat-expansion` while retaining private native Sass rows, production unreachability, zero target package/optional/runtime dependencies, and the closed release interlock. Complete provider/reference/product matrices pass 151/151, preprocessor package policy passes 6/6 plus check over 22 runtime sources, 28 archive files, 86 locked dependencies, and 5 targets, autonomous-runner selftest passes 59/59, and repository-wide Zig formatting, JavaScript/JSON syntax/parsing, and diff checks pass. Sibling probes designate legacy-global direct `opacify()` splats next: positional-list, keyword-map, and later-map override results are deterministic legacy colors with one global-built-in plus one `opacify()` deprecation per successful source call. Direct `fade-in()`/`transparentize()`/`fade-out()` splats, optional invert spaces, broader missing-channel behavior, modern color semantics, repeated-call warning deduplication, user-module reflection, dynamic loading, imports/forwards, nondeterministic `sass:string.unique-id()`, production routing, public capability claims, native graduation, version, release interlock, tag, artifact/package publication, deployment, and every external system remain unchanged.
- Quality closure: `QUAL-001` is verified at `3eced86`; repository-wide Zig formatting debt is zero and the final local validation matrix is green
- Next eligible package: measure the complete Dart Sass 1.101.0 legacy-global direct `opacify()` splat boundary and implement only the smallest closed deterministic `NSASS-011-COLOR-LEGACY-OPACIFY-DIRECT-SPLAT-EXPANSION` slice if executable evidence supports it, preserving both legacy warning classes. Keep `fade-in()`/`transparentize()`/`fade-out()` direct splats, repeated-call warning deduplication, missing-channel and modern-color semantics, and nondeterministic `sass:string.unique-id()` separate and unavailable pending their own exact compatibility decisions. Do not infer user-module reflection, dynamic loading, imports/forwards, broader function semantics, or normalized diagnostics beyond executable evidence. Provider removal and the package-level zero-dependency cutover remain gated on native graduation rather than deleting the exact development oracles early. Independently, `BENCH-007` still requires a dedicated non-emulated Linux x64 runner with labels `[self-hosted, linux, x64, zigcss-benchmark-v1]`; its absence must not pause eligible native frontend work.
- Runtime control: model identity, single-agent execution, isolated-worktree confinement, automatic supervisor-owned green-checkpoint pushes, the owner-bounded post-`NATIVE-009` GitHub-prerelease/npm-`next` authority, and every native graduation/release interlock remain unchanged.

## Prior current-work evidence (module color invert checkpoint)

- Milestone: Milestone 10 self-contained native stylesheet frontends, in parallel with Milestone 8 controlled benchmark evidence
- Work package: `NSASS-011` native Sass evaluation
- State: `IN_PROGRESS`
- Active phase: the two-hundred-eleventh native Sass semantic checkpoint, `NSASS-011-COLOR-MODULE-INVERT-DIRECT-SPLAT-EXPANSION`, is `VERIFIED` at local implementation checkpoint `f4625ce9d996901c19ad266304bf3adccd3afce3`. Fresh Dart Sass 1.101.0 probes establish direct `sass:color.invert()` module access, direct and splatted color/filter behavior, exact warning ownership, strict failures, limits, and allocation safety. Native dispatch selects the evaluated expansion, canonical binder, module callable identity, and reflected invert kernel only for loaded color-module members. The test-first gate stopped at the former undefined-module boundary; final native modes pass 560/560, full compiler modes pass 1055/1055, both cross-target/mode pairs pass, and every contract, provider/reference/product, package-policy, runner-selftest, formatting, syntax, JSON, and diff gate is green.
- Quality closure: `QUAL-001` is verified at `3eced86`; repository-wide Zig formatting debt is zero and the final local validation matrix is green
- Next eligible package at that checkpoint: implement the measured legacy-global direct `invert()` splat boundary while preserving color-versus-filter warning ownership.
- Runtime control: model identity, single-agent execution, isolated-worktree confinement, automatic supervisor-owned green-checkpoint pushes, the owner-bounded post-`NATIVE-009` GitHub-prerelease/npm-`next` authority, and every native graduation/release interlock remain unchanged.

## Prior current-work evidence (legacy color grayscale checkpoint)

- Milestone: Milestone 10 self-contained native stylesheet frontends, in parallel with Milestone 8 controlled benchmark evidence
- Work package: `NSASS-011` native Sass evaluation
- State: `IN_PROGRESS`
- Active phase: the two-hundred-tenth native Sass semantic checkpoint, `NSASS-011-COLOR-LEGACY-GRAYSCALE-DIRECT-SPLAT-EXPANSION`, is `VERIFIED` at local implementation checkpoint `edb7e792a930f85ddb535a6babd78a237e946068`. Fresh Dart Sass 1.101.0 probes establish legacy-global direct `grayscale()` list/map/scalar/bracketed/argument-list splats, later-map override, once-only source order, SCSS/indented-Sass output, legacy color typing, one global deprecation per color result, and warning-free numeric/unit/deferred CSS filters. Native dispatch selects the already-verified evaluated expansion, canonical binder, global callable identity, and reflected grayscale kernel only for global calls containing a syntactic splat. Invalid and unsupported inputs, limits, and allocation failures expose no CSS. The test-first filtered Debug gate failed 0/1 at `SplatUnsupported`; final native modes pass 559/559, full compiler modes pass 1054/1054, both cross-target/mode pairs pass, and every contract, provider/reference/product, package-policy, runner-selftest, formatting, syntax, JSON, and diff gate is green.
- Quality closure: `QUAL-001` is verified at `3eced86`; repository-wide Zig formatting debt is zero and the final local validation matrix is green
- Next eligible package at that checkpoint: measure the complete direct `sass:color.invert()` splat boundary while keeping legacy-global invert splats and nondeterministic `sass:string.unique-id()` separate.
- Runtime control: model identity, single-agent execution, isolated-worktree confinement, automatic supervisor-owned green-checkpoint pushes, the owner-bounded post-`NATIVE-009` GitHub-prerelease/npm-`next` authority, and every native graduation/release interlock remain unchanged.

## Prior current-work evidence (module color grayscale checkpoint)

- Milestone: Milestone 10 self-contained native stylesheet frontends, in parallel with Milestone 8 controlled benchmark evidence
- Work package: `NSASS-011` native Sass evaluation
- State: `IN_PROGRESS`
- Active phase: the two-hundred-ninth native Sass semantic checkpoint, `NSASS-011-COLOR-MODULE-GRAYSCALE-DIRECT-SPLAT-EXPANSION`, is `VERIFIED` at local implementation checkpoint `b5d8278abb8e7ce749f820c0be12dee73ddcfbeb`. Fresh Dart Sass 1.101.0 probes establish the admitted direct `sass:color.grayscale()` boundary: default, aliased, and unprefixed module access; ordinary and named calls; comma-list, keyword-map, scalar, bracketed-list, and positional/keyword argument-list splats; provider-compatible later-map override of an earlier named binding; positional/map duplicate rejection; once-only source order; SCSS and indented Sass; legacy RGB/HSL/HWB, transparent, and alpha behavior; exact CSS; color typing; and numeric CSS-filter preservation with one module deprecation per numeric source call. Native Zig now recognizes `grayscale` only for a loaded color module and routes its direct calls through the already-verified evaluated-splat expansion, canonical bounded binder, module callable identity, and shared grayscale kernel used by reflected invocation. Legacy-global direct grayscale splats retain their former raw unsupported path. Empty and overfull splats, unknown and non-string map keys, positional/map duplicates, invalid or deferred values, direct missing-channel constructor forms, modern colors, expanded argument limits, and every allocation failure expose no CSS. The provider accepts selected missing-channel and modern-color inputs, but the native boundary remains deliberately narrower and fail-closed rather than broadening beyond the reflected legacy-color semantics. The test-first focused Debug gate stopped at 21/23 steps with 556/558 tests because the positive path and expanded-limit classification still reached the former undefined-module boundary. Final focused native Debug/ReleaseSafe each pass 23/23 steps with 558/558 tests; full compiler Debug/ReleaseSafe each pass 60/60 steps with 1053/1053 tests; and evaluator no-emission cross-compiles pass for Windows x64 GNU and Linux ARM64 GNU in both modes. Native contract 12/12 plus check adds only `built-in-color-module-grayscale-direct-splat-expansion` while retaining private native Sass rows, production unreachability, zero target package/optional/runtime dependencies, and the closed release interlock. Complete provider/reference/product matrices pass 151/151, preprocessor package policy passes 6/6 plus check over 22 runtime sources, 28 archive files, 86 locked dependencies, and 5 targets, autonomous-runner selftest passes 59/59, and repository-wide Zig formatting, JavaScript/JSON syntax/parsing, and diff checks pass. Sibling probes designate legacy-global direct `grayscale()` splats next: color list/map splats have the global-built-in deprecation, while numeric CSS-filter splats remain warning-free. Broader missing-channel behavior, modern color semantics, user-module reflection, dynamic loading, imports/forwards, nondeterministic `sass:string.unique-id()`, production routing, public capability claims, native graduation, version, release interlock, tag, artifact/package publication, deployment, and every external system remain unchanged.
- Quality closure: `QUAL-001` is verified at `3eced86`; repository-wide Zig formatting debt is zero and the final local validation matrix is green
- Next eligible package at that checkpoint: measure the complete Dart Sass 1.101.0 legacy-global direct `grayscale()` splat boundary while keeping its color-versus-CSS-filter warning ownership and nondeterministic `sass:string.unique-id()` separate.
- Runtime control: model identity, single-agent execution, isolated-worktree confinement, automatic supervisor-owned green-checkpoint pushes, the owner-bounded post-`NATIVE-009` GitHub-prerelease/npm-`next` authority, and every native graduation/release interlock remain unchanged.

## Prior current-work evidence (legacy color complement splat checkpoint)

- Milestone: Milestone 10 self-contained native stylesheet frontends, in parallel with Milestone 8 controlled benchmark evidence
- Work package: `NSASS-011` native Sass evaluation
- State: `IN_PROGRESS`
- Active phase: the two-hundred-eighth native Sass semantic checkpoint, `NSASS-011-COLOR-LEGACY-COMPLEMENT-DIRECT-SPLAT-EXPANSION`, is `VERIFIED` at local implementation checkpoint `259f974e4a6c48af87e98a032adc7d091f8c4730`. Fresh Dart Sass 1.101.0 probes establish the admitted one-color legacy-global direct `complement()` boundary: comma-list, keyword-map, scalar, bracketed-list, and positional/keyword argument-list splats; provider-compatible later-map override of an earlier named binding; positional/map duplicate rejection; once-only source order; SCSS and indented Sass; legacy RGB/HSL/HWB, transparent, and alpha behavior; exact CSS; color typing; and one global-built-in deprecation per successful source call. Native Zig now detects a syntactic splat only on legacy-global direct `complement()` and routes that call through the already-verified evaluated-splat expansion, canonical bounded binder, global callable identity, and shared complement kernel used by reflected invocation. Ordinary legacy-global calls and module-owned direct calls retain their prior paths. Empty and overfull splats, unknown and non-string map keys, positional/map duplicates, invalid values, direct missing-channel constructor forms, modern colors, optional positional or keyword interpolation spaces, expanded argument limits, and every allocation failure expose no CSS. The provider accepts selected missing-channel inputs and explicit interpolation spaces, but those semantics remain deliberately separate and fail closed. The test-first focused Debug gate stopped at 21/23 steps with 555/557 tests because the positive path and invalid classification still reached the former `SplatUnsupported` boundary. Final focused native Debug/ReleaseSafe each pass 23/23 steps with 557/557 tests; full compiler Debug/ReleaseSafe each pass 60/60 steps with 1052/1052 tests; and evaluator no-emission cross-compiles pass for Windows x64 GNU and Linux ARM64 GNU in both modes. Native contract 12/12 plus check adds only `built-in-color-legacy-complement-direct-splat-expansion` while retaining private native Sass rows, production unreachability, zero target package/optional/runtime dependencies, and the closed release interlock. Complete provider/reference/product matrices pass 151/151, preprocessor package policy passes 6/6 plus check over 22 runtime sources, 28 archive files, 86 locked dependencies, and 5 targets, autonomous-runner selftest passes 59/59, and repository-wide Zig formatting, JavaScript/JSON syntax/parsing, and diff checks pass. Sibling probes designate direct `sass:color.grayscale()` next: module list/map splats produce deterministic color results with zero warnings, while a numeric filter result has its distinct module deprecation; legacy-global direct grayscale splats retain a separate warning and CSS-filter boundary. Optional complement interpolation spaces, broader missing-channel behavior, modern color semantics, user-module reflection, dynamic loading, imports/forwards, nondeterministic `sass:string.unique-id()`, production routing, public capability claims, native graduation, version, release interlock, tag, artifact/package publication, deployment, and every external system remain unchanged.
- Quality closure: `QUAL-001` is verified at `3eced86`; repository-wide Zig formatting debt is zero and the final local validation matrix is green
- Next eligible package at that checkpoint: measure the complete Dart Sass 1.101.0 direct `sass:color.grayscale()` splat boundary while keeping legacy-global grayscale splats and nondeterministic `sass:string.unique-id()` separate.
- Runtime control: model identity, single-agent execution, isolated-worktree confinement, automatic supervisor-owned green-checkpoint pushes, the owner-bounded post-`NATIVE-009` GitHub-prerelease/npm-`next` authority, and every native graduation/release interlock remain unchanged.

## Prior current-work evidence (module color complement checkpoint)

- Milestone: Milestone 10 self-contained native stylesheet frontends, in parallel with Milestone 8 controlled benchmark evidence
- Work package: `NSASS-011` native Sass evaluation
- State: `IN_PROGRESS`
- Active phase: the two-hundred-seventh native Sass semantic checkpoint, `NSASS-011-COLOR-MODULE-COMPLEMENT-DIRECT-SPLAT-EXPANSION`, is `VERIFIED` at local implementation checkpoint `deba79a5561c2e739496aaf23713009790208a27`. Fresh Dart Sass 1.101.0 probes establish the admitted direct `sass:color.complement()` boundary: default, aliased, and unprefixed module access; ordinary and named calls; comma-list, keyword-map, scalar, bracketed-list, and positional/keyword argument-list splats; provider-compatible later-map override of an earlier named binding; positional/map duplicate rejection; once-only source order; SCSS and indented Sass; legacy RGB/HSL/HWB, transparent, and alpha behavior; exact CSS; color typing; and zero warnings. Native Zig now recognizes `complement` only for a loaded color module and routes its direct calls through the already-verified evaluated-splat expansion, canonical binder, module callable identity, and bounded shared complement kernel used by reflected invocation. Legacy-global direct complement splats retain their former raw unsupported path, and the module's optional `$space` forms remain explicitly unsupported. Empty and overfull splats, unknown and non-string map keys, positional/map duplicates, invalid values, direct missing-channel constructor forms, modern colors, expanded argument limits, and every allocation failure expose no CSS. The provider accepts selected missing-channel inputs and explicit interpolation spaces, but the native boundary remains deliberately narrower and fail-closed rather than broadening beyond the reflected one-color semantics. The test-first focused Debug gate stopped at 554/556 with the positive path and invalid classification failing at the former undefined-module boundary. Final focused native Debug/ReleaseSafe each pass 23/23 steps with 556/556 tests; full compiler Debug/ReleaseSafe each pass 60/60 steps with 1051/1051 tests; and evaluator no-emission cross-compiles pass for Windows x64 GNU and Linux ARM64 GNU in both modes. Native contract 12/12 plus check adds only `built-in-color-module-complement-direct-splat-expansion` while retaining private native Sass rows, production unreachability, zero target package/optional/runtime dependencies, and the closed release interlock. Complete provider/reference/product matrices pass 151/151, preprocessor package policy passes 6/6 plus check over 22 runtime sources, 28 archive files, 86 locked dependencies, and 5 targets, autonomous-runner selftest passes 59/59, and repository-wide Zig formatting, JavaScript/JSON syntax/parsing, and diff checks pass. Sibling probes confirm deterministic one-color legacy-global direct `complement()` comma-list, keyword-map, scalar, bracketed-list, and later-map-override splats with one global-built-in deprecation per successful call; optional interpolation-space semantics remain a separate boundary, and nondeterministic `sass:string.unique-id()` remains unavailable. Other complement semantics, broader missing-channel behavior, modern color semantics, user-module reflection, dynamic loading, imports/forwards, production routing, public capability claims, native graduation, version, release interlock, tag, artifact/package publication, deployment, and every external system remain unchanged.
- Quality closure: `QUAL-001` is verified at `3eced86`; repository-wide Zig formatting debt is zero and the final local validation matrix is green
- Next eligible package: measure the complete Dart Sass 1.101.0 one-color legacy-global direct `complement()` splat boundary and implement only the smallest closed deterministic `NSASS-011-COLOR-LEGACY-COMPLEMENT-DIRECT-SPLAT-EXPANSION` slice if executable evidence supports it. Keep both legacy-global and module-owned optional interpolation-space semantics separate, and keep nondeterministic `sass:string.unique-id()` unavailable pending an explicit deterministic compatibility decision. Do not infer broader complement behavior, missing-channel semantics, user-module reflection, dynamic loading, imports/forwards, broader function semantics, or normalized diagnostics beyond executable evidence. Provider removal and the package-level zero-dependency cutover remain gated on native graduation rather than deleting the exact development oracles early. Independently, `BENCH-007` still requires a dedicated non-emulated Linux x64 runner with labels `[self-hosted, linux, x64, zigcss-benchmark-v1]`; its absence must not pause eligible native frontend work.
- Runtime control: model identity, single-agent execution, isolated-worktree confinement, automatic supervisor-owned green-checkpoint pushes, the owner-bounded post-`NATIVE-009` GitHub-prerelease/npm-`next` authority, and every native graduation/release interlock remain unchanged.

## Prior current-work evidence (legacy color adjust-hue splat checkpoint)

- Milestone: Milestone 10 self-contained native stylesheet frontends, in parallel with Milestone 8 controlled benchmark evidence
- Work package: `NSASS-011` native Sass evaluation
- State: `IN_PROGRESS`
- Active phase: the two-hundred-sixth native Sass semantic checkpoint, `NSASS-011-COLOR-LEGACY-ADJUST-HUE-DIRECT-SPLAT-EXPANSION`, is `VERIFIED` at local implementation checkpoint `a866db43330a0e0afdf0b5dd7dae483a4caa8a20`. Fresh Dart Sass 1.101.0 probes establish exact legacy-global direct `adjust-hue()` splat binding, ordering, SCSS/indented-Sass output, legacy color and alpha behavior, angle units, typing, warnings, invalid-input, limit, and allocation boundaries. Final focused native modes pass 23/23 and 555/555; full compiler modes pass 60/60 and 1050/1050; cross-target, contract, provider/reference/product, package-policy, runner, formatting, syntax, JSON, and diff gates pass.
- Quality closure: `QUAL-001` is verified at `3eced86`; repository-wide Zig formatting debt is zero and the final local validation matrix is green
- Next eligible package at that checkpoint: measure the exact Dart Sass 1.101.0 direct `sass:color.complement()` splat boundary while keeping legacy-global complement splats, optional interpolation spaces, and nondeterministic `sass:string.unique-id()` separate.
- Runtime control: model identity, single-agent execution, isolated-worktree confinement, automatic supervisor-owned green-checkpoint pushes, the owner-bounded post-`NATIVE-009` GitHub-prerelease/npm-`next` authority, and every native graduation/release interlock remain unchanged.

## Prior current-work evidence (legacy color desaturate splat checkpoint)

- Milestone: Milestone 10 self-contained native stylesheet frontends, in parallel with Milestone 8 controlled benchmark evidence
- Work package: `NSASS-011` native Sass evaluation
- State: `IN_PROGRESS`
- Active phase: the two-hundred-fifth native Sass semantic checkpoint, `NSASS-011-COLOR-LEGACY-DESATURATE-DIRECT-SPLAT-EXPANSION`, is `VERIFIED` at local implementation checkpoint `ada67d37ac757e6f447a7910439f317bc3587be6`. Fresh Dart Sass 1.101.0 probes establish exact legacy-global direct `desaturate()` splat binding, ordering, SCSS/indented-Sass output, legacy color and alpha behavior, typing, warnings, invalid-input, limit, and allocation boundaries. Final focused native modes pass 23/23 and 554/554; full compiler modes pass 60/60 and 1049/1049; cross-target, contract, provider/reference/product, package-policy, runner, formatting, syntax, JSON, and diff gates pass.
- Quality closure: `QUAL-001` is verified at `3eced86`; repository-wide Zig formatting debt is zero and the final local validation matrix is green
- Next eligible package at that checkpoint: measure the exact Dart Sass 1.101.0 legacy-global `adjust-hue()` direct-splat boundary while keeping direct `sass:color.adjust-hue()` and nondeterministic `sass:string.unique-id()` unavailable.
- Runtime control: model identity, single-agent execution, isolated-worktree confinement, automatic supervisor-owned green-checkpoint pushes, the owner-bounded post-`NATIVE-009` GitHub-prerelease/npm-`next` authority, and every native graduation/release interlock remain unchanged.

## Prior current-work evidence (legacy color saturate splat checkpoint)

- Milestone: Milestone 10 self-contained native stylesheet frontends, in parallel with Milestone 8 controlled benchmark evidence
- Work package: `NSASS-011` native Sass evaluation
- State: `IN_PROGRESS`
- Active phase: the two-hundred-fourth native Sass semantic checkpoint, `NSASS-011-COLOR-LEGACY-SATURATE-DIRECT-SPLAT-EXPANSION`, is `VERIFIED` at local implementation checkpoint `1d4e0f1864d48133180ae9022a3fa78efc2a6c98`. Fresh Dart Sass 1.101.0 probes establish exact legacy-global direct `saturate()` CSS-filter and color splat binding, ordering, SCSS/indented-Sass output, legacy color and alpha behavior, typing, warnings, invalid-input, limit, and allocation boundaries. Final focused native modes pass 23/23 and 553/553; full compiler modes pass 60/60 and 1048/1048; cross-target, contract, provider/reference/product, package-policy, runner, formatting, syntax, JSON, and diff gates pass.
- Quality closure: `QUAL-001` is verified at `3eced86`; repository-wide Zig formatting debt is zero and the final local validation matrix is green
- Next eligible package at that checkpoint: measure the exact Dart Sass 1.101.0 legacy-global `desaturate()` direct-splat boundary while keeping direct `sass:color.desaturate()` and nondeterministic `sass:string.unique-id()` unavailable.
- Runtime control: model identity, single-agent execution, isolated-worktree confinement, automatic supervisor-owned green-checkpoint pushes, the owner-bounded post-`NATIVE-009` GitHub-prerelease/npm-`next` authority, and every native graduation/release interlock remain unchanged.

## Prior current-work evidence (legacy color darken splat checkpoint)

- Milestone: Milestone 10 self-contained native stylesheet frontends, in parallel with Milestone 8 controlled benchmark evidence
- Work package: `NSASS-011` native Sass evaluation
- State: `IN_PROGRESS`
- Active phase: the two-hundred-third native Sass semantic checkpoint, `NSASS-011-COLOR-LEGACY-DARKEN-DIRECT-SPLAT-EXPANSION`, is `VERIFIED` at local implementation checkpoint `7a7808a47b6b3c88c19d21be3bd56993ce9d00e9`. Fresh Dart Sass 1.101.0 probes establish exact legacy-global direct `darken()` splat binding, ordering, SCSS/indented-Sass output, legacy color and alpha behavior, typing, warnings, invalid-input, limit, and allocation boundaries. Final focused native modes pass 23/23 and 552/552; full compiler modes pass 60/60 and 1047/1047; cross-target, contract, provider/reference/product, package-policy, runner, formatting, syntax, JSON, and diff gates pass.
- Quality closure: `QUAL-001` is verified at `3eced86`; repository-wide Zig formatting debt is zero and the final local validation matrix is green
- Next eligible package at that checkpoint: measure the exact Dart Sass 1.101.0 legacy-global `saturate()` direct-splat boundary while keeping direct `sass:color.saturate()` and nondeterministic `sass:string.unique-id()` unavailable.
- Runtime control: model identity, single-agent execution, isolated-worktree confinement, automatic supervisor-owned green-checkpoint pushes, the owner-bounded post-`NATIVE-009` GitHub-prerelease/npm-`next` authority, and every native graduation/release interlock remain unchanged.

## Prior current-work evidence (legacy color lighten splat checkpoint)

- Milestone: Milestone 10 self-contained native stylesheet frontends, in parallel with Milestone 8 controlled benchmark evidence
- Work package: `NSASS-011` native Sass evaluation
- State: `IN_PROGRESS`
- Active phase: the two-hundred-second native Sass semantic checkpoint, `NSASS-011-COLOR-LEGACY-LIGHTEN-DIRECT-SPLAT-EXPANSION`, is `VERIFIED` at local implementation checkpoint `511e47b06af9bf349704e938ea5abd46d302fca6`. Fresh Dart Sass 1.101.0 probes establish exact legacy-global direct `lighten()` splat binding, ordering, SCSS/indented-Sass output, legacy color and alpha behavior, typing, warnings, invalid-input, limit, and allocation boundaries. Final focused native modes pass 23/23 and 551/551; full compiler modes pass 60/60 and 1046/1046; cross-target, contract, provider/reference/product, package-policy, runner, formatting, syntax, JSON, and diff gates pass.
- Quality closure: `QUAL-001` is verified at `3eced86`; repository-wide Zig formatting debt is zero and the final local validation matrix is green
- Next eligible package at that checkpoint: measure the exact Dart Sass 1.101.0 legacy-global `darken()` direct-splat boundary while keeping direct `sass:color.darken()` and nondeterministic `sass:string.unique-id()` unavailable.
- Runtime control: model identity, single-agent execution, isolated-worktree confinement, automatic supervisor-owned green-checkpoint pushes, the owner-bounded post-`NATIVE-009` GitHub-prerelease/npm-`next` authority, and every native graduation/release interlock remain unchanged.

## Prior current-work evidence (legacy color mix splat checkpoint)

- Milestone: Milestone 10 self-contained native stylesheet frontends, in parallel with Milestone 8 controlled benchmark evidence
- Work package: `NSASS-011` native Sass evaluation
- State: `IN_PROGRESS`
- Active phase: the two-hundred-first native Sass semantic checkpoint, `NSASS-011-COLOR-LEGACY-MIX-DIRECT-SPLAT-EXPANSION`, is `VERIFIED` at local implementation checkpoint `98fa215aed394dd3254bb951c752afa7daa8c53f`. Fresh Dart Sass 1.101.0 probes establish exact legacy-global direct `mix()` splat binding, ordering, SCSS/indented-Sass output, legacy color and alpha behavior, typing, warning, invalid-input, limit, and allocation boundaries. Final focused native modes pass 23/23 and 550/550; full compiler modes pass 60/60 and 1045/1045; cross-target, contract, provider/reference/product, package-policy, runner, formatting, syntax, JSON, and diff gates pass.
- Quality closure: `QUAL-001` is verified at `3eced86`; repository-wide Zig formatting debt is zero and the final local validation matrix is green
- Next eligible package at that checkpoint: measure the exact Dart Sass 1.101.0 legacy-global `lighten()` direct-splat boundary while keeping direct `sass:color.lighten()` and nondeterministic `sass:string.unique-id()` unavailable.
- Runtime control: model identity, single-agent execution, isolated-worktree confinement, automatic supervisor-owned green-checkpoint pushes, the owner-bounded post-`NATIVE-009` GitHub-prerelease/npm-`next` authority, and every native graduation/release interlock remain unchanged.

## Prior current-work evidence (direct color mix checkpoint)

- Milestone: Milestone 10 self-contained native stylesheet frontends, in parallel with Milestone 8 controlled benchmark evidence
- Work package: `NSASS-011` native Sass evaluation
- State: `IN_PROGRESS`
- Active phase: the two-hundredth native Sass semantic checkpoint, `NSASS-011-COLOR-MODULE-MIX-DIRECT-FUNCTION-INVOCATION`, is `VERIFIED` at local implementation checkpoint `fa477b4ec940b79a307b2f9148d0f06bdf3a2777`. Fresh Dart Sass 1.101.0 probes establish exact direct `sass:color.mix()` module access, binding, ordering, SCSS/indented-Sass output, legacy color and alpha behavior, color typing, invalid-input, limit, allocation, and zero-warning boundaries. Final focused native modes pass 23/23 and 549/549; full compiler modes pass 60/60 and 1044/1044; cross-target, contract, provider/reference/product, package-policy, runner, formatting, syntax, JSON, and diff gates pass.
- Quality closure: `QUAL-001` is verified at `3eced86`; repository-wide Zig formatting debt is zero and the final local validation matrix is green
- Next eligible package at that checkpoint: measure the exact Dart Sass 1.101.0 legacy-global `mix()` direct-splat boundary while keeping nondeterministic `sass:string.unique-id()` unavailable.
- Runtime control: model identity, single-agent execution, isolated-worktree confinement, automatic supervisor-owned green-checkpoint pushes, the owner-bounded post-`NATIVE-009` GitHub-prerelease/npm-`next` authority, and every native graduation/release interlock remain unchanged.

## Prior current-work evidence (direct color blackness checkpoint)

- Milestone: Milestone 10 self-contained native stylesheet frontends, in parallel with Milestone 8 controlled benchmark evidence
- Work package: `NSASS-011` native Sass evaluation
- State: `IN_PROGRESS`
- Active phase: the one-hundred-ninety-ninth native Sass semantic checkpoint, `NSASS-011-COLOR-MODULE-BLACKNESS-DIRECT-FUNCTION-INVOCATION`, is `VERIFIED` at local implementation checkpoint `4cb8ea397ffd5a0c7549e9d0518bce2b0b4811f7`. Fresh Dart Sass 1.101.0 probes establish exact direct `sass:color.blackness()` module access, binding, ordering, SCSS/indented-Sass output, percentage typing, warning, invalid-input, limit, allocation, and plain-CSS boundaries. Final focused native modes pass 23/23 and 548/548; full compiler modes pass 60/60 and 1043/1043; cross-target, contract, provider/reference/product, package-policy, runner, formatting, syntax, JSON, and diff gates pass.
- Quality closure: `QUAL-001` is verified at `3eced86`; repository-wide Zig formatting debt is zero and the final local validation matrix is green
- Next eligible package at that checkpoint: measure the exact Dart Sass 1.101.0 direct `sass:color.mix()` boundary while keeping nondeterministic `sass:string.unique-id()` unavailable.
- Runtime control: model identity, single-agent execution, isolated-worktree confinement, automatic supervisor-owned green-checkpoint pushes, the owner-bounded post-`NATIVE-009` GitHub-prerelease/npm-`next` authority, and every native graduation/release interlock remain unchanged.

## Prior current-work evidence (direct color whiteness checkpoint)

- Milestone: Milestone 10 self-contained native stylesheet frontends, in parallel with Milestone 8 controlled benchmark evidence
- Work package: `NSASS-011` native Sass evaluation
- State: `IN_PROGRESS`
- Active phase: the one-hundred-ninety-eighth native Sass semantic checkpoint, `NSASS-011-COLOR-MODULE-WHITENESS-DIRECT-FUNCTION-INVOCATION`, is `VERIFIED` at local implementation checkpoint `c97dd52966eb08bbc4184ae1c5aaad9d9de3279b`. Fresh Dart Sass 1.101.0 probes establish exact direct `sass:color.whiteness()` module access, binding, ordering, SCSS/indented-Sass output, percentage typing, warning, invalid-input, limit, allocation, and bare-CSS boundaries. Final focused native modes pass 23/23 and 547/547; full compiler modes pass 60/60 and 1042/1042; cross-target, contract, provider/reference/product, package-policy, runner, formatting, syntax, JSON, and diff gates pass.
- Quality closure: `QUAL-001` is verified at `3eced86`; repository-wide Zig formatting debt is zero and the final local validation matrix is green
- Next eligible package at that checkpoint: measure the exact Dart Sass 1.101.0 direct `sass:color.blackness()` boundary while keeping nondeterministic `sass:string.unique-id()` unavailable.
- Runtime control: model identity, single-agent execution, isolated-worktree confinement, automatic supervisor-owned green-checkpoint pushes, the owner-bounded post-`NATIVE-009` GitHub-prerelease/npm-`next` authority, and every native graduation/release interlock remain unchanged.

## Prior current-work evidence (legacy string insert checkpoint)

- Milestone: Milestone 10 self-contained native stylesheet frontends, in parallel with Milestone 8 controlled benchmark evidence
- Work package: `NSASS-011` native Sass evaluation
- State: `IN_PROGRESS`
- Active phase: the one-hundred-ninety-seventh native Sass semantic checkpoint, `NSASS-011-STRING-LEGACY-INSERT-DIRECT-SPLAT-EXPANSION`, is `VERIFIED` at local implementation checkpoint `c4babb6decd0394ddcd28a3801f6e3c5674151fa`. Fresh Dart Sass 1.101.0 probes establish exact legacy-global direct `str-insert()` list and map splats, direct-plus-map binding, later-map override, Unicode-scalar insertion, once-only source order, SCSS/indented-Sass output, and one deprecation warning per successful call. Invalid splats, invalid values, deferred calculations, limits, and allocation failures expose no CSS. Final focused native modes pass 23/23 and 546/546; full compiler modes pass 60/60 and 1041/1041; cross-target, contract, provider/reference/product, package-policy, runner, formatting, syntax, JSON, and diff gates pass.
- Quality closure: `QUAL-001` is verified at `3eced86`; repository-wide Zig formatting debt is zero and the final local validation matrix is green
- Next eligible package at that checkpoint: measure and select the smallest deterministic unsupported `NSASS-011` semantic slice while keeping nondeterministic `sass:string.unique-id()` unavailable.
- Runtime control: model identity, single-agent execution, isolated-worktree confinement, automatic supervisor-owned green-checkpoint pushes, the owner-bounded post-`NATIVE-009` GitHub-prerelease/npm-`next` authority, and every native graduation/release interlock remain unchanged.

## Prior current-work evidence (legacy string slice checkpoint)

- Milestone: Milestone 10 self-contained native stylesheet frontends, in parallel with Milestone 8 controlled benchmark evidence
- Work package: `NSASS-011` native Sass evaluation
- State: `IN_PROGRESS`
- Active phase: the one-hundred-ninety-sixth native Sass semantic checkpoint, `NSASS-011-STRING-LEGACY-SLICE-DIRECT-SPLAT-EXPANSION`, is `VERIFIED` at local implementation checkpoint `9a8400d0275098d2d60a40695074a43a4c3b1c5b`. Fresh Dart Sass 1.101.0 probes establish exact two- and three-item positional-list and keyword-map splats, direct-plus-map binding, later-map override, quoted/unquoted/escaped/interpolated strings, Unicode-scalar/combining/ZWJ slicing, negative indexes, once-only source order, SCSS/indented-Sass output, and one deprecation warning per call. Native dispatch selects evaluated splat expansion, the canonical binder, global callable ownership, and the bounded slice kernel only for direct legacy slice calls containing a syntactic splat. Invalid splats, invalid values, deferred calculations, and allocation failures expose no CSS. Final focused native modes pass 23/23 and 545/545; full compiler modes pass 60/60 and 1040/1040; cross-target, contract, provider/reference/product, package-policy, runner, formatting, syntax, JSON, and diff gates pass.
- Quality closure: `QUAL-001` is verified at `3eced86`; repository-wide Zig formatting debt is zero and the final local validation matrix is green
- Next eligible package at that checkpoint: measure the exact Dart Sass 1.101.0 legacy-global `str-insert()` direct-splat boundary while keeping nondeterministic `sass:string.unique-id()` unavailable.
- Runtime control: model identity, single-agent execution, isolated-worktree confinement, automatic supervisor-owned green-checkpoint pushes, the owner-bounded post-`NATIVE-009` GitHub-prerelease/npm-`next` authority, and every native graduation/release interlock remain unchanged.

## Prior current-work evidence (legacy string index checkpoint)

- Milestone: Milestone 10 self-contained native stylesheet frontends, in parallel with Milestone 8 controlled benchmark evidence
- Work package: `NSASS-011` native Sass evaluation
- State: `IN_PROGRESS`
- Active phase: the one-hundred-ninety-fifth native Sass semantic checkpoint, `NSASS-011-STRING-LEGACY-INDEX-DIRECT-SPLAT-EXPANSION`, is `VERIFIED` at local implementation checkpoint `0d7a23e9090b839fda504bdc2ddc2b139bf88241`. Fresh Dart Sass 1.101.0 probes establish exact two-item positional-list and keyword-map splats, direct-plus-map binding, later-map override, quoted/unquoted/escaped/interpolated strings, Unicode-scalar indexing, nullable misses, once-only source order, SCSS/indented-Sass output, and one deprecation warning per call. Native dispatch selects evaluated splat expansion, the canonical binder, global callable ownership, and the bounded index kernel only for direct legacy index calls containing a syntactic splat. Invalid splats, invalid values, deferred calculations, and allocation failures expose no CSS. Final focused native modes pass 23/23 and 544/544; full compiler modes pass 60/60 and 1039/1039; cross-target, contract, provider/reference/product, package-policy, runner, formatting, syntax, JSON, and diff gates pass.
- Quality closure: `QUAL-001` is verified at `3eced86`; repository-wide Zig formatting debt is zero and the final local validation matrix is green
- Next eligible package at that checkpoint: measure and select the smallest deterministic unsupported `NSASS-011` semantic slice while keeping nondeterministic `sass:string.unique-id()` unavailable.
- Runtime control: model identity, single-agent execution, isolated-worktree confinement, automatic supervisor-owned green-checkpoint pushes, the owner-bounded post-`NATIVE-009` GitHub-prerelease/npm-`next` authority, and every native graduation/release interlock remain unchanged.

## Prior current-work evidence (legacy string length checkpoint)

- Milestone: Milestone 10 self-contained native stylesheet frontends, in parallel with Milestone 8 controlled benchmark evidence
- Work package: `NSASS-011` native Sass evaluation
- State: `IN_PROGRESS`
- Active phase: the one-hundred-ninety-fourth native Sass semantic checkpoint, `NSASS-011-STRING-LEGACY-LENGTH-DIRECT-SPLAT-EXPANSION`, is `VERIFIED` at local implementation checkpoint `4b142d4ef46dcbbe2bff6aa7aebf464b2553efba`. Fresh Dart Sass 1.101.0 probes establish exact positional-list and keyword-map splats, later-map override, quoted/unquoted/escaped/interpolated strings, Unicode-scalar length, once-only source order, SCSS/indented-Sass output, and one deprecation warning per call. Native dispatch selects evaluated splat expansion, the canonical binder, global callable ownership, and the bounded length kernel only for direct legacy length calls containing a syntactic splat. Invalid splats and allocation failures expose no CSS. Final focused native modes pass 23/23 and 543/543; full compiler modes pass 60/60 and 1038/1038; cross-target, contract, provider/reference/product, package-policy, runner, formatting, syntax, JSON, and diff gates pass.
- Quality closure: `QUAL-001` is verified at `3eced86`; repository-wide Zig formatting debt is zero and the final local validation matrix is green
- Next eligible package at that checkpoint: measure and select the smallest deterministic unsupported `NSASS-011` semantic slice while keeping nondeterministic `sass:string.unique-id()` unavailable.
- Runtime control: model identity, single-agent execution, isolated-worktree confinement, automatic supervisor-owned green-checkpoint pushes, the owner-bounded post-`NATIVE-009` GitHub-prerelease/npm-`next` authority, and every native graduation/release interlock remain unchanged.

## Prior current-work evidence (legacy string lower case checkpoint)

- Milestone: Milestone 10 self-contained native stylesheet frontends, in parallel with Milestone 8 controlled benchmark evidence
- Work package: `NSASS-011` native Sass evaluation
- State: `IN_PROGRESS`
- Active phase: the one-hundred-ninety-third native Sass semantic checkpoint, `NSASS-011-STRING-LEGACY-LOWER-CASE-DIRECT-SPLAT-EXPANSION`, is `VERIFIED` at local implementation checkpoint `6a746e79e78fb74f1db591cab21df27da93819ce`. Fresh Dart Sass 1.101.0 probes establish exact positional-list and keyword-map splats, later-map override, quoted/unquoted/escaped/interpolated strings, ASCII-only conversion with Unicode bytes preserved, once-only source order, SCSS/indented-Sass output, and one deprecation warning per call. Native dispatch selects evaluated splat expansion, the canonical binder, global callable ownership, and the bounded lower-case kernel only for direct legacy lower-case calls containing a syntactic splat. Invalid splats and allocation failures expose no CSS. Final focused native modes pass 23/23 and 542/542; full compiler modes pass 60/60 and 1037/1037; cross-target, contract, provider/reference/product, package-policy, runner, formatting, syntax, JSON, and diff gates pass.
- Quality closure: `QUAL-001` is verified at `3eced86`; repository-wide Zig formatting debt is zero and the final local validation matrix is green
- Next eligible package at that checkpoint: measure and select the smallest deterministic unsupported `NSASS-011` semantic slice while keeping nondeterministic `sass:string.unique-id()` unavailable.
- Runtime control: model identity, single-agent execution, isolated-worktree confinement, automatic supervisor-owned green-checkpoint pushes, the owner-bounded post-`NATIVE-009` GitHub-prerelease/npm-`next` authority, and every native graduation/release interlock remain unchanged.

## Prior current-work evidence (legacy string upper case checkpoint)

- Milestone: Milestone 10 self-contained native stylesheet frontends, in parallel with Milestone 8 controlled benchmark evidence
- Work package: `NSASS-011` native Sass evaluation
- State: `IN_PROGRESS`
- Active phase: the one-hundred-ninety-second native Sass semantic checkpoint, `NSASS-011-STRING-LEGACY-UPPER-CASE-DIRECT-SPLAT-EXPANSION`, is `VERIFIED` at local implementation checkpoint `ecd81b59cae2d0f718a5d439d652527fc730acab`. Fresh Dart Sass 1.101.0 probes establish the exact direct-call splat boundary for legacy-global `to-upper-case()`: one-item positional-list and keyword-map splats; provider-compatible later-map override of an earlier named binding; quoted, unquoted, escaped, and interpolated strings; ASCII-only conversion with Unicode bytes preserved; once-only source order; SCSS and indented Sass; exact CSS; and one global-built-in deprecation warning per successful call. Native Zig selects the already-verified evaluated splat expansion, canonical binder, global callable ownership, and bounded shared upper-case kernel only when a direct legacy `to-upper-case()` argument is syntactically splatted; ordinary legacy calls and every sibling direct built-in retain their prior dispatch. Empty and overfull list splats, unknown map keys, non-string map keys, positional/map duplicates, expanded argument limits, and every allocation failure expose no CSS. The test-first focused Debug gate stopped at 539/541 with the positive path and invalid-splat classifications failing at the former `SplatUnsupported` boundary. Final focused native Debug/ReleaseSafe each pass 23/23 steps with 541/541 tests; full compiler Debug/ReleaseSafe each pass 60/60 steps with 1036/1036 tests; and evaluator no-emission cross-compiles pass for Windows x64 GNU and Linux ARM64 GNU in both modes. Native contract 12/12 plus check adds only `built-in-string-legacy-upper-case-direct-splat-expansion`; provider/reference/product, package-policy, runner, formatting, syntax, JSON, and diff gates pass.
- Quality closure: `QUAL-001` is verified at `3eced86`; repository-wide Zig formatting debt is zero and the final local validation matrix is green
- Next eligible package at that checkpoint: measure and select the smallest deterministic unsupported `NSASS-011` semantic slice while keeping nondeterministic `sass:string.unique-id()` unavailable.
- Runtime control: model identity, single-agent execution, isolated-worktree confinement, automatic supervisor-owned green-checkpoint pushes, the owner-bounded post-`NATIVE-009` GitHub-prerelease/npm-`next` authority, and every native graduation/release interlock remain unchanged.

## Prior current-work evidence (legacy string quote checkpoint)

- Milestone: Milestone 10 self-contained native stylesheet frontends, in parallel with Milestone 8 controlled benchmark evidence
- Work package: `NSASS-011` native Sass evaluation
- State: `IN_PROGRESS`
- Active phase: the one-hundred-ninety-first native Sass semantic checkpoint, `NSASS-011-STRING-LEGACY-QUOTE-DIRECT-SPLAT-EXPANSION`, is `VERIFIED` at local implementation checkpoint `c6d36205f7b38d7e4cc5936cb0d25b36cf14aa2c`. Fresh Dart Sass 1.101.0 probes establish the exact direct-call splat boundary for legacy-global `quote()`: one-item positional-list and keyword-map splats; provider-compatible later-map override of an earlier named binding; quoted, unquoted, escaped, and interpolated strings; once-only source order; SCSS and indented Sass; exact CSS; and one global-built-in deprecation warning per successful call. Native Zig selects the already-verified evaluated splat expansion, canonical binder, global callable ownership, and bounded shared quote kernel only when a direct legacy `quote()` argument is syntactically splatted; ordinary legacy calls and every sibling direct built-in retain their prior dispatch. Empty and overfull list splats, unknown map keys, non-string map keys, expanded argument limits, and every allocation failure expose no CSS. The test-first focused Debug gate stopped at 538/540 with the positive path and invalid-splat classifications failing at the former `SplatUnsupported` boundary. Final focused native Debug/ReleaseSafe each pass 23/23 steps with 540/540 tests; full compiler Debug/ReleaseSafe each pass 60/60 steps with 1035/1035 tests; and evaluator no-emission cross-compiles pass for Windows x64 GNU and Linux ARM64 GNU in both modes. Native contract 12/12 plus check adds only `built-in-string-legacy-quote-direct-splat-expansion`; provider/reference/product, package-policy, runner, formatting, syntax, JSON, and diff gates pass.
- Quality closure: `QUAL-001` is verified at `3eced86`; repository-wide Zig formatting debt is zero and the final local validation matrix is green
- Next eligible package at that checkpoint: measure and select the smallest deterministic unsupported `NSASS-011` semantic slice while keeping nondeterministic `sass:string.unique-id()` unavailable.
- Runtime control: model identity, single-agent execution, isolated-worktree confinement, automatic supervisor-owned green-checkpoint pushes, the owner-bounded post-`NATIVE-009` GitHub-prerelease/npm-`next` authority, and every native graduation/release interlock remain unchanged.

## Prior current-work evidence (legacy string unquote checkpoint)

- Milestone: Milestone 10 self-contained native stylesheet frontends, in parallel with Milestone 8 controlled benchmark evidence
- Work package: `NSASS-011` native Sass evaluation
- State: `IN_PROGRESS`
- Active phase: the one-hundred-ninetieth native Sass semantic checkpoint, `NSASS-011-STRING-LEGACY-UNQUOTE-DIRECT-SPLAT-EXPANSION`, is `VERIFIED` at local implementation checkpoint `c3c4f74d2e935f7c4fdb656f6f7e472f5127dd61`. Fresh Dart Sass 1.101.0 probes establish the exact direct-call splat boundary for legacy-global `unquote()`: one-item positional-list and keyword-map splats; provider-compatible later-map override of an earlier named binding; quoted, escaped, and interpolated strings; once-only source order; SCSS and indented Sass; exact CSS; and one exact global-built-in deprecation warning per successful call. Native Zig selects the already-verified evaluated splat expansion, canonical binder, global callable ownership, and bounded shared unquote kernel only when a direct legacy `unquote()` argument is syntactically splatted; ordinary legacy calls and every sibling direct built-in retain their prior dispatch. Empty and overfull list splats, unknown map keys, non-string map keys, expanded argument limits, and every allocation failure expose no CSS. The test-first focused Debug gate stopped at 537/539 with the positive path and invalid-splat classifications failing at the former `SplatUnsupported` boundary. Final focused native Debug/ReleaseSafe each pass 23/23 steps with 539/539 tests; full compiler Debug/ReleaseSafe each pass 60/60 steps with 1034/1034 tests; and evaluator no-emission cross-compiles pass for Windows x64 GNU and Linux ARM64 GNU in both modes. Native contract 12/12 plus check adds only `built-in-string-legacy-unquote-direct-splat-expansion`; provider/reference/product, package-policy, runner, formatting, syntax, JSON, and diff gates pass.
- Quality closure: `QUAL-001` is verified at `3eced86`; repository-wide Zig formatting debt is zero and the final local validation matrix is green
- Next eligible package at that checkpoint: measure the exact legacy-global `quote()` direct-splat boundary while keeping nondeterministic `sass:string.unique-id()` unavailable.
- Runtime control: model identity, single-agent execution, isolated-worktree confinement, automatic supervisor-owned green-checkpoint pushes, the owner-bounded post-`NATIVE-009` GitHub-prerelease/npm-`next` authority, and every native graduation/release interlock remain unchanged.

## Prior current-work evidence (direct string unquote checkpoint)

- Milestone: Milestone 10 self-contained native stylesheet frontends, in parallel with Milestone 8 controlled benchmark evidence
- Work package: `NSASS-011` native Sass evaluation
- State: `IN_PROGRESS`
- Active phase: the one-hundred-eighty-ninth native Sass semantic checkpoint, `NSASS-011-STRING-MODULE-UNQUOTE-DIRECT-SPLAT-EXPANSION`, is `VERIFIED` at local implementation checkpoint `ef4eae861d60678a7f1579a242b6d82e0da2d725`. Fresh Dart Sass 1.101.0 probes establish the exact direct-call splat boundary for module-owned `sass:string.unquote()`: default, aliased, and unprefixed access; one-item positional-list and keyword-map splats; provider-compatible later-map override of an earlier named binding; quoted, unquoted, escaped, and interpolated strings; once-only source order; SCSS and indented Sass; exact CSS; and zero warnings. Native Zig routes only module-owned direct `string.unquote()` arguments through the already-verified evaluated splat expansion, canonical binder, and bounded shared unquote kernel used by reflected invocation. Legacy-global `unquote()` retained its prior raw unsupported direct-splat boundary at this checkpoint; every other direct built-in retained its prior dispatch. Empty and overfull list splats, unknown map keys, non-string map keys, positional/map duplicates, expanded argument limits, and every allocation failure expose no CSS. The test-first focused Debug gate stopped at 536/538 with both new tests failing at `SplatUnsupported`. Final focused native Debug/ReleaseSafe each pass 23/23 steps with 538/538 tests; full compiler Debug/ReleaseSafe each pass 60/60 steps with 1033/1033 tests; and evaluator no-emission cross-compiles pass for Windows x64 GNU and Linux ARM64 GNU in both modes. Native contract 12/12 plus check adds only `built-in-string-module-unquote-direct-splat-expansion`; provider/reference/product, package-policy, runner, formatting, syntax, JSON, and diff gates pass.
- Quality closure: `QUAL-001` is verified at `3eced86`; repository-wide Zig formatting debt is zero and the final local validation matrix is green
- Next eligible package at that checkpoint: measure the exact legacy-global `unquote()` direct-splat boundary while keeping nondeterministic `sass:string.unique-id()` unavailable.
- Runtime control: model identity, single-agent execution, isolated-worktree confinement, automatic supervisor-owned green-checkpoint pushes, the owner-bounded post-`NATIVE-009` GitHub-prerelease/npm-`next` authority, and every native graduation/release interlock remain unchanged.

## Prior current-work evidence (direct string quote checkpoint)

- Milestone: Milestone 10 self-contained native stylesheet frontends, in parallel with Milestone 8 controlled benchmark evidence
- Work package: `NSASS-011` native Sass evaluation
- State: `IN_PROGRESS`
- Active phase: the one-hundred-eighty-eighth native Sass semantic checkpoint, `NSASS-011-STRING-MODULE-QUOTE-DIRECT-SPLAT-EXPANSION`, is `VERIFIED` at local implementation checkpoint `dcb2576b1e3ef21f19d3507f0270d9704d8f7d8e`. Fresh Dart Sass 1.101.0 probes establish exact module-owned direct `sass:string.quote()` default/alias/unprefixed access, positional-list and keyword-map splats, later-map override, quoted/unquoted/escaped/interpolated strings, once-only source order, SCSS/indented-Sass output, and zero warnings. Native direct quote dispatch alone reuses the reflected path's evaluated splat expansion, canonical binder, and bounded kernel; legacy-global quote direct splats retain their raw unsupported boundary. Invalid splats, expanded argument ceilings, and allocation failures expose no CSS. The test-first positive and negative filtered Debug gates stopped at the former unsupported boundary before final focused native modes passed 23/23 and 537/537 and full compiler modes passed 60/60 and 1032/1032. Evaluator cross-compiles, native contract, provider/reference/product, package policy, runner, formatting, syntax, JSON, and diff gates pass. Production reachability, native graduation, release state, and external systems are unchanged.
- Quality closure: `QUAL-001` is verified at `3eced86`; repository-wide Zig formatting debt is zero and the final local validation matrix is green
- Next eligible package at that checkpoint: measure the exact module-owned `sass:string.unquote()` direct-splat boundary while keeping nondeterministic `sass:string.unique-id()` unavailable.
- Runtime control: model identity, single-agent execution, isolated-worktree confinement, automatic supervisor-owned green-checkpoint pushes, the owner-bounded post-`NATIVE-009` GitHub-prerelease/npm-`next` authority, and every native graduation/release interlock remain unchanged.

## Prior current-work evidence (direct string lower-case checkpoint)

- Milestone: Milestone 10 self-contained native stylesheet frontends, in parallel with Milestone 8 controlled benchmark evidence
- Work package: `NSASS-011` native Sass evaluation
- State: `IN_PROGRESS`
- Active phase: the one-hundred-eighty-seventh native Sass semantic checkpoint, `NSASS-011-STRING-MODULE-LOWER-CASE-DIRECT-SPLAT-EXPANSION`, is `VERIFIED` at local implementation checkpoint `a2e73a6156345418ee20b8c4abc8113dbda74dee`. Fresh Dart Sass 1.101.0 probes establish exact module-owned direct `sass:string.to-lower-case()` default/alias/unprefixed access, positional-list and keyword-map splats, later-map override, ASCII-only conversion with Unicode bytes preserved, once-only source order, SCSS/indented-Sass output, and zero warnings. Native direct lower-case dispatch alone reuses the reflected path's evaluated splat expansion, canonical binder, and bounded kernel; legacy-global lower-case and every other direct string function retain their former binders. Invalid splats, expanded argument ceilings, and allocation failures expose no CSS. The test-first filtered Debug gate stopped at the former unsupported boundary before final focused native modes passed 23/23 and 536/536 and full compiler modes passed 60/60 and 1031/1031. Evaluator cross-compiles, native contract, provider/reference/product, package policy, runner, formatting, syntax, JSON, and diff gates pass. Production reachability, native graduation, release state, and external systems are unchanged.
- Quality closure: `QUAL-001` is verified at `3eced86`; repository-wide Zig formatting debt is zero and the final local validation matrix is green
- Next eligible package at that checkpoint: measure and select the smallest deterministic unsupported `NSASS-011` semantic slice while keeping nondeterministic `sass:string.unique-id()` unavailable.
- Runtime control: model identity, single-agent execution, isolated-worktree confinement, automatic supervisor-owned green-checkpoint pushes, the owner-bounded post-`NATIVE-009` GitHub-prerelease/npm-`next` authority, and every native graduation/release interlock remain unchanged.

## Prior current-work evidence (direct string upper-case checkpoint)

- Milestone: Milestone 10 self-contained native stylesheet frontends, in parallel with Milestone 8 controlled benchmark evidence
- Work package: `NSASS-011` native Sass evaluation
- State: `IN_PROGRESS`
- Active phase: the one-hundred-eighty-sixth native Sass semantic checkpoint, `NSASS-011-STRING-MODULE-UPPER-CASE-DIRECT-SPLAT-EXPANSION`, is `VERIFIED` at local implementation checkpoint `d9133550a6233e833fda40f3b728784c1b69a4d4`. Fresh Dart Sass 1.101.0 probes establish exact module-owned direct `sass:string.to-upper-case()` default/alias/unprefixed access, positional-list and keyword-map splats, later-map override, ASCII-only conversion with Unicode bytes preserved, once-only source order, SCSS/indented-Sass output, and zero warnings. Native direct upper-case dispatch alone reuses the reflected path's evaluated splat expansion, canonical binder, and bounded kernel; legacy-global upper-case and module-owned lower-case direct splats retain their former binders. Invalid splats, expanded argument ceilings, and allocation failures expose no CSS. The test-first filtered Debug gates stopped at the former unsupported boundary before final focused native modes passed 23/23 and 535/535 and full compiler modes passed 60/60 and 1030/1030. Evaluator cross-compiles, native contract, provider/reference/product, package policy, runner, formatting, syntax, JSON, and diff gates pass. Production reachability, native graduation, release state, and external systems are unchanged.
- Quality closure: `QUAL-001` is verified at `3eced86`; repository-wide Zig formatting debt is zero and the final local validation matrix is green
- Next eligible package at that checkpoint: measure and select the smallest deterministic unsupported `NSASS-011` semantic slice while keeping nondeterministic `sass:string.unique-id()` unavailable.
- Runtime control: model identity, single-agent execution, isolated-worktree confinement, automatic supervisor-owned green-checkpoint pushes, the owner-bounded post-`NATIVE-009` GitHub-prerelease/npm-`next` authority, and every native graduation/release interlock remain unchanged.

## Prior current-work evidence (direct string insert checkpoint)

- Milestone: Milestone 10 self-contained native stylesheet frontends, in parallel with Milestone 8 controlled benchmark evidence
- Work package: `NSASS-011` native Sass evaluation
- State: `IN_PROGRESS`
- Active phase: the one-hundred-eighty-fifth native Sass semantic checkpoint, `NSASS-011-STRING-MODULE-INSERT-DIRECT-SPLAT-EXPANSION`, is `VERIFIED` at local implementation checkpoint `d11b4f7af81c54628db488edb79a0a6d00d359ce`. Fresh Dart Sass 1.101.0 probes establish exact module-owned direct `sass:string.insert()` default/alias/unprefixed access, positional-list and keyword-map splats, direct-plus-map binding, later-map override, Unicode-scalar insertion, once-only source order, SCSS/indented-Sass output, and zero warnings. Native direct insert dispatch alone reuses the reflected path's evaluated splat expansion, canonical binder, and bounded kernel; legacy-global `str-insert()` and every other direct string function retain their former binder. Invalid splats, expanded argument ceilings, and allocation failures expose no CSS. The test-first focused Debug gate stopped at 532/534 before final focused native modes passed 23/23 and 534/534 and full compiler modes passed 60/60 and 1029/1029. Evaluator cross-compiles, native contract, provider/reference/product, package policy, runner, formatting, syntax, JSON, and diff gates pass. Production reachability, native graduation, release state, and external systems are unchanged.
- Quality closure: `QUAL-001` is verified at `3eced86`; repository-wide Zig formatting debt is zero and the final local validation matrix is green
- Next eligible package at that checkpoint: measure and select the smallest deterministic unsupported `NSASS-011` semantic slice while keeping nondeterministic `sass:string.unique-id()` unavailable.
- Runtime control: model identity, single-agent execution, isolated-worktree confinement, automatic supervisor-owned green-checkpoint pushes, the owner-bounded post-`NATIVE-009` GitHub-prerelease/npm-`next` authority, and every native graduation/release interlock remain unchanged.

## Prior current-work evidence (direct string slice checkpoint)

- Milestone: Milestone 10 self-contained native stylesheet frontends, in parallel with Milestone 8 controlled benchmark evidence
- Work package: `NSASS-011` native Sass evaluation
- State: `IN_PROGRESS`
- Active phase: the one-hundred-eighty-fourth native Sass semantic checkpoint, `NSASS-011-STRING-MODULE-SLICE-DIRECT-SPLAT-EXPANSION`, is `VERIFIED` at local implementation checkpoint `55680fc088ce31b981094849e2418f3fca525da5`. Fresh Dart Sass 1.101.0 probes establish the exact direct-call splat boundary for module-owned `sass:string.slice()`: default, aliased, and unprefixed access; two- and three-item positional list and keyword map splats; direct positional arguments followed by a map splat; provider-compatible later-map override of earlier named bindings; Unicode-scalar slicing; once-only source order; SCSS and indented Sass; exact CSS; and zero warnings. Native Zig now routes only module-owned direct `string.slice()` arguments through the already-verified evaluated splat expansion, canonical binder, and bounded shared string-slice kernel used by reflected invocation. Legacy-global `str-slice()` retains its prior raw unsupported direct-splat boundary even though the sibling oracle probe measured upstream acceptance, and every other direct string function retains its prior binder. Empty, short, and overfull list splats, unknown map keys, non-string map keys, positional/map duplicates, expanded argument limits, and every allocation failure expose no CSS. The test-first focused Debug gate stopped at 531/533 with the positive path returning `SplatUnsupported` and the negative cases retaining the old unsupported classification. Final focused native Debug/ReleaseSafe each pass 23/23 steps with 533/533 tests; full compiler Debug/ReleaseSafe each pass 60/60 steps with 1028/1028 tests; and evaluator no-emission cross-compiles pass for Windows x64 GNU and Linux ARM64 GNU in both modes. Native contract 12/12 plus check adds only `built-in-string-module-slice-direct-splat-expansion` while retaining private native Sass rows, production unreachability, zero target package/optional/runtime dependencies, and the closed release interlock. Complete provider/reference/product matrices pass 151/151, preprocessor package policy passes 6/6 plus check over 22 runtime sources, 28 archive files, 86 locked dependencies, and 5 targets, autonomous-runner selftest passes 59/59, and repository Zig formatting, JavaScript/JSON syntax/parsing, and diff checks pass. The inherited ignored macOS 15.4 SDK selector required by pinned Zig 0.15.2 changed no tracked or system state. Nondeterministic `sass:string.unique-id()` remains unavailable. Broader Sass semantics, legacy-global direct splats, direct splats for other functions, user-module reflection, dynamic loading, imports/forwards, production routing, public capability claims, native graduation, version, release interlock, tag, artifact/package publication, deployment, and every external system remain unchanged.
- Quality closure: `QUAL-001` is verified at `3eced86`; repository-wide Zig formatting debt is zero and the final local validation matrix is green
- Next eligible package: measure and select the smallest deterministic unsupported `NSASS-011` semantic slice. Keep nondeterministic `sass:string.unique-id()` unavailable pending an explicit deterministic compatibility decision. Do not infer direct-call splat behavior for another function, any legacy-global direct-splat boundary, dynamic loading, user-module reflection, imports/forwards, broader function semantics, or normalized diagnostics beyond executable evidence. Provider removal and the package-level zero-dependency cutover remain gated on native graduation rather than deleting the exact development oracles early. Independently, `BENCH-007` still requires a dedicated non-emulated Linux x64 runner with labels `[self-hosted, linux, x64, zigcss-benchmark-v1]`; its absence must not pause eligible native frontend work.
- Runtime control: model identity, single-agent execution, isolated-worktree confinement, automatic supervisor-owned green-checkpoint pushes, the owner-bounded post-`NATIVE-009` GitHub-prerelease/npm-`next` authority, and every native graduation/release interlock remain unchanged.

## Prior current-work evidence (direct string index checkpoint)

- Milestone: Milestone 10 self-contained native stylesheet frontends, in parallel with Milestone 8 controlled benchmark evidence
- Work package: `NSASS-011` native Sass evaluation
- State: `IN_PROGRESS`
- Active phase: the one-hundred-eighty-third native Sass semantic checkpoint, `NSASS-011-STRING-MODULE-INDEX-DIRECT-SPLAT-EXPANSION`, is `VERIFIED` at local implementation checkpoint `980608175ef020af6cc2a107cc5e085a93e4051f`. Fresh Dart Sass 1.101.0 probes establish the exact direct-call splat boundary for module-owned `sass:string.index()`: default, aliased, and unprefixed access; positional list and keyword map splats; direct positional arguments followed by a map splat; provider-compatible later-map override of an earlier named binding; Unicode-scalar indexing; once-only source order; SCSS and indented Sass; exact CSS; and zero warnings. Native Zig now routes only module-owned direct `string.index()` arguments through the already-verified evaluated splat expansion, canonical binder, and bounded shared string-index kernel used by reflected invocation. Legacy-global `str-index()` and direct splats for every other string function retain their prior raw binding boundary. Empty, short, and overfull list splats, unknown map keys, non-string map keys, positional/map duplicates, expanded argument limits, and every allocation failure expose no CSS. The test-first focused Debug gate stopped at 530/532 with the positive path returning `SplatUnsupported` and the negative cases retaining the old unsupported classification. Final focused native Debug/ReleaseSafe each pass 23/23 steps with 532/532 tests; full compiler Debug/ReleaseSafe each pass 60/60 steps with 1027/1027 tests; and root-module no-emission cross-compiles pass for Windows x64 GNU and Linux ARM64 GNU in both modes. Native contract 12/12 plus check adds only `built-in-string-module-index-direct-splat-expansion` while retaining private native Sass rows, production unreachability, zero target package/optional/runtime dependencies, and the closed release interlock. Complete provider/reference/product matrices pass 151/151, preprocessor package policy passes 6/6 plus check over 22 runtime sources, 28 archive files, 86 locked dependencies, and 5 targets, autonomous-runner selftest passes 59/59, and repository Zig formatting, JavaScript/JSON syntax/parsing, and diff checks pass. The inherited ignored macOS 15.4 SDK selector required by pinned Zig 0.15.2 changed no tracked or system state. Direct `string.slice()` splats and nondeterministic `sass:string.unique-id()` remain unavailable. Broader Sass semantics, user-module reflection, dynamic loading, imports/forwards, production routing, public capability claims, native graduation, version, release interlock, tag, artifact/package publication, deployment, and every external system remain unchanged.
- Quality closure: `QUAL-001` is verified at `3eced86`; repository-wide Zig formatting debt is zero and the final local validation matrix is green
- Next eligible package: measure and select the smallest deterministic unsupported `NSASS-011` semantic slice. Keep nondeterministic `sass:string.unique-id()` unavailable pending an explicit deterministic compatibility decision. Do not infer direct-call splat behavior for another function, the legacy-global `str-index()` splat boundary, dynamic loading, user-module reflection, imports/forwards, broader function semantics, or normalized diagnostics beyond executable evidence. Provider removal and the package-level zero-dependency cutover remain gated on native graduation rather than deleting the exact development oracles early. Independently, `BENCH-007` still requires a dedicated non-emulated Linux x64 runner with labels `[self-hosted, linux, x64, zigcss-benchmark-v1]`; its absence must not pause eligible native frontend work.
- Runtime control: model identity, single-agent execution, isolated-worktree confinement, automatic supervisor-owned green-checkpoint pushes, the owner-bounded post-`NATIVE-009` GitHub-prerelease/npm-`next` authority, and every native graduation/release interlock remain unchanged.

## Prior current-work evidence (direct string length checkpoint)

- Milestone: Milestone 10 self-contained native stylesheet frontends, in parallel with Milestone 8 controlled benchmark evidence
- Work package: `NSASS-011` native Sass evaluation
- State: `IN_PROGRESS`
- Active phase: the one-hundred-eighty-second native Sass semantic checkpoint, `NSASS-011-STRING-MODULE-LENGTH-DIRECT-SPLAT-EXPANSION`, is `VERIFIED` at local implementation checkpoint `9f5a82c266abd3b2e6df83a907ad00ba67cc42a2`. Fresh Dart Sass 1.101.0 probes establish the exact direct-call splat boundary for module-owned `sass:string.length()`: default, aliased, and unprefixed access; positional list and keyword map splats; provider-compatible later-map override of an earlier named binding; once-only source order; SCSS and indented Sass; exact CSS; and zero warnings. Native Zig now routes only module-owned direct `string.length()` arguments through the already-verified evaluated splat expansion, canonical binder, and bounded shared string-length kernel used by reflected invocation. Legacy-global `str-length()` and direct splats for every other string function retain their prior raw binding boundary. Empty and overfull list splats, unknown map keys, non-string map keys, positional/map duplicates, expanded argument limits, and every allocation failure expose no CSS. The test-first focused Debug gate stopped at 529/531 with the positive path returning `SplatUnsupported` and the negative cases retaining the old unsupported classification. Final focused native Debug/ReleaseSafe each pass 23/23 steps with 531/531 tests; full compiler Debug/ReleaseSafe each pass 60/60 steps with 1026/1026 tests; and evaluator no-emission cross-compiles pass for Windows x64 GNU and Linux ARM64 GNU in both modes. Native contract 12/12 plus check adds only `built-in-string-module-length-direct-splat-expansion` while retaining private native Sass rows, production unreachability, zero target package/optional/runtime dependencies, and the closed release interlock. Complete provider/reference/product matrices pass 151/151, preprocessor package policy passes 6/6 plus check over 22 runtime sources, 28 archive files, 86 locked dependencies, and 5 targets, autonomous-runner selftest passes 59/59, and repository Zig formatting, JavaScript/JSON syntax/parsing, and diff checks pass. The inherited ignored macOS 15.4 SDK selector required by pinned Zig 0.15.2 changed no tracked or system state. Nondeterministic `sass:string.unique-id()` remains unavailable. Direct-call splat behavior for every other function, broader Sass semantics, user-module reflection, dynamic loading, imports/forwards, production routing, public capability claims, native graduation, version, release interlock, tag, artifact/package publication, deployment, and every external system remain unchanged.
- Quality closure: `QUAL-001` is verified at `3eced86`; repository-wide Zig formatting debt is zero and the final local validation matrix is green
- Next eligible package: measure and select the smallest deterministic unsupported `NSASS-011` semantic slice. Keep nondeterministic `sass:string.unique-id()` unavailable pending an explicit deterministic compatibility decision. Do not infer direct-call splat behavior for another function, the legacy-global `str-length()` splat boundary, dynamic loading, user-module reflection, imports/forwards, broader function semantics, or normalized diagnostics beyond executable evidence. Provider removal and the package-level zero-dependency cutover remain gated on native graduation rather than deleting the exact development oracles early. Independently, `BENCH-007` still requires a dedicated non-emulated Linux x64 runner with labels `[self-hosted, linux, x64, zigcss-benchmark-v1]`; its absence must not pause eligible native frontend work.
- Runtime control: model identity, single-agent execution, isolated-worktree confinement, automatic supervisor-owned green-checkpoint pushes, the owner-bounded post-`NATIVE-009` GitHub-prerelease/npm-`next` authority, and every native graduation/release interlock remain unchanged.

## Prior current-work evidence (direct string split checkpoint)

- Milestone: Milestone 10 self-contained native stylesheet frontends, in parallel with Milestone 8 controlled benchmark evidence
- Work package: `NSASS-011` native Sass evaluation
- State: `IN_PROGRESS`
- Active phase: the one-hundred-eighty-first native Sass semantic checkpoint, `NSASS-011-STRING-SPLIT-DIRECT-SPLAT-EXPANSION`, is `VERIFIED` at local implementation checkpoint `3a409222cc9c7bdb3d7066e07be46f0f354a3d7f`. Fresh Dart Sass 1.101.0 probes establish the exact direct-call splat boundary for the already-graduated `sass:string.split()` callable. Positional list splats, keyword map splats, direct positional arguments followed by a map splat, provider-compatible later-map override of an earlier named binding, once-only source order, SCSS, and indented Sass match exact oracle CSS with no warnings. Native Zig now routes only direct `string.split()` arguments through the already-verified evaluated splat expansion, canonical binder, and bounded shared split kernel used by reflected invocation; every other direct built-in retains its prior binding path. Empty and overfull list splats, unknown map keys, non-string map keys, expanded argument limits, and every allocation failure expose no CSS. The test-first focused Debug gate stopped at 529/530 with `SplatUnsupported` at the former raw direct binder. Final focused native Debug/ReleaseSafe each pass 23/23 steps with 530/530 tests, full compiler Debug/ReleaseSafe each pass 60/60 steps with 1025/1025 tests, and evaluator no-emission cross-compiles pass for Windows x64 GNU and Linux ARM64 GNU in both modes. Native contract 12/12 plus check adds only `built-in-string-split-direct-splat-expansion` while retaining private native Sass rows, production unreachability, zero target package/optional/runtime dependencies, and the closed release interlock. Complete provider/reference/product matrices pass 151/151, preprocessor package policy passes 6/6 plus check over 22 runtime sources, 28 archive files, 86 locked dependencies, and 5 targets, autonomous-runner selftest passes 59/59, and repository Zig formatting, JavaScript/JSON syntax/parsing, and diff checks pass. The inherited ignored macOS 15.4 SDK selector required by pinned Zig 0.15.2 changed no tracked or system state. Nondeterministic `sass:string.unique-id()` remains unavailable. Direct-call splat behavior for every other function, other Sass semantics, user-module reflection, dynamic loading, imports/forwards, production routing, public capability claims, native graduation, version, release interlock, tag, artifact/package publication, deployment, and every external system remain unchanged.
- Quality closure: `QUAL-001` is verified at `3eced86`; repository-wide Zig formatting debt is zero and the final local validation matrix is green
- Next eligible package: measure and select the smallest deterministic unsupported `NSASS-011` semantic slice. Keep nondeterministic `sass:string.unique-id()` unavailable pending an explicit deterministic compatibility decision. Do not infer direct-call splat behavior for any other function, dynamic loading, user-module reflection, imports/forwards, broader function semantics, or normalized diagnostics beyond executable evidence. Provider removal and the package-level zero-dependency cutover remain gated on native graduation rather than deleting the exact development oracles early. Independently, `BENCH-007` still requires a dedicated non-emulated Linux x64 runner with labels `[self-hosted, linux, x64, zigcss-benchmark-v1]`; its absence must not pause eligible native frontend work.
- Runtime control: model identity, single-agent execution, isolated-worktree confinement, automatic supervisor-owned green-checkpoint pushes, the owner-bounded post-`NATIVE-009` GitHub-prerelease/npm-`next` authority, and every native graduation/release interlock remain unchanged.

## Prior current-work evidence (string split checkpoint)

- Milestone: Milestone 10 self-contained native stylesheet frontends, in parallel with Milestone 8 controlled benchmark evidence
- Work package: `NSASS-011` native Sass evaluation
- State: `IN_PROGRESS`
- Active phase: the one-hundred-eightieth native Sass semantic checkpoint, `NSASS-011-META-STRING-SPLIT-FUNCTION-INVOCATION`, is `VERIFIED` at local implementation checkpoint `791b0dc60d6b1d8b019aacfdb26c04ebfa74d763`. Fresh Dart Sass 1.101.0 probes establish the exact direct and reflected `sass:string.split()` callable boundary. Default, aliased, and unprefixed module access retain one callable identity; the callable is not a legacy global unless introduced through `@use "sass:string" as *`, and an otherwise bare call remains plain CSS. Positional, named, list and map splats through the reflected boundary, once-only source order, bracketed comma-list results, quoted/unquoted item typing, SCSS, and indented Sass match the provider. Splitting uses decoded non-overlapping matches; preserves leading, trailing, consecutive, and absent-separator results; handles Unicode scalars, combining marks, ZWJ sequences, and escapes; returns an empty list for an empty input; and splits by Unicode scalar while ignoring the limit for an empty separator. A null limit is unbounded, positive integer limits count splits, units are ignored, and integer comparison uses the provider-compatible `1e-11` rounded fuzzy boundary. Direct invocation and `meta.call()` share one bounded native kernel and canonical binders. Missing, extra, unknown, or duplicate arguments; invalid values; non-positive or non-integer limits; deferred calculations; unloaded modules; argument, temporary-byte, operation, and collection ceilings; and allocation failures expose no CSS. The test-first focused gate stopped at unavailable split dispatch after aligning a new boundary assertion with the already-preserved plain-CSS spacing. Final string-kernel tests pass 5/5, filtered split evaluator tests pass 3/3, focused native Debug/ReleaseSafe each pass 23/23 steps with 529/529 tests, full compiler Debug/ReleaseSafe each pass 60/60 steps with 1024/1024 tests, and root-module no-emission cross-compiles pass for Windows x64 GNU and Linux ARM64 GNU in both modes. Native contract 12/12 plus check adds only `built-in-meta-string-split-function-invocation` while retaining private native Sass rows, production unreachability, zero target package/optional/runtime dependencies, and the closed release interlock. Complete provider/reference/product matrices pass 151/151, preprocessor package policy passes 6/6 plus check over 22 runtime sources, 28 archive files, 86 locked dependencies, and 5 targets, autonomous-runner selftest passes 59/59, and repository Zig formatting, JavaScript/JSON syntax/parsing, and diff checks pass. The inherited ignored macOS 15.4 SDK selector required by pinned Zig 0.15.2 changed no tracked or system state. Nondeterministic `sass:string.unique-id()` remains unavailable. Direct-call splat behavior, other Sass semantics, user-module reflection, dynamic loading, imports/forwards, production routing, public capability claims, native graduation, version, release interlock, tag, artifact/package publication, deployment, and every external system remain unchanged.
- Quality closure: `QUAL-001` is verified at `3eced86`; repository-wide Zig formatting debt is zero and the final local validation matrix is green
- Next eligible package: measure and select the smallest deterministic unsupported `NSASS-011` semantic slice. Keep nondeterministic `sass:string.unique-id()` unavailable pending an explicit deterministic compatibility decision. Do not infer direct-call splat behavior, dynamic loading, user-module reflection, imports/forwards, broader function semantics, or normalized diagnostics beyond executable evidence. Provider removal and the package-level zero-dependency cutover remain gated on native graduation rather than deleting the exact development oracles early. Independently, `BENCH-007` still requires a dedicated non-emulated Linux x64 runner with labels `[self-hosted, linux, x64, zigcss-benchmark-v1]`; its absence must not pause eligible native frontend work.
- Runtime control: model identity, single-agent execution, isolated-worktree confinement, automatic supervisor-owned green-checkpoint pushes, the owner-bounded post-`NATIVE-009` GitHub-prerelease/npm-`next` authority, and every native graduation/release interlock remain unchanged.

## Prior current-work evidence (color powerless checkpoint)

- Milestone: Milestone 10 self-contained native stylesheet frontends, in parallel with Milestone 8 controlled benchmark evidence
- Work package: `NSASS-011` native Sass evaluation
- State: `IN_PROGRESS`
- Active phase: the one-hundred-seventy-ninth native Sass semantic checkpoint, `NSASS-011-META-COLOR-IS-POWERLESS-FUNCTION-INVOCATION`, is `VERIFIED` at local implementation checkpoint `b380a51d1dba6bf53fe0fc6803a07af4cd64c75c`. Fresh Dart Sass 1.101.0 probes establish the exact direct and reflected `sass:color.is-powerless()` callable boundary. Default, aliased, and unprefixed module access retain one callable identity; the callable is not a legacy global unless introduced through `@use "sass:color" as *`, and an otherwise bare call remains plain CSS. Positional, named, list and map splats, once-only source order, boolean result typing, SCSS, and indented Sass match the provider. Only the stored `hue` channel can be powerless: HSL saturation is fuzzy zero, HWB whiteness plus blackness is fuzzy at least 100%, and LCH/OKLCH chroma is fuzzy zero; every other valid channel and space returns false. The allocation-free native kernel uses the provider's exact absolute `5e-12` powerless boundary and treats already-represented missing modern LCH/OKLCH chroma as stored zero. Direct invocation and `meta.call()` share the typed kernel, quoted case-sensitive channel decoder, and canonical binders. Missing or extra arguments, unknown or duplicate bindings, non-color or non-string values, invalid or wrong-space channels, deferred values, unloaded modules, argument limits, unsupported legacy HSL/HWB missing-channel constructors, and every allocation failure expose no CSS. The test-first focused Debug gate stopped exactly at the absent `isPowerless` kernel entry point. Final direct evaluator Debug/ReleaseSafe each pass 388/388; focused native Debug/ReleaseSafe each pass 23/23 steps with 525/525 tests; full compiler Debug/ReleaseSafe each pass 60/60 steps with 1020/1020 tests; and root-module no-emission cross-compiles pass for Windows x64 GNU and Linux ARM64 GNU in both modes. Native contract 12/12 plus check adds only `built-in-meta-color-is-powerless-function-invocation` while retaining private native Sass rows, production unreachability, zero target package/optional/runtime dependencies, and the closed release interlock. Complete provider/reference/product matrices pass 151/151, preprocessor package policy passes 6/6 plus check over 22 runtime sources, 28 archive files, 86 locked dependencies, and 5 targets, autonomous-runner selftest passes 59/59, and repository Zig formatting, JavaScript/JSON syntax/parsing, and diff checks pass. The inherited ignored macOS 15.4 SDK selector required by pinned Zig 0.15.2 changed no tracked or system state. Sibling probing confirms `sass:string.unique-id()` is nondeterministic across identical oracle runs while `sass:string.split()` is deterministic. Legacy missing-channel parsing, unsupported string functions, user-module reflection, dynamic loading, imports/forwards, production routing, public capability claims, native graduation, version, release interlock, tag, artifact/package publication, deployment, and every external system remain unchanged.
- Quality closure: `QUAL-001` is verified at `3eced86`; repository-wide Zig formatting debt is zero and the final local validation matrix is green
- Next eligible package: measure the exact Dart Sass 1.101.0 direct and reflected callable boundary for deterministic `sass:string.split()` and implement only the smallest closed `NSASS-011-META-STRING-SPLIT-FUNCTION-INVOCATION` slice if the executable evidence supports it. Keep nondeterministic `sass:string.unique-id()` unavailable pending an explicit deterministic compatibility decision. Do not infer other string functions, dynamic loading, user-module reflection, imports/forwards, broader direct-call variadic keyword behavior, or normalized diagnostics beyond executable evidence. Provider removal and the package-level zero-dependency cutover remain gated on native graduation rather than deleting the exact development oracles early. Independently, `BENCH-007` still requires a dedicated non-emulated Linux x64 runner with labels `[self-hosted, linux, x64, zigcss-benchmark-v1]`; its absence must not pause eligible native frontend work.
- Runtime control: model identity, single-agent execution, isolated-worktree confinement, automatic supervisor-owned green-checkpoint pushes, the owner-bounded post-`NATIVE-009` GitHub-prerelease/npm-`next` authority, and every native graduation/release interlock remain unchanged.

## Prior current-work evidence (color equality checkpoint)

- Milestone: Milestone 10 self-contained native stylesheet frontends, in parallel with Milestone 8 controlled benchmark evidence
- Work package: `NSASS-011` native Sass evaluation
- State: `IN_PROGRESS`
- Active phase: the one-hundred-seventy-eighth native Sass semantic checkpoint, `NSASS-011-META-COLOR-SAME-FUNCTION-INVOCATION`, is `VERIFIED` at local implementation checkpoint `cfd27ca492060707bb71a3b5837e9f51ce2b871d`. Fresh Dart Sass 1.101.0 probes establish the exact direct and reflected `sass:color.same()` callable boundary. Default, aliased, and unprefixed module access retain one callable identity without warnings; `same` is not a legacy global unless explicitly introduced through `@use "sass:color" as *`, and an otherwise bare call remains plain CSS. Positional, named, list and map splats, once-only source order, boolean result typing, SCSS, and indented Sass match the provider. Same-space values compare all four stored channels with missing channels replaced by zero and Dart Sass's exact `1e-11` rounded fuzzy equality; different-space values replace missing channels with zero, convert through allocation-free XYZ D65, and compare the converted channels by the same rule. Exact and converted equivalents across all 15 represented spaces compare true, XYZ D65 aliases remain identical, different colors compare false, and powerless HSL/LCH hues remain semantically significant rather than being discarded. The dedicated native kernel is separate from the existing Sass `==` implementation, so no broader equality semantics changed. Direct invocation and `meta.call()` share the typed kernel and their canonical binders. Missing or extra arguments, unknown or duplicate bindings, non-color or deferred values, unloaded modules, argument limits, unavailable `is-powerless`, and every allocation failure expose no CSS. The test-first focused Debug gate stopped at 520/521 exactly at the absent direct dispatch. Final direct evaluator Debug/ReleaseSafe each pass 386/386; focused native Debug/ReleaseSafe each pass 23/23 steps with 522/522 tests; full compiler Debug/ReleaseSafe each pass 60/60 steps with 1017/1017 tests; and root-module no-emission cross-compiles pass for Windows x64 GNU and Linux ARM64 GNU in both modes. Native contract 12/12 plus check adds only `built-in-meta-color-same-function-invocation` while retaining private native Sass rows, production unreachability, zero target package/optional/runtime dependencies, and the closed release interlock. Complete provider/reference/product matrices pass 151/151, preprocessor package policy passes 6/6 plus check over 22 runtime sources, 28 archive files, 86 locked dependencies, and 5 targets, autonomous-runner selftest passes 59/59, and repository Zig formatting, JavaScript/JSON syntax/parsing, and diff checks pass. The inherited ignored macOS 15.4 SDK selector required by pinned Zig 0.15.2 changed no tracked or system state. Analogous missing-channel propagation, `is-powerless`, other unsupported color-function invocation, implicit gamut mapping elsewhere, user-module reflection, dynamic loading, imports/forwards, production routing, public capability claims, native graduation, version, release interlock, tag, artifact/package publication, deployment, and every external system remain unchanged.
- Quality closure: `QUAL-001` is verified at `3eced86`; repository-wide Zig formatting debt is zero and the final local validation matrix is green
- Next eligible package: measure the exact Dart Sass 1.101.0 direct and reflected callable boundary for `sass:color.is-powerless()` and implement only the smallest closed `NSASS-011-META-COLOR-IS-POWERLESS-FUNCTION-INVOCATION` slice if the executable evidence supports it. Do not infer analogous missing-channel propagation, any other newly enumerable function, implicit gamut mapping elsewhere, three or more legacy `if()` splats, nonterminal dual-splat recovery, modern CSS conditional evaluation, plain-CSS function references, dynamic loading, user-module reflection, imports/forwards, direct callable CSS serialization, broader direct-call variadic keyword behavior, or normalized diagnostics beyond executable evidence. Provider removal and the package-level zero-dependency cutover remain gated on native graduation rather than deleting the exact development oracles early. Independently, `BENCH-007` still requires a dedicated non-emulated Linux x64 runner with labels `[self-hosted, linux, x64, zigcss-benchmark-v1]`; its absence must not pause eligible native frontend work.
- Runtime control: model identity, single-agent execution, isolated-worktree confinement, automatic supervisor-owned green-checkpoint pushes, the owner-bounded post-`NATIVE-009` GitHub-prerelease/npm-`next` authority, and every native graduation/release interlock remain unchanged.

## Prior current-work evidence (color channel query checkpoint)

- Milestone: Milestone 10 self-contained native stylesheet frontends, in parallel with Milestone 8 controlled benchmark evidence
- Work package: `NSASS-011` native Sass evaluation
- State: `IN_PROGRESS`
- Active phase: the one-hundred-seventy-seventh native Sass semantic checkpoint, `NSASS-011-META-COLOR-CHANNEL-FUNCTION-INVOCATION`, is `VERIFIED` at local implementation checkpoint `2a68222f92072f031e788f5fb762ec5cc2b2afa9`. Fresh Dart Sass 1.101.0 probes establish exact direct and reflected `sass:color.channel()` callable identity, alias and unprefixed access, the closed legacy-global boundary, positional/named/list/map-splat binding, once-only source order, numeric typing, quoted case-sensitive escaped/interpolated channel names, nullable case-insensitive target spaces, XYZ aliasing, and exact unitless, degree, or percentage results for stored and converted values across all 15 represented spaces. Native direct invocation and `meta.call()` share the canonical evaluated binder, quoted-string decoder, and allocation-free conversion core. Missing-channel inputs remain fail-closed rather than inferring substitution or analogous-channel propagation. Invalid binding/value/module/channel/space, deferred values, argument and temporary ceilings, missing channels, and allocation paths fail without CSS. The test-first focused Debug gate stopped at 518/519 exactly at the absent direct dispatch before final direct evaluator Debug/ReleaseSafe passed 384/384, focused native modes passed 23/23 steps and 519/519 tests, and full compiler modes passed 60/60 steps and 1014/1014 tests. Root-module no-emission cross-compiles pass for Windows x64 GNU and Linux ARM64 GNU in both modes; native contract 12/12 plus check, provider/reference/product 151/151, package policy 6/6 plus check, runner selftest 59/59, formatting, syntax, JSON, and diff gates pass. Production reachability, broader missing-channel semantics, native graduation, release state, and external systems are unchanged.
- Quality closure: `QUAL-001` is verified at `3eced86`; repository-wide Zig formatting debt is zero and the final local validation matrix is green
- Next eligible package: measure the exact Dart Sass 1.101.0 direct and reflected callable boundary for `sass:color.same()` and implement only the smallest closed `NSASS-011-META-COLOR-SAME-FUNCTION-INVOCATION` slice if the executable evidence supports it. Do not infer analogous missing-channel propagation, `is-powerless`, any other newly enumerable function, implicit gamut mapping elsewhere, dynamic loading, user-module reflection, imports/forwards, production routing, public capability claims, or release-state changes.
- Runtime control: model identity, single-agent execution, isolated-worktree confinement, automatic supervisor-owned green-checkpoint pushes, the owner-bounded post-`NATIVE-009` GitHub-prerelease/npm-`next` authority, and every native graduation/release interlock remain unchanged.

## Prior current-work evidence (color gamut mapping checkpoint)

- Milestone: Milestone 10 self-contained native stylesheet frontends, in parallel with Milestone 8 controlled benchmark evidence
- Work package: `NSASS-011` native Sass evaluation
- State: `IN_PROGRESS`
- Active phase: the one-hundred-seventy-sixth native Sass semantic checkpoint, `NSASS-011-META-COLOR-TO-GAMUT-FUNCTION-INVOCATION`, is `VERIFIED` at local implementation checkpoint `cc31b78381cb7da18f48877c8632db4b891c459d`. Fresh Dart Sass 1.101.0 probes establish the exact direct and reflected `sass:color.to-gamut()` callable boundary. Default, aliased, underscore-normalized, and unprefixed module access retain one callable identity without warnings; `to-gamut` is not a legacy global unless explicitly introduced through `@use "sass:color" as *`, and an otherwise bare call remains plain CSS. Positional, named, list and map splats, once-only source order, color result typing, SCSS, and indented Sass match the provider. The method is mandatory and accepts only the exact case-sensitive unquoted identifiers `clip` and `local-minde`; the optional space is null/default or a case-insensitive unquoted identifier across all 15 represented spaces, including `xyz-d65` aliasing to `xyz`. Native Zig maps only bounded RGB, HSL, HWB, and predefined RGB-family targets, leaves perceptual and XYZ targets unchanged, preserves alpha, and converts the mapped value back to its original space. The allocation-free kernel implements exact channel clipping and deterministic local-MINDE chroma reduction in OKLCH using Delta E OK, the provider thresholds, and a bounded binary search. Direct invocation and `meta.call()` share one canonical evaluated binder and gamut-mapping kernel. Missing-channel inputs and local-MINDE results whose legacy HSL/HWB hue becomes powerless remain deliberately fail-closed because the current native value serialization cannot preserve the provider's resulting missing hue; no approximate CSS is emitted. Missing or null method, extra, unknown, duplicate, non-color, invalid or quoted space/method, unsupported method casing, deferred, unloaded-module, argument-limit, missing-channel, powerless-result, and every allocation failure expose no CSS. The test-first focused Debug gate stopped exactly at the absent `toGamut` kernel entry point. Final direct evaluator Debug/ReleaseSafe each pass 382/382; focused native Debug/ReleaseSafe each pass 23/23 steps with 517/517 tests; full compiler Debug/ReleaseSafe each pass 60/60 steps with 1012/1012 tests; and evaluator no-emission cross-compiles pass for Windows x64 GNU and Linux ARM64 GNU in both modes. Native contract 12/12 plus check adds only `built-in-meta-color-to-gamut-function-invocation` while retaining private native Sass rows, production unreachability, zero target package/optional/runtime dependencies, and the closed release interlock. Complete provider/reference/product matrices pass 151/151, preprocessor package policy passes 6/6 plus check over 22 runtime sources, 28 archive files, 86 locked dependencies, and 5 targets, autonomous-runner selftest passes 59/59, and repository Zig formatting, JavaScript/JSON syntax/parsing, and diff checks pass. The inherited ignored macOS 15.4 SDK selector required by pinned Zig 0.15.2 changed no tracked or system state. Implicit gamut mapping elsewhere, broader missing-channel propagation, `channel`, `same`, `is-powerless`, other unsupported color-function invocation, user-module reflection, dynamic loading, imports/forwards, production routing, public capability claims, native graduation, version, release interlock, tag, artifact/package publication, deployment, and every external system remain unchanged.
- Quality closure: `QUAL-001` is verified at `3eced86`; repository-wide Zig formatting debt is zero and the final local validation matrix is green
- Next eligible package: measure the exact Dart Sass 1.101.0 direct and reflected callable boundary for `sass:color.channel()` and implement only the smallest closed `NSASS-011-META-COLOR-CHANNEL-FUNCTION-INVOCATION` slice if the executable evidence supports it. Do not infer missing-channel substitution, `same`, `is-powerless`, any other newly enumerable function, implicit gamut mapping elsewhere, three or more legacy `if()` splats, nonterminal dual-splat recovery, modern CSS conditional evaluation, plain-CSS function references, dynamic loading, user-module reflection, imports/forwards, direct callable CSS serialization, broader direct-call variadic keyword behavior, or normalized diagnostics beyond executable evidence. Provider removal and the package-level zero-dependency cutover remain gated on native graduation rather than deleting the exact development oracles early. Independently, `BENCH-007` still requires a dedicated non-emulated Linux x64 runner with labels `[self-hosted, linux, x64, zigcss-benchmark-v1]`; its absence must not pause eligible native frontend work.
- Runtime control: model identity, single-agent execution, isolated-worktree confinement, automatic supervisor-owned green-checkpoint pushes, the owner-bounded post-`NATIVE-009` GitHub-prerelease/npm-`next` authority, and every native graduation/release interlock remain unchanged.

## Prior current-work evidence (color gamut predicate checkpoint)

- Milestone: Milestone 10 self-contained native stylesheet frontends, in parallel with Milestone 8 controlled benchmark evidence
- Work package: `NSASS-011` native Sass evaluation
- State: `IN_PROGRESS`
- Active phase: the one-hundred-seventy-fifth native Sass semantic checkpoint, `NSASS-011-META-COLOR-IS-IN-GAMUT-FUNCTION-INVOCATION`, is `VERIFIED` at local implementation checkpoint `f5be9899409242b3fa777236d328d928954b7223`. Fresh Dart Sass 1.101.0 probes establish the exact direct and reflected `sass:color.is-in-gamut()` callable boundary. Default, aliased, underscore-normalized, and unprefixed module access retain one callable identity without warnings; `is-in-gamut` is not a legacy global unless explicitly introduced through `@use "sass:color" as *`, and an otherwise bare call remains plain CSS. Positional, named, list and map splats, once-only source order, boolean result typing, SCSS, and indented Sass match the provider. Native Zig checks the stored space by default or performs the existing allocation-free conversion for an explicit case-insensitive unquoted target across all 15 represented spaces, including `xyz-d65` aliasing to `xyz`. RGB, HSL, HWB, and the six predefined RGB-family spaces enforce their exact channel bounds with Dart Sass's `1e-11` rounded fuzzy comparison; hue and alpha are excluded, while Lab, LCH, OKLab, OKLCH, and XYZ are unbounded. Missing channels are treated as zero for stored-space and identity checks, while cross-space missing-channel conversion remains deliberately fail-closed rather than inferring propagation. Direct invocation and `meta.call()` share one canonical evaluated binder and gamut predicate without mapping a color. Missing, extra, unknown, duplicate, non-color, invalid target, quoted target, deferred, unloaded-module, argument-limit, cross-space missing-channel, and every allocation failure expose no CSS. The test-first focused Debug gate stopped exactly at the absent `isInGamut` kernel entry point. Final direct evaluator Debug/ReleaseSafe each pass 380/380; focused native Debug/ReleaseSafe each pass 23/23 steps with 514/514 tests; full compiler Debug/ReleaseSafe each pass 60/60 steps with 1009/1009 tests; and evaluator no-emission cross-compiles pass for Windows x64 GNU and Linux ARM64 GNU in both modes. Native contract 12/12 plus check adds only `built-in-meta-color-is-in-gamut-function-invocation` while retaining private native Sass rows, production unreachability, zero target package/optional/runtime dependencies, and the closed release interlock. Complete provider/reference/product matrices pass 151/151, preprocessor package policy passes 6/6 plus check over 22 runtime sources, 28 archive files, 86 locked dependencies, and 5 targets, autonomous-runner selftest passes 59/59, and repository Zig formatting, JavaScript/JSON syntax/parsing, and diff checks pass. The inherited ignored macOS 15.4 SDK selector required by pinned Zig 0.15.2 changed no tracked or system state. Gamut mapping, unsupported mapping methods, broader missing-channel propagation, `to-gamut`, `channel`, `same`, `is-powerless`, other unsupported color-function invocation, user-module reflection, dynamic loading, imports/forwards, production routing, public capability claims, native graduation, version, release interlock, tag, artifact/package publication, deployment, and every external system remain unchanged.
- Quality closure: `QUAL-001` is verified at `3eced86`; repository-wide Zig formatting debt is zero and the final local validation matrix is green
- Next eligible package: measure the exact Dart Sass 1.101.0 direct and reflected callable boundary for `sass:color.to-gamut()` and implement only the smallest closed `NSASS-011-META-COLOR-TO-GAMUT-FUNCTION-INVOCATION` slice if the executable evidence supports it. Do not infer unsupported mapping methods, missing-channel propagation or substitution, `channel`, `same`, `is-powerless`, any other newly enumerable function, three or more legacy `if()` splats, nonterminal dual-splat recovery, modern CSS conditional evaluation, plain-CSS function references, dynamic loading, user-module reflection, imports/forwards, direct callable CSS serialization, broader direct-call variadic keyword behavior, or normalized diagnostics beyond executable evidence. Provider removal and the package-level zero-dependency cutover remain gated on native graduation rather than deleting the exact development oracles early. Independently, `BENCH-007` still requires a dedicated non-emulated Linux x64 runner with labels `[self-hosted, linux, x64, zigcss-benchmark-v1]`; its absence must not pause eligible native frontend work.
- Runtime control: model identity, single-agent execution, isolated-worktree confinement, automatic supervisor-owned green-checkpoint pushes, the owner-bounded post-`NATIVE-009` GitHub-prerelease/npm-`next` authority, and every native graduation/release interlock remain unchanged.

## Prior current-work evidence (missing-channel predicate checkpoint)

- Milestone: Milestone 10 self-contained native stylesheet frontends, in parallel with Milestone 8 controlled benchmark evidence
- Work package: `NSASS-011` native Sass evaluation
- State: `IN_PROGRESS`
- Active phase: the one-hundred-seventy-fourth native Sass semantic checkpoint, `NSASS-011-META-COLOR-IS-MISSING-FUNCTION-INVOCATION`, is `VERIFIED` at local implementation checkpoint `8500ed90300887e0df75eee1bc75aea711d856af`. Fresh Dart Sass 1.101.0 probes establish the exact direct and reflected `sass:color.is-missing()` callable boundary. Default, aliased, underscore-normalized, and unprefixed module access retain one callable identity without warnings; `is-missing` is not a legacy global unless explicitly introduced through `@use "sass:color" as *`, and an otherwise bare call remains plain CSS. Positional, named, list and map splats, escaped and interpolated quoted channel names, once-only source order, boolean result typing, SCSS, and indented Sass match the provider. Native Zig accepts only quoted, case-sensitive channel names valid for the color's stored space and queries the existing four-bit missing mask across all 15 represented spaces without parsing, substituting, converting, or gamut-mapping a channel. Direct invocation and `meta.call()` share one canonical evaluated binder and missing-channel query. Missing, extra, unknown, duplicate, non-color, non-string, unquoted, deferred, wrong-space, unloaded-module, argument-limit, temporary-limit, and every allocation failure expose no CSS. The test-first Debug gate stopped at direct evaluator 376/378 and focused native 509/511 exactly at the unavailable callable. Final direct evaluator Debug/ReleaseSafe each pass 378/378; focused native Debug/ReleaseSafe each pass 23/23 steps with 511/511 tests; full compiler Debug/ReleaseSafe each pass 60/60 steps with 1006/1006 tests; and evaluator no-emission cross-compiles pass for Windows x64 GNU and Linux ARM64 GNU in both modes. Native contract 12/12 plus check adds only `built-in-meta-color-is-missing-function-invocation` while retaining private native Sass rows, production unreachability, zero target package/optional/runtime dependencies, and the closed release interlock. Complete provider/reference/product matrices pass 151/151, preprocessor package policy passes 6/6 plus check over 22 runtime sources, 28 archive files, 86 locked dependencies, and 5 targets, autonomous-runner selftest passes 59/59, and repository Zig formatting, JavaScript/JSON syntax/parsing, and diff checks pass. The inherited ignored macOS 15.4 SDK selector required by pinned Zig 0.15.2 changed no tracked or system state. Missing-channel parsing or substitution, color conversion, gamut semantics, other unsupported color-function invocation, user-module reflection, dynamic loading, imports/forwards, production routing, public capability claims, native graduation, version, release interlock, tag, artifact/package publication, deployment, and every external system remain unchanged.
- Quality closure: `QUAL-001` is verified at `3eced86`; repository-wide Zig formatting debt is zero and the final local validation matrix is green
- Next eligible package: measure the exact Dart Sass 1.101.0 direct and reflected callable boundary for `sass:color.is-in-gamut()` and implement only the smallest closed `NSASS-011-META-COLOR-IS-IN-GAMUT-FUNCTION-INVOCATION` slice if the executable evidence supports it. Do not infer gamut mapping, `to-gamut`, `channel`, `same`, `is-powerless`, missing-channel parsing or substitution, any other newly enumerable function, three or more legacy `if()` splats, nonterminal dual-splat recovery, modern CSS conditional evaluation, plain-CSS function references, dynamic loading, user-module reflection, imports/forwards, direct callable CSS serialization, broader direct-call variadic keyword behavior, or normalized diagnostics beyond executable evidence. Provider removal and the package-level zero-dependency cutover remain gated on native graduation rather than deleting the exact development oracles early. Independently, `BENCH-007` still requires a dedicated non-emulated Linux x64 runner with labels `[self-hosted, linux, x64, zigcss-benchmark-v1]`; its absence must not pause eligible native frontend work.
- Runtime control: model identity, single-agent execution, isolated-worktree confinement, automatic supervisor-owned green-checkpoint pushes, the owner-bounded post-`NATIVE-009` GitHub-prerelease/npm-`next` authority, and every native graduation/release interlock remain unchanged.

## Prior current-work evidence (legacy color predicate checkpoint)

- Milestone: Milestone 10 self-contained native stylesheet frontends, in parallel with Milestone 8 controlled benchmark evidence
- Work package: `NSASS-011` native Sass evaluation
- State: `IN_PROGRESS`
- Active phase: the one-hundred-seventy-third native Sass semantic checkpoint, `NSASS-011-META-COLOR-IS-LEGACY-FUNCTION-INVOCATION`, is `VERIFIED` at local implementation checkpoint `a0c1b5f85c07ea01033512042990629d1c7b5878`. Fresh Dart Sass 1.101.0 probes establish the exact direct and reflected `sass:color.is-legacy()` callable boundary. Default, aliased, underscore-normalized, and unprefixed module access retain one callable identity without warnings; `is-legacy` is not a legacy global unless explicitly introduced through `@use "sass:color" as *`, and an otherwise bare call remains plain CSS. Positional, named, list and map splats, once-only source order, boolean result typing, SCSS, and indented Sass match the provider. Native Zig classifies the three represented legacy spaces RGB, HSL, and HWB as true and every represented modern space as false, including converted colors and already-represented modern missing-channel values without substituting or inspecting channels. Direct invocation and `meta.call()` share one typed color-only predicate and the canonical evaluated binder. Missing, extra, unknown, duplicate, non-color, deferred, unloaded-module, argument-limit, and every allocation failure expose no CSS; the pre-existing unsupported legacy missing-channel constructor boundary was not widened. The test-first Debug gate stopped 1/2 exactly at the unavailable callable. Final direct evaluator Debug/ReleaseSafe each pass 376/376; focused native Debug/ReleaseSafe each pass 23/23 steps with 509/509 tests; full compiler Debug/ReleaseSafe each pass 60/60 steps with 1004/1004 tests; and evaluator no-emission cross-compiles pass for Windows x64 GNU and Linux ARM64 GNU in both modes. Native contract 12/12 plus check adds only `built-in-meta-color-is-legacy-function-invocation` while retaining private native Sass rows, production unreachability, zero target package/optional/runtime dependencies, and the closed release interlock. Complete provider/reference/product matrices pass 151/151, preprocessor package policy passes 6/6 plus check over 22 runtime sources, 28 archive files, 86 locked dependencies, and 5 targets, autonomous-runner selftest passes 59/59, and repository Zig formatting, JavaScript/JSON syntax/parsing, and diff checks pass. The inherited ignored macOS 15.4 SDK selector required by pinned Zig 0.15.2 changed no tracked or system state. Missing-channel parsing or substitution, gamut mapping, other unsupported color-function invocation, user-module reflection, dynamic loading, imports/forwards, production routing, public capability claims, native graduation, version, release interlock, tag, artifact/package publication, deployment, and every external system remain unchanged.
- Quality closure: `QUAL-001` is verified at `3eced86`; repository-wide Zig formatting debt is zero and the final local validation matrix is green
- Next eligible package: measure the exact Dart Sass 1.101.0 direct and reflected callable boundary for `sass:color.is-missing()` and implement only the smallest closed `NSASS-011-META-COLOR-IS-MISSING-FUNCTION-INVOCATION` slice if the executable evidence supports it. Do not infer missing-channel parsing or substitution, `is-in-gamut`, `to-gamut`, `channel`, `same`, `is-powerless`, gamut mapping, any other newly enumerable function, three or more legacy `if()` splats, nonterminal dual-splat recovery, modern CSS conditional evaluation, plain-CSS function references, dynamic loading, user-module reflection, imports/forwards, direct callable CSS serialization, broader direct-call variadic keyword behavior, or normalized diagnostics beyond executable evidence. Provider removal and the package-level zero-dependency cutover remain gated on native graduation rather than deleting the exact development oracles early. Independently, `BENCH-007` still requires a dedicated non-emulated Linux x64 runner with labels `[self-hosted, linux, x64, zigcss-benchmark-v1]`; its absence must not pause eligible native frontend work.
- Runtime control: model identity, single-agent execution, isolated-worktree confinement, automatic supervisor-owned green-checkpoint pushes, the owner-bounded post-`NATIVE-009` GitHub-prerelease/npm-`next` authority, and every native graduation/release interlock remain unchanged.

## Prior current-work evidence (color conversion checkpoint)

- Milestone: Milestone 10 self-contained native stylesheet frontends, in parallel with Milestone 8 controlled benchmark evidence
- Work package: `NSASS-011` native Sass evaluation
- State: `IN_PROGRESS`
- Active phase: the one-hundred-seventy-second native Sass semantic checkpoint, `NSASS-011-META-COLOR-TO-SPACE-FUNCTION-INVOCATION`, is `VERIFIED` at local implementation checkpoint `c9470334dcbf6d00217a4cc3392e92ef30b829f7`. Fresh Dart Sass 1.101.0 probes establish the exact direct and reflected `sass:color.to-space()` boundary. Default, aliased, and unprefixed module access retain one callable identity without warnings; `to-space` is not a legacy global unless explicitly introduced through `@use "sass:color" as *`, and an otherwise bare call remains plain CSS. Positional, named, list and map splats, once-only source order, SCSS, and indented Sass match the provider. Native Zig converts `#369` into all 15 represented target spaces with exact provider serialization and alpha preservation, accepts case-insensitive target identifiers, canonicalizes XYZ D65 to `xyz`, and preserves computed-color serialization provenance through nested identity conversions. Direct invocation and `meta.call()` share the existing allocation-free color conversion core and canonical typed binder. Quoted, malformed, unknown, and non-color/non-string targets, invalid bindings, missing-channel colors, deferred values, unloaded modules, argument limits, and every allocation failure expose no CSS; no missing-channel substitution or gamut mapping was inferred. The test-first baseline stopped at direct evaluator 373/374 and focused native 21/23 steps with 506/507 tests, exactly at the unavailable callable boundary. Final direct evaluator coverage passes 374/374 in Debug and ReleaseSafe; focused native Debug/ReleaseSafe each pass 23/23 steps with 507/507 tests; full compiler Debug/ReleaseSafe each pass 60/60 steps with 1002/1002 tests; and evaluator no-emission cross-compiles pass for Windows x64 GNU and Linux ARM64 GNU in both modes. Native contract 12/12 plus check adds only `built-in-meta-color-to-space-function-invocation` while retaining private native Sass rows, production unreachability, zero target package/optional/runtime dependencies, and the closed release interlock. Complete provider/reference/product matrices pass 151/151, preprocessor package policy passes 6/6 plus check over 22 runtime sources, 28 archive files, 86 locked dependencies, and 5 targets, autonomous-runner selftest passes 59/59, and repository Zig formatting, JavaScript/JSON syntax/parsing, and diff checks pass. The inherited ignored macOS 15.4 SDK selector required by pinned Zig 0.15.2 changed no tracked or system state. Missing-channel substitution, gamut mapping, other unsupported color-function invocation, user-module reflection, dynamic loading, imports/forwards, production routing, public capability claims, native graduation, version, release interlock, tag, artifact/package publication, deployment, and every external system remain unchanged.
- Quality closure: `QUAL-001` is verified at `3eced86`; repository-wide Zig formatting debt is zero and the final local validation matrix is green
- Next eligible package: measure the exact Dart Sass 1.101.0 direct and reflected callable boundary for `sass:color.is-legacy()` and implement only the smallest closed `NSASS-011-META-COLOR-IS-LEGACY-FUNCTION-INVOCATION` slice if the executable evidence supports it. Do not infer `is-missing`, `is-in-gamut`, `to-gamut`, `channel`, `same`, `is-powerless`, missing-channel substitution, gamut mapping, any other newly enumerable function, three or more legacy `if()` splats, nonterminal dual-splat recovery, modern CSS conditional evaluation, plain-CSS function references, dynamic loading, user-module reflection, imports/forwards, direct callable CSS serialization, broader direct-call variadic keyword behavior, or normalized diagnostics beyond executable evidence. Provider removal and the package-level zero-dependency cutover remain gated on native graduation rather than deleting the exact development oracles early. Independently, `BENCH-007` still requires a dedicated non-emulated Linux x64 runner with labels `[self-hosted, linux, x64, zigcss-benchmark-v1]`; its absence must not pause eligible native frontend work.
- Runtime control: model identity, single-agent execution, isolated-worktree confinement, automatic supervisor-owned green-checkpoint pushes, the owner-bounded post-`NATIVE-009` GitHub-prerelease/npm-`next` authority, and every native graduation/release interlock remain unchanged.

## Prior current-work evidence (color space identity checkpoint)

- Milestone: Milestone 10 self-contained native stylesheet frontends, in parallel with Milestone 8 controlled benchmark evidence
- Work package: `NSASS-011` native Sass evaluation
- State: `IN_PROGRESS`
- Active phase: the one-hundred-seventy-first native Sass semantic checkpoint, `NSASS-011-META-COLOR-SPACE-FUNCTION-INVOCATION`, is `VERIFIED` at local implementation checkpoint `96a04da2b35ad93e8f7a2ef1666cef9da8dddded`. Fresh Dart Sass 1.101.0 probes establish the exact direct and reflected `sass:color.space()` boundary. Default, aliased, and unprefixed module access retain one callable identity without warnings; `space` is not a legacy global unless explicitly introduced through `@use "sass:color" as *`, and an otherwise bare `space(red)` remains plain CSS. Positional, named, one-item list and map splats, once-only source order, SCSS, and indented Sass match the provider. Native Zig returns an unquoted string for each represented stored identity: RGB, HSL, HWB, Lab, LCH, OKLab, OKLCH, sRGB, linear sRGB, Display P3, A98 RGB, ProPhoto RGB, Rec. 2020, XYZ D50, and canonical XYZ D65; authored `xyz` and `xyz-d65` both report `xyz`. The direct path and `meta.call()` share one typed color-only implementation and the canonical evaluated binder, preserving the color rather than converting or gamut-mapping it. Missing, extra, unknown, duplicate, null, boolean, number, string, list, map, deferred, unloaded-module, argument-limit, and every allocation failure expose no CSS. Missing-channel colors remain explicitly rejected, and `color.to-space()` remains unavailable rather than inferring channel substitution, conversion, or gamut semantics. The test-first filtered Debug gate stopped 1/2 exactly at the absent callable boundary. Final direct evaluator coverage passes 372/372 in Debug and ReleaseSafe; focused native Debug/ReleaseSafe each pass 23/23 steps with 505/505 tests; full compiler Debug/ReleaseSafe each pass 60/60 steps with 1000/1000 tests; and evaluator no-emission cross-compiles pass for Windows x64 GNU and Linux ARM64 GNU in both modes. Native contract 12/12 plus check adds only `built-in-meta-color-space-function-invocation` while retaining private native Sass rows, production unreachability, zero target package/optional/runtime dependencies, and the closed release interlock. Complete provider/reference/product matrices pass 151/151, preprocessor package policy passes 6/6 plus check over 22 runtime sources, 28 archive files, 86 locked dependencies, and 5 targets, autonomous-runner selftest passes 59/59, and repository Zig formatting, JavaScript/JSON syntax/parsing, and diff checks pass. The inherited ignored macOS 15.4 SDK selector required by pinned Zig 0.15.2 changed no tracked or system state. Color conversion, missing-channel and gamut semantics, other unsupported function invocation, user-module reflection, dynamic loading, imports/forwards, production routing, public capability claims, native graduation, version, release interlock, tag, artifact/package publication, deployment, and every external system remain unchanged.
- Quality closure: `QUAL-001` is verified at `3eced86`; repository-wide Zig formatting debt is zero and the final local validation matrix is green
- Next eligible package: measure the exact Dart Sass 1.101.0 direct and reflected callable boundary for `sass:color.to-space()` and implement only the smallest closed `NSASS-011-META-COLOR-TO-SPACE-FUNCTION-INVOCATION` slice if the executable evidence supports native conversion semantics. Do not infer missing-channel substitution, gamut mapping, any other newly enumerable function, three or more legacy `if()` splats, nonterminal dual-splat recovery, modern CSS conditional evaluation, plain-CSS function references, dynamic loading, user-module reflection, imports/forwards, direct callable CSS serialization, broader direct-call variadic keyword behavior, or normalized diagnostics beyond executable evidence. Provider removal and the package-level zero-dependency cutover remain gated on native graduation rather than deleting the exact development oracles early. Independently, `BENCH-007` still requires a dedicated non-emulated Linux x64 runner with labels `[self-hosted, linux, x64, zigcss-benchmark-v1]`; its absence must not pause eligible native frontend work.
- Runtime control: model identity, single-agent execution, isolated-worktree confinement, automatic supervisor-owned green-checkpoint pushes, the owner-bounded post-`NATIVE-009` GitHub-prerelease/npm-`next` authority, and every native graduation/release interlock remain unchanged.

## Prior current-work evidence (module function enumeration checkpoint)

- Milestone: Milestone 10 self-contained native stylesheet frontends, in parallel with Milestone 8 controlled benchmark evidence
- Work package: `NSASS-011` native Sass evaluation
- State: `IN_PROGRESS`
- Active phase: the one-hundred-seventieth native Sass semantic checkpoint, `NSASS-011-META-MODULE-FUNCTIONS-FUNCTION-INVOCATION`, is `VERIFIED` at local implementation checkpoint `6ce2d4344b9a68de7f2950f54467ace15413d0c5`. Fresh Dart Sass 1.101.0 probes establish direct and reflected `sass:meta.module-functions()` behavior for every loaded built-in module. Default, aliased, underscore-normalized, and unprefixed function origins preserve module ownership; the legacy-global reference remains distinct and owns the normalized global-built-in warning. Positional, named, list-splat, and map-splat binding, once-only source order, SCSS, and indented Sass match the provider. Native Zig returns the complete exact source-ordered inventories for `sass:color` (37), `sass:list` (10), `sass:map` (9), `sass:math` (24), `sass:meta` (18), `sass:selector` (8), and `sass:string` (10): 116 callable references total, including distinct owner identities for same-spelling functions and the provider's shared canonical identity for `ie-hex-str`. One centralized immutable inventory now drives enumeration and reflected lookup, resolves only an already-loaded built-in namespace, charges the call, namespace scan, and each entry to the transaction budget, and transfers the bounded map through the owned value store. The nine newly enumerable color members `space`, `to-space`, `is-legacy`, `is-missing`, `is-in-gamut`, `to-gamut`, `channel`, `same`, and `is-powerless`, plus string `unique-id` and `split`, remain deliberately non-invocable through both direct and `meta.call()` paths rather than admitting unimplemented semantics. Missing, extra, unknown, duplicate, null, non-string, unloaded-namespace, argument-limit, collection-limit, unsupported-reflection invocation, and every allocation failure expose no CSS. The test-first Debug gates stopped exactly at the absent callable/reflection boundary with direct evaluator 368/370 and focused native 501/503. Final direct evaluator coverage passes 370/370 in Debug and ReleaseSafe; focused native Debug/ReleaseSafe each pass 23/23 steps with 503/503 tests; full compiler Debug/ReleaseSafe each pass 60/60 steps with 998/998 tests; and evaluator no-emission cross-compiles pass for Windows x64 GNU and Linux ARM64 GNU in both modes. Native contract 12/12 plus check adds only `built-in-meta-module-functions-function-invocation` while retaining private native Sass rows, production unreachability, zero target package/optional/runtime dependencies, and the closed release interlock. Complete provider/reference/product matrices pass 151/151, preprocessor package policy passes 6/6 plus check over 22 runtime sources, 28 archive files, 86 locked dependencies, and 5 targets, autonomous-runner selftest passes 59/59, and repository Zig formatting, JavaScript/JSON syntax/parsing, and diff checks pass. The inherited ignored macOS 15.4 SDK selector required by pinned Zig 0.15.2 changed no tracked or system state. Unsupported function invocation, user-module reflection, dynamic loading, imports/forwards, production routing, public capability claims, native graduation, version, release interlock, tag, artifact/package publication, deployment, and every external system remain unchanged.
- Quality closure: `QUAL-001` is verified at `3eced86`; repository-wide Zig formatting debt is zero and the final local validation matrix is green
- Next eligible package: measure the exact Dart Sass 1.101.0 direct and reflected callable boundary for `sass:color.space()` and implement only the smallest closed `NSASS-011-META-COLOR-SPACE-FUNCTION-INVOCATION` slice if the executable evidence supports it. Do not infer `to-space`, missing-channel or native gamut-mapping behavior, any other newly enumerable function, three or more legacy `if()` splats, nonterminal dual-splat recovery, modern CSS conditional evaluation, plain-CSS function references, dynamic loading, user-module reflection, imports/forwards, direct callable CSS serialization, broader direct-call variadic keyword behavior, or normalized diagnostics beyond executable evidence. Provider removal and the package-level zero-dependency cutover remain gated on native graduation rather than deleting the exact development oracles early. Independently, `BENCH-007` still requires a dedicated non-emulated Linux x64 runner with labels `[self-hosted, linux, x64, zigcss-benchmark-v1]`; its absence must not pause eligible native frontend work.
- Runtime control: model identity, single-agent execution, isolated-worktree confinement, automatic supervisor-owned green-checkpoint pushes, the owner-bounded post-`NATIVE-009` GitHub-prerelease/npm-`next` authority, and every native graduation/release interlock remain unchanged.

## Prior current-work evidence (module variable enumeration checkpoint)

- Milestone: Milestone 10 self-contained native stylesheet frontends, in parallel with Milestone 8 controlled benchmark evidence
- Work package: `NSASS-011` native Sass evaluation
- State: `IN_PROGRESS`
- Active phase: the one-hundred-sixty-ninth native Sass semantic checkpoint, `NSASS-011-META-MODULE-VARIABLES-FUNCTION-INVOCATION`, is `VERIFIED` at local implementation checkpoint `5acabc98ed7e691553b5a901d12251b04e7fae40`. Fresh Dart Sass 1.101.0 probes establish direct and reflected `sass:meta.module-variables()` behavior for every loaded built-in module. Default, aliased, underscore-normalized, and unprefixed function origins preserve module ownership; the legacy-global reference remains distinct and owns the normalized global-built-in warning. Positional, named, list-splat, and map-splat binding, once-only source order, SCSS, and indented Sass match the provider. `sass:math` returns the exact source-ordered keys `"e"`, `"pi"`, `"epsilon"`, `"max-safe-integer"`, `"min-safe-integer"`, `"max-number"`, and `"min-number"` with the existing IEEE values; the other six built-in modules return empty maps. Native Zig resolves only an already-loaded built-in namespace, walks one static inventory, charges the call, namespace scan, and each math entry to the transaction budget, and transfers the bounded map through the owned value store. Built-in precision provenance now survives value-store cloning and later numeric conversion, so reflected epsilon retains its full payload until arithmetic consumes it rather than silently becoming zero. Missing, extra, unknown, duplicate, null, non-string, unloaded-namespace, argument-limit, collection-limit, and every allocation failure expose no CSS. The test-first filtered Debug gate stopped 0/2 exactly at the absent callable/reflection boundary. Final direct evaluator coverage passes 368/368 in Debug and ReleaseSafe; the numeric unit passes 8/8; focused native Debug/ReleaseSafe each pass 23/23 steps with 501/501 tests; full compiler Debug/ReleaseSafe each pass 60/60 steps with 996/996 tests; and evaluator no-emission cross-compiles pass for Windows x64 GNU and Linux ARM64 GNU in both modes. Native contract 12/12 plus check adds only `built-in-meta-module-variables-function-invocation` while retaining private native Sass rows, production unreachability, zero target package/optional/runtime dependencies, and the closed release interlock. Complete provider/reference/product matrices pass 151/151, preprocessor package policy passes 6/6 plus check, autonomous-runner selftest passes 59/59, and repository Zig formatting, JavaScript/JSON syntax/parsing, and diff checks pass. The inherited ignored macOS 15.4 SDK selector required by pinned Zig 0.15.2 changed no tracked or system state. Module-function enumeration, user-module reflection, dynamic loading, imports/forwards, production routing, public capability claims, native graduation, version, release interlock, tag, artifact/package publication, deployment, and every external system remain unchanged.
- Quality closure: `QUAL-001` is verified at `3eced86`; repository-wide Zig formatting debt is zero and the final local validation matrix is green
- Next eligible package: measure the exact complete built-in `sass:meta.module-functions()` inventory and select the smallest closed implementation boundary without returning a partial provider map or admitting unimplemented callable invocation. Do not infer missing-channel or native gamut-mapping behavior, three or more legacy `if()` splats, nonterminal dual-splat recovery, modern CSS conditional evaluation, plain-CSS function references, remaining special-function reflection or invocation, dynamic loading, user-module function or variable enumeration, imports/forwards, direct callable CSS serialization, broader direct-call variadic keyword behavior, or normalized diagnostics beyond executable evidence. Provider removal and the package-level zero-dependency cutover remain gated on native graduation rather than deleting the exact development oracles early. Independently, `BENCH-007` still requires a dedicated non-emulated Linux x64 runner with labels `[self-hosted, linux, x64, zigcss-benchmark-v1]`; its absence must not pause eligible native frontend work.
- Runtime control: model identity, single-agent execution, isolated-worktree confinement, automatic supervisor-owned green-checkpoint pushes, the owner-bounded post-`NATIVE-009` GitHub-prerelease/npm-`next` authority, and every native graduation/release interlock remain unchanged.

## Prior current-work evidence (module mixin enumeration checkpoint)

- Milestone: Milestone 10 self-contained native stylesheet frontends, in parallel with Milestone 8 controlled benchmark evidence
- Work package: `NSASS-011` native Sass evaluation
- State: `IN_PROGRESS`
- Active phase: the one-hundred-sixty-eighth native Sass semantic checkpoint, `NSASS-011-META-MODULE-MIXINS-FUNCTION-INVOCATION`, is `VERIFIED` at local implementation checkpoint `69d011060418ab0d322f4cea3d7ba67369a45f91`. Fresh Dart Sass 1.101.0 probes establish direct and reflected `sass:meta.module-mixins()` behavior for loaded built-in modules. Default, aliased, underscore-normalized, and unprefixed function origins preserve module ownership; the legacy-global reference remains distinct and owns the normalized global-built-in warning. Positional, named, list-splat, and map-splat binding, once-only source order, SCSS, and indented Sass match the provider. The loaded `sass:meta` namespace returns the exact source-ordered map `("load-css": get-mixin("load-css"), "apply": get-mixin("apply"))`; loaded modules without native mixins return an empty map, and the callable values retain the already-verified mixin identities. Native Zig resolves only an already-loaded built-in namespace, walks one static inventory, charges the call, namespace scan, and each admitted entry to the transaction budget, and transfers the bounded map through the owned value store. Missing, extra, unknown, duplicate, null, non-string, unloaded-namespace, argument-limit, collection-limit, and every allocation failure expose no CSS. Enumeration does not invoke `load-css`, read a stylesheet, or admit user modules. The test-first Debug baseline stopped exactly at the absent callable with direct evaluator 364/366 and focused native 497/499. Final direct evaluator coverage passes 366/366 in Debug and ReleaseSafe; focused native Debug/ReleaseSafe each pass 23/23 steps with 499/499 tests; full compiler Debug/ReleaseSafe each pass 60/60 steps with 994/994 tests; and evaluator no-emission cross-compiles pass for Windows x64 GNU and Linux ARM64 GNU in both modes. Native contract 12/12 plus check adds only `built-in-meta-module-mixins-function-invocation` while retaining private native Sass rows, production unreachability, zero target package/optional/runtime dependencies, and the closed release interlock. Complete provider/reference/product matrices pass 151/151, preprocessor package policy passes 6/6 plus check, autonomous-runner selftest passes 59/59, and repository Zig formatting, JavaScript/JSON syntax/parsing, and diff checks pass. The isolated macOS 15.4 SDK selector required by pinned Zig 0.15.2 was removed after validation and changed no tracked or system state. Dynamic loading, module-function and module-variable enumeration, user modules, imports/forwards, production routing, public capability claims, native graduation, version, release interlock, tag, artifact/package publication, deployment, and every external system remain unchanged.
- Quality closure: `QUAL-001` is verified at `3eced86`; repository-wide Zig formatting debt is zero and the final local validation matrix is green
- Next eligible package: measure and select the smallest next unsupported dependency-ordered `NSASS-011` Sass semantic slice. Do not infer missing-channel or native gamut-mapping behavior, three or more legacy `if()` splats, nonterminal dual-splat recovery, modern CSS conditional evaluation, plain-CSS function references, remaining special-function reflection or invocation, dynamic loading, remaining module enumeration such as `meta.module-functions()` or `meta.module-variables()`, user-module reflection, imports/forwards, direct callable CSS serialization, broader direct-call variadic keyword behavior, or normalized diagnostics beyond executable evidence. Provider removal and the package-level zero-dependency cutover remain gated on native graduation rather than deleting the exact development oracles early. Independently, `BENCH-007` still requires a dedicated non-emulated Linux x64 runner with labels `[self-hosted, linux, x64, zigcss-benchmark-v1]`; its absence must not pause eligible native frontend work.
- Runtime control: model identity, single-agent execution, isolated-worktree confinement, automatic supervisor-owned green-checkpoint pushes, the owner-bounded post-`NATIVE-009` GitHub-prerelease/npm-`next` authority, and every native graduation/release interlock remain unchanged.

## Prior current-work evidence (reflected apply checkpoint)

- Milestone: Milestone 10 self-contained native stylesheet frontends, in parallel with Milestone 8 controlled benchmark evidence
- Work package: `NSASS-011` native Sass evaluation
- State: `IN_PROGRESS`
- Active phase: the one-hundred-sixty-seventh native Sass semantic checkpoint, `NSASS-011-META-APPLY-MIXIN-APPLICATION`, is `VERIFIED` at local implementation checkpoint `dc169186fb6572a28876fe66ec99f3bd4f391329`. Fresh Dart Sass 1.101.0 probes establish that the already-reflectable `sass:meta` `apply` mixin reference is itself applicable through `meta.apply()`. Default and aliased namespaces plus unprefixed invocation, positional/list-splat/map-splat forwarding, multiple reflected-apply hops, once-only source evaluation, captured parameterized `@content`, SCSS, and indented Sass match the provider without warnings. Native Zig now evaluates one bounded argument list, iteratively consumes only exact reflected `meta.apply` identities, charges each reflected built-in hop to the transaction operation budget, materializes forwarded arguments once, and delegates the terminal already-owned user-mixin reference plus content closure to the existing bounded executor. Missing nested targets, non-callable and function targets, argument limits, and every allocation failure expose no CSS. Reflected `meta.load-css` remains explicitly unsupported and cannot read or resolve any stylesheet. The test-first Debug baseline stopped exactly at the former built-in-mixin rejection with direct evaluator 362/364 and focused native 495/497. Final direct evaluator coverage passes 364/364 in Debug and ReleaseSafe; focused native Debug/ReleaseSafe each pass 23/23 steps with 497/497 tests; full compiler Debug/ReleaseSafe each pass 60/60 steps with 992/992 tests; and evaluator no-emission cross-compiles pass for Windows x64 GNU and Linux ARM64 GNU in both modes. Native contract 12/12 plus check adds only `built-in-meta-apply-mixin-application` while retaining private native Sass rows, production unreachability, zero target package/optional/runtime dependencies, and the closed release interlock. Complete provider/reference/product matrices pass 151/151, preprocessor package policy passes 6/6 plus check, autonomous-runner selftest passes 59/59, and repository Zig formatting, JavaScript/JSON syntax/parsing, and diff checks pass. The isolated macOS 15.4 SDK selector required by pinned Zig 0.15.2 was removed after validation and changed no tracked or system state. Dynamic loading, module enumeration, user modules, imports/forwards, production routing, public capability claims, native graduation, version, release interlock, tag, artifact/package publication, deployment, and every external system remain unchanged.
- Quality closure: `QUAL-001` is verified at `3eced86`; repository-wide Zig formatting debt is zero and the final local validation matrix is green
- Next eligible package: measure and select the smallest next unsupported dependency-ordered `NSASS-011` Sass semantic slice. Do not infer missing-channel or native gamut-mapping behavior, three or more legacy `if()` splats, nonterminal dual-splat recovery, modern CSS conditional evaluation, plain-CSS function references, remaining special-function reflection or invocation, dynamic loading, module enumeration, user-module reflection, imports/forwards, direct callable CSS serialization, broader direct-call variadic keyword behavior, or normalized diagnostics beyond executable evidence. Provider removal and the package-level zero-dependency cutover remain gated on native graduation rather than deleting the exact development oracles early. Independently, `BENCH-007` still requires a dedicated non-emulated Linux x64 runner with labels `[self-hosted, linux, x64, zigcss-benchmark-v1]`; its absence must not pause eligible native frontend work.
- Runtime control: model identity, single-agent execution, isolated-worktree confinement, automatic supervisor-owned green-checkpoint pushes, the owner-bounded post-`NATIVE-009` GitHub-prerelease/npm-`next` authority, and every native graduation/release interlock remain unchanged.

## Prior current-work evidence (user mixin application checkpoint)

- Milestone: Milestone 10 self-contained native stylesheet frontends, in parallel with Milestone 8 controlled benchmark evidence
- Work package: `NSASS-011` native Sass evaluation
- State: `IN_PROGRESS`
- Active phase: the one-hundred-sixty-sixth native Sass semantic checkpoint, `NSASS-011-META-USER-MIXIN-APPLICATION`, is `VERIFIED` at local implementation checkpoint `bb14daa08475bb69bf124de687dd4d038641f137`. Fresh Dart Sass 1.101.0 probes establish that user-mixin references are invocable through `sass:meta.apply()`. Default, aliased, and unprefixed module access, positional and named `$mixin` binding, target positional/named/list-splat/map-splat/rest arguments, once-only source evaluation, captured `@content` forwarding with parameters, SCSS, and indented Sass match the provider without warnings. Native Zig now resolves only loaded built-in module mixin members at `@include`, evaluates the complete `meta.apply()` argument list once, removes the one bound mixin reference, and delegates the remainder plus any content closure to the existing bounded user-mixin executor. Missing/non-callable/function targets, duplicate target binding, missing/extra/unknown target arguments, unloaded namespaces, absent global `apply`, rejected content, built-in mixin targets, argument limits, and every allocation failure expose no CSS. Built-in mixin recursion and `meta.load-css()` remain explicitly unavailable. The test-first Debug baseline stopped exactly at the former qualified-mixin boundary with direct evaluator 360/362 and focused native 493/495. Final direct evaluator coverage passes 362/362 in Debug and ReleaseSafe; focused native Debug/ReleaseSafe each pass 23/23 steps with 495/495 tests; full compiler Debug/ReleaseSafe each pass 60/60 steps with 990/990 tests; and evaluator no-emission cross-compiles pass for Windows x64 GNU and Linux ARM64 GNU in both modes. Native contract 12/12 plus check adds only `built-in-meta-user-mixin-application` while retaining private native Sass rows, production unreachability, zero target package/optional/runtime dependencies, and the closed release interlock. Complete provider/reference/product matrices pass 151/151, preprocessor package policy passes 6/6 plus check, autonomous-runner selftest passes 59/59, and repository Zig formatting, JavaScript/JSON syntax/parsing, and diff checks pass. The existing isolated macOS 15.4 SDK selector required by pinned Zig 0.15.2 changed no tracked or system state. Built-in mixin application, dynamic loading, production routing, public capability claims, native graduation, version, release interlock, tag, artifact/package publication, deployment, and every external system remain unchanged.
- Quality closure: `QUAL-001` is verified at `3eced86`; repository-wide Zig formatting debt is zero and the final local validation matrix is green
- Next eligible package: measure and select the smallest next unsupported dependency-ordered `NSASS-011` Sass semantic slice. Do not infer missing-channel or native gamut-mapping behavior, three or more legacy `if()` splats, nonterminal dual-splat recovery, modern CSS conditional evaluation, plain-CSS function references, remaining special-function reflection or invocation, built-in mixin application, module enumeration, dynamic loading, user-module reflection, imports/forwards, direct callable CSS serialization, broader direct-call variadic keyword behavior, or normalized diagnostics beyond executable evidence. Provider removal and the package-level zero-dependency cutover remain gated on native graduation rather than deleting the exact development oracles early. Independently, `BENCH-007` still requires a dedicated non-emulated Linux x64 runner with labels `[self-hosted, linux, x64, zigcss-benchmark-v1]`; its absence must not pause eligible native frontend work.
- Runtime control: model identity, single-agent execution, isolated-worktree confinement, automatic supervisor-owned green-checkpoint pushes, the owner-bounded post-`NATIVE-009` GitHub-prerelease/npm-`next` authority, and every native graduation/release interlock remain unchanged.

## Prior current-work evidence (reflected if checkpoint)

- Milestone: Milestone 10 self-contained native stylesheet frontends, in parallel with Milestone 8 controlled benchmark evidence
- Work package: `NSASS-011` native Sass evaluation
- State: `IN_PROGRESS`
- Active phase: the one-hundred-sixty-fifth native Sass semantic checkpoint, `NSASS-011-META-REFLECTED-IF-FUNCTION-INVOCATION`, is `VERIFIED` at local implementation checkpoint `c11b932d2a5e8fca0f044533c12b3290fcf3fc98`. Fresh Dart Sass 1.101.0 probes establish that the already-reflectable special legacy `if` function reference is callable through `meta.call()`. Namespaced and aliased `sass:meta` construction plus legacy-global `get-function` retain one callable identity; existence, type, and inspection remain exact. Positional, named, list-splat, and map-splat binding for `$condition`, `$if-true`, and `$if-false`, eager once-only callable/argument evaluation, Sass truthiness for true, false, null, and zero, SCSS, and indented Sass match the provider. Reflected invocation emits no direct legacy-`if()` deprecation; only legacy-global reference construction preserves the existing global-built-in warning. Missing, extra, unknown, duplicate, eager undefined-variable, argument-limit, and allocation paths expose no CSS. Native Zig now routes only the reserved reflected-`if` callable identity through the canonical evaluated binder, consumes a bounded operation, and returns the selected already-owned value without changing direct lazy `if()` semantics. The test-first Debug baseline stopped exactly at the former unavailable-callable boundary with direct evaluator 359/360 and focused native 492/493. Final direct evaluator coverage passes 360/360 in Debug and ReleaseSafe; focused native Debug/ReleaseSafe each pass 23/23 steps with 493/493 tests; full compiler Debug/ReleaseSafe each pass 60/60 steps with 988/988 tests; and evaluator no-emission cross-compiles pass for Windows x64 GNU and Linux ARM64 GNU in both modes. Native contract 12/12 plus check adds only `built-in-meta-reflected-if-function-invocation` while retaining private native Sass rows, production unreachability, zero target package/optional/runtime dependencies, and the closed release interlock. Complete provider/reference/product matrices pass 151/151, preprocessor package policy passes 6/6 plus check, autonomous-runner selftest passes 59/59, and repository Zig formatting, JavaScript/JSON syntax/parsing, and diff checks pass. The existing isolated macOS 15.4 SDK selector required by pinned Zig 0.15.2 changed no tracked or system state. Direct legacy `if()`, plain-CSS function references, mixin application, production routing, public capability claims, native graduation, version, release interlock, tag, artifact/package publication, deployment, and every external system remain unchanged.
- Quality closure: `QUAL-001` is verified at `3eced86`; repository-wide Zig formatting debt is zero and the final local validation matrix is green
- Next eligible package: measure and select the smallest next unsupported dependency-ordered `NSASS-011` Sass semantic slice without widening any unmeasured mixin, function-reference, diagnostics, or production boundary.
- Runtime control: model identity, single-agent execution, isolated-worktree confinement, automatic supervisor-owned green-checkpoint pushes, the owner-bounded post-`NATIVE-009` GitHub-prerelease/npm-`next` authority, and every native graduation/release interlock remain unchanged.

## Prior current-work evidence (meta get-mixin checkpoint)

- Milestone: Milestone 10 self-contained native stylesheet frontends, in parallel with Milestone 8 controlled benchmark evidence
- Work package: `NSASS-011` native Sass evaluation
- State: `IN_PROGRESS`
- Active phase: the one-hundred-sixty-fourth native Sass semantic checkpoint, `NSASS-011-META-GET-MIXIN-FUNCTION-INVOCATION`, is `VERIFIED` at local implementation checkpoint `f41b05a57050b1052044552090441a5943531b41`. Fresh Dart Sass 1.101.0 probes establish that the already-reflectable module-owned `sass:meta` `get-mixin` reference and its distinct legacy-global counterpart are callable through `meta.call()`. Default, aliased, underscore-normalized, and unprefixed module access retain one identity; the global reference remains distinct. Positional, named, list-splat, and map-splat binding for `$name` and `$module`, once-only callable/argument evaluation, explicit-null local lookup, module alias resolution, user and built-in mixin references, SCSS, and indented Sass match the provider; only successful legacy-global invocation preserves the existing global-built-in deprecation. Missing, extra, unknown, duplicate, non-string, unknown-mixin, unloaded-module, argument-limit, and allocation paths expose no CSS. Native Zig now routes only the exact global or `sass:meta` `get-mixin` function reference through the canonical evaluated binder and existing mixin-reference constructor, preserving lexical scope, warning ownership, callable identity, argument ceilings, and single evaluation without enabling mixin application. The test-first Debug baseline stopped exactly at the former unavailable-callable boundary with direct evaluator 357/358 and focused native 490/491. Final direct evaluator coverage passes 358/358 in Debug and ReleaseSafe; focused native Debug/ReleaseSafe each pass 23/23 steps with 491/491 tests; full compiler Debug/ReleaseSafe each pass 60/60 steps with 986/986 tests; and evaluator no-emission cross-compiles pass for Windows x64 GNU and Linux ARM64 GNU in both modes. Native contract 12/12 plus check adds only `built-in-meta-get-mixin-function-invocation` while retaining private native Sass rows, production unreachability, zero target package/optional/runtime dependencies, and the closed release interlock. Complete provider/reference/product matrices pass 151/151, preprocessor package policy passes 6/6 plus check, autonomous-runner selftest passes 59/59, and repository Zig formatting, JavaScript/JSON syntax/parsing, and diff checks pass. The existing isolated macOS 15.4 SDK selector required by pinned Zig 0.15.2 changed no tracked or system state. Production routing, public capability claims, native graduation, version, release interlock, tag, artifact/package publication, deployment, and every external system remain unchanged.
- Quality closure: `QUAL-001` is verified at `3eced86`; repository-wide Zig formatting debt is zero and the final local validation matrix is green
- Next eligible package: measure and select the smallest next unsupported dependency-ordered `NSASS-011` Sass semantic slice without widening any unmeasured function-reference, diagnostics, or production boundary.
- Runtime control: model identity, single-agent execution, isolated-worktree confinement, automatic supervisor-owned green-checkpoint pushes, the owner-bounded post-`NATIVE-009` GitHub-prerelease/npm-`next` authority, and every native graduation/release interlock remain unchanged.

## Prior current-work evidence (meta call checkpoint)

- Milestone: Milestone 10 self-contained native stylesheet frontends, in parallel with Milestone 8 controlled benchmark evidence
- Work package: `NSASS-011` native Sass evaluation
- State: `IN_PROGRESS`
- Active phase: the one-hundred-sixty-second native Sass semantic checkpoint, `NSASS-011-META-CALL-FUNCTION-INVOCATION`, is `VERIFIED` at local implementation checkpoint `d733a1c1c921e4d8577a7c8ea3c4883892d49f85`. Fresh Dart Sass 1.101.0 probes establish that the already-constructible module-owned `sass:meta` `call` reference and its distinct legacy-global counterpart are themselves callable through `meta.call()`. Default, aliased, escaped-name, and unprefixed module access retain one identity without warnings; the global reference remains distinct and preserves the global-built-in deprecation. Positional target arguments, list-splat forwarding, once-only callable/target/argument evaluation, SCSS, and indented Sass match the provider. Missing or non-callable targets, duplicate `$function` binding, conflicting map splats, argument limits, and every allocation failure expose no CSS. Native Zig now reuses the existing evaluated `meta.call()` dispatcher for only the exact global or `sass:meta` reference, preserving lexical scope, module/global warning ownership, argument ceilings, and single evaluation without adding a second callable engine. The test-first Debug baseline stopped exactly at the former unavailable-callable boundary with direct evaluator 353/354 and focused native 486/487. Final direct evaluator coverage passes 354/354 in Debug and ReleaseSafe; focused native Debug/ReleaseSafe each pass 23/23 steps with 487/487 tests; full compiler Debug/ReleaseSafe each pass 60/60 steps with 982/982 tests; and evaluator no-emission cross-compiles pass for Windows x64 GNU and Linux ARM64 GNU in both modes. Native contract 12/12 plus check adds only `built-in-meta-call-function-invocation` while retaining private native Sass rows, production unreachability, zero target package/optional/runtime dependencies, and the closed release interlock. Complete provider/reference/product matrices pass 151/151, preprocessor package policy passes 6/6 plus check, autonomous-runner selftest passes 59/59, and repository Zig formatting, JavaScript/JSON syntax/parsing, and diff checks pass. The existing isolated macOS 15.4 SDK selector required by pinned Zig 0.15.2 changed no tracked or system state. Production routing, public capability claims, native graduation, version, release interlock, tag, artifact/package publication, deployment, and every external system remain unchanged.
- Quality closure: `QUAL-001` is verified at `3eced86`; repository-wide Zig formatting debt is zero and the final local validation matrix is green
- Next eligible package: measure and select the smallest next unsupported dependency-ordered `NSASS-011` Sass semantic slice without widening any unmeasured function-reference, diagnostics, or production boundary.
- Runtime control: model identity, single-agent execution, isolated-worktree confinement, automatic supervisor-owned green-checkpoint pushes, the owner-bounded post-`NATIVE-009` GitHub-prerelease/npm-`next` authority, and every native graduation/release interlock remain unchanged.

## Prior current-work evidence (calc reflection checkpoint)

- Milestone: Milestone 10 self-contained native stylesheet frontends, in parallel with Milestone 8 controlled benchmark evidence
- Work package: `NSASS-011` native Sass evaluation
- State: `IN_PROGRESS`
- Active phase: the one-hundred-sixtieth native Sass semantic checkpoint, `NSASS-011-META-CALC-FUNCTION-REFERENCE-REJECTION`, is `VERIFIED` at local implementation checkpoint `c949fa80c8dcd243db02df9fed7e609b0268411f`. Fresh Dart Sass 1.101.0 probes establish that `calc()` remains an ordinary CSS calculation but is not a reflectable Sass function: module-owned `meta.function-exists("calc")` and the legacy global return false for SCSS and indented Sass, while `meta.get-function("calc")` fails. Provider-free native Zig matches that exact boundary without changing direct behavior: static `calc(1px + 2px)` reduces to `3px`, deferred `calc(1px + var(--x))` remains preserved, and only the legacy existence query emits the existing normalized global-built-in warning. The test-first Debug baseline stopped at direct evaluator 349/350 and focused native 482/483 before final direct evaluator 350/350, focused native 483/483, and full compiler 978/978 passed in Debug and ReleaseSafe. Evaluator no-emission cross-compiles pass for Windows x64 GNU and Linux ARM64 GNU in both modes; native contract 12/12 plus check, provider/reference/product 151/151, package policy 6/6 plus check, runner selftest 59/59, formatting, syntax, JSON, and diff gates pass. Production reachability, native graduation, release state, and external systems are unchanged.
- Quality closure: `QUAL-001` is verified at `3eced86`; repository-wide Zig formatting debt is zero and the final local validation matrix is green
- Next eligible package: measure and select the smallest next unsupported dependency-ordered `NSASS-011` Sass semantic slice without widening any unmeasured function-reference, diagnostics, or production boundary.
- Runtime control: model identity, single-agent execution, isolated-worktree confinement, automatic supervisor-owned green-checkpoint pushes, the owner-bounded post-`NATIVE-009` GitHub-prerelease/npm-`next` authority, and every native graduation/release interlock remain unchanged.

## Prior current-work evidence (ie-hex-str checkpoint)

- Milestone: Milestone 10 self-contained native stylesheet frontends, in parallel with Milestone 8 controlled benchmark evidence
- Work package: `NSASS-011` native Sass evaluation
- State: `IN_PROGRESS`
- Active phase: the one-hundred-fifty-ninth native Sass semantic checkpoint, `NSASS-011-META-COLOR-IE-HEX-STR-FUNCTION-INVOCATION`, is `VERIFIED` at local implementation checkpoint `9b1657baf849add764ca4941310abf3b2cc9b083`. Provider-free native Zig now invokes the shared legacy-global and module-owned `sass:color` `ie-hex-str` function reference through `meta.call()` for SCSS and indented Sass, and admits the same exact callable through direct qualified module invocation. Fresh Dart Sass 1.101.0 probes establish global/default/aliased/unprefixed constructibility, one shared callable identity across all origins, exact required `$color` positional/named binding, list/map splats, uppercase `#AARRGGBB` string output, once-only callable/argument source order, and zero diagnostics. Transparent, opaque, alpha-bearing RGB, HSL, and HWB values match the oracle, including rounded alpha bytes. Exact in-gamut conversion fixtures cover all twelve represented modern spaces: Lab, LCH, OKLab, OKLCH, sRGB, linear sRGB, Display P3, A98 RGB, ProPhoto RGB, Rec. 2020, XYZ D50, and XYZ D65. Missing/extra/unknown/duplicate/empty-splat binding, non-color and deferred values, missing legacy or modern channels, unloaded modules, argument limits, and every allocation failure expose no CSS. Dart Sass gamut-maps out-of-gamut modern colors and treats selected missing channels as zero; native Zig deliberately rejects both boundaries until exact native gamut mapping and missing-channel semantics exist rather than silently clipping or inventing values. The test-first Debug baseline stopped at the new color and callable boundaries with direct evaluator 347/349 and focused native 479/482 before implementation. Final direct evaluator coverage passes 349/349 in Debug and ReleaseSafe; focused native Debug/ReleaseSafe each pass 23/23 steps with 482/482 tests; full compiler Debug/ReleaseSafe each pass 60/60 steps with 977/977 tests; and evaluator no-emission cross-compiles pass for Windows x64 GNU and Linux ARM64 GNU in both modes. Native contract 12/12 plus check adds only `built-in-meta-color-ie-hex-str-function-invocation` while retaining private native Sass rows, production unreachability, zero target package/optional/runtime dependencies, and the closed release interlock. Complete provider/reference/product matrices pass 151/151, preprocessor package policy passes 6/6 plus check, autonomous-runner selftest passes 59/59, and repository Zig formatting, JavaScript/JSON syntax/parsing, and diff checks pass. The existing isolated macOS 15.4 SDK selector required by pinned Zig 0.15.2 changed no tracked or system state. Production routing, public capability claims, native graduation, version, release interlock, tag, artifact/package publication, deployment, and every external system remain unchanged.
- Quality closure: `QUAL-001` is verified at `3eced86`; repository-wide Zig formatting debt is zero and the final local validation matrix is green
- Next eligible package: measure and select the smallest next unsupported dependency-ordered `NSASS-011` Sass semantic slice. Do not infer missing-channel or native gamut-mapping behavior, three or more legacy `if()` splats, nonterminal dual-splat recovery, modern CSS conditional evaluation, plain-CSS function references, remaining calculation/special-function reference invocation, mixin application, module enumeration, dynamic loading, user-module reflection, imports/forwards, direct callable CSS serialization, broader direct-call variadic keyword behavior, or normalized diagnostics beyond executable evidence. Provider removal and the package-level zero-dependency cutover remain gated on native graduation rather than deleting the exact development oracles early. Independently, `BENCH-007` still requires a dedicated non-emulated Linux x64 runner with labels `[self-hosted, linux, x64, zigcss-benchmark-v1]`; its absence must not pause eligible native frontend work.
- Runtime control: model identity, single-agent execution, isolated-worktree confinement, automatic supervisor-owned green-checkpoint pushes, the owner-bounded post-`NATIVE-009` GitHub-prerelease/npm-`next` authority, and every native graduation/release interlock remain unchanged.

## Prior current-work evidence (fade-out checkpoint)

- Milestone: Milestone 10 self-contained native stylesheet frontends, in parallel with Milestone 8 controlled benchmark evidence
- Work package: `NSASS-011` native Sass evaluation
- State: `IN_PROGRESS`
- Active phase: the one-hundred-fifty-eighth native Sass semantic checkpoint, `NSASS-011-META-COLOR-FADE-OUT-FUNCTION-INVOCATION`, is `VERIFIED` at local implementation checkpoint `b3e747ed50fb1918aee87867636d76a8a7df9e5d`. Provider-free native Zig now invokes the distinct legacy-global `fade-out` reference through `meta.call()` for SCSS and indented Sass while preserving the constructible-but-removed module-owned `sass:color` reference. Fresh Dart Sass 1.101.0 probes establish exact global/default/aliased/unprefixed constructibility and identity: module aliases and unprefixed import share the module reference, while the global reference remains distinct. Exact required `$color`/`$amount` positional/named binding, list/map splats, transparent, opaque, alpha-bearing RGB, HSL, and HWB legacy inputs, simple unit-bearing numeric magnitude, zero/full clamping, color result typing, once-only callable/argument source order, and both normalized legacy deprecations match the oracle. HSL/HWB values convert to legacy RGB before alpha subtraction rather than retaining their authored spaces. Removed module invocation, direct qualified calls, missing/extra/unknown/duplicate/empty-splat binding, non-color and non-number values, compound/deferred/out-of-range amounts, missing channels, modern color spaces, unloaded modules, argument limits, and every allocation failure expose no CSS. Provider-accepted compound-unit and missing-channel behavior remains deliberately ungraduated, and the explicit unavailable-callable sentinel now keeps `ie-hex-str` outside this slice. Exact compressed SCSS `.values{global-exists:true;module-exists:true;alias-exists:true;type:function;module-type:function;global-module-same:false;module-alias-same:true;star-module-same:true;star-global-same:false;inspect-global:get-function("fade-out");inspect-module:get-function("fade-out");global:rgba(18,52,86,.4);named:rgba(18,52,86,.4);transparent:rgba(0,0,0,0);opaque:rgba(18,52,86,.8);alpha:rgba(18,52,86,0);hsl:rgba(51,76.5,51,.4);hwb:rgba(25.5,25.5,204,.4);list-splat:rgba(18,52,86,.4);map-splat:rgba(51,76.5,51,.4);zero:rgba(18,52,86,.6);full:rgba(18,52,86,0);ordered:rgba(18,52,86,.4);trace:132;result-type:color}` passes with thirteen normalized global-built-in warnings and thirteen normalized `fade-out()` warnings; indented Sass `.sass{type:function;module-type:function;plain:rgba(51,76.5,51,.4);named:rgba(18,52,86,.4);map:rgba(25.5,25.5,204,.4);module-inspect:get-function("fade-out")}` passes with three warnings of each kind. The test-first Debug gate stopped exactly at unavailable module reflection with direct evaluator 346/347 and focused native 479/480 before implementation. Final direct evaluator coverage passes 347/347 in Debug and ReleaseSafe; focused native Debug/ReleaseSafe each pass 23/23 steps with 480/480 tests; full compiler Debug/ReleaseSafe each pass 60/60 steps with 975/975 tests; and evaluator no-emission cross-compiles pass for Windows x64 GNU and Linux ARM64 GNU in both modes. Native contract 12/12 plus check adds only `built-in-meta-color-fade-out-function-invocation` while retaining private native Sass rows, production unreachability, zero target package/optional/runtime dependencies, and the closed release interlock. Complete provider/reference/product matrices pass 151/151, preprocessor package policy passes 6/6 plus check, autonomous-runner selftest passes 59/59, and repository Zig formatting, JavaScript/JSON syntax/parsing, and diff checks pass. The existing isolated macOS 15.4 SDK selector required by pinned Zig 0.15.2 changed no tracked or system state. Production routing, public capability claims, native graduation, version, release interlock, tag, artifact/package publication, deployment, and every external system remain unchanged.
- Quality closure: `QUAL-001` is verified at `3eced86`; repository-wide Zig formatting debt is zero and the final local validation matrix is green
- Next eligible package: measure the exact Dart Sass 1.101.0 global and module-owned `sass:color` constructibility, callable identity, alias behavior, color binding, supported input-color spaces and alpha behavior, list/map-splat, string result typing, ordering, error, warning, and limit boundaries for `ie-hex-str`, then implement only the smallest evidence-closed `NSASS-011-META-COLOR-IE-HEX-STR-FUNCTION-INVOCATION` slice if safe. Do not infer any `ie-hex-str` semantics or any other remaining color/calculation/special-function reference invocation, modern interpolation, missing-channel behavior, three or more legacy `if()` splats, nonterminal dual-splat recovery, modern CSS conditional evaluation, plain-CSS function references, mixin application, module enumeration, dynamic loading, user-module reflection, imports/forwards, direct callable CSS serialization, broader direct-call variadic keyword behavior, or normalized diagnostics beyond executable evidence. Provider removal and the package-level zero-dependency cutover remain gated on native graduation rather than deleting the exact development oracles early. Independently, `BENCH-007` still requires a dedicated non-emulated Linux x64 runner with labels `[self-hosted, linux, x64, zigcss-benchmark-v1]`; its absence must not pause eligible native frontend work.
- Runtime control: model identity, single-agent execution, isolated-worktree confinement, automatic supervisor-owned green-checkpoint pushes, the owner-bounded post-`NATIVE-009` GitHub-prerelease/npm-`next` authority, and every native graduation/release interlock remain unchanged.

## Prior current-work evidence (transparentize checkpoint)

- Milestone: Milestone 10 self-contained native stylesheet frontends, in parallel with Milestone 8 controlled benchmark evidence
- Work package: `NSASS-011` native Sass evaluation
- State: `IN_PROGRESS`
- Active phase: the one-hundred-fifty-seventh native Sass semantic checkpoint, `NSASS-011-META-COLOR-TRANSPARENTIZE-FUNCTION-INVOCATION`, is `VERIFIED` at local implementation checkpoint `fef4fe6e91a1f858e839bc77b9d2c36993fd0205`. Provider-free native Zig now invokes the distinct legacy-global `transparentize` reference through `meta.call()` for SCSS and indented Sass while preserving the constructible-but-removed module-owned `sass:color` reference. Fresh Dart Sass 1.101.0 probes establish exact global/default/aliased/unprefixed constructibility and identity: module aliases and unprefixed import share the module reference, while the global reference remains distinct. Exact required `$color`/`$amount` positional/named binding, list/map splats, transparent, opaque, alpha-bearing RGB, HSL, and HWB legacy inputs, simple unit-bearing numeric magnitude, zero/full clamping, color result typing, once-only callable/argument source order, and both normalized legacy deprecations match the oracle. HSL/HWB values convert to legacy RGB before alpha subtraction rather than retaining their authored spaces. Removed module invocation, direct qualified calls, missing/extra/unknown/duplicate/empty-splat binding, non-color and non-number values, compound/deferred/out-of-range amounts, missing channels, modern color spaces, unloaded modules, argument limits, and every allocation failure expose no CSS. Provider-accepted compound-unit and missing-channel behavior remains deliberately ungraduated, and the explicit unavailable-callable sentinel now keeps `fade-out` outside this slice. Exact compressed SCSS `.values{global-exists:true;module-exists:true;alias-exists:true;type:function;module-type:function;global-module-same:false;module-alias-same:true;star-module-same:true;star-global-same:false;inspect-global:get-function("transparentize");inspect-module:get-function("transparentize");global:rgba(18,52,86,.4);named:rgba(18,52,86,.4);transparent:rgba(0,0,0,0);opaque:rgba(18,52,86,.8);alpha:rgba(18,52,86,0);hsl:rgba(51,76.5,51,.4);hwb:rgba(25.5,25.5,204,.4);list-splat:rgba(18,52,86,.4);map-splat:rgba(51,76.5,51,.4);zero:rgba(18,52,86,.6);full:rgba(18,52,86,0);ordered:rgba(18,52,86,.4);trace:132;result-type:color}` passes with thirteen normalized global-built-in warnings and thirteen normalized `transparentize()` warnings; indented Sass `.sass{type:function;module-type:function;plain:rgba(51,76.5,51,.4);named:rgba(18,52,86,.4);map:rgba(25.5,25.5,204,.4);module-inspect:get-function("transparentize")}` passes with three warnings of each kind. The test-first Debug gate stopped exactly at unavailable module reflection with direct evaluator 344/345 and focused native 477/478 before implementation. Final direct evaluator coverage passes 345/345 in Debug and ReleaseSafe; focused native Debug/ReleaseSafe each pass 23/23 steps with 478/478 tests; full compiler Debug/ReleaseSafe each pass 60/60 steps with 973/973 tests; and evaluator no-emission cross-compiles pass for Windows x64 GNU and Linux ARM64 GNU in both modes. Native contract 12/12 plus check adds only `built-in-meta-color-transparentize-function-invocation` while retaining private native Sass rows, production unreachability, zero target package/optional/runtime dependencies, and the closed release interlock. Complete provider/reference/product matrices pass 151/151, preprocessor package policy passes 6/6 plus check, autonomous-runner selftest passes 59/59, and repository Zig formatting, JavaScript/JSON syntax/parsing, and diff checks pass. The existing isolated macOS 15.4 SDK selector required by pinned Zig 0.15.2 changed no tracked or system state. Production routing, public capability claims, native graduation, version, release interlock, tag, artifact/package publication, deployment, and every external system remain unchanged.
- Quality closure: `QUAL-001` is verified at `3eced86`; repository-wide Zig formatting debt is zero and the final local validation matrix is green
- Next eligible package: measure the exact Dart Sass 1.101.0 global and module-owned `sass:color` constructibility, callable identity, alias behavior, alpha-adjustment binding, supported input-color spaces, list/map-splat, ordering, error, warning, and limit boundaries for `fade-out`, then implement only the smallest evidence-closed `NSASS-011-META-COLOR-FADE-OUT-FUNCTION-INVOCATION` slice if safe. Do not infer any `fade-out` semantics or any other remaining color/calculation/special-function reference invocation, modern interpolation, missing-channel behavior, three or more legacy `if()` splats, nonterminal dual-splat recovery, modern CSS conditional evaluation, plain-CSS function references, mixin application, module enumeration, dynamic loading, user-module reflection, imports/forwards, direct callable CSS serialization, broader direct-call variadic keyword behavior, or normalized diagnostics beyond executable evidence. Provider removal and the package-level zero-dependency cutover remain gated on native graduation rather than deleting the exact development oracles early. Independently, `BENCH-007` still requires a dedicated non-emulated Linux x64 runner with labels `[self-hosted, linux, x64, zigcss-benchmark-v1]`; its absence must not pause eligible native frontend work.
- Runtime control: model identity, single-agent execution, isolated-worktree confinement, automatic supervisor-owned green-checkpoint pushes, the owner-bounded post-`NATIVE-009` GitHub-prerelease/npm-`next` authority, and every native graduation/release interlock remain unchanged.

## Prior current-work evidence (fade-in checkpoint)

- Milestone: Milestone 10 self-contained native stylesheet frontends, in parallel with Milestone 8 controlled benchmark evidence
- Work package: `NSASS-011` native Sass evaluation
- State: `IN_PROGRESS`
- Active phase: the one-hundred-fifty-sixth native Sass semantic checkpoint, `NSASS-011-META-COLOR-FADE-IN-FUNCTION-INVOCATION`, is `VERIFIED` at local implementation checkpoint `1d58bb2f596ff110eb05040732799aa55cf85ab3`. Provider-free native Zig now invokes the distinct legacy-global `fade-in` reference through `meta.call()` for SCSS and indented Sass while preserving the constructible-but-removed module-owned `sass:color` reference. Fresh Dart Sass 1.101.0 probes establish exact global/default/aliased/unprefixed constructibility and identity: module aliases and unprefixed import share the module reference, while the global reference remains distinct. Exact required `$color`/`$amount` positional/named binding, list/map splats, transparent, opaque, alpha-bearing RGB, HSL, and HWB legacy inputs, simple unit-bearing numeric magnitude, zero/full clamping, color result typing, once-only callable/argument source order, and both normalized legacy deprecations match the oracle. HSL/HWB values convert to legacy RGB before alpha adjustment rather than retaining their authored spaces. Removed module invocation, direct qualified calls, missing/extra/unknown/duplicate/empty-splat binding, non-color and non-number values, compound/deferred/out-of-range amounts, missing channels, modern color spaces, unloaded modules, argument limits, and every allocation failure expose no CSS. Provider-accepted compound-unit and missing-channel behavior remains deliberately ungraduated, and the explicit unavailable-callable sentinel now keeps `transparentize` outside this slice. Exact compressed SCSS `.values{global-exists:true;module-exists:true;alias-exists:true;type:function;module-type:function;global-module-same:false;module-alias-same:true;star-module-same:true;star-global-same:false;inspect-global:get-function("fade-in");inspect-module:get-function("fade-in");global:rgba(18,52,86,.6);named:rgba(18,52,86,.6);transparent:rgba(0,0,0,.2);opaque:#123456;alpha:#123456;hsl:rgba(51,76.5,51,.6);hwb:rgba(25.5,25.5,204,.6);list-splat:rgba(18,52,86,.6);map-splat:rgba(51,76.5,51,.6);zero:rgba(18,52,86,.4);full:#123456;ordered:rgba(18,52,86,.6);trace:132;result-type:color}` passes with thirteen normalized global-built-in warnings and thirteen normalized `fade-in()` warnings; indented Sass `.sass{type:function;module-type:function;plain:rgba(51,76.5,51,.6);named:rgba(18,52,86,.6);map:rgba(25.5,25.5,204,.6);module-inspect:get-function("fade-in")}` passes with three warnings of each kind. The test-first Debug gate stopped exactly at unavailable module reflection with direct evaluator 342/343 and focused native 475/476 before implementation. Final direct evaluator coverage passes 343/343 in Debug and ReleaseSafe; focused native Debug/ReleaseSafe each pass 23/23 steps with 476/476 tests; full compiler Debug/ReleaseSafe each pass 60/60 steps with 971/971 tests; and evaluator no-emission cross-compiles pass for Windows x64 GNU and Linux ARM64 GNU in both modes. Native contract 12/12 plus check adds only `built-in-meta-color-fade-in-function-invocation` while retaining private native Sass rows, production unreachability, zero target package/optional/runtime dependencies, and the closed release interlock. Complete provider/reference/product matrices pass 151/151, preprocessor package policy passes 6/6 plus check, autonomous-runner selftest passes 59/59, and repository Zig formatting, JavaScript/JSON syntax/parsing, and diff checks pass. The existing isolated macOS 15.4 SDK selector required by pinned Zig 0.15.2 changed no tracked or system state. Production routing, public capability claims, native graduation, version, release interlock, tag, artifact/package publication, deployment, and every external system remain unchanged.
- Quality closure: `QUAL-001` is verified at `3eced86`; repository-wide Zig formatting debt is zero and the final local validation matrix is green
- Next eligible package: measure the exact Dart Sass 1.101.0 global and module-owned `sass:color` constructibility, callable identity, alias behavior, alpha-adjustment binding, supported input-color spaces, list/map-splat, ordering, error, warning, and limit boundaries for `transparentize`, then implement only the smallest evidence-closed `NSASS-011-META-COLOR-TRANSPARENTIZE-FUNCTION-INVOCATION` slice if safe. Do not infer any `transparentize` semantics or any other remaining color/calculation/special-function reference invocation, modern interpolation, missing-channel behavior, three or more legacy `if()` splats, nonterminal dual-splat recovery, modern CSS conditional evaluation, plain-CSS function references, mixin application, module enumeration, dynamic loading, user-module reflection, imports/forwards, direct callable CSS serialization, broader direct-call variadic keyword behavior, or normalized diagnostics beyond executable evidence. Provider removal and the package-level zero-dependency cutover remain gated on native graduation rather than deleting the exact development oracles early. Independently, `BENCH-007` still requires a dedicated non-emulated Linux x64 runner with labels `[self-hosted, linux, x64, zigcss-benchmark-v1]`; its absence must not pause eligible native frontend work.
- Runtime control: model identity, single-agent execution, isolated-worktree confinement, automatic supervisor-owned green-checkpoint pushes, the owner-bounded post-`NATIVE-009` GitHub-prerelease/npm-`next` authority, and every native graduation/release interlock remain unchanged.

## Prior current-work evidence (invert checkpoint)

- Milestone: Milestone 10 self-contained native stylesheet frontends, in parallel with Milestone 8 controlled benchmark evidence
- Work package: `NSASS-011` native Sass evaluation
- State: `IN_PROGRESS`
- Active phase: the one-hundred-fifty-fourth native Sass semantic checkpoint, `NSASS-011-META-COLOR-INVERT-FUNCTION-INVOCATION`, is `VERIFIED` at local implementation checkpoint `0ed5532273788ceca9d04cace2aaeb009490739b`. Provider-free native Zig now invokes the distinct legacy-global and module-owned `sass:color` `invert` references through `meta.call()` for SCSS and indented Sass. Fresh Dart Sass 1.101.0 probes establish exact global/default/aliased/unprefixed constructibility and identity: the legacy reference remains distinct, while module aliases and unprefixed import share one module-owned reference. Exact required `$color` plus optional `$weight` positional/named binding, list/map splats, transparent and alpha-bearing RGB plus HSL/HWB legacy inputs, color-versus-CSS-filter result typing, once-only callable/argument source order, and warning ownership match the oracle. Non-missing legacy colors use the already bounded weighted inversion core. Global numeric, unit-bearing numeric, and deferred CSS filter values remain warning-free strings; module numeric filters remain strings with the normalized `color.invert()` compatibility warning; module deferred filters fail closed. Global color calls emit the normalized global-built-in warning while module color calls remain warning-free. Explicit `$space` interpolation, missing channels, all twelve modern color spaces, deprecated non-percentage weight coercion, invalid values, missing/extra/unknown/duplicate/empty-splat arguments, unloaded/unknown modules, direct qualified calls, argument limits, and every allocation failure expose no CSS. Provider-accepted interpolation, selected missing-channel behavior, and deprecated weight coercion remain ungraduated, and the explicit unavailable-callable sentinel now keeps `opacify` outside this slice. Exact compressed SCSS `.values{global-exists:true;module-exists:true;alias-exists:true;type:function;global-module-same:false;module-alias-same:true;star-module-same:true;star-global-same:false;inspect-global:get-function("invert");inspect-module:get-function("invert");global:#edcba9;module:#edcba9;weighted-global:rgb(72.75,89.75,106.75);weighted-module:rgb(72.75,89.75,106.75);named-global:rgb(72.75,89.75,106.75);named-module:rgb(72.75,89.75,106.75);transparent:hsla(0,0%,100%,0);alpha:rgba(237,203,169,.5);hsl:hsl(300,20%,75%);hwb:rgb(229.5,229.5,51);list-splat:rgb(72.75,89.75,106.75);map-splat:rgb(76.5,76.5,165.75);global-filter:invert(20%);global-filter-unit:invert(2px);global-filter-type:string;global-filter-calculation:invert(calc(10% + var(--amount)));module-filter:invert(20%);module-filter-type:string;filter-list-splat:invert(20%);filter-map-splat:invert(20%);ordered:rgb(72.75,89.75,106.75);trace:123;result-type:color}` passes with eight normalized global warnings and three normalized module-filter warnings; indented Sass `.sass{type:function;module-type:function;filter:invert(20%);module-filter:invert(20%);plain:hsl(300,20%,75%);named:rgb(72.75,89.75,106.75);map:rgb(76.5,76.5,165.75);module-inspect:get-function("invert")}` passes with one warning of each kind. The test-first Debug gate stopped exactly at unavailable module reflection with direct evaluator coverage at 337/339 and the focused suite at 470/472. Final direct evaluator coverage passes 339/339 in Debug and ReleaseSafe; focused native Debug/ReleaseSafe each pass 23/23 steps with 472/472 tests; full compiler Debug/ReleaseSafe each pass 60/60 steps with 967/967 tests; and evaluator no-emission cross-compiles pass for Windows x64 GNU and Linux ARM64 GNU in both modes. Native contract 12/12 plus check adds only `built-in-meta-color-invert-function-invocation` while retaining private native Sass rows, production unreachability, zero target package/optional/runtime dependencies, and the closed release interlock. Complete provider/reference/product matrices pass 151/151, preprocessor package policy passes 6/6 plus check, autonomous-runner selftest passes 59/59, and repository Zig formatting, JavaScript/JSON syntax/parsing, and diff checks pass. The existing isolated macOS 15.4 SDK selector required by pinned Zig 0.15.2 changed no tracked or system state. Production routing, public capability claims, native graduation, version, release interlock, tag, artifact/package publication, deployment, and every external system remain unchanged.
- Quality closure: `QUAL-001` is verified at `3eced86`; repository-wide Zig formatting debt is zero and the final local validation matrix is green
- Next eligible package: measure the exact Dart Sass 1.101.0 global and module-owned `sass:color` constructibility, callable identity, alias behavior, alpha-adjustment binding, supported input-color spaces, list/map-splat, ordering, error, warning, and limit boundaries for `opacify`, then implement only the smallest evidence-closed `NSASS-011-META-COLOR-OPACIFY-FUNCTION-INVOCATION` slice if safe. Do not infer any `opacify` semantics or any other remaining color/calculation/special-function reference invocation, modern interpolation, missing-channel behavior, three or more legacy `if()` splats, nonterminal dual-splat recovery, modern CSS conditional evaluation, plain-CSS function references, mixin application, module enumeration, dynamic loading, user-module reflection, imports/forwards, direct callable CSS serialization, broader direct-call variadic keyword behavior, or normalized diagnostics beyond executable evidence. Provider removal and the package-level zero-dependency cutover remain gated on native graduation rather than deleting the exact development oracles early. Independently, `BENCH-007` still requires a dedicated non-emulated Linux x64 runner with labels `[self-hosted, linux, x64, zigcss-benchmark-v1]`; its absence must not pause eligible native frontend work.
- Runtime control: model identity, single-agent execution, isolated-worktree confinement, automatic supervisor-owned green-checkpoint pushes, the owner-bounded post-`NATIVE-009` GitHub-prerelease/npm-`next` authority, and every native graduation/release interlock remain unchanged.

## Prior current-work evidence

- Milestone: Milestone 10 self-contained native stylesheet frontends, in parallel with Milestone 8 controlled benchmark evidence
- Work package: `NSASS-011` native Sass evaluation
- State: `IN_PROGRESS`
- Active phase: the one-hundred-twentieth native Sass semantic checkpoint, `NSASS-011-META-STRING-INSERT-FUNCTION-INVOCATION`, is `VERIFIED` at local implementation checkpoint `1b55197b115c333bf943e06a78655a8506923d99`; the exact detailed evidence remains recorded in the Milestone 10 execution ledger and last-full-validation history below.
- Quality closure: `QUAL-001` is verified at `3eced86`; repository-wide Zig formatting debt is zero and the final local validation matrix is green
- Next eligible package at that checkpoint: `NSASS-011-META-STRING-UPPER-CASE-FUNCTION-INVOCATION`.
- Runtime control: model identity, single-agent execution, isolated-worktree confinement, automatic supervisor-owned green-checkpoint pushes, the owner-bounded post-`NATIVE-009` GitHub-prerelease/npm-`next` authority, and every native graduation/release interlock remained unchanged.

- Milestone: Milestone 10 self-contained native stylesheet frontends, in parallel with Milestone 8 controlled benchmark evidence
- Work package: `NSASS-011` native Sass evaluation
- State: `IN_PROGRESS`
- Active phase: the one-hundred-nineteenth native Sass semantic checkpoint, `NSASS-011-META-STRING-SLICE-FUNCTION-INVOCATION`, is `VERIFIED` at local implementation checkpoint `591a5f330c134594c16de1c53ee1f32e6691dd98`. Provider-free native Zig now invokes the exact already-admitted module-owned `sass:string` `slice` function reference through `meta.call()` for SCSS and indented Sass, plus the independently measured legacy-global `str-slice` reference. Default, exact custom, and unprefixed module access retain one module-owned identity without warnings; the legacy and module references remain distinct despite their shared implementation, and only legacy invocation emits the existing normalized warning. Canonical positional and named `$string`/`$start-at`/optional `$end-at` binding, three-item list and map splats, ASCII, Unicode scalar, combining-mark, ZWJ, quoted and unquoted string values, quoted and unquoted hexadecimal escapes, simple escapes, omitted-end behavior, negative, zero, reversed, and out-of-range clamping, interpolation, preserved result quoting and string typing, and once-only callable/argument source order agree with Dart Sass 1.101.0. Missing, extra, unknown, duplicate, empty-splat, null, boolean, number, list, map, color, fractional, unitful, calculation-string, calculation-index, temporary-byte, function-argument, and every allocation failure fail closed; the calculation-string guard also closes the already-admitted direct `string.slice()` sibling. Fresh probes reproduce exact compressed SCSS `.values{exists:true;type:function;same:true;star-same:true;ascii:ana;default-end:nana;quoted-unicode:"💚";combining:"́";zwj:"‍💻";quoted-hex:"b";unquoted-hex:b;quoted-simple:" ";unquoted-simple:\ ;negative:"lo";clamped:"hello";empty:"";zero-start:"he";zero-end:"";list-splat:"💚";map-splat:"́";interpolated:tem;ordered:bcd;trace:1234;result-type:string;inspected:"spe"}`, indented Sass `.sass{type:function;plain:ana;named:"💚";escaped:\ ;clamped:hello}`, and legacy `.legacy{exists:true;module-exists:true;distinct:false;inspect:get-function("str-slice");module-inspect:get-function("slice");value:"💚"}` with one warning. The test-first Debug gate stopped only at the old dispatch, direct calculation guard, and allocation path with direct evaluator coverage at 259/263 and the focused suite at 392/396. Final direct evaluator coverage passes 263/263; focused native Debug/ReleaseSafe each pass 23/23 steps with 396/396 tests; full compiler Debug/ReleaseSafe each pass 60/60 steps with 891/891 tests; and direct evaluator no-emission cross-compiles pass for Windows x64 GNU and Linux ARM64 GNU in both modes. Native contract 12/12 plus check adds only `built-in-meta-string-slice-function-invocation` while retaining private native Sass rows, production unreachability, zero target package/optional/runtime dependencies, and the closed release interlock. Complete provider/reference/product matrices pass 151/151, preprocessor package policy passes 6/6 plus check, autonomous-runner selftest passes 59/59, and repository Zig formatting, JavaScript/JSON syntax, and diff checks pass. The temporary isolated macOS 15.4 SDK selector required by pinned Zig 0.15.2 was removed after validation; no tracked or system state changed. Production routing, public capability claims, native graduation, version, release interlock, tag, artifact/package publication, deployment, and every external system remain unchanged.
- Quality closure: `QUAL-001` is verified at `3eced86`; repository-wide Zig formatting debt is zero and the final local validation matrix is green
- Next eligible package: measure the exact Dart Sass 1.101.0 callable, namespace, three-argument binding, Unicode/escape insertion and clamping behavior, quoting, ordering, error, and limit boundaries for the already-constructible module-owned `sass:string` `insert` reference, then implement only the smallest evidence-closed `NSASS-011-META-STRING-INSERT-FUNCTION-INVOCATION` slice if those boundaries preserve the native safety contract. Do not infer case conversion, any other remaining string/color/calculation/special-function reference invocation, three or more legacy `if()` splats, nonterminal dual-splat recovery, modern CSS conditional evaluation, plain-CSS function references, mixin application, module enumeration, dynamic loading, user-module reflection, imports/forwards, direct callable CSS serialization, broader direct-call variadic keyword behavior, or normalized diagnostics beyond executable evidence. Provider removal and the package-level zero-dependency cutover remain gated on native graduation rather than deleting the exact development oracles early. Independently, `BENCH-007` still requires a dedicated non-emulated Linux x64 runner with labels `[self-hosted, linux, x64, zigcss-benchmark-v1]`; its absence must not pause eligible native frontend work.
- Runtime control: model identity, single-agent execution, isolated-worktree confinement, automatic supervisor-owned green-checkpoint pushes, the owner-bounded post-`NATIVE-009` GitHub-prerelease/npm-`next` authority, and every native graduation/release interlock remain unchanged.

### Earlier current-work evidence

- Milestone: Milestone 10 self-contained native stylesheet frontends, in parallel with Milestone 8 controlled benchmark evidence
- Work package: `NSASS-011` native Sass evaluation
- State: `IN_PROGRESS`
- Active phase: the one-hundred-eighteenth native Sass semantic checkpoint, `NSASS-011-META-STRING-INDEX-FUNCTION-INVOCATION`, is `VERIFIED` at local implementation checkpoint `cc9a58ad2ec062afc9cfb3e9d8c3700a89a79b82`. Provider-free native Zig now invokes the exact already-admitted module-owned `sass:string` `index` function reference through `meta.call()` for SCSS and indented Sass, plus the independently measured legacy-global `str-index` reference. Default, exact custom, and unprefixed module access retain one module-owned identity without warnings; the legacy and module references remain distinct despite their shared implementation, and only legacy invocation emits the existing normalized warning. Canonical positional and named `$string`/`$substring` binding, two-item list and map splats, ASCII, Unicode scalar, combining-mark, ZWJ, quoted and unquoted string values, quoted and unquoted hexadecimal escapes, simple escapes, empty-substring behavior, null misses, interpolation, number/null result typing, and once-only callable/argument source order agree with Dart Sass 1.101.0. Missing, extra, unknown, duplicate, empty-splat, null, boolean, number, list, map, color, calculation in either operand, temporary-byte, function-argument, and every allocation failure fail closed; the calculation guards also close the already-admitted direct `string.index()` sibling for either operand. Fresh probes reproduce exact compressed SCSS `.values{exists:true;type:function;same:true;star-same:true;ascii:3;quoted-unicode:2;combining:2;zwj:3;quoted-hex:2;unquoted-hex:2;quoted-simple:4;unquoted-simple:4;empty:1;list-splat:2;map-splat:2;interpolated:4;ordered:2;trace:123;result-type:number;missing-type:null;inspected:3}`, indented Sass `.sass{type:function;plain:3;named:2;escaped:4}`, and legacy `.legacy{exists:true;module-exists:true;distinct:false;inspect:get-function("str-index");module-inspect:get-function("index");value:2}` with one warning. The test-first Debug gate stopped only at the old dispatch, direct calculation guards, and allocation path with direct evaluator coverage at 256/260 and the focused suite at 389/393. Final direct evaluator coverage passes 260/260; focused native Debug/ReleaseSafe each pass 23/23 steps with 393/393 tests; full compiler Debug/ReleaseSafe each pass 60/60 steps with 888/888 tests; and direct evaluator no-emission cross-compiles pass for Windows x64 GNU and Linux ARM64 GNU in both modes. Native contract 12/12 plus check adds only `built-in-meta-string-index-function-invocation` while retaining private native Sass rows, production unreachability, zero target package/optional/runtime dependencies, and the closed release interlock. Complete provider/reference/product matrices pass 151/151, preprocessor package policy passes 6/6 plus check, autonomous-runner selftest passes 59/59, and repository Zig formatting, JavaScript/JSON syntax, and diff checks pass. The temporary isolated macOS 15.4 SDK selector required by pinned Zig 0.15.2 was removed after validation; no tracked or system state changed. Production routing, public capability claims, native graduation, version, release interlock, tag, artifact/package publication, deployment, and every external system remain unchanged.
- Quality closure: `QUAL-001` is verified at `3eced86`; repository-wide Zig formatting debt is zero and the final local validation matrix is green
- Next eligible package: measure the exact Dart Sass 1.101.0 callable, namespace, two-or-three-argument binding, Unicode/escape index and clamping behavior, quoting, ordering, error, and limit boundaries for the already-constructible module-owned `sass:string` `slice` reference, then implement only the smallest evidence-closed `NSASS-011-META-STRING-SLICE-FUNCTION-INVOCATION` slice if those boundaries preserve the native safety contract. Do not infer `string.insert`, case conversion, any other remaining string/color/calculation/special-function reference invocation, three or more legacy `if()` splats, nonterminal dual-splat recovery, modern CSS conditional evaluation, plain-CSS function references, mixin application, module enumeration, dynamic loading, user-module reflection, imports/forwards, direct callable CSS serialization, broader direct-call variadic keyword behavior, or normalized diagnostics beyond executable evidence. Provider removal and the package-level zero-dependency cutover remain gated on native graduation rather than deleting the exact development oracles early. Independently, `BENCH-007` still requires a dedicated non-emulated Linux x64 runner with labels `[self-hosted, linux, x64, zigcss-benchmark-v1]`; its absence must not pause eligible native frontend work.
- Runtime control: model identity, single-agent execution, isolated-worktree confinement, automatic supervisor-owned green-checkpoint pushes, the owner-bounded post-`NATIVE-009` GitHub-prerelease/npm-`next` authority, and every native graduation/release interlock remain unchanged.

- Milestone: Milestone 10 self-contained native stylesheet frontends, in parallel with Milestone 8 controlled benchmark evidence
- Work package: `NSASS-011` native Sass evaluation
- State: `IN_PROGRESS`
- Active phase: the one-hundred-seventeenth native Sass semantic checkpoint, `NSASS-011-META-STRING-LENGTH-FUNCTION-INVOCATION`, is `VERIFIED` at local implementation checkpoint `0853efb093df6aab26f32ffea85e34de9a84e9d4`. Provider-free native Zig now invokes the exact already-admitted module-owned `sass:string` `length` function reference through `meta.call()` for SCSS and indented Sass, plus the independently measured legacy-global `str-length` reference. Default, exact custom, and unprefixed module access retain one module-owned identity without warnings; legacy `str-length`, global list `length`, and module string `length` retain their distinct owners even where inspected spelling collides, and only legacy invocation emits the existing normalized warning. Canonical positional and named `$string` binding, one-item list and map splats, ASCII, Unicode scalar, combining-mark, ZWJ, quoted and unquoted string values, quoted and unquoted hexadecimal escapes, simple escapes, empty strings, interpolation, numeric result typing, and once-only callable/argument source order agree with Dart Sass 1.101.0. Missing, extra, unknown, duplicate, empty-splat, null, boolean, number, list, map, color, calculation, temporary-byte, function-argument, and every allocation failure fail closed; the calculation guard also closes the already-admitted direct `string.length()` sibling, while top-level list splitting now consumes an optional hexadecimal-escape terminator without disturbing the existing escaped plain-CSS function path. Fresh probes reproduce exact compressed SCSS `.values{exists:true;type:function;same:true;star-same:true;ascii:5;quoted-unicode:3;combining:2;zwj:3;quoted-hex:3;unquoted-hex:3;quoted-simple:7;unquoted-simple:8;empty:0;list-splat:3;map-splat:2;interpolated:6;ordered:7;trace:12;result-type:number;inspected:7}`, indented Sass `.sass{type:function;plain:5;named:3;escaped:8}`, and legacy `.legacy{exists:true;module-exists:true;distinct:false;list-distinct:false;inspect:get-function("str-length");module-inspect:get-function("length");list-inspect:get-function("length");value:3}` with one warning. The test-first Debug gate stopped only at the old dispatch with direct evaluator coverage at 254/257 and the focused suite at 387/390. Final direct evaluator coverage passes 257/257; focused native Debug/ReleaseSafe each pass 23/23 steps with 390/390 tests; full compiler Debug/ReleaseSafe each pass 60/60 steps with 885/885 tests; and direct evaluator no-emission cross-compiles pass for Windows x64 GNU and Linux ARM64 GNU in both modes. Native contract 12/12 plus check adds only `built-in-meta-string-length-function-invocation` while retaining private native Sass rows, production unreachability, zero target package/optional/runtime dependencies, and the closed release interlock. Complete provider/reference/product matrices pass 151/151, preprocessor package policy passes 6/6 plus check, autonomous-runner selftest passes 59/59, and repository Zig formatting, JavaScript/JSON syntax, and diff checks pass. The temporary isolated macOS 15.4 SDK selector required by pinned Zig 0.15.2 was removed after validation; no tracked or system state changed. Production routing, public capability claims, native graduation, version, release interlock, tag, artifact/package publication, deployment, and every external system remain unchanged.
- Quality closure: `QUAL-001` is verified at `3eced86`; repository-wide Zig formatting debt is zero and the final local validation matrix is green
- Next eligible package: measure the exact Dart Sass 1.101.0 callable, namespace, two-string binding, Unicode/escape indexing, null-result, ordering, error, and limit boundaries for the already-constructible module-owned `sass:string` `index` reference, then implement only the smallest evidence-closed `NSASS-011-META-STRING-INDEX-FUNCTION-INVOCATION` slice if those boundaries preserve the native safety contract. Do not infer `string.slice`, `string.insert`, case conversion, any other remaining string/color/calculation/special-function reference invocation, three or more legacy `if()` splats, nonterminal dual-splat recovery, modern CSS conditional evaluation, plain-CSS function references, mixin application, module enumeration, dynamic loading, user-module reflection, imports/forwards, direct callable CSS serialization, broader direct-call variadic keyword behavior, or normalized diagnostics beyond executable evidence. Provider removal and the package-level zero-dependency cutover remain gated on native graduation rather than deleting the exact development oracles early. Independently, `BENCH-007` still requires a dedicated non-emulated Linux x64 runner with labels `[self-hosted, linux, x64, zigcss-benchmark-v1]`; its absence must not pause eligible native frontend work.
- Runtime control: model identity, single-agent execution, isolated-worktree confinement, automatic supervisor-owned green-checkpoint pushes, the owner-bounded post-`NATIVE-009` GitHub-prerelease/npm-`next` authority, and every native graduation/release interlock remain unchanged.

- Milestone: Milestone 10 self-contained native stylesheet frontends, in parallel with Milestone 8 controlled benchmark evidence
- Work package: `NSASS-011` native Sass evaluation
- State: `IN_PROGRESS`
- Active phase: the one-hundred-sixteenth native Sass semantic checkpoint, `NSASS-011-META-STRING-UNQUOTE-FUNCTION-INVOCATION`, is `VERIFIED` at local implementation checkpoint `33bd709eb410f402f0f75637446dc25fc8f545bb`. Provider-free native Zig now invokes the exact already-admitted module-owned `sass:string` `unquote` function reference through `meta.call()` for SCSS and indented Sass, plus the independently measured legacy-global `unquote` reference. Default, exact custom, and unprefixed module access retain one module-owned identity without warnings; legacy-global reflection remains true, the global and module references remain distinct despite identical inspected spelling, and legacy invocation preserves the existing normalized warning. Canonical positional and named `$string` binding, one-item list and map splats, quoted and unquoted string values, escaped-space normalization, interpolation, exact unquoted serialization, result typing, and once-only callable/argument source order agree with Dart Sass 1.101.0. Missing, extra, unknown, duplicate, empty-splat, null, boolean, number, list, map, color, calculation, temporary-byte, function-argument, invalid generated CSS, and every allocation failure fail closed; the unquote calculation guard also closes the already-admitted direct `string.unquote()` sibling without changing another string function. Fresh probes reproduce exact compressed SCSS `.values{exists:true;type:function;same:true;star-same:true;plain:plain;unquoted:already-unquoted;space:space value;escaped-space:foo bar;list-splat:list value;map-splat:map value;interpolated:item-2;ordered:ordered value;trace:12;result-type:string;inspected:inspect value}`, indented Sass `.sass{type:function;plain:plain;named:named value;escaped:foo bar}`, and legacy `.legacy{exists:true;module-exists:true;distinct:false;inspect:get-function("unquote");module-inspect:get-function("unquote");value:legacy value}` with one warning. Dart Sass also emits the invalid bytes `foo"bar` for one quote-removal probe; ZigCSS deliberately admits no invalid generated CSS and its recovery-disabled core rejects that result without exposing CSS. The test-first Debug gate stopped only at the old dispatch with direct evaluator coverage at 252/254 and the focused suite at 385/387. Final direct evaluator coverage passes 254/254; focused native Debug/ReleaseSafe each pass 23/23 steps with 387/387 tests; full compiler Debug/ReleaseSafe each pass 60/60 steps with 882/882 tests; and direct evaluator no-emission cross-compiles pass for Windows x64 GNU and Linux ARM64 GNU in both modes. Native contract 12/12 plus check adds only `built-in-meta-string-unquote-function-invocation` while retaining private native Sass rows, production unreachability, zero target package/optional/runtime dependencies, and the closed release interlock. Complete provider/reference/product matrices pass 151/151, preprocessor package policy passes 6/6 plus check, autonomous-runner selftest passes 59/59, and repository Zig formatting, JavaScript/JSON syntax, and diff checks pass. The temporary isolated macOS 15.4 SDK selector required by pinned Zig 0.15.2 was removed after validation; no tracked or system state changed. Production routing, public capability claims, native graduation, version, release interlock, tag, artifact/package publication, deployment, and every external system remain unchanged.
- Quality closure: `QUAL-001` is verified at `3eced86`; repository-wide Zig formatting debt is zero and the final local validation matrix is green
- Next eligible package: measure the exact Dart Sass 1.101.0 callable, namespace, one-argument binding, Unicode/escape counting, ordering, error, and limit boundaries for the already-constructible module-owned `sass:string` `length` reference, then implement only the smallest evidence-closed `NSASS-011-META-STRING-LENGTH-FUNCTION-INVOCATION` slice if those boundaries preserve the native safety contract. Do not infer `string.index`, `string.slice`, `string.insert`, case conversion, any other remaining string/color/calculation/special-function reference invocation, three or more legacy `if()` splats, nonterminal dual-splat recovery, modern CSS conditional evaluation, plain-CSS function references, mixin application, module enumeration, dynamic loading, user-module reflection, imports/forwards, direct callable CSS serialization, broader direct-call variadic keyword behavior, or normalized diagnostics beyond executable evidence. Provider removal and the package-level zero-dependency cutover remain gated on native graduation rather than deleting the exact development oracles early. Independently, `BENCH-007` still requires a dedicated non-emulated Linux x64 runner with labels `[self-hosted, linux, x64, zigcss-benchmark-v1]`; its absence must not pause eligible native frontend work.
- Runtime control: the graceful operator stop requested on 2026-07-25 completed at clean pushed checkpoint `1d00f1b`. On 2026-07-27 the owner approved changing reasoning effort from ultra to xhigh and requested immediate autonomous continuation. The next pass completed and pushed the exact `NSASS-011-META-MATH-CLAMP-FUNCTION-INVOCATION` package at `8f6c0fc`, then the runner paused cleanly for the owner's publication-authority amendment. Plan v1.3, amended ADR-013, and native contract schema v4 now bind the authority to one exact fully graduated candidate, the existing fail-closed tag workflow, a GitHub prerelease, and npm `next`; the release interlock remains closed. Native contract passes 12/12 plus check, workflow policy passes 5/5 plus check, the autonomous runner passes 59/59, the site passes 153/153 plus production build and bundle budget, documentation structure passes over 38 Markdown files/69 fences/49 links/23 routes, JavaScript/JSON/Bash syntax and diff checks pass. The optional executable-fence rerun stopped at its unchanged explicit-`NVIM` environment precondition before repository validation; no executable fence changed. Model identity, single-agent execution, isolated-worktree confinement, automatic green-checkpoint pushes, and all native graduation gates remain unchanged; only the exact post-`NATIVE-009` GitHub-prerelease/npm-`next` path is now authorized.

- Milestone: Milestone 10 self-contained native stylesheet frontends, in parallel with Milestone 8 controlled benchmark evidence
- Work package: `NSASS-011` native Sass evaluation
- State: `IN_PROGRESS`
- Active phase: the one-hundred-fifteenth native Sass semantic checkpoint, `NSASS-011-META-STRING-QUOTE-FUNCTION-INVOCATION`, is `VERIFIED` at local implementation checkpoint `4428e957b9b0e444f44c88bfcd5593d4aa43e2b5`. Provider-free native Zig invokes module-owned and legacy-global `sass:string` `quote` references through `meta.call()` with exact Dart Sass 1.101.0 binding, string values, quoting, escapes, warning ownership, ordering, limits, and allocation-failure evidence. Direct evaluator coverage passes 251/251; focused Debug/ReleaseSafe pass 384/384; full compiler modes pass 879/879; cross-target, native contract, provider/reference/product, package policy, runner, formatting, syntax, JSON, and diff gates pass. Native rows remain private and production-unreachable, the release interlock remains closed, and no external state changed.
- Quality closure: `QUAL-001` is verified at `3eced86`; repository-wide Zig formatting debt is zero and the final local validation matrix is green
- Next eligible package: measure the exact module-owned `sass:string` `unquote` callable boundary and implement only `NSASS-011-META-STRING-UNQUOTE-FUNCTION-INVOCATION`; keep every other unmeasured callable family, native graduation, provider removal, and publication outside the slice.

- Milestone: Milestone 10 self-contained native stylesheet frontends, in parallel with Milestone 8 controlled benchmark evidence
- Work package: `NSASS-011` native Sass evaluation
- State: `IN_PROGRESS`
- Active phase: the one-hundred-fourteenth native Sass semantic checkpoint, `NSASS-011-META-SELECTOR-REPLACE-FUNCTION-INVOCATION`, is `VERIFIED` at local implementation checkpoint `98d4bc318414f04dd810fc320130a79890b0c1f4`. Provider-free native Zig invokes module-owned `sass:selector` `replace` references plus the legacy-global `selector-replace` reference through `meta.call()` with exact Dart Sass 1.101.0 positional/named binding, list/map splats, selector values, replacement-only results, warning ownership, serialization, ordering, limits, and allocation-failure evidence. Direct evaluator coverage passes 248/248; focused Debug/ReleaseSafe pass 381/381; full compiler modes pass 876/876; cross-target, native contract, provider/reference/product, package policy, runner, formatting, syntax, JSON, and diff gates pass. Native rows remain private and production-unreachable, the release interlock remains closed, and no external state changed.
- Quality closure: `QUAL-001` is verified at `3eced86`; repository-wide Zig formatting debt is zero and the final local validation matrix is green
- Next eligible package: measure the exact module-owned `sass:string` `quote` callable boundary and implement only `NSASS-011-META-STRING-QUOTE-FUNCTION-INVOCATION`; keep `string.unquote`, every other unmeasured callable family, native graduation, provider removal, and publication outside the slice.

- Milestone: Milestone 10 self-contained native stylesheet frontends, in parallel with Milestone 8 controlled benchmark evidence
- Work package: `NSASS-011` native Sass evaluation
- State: `IN_PROGRESS`
- Active phase: the one-hundred-thirteenth native Sass semantic checkpoint, `NSASS-011-META-SELECTOR-EXTEND-FUNCTION-INVOCATION`, is `VERIFIED` at local implementation checkpoint `bff2eed61a7756244b076b8a3d72cb3ba3503377`. Provider-free native Zig invokes module-owned `sass:selector` `extend` references plus the legacy-global `selector-extend` reference through `meta.call()` with exact Dart Sass 1.101.0 positional/named binding, list/map splats, selector values, retained-original extension results, warning ownership, serialization, ordering, limits, and allocation-failure evidence. Direct evaluator coverage passes 245/245; focused Debug/ReleaseSafe pass 378/378; full compiler modes pass 873/873; cross-target, native contract, provider/reference/product, package policy, runner, formatting, syntax, JSON, and diff gates pass. Native rows remain private and production-unreachable, the release interlock remains closed, and no external state changed.
- Quality closure: `QUAL-001` is verified at `3eced86`; repository-wide Zig formatting debt is zero and the final local validation matrix is green
- Next eligible package: measure the exact module-owned `sass:selector` `replace` callable boundary and implement only `NSASS-011-META-SELECTOR-REPLACE-FUNCTION-INVOCATION`; keep every remaining unmeasured callable family, native graduation, provider removal, and publication outside the slice.

- Milestone: Milestone 10 self-contained native stylesheet frontends, in parallel with Milestone 8 controlled benchmark evidence
- Work package: `NSASS-011` native Sass evaluation
- State: `IN_PROGRESS`
- Active phase: the one-hundred-twelfth native Sass semantic checkpoint, `NSASS-011-META-SELECTOR-NEST-FUNCTION-INVOCATION`, is `VERIFIED` at local implementation checkpoint `955d44b11807b9b4a6602e7411785d2db4f460f5`. Provider-free native Zig invokes module-owned `sass:selector` `nest` references plus the legacy-global `selector-nest` reference through `meta.call()` with exact Dart Sass 1.101.0 positional binding, list splats, selector values, Cartesian parent substitution, leading combinators, warning ownership, serialization, ordering, limits, and allocation-failure evidence. Direct evaluator coverage passes 242/242; focused Debug/ReleaseSafe pass 375/375; full compiler modes pass 870/870; cross-target, native contract, provider/reference/product, package policy, runner, formatting, syntax, JSON, and diff gates pass. Native rows remain private and production-unreachable, the release interlock remains closed, and no external state changed.
- Quality closure: `QUAL-001` is verified at `3eced86`; repository-wide Zig formatting debt is zero and the final local validation matrix is green
- Next eligible package: measure the exact module-owned `sass:selector` `extend` callable boundary and implement only `NSASS-011-META-SELECTOR-EXTEND-FUNCTION-INVOCATION`; keep `selector.replace`, every other unmeasured callable family, native graduation, provider removal, and publication outside the slice.

- Milestone: Milestone 10 self-contained native stylesheet frontends, in parallel with Milestone 8 controlled benchmark evidence
- Work package: `NSASS-011` native Sass evaluation
- State: `IN_PROGRESS`
- Active phase: the one-hundred-eleventh native Sass semantic checkpoint, `NSASS-011-META-SELECTOR-APPEND-FUNCTION-INVOCATION`, is `VERIFIED` at local implementation checkpoint `6adfbcc5ac5ba3b58f987295eac70807f6d2f87e`. Provider-free native Zig invokes the module-owned `sass:selector` `append` reference through `meta.call()` with exact Dart Sass 1.101.0 variadic binding, list splats, selector values, composition results, distinct legacy list ownership, warning behavior, ordering, limits, and allocation-failure evidence. Direct evaluator coverage passes 239/239; focused Debug/ReleaseSafe pass 372/372; full compiler modes pass 867/867; cross-target, native contract, provider/reference/product, package policy, runner, formatting, syntax, JSON, and diff gates pass. Native rows remain private and production-unreachable, the release interlock remains closed, and no external state changed.
- Quality closure: `QUAL-001` is verified at `3eced86`; repository-wide Zig formatting debt is zero and the final local validation matrix is green
- Next eligible package: measure the exact module-owned `sass:selector` `nest` callable boundary and implement only `NSASS-011-META-SELECTOR-NEST-FUNCTION-INVOCATION`; keep `selector.extend`, `selector.replace`, every other unmeasured callable family, native graduation, provider removal, and publication outside the slice.

- Milestone: Milestone 10 self-contained native stylesheet frontends, in parallel with Milestone 8 controlled benchmark evidence
- Work package: `NSASS-011` native Sass evaluation
- State: `IN_PROGRESS`
- Active phase: the one-hundred-tenth native Sass semantic checkpoint, `NSASS-011-META-SELECTOR-UNIFY-FUNCTION-INVOCATION`, is `VERIFIED` at local implementation checkpoint `85c7dfcccc8ffa353c4a074d3589b7a9f44fed5b`. Provider-free native Zig invokes module-owned `sass:selector` `unify` references plus the legacy-global `selector-unify` reference through `meta.call()` with exact Dart Sass 1.101.0 positional/named binding, list/map splats, selector values, list/null results, warning ownership, serialization, ordering, limits, and allocation-failure evidence. Direct evaluator coverage passes 236/236; focused Debug/ReleaseSafe pass 369/369; full compiler modes pass 864/864; cross-target, native contract, provider/reference/product, package policy, runner, formatting, syntax, JSON, and diff gates pass. Native rows remain private and production-unreachable, the release interlock remains closed, and no external state changed.
- Quality closure: `QUAL-001` is verified at `3eced86`; repository-wide Zig formatting debt is zero and the final local validation matrix is green
- Next eligible package: measure the exact module-owned `sass:selector` `append` callable boundary and implement only `NSASS-011-META-SELECTOR-APPEND-FUNCTION-INVOCATION`; keep `selector.nest`, `selector.extend`, `selector.replace`, every other unmeasured callable family, native graduation, provider removal, and publication outside the slice.

- Milestone: Milestone 10 self-contained native stylesheet frontends, in parallel with Milestone 8 controlled benchmark evidence
- Work package: `NSASS-011` native Sass evaluation
- State: `IN_PROGRESS`
- Active phase: the one-hundred-ninth native Sass semantic checkpoint, `NSASS-011-META-SELECTOR-IS-SUPERSELECTOR-FUNCTION-INVOCATION`, is `VERIFIED` at local implementation checkpoint `a4ad37237ebd119ac38065be9dd862be65c72087`. Provider-free native Zig now invokes the exact already-admitted module-owned `sass:selector` `is-superselector` function reference through `meta.call()` for SCSS and indented Sass, plus the independently measured legacy-global reference. Default, exact custom, and unprefixed module access retain one module-owned identity without warnings; legacy-global reflection remains true, the global and module references remain distinct, and legacy invocation emits the existing normalized warning. Canonical positional and named `$super`/`$sub` binding, list/map splats, string/existing-selector/list values, interpolation, true and false relation results, and once-only source order agree with Dart Sass 1.101.0. Missing, extra, unknown, duplicate, empty, null, boolean, number, map, parent-selector, malformed-selector, short-splat, unsupported-relation, selector-count, selector-byte, temporary-byte, function-argument, and every allocation failure fail closed, while module-owned `selector.unify` keeps selector composition and mutation reference invocation outside this slice. Fresh probes reproduce exact compressed SCSS `.values{exists:true;type:function;same:true;star-same:true;plain:true;false-result:false;named:true;list-splat:true;map-splat:true;typed:true;from-list:true;interpolated:true;ordered:true;trace:123}`, indented Sass `.sass{type:function;plain:true;named:false;typed:true}`, and legacy `.legacy{exists:true;distinct:false;inspect:get-function("is-superselector");value:true}` with one warning. The test-first Debug gate stopped only at the measured old dispatch and its limit paths with direct evaluator coverage at 230/233 and the focused suite at 363/366. Final direct evaluator coverage passes 233/233; focused native Debug/ReleaseSafe each pass 23/23 steps with 366/366 tests; full compiler Debug/ReleaseSafe each pass 60/60 steps with 861/861 tests; and direct evaluator no-emission cross-compiles pass for Windows x64 GNU and Linux ARM64 GNU in both modes. Native contract 12/12 plus check adds only `built-in-meta-selector-is-superselector-function-invocation` while retaining private native Sass rows, production unreachability, zero target package/optional/runtime dependencies, and the closed release interlock. Complete provider/reference/product matrices pass 151/151, preprocessor package policy passes 6/6 plus check, autonomous-runner selftest passes 59/59, and repository Zig formatting, JavaScript/JSON syntax, and diff checks pass. The temporary isolated macOS 15.4 SDK selector required by pinned Zig 0.15.2 was removed after validation; no tracked or system state changed. Production routing, public capability claims, native graduation, version, release interlock, tag, artifact/package publication, deployment, and every external system remain unchanged.
- Quality closure: `QUAL-001` is verified at `3eced86`; repository-wide Zig formatting debt is zero and the final local validation matrix is green
- Next eligible package: measure the exact Dart Sass 1.101.0 callable, namespace, argument, selector-value, composition-result, ordering, error, and limit boundaries for the already-constructible module-owned `sass:selector` `unify` reference, then implement only the smallest evidence-closed `NSASS-011-META-SELECTOR-UNIFY-FUNCTION-INVOCATION` slice if those boundaries preserve the native safety contract. Do not infer `selector.append`, `selector.nest`, `selector.extend`, or `selector.replace` reference invocation, any other selector callable, three or more legacy `if()` splats, nonterminal dual-splat recovery, modern CSS conditional evaluation, plain-CSS function references, remaining meta/string/color/calculation/special-function reference invocation, mixin application, module enumeration, dynamic loading, user-module reflection, imports/forwards, direct callable CSS serialization, broader direct-call variadic keyword behavior, or normalized diagnostics beyond executable evidence. Provider removal and the package-level zero-dependency cutover remain gated on native graduation rather than deleting the exact development oracles early. Independently, `BENCH-007` still requires a dedicated non-emulated Linux x64 runner with labels `[self-hosted, linux, x64, zigcss-benchmark-v1]`; its absence must not pause eligible native frontend work.

- Milestone: Milestone 10 self-contained native stylesheet frontends, in parallel with Milestone 8 controlled benchmark evidence
- Work package: `NSASS-011` native Sass evaluation
- State: `IN_PROGRESS`
- Active phase: the one-hundred-eighth native Sass semantic checkpoint, `NSASS-011-META-SELECTOR-SIMPLE-SELECTORS-FUNCTION-INVOCATION`, is `VERIFIED` at local implementation checkpoint `964d2163caa92213d589f8ff7f0addde1fb16005`. Provider-free native Zig now invokes the exact already-admitted module-owned `sass:selector` `simple-selectors` function reference through `meta.call()` for SCSS and indented Sass, plus the independently measured legacy-global reference. Default, exact custom, and unprefixed module access retain one module-owned identity without warnings; legacy-global reflection remains true, the global and module references remain distinct, and legacy invocation emits the existing normalized warning. Canonical positional and named `$selector` binding, list/map splats, string/existing-selector values, interpolation, simple-selector ordering and serialization, exact list item types, and once-only source order agree with Dart Sass 1.101.0. Missing, extra, unknown, duplicate, empty, null, boolean, number, map, complex-selector, selector-list, parent-selector, expanded-argument, selector-count, selector-byte, temporary-byte, function-argument, and every allocation failure fail closed, while module-owned `selector.is-superselector` keeps every remaining selector callable family outside this slice. Fresh probes reproduce exact compressed SCSS `.values{exists:true;type:function;same:true;star-same:true;plain:a,.foo,#id,:hover,::before,[title=x];named:button,.named,:hover;list-splat:.list,#item,:hover;map-splat:[data-x="a,b"],.item,:not(.x, .y);typed:button,.typed,:hover;interpolated:.item-2,:hover;inspected:article, .inspect, :hover;item-type:string;ordered:.ordered,:hover;trace:12;star:section,.star,:hover}`, indented Sass `.sass{type:function;plain:button,.primary,:hover;named:[title=x],.item,:hover;typed:article,.typed}`, and legacy `.legacy{exists:true;distinct:false;inspect:get-function("simple-selectors");value:a,.foo,:hover}` with one warning. The test-first focused Debug gate stopped only at the measured old dispatch and its limit/allocation paths with 359/363 tests. Final direct evaluator coverage passes 230/230; focused native Debug/ReleaseSafe each pass 23/23 steps with 363/363 tests; full compiler Debug/ReleaseSafe each pass 60/60 steps with 858/858 tests; and direct evaluator no-emission cross-compiles pass for Windows x64 GNU and Linux ARM64 GNU in both modes. Native contract 12/12 plus check adds only `built-in-meta-selector-simple-selectors-function-invocation` while retaining private native Sass rows, production unreachability, zero target package/optional/runtime dependencies, and the closed release interlock. Complete provider/reference/product matrices pass 151/151, preprocessor package policy passes 6/6 plus check, autonomous-runner selftest passes 59/59, and repository Zig formatting, JavaScript/JSON syntax, and diff checks pass. The temporary isolated macOS 15.4 SDK selector required by pinned Zig 0.15.2 was removed after validation; no tracked or system state changed. Production routing, public capability claims, native graduation, version, release interlock, tag, artifact/package publication, deployment, and every external system remain unchanged.
- Quality closure: `QUAL-001` is verified at `3eced86`; repository-wide Zig formatting debt is zero and the final local validation matrix is green
- Next eligible package: measure the exact Dart Sass 1.101.0 callable, namespace, argument, selector-value, relation-result, ordering, error, and limit boundaries for the already-constructible module-owned `sass:selector` `is-superselector` reference, then implement only the smallest evidence-closed `NSASS-011-META-SELECTOR-IS-SUPERSELECTOR-FUNCTION-INVOCATION` slice if those boundaries preserve the native safety contract. Do not infer selector composition or mutation reference invocation, any other selector callable, three or more legacy `if()` splats, nonterminal dual-splat recovery, modern CSS conditional evaluation, plain-CSS function references, remaining meta/string/color/calculation/special-function reference invocation, mixin application, module enumeration, dynamic loading, user-module reflection, imports/forwards, direct callable CSS serialization, broader direct-call variadic keyword behavior, or normalized diagnostics beyond executable evidence. Provider removal and the package-level zero-dependency cutover remain gated on native graduation rather than deleting the exact development oracles early. Independently, `BENCH-007` still requires a dedicated non-emulated Linux x64 runner with labels `[self-hosted, linux, x64, zigcss-benchmark-v1]`; its absence must not pause eligible native frontend work.

- Milestone: Milestone 10 self-contained native stylesheet frontends, in parallel with Milestone 8 controlled benchmark evidence
- Work package: `NSASS-011` native Sass evaluation
- State: `IN_PROGRESS`
- Active phase: the one-hundred-seventh native Sass semantic checkpoint, `NSASS-011-META-SELECTOR-PARSE-FUNCTION-INVOCATION`, is `VERIFIED` at local implementation checkpoint `7064ec8dea362750409ee57600407012c088c9d8`. Provider-free native Zig now invokes the exact already-admitted module-owned `sass:selector` `parse` function reference through `meta.call()` for SCSS and indented Sass. Default, exact custom, and unprefixed module access retain one module-owned identity without warnings; without an unprefixed module import, global reflection remains false and global reference lookup fails. Canonical positional and named `$selector` binding, list/map splats, string/list/existing-selector values, interpolation, selector normalization and serialization, leading combinators, and exact once-only source order agree with Dart Sass 1.101.0. Missing, extra, unknown, duplicate, empty, null, boolean, number, map, malformed parent-selector, expanded-argument, selector-count, selector-byte, temporary-byte, function-argument, and every allocation failure fail closed, while module-owned `selector.simple-selectors` keeps all remaining selector callable families outside this slice. Fresh probes reproduce exact compressed SCSS `.values{exists:true;type:function;same:true;star-same:true;plain:.foo > .bar,#id:hover;named:.named > .item,#id:hover;list-splat:.list > .item,#listed:hover;map-splat:[data-x="a,b"]:not(.x, .y);from-list:a,b;typed:.typed;interpolated:.item-2;inspected:.inspect, #me:hover;ordered:.ordered;trace:12;star:> .star,+ .next,~ .later}`, indented Sass `.sass{type:function;plain:.foo > .bar,#id:hover;named:[data-x="a,b"]:not(.x, .y);typed:.typed}`, false global reflection, the failed global lookup, no warnings, and all selected invalid bindings. The test-first focused Debug gate stopped only at the measured old dispatch and its limit/allocation paths with 357/360 tests. Final direct evaluator coverage passes 227/227; focused native Debug/ReleaseSafe each pass 23/23 steps with 360/360 tests; full compiler Debug/ReleaseSafe each pass 60/60 steps with 855/855 tests; and direct evaluator no-emission cross-compiles pass for Windows x64 GNU and Linux ARM64 GNU in both modes. Native contract 12/12 plus check adds only `built-in-meta-selector-parse-function-invocation` while retaining private native Sass rows, production unreachability, zero target package/optional/runtime dependencies, and the closed release interlock. Complete provider/reference/product matrices pass 151/151, preprocessor package policy passes 6/6 plus check, autonomous-runner selftest passes 59/59, and repository Zig formatting, JavaScript/JSON syntax, and diff checks pass. The temporary isolated macOS 15.4 SDK selector required by pinned Zig 0.15.2 was removed after validation; no tracked or system state changed. Production routing, public capability claims, native graduation, version, release interlock, tag, artifact/package publication, deployment, and every external system remain unchanged.
- Quality closure: `QUAL-001` is verified at `3eced86`; repository-wide Zig formatting debt is zero and the final local validation matrix is green
- Next eligible package: measure the exact Dart Sass 1.101.0 callable, namespace, argument, selector-value, serialization, ordering, error, and limit boundaries for the already-constructible module-owned `sass:selector` `simple-selectors` reference, then implement only the smallest evidence-closed `NSASS-011-META-SELECTOR-SIMPLE-SELECTORS-FUNCTION-INVOCATION` slice if those boundaries preserve the native safety contract. Do not infer selector composition/relation/mutation reference invocation or any other selector callable, three or more legacy `if()` splats, nonterminal dual-splat recovery, modern CSS conditional evaluation, plain-CSS function references, remaining meta/string/color/calculation/special-function reference invocation, mixin application, module enumeration, dynamic loading, user-module reflection, imports/forwards, direct callable CSS serialization, broader direct-call variadic keyword behavior, or normalized diagnostics beyond executable evidence. Provider removal and the package-level zero-dependency cutover remain gated on native graduation rather than deleting the exact development oracles early. Independently, `BENCH-007` still requires a dedicated non-emulated Linux x64 runner with labels `[self-hosted, linux, x64, zigcss-benchmark-v1]`; its absence must not pause eligible native frontend work.

- Milestone: Milestone 10 self-contained native stylesheet frontends, in parallel with Milestone 8 controlled benchmark evidence
- Work package: `NSASS-011` native Sass evaluation
- State: `IN_PROGRESS`
- Active phase: the one-hundred-sixth native Sass semantic checkpoint, `NSASS-011-META-MATH-RANDOM-FUNCTION-INVOCATION`, is `VERIFIED` at local implementation checkpoint `c217d28f3e8618885fb78b38d7ff0a60f79ba1d6`. Provider-free native Zig now invokes the exact already-admitted module-owned `sass:math` `random` function reference through `meta.call()` for SCSS and indented Sass, plus the independently measured legacy-global reference. Default, exact custom, and unprefixed module access retain module ownership without a legacy warning; legacy-global reflection remains true, the global and module references remain distinct, and each legacy invocation emits the existing normalized warning. Omitted and null limits, positional and named limits, list/map/empty splats, map override behavior, the full positive 32-bit bound, once-only source order, and unit-warning behavior agree with Dart Sass 1.101.0. Decimal, zero, negative, oversized, string, dynamic, extra, unknown, duplicate, argument-limit, and every allocation failure fail closed. The oracle produces different samples across identical compilations, while the native implementation deliberately retains its existing source-seeded deterministic stream so identical source bytes reproduce exactly without weakening range or sequence behavior. Fresh probes reproduce exact compressed SCSS range/reflection output, indented Sass `.sass{type:function;range:true}`, legacy `.legacy{exists:true;distinct:false;unit-range:true;bounded-range:true;unitful-range:true}` with three global warnings plus one unit warning, all selected binding failures, and four distinct outputs across four oracle replays. The test-first focused Debug gate stopped only at the measured old dispatch and allocation path with 354/357 tests. Final direct evaluator coverage passes 224/224; focused native Debug/ReleaseSafe each pass 23/23 steps with 357/357 tests; full compiler Debug/ReleaseSafe each pass 60/60 steps with 852/852 tests; and direct evaluator no-emission cross-compiles pass for Windows x64 GNU and Linux ARM64 GNU in both modes. Native contract 12/12 plus check adds only `built-in-meta-math-random-function-invocation` while retaining private native Sass rows, production unreachability, zero target package/optional/runtime dependencies, and the closed release interlock. Complete provider/reference/product matrices pass 151/151, preprocessor package policy passes 6/6 plus check, autonomous-runner selftest passes 59/59, and repository Zig formatting, JavaScript/JSON syntax, and diff checks pass. The temporary isolated macOS 15.4 SDK selector required by pinned Zig 0.15.2 was removed after validation; no tracked or system state changed. Production routing, public capability claims, native graduation, version, release interlock, tag, artifact/package publication, deployment, and every external system remain unchanged.
- Quality closure: `QUAL-001` is verified at `3eced86`; repository-wide Zig formatting debt is zero and the final local validation matrix is green
- Next eligible package: measure the exact Dart Sass 1.101.0 callable, namespace, argument, selector-value, serialization, ordering, error, and limit boundaries for the already-constructible module-owned `sass:selector` `parse` reference, then implement only the smallest evidence-closed `NSASS-011-META-SELECTOR-PARSE-FUNCTION-INVOCATION` slice if those boundaries preserve the native safety contract. Do not infer other selector composition/relation/mutation reference invocation, three or more legacy `if()` splats, nonterminal dual-splat recovery, modern CSS conditional evaluation, plain-CSS function references, remaining meta/string/color/calculation/special-function reference invocation, mixin application, module enumeration, dynamic loading, user-module reflection, imports/forwards, direct callable CSS serialization, broader direct-call variadic keyword behavior, or normalized diagnostics beyond executable evidence. Provider removal and the package-level zero-dependency cutover remain gated on native graduation rather than deleting the exact development oracles early. Independently, `BENCH-007` still requires a dedicated non-emulated Linux x64 runner with labels `[self-hosted, linux, x64, zigcss-benchmark-v1]`; its absence must not pause eligible native frontend work.

- Milestone: Milestone 10 self-contained native stylesheet frontends, in parallel with Milestone 8 controlled benchmark evidence
- Work package: `NSASS-011` native Sass evaluation
- State: `IN_PROGRESS`
- Active phase: the one-hundred-fifth native Sass semantic checkpoint, `NSASS-011-META-MATH-MAX-FUNCTION-INVOCATION`, is `VERIFIED` at local implementation checkpoint `5a23c9828094ef08dbde62a355d56a2f8ae8dce3`. Provider-free native Zig now invokes the exact already-admitted module-owned `sass:math` `max` function reference through `meta.call()` for SCSS and indented Sass, plus the independently measured legacy-global reference. Default, exact custom, and unprefixed module access retain module ownership without a legacy warning; legacy-global reflection remains true, the global and module references remain distinct, and each legacy invocation emits the existing normalized warning. Canonical one-or-more positional binding, list splats, converted units, unitless/unitful comparison, exact selected-argument unit spelling, and once-only source order agree with Dart Sass 1.101.0. Empty calls, keyword and map splat bindings, strings, lists, incompatible units, argument limits, and every allocation failure fail closed, while explicit module-owned `math.random` keeps the next unmeasured math callable outside this slice. Fresh oracle probes reproduce exact compressed SCSS `.values{plain:3px;one:3px;converted:97px;mixed-unitless:2;list-splat:3px;ordered:3px;trace:1234;star:5em}`, indented Sass `.sass{plain:3px;one:-5px}`, legacy `.legacy{exists:true;distinct:false;plain:3px;list-splat:3px}` plus two warnings, and all selected binding failures. The test-first focused Debug gate stopped only at the measured old dispatch, incompatible-unit propagation, and allocation paths with 350/354 tests. Final direct evaluator coverage passes 221/221; focused native Debug/ReleaseSafe each pass 23/23 steps with 354/354 tests; full compiler Debug/ReleaseSafe each pass 60/60 steps with 849/849 tests; and direct evaluator no-emission cross-compiles pass for Windows x64 GNU and Linux ARM64 GNU in both modes. Native contract 12/12 plus check adds only `built-in-meta-math-max-function-invocation` while retaining private native Sass rows, production unreachability, zero target package/optional/runtime dependencies, and the closed release interlock. Complete provider/reference/product matrices pass 151/151, preprocessor package policy passes 6/6 plus check, autonomous-runner selftest passes 59/59, and repository Zig formatting, JavaScript/JSON syntax, and diff checks pass. The temporary isolated macOS 15.4 SDK selector required by pinned Zig 0.15.2 was removed after validation; no tracked or system state changed. Production routing, public capability claims, native graduation, version, release interlock, tag, artifact/package publication, deployment, and every external system remain unchanged.
- Quality closure: `QUAL-001` is verified at `3eced86`; repository-wide Zig formatting debt is zero and the final local validation matrix is green
- Next eligible package: measure the exact Dart Sass 1.101.0 callable, argument, legacy-global, randomness, warning, limit, and determinism boundaries for the already-constructible module-owned `sass:math` `random` reference, then implement only the smallest evidence-closed `NSASS-011-META-MATH-RANDOM-FUNCTION-INVOCATION` slice if those boundaries preserve the native safety contract. Do not infer three or more legacy `if()` splats, nonterminal dual-splat recovery, modern CSS conditional evaluation, plain-CSS function references, other remaining meta/selector/string/color/calculation/special-function reference invocation, mixin application, module enumeration, dynamic loading, user-module reflection, imports/forwards, direct callable CSS serialization, broader direct-call variadic keyword behavior, or normalized diagnostics beyond executable evidence. Provider removal and the package-level zero-dependency cutover remain gated on native graduation rather than deleting the exact development oracles early. Independently, `BENCH-007` still requires a dedicated non-emulated Linux x64 runner with labels `[self-hosted, linux, x64, zigcss-benchmark-v1]`; its absence must not pause eligible native frontend work.

- Milestone: Milestone 10 self-contained native stylesheet frontends, in parallel with Milestone 8 controlled benchmark evidence
- Work package: `NSASS-011` native Sass evaluation
- State: `IN_PROGRESS`
- Active phase: the one-hundred-fourth native Sass semantic checkpoint, `NSASS-011-META-MATH-MIN-FUNCTION-INVOCATION`, is `VERIFIED` at local implementation checkpoint `1a009fdbfea524d55015f7c70508ac79767c3090`. Provider-free native Zig now invokes the exact already-admitted module-owned `sass:math` `min` function reference through `meta.call()` for SCSS and indented Sass, plus the independently measured legacy-global reference. Default, exact custom, and unprefixed module access retain module ownership without a legacy warning; legacy-global reflection remains true, the global and module references remain distinct, and each legacy invocation emits the existing normalized warning. Canonical one-or-more positional binding, list splats, converted units, unitless/unitful comparison, exact selected-argument unit spelling, and once-only source order agree with Dart Sass 1.101.0. Empty calls, keyword and map splat bindings, strings, lists, incompatible units, argument limits, and every allocation failure fail closed, while explicit module-owned `math.max` keeps every remaining unmeasured callable family outside this slice. Fresh oracle probes reproduce exact compressed SCSS `.values{plain:1px;one:3px;converted:95px;mixed-unitless:1px;list-splat:1px;ordered:1px;trace:1234;star:2em}`, indented Sass `.sass{plain:1px;one:-5px}`, legacy `.legacy{exists:true;distinct:false;plain:1px;list-splat:1px}` plus two warnings, and all selected binding failures. The test-first focused Debug gate stopped only at the measured old dispatch, incompatible-unit propagation, and allocation paths with 347/351 tests. Final direct evaluator coverage passes 218/218; focused native Debug/ReleaseSafe each pass 23/23 steps with 351/351 tests; full compiler Debug/ReleaseSafe each pass 60/60 steps with 846/846 tests; and direct evaluator no-emission cross-compiles pass for Windows x64 GNU and Linux ARM64 GNU in both modes. Native contract 12/12 plus check adds only `built-in-meta-math-min-function-invocation` while retaining private native Sass rows, production unreachability, zero target package/optional/runtime dependencies, and the closed release interlock. Complete provider/reference/product matrices pass 151/151, preprocessor package policy passes 6/6 plus check, autonomous-runner selftest passes 59/59, and repository Zig formatting, JavaScript/JSON syntax, and diff checks pass. The temporary isolated macOS 15.4 SDK selector required by pinned Zig 0.15.2 was removed after validation; no tracked or system state changed. Production routing, public capability claims, native graduation, version, release interlock, tag, artifact/package publication, deployment, and every external system remain unchanged.
- Quality closure: `QUAL-001` is verified at `3eced86`; repository-wide Zig formatting debt is zero and the final local validation matrix is green
- Next eligible package: measure and implement the smallest exact `NSASS-011-META-MATH-MAX-FUNCTION-INVOCATION` slice for the already-constructible module-owned `sass:math` `max` reference, including positional/list-splat behavior, the independently measured legacy-global reflection boundary, units, ordering, limits, and no-partial-result behavior. Do not infer three or more legacy `if()` splats, nonterminal dual-splat recovery, modern CSS conditional evaluation, plain-CSS function references, other remaining meta/math/selector/string/color/calculation/special-function reference invocation, mixin application, module enumeration, dynamic loading, user-module reflection, imports/forwards, direct callable CSS serialization, broader direct-call variadic keyword behavior, or normalized diagnostics beyond executable evidence. Provider removal and the package-level zero-dependency cutover remain gated on native graduation rather than deleting the exact development oracles early. Independently, `BENCH-007` still requires a dedicated non-emulated Linux x64 runner with labels `[self-hosted, linux, x64, zigcss-benchmark-v1]`; its absence must not pause eligible native frontend work.

- Milestone: Milestone 10 self-contained native stylesheet frontends, in parallel with Milestone 8 controlled benchmark evidence
- Work package: `NSASS-011` native Sass evaluation
- State: `IN_PROGRESS`
- Active phase: the one-hundred-third native Sass semantic checkpoint, `NSASS-011-META-MATH-HYPOT-FUNCTION-INVOCATION`, is `VERIFIED` at local implementation checkpoint `29bcfede3ff977272fdae4725b46c367b74ed1a9`. Provider-free native Zig now invokes the exact already-admitted module-owned `sass:math` `hypot` function reference through `meta.call()` for SCSS and indented Sass. Default, exact custom, and unprefixed module access retain module ownership without a legacy-global warning. The ordinary CSS-compatible `hypot()` call remains directly usable, but it is no longer misclassified as a legacy Sass reflection target: without an unprefixed module import, `meta.function-exists("hypot")` is false and `meta.get-function("hypot")` fails, matching Dart Sass 1.101.0. Canonical one-or-more positional binding, list splats, converted units, exact first-argument return-unit spelling, and once-only source order agree with the oracle. Empty calls, keyword and map splat bindings, strings, lists, mixed units, and incompatible units fail closed, while an explicit module-owned `math.min` reference keeps every remaining unmeasured callable family outside this slice. Fresh oracle probes reproduce exact compressed SCSS `.values{plain:5px;one:3px;converted:1.4142135624in;list-splat:13px;ordered:13px;trace:1234;star:13em}`, indented Sass `.sass{plain:5px;one:5px}`, false global reflection, the failed global reference lookup, no warnings, and all selected binding failures. The test-first focused Debug gate stopped only at the measured old dispatch, global-reflection, unit-error propagation, and allocation paths with 344/348 tests. Final direct evaluator coverage passes 215/215; focused native Debug/ReleaseSafe each pass 23/23 steps with 348/348 tests; full compiler Debug/ReleaseSafe each pass 60/60 steps with 843/843 tests; and direct evaluator no-emission cross-compiles pass for Windows x64 GNU and Linux ARM64 GNU in both modes. Native contract 12/12 plus check adds only `built-in-meta-math-hypot-function-invocation` while retaining private native Sass rows, production unreachability, zero target package/optional/runtime dependencies, and the closed release interlock. Complete provider/reference/product matrices pass 151/151, preprocessor package policy passes 6/6 plus check, autonomous-runner selftest passes 59/59, and repository Zig formatting, JavaScript/JSON syntax, Bash syntax, and diff checks pass. The temporary isolated macOS 15.4 SDK selector required by pinned Zig 0.15.2 was removed after validation; no tracked or system state changed. Production routing, public capability claims, native graduation, version, release interlock, tag, artifact/package publication, deployment, and every external system remain unchanged.
- Quality closure: `QUAL-001` is verified at `3eced86`; repository-wide Zig formatting debt is zero and the final local validation matrix is green
- Next eligible package: measure and implement the smallest exact `NSASS-011-META-MATH-MIN-FUNCTION-INVOCATION` slice for the already-constructible module-owned `sass:math` `min` reference, including positional/list-splat behavior, the independently measured legacy-global reflection boundary, units, ordering, limits, and no-partial-result behavior. Do not infer `math.max`, three or more legacy `if()` splats, nonterminal dual-splat recovery, modern CSS conditional evaluation, plain-CSS function references, other remaining meta/math/selector/string/color/calculation/special-function reference invocation, mixin application, module enumeration, dynamic loading, user-module reflection, imports/forwards, direct callable CSS serialization, broader direct-call variadic keyword behavior, or normalized diagnostics beyond executable evidence. Provider removal and the package-level zero-dependency cutover remain gated on native graduation rather than deleting the exact development oracles early. Independently, `BENCH-007` still requires a dedicated non-emulated Linux x64 runner with labels `[self-hosted, linux, x64, zigcss-benchmark-v1]`; its absence must not pause eligible native frontend work.

- Milestone: Milestone 10 self-contained native stylesheet frontends, in parallel with Milestone 8 controlled benchmark evidence
- Work package: `NSASS-011` native Sass evaluation
- State: `IN_PROGRESS`
- Active phase: the one-hundred-second native Sass semantic checkpoint, `NSASS-011-META-MATH-CLAMP-FUNCTION-INVOCATION`, is `VERIFIED` at local implementation checkpoint `0135b72fd63ab32fa04d6a862deb4b770d3c85ca`. Provider-free native Zig now invokes the exact already-admitted module-owned `sass:math` `clamp` function reference through `meta.call()` for SCSS and indented Sass. Default, exact custom, and unprefixed module access retain module ownership without a legacy-global warning. The ordinary CSS `clamp()` function remains directly callable, but it is no longer misclassified as a legacy Sass callable: without an unprefixed module import, `meta.function-exists("clamp")` is false and `meta.get-function("clamp")` fails, matching Dart Sass 1.101.0. Canonical positional and named `$min`/`$number`/`$max` binding, list/map splats, converted units, lower/upper bounding, exact return-unit spelling, and once-only source order agree with the oracle. Missing operands, extra, unknown, duplicate, incompatible-unit, string, and list bindings fail closed, while an explicit `math.hypot` reference keeps every remaining unmeasured callable family outside this slice. Fresh oracle probes reproduce exact compressed SCSS `.values{plain:2px;low:1px;high:3px;named:2px;list-splat:2px;map-splat:96px;ordered:2px;trace:1432;star:2em}`, indented Sass `.sass{plain:2px;named:3px}`, false global reflection, the failed global reference lookup, no warnings, and all selected binding failures. The test-first focused Debug gate stopped only at the measured old dispatch, global-reflection, and allocation paths with 341/344 tests. Final direct evaluator coverage passes 212/212; focused native Debug/ReleaseSafe each pass 23/23 steps with 345/345 tests; full compiler Debug/ReleaseSafe each pass 60/60 steps with 840/840 tests; and direct evaluator no-emission cross-compiles pass for Windows x64 GNU and Linux ARM64 GNU in both modes. Native contract 10/10 plus check adds only `built-in-meta-math-clamp-function-invocation` while retaining private native Sass rows, production unreachability, zero target package/optional/runtime dependencies, and the closed release interlock. Complete provider/reference/product matrices pass 151/151, preprocessor package policy passes 6/6 plus check, autonomous-runner selftest passes 58/58, and repository Zig formatting, JavaScript/JSON syntax, Bash syntax, and diff checks pass. The temporary isolated macOS 15.4 SDK selector required by pinned Zig 0.15.2 was removed after validation; no tracked or system state changed. Production routing, public capability claims, native graduation, version, release interlock, tag, artifact/package publication, deployment, and every external system remain unchanged.
- Quality closure: `QUAL-001` is verified at `3eced86`; repository-wide Zig formatting debt is zero and the final local validation matrix is green
- Next eligible package: measure and select the smallest next unsupported dependency-ordered `NSASS-011` Sass semantic slice. Do not infer three or more legacy `if()` splats, nonterminal dual-splat recovery, modern CSS conditional evaluation, plain-CSS function references, remaining meta/math/selector/string/color/calculation/special-function reference invocation including the explicit `math.hypot` boundary, mixin application, module enumeration, dynamic loading, user-module reflection, imports/forwards, direct callable CSS serialization, broader direct-call variadic keyword behavior, or normalized diagnostics beyond executable evidence. Provider removal and the package-level zero-dependency cutover remain gated on native graduation rather than deleting the exact development oracles early. Independently, `BENCH-007` still requires a dedicated non-emulated Linux x64 runner with labels `[self-hosted, linux, x64, zigcss-benchmark-v1]`; its absence must not pause eligible native frontend work.

- Milestone: Milestone 10 self-contained native stylesheet frontends, in parallel with Milestone 8 controlled benchmark evidence
- Work package: `NSASS-011` native Sass evaluation
- State: `IN_PROGRESS`
- Active phase: the one-hundred-first native Sass semantic checkpoint, `NSASS-011-META-MATH-DIV-FUNCTION-INVOCATION`, is `VERIFIED` at local implementation checkpoint `f400590bb05d9a31ea2742759ed7031f4190f2ff`. Provider-free native Zig now invokes the exact already-admitted module-owned `sass:math` `div` function reference through `meta.call()` for SCSS and indented Sass. Default, exact custom, and unprefixed module access retain module ownership without a legacy-global warning. The ordinary CSS `div()` function name remains directly serializable but is not invented as a legacy Sass callable: without an unprefixed module import, `meta.function-exists("div")` is false and `meta.get-function("div")` fails, matching Dart Sass 1.101.0. Canonical positional and named `$number1`/`$number2` binding, list/map splats, exact once-only source order, numeric and unit division, compound-unit inspection, and bounded legacy slash values agree with the oracle. Missing operands, extra, unknown, duplicate, map, and invalid left-color bindings fail closed. Zero division retains the existing deliberate `DivisionByZero` safe-narrowing, while an explicit `math.clamp` reference keeps every remaining unmeasured callable family outside this slice. Fresh oracle probes reproduce exact compressed SCSS `.values{plain:3px;named:.25;list-splat:3px;map-splat:1;compound-unit:"px/s";strings:foo/bar;brackets:[a, b]/[c d];ordered:3px;trace:132;star:2em}`, indented Sass `.sass{plain:4px;named:3px}`, false global reflection, the failed global reference lookup, and the two forward-compatibility warnings for nonnumber operands; Dart Sass's `calc(infinity)`, `calc(-infinity)`, and `calc(NaN)` zero-division outputs remain intentionally outside the native safety boundary, and normalized reference-warning parity remains outside this slice. The test-first focused Debug gate stopped only at the measured old dispatch, zero-error propagation, and allocation paths with 339/342 tests. Final direct evaluator coverage passes 209/209; focused native Debug/ReleaseSafe each pass 23/23 steps with 342/342 tests; full compiler Debug/ReleaseSafe each pass 60/60 steps with 837/837 tests; and direct evaluator no-emission cross-compiles pass for Windows x64 GNU and Linux ARM64 GNU in both modes. Native contract 10/10 plus check adds only `built-in-meta-math-div-function-invocation` while retaining private native Sass rows, production unreachability, zero target package/optional/runtime dependencies, and the closed release interlock. Complete provider/reference/product matrices pass 151/151, preprocessor package policy passes 6/6 plus check, autonomous-runner selftest passes 58/58, and repository Zig formatting, JavaScript/JSON syntax, Bash syntax, and diff checks pass. The temporary isolated macOS 15.4 SDK selector required by pinned Zig 0.15.2 was removed after validation; no tracked or system state changed. Production routing, public capability claims, native graduation, version, release interlock, tag, artifact/package publication, deployment, and every external system remain unchanged.
- Quality closure: `QUAL-001` is verified at `3eced86`; repository-wide Zig formatting debt is zero and the final local validation matrix is green
- Next eligible package: measure and select the smallest next unsupported dependency-ordered `NSASS-011` Sass semantic slice. Do not infer three or more legacy `if()` splats, nonterminal dual-splat recovery, modern CSS conditional evaluation, plain-CSS function references, remaining meta/math/selector/string/color/calculation/special-function reference invocation including the explicit `math.clamp` boundary, mixin application, module enumeration, dynamic loading, user-module reflection, imports/forwards, direct callable CSS serialization, broader direct-call variadic keyword behavior, or normalized diagnostics beyond executable evidence. Provider removal and the package-level zero-dependency cutover remain gated on native graduation rather than deleting the exact development oracles early. Independently, `BENCH-007` still requires a dedicated non-emulated Linux x64 runner with labels `[self-hosted, linux, x64, zigcss-benchmark-v1]`; its absence must not pause eligible native frontend work.

- Milestone: Milestone 10 self-contained native stylesheet frontends, in parallel with Milestone 8 controlled benchmark evidence
- Work package: `NSASS-011` native Sass evaluation
- State: `IN_PROGRESS`
- Active phase: the one-hundredth native Sass semantic checkpoint, `NSASS-011-META-MATH-SQRT-FUNCTION-INVOCATION`, is `VERIFIED` at local implementation checkpoint `c93d770e69a9ce445f4d24898d5555190b85ef35`. Provider-free native Zig now invokes the exact already-admitted module-owned `sass:math` `sqrt` function reference through `meta.call()` for SCSS and indented Sass. Default, exact custom, and unprefixed module access retain module ownership without a legacy-global warning. The global CSS-compatible `sqrt()` calculation name remains directly callable but is not misclassified as a legacy Sass callable: without an unprefixed module import, `meta.function-exists("sqrt")` is false and `meta.get-function("sqrt")` fails, matching Dart Sass 1.101.0. Canonical positional and named `$number` binding, list/map splats, irrational, perfect, zero, and fractional roots, and exact once-only source order agree with the oracle. Missing, extra, unknown, duplicate, string, list, and unit-bearing bindings fail closed. Negative roots retain the existing deliberate nonfinite `InvalidNumber` safe-narrowing, while an explicit `math.div` reference keeps every remaining unmeasured callable family outside this slice. Fresh oracle probes reproduce exact compressed SCSS `.values{irrational:1.4142135624;named:9;list-splat:9;map-splat:.25;zero:0;fraction:.5;ordered:9;trace:12;star:7}`, indented Sass `.sass{irrational:1.4142135624;named:11}`, false global reflection, the failed global reference lookup, no warnings, and all selected binding failures; Dart Sass's `calc(NaN)` negative-root output remains intentionally outside the native safety boundary. The test-first focused Debug gate stopped only at the measured old dispatch, global-reflection, nonfinite-narrowing, and allocation paths with 335/339 tests. Final direct evaluator coverage passes 206/206; focused native Debug/ReleaseSafe each pass 23/23 steps with 339/339 tests; full compiler Debug/ReleaseSafe each pass 60/60 steps with 834/834 tests; and direct evaluator no-emission cross-compiles pass for Windows x64 GNU and Linux ARM64 GNU in both modes. Native contract 10/10 plus check adds only `built-in-meta-math-sqrt-function-invocation` while retaining private native Sass rows, production unreachability, zero target package/optional/runtime dependencies, and the closed release interlock. Complete provider/reference/product matrices pass 151/151, preprocessor package policy passes 6/6 plus check, autonomous-runner selftest passes 58/58, and repository Zig formatting, JavaScript/JSON syntax, Bash syntax, and diff checks pass. The temporary isolated macOS 15.4 SDK selector required by pinned Zig 0.15.2 was removed after validation; no tracked or system state changed. Production routing, public capability claims, native graduation, version, release interlock, tag, artifact/package publication, deployment, and every external system remain unchanged.
- Quality closure: `QUAL-001` is verified at `3eced86`; repository-wide Zig formatting debt is zero and the final local validation matrix is green
- Next eligible package: measure and select the smallest next unsupported dependency-ordered `NSASS-011` Sass semantic slice. Do not infer three or more legacy `if()` splats, nonterminal dual-splat recovery, modern CSS conditional evaluation, plain-CSS function references, remaining meta/math/selector/string/color/calculation/special-function reference invocation including the explicit `math.div` boundary, mixin application, module enumeration, dynamic loading, user-module reflection, imports/forwards, direct callable CSS serialization, broader direct-call variadic keyword behavior, or normalized diagnostics beyond executable evidence. Provider removal and the package-level zero-dependency cutover remain gated on native graduation rather than deleting the exact development oracles early. Independently, `BENCH-007` still requires a dedicated non-emulated Linux x64 runner with labels `[self-hosted, linux, x64, zigcss-benchmark-v1]`; its absence must not pause eligible native frontend work.

- Milestone: Milestone 10 self-contained native stylesheet frontends, in parallel with Milestone 8 controlled benchmark evidence
- Work package: `NSASS-011` native Sass evaluation
- State: `IN_PROGRESS`
- Active phase: the ninety-ninth native Sass semantic checkpoint, `NSASS-011-META-MATH-POW-FUNCTION-INVOCATION`, is `VERIFIED` at local implementation checkpoint `0c32bdadceaa306263852072ffa882be51f7a7bd`. Provider-free native Zig now invokes the exact already-admitted module-owned `sass:math` `pow` function reference through `meta.call()` for SCSS and indented Sass. Default, exact custom, and unprefixed module access retain module ownership without a legacy-global warning. The global CSS-compatible `pow()` calculation name remains directly callable but is not misclassified as a legacy Sass callable: without an unprefixed module import, `meta.function-exists("pow")` is false and `meta.get-function("pow")` fails, matching Dart Sass 1.101.0. Canonical positional and named `$base`/`$exponent` binding, list/map splats, integer, negative, fractional, and zero-to-zero powers, and exact once-only source order agree with the oracle. Missing base or exponent, extra, unknown, duplicate, string, list, and unit-bearing bindings fail closed. Infinite and NaN results retain the existing deliberate nonfinite `InvalidNumber` safe-narrowing, while an explicit `math.sqrt` reference keeps every remaining unmeasured callable family outside this slice. Fresh oracle probes reproduce exact compressed SCSS `.values{integer:8;named:8;list-splat:8;map-splat:2;negative:.125;fraction:3;zero-zero:1;ordered:8;trace:123;star:8}`, indented Sass `.sass{integer:32;named:7}`, false global reflection, the failed global reference lookup, no warnings, and all selected binding failures; Dart Sass's `calc(infinity)` and `calc(NaN)` outputs remain intentionally outside the native safety boundary. The test-first focused Debug gate stopped only at the measured old dispatch, global-reflection, nonfinite-narrowing, and allocation paths with 332/336 tests. Final direct evaluator coverage passes 203/203; focused native Debug/ReleaseSafe each pass 23/23 steps with 336/336 tests; full compiler Debug/ReleaseSafe each pass 60/60 steps with 831/831 tests; and direct evaluator no-emission cross-compiles pass for Windows x64 GNU and Linux ARM64 GNU in both modes. Native contract 10/10 plus check adds only `built-in-meta-math-pow-function-invocation` while retaining private native Sass rows, production unreachability, zero target package/optional/runtime dependencies, and the closed release interlock. Complete provider/reference/product matrices pass 151/151, preprocessor package policy passes 6/6 plus check, autonomous-runner selftest passes 58/58, and repository Zig formatting, JavaScript/JSON syntax, Bash syntax, and diff checks pass. The temporary isolated macOS 15.4 SDK selector required by pinned Zig 0.15.2 was removed after validation; no tracked or system state changed. Production routing, public capability claims, native graduation, version, release interlock, tag, artifact/package publication, deployment, and every external system remain unchanged.
- Quality closure: `QUAL-001` is verified at `3eced86`; repository-wide Zig formatting debt is zero and the final local validation matrix is green
- Next eligible package: measure and select the smallest next unsupported dependency-ordered `NSASS-011` Sass semantic slice. Do not infer three or more legacy `if()` splats, nonterminal dual-splat recovery, modern CSS conditional evaluation, plain-CSS function references, remaining meta/math/selector/string/color/calculation/special-function reference invocation including the explicit `math.sqrt` boundary, mixin application, module enumeration, dynamic loading, user-module reflection, imports/forwards, direct callable CSS serialization, broader direct-call variadic keyword behavior, or normalized diagnostics beyond executable evidence. Provider removal and the package-level zero-dependency cutover remain gated on native graduation rather than deleting the exact development oracles early. Independently, `BENCH-007` still requires a dedicated non-emulated Linux x64 runner with labels `[self-hosted, linux, x64, zigcss-benchmark-v1]`; its absence must not pause eligible native frontend work.

- Milestone: Milestone 10 self-contained native stylesheet frontends, in parallel with Milestone 8 controlled benchmark evidence
- Work package: `NSASS-011` native Sass evaluation
- State: `IN_PROGRESS`
- Active phase: the ninety-eighth native Sass semantic checkpoint, `NSASS-011-META-MATH-LOG-FUNCTION-INVOCATION`, is `VERIFIED` at local implementation checkpoint `a6cd31de785aade9641fabf3e60d1a285c116dbe`. Provider-free native Zig now invokes the exact already-admitted module-owned `sass:math` `log` function reference through `meta.call()` for SCSS and indented Sass. Default, exact custom, and unprefixed module access retain module ownership without a legacy-global warning. The global CSS-compatible `log()` calculation name remains directly callable but is not misclassified as a legacy Sass callable: without an unprefixed module import, `meta.function-exists("log")` is false and `meta.get-function("log")` fails, matching Dart Sass 1.101.0. Canonical positional and named `$number`/optional `$base` binding, list/map splats, natural and custom-base logarithms, the canonical base-zero result, and exact once-only source order agree with the oracle. Missing, extra, unknown, duplicate, string, list, and unit-bearing bindings fail closed. Zero or negative inputs and one or negative bases retain the existing deliberate nonfinite `InvalidNumber` safe-narrowing, while an explicit `math.pow` reference keeps every remaining unmeasured callable family outside this slice. Fresh oracle probes reproduce exact compressed SCSS `.values{natural:1;named:3;list-splat:3;map-splat:2;decimal:2.302585093;binary:3;zero-base:0;ordered:2;trace:123;star:3}`, indented Sass `.sass{natural:1;named:3}`, false global reflection, the failed global reference lookup, no warnings, and all selected binding failures; Dart Sass's nonfinite CSS calculation outputs remain intentionally outside the native safety boundary. The test-first focused Debug gate stopped only at the measured old dispatch, global-reflection, and allocation paths with 329/332 tests. Final direct evaluator coverage passes 200/200; focused native Debug/ReleaseSafe each pass 23/23 steps with 333/333 tests; full compiler Debug/ReleaseSafe each pass 60/60 steps with 828/828 tests; and direct evaluator no-emission cross-compiles pass for Windows x64 GNU and Linux ARM64 GNU in both modes. Native contract 10/10 plus check adds only `built-in-meta-math-log-function-invocation` while retaining private native Sass rows, production unreachability, zero target package/optional/runtime dependencies, and the closed release interlock. Complete provider/reference/product matrices pass 151/151, preprocessor package policy passes 6/6 plus check, autonomous-runner selftest passes 58/58, and repository Zig formatting, JavaScript/JSON syntax, and diff checks pass. The temporary isolated macOS 15.4 SDK selector required by pinned Zig 0.15.2 was removed after validation; no tracked or system state changed. Production routing, public capability claims, native graduation, version, release interlock, tag, artifact/package publication, deployment, and every external system remain unchanged.
- Quality closure: `QUAL-001` is verified at `3eced86`; repository-wide Zig formatting debt is zero and the final local validation matrix is green
- Next eligible package: measure and select the smallest next unsupported dependency-ordered `NSASS-011` Sass semantic slice. Do not infer three or more legacy `if()` splats, nonterminal dual-splat recovery, modern CSS conditional evaluation, plain-CSS function references, remaining meta/math/selector/string/color/calculation/special-function reference invocation including the explicit `math.pow` boundary, mixin application, module enumeration, dynamic loading, user-module reflection, imports/forwards, direct callable CSS serialization, broader direct-call variadic keyword behavior, or normalized diagnostics beyond executable evidence. Provider removal and the package-level zero-dependency cutover remain gated on native graduation rather than deleting the exact development oracles early. Independently, `BENCH-007` still requires a dedicated non-emulated Linux x64 runner with labels `[self-hosted, linux, x64, zigcss-benchmark-v1]`; its absence must not pause eligible native frontend work.

- Milestone: Milestone 10 self-contained native stylesheet frontends, in parallel with Milestone 8 controlled benchmark evidence
- Work package: `NSASS-011` native Sass evaluation
- State: `IN_PROGRESS`
- Active phase: the ninety-seventh native Sass semantic checkpoint, `NSASS-011-META-MATH-TAN-FUNCTION-INVOCATION`, is `VERIFIED` at local implementation checkpoint `a470002f49138bf5b93d822fa5fd389c9360792d`. Provider-free native Zig now invokes the exact already-admitted module-owned `sass:math` `tan` function reference through `meta.call()` for SCSS and indented Sass. Default, exact custom, and unprefixed module access retain module ownership without a legacy-global warning. The global CSS-compatible `tan()` calculation name remains directly callable but is not misclassified as a legacy Sass callable: without an unprefixed module import, `meta.function-exists("tan")` is false and `meta.get-function("tan")` fails, matching Dart Sass 1.101.0. Canonical positional and named `$number` binding, list/map splats, unitless radians, `deg`/`grad`/`turn`/`rad` angles, finite tangent results, and exact once-only source order agree with the oracle. Missing, extra, unknown, duplicate, string, list, and non-angle-unit arguments fail closed; the prior deliberate nonfinite safe-narrowing remains unchanged, while an explicit `math.log` reference keeps every remaining unmeasured callable family outside this slice. Fresh oracle probes reproduce exact compressed SCSS `.values{zero:0;named:1;list-splat:.5773502692;map-splat:-.5773502692;unitless:1.5574077247;grad:1;turn:1;rad:1.5574077247;ordered:.5773502692;trace:12;star:-.5773502692}`, indented Sass `.sass{one:1;named:-1}`, false global reflection, the failed global reference lookup, no warnings, and all selected invalid bindings. The test-first focused Debug gate stopped only at the measured old dispatch, global-reflection, and allocation paths with 327/330 tests. Final direct evaluator coverage passes 197/197; focused native Debug/ReleaseSafe each pass 23/23 steps with 330/330 tests; full compiler Debug/ReleaseSafe each pass 60/60 steps with 825/825 tests; and direct evaluator no-emission cross-compiles pass for Windows x64 GNU and Linux ARM64 GNU in both modes. Native contract 10/10 plus check adds only `built-in-meta-math-tan-function-invocation` while retaining private native Sass rows, production unreachability, zero target package/optional/runtime dependencies, and the closed release interlock. Complete provider/reference/product matrices pass 151/151, preprocessor package policy passes 6/6 plus check, autonomous-runner selftest passes 58/58, and repository Zig formatting, JavaScript/JSON syntax, and diff checks pass. The temporary isolated macOS 15.4 SDK selector required by pinned Zig 0.15.2 was removed after validation; no tracked or system state changed. Production routing, public capability claims, native graduation, version, release interlock, tag, artifact/package publication, deployment, and every external system remain unchanged.
- Quality closure: `QUAL-001` is verified at `3eced86`; repository-wide Zig formatting debt is zero and the final local validation matrix is green
- Next eligible package: measure and select the smallest next unsupported dependency-ordered `NSASS-011` Sass semantic slice. Do not infer three or more legacy `if()` splats, nonterminal dual-splat recovery, modern CSS conditional evaluation, plain-CSS function references, remaining meta/math/selector/string/color/calculation/special-function reference invocation including the explicit `math.log` boundary, mixin application, module enumeration, dynamic loading, user-module reflection, imports/forwards, direct callable CSS serialization, broader direct-call variadic keyword behavior, or normalized diagnostics beyond executable evidence. Provider removal and the package-level zero-dependency cutover remain gated on native graduation rather than deleting the exact development oracles early. Independently, `BENCH-007` still requires a dedicated non-emulated Linux x64 runner with labels `[self-hosted, linux, x64, zigcss-benchmark-v1]`; its absence must not pause eligible native frontend work.

- Milestone: Milestone 10 self-contained native stylesheet frontends, in parallel with Milestone 8 controlled benchmark evidence
- Work package: `NSASS-011` native Sass evaluation
- State: `IN_PROGRESS`
- Active phase: the ninety-sixth native Sass semantic checkpoint, `NSASS-011-META-MATH-COS-FUNCTION-INVOCATION`, is `VERIFIED` at local implementation checkpoint `220057dc7b67122e520aa017e16b14cf53ff4080`. Provider-free native Zig now invokes the exact already-admitted module-owned `sass:math` `cos` function reference through `meta.call()` for SCSS and indented Sass. Default, exact custom, and unprefixed module access retain module ownership without a legacy-global warning. The global CSS-compatible `cos()` calculation name remains directly callable but is not misclassified as a legacy Sass callable: without an unprefixed module import, `meta.function-exists("cos")` is false and `meta.get-function("cos")` fails, matching Dart Sass 1.101.0. Canonical positional and named `$number` binding, list/map splats, unitless radians, `deg`/`grad`/`turn`/`rad` angles, finite cosine results, and exact once-only source order agree with the oracle. Missing, extra, unknown, duplicate, string, list, and non-angle-unit arguments fail closed; the prior deliberate nonfinite safe-narrowing remains unchanged, while an explicit `math.tan` reference keeps every remaining unmeasured callable family outside this slice. Fresh oracle probes reproduce exact compressed SCSS `.values{zero:1;named:0;list-splat:.5;map-splat:.5;unitless:.5403023059;grad:0;turn:0;rad:.5403023059;ordered:.5;trace:12;star:.5}`, indented Sass `.sass{half:.5;named:.5}`, false global reflection, the failed global reference lookup, no warnings, and all selected invalid bindings. The test-first focused Debug gate stopped only at the measured old dispatch, global-reflection, and allocation paths with 325/328 tests. Final direct evaluator coverage passes 195/195; focused native Debug/ReleaseSafe each pass 23/23 steps with 328/328 tests; full compiler Debug/ReleaseSafe each pass 60/60 steps with 823/823 tests; and direct evaluator no-emission cross-compiles pass for Windows x64 GNU and Linux ARM64 GNU in both modes. Native contract 10/10 plus check adds only `built-in-meta-math-cos-function-invocation` while retaining private native Sass rows, production unreachability, zero target package/optional/runtime dependencies, and the closed release interlock. Complete provider/reference/product matrices pass 151/151, preprocessor package policy passes 6/6 plus check, autonomous-runner selftest passes 58/58, and repository Zig formatting, JavaScript/JSON syntax, and diff checks pass. The temporary isolated macOS 15.4 SDK selector required by pinned Zig 0.15.2 was removed after validation; no tracked or system state changed. Production routing, public capability claims, native graduation, version, release interlock, tag, artifact/package publication, deployment, and every external system remain unchanged.
- Quality closure: `QUAL-001` is verified at `3eced86`; repository-wide Zig formatting debt is zero and the final local validation matrix is green
- Next eligible package: measure and select the smallest next unsupported dependency-ordered `NSASS-011` Sass semantic slice. Do not infer three or more legacy `if()` splats, nonterminal dual-splat recovery, modern CSS conditional evaluation, plain-CSS function references, remaining meta/math/selector/string/color/calculation/special-function reference invocation including the explicit `math.tan` boundary, mixin application, module enumeration, dynamic loading, user-module reflection, imports/forwards, direct callable CSS serialization, broader direct-call variadic keyword behavior, or normalized diagnostics beyond executable evidence. Provider removal and the package-level zero-dependency cutover remain gated on native graduation rather than deleting the exact development oracles early. Independently, `BENCH-007` still requires a dedicated non-emulated Linux x64 runner with labels `[self-hosted, linux, x64, zigcss-benchmark-v1]`; its absence must not pause eligible native frontend work.

- Milestone: Milestone 10 self-contained native stylesheet frontends, in parallel with Milestone 8 controlled benchmark evidence
- Work package: `NSASS-011` native Sass evaluation
- State: `IN_PROGRESS`
- Active phase: the ninety-fifth native Sass semantic checkpoint, `NSASS-011-META-MATH-SIN-FUNCTION-INVOCATION`, is `VERIFIED` at local implementation checkpoint `944d3534b0882a4a4d30bcfb5238628fcb8eb519`. Provider-free native Zig now invokes the exact already-admitted module-owned `sass:math` `sin` function reference through `meta.call()` for SCSS and indented Sass. Default, exact custom, and unprefixed module access retain module ownership without a legacy-global warning. The global CSS-compatible `sin()` calculation name remains directly callable but is not misclassified as a legacy Sass callable: without an unprefixed module import, `meta.function-exists("sin")` is false and `meta.get-function("sin")` fails, matching Dart Sass 1.101.0. Canonical positional and named `$number` binding, list/map splats, unitless radians, `deg`/`grad`/`turn`/`rad` angles, finite sine results, and exact once-only source order agree with the oracle. Missing, extra, unknown, duplicate, string, list, and non-angle-unit arguments fail closed; the prior deliberate nonfinite safe-narrowing remains unchanged, while an explicit `math.cos` reference keeps every remaining unmeasured callable family outside this slice. Fresh oracle probes reproduce exact compressed SCSS `.values{zero:0;named:1;list-splat:.5;map-splat:-.5;unitless:.8414709848;grad:1;turn:1;rad:.8414709848;ordered:.5;trace:12;star:-.5}`, indented Sass `.sass{half:.5;named:-.5}`, false global reflection, the failed global reference lookup, no warnings, and all selected invalid bindings. The test-first focused Debug gate stopped only at the measured old dispatch, global-reflection, and allocation paths with 323/326 tests. Final direct evaluator coverage passes 193/193; focused native Debug/ReleaseSafe each pass 23/23 steps with 326/326 tests; full compiler Debug/ReleaseSafe each pass 60/60 steps with 821/821 tests; and direct evaluator no-emission cross-compiles pass for Windows x64 GNU and Linux ARM64 GNU in both modes. Native contract 10/10 plus check adds only `built-in-meta-math-sin-function-invocation` while retaining private native Sass rows, production unreachability, zero target package/optional/runtime dependencies, and the closed release interlock. Complete provider/reference/product matrices pass 151/151, preprocessor package policy passes 6/6 plus check, autonomous-runner selftest passes 58/58, and repository Zig formatting, JavaScript/JSON syntax, and diff checks pass. The temporary isolated macOS 15.4 SDK selector required by pinned Zig 0.15.2 was removed after validation; no tracked or system state changed. Production routing, public capability claims, native graduation, version, release interlock, tag, artifact/package publication, deployment, and every external system remain unchanged.
- Quality closure: `QUAL-001` is verified at `3eced86`; repository-wide Zig formatting debt is zero and the final local validation matrix is green
- Next eligible package: measure and select the smallest next unsupported dependency-ordered `NSASS-011` Sass semantic slice. Do not infer three or more legacy `if()` splats, nonterminal dual-splat recovery, modern CSS conditional evaluation, plain-CSS function references, remaining meta/math/selector/string/color/calculation/special-function reference invocation including the explicit `math.cos` boundary, mixin application, module enumeration, dynamic loading, user-module reflection, imports/forwards, direct callable CSS serialization, broader direct-call variadic keyword behavior, or normalized diagnostics beyond executable evidence. Provider removal and the package-level zero-dependency cutover remain gated on native graduation rather than deleting the exact development oracles early. Independently, `BENCH-007` still requires a dedicated non-emulated Linux x64 runner with labels `[self-hosted, linux, x64, zigcss-benchmark-v1]`; its absence must not pause eligible native frontend work.

- Milestone: Milestone 10 self-contained native stylesheet frontends, in parallel with Milestone 8 controlled benchmark evidence
- Work package: `NSASS-011` native Sass evaluation
- State: `IN_PROGRESS`
- Active phase: the ninety-fourth native Sass semantic checkpoint, `NSASS-011-META-MATH-ATAN2-FUNCTION-INVOCATION`, is `VERIFIED` at local implementation checkpoint `0b6c32fcb0e97ad7927b0ef873ed4cf11d41bae0`. Provider-free native Zig now invokes the exact already-admitted module-owned `sass:math` `atan2` function reference through `meta.call()` for SCSS and indented Sass. Default, exact custom, and unprefixed module access retain module ownership without a legacy-global warning. The global CSS-compatible `atan2()` calculation name remains directly callable but is not misclassified as a legacy Sass callable: without an unprefixed module import, `meta.function-exists("atan2")` is false and `meta.get-function("atan2")` fails, matching Dart Sass 1.101.0. Canonical positional and named `$y`/`$x` binding, list/map splats, compatible known and percentage units, finite quadrant results in degrees, and exact once-only source order agree with the oracle. Missing, extra, unknown, duplicate, string, list, mixed-unit, and incompatible-unit arguments fail closed; the prior deliberate nonfinite safe-narrowing remains unchanged, while an explicit `math.sin` reference keeps every remaining unmeasured callable family outside this slice. Fresh oracle probes reproduce exact compressed SCSS `.values{diagonal:45deg;named:180deg;list-splat:45deg;map-splat:-45deg;units:45deg;percent:26.5650511771deg;ordered:45deg;trace:123;star:-135deg}`, indented Sass `.sass{diagonal:45deg;named:-45deg}`, false global reflection, the failed global reference lookup, no warnings, and all selected invalid bindings. The test-first focused Debug gate stopped only at the measured old dispatch, global-reflection, and allocation paths with 321/324 tests. Final direct evaluator coverage passes 191/191; focused native Debug/ReleaseSafe each pass 23/23 steps with 324/324 tests; full compiler Debug/ReleaseSafe each pass 60/60 steps with 819/819 tests; and direct evaluator no-emission cross-compiles pass for Windows x64 GNU and Linux ARM64 GNU in both modes. Native contract 10/10 plus check adds only `built-in-meta-math-atan2-function-invocation` while retaining private native Sass rows, production unreachability, zero target package/optional/runtime dependencies, and the closed release interlock. Complete provider/reference/product matrices pass 151/151, preprocessor package policy passes 6/6 plus check, autonomous-runner selftest passes 58/58, and repository Zig formatting, JavaScript/JSON syntax, and diff checks pass. The temporary isolated macOS 15.4 SDK selector required by pinned Zig 0.15.2 was removed after validation; no tracked or system state changed. Production routing, public capability claims, native graduation, version, release interlock, tag, artifact/package publication, deployment, and every external system remain unchanged.
- Quality closure: `QUAL-001` is verified at `3eced86`; repository-wide Zig formatting debt is zero and the final local validation matrix is green
- Next eligible package: measure and select the smallest next unsupported dependency-ordered `NSASS-011` Sass semantic slice. Do not infer three or more legacy `if()` splats, nonterminal dual-splat recovery, modern CSS conditional evaluation, plain-CSS function references, remaining meta/math/selector/string/color/calculation/special-function reference invocation including the explicit `math.sin` boundary, mixin application, module enumeration, dynamic loading, user-module reflection, imports/forwards, direct callable CSS serialization, broader direct-call variadic keyword behavior, or normalized diagnostics beyond executable evidence. Provider removal and the package-level zero-dependency cutover remain gated on native graduation rather than deleting the exact development oracles early. Independently, `BENCH-007` still requires a dedicated non-emulated Linux x64 runner with labels `[self-hosted, linux, x64, zigcss-benchmark-v1]`; its absence must not pause eligible native frontend work.

- Milestone: Milestone 10 self-contained native stylesheet frontends, in parallel with Milestone 8 controlled benchmark evidence
- Work package: `NSASS-011` native Sass evaluation
- State: `IN_PROGRESS`
- Active phase: the ninety-third native Sass semantic checkpoint, `NSASS-011-META-MATH-ATAN-FUNCTION-INVOCATION`, is `VERIFIED` at local implementation checkpoint `c51b2d85a0e82698c1317e46008886b482771c5c`. Provider-free native Zig now invokes the exact already-admitted module-owned `sass:math` `atan` function reference through `meta.call()` for SCSS and indented Sass. Default, exact custom, and unprefixed module access retain module ownership without a legacy-global warning. The global CSS-compatible `atan()` calculation name remains directly callable but is not misclassified as a legacy Sass callable: without an unprefixed module import, `meta.function-exists("atan")` is false and `meta.get-function("atan")` fails, matching Dart Sass 1.101.0. Canonical positional and named `$number` binding, list/map splats, finite arctangent results in degrees, and exact once-only source order agree with the oracle. Missing, extra, unknown, duplicate, string, list, and unitful arguments fail closed; the prior deliberate nonfinite safe-narrowing remains unchanged, while an explicit `math.atan2` reference keeps every remaining unmeasured callable family outside this slice. Fresh oracle probes reproduce exact compressed SCSS `.values{half:45deg;named:0deg;list-splat:45deg;map-splat:-45deg;ordered:45deg;trace:12;star:-45deg}`, indented Sass `.sass{half:45deg;named:-45deg}`, false global reflection, the failed global reference lookup, no warnings, and all selected invalid bindings. The test-first focused Debug gate stopped only at the measured old dispatch, global-reflection, and allocation paths with 319/322 tests. Final direct evaluator coverage passes 189/189; focused native Debug/ReleaseSafe each pass 23/23 steps with 322/322 tests; full compiler Debug/ReleaseSafe each pass 60/60 steps with 817/817 tests; and direct evaluator no-emission cross-compiles pass for Windows x64 GNU and Linux ARM64 GNU in both modes. Native contract 10/10 plus check adds only `built-in-meta-math-atan-function-invocation` while retaining private native Sass rows, production unreachability, zero target package/optional/runtime dependencies, and the closed release interlock. Complete provider/reference/product matrices pass 151/151, preprocessor package policy passes 6/6 plus check, autonomous-runner selftest passes 58/58, and repository Zig formatting, JavaScript/JSON syntax, and diff checks pass. The temporary isolated macOS 15.4 SDK selector required by pinned Zig 0.15.2 was removed after validation; no tracked or system state changed. Production routing, public capability claims, native graduation, version, release interlock, tag, artifact/package publication, deployment, and every external system remain unchanged.
- Quality closure: `QUAL-001` is verified at `3eced86`; repository-wide Zig formatting debt is zero and the final local validation matrix is green
- Next eligible package: measure and select the smallest next unsupported dependency-ordered `NSASS-011` Sass semantic slice. Do not infer three or more legacy `if()` splats, nonterminal dual-splat recovery, modern CSS conditional evaluation, plain-CSS function references, remaining meta/math/selector/string/color/calculation/special-function reference invocation including the explicit `math.atan2` boundary, mixin application, module enumeration, dynamic loading, user-module reflection, imports/forwards, direct callable CSS serialization, broader direct-call variadic keyword behavior, or normalized diagnostics beyond executable evidence. Provider removal and the package-level zero-dependency cutover remain gated on native graduation rather than deleting the exact development oracles early. Independently, `BENCH-007` still requires a dedicated non-emulated Linux x64 runner with labels `[self-hosted, linux, x64, zigcss-benchmark-v1]`; its absence must not pause eligible native frontend work.

- Milestone: Milestone 10 self-contained native stylesheet frontends, in parallel with Milestone 8 controlled benchmark evidence
- Work package: `NSASS-011` native Sass evaluation
- State: `IN_PROGRESS`
- Active phase: the ninety-second native Sass semantic checkpoint, `NSASS-011-META-MATH-ASIN-FUNCTION-INVOCATION`, is `VERIFIED` at local implementation checkpoint `b7ff0501e0dcced2187cd1558e0bb9e119c0f135`. Provider-free native Zig now invokes the exact already-admitted module-owned `sass:math` `asin` function reference through `meta.call()` for SCSS and indented Sass. Default, exact custom, and unprefixed module access retain module ownership without a legacy-global warning. The global CSS-compatible `asin()` calculation name remains directly callable but is not misclassified as a legacy Sass callable: without an unprefixed module import, `meta.function-exists("asin")` is false and `meta.get-function("asin")` fails, matching Dart Sass 1.101.0. Canonical positional and named `$number` binding, list/map splats, finite-domain inverse-sine results in degrees, and exact once-only source order agree with the oracle. Missing, extra, unknown, duplicate, string, list, and unitful arguments fail closed; the prior deliberate nonfinite safe-narrowing remains unchanged, while an explicit `math.atan` reference keeps every remaining unmeasured callable family outside this slice. Fresh oracle probes reproduce exact compressed SCSS `.values{half:30deg;named:0deg;list-splat:30deg;map-splat:-90deg;ordered:90deg;trace:12;star:-30deg}`, indented Sass `.sass{half:30deg;named:-90deg}`, false global reflection, the failed global reference lookup, no warnings, and all selected invalid bindings. The test-first focused Debug gate stopped only at the measured old dispatch, global-reflection, and allocation paths with 317/320 tests. Final direct evaluator coverage passes 187/187; focused native Debug/ReleaseSafe each pass 23/23 steps with 320/320 tests; full compiler Debug/ReleaseSafe each pass 60/60 steps with 815/815 tests; and direct evaluator no-emission cross-compiles pass for Windows x64 GNU and Linux ARM64 GNU in both modes. Native contract 10/10 plus check adds only `built-in-meta-math-asin-function-invocation` while retaining private native Sass rows, production unreachability, zero target package/optional/runtime dependencies, and the closed release interlock. Complete provider/reference/product matrices pass 151/151, preprocessor package policy passes 6/6 plus check, autonomous-runner selftest passes 58/58, and repository Zig formatting, JavaScript/JSON syntax, and diff checks pass. The temporary isolated macOS 15.4 SDK selector required by pinned Zig 0.15.2 was removed after validation; no tracked or system state changed. Production routing, public capability claims, native graduation, version, release interlock, tag, artifact/package publication, deployment, and every external system remain unchanged.
- Quality closure: `QUAL-001` is verified at `3eced86`; repository-wide Zig formatting debt is zero and the final local validation matrix is green
- Next eligible package: measure and select the smallest next unsupported dependency-ordered `NSASS-011` Sass semantic slice. Do not infer three or more legacy `if()` splats, nonterminal dual-splat recovery, modern CSS conditional evaluation, plain-CSS function references, remaining meta/math/selector/string/color/calculation/special-function reference invocation including the explicit `math.atan` boundary, mixin application, module enumeration, dynamic loading, user-module reflection, imports/forwards, direct callable CSS serialization, broader direct-call variadic keyword behavior, or normalized diagnostics beyond executable evidence. Provider removal and the package-level zero-dependency cutover remain gated on native graduation rather than deleting the exact development oracles early. Independently, `BENCH-007` still requires a dedicated non-emulated Linux x64 runner with labels `[self-hosted, linux, x64, zigcss-benchmark-v1]`; its absence must not pause eligible native frontend work.

- Milestone: Milestone 10 self-contained native stylesheet frontends, in parallel with Milestone 8 controlled benchmark evidence
- Work package: `NSASS-011` native Sass evaluation
- State: `IN_PROGRESS`
- Active phase: the ninety-first native Sass semantic checkpoint, `NSASS-011-META-MATH-ACOS-FUNCTION-INVOCATION`, is `VERIFIED` at local implementation checkpoint `ef5ab8e1f03e1f31a652720af8397ec8cf324a94`. Provider-free native Zig now invokes the exact already-admitted module-owned `sass:math` `acos` function reference through `meta.call()` for SCSS and indented Sass. Default, exact custom, and unprefixed module access retain module ownership without a legacy-global warning. The global CSS-compatible `acos()` calculation name remains directly callable but is not misclassified as a legacy Sass callable: without an unprefixed module import, `meta.function-exists("acos")` is false and `meta.get-function("acos")` fails, matching Dart Sass 1.101.0. Canonical positional and named `$number` binding, list/map splats, finite-domain inverse-cosine results in degrees, and exact once-only source order agree with the oracle. Missing, extra, unknown, duplicate, string, list, and unitful arguments fail closed; the prior deliberate nonfinite safe-narrowing remains unchanged, while an explicit `math.asin` reference keeps every remaining unmeasured callable family outside this slice. Fresh oracle probes reproduce exact compressed SCSS `.values{half:60deg;named:90deg;list-splat:60deg;map-splat:180deg;ordered:0deg;trace:12;star:120deg}`, indented Sass `.sass{half:60deg;named:180deg}`, false global reflection, the failed global reference lookup, no warnings, and all selected invalid bindings. The test-first focused Debug gate stopped only at the measured old dispatch, global-reflection, and allocation paths with 315/318 tests. Final direct evaluator coverage passes 185/185; focused native Debug/ReleaseSafe each pass 23/23 steps with 318/318 tests; full compiler Debug/ReleaseSafe each pass 60/60 steps with 813/813 tests; and direct evaluator no-emission cross-compiles pass for Windows x64 GNU and Linux ARM64 GNU in both modes. Native contract 10/10 plus check adds only `built-in-meta-math-acos-function-invocation` while retaining private native Sass rows, production unreachability, zero target package/optional/runtime dependencies, and the closed release interlock. Complete provider/reference/product matrices pass 151/151, preprocessor package policy passes 6/6 plus check, autonomous-runner selftest passes 58/58, and repository Zig formatting, JavaScript/JSON syntax, and diff checks pass. The temporary isolated macOS 15.4 SDK selector required by pinned Zig 0.15.2 was removed after validation; no tracked or system state changed. Production routing, public capability claims, native graduation, version, release interlock, tag, artifact/package publication, deployment, and every external system remain unchanged.
- Quality closure: `QUAL-001` is verified at `3eced86`; repository-wide Zig formatting debt is zero and the final local validation matrix is green
- Next eligible package: measure and select the smallest next unsupported dependency-ordered `NSASS-011` Sass semantic slice. Do not infer three or more legacy `if()` splats, nonterminal dual-splat recovery, modern CSS conditional evaluation, plain-CSS function references, remaining meta/math/selector/string/color/calculation/special-function reference invocation including the explicit `math.asin` boundary, mixin application, module enumeration, dynamic loading, user-module reflection, imports/forwards, direct callable CSS serialization, broader direct-call variadic keyword behavior, or normalized diagnostics beyond executable evidence. Provider removal and the package-level zero-dependency cutover remain gated on native graduation rather than deleting the exact development oracles early. Independently, `BENCH-007` still requires a dedicated non-emulated Linux x64 runner with labels `[self-hosted, linux, x64, zigcss-benchmark-v1]`; its absence must not pause eligible native frontend work.

- Milestone: Milestone 10 self-contained native stylesheet frontends, in parallel with Milestone 8 controlled benchmark evidence
- Work package: `NSASS-011` native Sass evaluation
- State: `IN_PROGRESS`
- Active phase: the ninetieth native Sass semantic checkpoint, `NSASS-011-META-MATH-UNIT-FUNCTION-INVOCATION`, is `VERIFIED` at local implementation checkpoint `cdeec12b6b3f682c9abbe2500d39afc790f81dbd`. Provider-free native Zig now invokes the exact already-admitted `sass:math` `unit` function reference through `meta.call()` for SCSS and indented Sass. Default, exact custom, and unprefixed module access retain module ownership without a legacy-global warning, while the true legacy-global `unit` reference emits the existing normalized warning once per invocation. Canonical positional and named `$number` binding, list/map splats, quoted unitless/simple/product/quotient serialization, and exact once-only source order agree with Dart Sass 1.101.0. Missing, extra, unknown, duplicate, string, and list arguments fail closed, while an explicit `math.acos` reference keeps every remaining unmeasured callable family outside this slice. Fresh oracle probes reproduce exact compressed SCSS `.values{unitless:"";single:"px";list-splat:"px*s";map-splat:"px/s";ordered:"deg/s";trace:12;star:"em/ms"}`, indented Sass `.sass{unitless:"";named:"px/s"}`, legacy `.legacy{unitless:"";unitful:"foo"}` plus two warnings, and all selected invalid bindings. The test-first focused Debug gate stopped only at the old dispatch boundary with 313/316 tests. Final direct evaluator coverage passes 183/183; focused native Debug/ReleaseSafe each pass 23/23 steps with 316/316 tests; full compiler Debug/ReleaseSafe each pass 60/60 steps with 811/811 tests; and direct evaluator no-emission cross-compiles pass for Windows x64 GNU and Linux ARM64 GNU in both modes. Native contract 10/10 plus check adds only `built-in-meta-math-unit-function-invocation` while retaining private native Sass rows, production unreachability, zero target package/optional/runtime dependencies, and the closed release interlock. Complete provider/reference/product matrices pass 151/151, preprocessor package policy passes 6/6 plus check, autonomous-runner selftest passes 58/58, and repository Zig formatting, JavaScript/JSON syntax, and diff checks pass. The temporary isolated macOS 15.4 SDK selector required by pinned Zig 0.15.2 was removed after validation; no tracked or system state changed. Production routing, public capability claims, native graduation, version, release interlock, tag, artifact/package publication, deployment, and every external system remain unchanged.
- Quality closure: `QUAL-001` is verified at `3eced86`; repository-wide Zig formatting debt is zero and the final local validation matrix is green
- Next eligible package: measure and select the smallest next unsupported dependency-ordered `NSASS-011` Sass semantic slice. Do not infer three or more legacy `if()` splats, nonterminal dual-splat recovery, modern CSS conditional evaluation, plain-CSS function references, remaining meta/math/selector/string/color/calculation/special-function reference invocation including the explicit `math.acos` boundary, mixin application, module enumeration, dynamic loading, user-module reflection, imports/forwards, direct callable CSS serialization, broader direct-call variadic keyword behavior, or normalized diagnostics beyond executable evidence. Provider removal and the package-level zero-dependency cutover remain gated on native graduation rather than deleting the exact development oracles early. Independently, `BENCH-007` still requires a dedicated non-emulated Linux x64 runner with labels `[self-hosted, linux, x64, zigcss-benchmark-v1]`; its absence must not pause eligible native frontend work.

- Milestone: Milestone 10 self-contained native stylesheet frontends, in parallel with Milestone 8 controlled benchmark evidence
- Work package: `NSASS-011` native Sass evaluation
- State: `IN_PROGRESS`
- Active phase: the eighty-ninth native Sass semantic checkpoint, `NSASS-011-META-MATH-UNITLESS-FUNCTION-INVOCATION`, is `VERIFIED` at local implementation checkpoint `934ad1f5efced95621cc0163cdaf73aacc9536ab`. Provider-free native Zig now invokes the exact already-admitted `sass:math` `is-unitless` function reference through `meta.call()` for SCSS and indented Sass. Default, exact custom, and unprefixed module access retain module ownership without a legacy-global warning, while the true legacy `unitless` reference emits the existing normalized warning once per invocation. Canonical positional and named `$number` binding, list/map splats, dimensionless and unitful boolean results, and exact once-only source order agree with Dart Sass 1.101.0. Missing, extra, unknown, duplicate, string, and list arguments fail closed, while an explicit `math.unit` reference keeps every remaining unmeasured callable family outside this slice. Fresh oracle probes reproduce exact compressed SCSS `.values{unitless:true;unitful:false;list-splat:true;map-splat:false;ordered:true;trace:12;star:true}`, indented Sass `.sass{unitless:true;named:false}`, legacy `.legacy{unitless:true;unitful:false}` plus two warnings, and all selected invalid bindings. The test-first focused Debug gate stopped only at the old dispatch boundary with 312/314 tests. Final direct evaluator coverage passes 181/181; focused native Debug/ReleaseSafe each pass 23/23 steps with 314/314 tests; full compiler Debug/ReleaseSafe each pass 60/60 steps with 809/809 tests; and direct evaluator no-emission cross-compiles pass for Windows x64 GNU and Linux ARM64 GNU in both modes. Native contract 10/10 plus check adds only `built-in-meta-math-unitless-function-invocation` while retaining private native Sass rows, production unreachability, zero target package/optional/runtime dependencies, and the closed release interlock. Complete provider/reference/product matrices pass 151/151, preprocessor package policy passes 6/6 plus check, autonomous-runner selftest passes 58/58, and repository Zig formatting, JavaScript/JSON syntax, and diff checks pass. The temporary isolated macOS 15.4 SDK selector required by pinned Zig 0.15.2 was removed after validation; no tracked or system state changed. Production routing, public capability claims, native graduation, version, release interlock, tag, artifact/package publication, deployment, and every external system remain unchanged.
- Quality closure: `QUAL-001` is verified at `3eced86`; repository-wide Zig formatting debt is zero and the final local validation matrix is green
- Next eligible package: measure and select the smallest next unsupported dependency-ordered `NSASS-011` Sass semantic slice. Do not infer three or more legacy `if()` splats, nonterminal dual-splat recovery, modern CSS conditional evaluation, plain-CSS function references, remaining meta/math/selector/string/color/calculation/special-function reference invocation including the explicit `math.unit` boundary, mixin application, module enumeration, dynamic loading, user-module reflection, imports/forwards, direct callable CSS serialization, broader direct-call variadic keyword behavior, or normalized diagnostics beyond executable evidence. Provider removal and the package-level zero-dependency cutover remain gated on native graduation rather than deleting the exact development oracles early. Independently, `BENCH-007` still requires a dedicated non-emulated Linux x64 runner with labels `[self-hosted, linux, x64, zigcss-benchmark-v1]`; its absence must not pause eligible native frontend work.

- Milestone: Milestone 10 self-contained native stylesheet frontends, in parallel with Milestone 8 controlled benchmark evidence
- Work package: `NSASS-011` native Sass evaluation
- State: `IN_PROGRESS`
- Active phase: the eighty-eighth native Sass semantic checkpoint, `NSASS-011-META-MATH-COMPATIBILITY-FUNCTION-INVOCATION`, is `VERIFIED` at local implementation checkpoint `f6216b905538cae8ee9f572c37fd280f18a08a6c`. Provider-free native Zig now invokes the exact already-admitted `sass:math` `compatible` function reference through `meta.call()` for SCSS and indented Sass. Default, exact custom, and unprefixed module access retain module ownership without a legacy-global warning, while the true legacy `comparable` reference emits the existing normalized warning once per invocation. Canonical positional and named `$number1`/`$number2` binding, list/map splats, known/custom/compound unit compatibility, boolean results, and exact once-only source order agree with Dart Sass 1.101.0. Missing, extra, unknown, duplicate, string, and list arguments fail closed, while an explicit `math.is-unitless` reference keeps every remaining unmeasured callable family outside this slice. Fresh oracle probes reproduce exact compressed SCSS `.values{compatible:true;incompatible:false;list-splat:true;map-splat:false;compound:true;ordered:true;trace:123}`, indented Sass `.sass{compatible:true;named:false}`, legacy `.legacy{compatible:true;incompatible:false}` plus two warnings, and all selected invalid bindings. The test-first focused Debug gate stopped only at the old dispatch boundary with 310/312 tests. Final direct evaluator coverage passes 179/179; focused native Debug/ReleaseSafe each pass 23/23 steps with 312/312 tests; full compiler Debug/ReleaseSafe each pass 60/60 steps with 807/807 tests; and direct evaluator no-emission cross-compiles pass for Windows x64 GNU and Linux ARM64 GNU in both modes. Native contract 10/10 plus check adds only `built-in-meta-math-compatibility-function-invocation` while retaining private native Sass rows, production unreachability, zero target package/optional/runtime dependencies, and the closed release interlock. Complete provider/reference/product matrices pass 151/151, preprocessor package policy passes 6/6 plus check, autonomous-runner selftest passes 58/58, and repository Zig formatting, JavaScript/JSON syntax, and diff checks pass. The temporary isolated macOS 15.4 SDK selector required by pinned Zig 0.15.2 was removed after validation; no tracked or system state changed. Production routing, public capability claims, native graduation, version, release interlock, tag, artifact/package publication, deployment, and every external system remain unchanged.
- Quality closure: `QUAL-001` is verified at `3eced86`; repository-wide Zig formatting debt is zero and the final local validation matrix is green
- Next eligible package: measure and select the smallest next unsupported dependency-ordered `NSASS-011` Sass semantic slice. Do not infer three or more legacy `if()` splats, nonterminal dual-splat recovery, modern CSS conditional evaluation, plain-CSS function references, remaining meta/math/selector/string/color/calculation/special-function reference invocation including the explicit `math.is-unitless` boundary, mixin application, module enumeration, dynamic loading, user-module reflection, imports/forwards, direct callable CSS serialization, broader direct-call variadic keyword behavior, or normalized diagnostics beyond executable evidence. Provider removal and the package-level zero-dependency cutover remain gated on native graduation rather than deleting the exact development oracles early. Independently, `BENCH-007` still requires a dedicated non-emulated Linux x64 runner with labels `[self-hosted, linux, x64, zigcss-benchmark-v1]`; its absence must not pause eligible native frontend work.

- Milestone: Milestone 10 self-contained native stylesheet frontends, in parallel with Milestone 8 controlled benchmark evidence
- Work package: `NSASS-011` native Sass evaluation
- State: `IN_PROGRESS`
- Active phase: the eighty-seventh native Sass semantic checkpoint, `NSASS-011-META-MATH-UNARY-FUNCTION-INVOCATION`, is `VERIFIED` at local implementation checkpoint `798a3d8359b539c69abc4b396cf9c019fc9a6752`. Provider-free native Zig now invokes the exact already-admitted unary `sass:math` function-reference family through `meta.call()` for SCSS and indented Sass: `abs`, `ceil`, `floor`, `round`, and `percentage`. Default, exact custom, and unprefixed module access retain module ownership without a legacy-global warning, while true legacy-global references emit the existing normalized warning. Canonical positional and named `$number` binding, list/map splats, numeric units, percentage conversion, and exact once-only source order agree with Dart Sass 1.101.0. Missing, extra, unknown, duplicate, non-number, and unitful-percentage arguments fail closed, while an explicit `math.compatible` reference keeps every remaining unmeasured callable family outside this slice. Fresh oracle probes reproduce exact compressed SCSS `.values{abs:1.5px;ceil:2px;floor:-2px;round-list:-1px;percentage-map:12.5%;ordered:-2foo;trace:12}`, indented Sass `.sass{abs:1.5px;named:-2deg}`, legacy `.legacy{abs:1px;ceil:2px;floor:1px;round:2px;percentage:12.5%}` plus five warnings, and all selected invalid bindings. The test-first focused Debug gate stopped only at the old dispatch boundary with 307/310 tests. Final direct evaluator coverage passes 177/177; focused native Debug/ReleaseSafe each pass 23/23 steps with 310/310 tests; full compiler Debug/ReleaseSafe each pass 60/60 steps with 805/805 tests; and direct evaluator no-emission cross-compiles pass for Windows x64 GNU and Linux ARM64 GNU in both modes. Native contract 10/10 plus check adds only `built-in-meta-math-unary-function-invocation` while retaining private native Sass rows, production unreachability, zero target package/optional/runtime dependencies, and the closed release interlock. Complete provider/reference/product matrices pass 151/151, preprocessor package policy passes 6/6 plus check, autonomous-runner selftest passes 58/58, and repository Zig formatting, JavaScript/JSON syntax, and diff checks pass. The temporary isolated macOS 15.4 SDK selector required by pinned Zig 0.15.2 was removed after validation; no tracked or system state changed. Production routing, public capability claims, native graduation, version, release interlock, tag, artifact/package publication, deployment, and every external system remain unchanged.
- Quality closure: `QUAL-001` is verified at `3eced86`; repository-wide Zig formatting debt is zero and the final local validation matrix is green
- Next eligible package: measure and select the smallest next unsupported dependency-ordered `NSASS-011` Sass semantic slice. Do not infer three or more legacy `if()` splats, nonterminal dual-splat recovery, modern CSS conditional evaluation, plain-CSS function references, remaining meta/math/selector/string/color/calculation/special-function reference invocation including the explicit `math.compatible` boundary, mixin application, module enumeration, dynamic loading, user-module reflection, imports/forwards, direct callable CSS serialization, broader direct-call variadic keyword behavior, or normalized diagnostics beyond executable evidence. Provider removal and the package-level zero-dependency cutover remain gated on native graduation rather than deleting the exact development oracles early. Independently, `BENCH-007` still requires a dedicated non-emulated Linux x64 runner with labels `[self-hosted, linux, x64, zigcss-benchmark-v1]`; its absence must not pause eligible native frontend work.

- Milestone: Milestone 10 self-contained native stylesheet frontends, in parallel with Milestone 8 controlled benchmark evidence
- Work package: `NSASS-011` native Sass evaluation
- State: `IN_PROGRESS`
- Active phase: the eighty-sixth native Sass semantic checkpoint, `NSASS-011-META-EXISTENCE-FUNCTION-INVOCATION`, is `VERIFIED` at local implementation checkpoint `272dc9fa7c52e01bb4ca598175d4038df1ea323a`. Provider-free native Zig now invokes the exact `sass:meta` existence-query function-reference family through `meta.call()` for SCSS and indented Sass: `feature-exists`, `function-exists`, `mixin-exists`, `variable-exists`, and `global-variable-exists`. Default, exact custom, and unprefixed module access retain module ownership without a legacy-global warning; true legacy-global references emit the existing normalized warning, and `feature-exists` independently emits its own deprecation. Canonical `$feature`, `$name`, and optional `$module` binding, positional/named arguments, list/map splats, lexical call-site scope, underscore/hyphen normalization, loaded-module reflection, and exact once-only source order agree with Dart Sass 1.101.0. Missing, extra, unknown, non-string, or unloaded-module target arguments fail closed, while the explicit unavailable built-in regression keeps math and every other unmeasured callable family outside this slice. Fresh oracle probes reproduce exact compressed SCSS `.values{function:true;function-module:true;function-named:true;function-list:true;mixin:true;variable:true;global:true;scope:(local: true, local-global: false, outer: true, outer-global: true);feature:true;ordered:true;trace:12;star:true}`, indented Sass `.sass{function:true;module:true;mixin:true;variable:true;global:true}`, the five legacy-global warnings plus the independent feature warning, and the selected invalid bindings. The test-first focused Debug gate stopped only at the old dispatch boundary with 305/308 tests. Final direct evaluator coverage passes 175/175; focused native Debug/ReleaseSafe each pass 23/23 steps with 308/308 tests; full compiler Debug/ReleaseSafe each pass 60/60 steps with 803/803 tests; and direct evaluator no-emission cross-compiles pass for Windows x64 GNU and Linux ARM64 GNU in both modes. Native contract 10/10 plus check adds only `built-in-meta-existence-function-invocation` while retaining private native Sass rows, production unreachability, zero target package/optional/runtime dependencies, and the closed release interlock. Complete provider/reference/product matrices pass 151/151, preprocessor package policy passes 6/6 plus check, autonomous-runner selftest passes 58/58, and repository Zig formatting, JavaScript/JSON syntax, and diff checks pass. The temporary isolated macOS 15.4 SDK selector required by pinned Zig 0.15.2 was removed after validation; no tracked or system state changed. Production routing, public capability claims, native graduation, version, release interlock, tag, artifact/package publication, deployment, and every external system remain unchanged.
- Quality closure: `QUAL-001` is verified at `3eced86`; repository-wide Zig formatting debt is zero and the final local validation matrix is green
- Next eligible package: measure and select the smallest next unsupported dependency-ordered `NSASS-011` Sass semantic slice. Do not infer three or more legacy `if()` splats, nonterminal dual-splat recovery, modern CSS conditional evaluation, plain-CSS function references, other meta/math/selector/string/color/calculation/special-function reference invocation, mixin application, module enumeration, dynamic loading, user-module reflection, imports/forwards, direct callable CSS serialization, broader direct-call variadic keyword behavior, or normalized diagnostics beyond executable evidence. Provider removal and the package-level zero-dependency cutover remain gated on native graduation rather than deleting the exact development oracles early. Independently, `BENCH-007` still requires a dedicated non-emulated Linux x64 runner with labels `[self-hosted, linux, x64, zigcss-benchmark-v1]`; its absence must not pause eligible native frontend work.

- Milestone: Milestone 10 self-contained native stylesheet frontends, in parallel with Milestone 8 controlled benchmark evidence
- Work package: `NSASS-011` native Sass evaluation
- State: `IN_PROGRESS`
- Active phase: the eighty-fifth native Sass semantic checkpoint, `NSASS-011-META-CALCULATION-FUNCTION-INVOCATION`, is `VERIFIED` at local implementation checkpoint `8b103a797938e52fc583cec5d6c0875f9b3c6880`. Provider-free native Zig now invokes the exact module-owned `sass:meta` `calc-name` and `calc-args` function references through `meta.call()` for SCSS and indented Sass. Default, exact custom, and unprefixed module access retain module ownership and emit no legacy warning. Canonical positional and named `$calc` binding, list/map splats, retained calculation values, and exact once-only source order agree with Dart Sass 1.101.0. Missing, extra, unknown, duplicate, or non-calculation target arguments fail closed, and an explicit sibling regression keeps meta existence reference invocation unavailable. Every other meta/math/selector/string/color/calculation or special-function reference family, mixin application, plain-CSS references, and direct callable CSS serialization remain unavailable. Fresh oracle probes reproduce exact compressed SCSS `.values{name:"calc";named:"max";list-splat:"min";map-splat:"clamp";args:var(--x, 1px), max(2px, var(--y, 3px));ordered:"round";trace:12}`, indented Sass `.sass{name:"calc";args:1px, var(--x)}`, no warnings, and rejection of all four selected invalid bindings. The test-first focused Debug gate stopped only at the old dispatch boundary with 304/306 tests. Final direct evaluator coverage passes 173/173; focused native Debug/ReleaseSafe each pass 23/23 steps with 306/306 tests; full compiler Debug/ReleaseSafe each pass 60/60 steps with 801/801 tests; and direct evaluator no-emission cross-compiles pass for Windows x64 GNU and Linux ARM64 GNU in both modes. Native contract 10/10 plus check adds only `built-in-meta-calculation-function-invocation` while retaining private native Sass rows, production unreachability, zero target package/optional/runtime dependencies, and the closed release interlock. Complete provider/reference/product matrices pass 151/151, preprocessor package policy passes 6/6 plus check, autonomous-runner selftest passes 58/58, and repository Zig formatting, JavaScript/JSON syntax, and diff checks pass. The temporary isolated macOS 15.4 SDK selector required by pinned Zig 0.15.2 was removed after validation; no tracked or system state changed. Production routing, public capability claims, native graduation, version, release interlock, tag, artifact/package publication, deployment, and every external system remain unchanged.
- Quality closure: `QUAL-001` is verified at `3eced86`; repository-wide Zig formatting debt is zero and the final local validation matrix is green
- Next eligible package: measure and select the smallest next unsupported dependency-ordered `NSASS-011` Sass semantic slice. Do not infer three or more legacy `if()` splats, nonterminal dual-splat recovery, modern CSS conditional evaluation, plain-CSS function references, meta existence or other meta/math/selector/string/color/calculation/special-function reference invocation, mixin application, module enumeration, dynamic loading, user-module reflection, imports/forwards, direct callable CSS serialization, broader direct-call variadic keyword behavior, or normalized diagnostics beyond executable evidence. Provider removal and the package-level zero-dependency cutover remain gated on native graduation rather than deleting the exact development oracles early. Independently, `BENCH-007` still requires a dedicated non-emulated Linux x64 runner with labels `[self-hosted, linux, x64, zigcss-benchmark-v1]`; its absence must not pause eligible native frontend work.

- Milestone: Milestone 10 self-contained native stylesheet frontends, in parallel with Milestone 8 controlled benchmark evidence
- Work package: `NSASS-011` native Sass evaluation
- State: `IN_PROGRESS`
- Active phase: the eighty-fourth native Sass semantic checkpoint, `NSASS-011-META-CONTENT-ACCEPTANCE-FUNCTION-INVOCATION`, is `VERIFIED` at local implementation checkpoint `45593411e739f4837fbb4aecc8f63cd13e4f9f14`. Provider-free native Zig now invokes the exact module-owned `sass:meta` `accepts-content` function reference through `meta.call()` for SCSS and indented Sass. Default, exact custom, and unprefixed module access retain module ownership and emit no legacy warning. Canonical positional and named `$mixin` binding, list/map splats, and exact once-only source order agree with Dart Sass 1.101.0; user mixins without and with content return false and true, while built-in `load-css` and `apply` references return false and true. Missing, extra, unknown, or non-mixin target arguments fail closed, and an explicit sibling regression keeps meta calculation reference invocation unavailable. Every other meta/math/selector/string/color/calculation or special-function family, mixin application, plain-CSS references, and direct callable CSS serialization remain unavailable. Fresh oracle probes reproduce exact compressed SCSS `.values{plain:false;slot:true;named:true;list-splat:false;map-splat:true;load:false;apply:true;ordered:true;trace:12;star:false}`, indented Sass `.sass{accepts:true}`, no warnings, and rejection of all four selected invalid bindings. The test-first focused Debug gate stopped only at the old dispatch boundary with 303/305 tests. Final focused native Debug/ReleaseSafe each pass 23/23 steps with 305/305 tests; full compiler Debug/ReleaseSafe each pass 60/60 steps with 800/800 tests; and direct evaluator no-emission cross-compiles pass for Windows x64 GNU and Linux ARM64 GNU in both modes. Native contract 10/10 plus check adds only `built-in-meta-content-acceptance-function-invocation` while retaining private native Sass rows, production unreachability, zero target package/optional/runtime dependencies, and the closed release interlock. Complete provider/reference/product matrices pass 151/151, preprocessor package policy passes 6/6 plus check, autonomous-runner selftest passes 58/58, and repository Zig formatting, JavaScript/JSON syntax, and diff checks pass. The temporary isolated macOS 15.4 SDK selector required by pinned Zig 0.15.2 was removed after validation; no tracked or system state changed. Production routing, public capability claims, native graduation, version, release interlock, tag, artifact/package publication, deployment, and every external system remain unchanged.
- Quality closure: `QUAL-001` is verified at `3eced86`; repository-wide Zig formatting debt is zero and the final local validation matrix is green
- Next eligible package: measure and select the smallest next unsupported dependency-ordered `NSASS-011` Sass semantic slice. Do not infer three or more legacy `if()` splats, nonterminal dual-splat recovery, modern CSS conditional evaluation, plain-CSS function references, other meta/math/selector/string/color/calculation/special-function reference invocation, mixin application, module enumeration, dynamic loading, user-module reflection, imports/forwards, direct callable CSS serialization, broader direct-call variadic keyword behavior, or normalized diagnostics beyond executable evidence. Provider removal and the package-level zero-dependency cutover remain gated on native graduation rather than deleting the exact development oracles early. Independently, `BENCH-007` still requires a dedicated non-emulated Linux x64 runner with labels `[self-hosted, linux, x64, zigcss-benchmark-v1]`; its absence must not pause eligible native frontend work.

- Milestone: Milestone 10 self-contained native stylesheet frontends, in parallel with Milestone 8 controlled benchmark evidence
- Work package: `NSASS-011` native Sass evaluation
- State: `IN_PROGRESS`
- Active phase: the eighty-third native Sass semantic checkpoint, `NSASS-011-META-KEYWORDS-FUNCTION-INVOCATION`, is `VERIFIED` at local implementation checkpoint `ad4ac970dfd6a095ba3c7c6b18d3100f7eb74080`. Provider-free native Zig now invokes the exact `sass:meta` `keywords` function reference through `meta.call()` for SCSS and indented Sass. Default, exact custom, and unprefixed module access preserve module ownership; the callable-only legacy-global inventory admits `keywords` without widening direct bare-global built-in dispatch, and only the true legacy reference emits the normalized deprecation. Evaluated forwarding preserves positional and named `$args` binding, named `$function` binding, list/map/argument-list splats, rest forwarding, normalized underscore/hyphen keyword spelling, exact once-only source order, and the original argument-list keyword-consumption state while still deeply owning and bounding collection bytes. Global and module `meta.function-exists("keywords")` reflection now agrees with constructible references. Missing, extra, unknown, or non-argument-list target arguments, non-callables, mixins, every other meta/math/selector/string/color/calculation or special-function reference, the special `if()` reference, expanded limits, cancellation, and every allocation failure fail without partial CSS. Direct callable CSS serialization, plain-CSS references, broader built-in invocation, and mixin application remain unavailable. Fresh Dart Sass 1.101.0 probes reproduce exact compressed SCSS `.values{direct:(alpha: 1, start-at: 2);named:(tone: red);named-function:(via-named: 7);list-splat:(left: 3);map-splat:(right: 4);ordered:(final: 5);trace:123;star:(unprefixed: 6)}`, indented Sass `.sass{keywords:(tone: blue)}`, and legacy `.legacy{value:(tone: red)}` plus one deprecation. The focused test-first probe stopped at the former callable boundary with 0/1; the first dispatch replay then exposed origin and shared argument-list-state gaps at 11/13 before the bounded ownership fix. Final direct evaluator coverage passes 171/171; focused native Debug/ReleaseSafe each pass 23/23 steps with 304/304 tests; full compiler Debug/ReleaseSafe each pass 60/60 steps with 799/799 tests; and direct evaluator no-emission cross-compiles pass for Windows x64 GNU and Linux ARM64 GNU in both modes. Native contract 10/10 plus check adds only `built-in-meta-keywords-function-invocation` while retaining private native Sass rows, production unreachability, zero target package/optional/runtime dependencies, and the closed release interlock. Complete provider/reference/product matrices pass 151/151, preprocessor package policy passes 6/6 plus check, autonomous-runner selftest passes 58/58, and repository Zig formatting, JavaScript/JSON syntax, and diff checks pass. The temporary isolated macOS 15.4 SDK selector required by pinned Zig 0.15.2 was removed after validation; no tracked or system state changed. Production routing, public capability claims, native graduation, version, release interlock, tag, artifact/package publication, deployment, and every external system remain unchanged.
- Quality closure: `QUAL-001` is verified at `3eced86`; repository-wide Zig formatting debt is zero and the final local validation matrix is green
- Next eligible package: measure and select the smallest next unsupported dependency-ordered `NSASS-011` Sass semantic slice. Do not infer three or more legacy `if()` splats, nonterminal dual-splat recovery, modern CSS conditional evaluation, plain-CSS function references, other meta/math/selector/string/color/calculation/special-function reference invocation, mixin application, module enumeration, dynamic loading, user-module reflection, imports/forwards, direct callable CSS serialization, broader direct-call variadic keyword behavior, or normalized diagnostics beyond executable evidence. Provider removal and the package-level zero-dependency cutover remain gated on native graduation rather than deleting the exact development oracles early. Independently, `BENCH-007` still requires a dedicated non-emulated Linux x64 runner with labels `[self-hosted, linux, x64, zigcss-benchmark-v1]`; its absence must not pause eligible native frontend work.

- Milestone: Milestone 10 self-contained native stylesheet frontends, in parallel with Milestone 8 controlled benchmark evidence
- Work package: `NSASS-011` native Sass evaluation
- State: `IN_PROGRESS`
- Active phase: the eighty-second native Sass semantic checkpoint, `NSASS-011-META-INSPECTION-FUNCTION-INVOCATION`, is `VERIFIED` at local implementation checkpoint `85d89299ec761c54d9e69c4c8d0e1e5107bc1e56`. Provider-free native Zig invokes the exact `sass:meta` inspection references, `inspect` and `type-of`, through `meta.call()` with module/global ownership, canonical binding and splats, exact source order, strict negative and allocation-failure closure, and fresh Dart Sass 1.101.0 agreement. Direct evaluator coverage passes 169/169; focused native Debug/ReleaseSafe each pass 23/23 with 302/302 tests; full compiler Debug/ReleaseSafe each pass 60/60 with 797/797 tests; cross-compiles, contract 10/10, provider/product 151/151, package 6/6, runner 58/58, formatting, syntax, and diff gates pass. Both native Sass rows remain private and production-unreachable, other callable families remain closed, the temporary SDK selector was removed, and no external state changed.
- Quality closure: `QUAL-001` is verified at `3eced86`; repository-wide Zig formatting debt is zero and the final local validation matrix is green
- Next eligible package: measure and select the smallest next unsupported dependency-ordered `NSASS-011` Sass semantic slice. Do not infer unmeasured callable families or broader Sass behavior. Independently, the pending external `BENCH-007` runner remains nonblocking for eligible native frontend correctness work.

- Milestone: Milestone 10 self-contained native stylesheet frontends, in parallel with Milestone 8 controlled benchmark evidence
- Work package: `NSASS-011` native Sass evaluation
- State: `IN_PROGRESS`
- Active phase: the eighty-first native Sass semantic checkpoint, `NSASS-011-META-MAP-MUTATION-FUNCTION-INVOCATION`, is `VERIFIED` at local implementation checkpoint `3b559d6defb98f206ecab459a3a87ae09ba9c306`. Provider-free native Zig invokes all five immutable `sass:map` mutation references through `meta.call()` with module/global ownership, canonical binding/splats/variadics/source order, strict negative and allocation-failure closure, and fresh Dart Sass 1.101.0 agreement. Direct evaluator coverage passes 167/167; focused native Debug/ReleaseSafe each pass 23/23 with 300/300 tests; full compiler Debug/ReleaseSafe each pass 60/60 with 795/795 tests; cross-compiles, contract 10/10, provider/product 151/151, package 6/6, runner 58/58, formatting, syntax, and diff gates pass. Both native Sass rows remain private and production-unreachable, every other unsupported callable family remains unavailable, the temporary SDK selector was removed, and no external state changed.
- Quality closure: `QUAL-001` is verified at `3eced86`; repository-wide Zig formatting debt is zero and the final local validation matrix is green
- Next eligible package: measure and select the smallest next unsupported dependency-ordered `NSASS-011` Sass semantic slice. Do not infer meta/math/selector/string/color/calculation or special-function invocation, mixin application, plain-CSS references, module enumeration, dynamic loading, user-module reflection, imports/forwards, or direct callable CSS serialization. The unrelated external `BENCH-007` runner remains non-blocking.

- Milestone: Milestone 10 self-contained native stylesheet frontends, in parallel with Milestone 8 controlled benchmark evidence
- Work package: `NSASS-011` native Sass evaluation
- State: `IN_PROGRESS`
- Active phase: the eightieth native Sass semantic checkpoint, `NSASS-011-META-MAP-QUERY-FUNCTION-INVOCATION`, is `VERIFIED` at local implementation checkpoint `9822b9cb5811cae8a0c533e0ec52da529d495bf4`. Provider-free native Zig invokes all four read-only `sass:map` references through `meta.call()` with module/global ownership, canonical binding/splats/variadics/source order, strict negative and allocation-failure closure, and fresh Dart Sass 1.101.0 agreement. Direct evaluator coverage passes 165/165; focused native Debug/ReleaseSafe each pass 23/23 with 298/298 tests; full compiler Debug/ReleaseSafe each pass 60/60 with 793/793 tests; cross-compiles, contract 10/10, provider/product 151/151, package 6/6, runner 58/58, formatting, syntax, and diff gates pass. Both native Sass rows remain private and production-unreachable, every map mutation and other unsupported callable family remains unavailable, the temporary SDK selector was removed, and no external state changed.
- Quality closure: `QUAL-001` is verified at `3eced86`; repository-wide Zig formatting debt is zero and the final local validation matrix is green
- Next eligible package: measure and select the smallest next unsupported dependency-ordered `NSASS-011` Sass semantic slice. Do not infer map mutation or math/meta/selector/string/color/calculation or special-function invocation, mixin application, plain-CSS references, module enumeration, dynamic loading, user-module reflection, imports/forwards, or direct callable CSS serialization. The unrelated external `BENCH-007` runner remains non-blocking.

- Milestone: Milestone 10 self-contained native stylesheet frontends, in parallel with Milestone 8 controlled benchmark evidence
- Work package: `NSASS-011` native Sass evaluation
- State: `IN_PROGRESS`
- Active phase: the seventy-ninth native Sass semantic checkpoint, `NSASS-011-META-LIST-FUNCTION-INVOCATION`, is `VERIFIED` at local implementation checkpoint `198aa1bb809820bd260b56fd18ddd7e825e79ac7`. Provider-free native Zig invokes all ten admitted `sass:list` references through `meta.call()` with module/global ownership, canonical binding/defaults/splats/variadics/source order, strict negative and allocation-failure closure, and fresh Dart Sass 1.101.0 agreement. Direct evaluator coverage passes 163/163; focused native Debug/ReleaseSafe each pass 23/23 with 296/296 tests; full compiler Debug/ReleaseSafe each pass 60/60 with 791/791 tests; cross-compiles, contract 10/10, provider/product 151/151, package 6/6, runner 58/58, formatting, syntax, and diff gates pass. Both native Sass rows remain private and production-unreachable, every other built-in family and mixin application remain unavailable, the temporary SDK selector was removed, and no external state changed.
- Quality closure: `QUAL-001` is verified at `3eced86`; repository-wide Zig formatting debt is zero and the final local validation matrix is green
- Next eligible package: measure and select the smallest next unsupported dependency-ordered `NSASS-011` Sass semantic slice. Do not infer map/math/meta/selector/string/color/calculation or special-function invocation, mixin application, plain-CSS references, module enumeration, dynamic loading, user-module reflection, imports/forwards, or direct callable CSS serialization. The unrelated external `BENCH-007` runner remains non-blocking.

- Milestone: Milestone 10 self-contained native stylesheet frontends, in parallel with Milestone 8 controlled benchmark evidence
- Work package: `NSASS-011` native Sass evaluation
- State: `IN_PROGRESS`
- Active phase: the seventy-eighth native Sass semantic checkpoint, `NSASS-011-META-USER-FUNCTION-INVOCATION`, is `VERIFIED` at local implementation checkpoint `da8c09f44c25f52d8838fb58e62f50679aea763e`. Provider-free native Zig invokes declaration-stable user-function references through `meta.call()` across default, custom, unprefixed, indented-Sass, and legacy-global surfaces with defaults, positional/named binding, list/map/argument-list splats, rest forwarding, keyword-rest consumption, nested calls, and exact once-only source order. Non-callables, mixins, built-in and special-function references, invalid target binding, expanded limits, cancellation, and every allocation failure fail closed. Fresh Dart Sass 1.101.0 probes match the executable output and warning contract. Direct evaluator coverage passes 161/161; focused native Debug/ReleaseSafe each pass 23/23 steps with 294/294 tests; full compiler Debug/ReleaseSafe each pass 60/60 steps with 789/789 tests; cross-compiles, contract 10/10, provider/product 151/151, package 6/6, runner 58/58, formatting, syntax, and diff gates pass. The temporary SDK selector was removed and no production or external state changed.
- Quality closure: `QUAL-001` is verified at `3eced86`; repository-wide Zig formatting debt is zero and the final local validation matrix is green
- Next eligible package: measure and select the smallest next unsupported dependency-ordered `NSASS-011` Sass semantic slice. Do not infer built-in or special-function invocation, mixin application, plain-CSS references, module enumeration, dynamic loading, user-module reflection, imports/forwards, or direct callable CSS serialization. The unrelated external `BENCH-007` runner remains non-blocking.

- Milestone: Milestone 10 self-contained native stylesheet frontends, in parallel with Milestone 8 controlled benchmark evidence
- Work package: `NSASS-011` native Sass evaluation
- State: `IN_PROGRESS`
- Active phase: the seventy-seventh native Sass semantic checkpoint, `NSASS-011-META-CALLABLE-INSPECTION`, is `VERIFIED` at local implementation checkpoint `4850e7efe53491d8fac7099092ba2accfb6aadcc`. Provider-free native Zig renders every already-constructible function and mixin reference through `meta.inspect()` with canonical namespace-independent names, normalized aliases, validated built-in ownership, and nested collection punctuation. Direct callable serialization and invocation remained fail-closed. Fresh Dart Sass 1.101.0 probes cover all four callable kinds and every module naming family. The targeted test-first gate reproduced the former 0/1 unsupported boundary. Direct evaluator coverage passes 158/158; focused native Debug/ReleaseSafe each pass 23/23 steps with 291/291 tests; full compiler Debug/ReleaseSafe each pass 60/60 steps with 786/786 tests; cross-compiles, contract 10/10, provider/product 151/151, package 6/6, runner 58/58, formatting, syntax, and diff gates pass. The temporary SDK selector was removed and no production or external state changed.
- Quality closure: `QUAL-001` is verified at `3eced86`; repository-wide Zig formatting debt is zero and the final local validation matrix is green
- Next eligible package: measure and select the smallest next unsupported dependency-ordered `NSASS-011` Sass semantic slice. Do not infer three or more legacy `if()` splats, nonterminal dual-splat recovery, modern CSS conditional evaluation, plain-CSS function references, callable invocation, mixin application, module enumeration, dynamic loading, user-module reflection, imports/forwards, or broader normalized diagnostic behavior beyond executable evidence. Provider removal and the package-level zero-dependency cutover remain gated on native graduation. The unrelated external `BENCH-007` runner remains non-blocking.

- Milestone: Milestone 10 self-contained native stylesheet frontends, in parallel with Milestone 8 controlled benchmark evidence
- Work package: `NSASS-011` native Sass evaluation
- State: `IN_PROGRESS`
- Active phase: the seventy-sixth native Sass semantic checkpoint, `NSASS-011-META-FUNCTION-REFERENCES`, is `VERIFIED` at local implementation checkpoint `d41e1ab83cc2d9dc540e0aaeed2b217443f1afca`. Provider-free native Zig now creates first-class Sass function references with `meta.get-function()` through default, exact custom, and unprefixed `sass:meta` namespaces in SCSS and indented Sass, plus the canonical legacy global. Declaration-order-visible user functions retain identity across stored references and underscore/hyphen or escaped spellings; later redefinitions and different declarations compare unequal and case remains distinct. Canonical `$name`, `$css`, and `$module` binding evaluates source arguments exactly once in source order. False or null `$css` selects Sass lookup, explicit null retains global lookup, loaded module aliases preserve identity, unprefixed module members equal their namespaced reference, and a legacy global remains distinct from the corresponding module member. The special legacy `if()` reference and the exact native-admitted built-in inventory are typed as functions, while module ownership is encoded into stable callable identity. The global call retains the existing normalized deprecation warning. Missing or later declarations, case and ownership mismatches, unavailable namespaces or members, malformed bindings, truthy `$css` plain-CSS references, callable invocation or inspection, direct callable CSS serialization, temporary exhaustion, and every allocation failure fail closed without partial CSS. Fresh Dart Sass 1.101.0 probes reproduce exact compressed SCSS `.values{type:function;same:true;marked:true;redefined:false;different:false;alias:function;star:function;global:function;global-alias:true;module:function;module-alias:true;star-module:true;global-distinct:false;own:function;own-alias:true;conditional:function;conditional-same:true;reflected:true;evaluations:3;trace:123}` and indented Sass `.sass{user:function;same:true;module:function;global-distinct:false}`. Test-first focused Debug stopped at the absent module member with 287/290 tests and three expected failures. Final direct evaluator coverage passes 157/157; focused native Debug/ReleaseSafe each pass 23/23 steps with 290/290 tests; full compiler Debug/ReleaseSafe each pass 60/60 steps with 785/785 tests; and direct evaluator no-emission cross-compiles pass for Windows x64 GNU and Linux ARM64 GNU in both modes. Native contract 10/10 plus check adds only `built-in-meta-sass-function-references` while retaining private native Sass rows, production unreachability, zero target package/optional/runtime dependencies, and the closed release interlock. Complete provider/reference/product matrices pass 151/151, preprocessor package policy passes 6/6 plus check, autonomous-runner selftest passes 58/58, and repository Zig formatting, JavaScript/JSON syntax, and diff checks pass. Host linking reused the existing isolated macOS 15.4 SDK selector required by pinned Zig 0.15.2; no tracked or system state changed. Production routing, public capability claims, native graduation, version, release interlock, tag, artifact/package publication, deployment, and every external system remain unchanged.
- Quality closure: `QUAL-001` is verified at `3eced86`; repository-wide Zig formatting debt is zero and the final local validation matrix is green
- Next eligible package: measure and select the smallest next unsupported dependency-ordered `NSASS-011` Sass semantic slice. Do not infer three or more legacy `if()` splats, nonterminal dual-splat recovery, modern CSS conditional evaluation, plain-CSS function references, callable invocation or inspection, mixin application, module enumeration, dynamic loading, user-module reflection, imports/forwards, or broader normalized diagnostic behavior beyond executable evidence. Provider removal and the package-level zero-dependency cutover remain gated on native graduation rather than deleting the exact development oracles early. Independently, `BENCH-007` still requires a dedicated non-emulated Linux x64 runner with labels `[self-hosted, linux, x64, zigcss-benchmark-v1]`; its absence must not pause eligible native frontend work.

- Milestone: Milestone 10 self-contained native stylesheet frontends, in parallel with Milestone 8 controlled benchmark evidence
- Work package: `NSASS-011` native Sass evaluation
- State: `IN_PROGRESS`
- Active phase: the seventy-fifth native Sass semantic checkpoint, `NSASS-011-LEGACY-IF-DUAL-SPLAT`, is `VERIFIED` at local implementation checkpoint `c23672df4b232031e39afc46f824cc6c6943f144`. Provider-free native Zig now expands exactly one adjacent terminal positional-rest plus keyword-rest pair for legacy `if()` in SCSS and indented Sass. Fresh Dart Sass 1.101.0 probes establish and the native evaluator matches left-to-right evaluation of both rest expressions before every direct argument, ordinary scalar/list/map/argument-list expansion for the first rest, an actual string-keyed map requirement for the second rest, and direct-then-first-map-then-second-map keyword precedence. Exact map-key spelling is retained; positional/name conflicts, missing bindings, non-map or non-string-key keyword rest values, expanded limits, and every allocation failure fail without partial CSS. Valid pairs emit only the existing throttled `if-function` warning and no `misplaced-rest` warning. Nonterminal pairs and three or more splats remain explicitly unsupported. Test-first focused Debug stopped at the former multiple-splat boundary with 284/287 tests and three expected failures; final focused native Debug/ReleaseSafe each pass 23/23 steps with 287/287 tests, full compiler Debug/ReleaseSafe each pass 60/60 steps with 782/782 tests, and direct evaluator no-emission cross-compiles pass for Windows x64 GNU and Linux ARM64 GNU in both modes. Native contract 10/10 plus check adds only `legacy-if-dual-splat-expansion` while retaining private native Sass rows, production unreachability, zero target package/optional/runtime dependencies, and the closed release interlock. Complete provider/reference/product matrices pass 151/151, preprocessor package policy passes 6/6 plus check, autonomous-runner selftest passes 58/58, and repository Zig formatting, JavaScript/JSON syntax, and diff checks pass. Host linking reused the existing isolated macOS 15.4 SDK selector required by pinned Zig 0.15.2; no tracked or system state changed. Production routing, public capability claims, native graduation, version, release interlock, tag, artifact/package publication, deployment, and every external system remain unchanged.
- Quality closure: `QUAL-001` is verified at `3eced86`; repository-wide Zig formatting debt is zero and the final local validation matrix is green
- Next eligible package: measure and select the smallest next unsupported dependency-ordered `NSASS-011` Sass semantic slice. Do not infer three or more legacy `if()` splats, nonterminal dual-splat recovery, modern CSS conditional evaluation, first-class function creation, callable invocation or inspection, mixin application, module enumeration, dynamic loading, user-module reflection, imports/forwards, or broader normalized diagnostic behavior beyond executable evidence. Provider removal and the package-level zero-dependency cutover remain gated on native graduation rather than deleting the exact development oracles early. Independently, `BENCH-007` still requires a dedicated non-emulated Linux x64 runner with labels `[self-hosted, linux, x64, zigcss-benchmark-v1]`; its absence must not pause eligible native frontend work.

- Milestone: Milestone 10 self-contained native stylesheet frontends, in parallel with Milestone 8 controlled benchmark evidence
- Work package: `NSASS-011` native Sass evaluation
- State: `IN_PROGRESS`
- Active phase: the seventy-fourth native Sass semantic checkpoint, `NSASS-011-LEGACY-IF-MISPLACED-REST`, is `VERIFIED` at local implementation checkpoint `a1d57820be76f5ed2f3ac05e5303e7247b3b1e6a`. Provider-free native Zig now preserves exactly one non-final legacy `if()` rest argument in SCSS and indented Sass. Fresh Dart Sass 1.101.0 probes establish and the native evaluator matches rest-first evaluation, source-ordered eager evaluation of every direct argument, direct-argument binding before expanded positional/keyword rest values, scalar/list/map expansion, and distinct positional-versus-named `misplaced-rest` warnings before the existing `if-function` warning. Both deprecation types independently retain five warnings and contribute to one combined omitted-warning summary; provider newlines are normalized through the existing single-line diagnostic contract. Exact two-terminal-splat calls remain explicitly unsupported. Test-first focused Debug stopped at the former unsupported boundary with 284/286 tests; final focused native Debug/ReleaseSafe each pass 23/23 steps with 286/286 tests, full compiler Debug/ReleaseSafe each pass 60/60 steps with 781/781 tests, and direct evaluator no-emission cross-compiles pass for Windows x64 GNU and Linux ARM64 GNU in both modes. Native contract 10/10 plus check adds only `legacy-if-single-misplaced-rest-expansion` while retaining private native Sass rows, production unreachability, zero target package/optional/runtime dependencies, and the closed release interlock. Complete provider/reference/product matrices pass 151/151, preprocessor package policy passes 6/6 plus check, autonomous-runner selftest passes 58/58, and repository Zig formatting, JavaScript/JSON syntax, and diff checks pass. The temporary macOS 15.4 SDK selector was removed after the pinned Zig 0.15.2 gates passed; no source or system state changed. Production routing, public capability claims, native graduation, version, release interlock, tag, artifact/package publication, deployment, and every external system remain unchanged.
- Quality closure: `QUAL-001` is verified at `3eced86`; repository-wide Zig formatting debt is zero and the final local validation matrix is green
- Next eligible package: measure and implement the smallest exact `NSASS-011-LEGACY-IF-DUAL-SPLAT` slice for Dart Sass 1.101.0's terminal positional-rest plus keyword-rest pair, including evaluation order, map requirements, binding precedence, limits, diagnostics, and no-partial-result behavior. Do not infer three or more splats, malformed-parser recovery, modern CSS conditional evaluation, first-class function creation, callable invocation or inspection, mixin application, module enumeration, dynamic loading, user-module reflection, imports/forwards, or broader normalized diagnostic behavior beyond executable evidence. Provider removal and the package-level zero-dependency cutover remain gated on native graduation. Independently, `BENCH-007` still requires a dedicated non-emulated Linux x64 runner with labels `[self-hosted, linux, x64, zigcss-benchmark-v1]`; its absence must not pause eligible native frontend work.

- Milestone: Milestone 10 self-contained native stylesheet frontends, in parallel with Milestone 8 controlled benchmark evidence
- Work package: `NSASS-011` native Sass evaluation
- State: `IN_PROGRESS`
- Active phase: the seventy-third native Sass semantic checkpoint, `NSASS-011-LEGACY-IF-FINAL-SPLAT`, is `VERIFIED` at local implementation checkpoint `dad88d5df9e34e1f0978bc9c97ad689b6ff788ef`. Provider-free native Zig now expands exactly one final argument-list splat for legacy `if()` in SCSS and indented Sass. Exact Dart Sass 1.101.0 probes establish and the native evaluator preserves the special eager contract: the final splat expression evaluates before preceding direct arguments, and the presence of a splat makes every argument eager while the existing splat-free call remains lazy. Comma and space lists, scalar values, exact-key maps, and positional/keyword rest argument lists expand through the shared bounded call machinery; splat-map keywords use exact spelling and override matching direct keywords in canonical order. A collection-first arithmetic probe prevents a parenthesized list's first callable item from being executed speculatively and then repeated. Non-final or multiple splats remain explicitly unsupported rather than silently omitting Dart Sass's separate misplaced-rest warning contract. Missing/extra bindings, non-string map keys, underscore map-key mismatches, eager undefined variables, expanded-argument exhaustion, every allocation failure, and existing cancellation/transaction limits fail without partial CSS. Test-first focused evaluation reproduced the former unsupported boundary at 149/152 tests; final focused native Debug/ReleaseSafe each pass 23/23 steps with 285/285 tests, full compiler Debug/ReleaseSafe each pass 60/60 steps with 780/780 tests, and the evaluator cross-compiles without emission for Windows x64 GNU and Linux ARM64 GNU in both modes. Native contract 10/10 plus check adds only `legacy-if-final-splat-expansion` while retaining the recursive Zig/`std`/`builtin` production closure, private native Sass rows, zero target package/optional/runtime dependencies, production unreachability, and the closed release interlock. Complete provider/reference/product matrices pass 151/151, preprocessor package policy passes 6/6 plus check, the autonomous-runner selftest passes 58/58, and repository Zig formatting, JavaScript/JSON syntax, and diff checks pass. Pinned Zig 0.15.2 host builds used the already-installed macOS 15.4 SDK through a temporary isolated selector because the host macOS 26.5 SDK exceeds the pinned toolchain's supported link metadata; the selector was removed and no source or system state changed. The next `NSASS-011` semantic checkpoint has not been selected. Production routing, public capability claims, native graduation, version, release interlock, tag, artifact/package publication, deployment, and every external system remain unchanged.
- Quality closure: `QUAL-001` is verified at `3eced86`; repository-wide Zig formatting debt is zero and the final local validation matrix is green
- Next eligible package: measure and select the smallest next unsupported dependency-ordered `NSASS-011` Sass semantic slice. Do not infer modern CSS conditional evaluation beyond the preserved measured syntax, non-final or multiple legacy `if()` splats and their misplaced-rest warning behavior, first-class function creation, callable invocation or inspection, mixin application, module enumeration, dynamic loading, user-module reflection, imports/forwards, or broader normalized diagnostic behavior beyond executable evidence. Provider removal and the package-level zero-dependency cutover remain gated on native graduation rather than deleting the exact development oracles early. Independently, `BENCH-007` still requires a dedicated non-emulated Linux x64 runner with labels `[self-hosted, linux, x64, zigcss-benchmark-v1]`; its absence must not pause eligible native frontend work.
- Product-site side package: `SITE-001-TERMINAL-FUTURE-LANDING` is `VERIFIED`. It does not change the native Sass dependency order or claim a controlled benchmark result. GitHub Pages deployed the exact initial checkpoint. Three broader Build attempts then failed closed in sequence: first on stale exact-count expectations for the deliberately repeated release-version copy, second on the professional README having removed the exact native-migration contract heading, and third on two contract-bound Zig toolchain/package phrases replaced by equivalent prose. The strengthened three-placement homepage contract, restored two-placement README version contract, restored `Native dependency-free migration` section, and restored exact Zig package wording pass locally and are closed by exact-main Build `29906041968`, comprehensive Test Suite `88877622623`, and five green archive/offline-install jobs; the inherited `NSASS-011-META-CALC-INTROSPECTION` implementation remains preserved outside this package.
- Site CI repair evidence: checkpoint `513f83b0ca786a05d82811888a76a2428d9a15df` reached Build run `29904754581`; Test Suite job `88873414411` passed workflow, release-version, release-metadata, and npm-publication preflight, then failed closed because the rewritten README no longer contained the contract-bound `Native dependency-free migration` heading. The second repair restores that public section without changing the dependency boundary. Native-contract test/check pass 10/10 plus the closed release gate, release-version test/check pass 7/7 plus 28 synchronized surfaces, and `git diff --check` passes. A local documentation replay without an explicit `NVIM` executable correctly stopped at its harness prerequisite; Documentation run `29904509914` already passed the exact content and deployed the initial site checkpoint.
- Site CI toolchain-contract evidence: checkpoint `f3bb620074e9131e7bf15625de92448879d527ba` reached Build run `29905060485`. All five native archive/offline-install jobs passed: macOS ARM64 `88874406622`, Windows x64 `88874406631`, Linux x64 `88874406645`, Linux ARM64 `88874406696`, and macOS x64 `88874406714`. Test Suite `88874406655` passed the full native frontend and compiler graphs, dependency/audit, VS Code, Neovim 0.11.7/0.12.4, and documentation gates before `Verify Zig package metadata` failed closed because the professional README had replaced the exact `Use Zig 0.15.2:` and stable-package-identity wording with equivalent prose. The third repair restores both public contract phrases; the complete Zig package suite passes 7/7 locally. A repository-wide scan confirms the README is otherwise bound only by the already-green release-version, native-migration, capability-status, and package-inventory validators.

- Site hosted closure evidence: exact-main checkpoint `7b7cbcd16c3b1904fca442949560ee3d0b22428c` passed Build run `29906041968`. Comprehensive Test Suite `88877622623` passed every workflow/version/release/native/package/dependency/audit, full Zig, VS Code, Neovim, documentation, provider/product, format, consumer/integration, independent CSS/transform, and benchmark smoke/statistics/archive/publication-policy gate. All five native archive/offline-install jobs passed: macOS x64 `88877622637`, macOS ARM64 `88877622698`, Linux ARM64 `88877622753`, Linux x64 `88877622770`, and Windows x64 `88877622811`. The live Pages origin serves the exact title `ZigCSS — Five languages. One compiler contract.` and 1200×630 social metadata. This package changed no compiler runtime, native graduation state, benchmark result, package, release, or tag.

## Release continuation ledger

| Package | State | Evidence / decision | Commit |
|---|---|---|---|
| `NSASS-011-LOCAL-USE-MIXIN-ENUMERATION-FOUNDATION` | `VERIFIED` | Dart Sass 1.101.0 probes establish one-hop local mixin enumeration through `meta.module-mixins()` across public/private mixins, final redefinitions, canonical first-definition order, underscore/hyphen normalization, contentful/contentless identity, default/custom namespaces, canonical aliases, direct/reflected calls, both syntaxes, zero warnings, and exact unknown namespaces. Native Zig resolves only retained local targets, emits public root mixins in canonical first-definition order, binds each key to the final module-owned callable identity, and deep-owns the map in the caller without cross-arena callable cloning or execution. Returned references retain their exact content-acceptance and application behavior. Exact unknown-namespace/no-partial-CSS, argument/collection limits, temporary/operation bounds, and exhaustive allocation regressions close the boundary. The test-first focused Debug gate stopped at 594/597; the first replay reached 596/597 and exposed only a one-mixin collection fixture that correctly fit its one-item limit, so the fixture was strengthened without weakening the gate. Final focused modes pass 23/23 and 597/597; full compiler modes pass 60/60 and 1092/1092; Windows x64 GNU and Linux ARM64 GNU root cross-compiles pass in both modes. Native contract 12/12 plus check adds only `local-use-mixin-enumeration-foundation`; provider/reference/product tests pass 153/153, package policy passes 6/6 plus check, runner selftest passes 59/59, and repository formatting, syntax, JSON, and diff gates pass. Local variable enumeration is designated next while callable-valued cross-arena data beyond the mixin map, broader loading/resolution, production reachability, graduation, release state, and external systems remain unchanged. | `8ae034c` |
| `NSASS-011-LOCAL-USE-FUNCTION-ENUMERATION-FOUNDATION` | `VERIFIED` | Dart Sass 1.101.0 probes establish one-hop local function enumeration through `meta.module-functions()` across public/private functions, final redefinitions, canonical first-definition order, underscore/hyphen normalization, default/custom namespaces, canonical aliases, direct/reflected calls, both syntaxes, zero warnings, and exact unknown namespaces. Native Zig resolves only retained local targets, emits public root functions in canonical first-definition order, binds each key to the final module-owned callable identity, and deep-owns the map in the caller without cross-arena callable cloning or execution. Exact unknown-namespace/no-partial-CSS, argument/collection limits, temporary/operation bounds, and exhaustive allocation regressions close the boundary. The test-first focused Debug gate stopped at 592/595; the first replay reached 594/595 and exposed only a one-function collection fixture that correctly fit its one-item limit, so the fixture was strengthened without weakening the gate. Final focused modes pass 23/23 and 595/595; full compiler modes pass 60/60 and 1090/1090; Windows x64 GNU and Linux ARM64 GNU root cross-compiles pass in both modes. Native contract 12/12 plus check adds only `local-use-function-enumeration-foundation`; provider/reference/product tests pass 153/153, package policy passes 6/6 plus check, runner selftest passes 59/59, and repository formatting, syntax, JSON, and diff gates pass. Local mixin enumeration is designated next while variable reflection, callable-valued cross-arena data beyond the function map, broader loading/resolution, production reachability, graduation, release state, and external systems remain unchanged. | `42899a3` |
| `NSASS-011-LOCAL-USE-MIXIN-CONTENT-INSPECTION-FOUNDATION` | `VERIFIED` | Dart Sass 1.101.0 probes establish one-hop local mixin content inspection through `meta.accepts-content()` across public contentful/contentless mixins, final redefinitions, default/custom/unprefixed namespaces, canonical aliases, direct/reflected calls, both syntaxes, zero warnings, and exact invalid lookups. Native Zig validates the packed canonical module/final mixin identity and reads its immutable content-acceptance fact without cross-arena cloning or execution. Exact ambiguity/no-partial-CSS, argument-limit, and exhaustive direct-allocation regressions close the boundary. The test-first focused Debug gate stopped at 591/593; the first replay reached 592/593 and isolated the separate reflected built-in-reference injected-allocation reclassification, so allocation coverage was narrowed to the new direct branch while reflected semantics remain positively covered. Final focused modes pass 23/23 and 593/593; full compiler modes pass 60/60 and 1088/1088; Windows x64 GNU and Linux ARM64 GNU root cross-compiles pass in both modes. Native contract 12/12 plus check adds only `local-use-mixin-content-inspection-foundation`; provider/reference/product tests pass 153/153, package policy passes 6/6 plus check, runner selftest passes 59/59, and repository formatting, syntax, JSON, and diff gates pass. Local function enumeration is designated next while mixin/variable enumeration, cross-arena callable data beyond this predicate, broader loading/resolution, production reachability, graduation, release state, and external systems remain unchanged. | `9a63d80` |
| `NSASS-011-LOCAL-USE-MIXIN-REFERENCE-APPLICATION-FOUNDATION` | `VERIFIED` | Dart Sass 1.101.0 probes establish one-hop local mixin-reference application through `meta.apply()` across default/custom/unprefixed namespaces, canonical aliases, redefinitions, positional/named/list/map splats, map override, live state/defaults, caller-owned parameterized content, both syntaxes, and exact invalid applications. Native Zig decodes only local mixin references, clones once-evaluated non-callable arguments into the retained module evaluator, invokes the shared module-owned mixin path, and preserves the caller content document/scope. Callable arguments, invalid binding, expanded limits, and allocation failure remain fail-closed with no partial CSS. The test-first gate stopped at 587/591; the first replay exposed only existing rest serialization and generic binder wording, so the fixture was narrowed to fixed arity and the shared binder was contextualized without broadening either boundary. Focused Debug/ReleaseSafe pass 591/591, full Debug/ReleaseSafe pass 1086/1086, four cross-target no-emission builds pass, and native contract 12/12 plus check, provider/reference/product 153/153, package policy 6/6 plus check, runner selftest 59/59, formatting, JavaScript/JSON syntax, and diff gates pass. The private contract adds only `local-use-mixin-reference-application-foundation`; content inspection is next, while enumeration, cross-arena callable data, broader loading/resolution, production reachability, graduation, release state, and external systems remain unchanged. | `e0763bd` |
| `NSASS-011-LOCAL-USE-FUNCTION-REFERENCE-INVOCATION-FOUNDATION` | `VERIFIED` | Dart Sass 1.101.0 probes establish one-hop local function-reference invocation through `meta.call()` across default/custom/unprefixed namespaces, canonical aliases, final redefinitions, positional/named/list/map/mixed splats, later-map override, module state and lexical defaults, owned aggregate returns, SCSS/indented Sass, exact CSS, zero warnings, and exact invalid-call errors. Native Zig decodes only local function references and shares the bounded direct-call cross-arena clone/invoke/clone-back path, preserving live module identity while retaining recursive callable guards. Exact diagnostic/no-partial-CSS, argument-limit, separation, and exhaustive allocation regressions close the boundary. The test-first focused Debug gate stopped at 584/588; two over-broad fixtures were narrowed at the unchanged transitive-module guard rather than widening module loading. Final focused modes pass 23/23 and 588/588; full compiler modes pass 60/60 and 1083/1083; Windows x64 GNU and Linux ARM64 GNU root cross-compiles pass in both modes. Native contract 12/12 plus check adds only `local-use-function-reference-invocation-foundation`; provider/reference/product tests pass 153/153, package policy passes 6/6 plus check, runner selftest passes 59/59, and repository formatting, syntax, JSON, and diff gates pass. Local mixin-reference application is designated next while content inspection, enumeration, callable-valued cross-arena data, transitive/configured loading, imports/forwards, dynamic loading, private native reachability, native graduation, production dependencies, public claims, release state, and external systems remain unchanged. | `ad4780e` |
| `NSASS-011-LOCAL-USE-CALLABLE-REFERENCE-FOUNDATION` | `VERIFIED` | Dart Sass 1.101.0 probes establish public/private/missing/redefined one-hop function and mixin reference construction, default/custom/unprefixed namespaces, canonical alias identity, underscore normalization, type/inspection/equality, SCSS/indented-Sass behavior, and exact unknown/competing-module errors. Native Zig packs stable canonical module and final-definition identity into distinct local function/mixin callable kinds, exposes only public top-level references, and accumulates all unprefixed candidates so repeated aliases deduplicate while local/local and built-in/local collisions fail without CSS. Invocation/application, content inspection, enumeration, callable-valued cross-arena data, and broader reflection remain explicitly unavailable. Exact diagnostic/no-partial-CSS, argument-limit, separation, alias, collision, and exhaustive allocation regressions close the boundary. The initial test-first focused Debug gate stopped at 581/585, and the sibling collision regression stopped at 584/585. Final focused modes pass 23/23 and 585/585; full compiler modes pass 60/60 and 1080/1080; Windows x64 GNU and Linux ARM64 GNU root cross-compiles pass in both modes. Native contract 12/12 plus check adds only `local-use-callable-reference-foundation`; provider/reference/product tests pass 153/153, package policy passes 6/6 plus check, runner selftest passes 59/59, and repository formatting, syntax, JSON, and diff gates pass. Local function-reference invocation is designated next while mixin application, enumeration, callable-valued cross-arena data, transitive/configured loading, imports/forwards, dynamic loading, private native reachability, native graduation, production dependencies, public claims, release state, and external systems remain unchanged. | `ad01fe5` |
| `NSASS-011-LOCAL-USE-CALLABLE-EXISTENCE-FOUNDATION` | `VERIFIED` | Dart Sass 1.101.0 probes establish public/private/missing/redefined one-hop function and mixin existence, default/custom/unprefixed namespaces, underscore normalization, case sensitivity, nullable lookup, reflected predicates, SCSS/indented-Sass behavior, and exact unknown/ambiguous errors. Native Zig resolves only existence predicates against retained local module engines, exposes public top-level presence, treats aliases of one canonical module as one identity, and rejects competing star modules without CSS. Default/custom namespaces pass in both syntaxes and SCSS unprefixed lookup passes; the existing indented-Sass `as *` parser boundary remains fail-closed. First-class references, invocation/application, enumeration, variable reflection, and broader user-module reflection remain explicitly unavailable. Exact diagnostic/no-partial-CSS, argument-limit, separation, and exhaustive allocation regressions close the boundary. The test-first focused Debug gate stopped at 578/582. Final focused modes pass 23/23 and 582/582; full compiler modes pass 60/60 and 1077/1077; Windows x64 GNU and Linux ARM64 GNU root cross-compiles pass in both modes. Native contract 12/12 plus check adds only `local-use-callable-existence-foundation`; provider/reference/product tests pass 153/153, package policy passes 6/6 plus check, runner selftest passes 59/59, and repository formatting, syntax, JSON, and diff gates pass. First-class local callable reference construction is designated next while reference invocation/application, enumeration, callable-valued cross-arena data, transitive/configured loading, imports/forwards, dynamic loading, private native reachability, native graduation, production dependencies, public claims, release state, and external systems remain unchanged. | `bf1ab47` |
| `NSASS-011-LOCAL-USE-CALLABLE-MEMBER-FOUNDATION` | `VERIFIED` | Dart Sass 1.101.0 probes establish public/private one-hop module functions and mixins, default/custom/unprefixed namespaces, lexical module variables/defaults, positional/named/content binding, canonical shared state, last-definition replacement, first-load order, arithmetic/nested calls, SCSS/indented-Sass interaction, and closed invalid/private/ambiguous/limit boundaries. Native Zig retains each canonical module document and evaluator, gives top-level callables stable root ownership, clones non-callable arguments/results across arenas, returns `@content` execution to the caller document/scope, exposes live module state, and resolves only public qualified/unprefixed callables. Exact diagnostic/no-partial-CSS and exhaustive allocation regressions close the cross-engine lifetime boundary. The test-first focused Debug gate stopped at 575/577. Final focused modes pass 23/23 and 579/579; full compiler modes pass 60/60 and 1074/1074; Windows x64 GNU and Linux ARM64 GNU root cross-compiles pass in both modes. Native contract 12/12 plus check adds only `local-use-callable-member-foundation`; provider/reference/product tests pass 153/153, package policy passes 6/6 plus check, runner selftest passes 59/59, and repository formatting, syntax, JSON, and diff gates pass. Local callable existence queries are designated next while first-class references/application, enumeration, callable-valued cross-arena data, transitive/configured loading, imports/forwards, dynamic loading, private native reachability, native graduation, production dependencies, public claims, release state, and external systems remain unchanged. | `3905f7c` |
| `NSASS-011-LOCAL-USE-SINGLE-MODULE-LOADING-FOUNDATION` | `VERIFIED` | Dart Sass 1.101.0 probes establish sibling SCSS/Sass explicit and partial URLs, exact default/custom/unprefixed namespaces, first-load CSS order, canonical once-only loading, public/private variables, cross-syntax loading, dependencies, namespace/placement errors, self-cycles, missing files, and ambiguity. Native Zig performs bounded sibling candidate search through the resolver session, parses and evaluates the selected source in an isolated environment, clones public non-callable values into a canonical-URL cache, and preserves exact source/dependency/map identities. Every unsupported, security, resource, diagnostic, and allocation boundary fails without partial CSS. The test-first focused Debug gate stopped at 571/574. Final focused modes pass 23/23 and 575/575; full compiler modes pass 60/60 and 1070/1070; Windows x64 GNU and Linux ARM64 GNU root cross-compiles pass in both modes. Native contract 12/12 plus check adds only `local-use-single-module-loading-foundation`; provider/reference/product tests pass 153/153, package policy passes 6/6 plus check, runner selftest passes 59/59, and repository formatting, syntax, JSON, and diff gates pass. One-hop callable members are designated next while transitive/configured loading, imports/forwards, dynamic loading, reflection, private native reachability, native graduation, production dependencies, public claims, release state, and external systems remain unchanged. | `8613575` |
| `NSASS-011-STRING-UNIQUE-ID-DETERMINISTIC-COMPATIBILITY-DECISION` | `VERIFIED` | Dart Sass 1.101.0 returns an unquoted `u[0-9a-z]{6}` string from `sass:string.unique-id()` but advances randomized process-global state, so repeated calls/compilations, fresh processes, and concurrent compilations do not preserve deterministic bytes. Default/alias/unprefixed module access, direct/list-splat/empty-map-splat/reflected calls, SCSS/indented Sass, typing/interpolation, exact arity, and legacy-global warning ownership are executable provider evidence. Native Zig recognizes qualified, unprefixed, legacy-global, splatted, and reflected identities and rejects all of them with one exact dedicated nondeterminism diagnostic; legacy direct/reflected calls first emit the owned global-built-in warning, and no value or CSS escapes. The regression owns repeated compilations, exact identities/spans/order, failed completion, argument limits, and allocation failure. Test-first gates stopped at contract 6/12 and focused Debug 21/23 with 570/571. Final focused modes pass 23/23 and 571/571; full compiler modes pass 60/60 and 1066/1066; Windows x64 GNU and Linux ARM64 GNU root cross-compiles pass in both modes. Native contract 12/12 plus check adds only `built-in-string-unique-id-deterministic-rejection`; provider/reference/product tests pass 153/153, package policy passes 6/6 plus check, runner selftest passes 59/59, and repository formatting, syntax, JSON, and diff gates pass. Local module loading is designated next while private native reachability, native graduation, production dependencies, public claims, release state, and external systems remain unchanged. | `a8fce61` |
| `NSASS-011-COLOR-LEGACY-ALPHA-MODERN-COLOR-REJECTION-SEMANTICS` | `VERIFIED` | Dart Sass 1.101.0 rejects all 312 selected combinations of 13 modern-color spellings, four legacy-alpha functions, ordinary direct/direct-splat/reflected calls, and SCSS/indented Sass with one global-built-in warning followed by the function-specific legacy-colors-only error, no color-function warning, and no CSS. The shared native legacy-alpha color guard now preserves that exact ownership and order through the existing normalized global warning and provider error text without admitting any modern transform. The exhaustive regression owns every combination, exact identities/spans/position, failed completion, diagnostic-limit closure, and allocation failure. The test-first gate stopped at 21/23 and 569/570 on the missing warning. Final focused native modes pass 23/23 and 570/570; full compiler modes pass 60/60 and 1065/1065; Windows x64 GNU and Linux ARM64 GNU root cross-compiles pass in both modes. Native contract 12/12 plus check adds only `built-in-color-legacy-alpha-modern-color-rejection-semantics`; provider/reference/product tests pass 151/151, package policy passes 6/6 plus check, runner selftest passes 59/59, and repository formatting, syntax, JSON, and diff gates pass. Sibling evidence designates a deterministic `sass:string.unique-id()` compatibility decision next while modern transforms, private native reachability, native graduation, production dependencies, public claims, release state, and external systems remain unchanged. | `59fe32f` |
| `NSASS-011-COLOR-LEGACY-ALPHA-COMPOUND-UNIT-SEMANTICS` | `VERIFIED` | Fresh Dart Sass 1.101.0 probes establish completed numeric unit algebra across direct/direct-splat/reflected legacy-alpha calls, scalar retention after cancellation/conversion, fuzzy `1e-11` range equality, and final alpha clamping. Native ordinary direct calls now share the bounded legacy-alpha amount kernel, admit completed unit inventories without allocation or serialization, preserve finite/range rejection, and emit the exact two-warning pair on success. The test-first gate stopped at 21/23 and 568/569 on the former compound-unit rejection; stale negative inventories and aggregate warning ownership were corrected without weakening failure boundaries. Final focused native modes pass 23/23 and 569/569; full compiler modes pass 60/60 and 1064/1064; Windows x64 GNU and Linux ARM64 GNU root cross-compiles pass in both modes. Native contract 12/12 plus check adds only `built-in-color-legacy-alpha-compound-unit-semantics`; provider/reference/product tests pass 151/151, package policy passes 6/6 plus check, runner selftest passes 59/59, and repository formatting, JSON, and diff gates pass. Modern-color rejection remained fail-closed and was designated as the next distinct diagnostic slice while private native reachability, native graduation, production dependencies, public claims, release state, and external systems remained unchanged. | `027ddbf` |
| `NSASS-011-COLOR-LEGACY-ALPHA-MISSING-CHANNEL-SEMANTICS` | `VERIFIED` | Fresh Dart Sass 1.101.0 probes establish selected missing RGB/HSL/HWB channels and missing alpha across direct/reflected `opacify()`/`fade-in()`/`transparentize()`/`fade-out()`, SCSS/indented Sass, exact CSS, two-warning identity, and rejected comma syntax. Native whitespace/slash legacy constructors now retain missing masks until one shared legacy-alpha kernel replaces missing channels with zero, clears the mask, clamps alpha, and preserves prior non-missing conversion behavior across ordinary direct, direct-splat, and reflected paths. Other legacy/keyword transforms, IE hex, and legacy `color.is-powerless()` remain fail-closed; modern missing-query behavior is unchanged. The test-first gate stopped at 21/23 and 566/567 on the constructor boundary. Final focused native modes pass 23/23 and 568/568; full compiler modes pass 60/60 and 1063/1063; Windows x64 GNU and Linux ARM64 GNU root cross-compiles pass in both modes. Native contract 12/12 plus check adds only `built-in-color-legacy-alpha-missing-channel-semantics`; provider/reference/product tests pass 151/151, package policy passes 6/6 plus check, runner selftest passes 59/59, and repository formatting, syntax, JSON, and diff gates pass. Sibling evidence designates legacy-alpha compound-unit amounts next while private native reachability, native graduation, production dependencies, public claims, release state, and external systems remain unchanged. | `8845c60` |
| `NSASS-011-DIAGNOSTIC-DEPRECATION-SOURCE-DEDUPLICATION` | `VERIFIED` | Added one evaluator-transaction deprecation inventory keyed by exact diagnostic code, source, byte span, and message, with source-backed call-span refinement and a default 1,000/hard 65,536 unique-key limit. Repeated direct loop calls, repeatedly invoked user-function bodies, reflected static/dynamic calls, legacy globals, and legacy `if()` now emit once per exact source warning, while distinct locations and messages remain independent. Existing first-five legacy-`if()`/misplaced-rest throttling and the omitted-warning summary remain provider-compatible; exact diagnostic budgets count only emitted warnings. Fresh Dart Sass 1.101.0 SCSS/indented-Sass probes also corrected two inherited repeated-warning assumptions. The test-first filtered gate failed 0/1 with 29 warnings against 14 before the shared policy; the final regression owns 15 exact warnings plus zero/overflow/semantic-limit closure. Final focused native modes pass 23/23 and 566/566; full compiler modes pass 60/60 and 1061/1061; Windows x64 GNU and Linux ARM64 GNU preprocessor-root cross-compiles pass in both modes. Native contract 12/12 plus check adds only `diagnostic-deprecation-source-deduplication`; provider/reference/product tests pass 151/151, package policy passes 6/6 plus check, runner selftest passes 59/59, and repository formatting, syntax, JSON, and diff gates pass. Sibling probes designate legacy-alpha missing-channel semantics next while private native reachability, native graduation, production dependencies, public claims, release state, and external systems remain unchanged. | `27dfd2d` |
| `NSASS-011-COLOR-LEGACY-FADE-OUT-DIRECT-SPLAT-EXPANSION` | `VERIFIED` | Extended only direct legacy-global `fade-out()` calls containing a syntactic splat to use the already-verified evaluated expansion, canonical bounded binder, global callable ownership, and shared reflected legacy-alpha kernel. Fresh Dart Sass 1.101.0 probes establish positional-list, keyword-map, bracketed-list, and positional/keyword argument-list splats; later-map override; positional/map duplicate rejection; once-only source order; SCSS/indented-Sass output; legacy RGB/HSL/HWB, transparent, opaque, alpha-clamping, zero/full amounts, color typing, and two exact warning classes per successful source call. Empty/short/overfull splats, unknown or non-string map keys, duplicates, invalid values, deferred calculations, range failures, missing channels, modern colors, expanded argument ceilings, and allocation failures expose no CSS. Ordinary legacy and module-owned calls retain their former paths. The test-first Debug gate stopped at 21/23 and 563/565 on `SplatUnsupported` and the former unsupported negative classification. Final focused native modes pass 23/23 and 565/565; full compiler modes pass 60/60 and 1060/1060; Windows x64 GNU and Linux ARM64 GNU root cross-compiles pass in both modes. Native contract 12/12 plus check adds only `built-in-color-legacy-fade-out-direct-splat-expansion`; provider/reference/product tests pass 151/151, package policy passes 6/6 plus check, runner selftest passes 59/59, and repository formatting, syntax, JSON, and diff gates pass. Sibling probes designate source-location deprecation deduplication next while private native reachability, native graduation, production dependencies, public claims, release state, and external systems remain unchanged. | `4f35ca9` |
| `NSASS-011-COLOR-LEGACY-TRANSPARENTIZE-DIRECT-SPLAT-EXPANSION` | `VERIFIED` | Extended only direct legacy-global `transparentize()` calls containing a syntactic splat to use the already-verified evaluated expansion, canonical bounded binder, global callable ownership, and shared reflected legacy-alpha kernel. Fresh Dart Sass 1.101.0 probes establish positional-list, keyword-map, bracketed-list, and positional/keyword argument-list splats; later-map override; positional/map duplicate rejection; once-only source order; SCSS/indented-Sass output; legacy RGB/HSL/HWB, transparent, opaque, alpha-clamping, zero/full amounts, color typing, and two exact warning classes per successful source call. Empty/short/overfull splats, unknown or non-string map keys, duplicates, invalid values, deferred calculations, range failures, missing channels, modern colors, expanded argument ceilings, and allocation failures expose no CSS. Ordinary legacy calls, module-owned calls, and direct `fade-out()` retain their former paths. The test-first Debug gate stopped at 21/23 and 562/564 on `SplatUnsupported`. Final focused native modes pass 23/23 and 564/564; full compiler modes pass 60/60 and 1059/1059; Windows x64 GNU and Linux ARM64 GNU preprocessor-root cross-compiles pass in both modes. Native contract 12/12 plus check adds only `built-in-color-legacy-transparentize-direct-splat-expansion`; provider/reference/product tests pass 151/151, package policy passes 6/6 plus check, runner selftest passes 59/59, and repository formatting, syntax, JSON, and diff gates pass. Sibling probes designate legacy-global direct `fade-out()` splats next while private native reachability, native graduation, production dependencies, public claims, release state, and external systems remain unchanged. | `07eab66` |
| `NSASS-011-COLOR-LEGACY-FADE-IN-DIRECT-SPLAT-EXPANSION` | `VERIFIED` | Extended only direct legacy-global `fade-in()` calls containing a syntactic splat to use the already-verified evaluated expansion, canonical bounded binder, global callable ownership, and shared reflected legacy-alpha kernel. Fresh Dart Sass 1.101.0 probes establish positional-list, keyword-map, bracketed-list, and positional/keyword argument-list splats; later-map override; positional/map duplicate rejection; once-only source order; SCSS/indented-Sass output; legacy RGB/HSL/HWB, transparent, opaque, alpha-clamping, zero/full amounts, color typing, and two exact warning classes per successful source call. Empty/short/overfull splats, unknown or non-string map keys, duplicates, invalid values, deferred calculations, range failures, missing channels, modern colors, expanded argument ceilings, and allocation failures expose no CSS. Ordinary legacy calls, module-owned calls, and sibling alpha functions retain their former paths. The test-first Debug gate stopped at 21/23 and 561/563 on `SplatUnsupported`. Final focused native modes pass 23/23 and 563/563; full compiler modes pass 60/60 and 1058/1058; Windows x64 GNU and Linux ARM64 GNU evaluator cross-compiles pass in both modes. Native contract 12/12 plus check adds only `built-in-color-legacy-fade-in-direct-splat-expansion`; provider/reference/product tests pass 151/151, package policy passes 6/6 plus check, runner selftest passes 59/59, and repository formatting, syntax, JSON, and diff gates pass. Sibling probes designate legacy-global direct `transparentize()` splats next while private native reachability, native graduation, production dependencies, public claims, release state, and external systems remain unchanged. | `19027af` |
| `NSASS-011-COLOR-LEGACY-OPACIFY-DIRECT-SPLAT-EXPANSION` | `VERIFIED` | Extended only direct legacy-global `opacify()` calls containing a syntactic splat to use the already-verified evaluated expansion, canonical bounded binder, global callable ownership, and shared reflected legacy-alpha kernel. Fresh Dart Sass 1.101.0 probes establish positional-list, keyword-map, bracketed-list, and positional/keyword argument-list splats; later-map override; positional/map duplicate rejection; once-only source order; SCSS/indented-Sass output; legacy RGB/HSL/HWB, transparent, opaque, alpha-clamping, zero/full amounts, color typing, and two exact warning classes per successful source call. Empty/short/overfull splats, unknown or non-string map keys, duplicates, invalid values, deferred calculations, range failures, missing channels, modern colors, expanded argument ceilings, and allocation failures expose no CSS. Ordinary legacy calls, module-owned calls, and sibling alpha functions retain their former paths. The test-first Debug gate stopped at 21/23 and 560/562 on `SplatUnsupported`. Final focused native modes pass 23/23 and 562/562; full compiler modes pass 60/60 and 1057/1057; Windows x64 GNU and Linux ARM64 GNU evaluator cross-compiles pass in both modes. Native contract 12/12 plus check adds only `built-in-color-legacy-opacify-direct-splat-expansion`; provider/reference/product tests pass 151/151, package policy passes 6/6 plus check, runner selftest passes 59/59, and repository formatting, syntax, JSON, and diff gates pass. Sibling probes designate legacy-global direct `fade-in()` splats next while private native reachability, native graduation, production dependencies, public claims, release state, and external systems remain unchanged. | `573031f` |
| `NSASS-011-COLOR-LEGACY-INVERT-DIRECT-SPLAT-EXPANSION` | `VERIFIED` | Extended only direct legacy-global `invert()` calls containing a syntactic splat to use the already-verified evaluated expansion, canonical bounded binder, global callable ownership, and shared reflected invert kernel. Fresh Dart Sass 1.101.0 probes establish comma-list, keyword-map, scalar, bracketed-list, and positional/keyword argument-list splats; later-map override; positional/map duplicate rejection; once-only source order; SCSS/indented-Sass output; legacy RGB/HSL/HWB, transparent, and alpha behavior; color/filter typing; one global-built-in deprecation per successful color source call; and warning-free numeric, unit-bearing, deferred, and calculated CSS filters. Empty/overfull splats, unknown or non-string map keys, duplicates, invalid values, unitless/unit-bearing weights, optional spaces, missing-channel constructors, modern colors, expanded argument ceilings, and allocation failures expose no CSS. Ordinary legacy calls and module-owned direct calls retain their former paths. The test-first Debug gate stopped at 21/23 and 560/561 on `SplatUnsupported`. Final focused native modes pass 23/23 and 561/561; full compiler modes pass 60/60 and 1056/1056; Windows x64 GNU and Linux ARM64 GNU evaluator cross-compiles pass in both modes. Native contract 12/12 plus check adds only `built-in-color-legacy-invert-direct-splat-expansion`; provider/reference/product tests pass 151/151, package policy passes 6/6 plus check, runner selftest passes 59/59, and repository formatting, syntax, JSON, and diff gates pass. Sibling probes designate legacy-global direct `opacify()` splats next while private native reachability, native graduation, production dependencies, public claims, release state, and external systems remain unchanged. | `eedc79b` |
| `NSASS-011-COLOR-MODULE-INVERT-DIRECT-SPLAT-EXPANSION` | `VERIFIED` | Added loaded-module direct `sass:color.invert()` dispatch through the already-verified evaluated expansion, canonical bounded binder, module callable ownership, and shared reflected invert kernel. Fresh Dart Sass 1.101.0 probes establish default/alias/unprefixed access; ordinary/named calls; comma-list, keyword-map, scalar, bracketed-list, and positional/keyword argument-list splats; later-map override; positional/map duplicate rejection; once-only source order; SCSS/indented-Sass output; legacy RGB/HSL/HWB, transparent, and alpha behavior; color/filter typing; and one module compatibility warning per numeric filter source call. Empty/overfull splats, unknown or non-string map keys, duplicates, invalid/deferred values, unit coercions, optional spaces, missing-channel constructors, modern colors, expanded argument ceilings, and allocation failures expose no CSS. Legacy-global direct splats retain their former unsupported path. The test-first Debug gate stopped at 21/23 and 558/560 on the former undefined-module boundary. Final focused native modes pass 23/23 and 560/560; full compiler modes pass 60/60 and 1055/1055; Windows x64 GNU and Linux ARM64 GNU evaluator cross-compiles pass in both modes. Native contract 12/12 plus check adds only `built-in-color-module-invert-direct-splat-expansion`; provider/reference/product tests pass 151/151, package policy passes 6/6 plus check, runner selftest passes 59/59, and repository formatting, syntax, JSON, and diff gates pass. Sibling probes designate legacy-global direct `invert()` splats next while private native reachability, native graduation, production dependencies, public claims, release state, and external systems remain unchanged. | `f4625ce` |
| `NSASS-011-COLOR-LEGACY-GRAYSCALE-DIRECT-SPLAT-EXPANSION` | `VERIFIED` | Extended only direct legacy-global `grayscale()` calls containing a syntactic splat to use the already-verified evaluated expansion, canonical bounded binder, global callable ownership, and shared reflected grayscale kernel. Fresh Dart Sass 1.101.0 probes establish comma-list, keyword-map, scalar, bracketed-list, and positional/keyword argument-list splats; later-map override of an earlier named binding; positional/map duplicate rejection; once-only source order; SCSS/indented-Sass output; legacy RGB/HSL/HWB, transparent, and alpha behavior; color typing; warning-free numeric/unit/deferred CSS filters; and one global-built-in deprecation per color result. Empty/overfull splats, unknown or non-string map keys, duplicates, invalid values, direct missing-channel constructors, modern colors, expanded argument ceilings, and allocation failures expose no CSS. Ordinary legacy calls and module-owned direct calls retain their former paths. The test-first filtered Debug gate failed 0/1 at `SplatUnsupported`. Final focused native modes pass 23/23 and 559/559; full compiler modes pass 60/60 and 1054/1054; Windows x64 GNU and Linux ARM64 GNU evaluator cross-compiles pass in both modes. Native contract 12/12 plus check adds only `built-in-color-legacy-grayscale-direct-splat-expansion`; provider/reference/product tests pass 151/151, package policy passes 6/6 plus check, runner selftest passes 59/59, and repository formatting, syntax, JSON, and diff gates pass. Sibling probes designate direct `sass:color.invert()` next while private native reachability, native graduation, production dependencies, public claims, release state, and external systems remain unchanged. | `edb7e79` |
| `NSASS-011-COLOR-MODULE-GRAYSCALE-DIRECT-SPLAT-EXPANSION` | `VERIFIED` | Added loaded-module direct `sass:color.grayscale()` dispatch through the already-verified evaluated expansion, canonical bounded binder, module callable ownership, and shared reflected grayscale kernel. Fresh Dart Sass 1.101.0 probes establish default/alias/unprefixed access; ordinary/named calls; comma-list, keyword-map, scalar, bracketed-list, and positional/keyword argument-list splats; later-map override of an earlier named binding; positional/map duplicate rejection; once-only source order; SCSS/indented-Sass output; legacy RGB/HSL/HWB, transparent, and alpha behavior; color typing; and numeric CSS-filter preservation with one module deprecation per numeric source call. Empty/overfull splats, unknown or non-string map keys, duplicates, invalid or deferred values, direct missing-channel constructors, modern colors, expanded argument ceilings, and allocation failures expose no CSS. Legacy-global direct splats retain their former unsupported path. The test-first focused Debug gate stopped at 21/23 and 556/558 on the former undefined-module boundary. Final focused native modes pass 23/23 and 558/558; full compiler modes pass 60/60 and 1053/1053; Windows x64 GNU and Linux ARM64 GNU evaluator cross-compiles pass in both modes. Native contract 12/12 plus check adds only `built-in-color-module-grayscale-direct-splat-expansion`; provider/reference/product tests pass 151/151, package policy passes 6/6 plus check, runner selftest passes 59/59, and repository formatting, syntax, JSON, and diff gates pass. Sibling probes designate legacy-global direct `grayscale()` splats next while private native reachability, native graduation, production dependencies, public claims, release state, and external systems remain unchanged. | `b5d8278` |
| `NSASS-011-COLOR-LEGACY-COMPLEMENT-DIRECT-SPLAT-EXPANSION` | `VERIFIED` | Extended only direct legacy-global `complement()` calls containing a syntactic splat to use the already-verified evaluated expansion, canonical bounded binder, global callable ownership, and shared reflected complement kernel. Fresh Dart Sass 1.101.0 probes establish comma-list, keyword-map, scalar, bracketed-list, and positional/keyword argument-list splats; later-map override of an earlier named binding; positional/map duplicate rejection; once-only source order; SCSS/indented-Sass output; legacy RGB/HSL/HWB, transparent, and alpha behavior; color typing; and one global-built-in deprecation per successful source call. Empty/overfull splats, unknown or non-string map keys, duplicates, invalid values, direct missing-channel constructors, modern colors, optional interpolation spaces, expanded argument ceilings, and allocation failures expose no CSS. Ordinary legacy calls and module-owned direct calls retain their former paths. The test-first focused Debug gate stopped at 21/23 and 555/557 on the former `SplatUnsupported` boundary. Final focused native modes pass 23/23 and 557/557; full compiler modes pass 60/60 and 1052/1052; Windows x64 GNU and Linux ARM64 GNU evaluator cross-compiles pass in both modes. Native contract 12/12 plus check adds only `built-in-color-legacy-complement-direct-splat-expansion`; provider/reference/product tests pass 151/151, package policy passes 6/6 plus check, runner selftest passes 59/59, and repository formatting, syntax, JSON, and diff gates pass. Sibling probes designate direct `sass:color.grayscale()` next while legacy-global grayscale splats, private native reachability, native graduation, production dependencies, public claims, release state, and external systems remain unchanged. | `259f974` |
| `NSASS-011-COLOR-MODULE-COMPLEMENT-DIRECT-SPLAT-EXPANSION` | `VERIFIED` | Added loaded-module direct `sass:color.complement()` dispatch through the already-verified evaluated expansion, canonical bounded binder, module callable ownership, and shared reflected complement kernel. Fresh Dart Sass 1.101.0 probes establish default/alias/unprefixed access; ordinary/named calls; comma-list, keyword-map, scalar, bracketed-list, and positional/keyword argument-list splats; later-map override of an earlier named binding; positional/map duplicate rejection; once-only source order; SCSS/indented-Sass output; legacy RGB/HSL/HWB, transparent, and alpha behavior; color typing; and zero warnings. Empty/overfull splats, unknown or non-string map keys, duplicates, invalid values, direct missing-channel constructors, modern colors, expanded argument ceilings, and allocation failures expose no CSS. Legacy-global direct splats retain their former unsupported path, and optional interpolation spaces remain a separate unsupported boundary. The test-first focused Debug gate stopped at 554/556 on the former undefined-module boundary. Final focused native modes pass 23/23 and 556/556; full compiler modes pass 60/60 and 1051/1051; Windows x64 GNU and Linux ARM64 GNU evaluator cross-compiles pass in both modes. Native contract 12/12 plus check adds only `built-in-color-module-complement-direct-splat-expansion`; provider/reference/product tests pass 151/151, package policy passes 6/6 plus check, runner selftest passes 59/59, and repository formatting, syntax, JSON, and diff gates pass. Private native reachability, native graduation, production dependencies, public claims, release state, and external systems are unchanged. | `deba79a` |
| `NSASS-011-COLOR-LEGACY-ADJUST-HUE-DIRECT-SPLAT-EXPANSION` | `VERIFIED` | Extended only direct legacy-global `adjust-hue()` calls containing a syntactic splat to use the already-verified evaluated expansion, canonical bounded binder, global callable ownership, and shared hue-adjustment kernel. Fresh Dart Sass 1.101.0 probes establish positional-list and keyword-map splats, direct-plus-map binding, later-map override, once-only source order, SCSS/indented-Sass output, legacy RGB/HSL/HWB and alpha behavior, admitted angle units, negative/wrapped angles, color typing, and the provider warning boundary. Empty/short/overfull list splats, unknown or non-string map keys, positional/map duplicates, deferred/compound angles, expanded argument ceilings, modern colors, and allocation failures expose no CSS; existing missing-channel, compound-unit, and normalized-diagnostic boundaries remain deliberately narrower and fail closed, ordinary global calls retain their former dispatch, and direct module calls including splats remain unavailable. The test-first focused Debug gate stopped at 553/555 on the former `SplatUnsupported` boundary. Final focused native modes pass 23/23 and 555/555; full compiler modes pass 60/60 and 1050/1050; Windows x64 GNU and Linux ARM64 GNU preprocessor-root cross-compiles pass in both modes. Native contract 12/12 plus check adds only `built-in-color-legacy-adjust-hue-direct-splat-expansion`; provider/reference/product tests pass 151/151, package policy passes 6/6 plus check, runner selftest passes 59/59, and repository formatting, syntax, JSON, and diff gates pass. Private native reachability, native graduation, production dependencies, public claims, release state, and external systems are unchanged. | `a866db4` |
| `NSASS-011-COLOR-LEGACY-DESATURATE-DIRECT-SPLAT-EXPANSION` | `VERIFIED` | Extended only direct legacy-global `desaturate()` calls containing a syntactic splat to use the already-verified evaluated expansion, canonical bounded binder, global callable ownership, and shared desaturation kernel. Fresh Dart Sass 1.101.0 probes establish exact positional-list and keyword-map splats, direct-plus-map binding, later-map override, once-only source order, SCSS/indented-Sass output, legacy RGB/HSL/HWB and alpha behavior, unitless/unitful amounts, color typing, and one global-built-in plus one color-function deprecation per successful call. Empty/short/overfull list splats, unknown or non-string map keys, positional/map duplicates, negative/out-of-range amounts, expanded argument ceilings, modern colors, deferred values, and allocation failures expose no CSS; the existing native missing-channel boundary remains deliberately narrower and fail-closed, ordinary global calls retain their former dispatch, and direct module calls including splats remain unavailable. The test-first focused Debug gate stopped at 552/554 on the former `SplatUnsupported` boundary. Final focused native modes pass 23/23 and 554/554; full compiler modes pass 60/60 and 1049/1049; Windows x64 GNU and Linux ARM64 GNU preprocessor-root cross-compiles pass in both modes. Native contract 12/12 plus check adds only `built-in-color-legacy-desaturate-direct-splat-expansion`; provider/reference/product tests pass 151/151, package policy passes 6/6 plus check, runner selftest passes 59/59, and repository formatting, syntax, JSON, and diff gates pass. Private native reachability, native graduation, production dependencies, public claims, release state, and external systems are unchanged. | `ada67d3` |
| `NSASS-011-COLOR-LEGACY-SATURATE-DIRECT-SPLAT-EXPANSION` | `VERIFIED` | Extended only direct legacy-global `saturate()` calls containing a syntactic splat to use the already-verified evaluated expansion, canonical bounded binder, global callable ownership, and shared saturate kernel. Fresh Dart Sass 1.101.0 probes establish exact one-argument CSS-filter positional-list and keyword-map splats without warnings, deferred filter preservation, two-argument color splats, direct-plus-map binding, later-map override, once-only source order, SCSS/indented-Sass output, legacy RGB/HSL/HWB and alpha behavior, unitless/unitful amounts, color typing, and one global-built-in plus one color-function deprecation per successful color call. Empty/short/overfull list splats, unknown or non-string map keys, positional/map duplicates, negative/out-of-range color amounts, expanded argument ceilings, modern colors, deferred color amounts, and allocation failures expose no CSS; the existing native missing-channel boundary remains fail-closed, ordinary global calls retain their former dispatch, and direct module calls including splats remain unavailable. The test-first focused Debug gate stopped at 551/553 on the former `SplatUnsupported` boundary. Final focused native modes pass 23/23 and 553/553; full compiler modes pass 60/60 and 1048/1048; Windows x64 GNU and Linux ARM64 GNU preprocessor-root cross-compiles pass in both modes. Native contract 12/12 plus check adds only `built-in-color-legacy-saturate-direct-splat-expansion`; provider/reference/product tests pass 151/151, package policy passes 6/6 plus check, runner selftest passes 59/59, and repository formatting, syntax, JSON, and diff gates pass. Private native reachability, native graduation, production dependencies, public claims, release state, and external systems are unchanged. | `1d4e0f1` |
| `NSASS-011-COLOR-LEGACY-DARKEN-DIRECT-SPLAT-EXPANSION` | `VERIFIED` | Extended only direct legacy-global `darken()` calls containing a syntactic splat to use the already-verified evaluated expansion, canonical bounded binder, global callable ownership, and shared legacy darken kernel. Fresh Dart Sass 1.101.0 probes establish exact positional-list and keyword-map splats, direct-plus-map binding, later-map override, once-only source order, SCSS/indented-Sass output, legacy RGB/HSL/HWB and alpha behavior, unitless/unitful amounts, color typing, and one global-built-in plus one color-function deprecation per successful call. Empty/short/overfull list splats, unknown or non-string map keys, positional/map duplicates, negative/out-of-range amounts, expanded argument ceilings, modern colors, deferred values, and allocation failures expose no CSS; the existing native missing-channel boundary remains fail-closed, ordinary global calls retain their former dispatch, and direct module calls including splats remain unavailable. The test-first focused Debug gate stopped at 550/552 on the former `SplatUnsupported` boundary. Final focused native modes pass 23/23 and 552/552; full compiler modes pass 60/60 and 1047/1047; Windows x64 GNU and Linux ARM64 GNU preprocessor-root cross-compiles pass in both modes. Native contract 12/12 plus check adds only `built-in-color-legacy-darken-direct-splat-expansion`; provider/reference/product tests pass 151/151, package policy passes 6/6 plus check, runner selftest passes 59/59, and repository formatting, syntax, JSON, and diff gates pass. Private native reachability, native graduation, production dependencies, public claims, release state, and external systems are unchanged. | `7a7808a` |
| `NSASS-011-COLOR-LEGACY-LIGHTEN-DIRECT-SPLAT-EXPANSION` | `VERIFIED` | Extended only direct legacy-global `lighten()` calls containing a syntactic splat to use the already-verified evaluated expansion, canonical bounded binder, global callable ownership, and shared legacy lighten kernel. Fresh Dart Sass 1.101.0 probes establish exact positional-list and keyword-map splats, direct-plus-map binding, later-map override, once-only source order, SCSS/indented-Sass output, legacy RGB/HSL/HWB and alpha behavior, unitless/unitful amounts, color typing, and one global-built-in plus one color-function deprecation per successful call. Empty/short/overfull list splats, unknown or non-string map keys, positional/map duplicates, negative/out-of-range amounts, expanded argument ceilings, modern and missing-channel colors, deferred values, and allocation failures expose no CSS; ordinary global calls retain their former dispatch, and direct module calls including splats remain unavailable. The test-first focused Debug gate stopped at 549/551 on the former `SplatUnsupported` boundary. Final focused native modes pass 23/23 and 551/551; full compiler modes pass 60/60 and 1046/1046; Windows x64 GNU and Linux ARM64 GNU evaluator cross-compiles pass in both modes. Native contract 12/12 plus check adds only `built-in-color-legacy-lighten-direct-splat-expansion`; provider/reference/product tests pass 151/151, package policy passes 6/6 plus check, runner selftest passes 59/59, and repository formatting, syntax, JSON, and diff gates pass. Private native reachability, native graduation, production dependencies, public claims, release state, and external systems are unchanged. | `511e47b` |
| `NSASS-011-COLOR-LEGACY-MIX-DIRECT-SPLAT-EXPANSION` | `VERIFIED` | Extended only direct legacy-global `mix()` calls containing a syntactic splat to use the already-verified evaluated expansion, canonical bounded binder, global callable ownership, and shared legacy mix kernel. Fresh Dart Sass 1.101.0 probes establish exact positional-list and keyword-map splats, direct-plus-map binding, later-map override, once-only source order, SCSS/indented-Sass output, legacy RGB/HSL/HWB and alpha behavior, color typing, and one global-built-in deprecation per successful call. Empty/short list splats, the excluded fourth modern-method argument, unknown or non-string map keys, positional/map duplicates, unitless weights, expanded argument ceilings, modern methods, wide-gamut colors, deferred values, and allocation failures expose no CSS; ordinary legacy and module-owned direct calls retain their former dispatch. The test-first focused Debug gate stopped at 548/550 on the former `SplatUnsupported` boundary. Final focused native modes pass 23/23 and 550/550; full compiler modes pass 60/60 and 1045/1045; Windows x64 GNU and Linux ARM64 GNU evaluator cross-compiles pass in both modes. Native contract 12/12 plus check adds only `built-in-color-legacy-mix-direct-splat-expansion`; provider/reference/product tests pass 151/151, package policy passes 6/6 plus check, runner selftest passes 59/59, and repository formatting, syntax, JSON, and diff gates pass. Private native reachability, native graduation, production dependencies, public claims, release state, and external systems are unchanged. | `98fa215` |
| `NSASS-011-COLOR-MODULE-MIX-DIRECT-FUNCTION-INVOCATION` | `VERIFIED` | Added provider-free direct `sass:color.mix()` invocation through default, aliased, and unprefixed module namespaces in SCSS and indented Sass. Fresh Dart Sass 1.101.0 probes establish exact positional/named/list/map-splat binding, later-map override, once-only source order, legacy RGB/HSL/HWB plus alpha results, color typing, CSS bytes, and zero warnings. Native dispatch reuses the verified evaluated-splat binder, module callable identity, and bounded legacy mix kernel while retaining the separate global path; empty/short/overfull splats, unknown/non-string map keys, duplicate bindings, unitless weights, expanded limits, modern methods, wide-gamut colors, deferred values, and allocation failures expose no CSS. Test-first focused Debug stopped at 547/549 on the unavailable module boundary. Final focused native modes pass 23/23 and 549/549; full compiler modes pass 60/60 and 1044/1044; Windows x64 GNU and Linux ARM64 GNU evaluator cross-compiles pass in both modes. Native contract 12/12 plus check adds only `built-in-color-module-mix-direct-function-invocation`; provider/reference/product tests pass 151/151, package policy passes 6/6 plus check, runner selftest passes 59/59, and repository formatting, syntax, JSON, and diff gates pass. Private native reachability, native graduation, production dependencies, public claims, release state, and external systems are unchanged. | `fa477b4` |
| `NSASS-011-COLOR-MODULE-BLACKNESS-DIRECT-FUNCTION-INVOCATION` | `VERIFIED` | Added provider-free direct `sass:color.blackness()` invocation through default, aliased, and unprefixed module namespaces in SCSS and indented Sass. Fresh Dart Sass 1.101.0 probes establish exact positional/named/list/map-splat binding, later-map override, once-only source order, percentage typing, CSS bytes, one color-function deprecation per call, and the plain-CSS boundary for an unowned bare call. Native dispatch reuses the verified evaluated-splat binder, module callable identity, legacy-color kernel, and normalized warning; empty/overfull splats, unknown/non-string map keys, duplicate bindings, expanded limits, modern colors, deferred values, and allocation failures expose no CSS. Test-first focused Debug stopped at 546/548 on the unavailable module boundary. Final focused native modes pass 23/23 and 548/548; full compiler modes pass 60/60 and 1043/1043; Windows x64 GNU and Linux ARM64 GNU evaluator cross-compiles pass in both modes. Native contract 12/12 plus check adds only `built-in-color-module-blackness-direct-function-invocation`; provider/reference/product tests pass 151/151, package policy passes 6/6 plus check, runner selftest passes 59/59, and repository formatting, syntax, JSON, and diff gates pass. Private native reachability, native graduation, production dependencies, public claims, release state, and external systems are unchanged. | `4cb8ea3` |
| `NSASS-011-COLOR-MODULE-WHITENESS-DIRECT-FUNCTION-INVOCATION` | `VERIFIED` | Added provider-free direct `sass:color.whiteness()` invocation through default, aliased, and unprefixed module namespaces in SCSS and indented Sass. Fresh Dart Sass 1.101.0 probes establish exact positional/named/list/map-splat binding, later-map override, once-only source order, percentage typing, CSS bytes, one color-function deprecation per call, and the plain-CSS boundary for an unowned bare call. Native dispatch reuses the verified evaluated-splat binder, module callable identity, legacy-color kernel, and normalized warning; empty/overfull splats, unknown/non-string map keys, duplicate bindings, expanded limits, modern colors, deferred values, and allocation failures expose no CSS. Test-first focused Debug stopped at 545/547 on the unavailable module boundary. Final focused native modes pass 23/23 and 547/547; full compiler modes pass 60/60 and 1042/1042; Windows x64 GNU and Linux ARM64 GNU evaluator cross-compiles pass in both modes. Native contract 12/12 plus check adds only `built-in-color-module-whiteness-direct-function-invocation`; provider/reference/product tests pass 151/151, package policy passes 6/6 plus check, runner selftest passes 59/59, and repository formatting, syntax, JSON, and diff gates pass. Direct blackness, private native reachability, native graduation, production dependencies, public claims, release state, and external systems are unchanged. | `c97dd52` |
| `NSASS-011-STRING-LEGACY-INSERT-DIRECT-SPLAT-EXPANSION` | `VERIFIED` | Extended only direct legacy-global `str-insert()` calls containing a syntactic splat to use the already-verified evaluated expansion, canonical bounded binder, global callable ownership, and shared insert kernel. Fresh Dart Sass 1.101.0 probes establish exact three-item positional-list and keyword-map splats, direct-plus-map binding, later-map override, quoted/unquoted/escaped/interpolated strings, Unicode-scalar/combining/ZWJ insertion, once-only source order, SCSS/indented-Sass output, and one deprecation warning per call. Empty/short/overfull list splats, unknown or non-string map keys, positional/map duplicates, expanded argument ceilings, invalid values, deferred calculations, and allocation failures expose no CSS; ordinary legacy calls and every sibling direct built-in retain their former dispatch. The test-first focused Debug gate stopped at 544/546 on the former `SplatUnsupported` boundary. Final focused native modes pass 23/23 and 546/546; full compiler modes pass 60/60 and 1041/1041; Windows x64 GNU and Linux ARM64 GNU evaluator cross-compiles pass in both modes. Native contract 12/12 plus check adds only `built-in-string-legacy-insert-direct-splat-expansion`; provider/reference/product tests pass 151/151, package policy passes 6/6 plus check, runner selftest passes 59/59, and repository formatting, syntax, JSON, and diff gates pass. Private native reachability, native graduation, production dependencies, public claims, release state, and external systems are unchanged. | `c4babb6` |
| `NSASS-011-STRING-LEGACY-SLICE-DIRECT-SPLAT-EXPANSION` | `VERIFIED` | Extended only direct legacy-global `str-slice()` calls containing a syntactic splat to use the already-verified evaluated expansion, canonical bounded binder, global callable ownership, and shared slice kernel. Fresh Dart Sass 1.101.0 probes establish exact two- and three-item positional-list and keyword-map splats, direct-plus-map binding, later-map override, quoted/unquoted/escaped/interpolated strings, Unicode-scalar/combining/ZWJ slicing, negative indexes, once-only source order, SCSS/indented-Sass output, and one deprecation warning per call. Empty/short/overfull list splats, unknown or non-string map keys, positional/map duplicates, expanded argument ceilings, invalid values, deferred calculations, and allocation failures expose no CSS; ordinary legacy calls and every sibling direct built-in retain their former dispatch. The test-first focused Debug gate stopped at 543/545 on the former `SplatUnsupported` boundary. Final focused native modes pass 23/23 and 545/545; full compiler modes pass 60/60 and 1040/1040; Windows x64 GNU and Linux ARM64 GNU evaluator cross-compiles pass in both modes. Native contract 12/12 plus check adds only `built-in-string-legacy-slice-direct-splat-expansion`; provider/reference/product tests pass 151/151, package policy passes 6/6 plus check, runner selftest passes 59/59, and repository formatting, syntax, JSON, and diff gates pass. Private native reachability, other direct splats, native graduation, production dependencies, public claims, release state, and external systems are unchanged. | `9a8400d` |
| `NSASS-011-STRING-LEGACY-INDEX-DIRECT-SPLAT-EXPANSION` | `VERIFIED` | Extended only direct legacy-global `str-index()` calls containing a syntactic splat to use the already-verified evaluated expansion, canonical bounded binder, global callable ownership, and shared index kernel. Fresh Dart Sass 1.101.0 probes establish exact two-item positional-list and keyword-map splats, direct-plus-map binding, later-map override, quoted/unquoted/escaped/interpolated strings, Unicode-scalar indexing, nullable misses, once-only source order, SCSS/indented-Sass output, and one deprecation warning per call. Empty/short/overfull list splats, unknown or non-string map keys, positional/map duplicates, expanded argument ceilings, invalid values, deferred calculations, and allocation failures expose no CSS; ordinary legacy calls and every sibling direct built-in retain their former dispatch. The test-first focused Debug gate stopped at 542/544 on the former `SplatUnsupported` boundary. Final focused native modes pass 23/23 and 544/544; full compiler modes pass 60/60 and 1039/1039; Windows x64 GNU and Linux ARM64 GNU evaluator cross-compiles pass in both modes. Native contract 12/12 plus check adds only `built-in-string-legacy-index-direct-splat-expansion`; provider/reference/product tests pass 151/151, package policy passes 6/6 plus check, runner selftest passes 59/59, and repository formatting, syntax, JSON, and diff gates pass. Private native reachability, other direct splats, native graduation, production dependencies, public claims, release state, and external systems are unchanged. | `0d7a23e` |
| `NSASS-011-STRING-LEGACY-LENGTH-DIRECT-SPLAT-EXPANSION` | `VERIFIED` | Extended only direct legacy-global `str-length()` calls containing a syntactic splat to use the already-verified evaluated expansion, canonical bounded binder, global callable ownership, and shared length kernel. Fresh Dart Sass 1.101.0 probes establish exact positional-list and keyword-map splats, later-map override, quoted/unquoted/escaped/interpolated strings, Unicode-scalar length, once-only source order, SCSS/indented-Sass output, and one deprecation warning per call. Empty/overfull list splats, unknown or non-string map keys, positional/map duplicates, expanded argument ceilings, non-string values, deferred calculations, and allocation failures expose no CSS; ordinary legacy calls and every sibling direct built-in retain their former dispatch. The test-first focused Debug gate stopped at 541/543 on the former `SplatUnsupported` boundary. Final focused native modes pass 23/23 and 543/543; full compiler modes pass 60/60 and 1038/1038; Windows x64 GNU and Linux ARM64 GNU evaluator cross-compiles pass in both modes. Native contract 12/12 plus check adds only `built-in-string-legacy-length-direct-splat-expansion`; provider/reference/product tests pass 151/151, package policy passes 6/6 plus check, runner selftest passes 59/59, and repository formatting, syntax, JSON, and diff gates pass. Private native reachability, other direct splats, native graduation, production dependencies, public claims, release state, and external systems are unchanged. | `4b142d4` |
| `NSASS-011-STRING-LEGACY-LOWER-CASE-DIRECT-SPLAT-EXPANSION` | `VERIFIED` | Extended only direct legacy-global `to-lower-case()` calls containing a syntactic splat to use the already-verified evaluated expansion, canonical bounded binder, global callable ownership, and shared lower-case kernel. Fresh Dart Sass 1.101.0 probes establish exact positional-list and keyword-map splats, later-map override, quoted/unquoted/escaped/interpolated strings, ASCII-only conversion with Unicode bytes preserved, once-only source order, SCSS/indented-Sass output, and one deprecation warning per call. Empty/overfull list splats, unknown or non-string map keys, positional/map duplicates, expanded argument ceilings, and allocation failures expose no CSS; ordinary legacy calls and every sibling direct built-in retain their former dispatch. The test-first focused Debug gate stopped at 540/542 on the former `SplatUnsupported` boundary. Final focused native modes pass 23/23 and 542/542; full compiler modes pass 60/60 and 1037/1037; Windows x64 GNU and Linux ARM64 GNU evaluator cross-compiles pass in both modes. Native contract 12/12 plus check adds only `built-in-string-legacy-lower-case-direct-splat-expansion`; provider/reference/product tests pass 151/151, package policy passes 6/6 plus check, runner selftest passes 59/59, and repository formatting, syntax, JSON, and diff gates pass. Private native reachability, other direct splats, native graduation, production dependencies, public claims, release state, and external systems are unchanged. | `6a746e7` |
| `NSASS-011-STRING-LEGACY-UPPER-CASE-DIRECT-SPLAT-EXPANSION` | `VERIFIED` | Extended only direct legacy-global `to-upper-case()` calls containing a syntactic splat to use the already-verified evaluated expansion, canonical bounded binder, global callable ownership, and shared upper-case kernel. Fresh Dart Sass 1.101.0 probes establish exact positional-list and keyword-map splats, later-map override, quoted/unquoted/escaped/interpolated strings, ASCII-only conversion with Unicode bytes preserved, once-only source order, SCSS/indented-Sass output, and one deprecation warning per call. Empty/overfull list splats, unknown or non-string map keys, positional/map duplicates, expanded argument ceilings, and allocation failures expose no CSS; ordinary legacy calls and every sibling direct built-in retain their former dispatch. The test-first focused Debug gate stopped at 539/541 on the former `SplatUnsupported` boundary. Final focused native modes pass 23/23 and 541/541; full compiler modes pass 60/60 and 1036/1036; Windows x64 GNU and Linux ARM64 GNU evaluator cross-compiles pass in both modes. Native contract 12/12 plus check adds only `built-in-string-legacy-upper-case-direct-splat-expansion`; provider/reference/product tests pass 151/151, package policy passes 6/6 plus check, runner selftest passes 59/59, and repository formatting, syntax, JSON, and diff gates pass. Private native reachability, other direct splats, native graduation, production dependencies, public claims, release state, and external systems are unchanged. | `ecd81b5` |
| `NSASS-011-STRING-LEGACY-QUOTE-DIRECT-SPLAT-EXPANSION` | `VERIFIED` | Extended only direct legacy-global `quote()` calls containing a syntactic splat to use the already-verified evaluated expansion, canonical bounded binder, global callable ownership, and shared quote kernel. Fresh Dart Sass 1.101.0 probes establish exact positional-list and keyword-map splats, later-map override, quoted/unquoted/escaped/interpolated strings, once-only source order, SCSS/indented-Sass output, and one deprecation warning per call. Empty/overfull list splats, unknown or non-string map keys, expanded argument ceilings, and allocation failures expose no CSS; ordinary legacy calls and every sibling direct built-in retain their former dispatch. The test-first focused Debug gate stopped at 538/540 on the former `SplatUnsupported` boundary. Final focused native modes pass 23/23 and 540/540; full compiler modes pass 60/60 and 1035/1035; Windows x64 GNU and Linux ARM64 GNU evaluator cross-compiles pass in both modes. Native contract 12/12 plus check adds only `built-in-string-legacy-quote-direct-splat-expansion`; provider/reference/product tests pass 151/151, package policy passes 6/6 plus check, runner selftest passes 59/59, and repository formatting, syntax, JSON, and diff gates pass. Private native reachability, other direct splats, native graduation, production dependencies, public claims, release state, and external systems are unchanged. | `c6d3620` |
| `NSASS-011-STRING-LEGACY-UNQUOTE-DIRECT-SPLAT-EXPANSION` | `VERIFIED` | Extended only direct legacy-global `unquote()` calls containing a syntactic splat to use the already-verified evaluated expansion, canonical bounded binder, global callable ownership, and shared unquote kernel. Fresh Dart Sass 1.101.0 probes establish exact positional-list and keyword-map splats, later-map override, quoted/escaped/interpolated strings, once-only source order, SCSS/indented-Sass output, and one exact deprecation warning per call. Empty/overfull list splats, unknown or non-string map keys, expanded argument ceilings, and allocation failures expose no CSS; ordinary legacy calls and every sibling direct built-in retain their former dispatch. The test-first focused Debug gate stopped at 537/539 on the former `SplatUnsupported` boundary. Final focused native modes pass 23/23 and 539/539; full compiler modes pass 60/60 and 1034/1034; Windows x64 GNU and Linux ARM64 GNU evaluator cross-compiles pass in both modes. Native contract 12/12 plus check adds only `built-in-string-legacy-unquote-direct-splat-expansion`; provider/reference/product tests pass 151/151, package policy passes 6/6 plus check, runner selftest passes 59/59, and repository formatting, syntax, JSON, and diff gates pass. Private native reachability, other direct splats, native graduation, production dependencies, public claims, release state, and external systems are unchanged. | `c3c4f74` |
| `NSASS-011-STRING-MODULE-UNQUOTE-DIRECT-SPLAT-EXPANSION` | `VERIFIED` | Extended only module-owned direct `sass:string.unquote()` invocation to use the already-verified evaluated splat expansion and canonical bounded binder shared by reflected calls. Fresh Dart Sass 1.101.0 probes establish exact default/alias/unprefixed access, one-item positional-list and keyword-map splats, later-map override, quoted/unquoted/escaped/interpolated strings, once-only source order, SCSS/indented-Sass output, and zero warnings. Empty/overfull list splats, unknown or non-string map keys, positional/map duplicates, expanded argument ceilings, and allocation failures expose no CSS; legacy-global unquote direct splats retain their former unsupported binder. The test-first focused Debug gate stopped at 536/538 with both new tests failing at `SplatUnsupported`. Final focused native modes pass 23/23 and 538/538; full compiler modes pass 60/60 and 1033/1033; Windows x64 GNU and Linux ARM64 GNU evaluator cross-compiles pass in both modes. Native contract 12/12 plus check adds only `built-in-string-module-unquote-direct-splat-expansion`; provider/reference/product tests pass 151/151, package policy passes 6/6 plus check, runner selftest passes 59/59, and repository formatting, syntax, JSON, and diff gates pass. Private native reachability, other direct dispatch, native graduation, production dependencies, public claims, release state, and external systems are unchanged. | `ef4eae8` |
| `NSASS-011-STRING-MODULE-QUOTE-DIRECT-SPLAT-EXPANSION` | `VERIFIED` | Extended only module-owned direct `sass:string.quote()` invocation to use the already-verified evaluated splat expansion and canonical bounded binder shared by reflected calls. Fresh Dart Sass 1.101.0 probes establish exact default/alias/unprefixed access, one-item positional-list and keyword-map splats, later-map override, quoted/unquoted/escaped/interpolated strings, once-only source order, SCSS/indented-Sass output, and zero warnings. Empty/overfull list splats, unknown or non-string map keys, positional/map duplicates, expanded argument ceilings, and allocation failures expose no CSS; legacy-global quote direct splats retain their former unsupported binder. The test-first positive and negative filtered Debug gates each stopped 0/1 at `SplatUnsupported`. Final focused native modes pass 23/23 and 537/537; full compiler modes pass 60/60 and 1032/1032; Windows x64 GNU and Linux ARM64 GNU evaluator cross-compiles pass in both modes. Native contract 12/12 plus check adds only `built-in-string-module-quote-direct-splat-expansion`; provider/reference/product tests pass 151/151, package policy passes 6/6 plus check, runner selftest passes 59/59, and repository formatting, syntax, JSON, and diff gates pass. Private native reachability, other direct splats, native graduation, production dependencies, public claims, release state, and external systems are unchanged. | `dcb2576` |
| `NSASS-011-STRING-MODULE-LOWER-CASE-DIRECT-SPLAT-EXPANSION` | `VERIFIED` | Extended only module-owned direct `sass:string.to-lower-case()` invocation to use the already-verified evaluated splat expansion and canonical bounded binder shared by reflected calls. Fresh Dart Sass 1.101.0 probes establish exact default/alias/unprefixed access, one-item positional-list and keyword-map splats, later-map override, ASCII-only conversion with Unicode bytes preserved, once-only source order, SCSS/indented-Sass output, and zero warnings. Empty/overfull list splats, unknown or non-string map keys, positional/map duplicates, expanded argument ceilings, and allocation failures expose no CSS; legacy-global lower-case direct splats retain their former unsupported binder. The test-first filtered Debug gate stopped 0/1 at `SplatUnsupported`. Final focused native modes pass 23/23 and 536/536; full compiler modes pass 60/60 and 1031/1031; Windows x64 GNU and Linux ARM64 GNU evaluator cross-compiles pass in both modes. Native contract 12/12 plus check adds only `built-in-string-module-lower-case-direct-splat-expansion`; provider/reference/product tests pass 151/151, package policy passes 6/6 plus check, runner selftest passes 59/59, and repository formatting, syntax, JSON, and diff gates pass. Private native reachability, other direct splats, native graduation, production dependencies, public claims, release state, and external systems are unchanged. | `a2e73a6` |
| `NSASS-011-STRING-MODULE-UPPER-CASE-DIRECT-SPLAT-EXPANSION` | `VERIFIED` | Extended only module-owned direct `sass:string.to-upper-case()` invocation to use the already-verified evaluated splat expansion and canonical bounded binder shared by reflected calls. Fresh Dart Sass 1.101.0 probes establish exact default/alias/unprefixed access, one-item positional-list and keyword-map splats, later-map override, ASCII-only conversion with Unicode bytes preserved, once-only source order, SCSS/indented-Sass output, and zero warnings. Empty/overfull list splats, unknown or non-string map keys, positional/map duplicates, expanded argument ceilings, and allocation failures expose no CSS; legacy-global upper-case and module-owned lower-case direct splats retain their former unsupported binders. The test-first positive and negative filtered Debug gates each stopped 0/1 at `SplatUnsupported`. Final focused native modes pass 23/23 and 535/535; full compiler modes pass 60/60 and 1030/1030; Windows x64 GNU and Linux ARM64 GNU evaluator cross-compiles pass in both modes. Native contract 12/12 plus check adds only `built-in-string-module-upper-case-direct-splat-expansion`; provider/reference/product tests pass 151/151, package policy passes 6/6 plus check, runner selftest passes 59/59, and repository formatting, syntax, JSON, and diff gates pass. Private native reachability, other direct splats, native graduation, production dependencies, public claims, release state, and external systems are unchanged. | `d913355` |
| `NSASS-011-STRING-MODULE-INSERT-DIRECT-SPLAT-EXPANSION` | `VERIFIED` | Extended only module-owned direct `sass:string.insert()` invocation to use the already-verified evaluated splat expansion and canonical bounded binder shared by reflected calls. Fresh Dart Sass 1.101.0 probes establish exact default/alias/unprefixed access, three-item positional-list and keyword-map splats, direct-plus-map binding, later-map override, Unicode-scalar insertion, once-only source order, SCSS/indented-Sass output, and zero warnings. Empty/short/overfull list splats, unknown or non-string map keys, positional/map duplicates, expanded argument ceilings, and allocation failures expose no CSS; legacy-global `str-insert()` retains its raw unsupported direct-splat boundary and other direct string functions retain their former binders. The test-first focused Debug gate stopped at 532/534 on the former unsupported boundary. Final focused native modes pass 23/23 and 534/534; full compiler modes pass 60/60 and 1029/1029; Windows x64 GNU and Linux ARM64 GNU evaluator cross-compiles pass in both modes. Native contract 12/12 plus check adds only `built-in-string-module-insert-direct-splat-expansion`; provider/reference/product tests pass 151/151, package policy passes 6/6 plus check, runner selftest passes 59/59, and repository formatting, syntax, JSON, and diff gates pass. Private native reachability, legacy-global and other direct splats, native graduation, production dependencies, public claims, release state, and external systems are unchanged. | `d11b4f7` |
| `NSASS-011-STRING-MODULE-SLICE-DIRECT-SPLAT-EXPANSION` | `VERIFIED` | Extended only module-owned direct `sass:string.slice()` invocation to use the already-verified evaluated splat expansion and canonical bounded binder shared by reflected calls. Fresh Dart Sass 1.101.0 probes establish exact default/alias/unprefixed access, two- and three-item positional-list and keyword-map splats, direct-plus-map binding, later-map override, Unicode-scalar slicing, once-only source order, SCSS/indented-Sass output, and zero warnings. Empty/short/overfull list splats, unknown or non-string map keys, positional/map duplicates, expanded argument ceilings, and allocation failures expose no CSS; legacy-global `str-slice()` retains its raw unsupported direct-splat boundary and other direct string functions retain their former binders. The test-first focused Debug gate stopped at 531/533 on the former unsupported boundary. Final focused native modes pass 23/23 and 533/533; full compiler modes pass 60/60 and 1028/1028; Windows x64 GNU and Linux ARM64 GNU evaluator cross-compiles pass in both modes. Native contract 12/12 plus check adds only `built-in-string-module-slice-direct-splat-expansion`; provider/reference/product tests pass 151/151, package policy passes 6/6 plus check, runner selftest passes 59/59, and repository formatting, syntax, JSON, and diff gates pass. Private native reachability, legacy-global and other direct splats, native graduation, production dependencies, public claims, release state, and external systems are unchanged. | `55680fc` |
| `NSASS-011-STRING-MODULE-INDEX-DIRECT-SPLAT-EXPANSION` | `VERIFIED` | Extended only module-owned direct `sass:string.index()` invocation to use the already-verified evaluated splat expansion and canonical bounded binder shared by reflected calls. Fresh Dart Sass 1.101.0 probes establish exact default/alias/unprefixed access, positional-list and keyword-map splats, direct-plus-map binding, later-map override, Unicode-scalar indexing, once-only source order, SCSS/indented-Sass output, and zero warnings. Empty/short/overfull list splats, unknown or non-string map keys, positional/map duplicates, expanded argument ceilings, and allocation failures expose no CSS; legacy-global `str-index()` and other direct string functions retain their former binder. The test-first focused Debug gate stopped at 530/532 on the former unsupported boundary. Final focused native modes pass 23/23 and 532/532; full compiler modes pass 60/60 and 1027/1027; Windows x64 GNU and Linux ARM64 GNU root-module cross-compiles pass in both modes. Native contract 12/12 plus check adds only `built-in-string-module-index-direct-splat-expansion`; provider/reference/product tests pass 151/151, package policy passes 6/6 plus check, runner selftest passes 59/59, and repository formatting, syntax, JSON, and diff gates pass. Direct `string.slice()` splats remain explicitly unsupported. Private native reachability, splats for other functions, native graduation, production dependencies, public claims, release state, and external systems are unchanged. | `9806081` |
| `NSASS-011-STRING-MODULE-LENGTH-DIRECT-SPLAT-EXPANSION` | `VERIFIED` | Extended only module-owned direct `sass:string.length()` invocation to use the already-verified evaluated splat expansion and canonical bounded binder shared by reflected calls. Fresh Dart Sass 1.101.0 probes establish exact default/alias/unprefixed access, positional-list and keyword-map splats, later-map override, once-only source order, SCSS/indented-Sass output, and zero warnings. Empty/overfull list splats, unknown or non-string map keys, positional/map duplicates, expanded argument ceilings, and allocation failures expose no CSS; legacy-global `str-length()` and other direct string functions retain their former binder. The test-first focused Debug gate stopped at 529/531 on the former unsupported boundary. Final focused native modes pass 23/23 and 531/531; full compiler modes pass 60/60 and 1026/1026; Windows x64 GNU and Linux ARM64 GNU evaluator cross-compiles pass in both modes. Native contract 12/12 plus check adds only `built-in-string-module-length-direct-splat-expansion`; provider/reference/product tests pass 151/151, package policy passes 6/6 plus check, runner selftest passes 59/59, and repository formatting, syntax, JSON, and diff gates pass. Private native reachability, splats for other functions, native graduation, production dependencies, public claims, release state, and external systems are unchanged. | `9f5a82c` |
| `NSASS-011-STRING-SPLIT-DIRECT-SPLAT-EXPANSION` | `VERIFIED` | Extended only direct `sass:string.split()` invocation to use the already-verified evaluated splat expansion and canonical bounded binder shared by reflected calls. Fresh Dart Sass 1.101.0 probes establish exact positional-list and keyword-map splats, direct-plus-map binding, later-map override, once-only source order, SCSS/indented-Sass output, and zero warnings. Empty/overfull list splats, unknown or non-string map keys, expanded argument ceilings, and allocation failures expose no CSS; other direct built-ins retain their former binder. The test-first focused Debug gate stopped at 529/530 with `SplatUnsupported`. Final focused native modes pass 23/23 and 530/530; full compiler modes pass 60/60 and 1025/1025; Windows x64 GNU and Linux ARM64 GNU evaluator cross-compiles pass in both modes. Native contract 12/12 plus check adds only `built-in-string-split-direct-splat-expansion`; provider/reference/product tests pass 151/151, package policy passes 6/6 plus check, runner selftest passes 59/59, and repository formatting, syntax, JSON, and diff gates pass. Private native reachability, splats for other functions, native graduation, production dependencies, public claims, release state, and external systems are unchanged. | `3a40922` |
| `NSASS-011-META-STRING-SPLIT-FUNCTION-INVOCATION` | `VERIFIED` | Added provider-free native Zig direct and reflected `sass:string.split()` invocation for SCSS and indented Sass. Fresh Dart Sass 1.101.0 probes establish default/alias/unprefixed identity, the closed legacy-global boundary, reflected positional/named/list/map-splat binding, once-only order, bracketed comma-list metadata, quoted/unquoted item typing, decoded non-overlapping matching, Unicode/combining/ZWJ/escape behavior, empty/consecutive/leading/trailing/absent separators, empty-input and empty-separator semantics, and nullable fuzzy-positive integer limits with ignored units. One shared bounded native kernel and canonical binders enforce temporary-byte, operation, argument, and collection ceilings; invalid input and allocation failures expose no CSS. The test-first gate stopped at unavailable dispatch. Final kernel tests pass 5/5; filtered evaluator tests pass 3/3; focused native modes pass 23/23 and 529/529; full compiler modes pass 60/60 and 1024/1024; Windows x64 GNU and Linux ARM64 GNU root-module cross-compiles pass in both modes. Native contract 12/12 plus check adds only `built-in-meta-string-split-function-invocation`; provider/reference/product tests pass 151/151, package policy passes 6/6 plus check, runner selftest passes 59/59, and repository formatting, syntax, JSON, and diff gates pass. Nondeterministic `sass:string.unique-id()` remains unavailable. Private native reachability, broader Sass semantics, native graduation, production dependencies, public claims, release state, and external systems are unchanged. | `791b0dc` |
| `NSASS-011-META-COLOR-IS-POWERLESS-FUNCTION-INVOCATION` | `VERIFIED` | Added provider-free native Zig direct and reflected `sass:color.is-powerless()` invocation for SCSS and indented Sass. Fresh Dart Sass 1.101.0 probes establish default/alias/unprefixed callable identity, the closed legacy-global boundary, positional/named/list/map-splat binding, once-only evaluation, boolean typing, quoted case-sensitive channel names, and exact semantics across all 15 represented spaces. Only hue can be powerless: HSL saturation is fuzzy zero, HWB whiteness plus blackness is fuzzy at least 100%, and LCH/OKLCH chroma is fuzzy zero. A shared allocation-free kernel implements the provider's exact absolute `5e-12` boundary and treats already-represented missing modern chroma as stored zero. Invalid binding/value/module/channel, deferred values, limits, unsupported legacy missing-channel constructors, and allocation failures expose no CSS. The focused Debug baseline stopped exactly at the absent kernel entry point. Final direct evaluator modes pass 388/388; focused native modes pass 23/23 and 525/525; full compiler modes pass 60/60 and 1020/1020; Windows x64 GNU and Linux ARM64 GNU root-module cross-compiles pass in both modes. Native contract 12/12 plus check adds only `built-in-meta-color-is-powerless-function-invocation`; provider/reference/product tests pass 151/151, package policy passes 6/6 plus check, runner selftest passes 59/59, and repository formatting, syntax, JSON, and diff gates pass. A sibling oracle probe found `sass:string.unique-id()` nondeterministic and `sass:string.split()` deterministic. Private native reachability, broader missing-channel parsing, native graduation, production dependencies, public claims, release state, and external systems are unchanged. | `b380a51` |
| `NSASS-011-META-COLOR-SAME-FUNCTION-INVOCATION` | `VERIFIED` | Added provider-free native Zig direct and reflected `sass:color.same()` invocation for SCSS and indented Sass. Fresh Dart Sass 1.101.0 probes establish default/alias/unprefixed callable identity, the closed legacy-global boundary, positional/named/list/map-splat binding, once-only evaluation, boolean typing, all 15 represented spaces, XYZ D65 conversion, missing-channel zero substitution including alpha, exact `1e-11` rounded fuzzy equality, and retained powerless-channel significance. A separate allocation-free kernel leaves broader Sass `==` semantics unchanged; direct and reflected paths share that typed kernel and canonical binding. Invalid binding/value/module, deferred values, argument limits, unavailable `is-powerless`, and allocation failures expose no CSS. The focused Debug baseline stopped at 520/521 exactly at the absent direct dispatch. Final direct evaluator modes pass 386/386; focused native modes pass 23/23 and 522/522; full compiler modes pass 60/60 and 1017/1017; Windows x64 GNU and Linux ARM64 GNU root-module cross-compiles pass in both modes. Native contract 12/12 plus check adds only `built-in-meta-color-same-function-invocation`; provider/reference/product tests pass 151/151, package policy passes 6/6 plus check, runner selftest passes 59/59, and repository formatting, syntax, JSON, and diff gates pass. Private native reachability, analogous missing-channel propagation, `is-powerless`, native graduation, production dependencies, public claims, release state, and external systems are unchanged. | `cfd27ca` |
| `NSASS-011-META-COLOR-CHANNEL-FUNCTION-INVOCATION` | `VERIFIED` | Added provider-free native Zig direct and reflected `sass:color.channel()` invocation for SCSS and indented Sass. Fresh Dart Sass 1.101.0 probes establish default/alias/unprefixed callable identity, the closed legacy-global boundary, positional/named/list/map-splat binding, once-only evaluation, numeric typing, quoted case-sensitive escaped/interpolated channel names, nullable case-insensitive targets, XYZ aliasing, and exact stored plus converted channel values and units across all 15 represented spaces. Native direct and `meta.call()` paths share the canonical evaluated binder, quoted-string decoder, and allocation-free conversion core. Every missing-channel color remains fail-closed rather than admitting the provider's broader substitution and analogous-channel propagation. Invalid binding/value/module/channel/space, deferred values, argument and temporary limits, missing channels, and allocation failures expose no CSS; `same` remains explicitly unavailable. The focused Debug baseline stopped at 518/519 exactly at the absent direct dispatch. Final direct evaluator modes pass 384/384; focused native modes pass 23/23 and 519/519; full compiler modes pass 60/60 and 1014/1014; Windows x64 GNU and Linux ARM64 GNU root-module cross-compiles pass in both modes. Native contract 12/12 plus check adds only `built-in-meta-color-channel-function-invocation`; provider/reference/product tests pass 151/151, package policy passes 6/6 plus check, runner selftest passes 59/59, and repository formatting, syntax, JSON, and diff gates pass. Private native reachability, missing-channel semantics, `same`/`is-powerless`, native graduation, production dependencies, public claims, release state, and external systems are unchanged. | `2a68222` |
| `NSASS-011-META-COLOR-TO-GAMUT-FUNCTION-INVOCATION` | `VERIFIED` | Added provider-free native Zig direct and reflected `sass:color.to-gamut()` invocation for SCSS and indented Sass. Fresh Dart Sass 1.101.0 probes establish default/alias/underscore/unprefixed callable identity, the closed legacy-global boundary, positional/named/list/map-splat binding, once-only evaluation, mandatory exact `clip` or `local-minde` methods, nullable case-insensitive targets, XYZ aliasing, and exact mapped results. Native Zig maps bounded RGB, HSL, HWB, and predefined RGB-family targets before converting back to the original space, preserves alpha, and leaves perceptual/XYZ targets unchanged. Its allocation-free kernel implements clipping and deterministic local-MINDE chroma reduction in OKLCH using Delta E OK and a bounded binary search. Direct and `meta.call()` paths share the canonical evaluated binder and kernel. Missing channels and local legacy powerless-hue results remain fail-closed because the native value model cannot preserve the provider's resulting missing hue. Invalid binding/value/module/space/method, deferred values, argument limits, missing channels, powerless results, and allocation failures expose no CSS. The focused Debug baseline stopped exactly at the absent kernel entry point. Final direct evaluator modes pass 382/382; focused native modes pass 23/23 and 517/517; full compiler modes pass 60/60 and 1012/1012; Windows x64 GNU and Linux ARM64 GNU evaluator cross-compiles pass in both modes. Native contract 12/12 plus check adds only `built-in-meta-color-to-gamut-function-invocation`; provider/reference/product tests pass 151/151, package policy passes 6/6 plus check, runner selftest passes 59/59, and repository formatting, syntax, JSON, and diff gates pass. Private native reachability, implicit mapping elsewhere, broader missing-channel semantics, native graduation, production dependencies, public claims, release state, and external systems are unchanged. | `cc31b78` |
| `NSASS-011-META-COLOR-IS-IN-GAMUT-FUNCTION-INVOCATION` | `VERIFIED` | Added provider-free native Zig direct and reflected `sass:color.is-in-gamut()` invocation for SCSS and indented Sass. Fresh Dart Sass 1.101.0 probes establish default/alias/underscore/unprefixed callable identity, the closed legacy-global boundary, positional/named/list/map-splat binding, once-only evaluation, boolean typing, nullable targets, case-insensitive space names, XYZ aliasing, exact bounded/unbounded space classification, alpha/hue exclusion, missing-channel zero treatment, and the provider's `1e-11` rounded fuzzy boundary. Native Zig checks all 15 represented spaces through the existing allocation-free conversion core without gamut mapping; cross-space missing-channel conversion remains fail-closed. Direct and `meta.call()` paths share the canonical evaluated binder and predicate. Invalid binding/value/module/target, deferred values, argument limits, cross-space missing channels, and allocation failures expose no CSS. The focused Debug baseline stopped exactly at the absent kernel entry point. Final direct evaluator modes pass 380/380; focused native modes pass 23/23 and 514/514; full compiler modes pass 60/60 and 1009/1009; Windows x64 GNU and Linux ARM64 GNU evaluator cross-compiles pass in both modes. Native contract 12/12 plus check adds only `built-in-meta-color-is-in-gamut-function-invocation`; provider/reference/product tests pass 151/151, package policy passes 6/6 plus check, runner selftest passes 59/59, and repository formatting, syntax, JSON, and diff gates pass. Private native reachability, gamut mapping, broader missing-channel semantics, native graduation, production dependencies, public claims, release state, and external systems are unchanged. | `f5be989` |
| `NSASS-011-META-COLOR-IS-MISSING-FUNCTION-INVOCATION` | `VERIFIED` | Added provider-free native Zig direct and reflected `sass:color.is-missing()` invocation for SCSS and indented Sass. Fresh Dart Sass 1.101.0 probes establish default/alias/underscore/unprefixed callable identity, the closed legacy-global boundary, positional/named/list/map-splat binding, escaped/interpolated quoted channel names, once-only evaluation, and boolean typing. Native Zig accepts only quoted case-sensitive channel names valid for the stored color space and queries the existing missing mask across all 15 represented spaces without parsing, substitution, conversion, or gamut mapping. Direct and `meta.call()` paths share the canonical evaluated binder and query. Invalid binding/value/module/channel, argument and temporary limits, and allocation failures expose no CSS. The Debug baseline stopped at direct evaluator 376/378 and focused native 509/511 exactly at the unavailable callable. Final direct evaluator modes pass 378/378; focused native modes pass 23/23 and 511/511; full compiler modes pass 60/60 and 1006/1006; Windows x64 GNU and Linux ARM64 GNU evaluator cross-compiles pass in both modes. Native contract 12/12 plus check adds only `built-in-meta-color-is-missing-function-invocation`; provider/reference/product tests pass 151/151, package policy passes 6/6 plus check, runner selftest passes 59/59, and repository formatting, syntax, JSON, and diff gates pass. Private native reachability, missing-channel parsing/substitution, conversion/gamut semantics, native graduation, production dependencies, public claims, release state, and external systems are unchanged. | `8500ed9` |
| `NSASS-011-META-COLOR-IS-LEGACY-FUNCTION-INVOCATION` | `VERIFIED` | Added provider-free native Zig direct and reflected `sass:color.is-legacy()` invocation for SCSS and indented Sass. Fresh Dart Sass 1.101.0 probes establish default/alias/underscore/unprefixed callable identity, the closed legacy-global boundary, positional/named/list/map-splat binding, once-only evaluation, and boolean typing. Native Zig classifies RGB, HSL, and HWB as legacy and all 12 represented modern spaces as non-legacy, including conversions and already-represented modern missing-channel values without channel substitution. Direct and `meta.call()` paths share the canonical typed binder and color-only predicate. Invalid binding/value/module, deferred values, argument limits, and allocation failures expose no CSS; legacy missing-channel constructor parsing remains outside this slice. The Debug baseline stopped 1/2 at the unavailable callable. Final direct evaluator modes pass 376/376; focused native modes pass 23/23 and 509/509; full compiler modes pass 60/60 and 1004/1004; Windows x64 GNU and Linux ARM64 GNU evaluator cross-compiles pass in both modes. Native contract 12/12 plus check adds only `built-in-meta-color-is-legacy-function-invocation`; provider/reference/product tests pass 151/151, package policy passes 6/6 plus check, runner selftest passes 59/59, and repository formatting, syntax, JSON, and diff gates pass. Private native reachability, missing-channel parsing/substitution, gamut semantics, native graduation, production dependencies, public claims, release state, and external systems are unchanged. | `a0c1b5f` |
| `NSASS-011-META-COLOR-TO-SPACE-FUNCTION-INVOCATION` | `VERIFIED` | Added provider-free native Zig direct and reflected `sass:color.to-space()` invocation for SCSS and indented Sass. Fresh Dart Sass 1.101.0 probes establish default/alias/unprefixed callable identity, the closed legacy-global boundary, positional/named/list/map-splat binding, once-only evaluation, case-insensitive unquoted targets, XYZ canonicalization, and exact conversion of `#369` into all 15 represented spaces with alpha preservation. Computed-color provenance now survives nested identity conversion so modern negative fractional channels retain provider serialization. Direct and `meta.call()` paths share the canonical typed binder and allocation-free conversion core. Invalid binding/value/module, quoted or unknown targets, deferred values, missing channels, argument limits, and allocation failures expose no CSS; missing-channel substitution and gamut mapping remain closed. The baseline stopped at direct evaluator 373/374 and focused native 506/507. Final direct evaluator modes pass 374/374; focused native modes pass 23/23 and 507/507; full compiler modes pass 60/60 and 1002/1002; Windows x64 GNU and Linux ARM64 GNU evaluator cross-compiles pass in both modes. Native contract 12/12 plus check adds only `built-in-meta-color-to-space-function-invocation`; provider/reference/product tests pass 151/151, package policy passes 6/6 plus check, runner selftest passes 59/59, and repository formatting, syntax, JSON, and diff gates pass. Private native reachability, missing-channel and gamut semantics, native graduation, production dependencies, public claims, release state, and external systems are unchanged. | `c947033` |
| `NSASS-011-META-COLOR-SPACE-FUNCTION-INVOCATION` | `VERIFIED` | Added provider-free native Zig direct and reflected `sass:color.space()` invocation for SCSS and indented Sass. Fresh Dart Sass 1.101.0 probes establish default/alias/unprefixed callable identity, the closed legacy-global boundary, positional/named/list/map-splat binding, once-only evaluation, zero warnings, unquoted string typing, and exact stored identities for all 15 represented color spaces, including canonical `xyz` for both authored XYZ D65 spellings. Native direct and `meta.call()` paths share one typed color-only implementation without conversion or gamut mapping. Invalid binding/value/module, deferred values, missing channels, argument limits, and allocation failures expose no CSS; `to-space` remains unavailable. The filtered Debug baseline stopped 1/2 at the absent callable. Final direct evaluator modes pass 372/372; focused native modes pass 23/23 and 505/505; full compiler modes pass 60/60 and 1000/1000; Windows x64 GNU and Linux ARM64 GNU evaluator cross-compiles pass in both modes. Native contract 12/12 plus check adds only `built-in-meta-color-space-function-invocation`; provider/reference/product tests pass 151/151, package policy passes 6/6 plus check, runner selftest passes 59/59, and repository formatting, syntax, JSON, and diff gates pass. Private native reachability, color conversion, native graduation, production dependencies, public claims, release state, and external systems are unchanged. | `96a04da` |
| `NSASS-011-META-MODULE-FUNCTIONS-FUNCTION-INVOCATION` | `VERIFIED` | Added provider-free native Zig direct and reflected `sass:meta.module-functions()` enumeration for already-loaded built-in modules. Fresh Dart Sass 1.101.0 probes establish module/global callable identity and warning ownership, default/alias/underscore/unprefixed access, positional/named/list/map-splat binding, once-only evaluation, SCSS/indented-Sass behavior, and the exact ordered 116-reference inventories for `sass:color` (37), `sass:list` (10), `sass:map` (9), `sass:math` (24), `sass:meta` (18), `sass:selector` (8), and `sass:string` (10). One centralized immutable inventory drives enumeration and reflected lookup, charges the call, namespace scan, and entries, and owns the bounded result without loading files or admitting user modules. Nine reflected color functions and two reflected string functions without implemented semantics remain explicitly non-invocable. Invalid binding/value/namespace, argument and collection ceilings, unsupported calls, and allocation failures expose no CSS. The Debug baseline stopped at direct evaluator 368/370 and focused native 501/503. Final direct evaluator modes pass 370/370; focused native modes pass 23/23 and 503/503; full compiler modes pass 60/60 and 998/998; Windows x64 GNU and Linux ARM64 GNU evaluator cross-compiles pass in both modes. Native contract 12/12 plus check adds only `built-in-meta-module-functions-function-invocation`; provider/reference/product tests pass 151/151, package policy passes 6/6 plus check, runner selftest passes 59/59, and repository formatting, syntax, JSON, and diff gates pass. Private native reachability, unsupported function invocation, dynamic loading, native graduation, production dependencies, public claims, release state, and external systems are unchanged. | `6ce2d43` |
| `NSASS-011-META-MODULE-VARIABLES-FUNCTION-INVOCATION` | `VERIFIED` | Added provider-free native Zig direct and reflected `sass:meta.module-variables()` enumeration for already-loaded built-in modules. Fresh Dart Sass 1.101.0 probes establish module/global callable identity and warning ownership, default/alias/underscore/unprefixed access, positional/named/list/map-splat binding, once-only evaluation, SCSS/indented-Sass behavior, empty maps for the six built-in modules without variables, and the exact ordered seven-entry `sass:math` map. Native dispatch uses one static inventory, charges the call, namespace scan, and entries, and owns the bounded result without loading files or admitting user modules. Precision provenance survives map cloning and later arithmetic, retaining reflected epsilon's full IEEE payload until consumption. Invalid binding/value/namespace, argument and collection ceilings, and allocation failures expose no CSS. The filtered Debug baseline stopped 0/2 at the absent callable. Final direct evaluator modes pass 368/368; numeric units pass 8/8; focused native modes pass 23/23 and 501/501; full compiler modes pass 60/60 and 996/996; Windows x64 GNU and Linux ARM64 GNU evaluator cross-compiles pass in both modes. Native contract 12/12 plus check adds only `built-in-meta-module-variables-function-invocation`; provider/reference/product tests pass 151/151, package policy passes 6/6 plus check, runner selftest passes 59/59, and repository formatting, syntax, JSON, and diff gates pass. Private native reachability, module-function enumeration, dynamic loading, native graduation, production dependencies, public claims, release state, and external systems are unchanged. | `5acabc9` |
| `NSASS-011-META-MODULE-MIXINS-FUNCTION-INVOCATION` | `VERIFIED` | Added provider-free native Zig direct and reflected `sass:meta.module-mixins()` enumeration for already-loaded built-in modules. Fresh Dart Sass 1.101.0 probes establish module/global callable identity and warning ownership, default/alias/underscore/unprefixed access, positional/named/list/map-splat binding, once-only evaluation, SCSS/indented-Sass behavior, empty maps for modules without mixins, and the exact ordered `sass:meta` map containing the existing `load-css` and `apply` references. Native dispatch uses one static built-in inventory, charges the call, namespace scan, and entries, and owns the bounded result without loading files or admitting user modules. Invalid binding/value/namespace, argument and collection ceilings, and allocation failures expose no CSS. The Debug baseline stopped at direct evaluator 364/366 and focused native 497/499. Final direct evaluator modes pass 366/366; focused native modes pass 23/23 and 499/499; full compiler modes pass 60/60 and 994/994; Windows x64 GNU and Linux ARM64 GNU evaluator cross-compiles pass in both modes. Native contract 12/12 plus check adds only `built-in-meta-module-mixins-function-invocation`; provider/reference/product tests pass 151/151, package policy passes 6/6 plus check, runner selftest passes 59/59, and repository formatting, syntax, JSON, and diff gates pass. Private native reachability, dynamic loading, remaining module enumeration, native graduation, production dependencies, public claims, release state, and external systems are unchanged. | `69d0110` |
| `NSASS-011-META-APPLY-MIXIN-APPLICATION` | `VERIFIED` | Added provider-free native Zig application of exact reflected `sass:meta` `apply` mixin references to an already-owned terminal user mixin. Fresh Dart Sass 1.101.0 probes establish default/aliased/unprefixed invocation, positional/list/map-splat forwarding, multiple apply hops, once-only source order, parameterized content forwarding, SCSS/indented-Sass behavior, and zero warnings. The implementation walks the bounded evaluated positional view iteratively, admits only the exact `meta.apply` built-in identity, consumes one transaction operation per reflected hop, allocates the terminal forwarded set once, and reuses the existing user-mixin executor without host recursion or reevaluation. Missing/non-callable/function targets, argument limits, and allocation failures expose no CSS; reflected `meta.load-css` remains unsupported and performs no resolution. The Debug baseline stopped at direct evaluator 362/364 and focused native 495/497. Final direct evaluator modes pass 364/364; focused native modes pass 23/23 and 497/497; full compiler modes pass 60/60 and 992/992; Windows x64 GNU and Linux ARM64 GNU evaluator cross-compiles pass in both modes. Native contract 12/12 plus check adds only `built-in-meta-apply-mixin-application`; provider/reference/product tests pass 151/151, package policy passes 6/6 plus check, runner selftest passes 59/59, and repository formatting, syntax, JSON, and diff gates pass. Private native reachability, dynamic loading, native graduation, production dependencies, public claims, release state, and external systems are unchanged. | `dc16918` |
| `NSASS-011-META-USER-MIXIN-APPLICATION` | `VERIFIED` | Added provider-free native Zig invocation of already-owned user-mixin references through loaded `sass:meta.apply()` members. Fresh Dart Sass 1.101.0 probes establish default/aliased/unprefixed module access, positional/named target binding, positional/named/list/map-splat/rest forwarding, once-only source order, captured parameterized content, SCSS/indented-Sass behavior, and zero warnings. The implementation evaluates one complete argument list, separates only `$mixin`, and reuses the bounded user-mixin executor, preserving lexical scope, content ownership, call depth, transaction limits, and no-partial output. Invalid targets/bindings/namespaces/content, built-in mixin targets, limits, and allocation failures expose no CSS; built-in recursion and dynamic loading remain unavailable. The Debug baseline stopped at direct evaluator 360/362 and focused native 493/495. Final direct evaluator modes pass 362/362; focused native modes pass 23/23 and 495/495; full compiler modes pass 60/60 and 990/990; Windows x64 GNU and Linux ARM64 GNU evaluator cross-compiles pass in both modes. Native contract 12/12 plus check adds only `built-in-meta-user-mixin-application`; provider/reference/product tests pass 151/151, package policy passes 6/6 plus check, runner selftest passes 59/59, and repository formatting, syntax, JSON, and diff gates pass. Private native reachability, native graduation, production dependencies, public claims, release state, and external systems are unchanged. | `bb14daa` |
| `NSASS-011-META-REFLECTED-IF-FUNCTION-INVOCATION` | `VERIFIED` | Added provider-free native Zig invocation of the already-reflectable special legacy `if` function reference through `meta.call()`. Fresh Dart Sass 1.101.0 probes establish one identity across namespaced/aliased `sass:meta` and legacy-global construction, exact existence/type/inspection behavior, positional/named/list/map-splat binding, eager once-only source order, Sass truthiness, SCSS/indented-Sass behavior, no direct legacy-`if()` deprecation, and exact legacy-global warning ownership. The reserved callable identity uses the canonical evaluated binder and returns only the selected already-owned value while preserving direct lazy `if()` semantics. Missing, extra, unknown, duplicate, eager undefined-variable, argument-limit, and allocation paths expose no CSS. The Debug baseline stopped at direct evaluator 359/360 and focused native 492/493. Final direct evaluator modes pass 360/360; focused native modes pass 23/23 and 493/493; full compiler modes pass 60/60 and 988/988; Windows x64 GNU and Linux ARM64 GNU evaluator cross-compiles pass in both modes. Native contract 12/12 plus check adds only `built-in-meta-reflected-if-function-invocation`; provider/reference/product tests pass 151/151, package policy passes 6/6 plus check, runner selftest passes 59/59, and repository formatting, syntax, JSON, and diff gates pass. Private native reachability, native graduation, production dependencies, public claims, release state, and external systems are unchanged. | `c11b932` |
| `NSASS-011-META-GET-MIXIN-FUNCTION-INVOCATION` | `VERIFIED` | Added provider-free native Zig invocation of the exact already-reflectable module-owned `sass:meta` `get-mixin` reference and its distinct legacy-global counterpart through `meta.call()`. Fresh Dart Sass 1.101.0 probes establish module alias/underscore/unprefixed identity, global distinction, positional/named/list/map-splat binding, once-only evaluation, explicit-null local lookup, user and built-in mixin resolution, SCSS/indented-Sass behavior, and exact legacy warning ownership. Canonical evaluated binding delegates to the existing mixin-reference constructor, preserving lexical scope and callable identity without enabling mixin application. Invalid bindings, values, modules, mixin names, argument limits, and allocation failures expose no CSS. The Debug baseline stopped at direct evaluator 357/358 and focused native 490/491. Final direct evaluator modes pass 358/358; focused native modes pass 23/23 and 491/491; full compiler modes pass 60/60 and 986/986; Windows x64 GNU and Linux ARM64 GNU evaluator cross-compiles pass in both modes. Native contract 12/12 plus check adds only `built-in-meta-get-mixin-function-invocation`; provider/reference/product tests pass 151/151, package policy passes 6/6 plus check, runner selftest passes 59/59, and repository formatting, syntax, JSON, and diff gates pass. Private native reachability, native graduation, production dependencies, public claims, release state, and external systems are unchanged. | `f41b05a` |
| `NSASS-011-META-GET-FUNCTION-FUNCTION-INVOCATION` | `VERIFIED` | Added provider-free native Zig invocation of the exact already-reflectable module-owned `sass:meta` `get-function` reference and its distinct legacy-global counterpart through `meta.call()`. Fresh Dart Sass 1.101.0 probes establish module alias/underscore/unprefixed identity, global distinction, positional/named/list/map-splat binding, once-only evaluation, module lookup, returned-callable invocation, SCSS/indented-Sass behavior, and exact legacy warning ownership. Canonical evaluated binding delegates to the existing constructor, preserving lexical module scope and callable identity. Invalid bindings, values, modules, argument limits, and allocation failures expose no CSS; `$css: true` references and reflected `if` invocation remain explicitly fail-closed. The Debug baseline stopped at direct evaluator 355/356 and focused native 488/489. Final direct evaluator modes pass 356/356; focused native modes pass 23/23 and 489/489; full compiler modes pass 60/60 and 984/984; Windows x64 GNU and Linux ARM64 GNU evaluator cross-compiles pass in both modes. Native contract 12/12 plus check adds only `built-in-meta-get-function-function-invocation`; provider/reference/product tests pass 151/151, package policy passes 6/6 plus check, runner selftest passes 59/59, and repository formatting, syntax, JSON, and diff gates pass. Private native reachability, native graduation, production dependencies, public claims, release state, and external systems are unchanged. | `34dda15` |
| `NSASS-011-META-CALL-FUNCTION-INVOCATION` | `VERIFIED` | Added provider-free native Zig invocation of the exact already-constructible module-owned `sass:meta` `call` reference and its distinct legacy-global counterpart through `meta.call()`. Fresh Dart Sass 1.101.0 probes establish default/aliased/escaped/unprefixed identity, module/global distinction, positional and list-splat forwarding, once-only callable/target/argument order, the one normalized global warning, SCSS/indented-Sass behavior, and missing/non-callable/duplicate/map-splat binding failures. Native dispatch reuses the existing evaluated `meta.call()` path, preserving lexical scope and avoiding argument reevaluation; argument limits and allocation failures fail closed. The Debug baseline stopped at direct evaluator 353/354 and focused native 486/487. Final direct evaluator modes pass 354/354; focused native modes pass 23/23 and 487/487; full compiler modes pass 60/60 and 982/982; Windows x64 GNU and Linux ARM64 GNU evaluator cross-compiles pass in both modes. Native contract 12/12 plus check adds only `built-in-meta-call-function-invocation`; provider/reference/product tests pass 151/151, package policy passes 6/6 plus check, runner selftest passes 59/59, and repository formatting, syntax, JSON, and diff gates pass. Private native reachability, native graduation, production dependencies, public claims, release state, and external systems are unchanged. | `d733a1c` |
| `NSASS-011-META-CONTENT-EXISTENCE-FUNCTION-INVOCATION` | `VERIFIED` | Added provider-free native Zig `meta.call()` invocation of the exact already-constructible module-owned `sass:meta` `content-exists` reference and its distinct legacy-global counterpart. Fresh Dart Sass 1.101.0 probes establish default/aliased/underscore-normalized/unprefixed identity, module/global distinction, exact active-mixin true/false results, named `$function`, empty list/map splats, once-only source order, SCSS/indented-Sass behavior, root/function/caller-content rejection, and invalid-argument boundaries. Native dispatch reuses the existing context-sensitive direct implementation and canonical zero-argument binder; extra positional/keyword/splat arguments, argument limits, and allocation failures fail closed. The Debug baseline stopped at direct evaluator 351/352 and focused native 484/485. Final direct evaluator modes pass 352/352; focused native modes pass 23/23 and 485/485; full compiler modes pass 60/60 and 980/980; Windows x64 GNU and Linux ARM64 GNU evaluator cross-compiles pass in both modes. Native contract 12/12 plus check adds only `built-in-meta-content-existence-function-invocation`; provider/reference/product tests pass 151/151, package policy passes 6/6 plus check, runner selftest passes 59/59, and repository formatting, syntax, JSON, and diff gates pass. Private native reachability, native graduation, production dependencies, public claims, release state, and external systems are unchanged. | `73a406d` |
| `NSASS-011-META-CALC-FUNCTION-REFERENCE-REJECTION` | `VERIFIED` | Corrected the native callable inventory so `calc()` remains directly evaluable but cannot be exposed through Sass reflection. Fresh Dart Sass 1.101.0 probes establish false module and legacy existence queries, failed `meta.get-function("calc")`, static reduction, deferred preservation, and the one normalized legacy-global warning in both SCSS and indented Sass. The test-first Debug baseline stopped at direct evaluator 349/350 and focused native 482/483 before the one-member inventory correction. Final direct evaluator modes pass 350/350; focused native modes pass 23/23 and 483/483; full compiler modes pass 60/60 and 978/978; Windows x64 GNU and Linux ARM64 GNU evaluator cross-compiles pass in both modes. Native contract 12/12 plus check adds only `built-in-meta-calc-function-reference-rejection`; provider/reference/product tests pass 151/151, package policy passes 6/6 plus check, runner selftest passes 59/59, and repository formatting, syntax, JSON, and diff gates pass. Private native reachability, native graduation, production dependencies, public claims, release state, and external systems are unchanged. | `c949fa8` |
| `NSASS-011-META-COLOR-IE-HEX-STR-FUNCTION-INVOCATION` | `VERIFIED` | Added provider-free native Zig invocation of the shared legacy-global and module-owned `sass:color` `ie-hex-str` reference through `meta.call()` for SCSS and indented Sass, plus the exact direct qualified module call. Fresh Dart Sass 1.101.0 probes establish one callable identity across global/default/aliased/unprefixed origins, exact required `$color` positional/named binding, list/map splats, uppercase `#AARRGGBB` string results, once-only source order, and zero warnings. Transparent, opaque, alpha-bearing RGB, HSL, and HWB inputs match the oracle; exact in-gamut conversions cover Lab, LCH, OKLab, OKLCH, sRGB, linear sRGB, Display P3, A98 RGB, ProPhoto RGB, Rec. 2020, XYZ D50, and XYZ D65. Invalid bindings and values, missing channels, unloaded modules, argument limits, and allocation failures expose no CSS. Provider-accepted missing-channel substitution and out-of-gamut gamut mapping remain deliberately ungraduated; native typed out-of-gamut values fail closed instead of being silently clipped. The test-first Debug baseline stopped at direct evaluator 347/349 and focused native 479/482. Final direct evaluator Debug/ReleaseSafe pass 349/349; focused native modes pass 23/23 and 482/482; full compiler modes pass 60/60 and 977/977; Windows x64 GNU and Linux ARM64 GNU evaluator cross-compiles pass in both modes. Native contract 12/12 plus check adds only `built-in-meta-color-ie-hex-str-function-invocation`; provider/reference/product tests pass 151/151, package policy passes 6/6 plus check, runner selftest passes 59/59, and repository formatting, syntax, JSON, and diff gates pass. Private native reachability, native graduation, production dependencies, public claims, release state, and external systems are unchanged. | `9b1657b` |
| `NSASS-011-META-COLOR-ALPHA-FUNCTION-INVOCATION` | `VERIFIED` | Added provider-free native Zig invocation of distinct legacy-global and module-owned `sass:color` `alpha` references through `meta.call()` for SCSS and indented Sass. Exact Dart Sass 1.101.0 probes establish global/default/aliased/unprefixed constructibility and identity, exact single-`$color` positional/named binding, list/map splats, transparent/RGB/HSL/HWB inputs, fractional alpha preservation, number typing, once-only source order, and the exact warning-free module versus global-built-in warning split. Callable-only module ownership does not widen the direct qualified-call surface. Missing/extra/unknown/duplicate/empty-splat and non-color/list/map/callable/space/module/modern-color/limit/allocation paths fail closed without CSS; `opacity` remains unavailable. The red gate stopped at direct 308/309 and focused 441/442 before final Debug/ReleaseSafe each passed 23/23 steps and 442/442 tests; direct evaluator coverage passes 309/309, full compiler modes pass 60/60 and 937/937, and Windows x64 GNU plus Linux ARM64 GNU evaluator cross-compiles pass in both modes. Native contract 12/12 plus check adds only `built-in-meta-color-alpha-function-invocation`; complete provider/reference/product matrices pass 151/151, package policy passes 6/6 plus check, runner selftest passes 59/59, and repository formatting, JavaScript/JSON syntax/parsing, and diff checks pass. Both native Sass rows remain private and production-unreachable, the release interlock stays closed, the SDK selector was removed, and no production or external state changed. | `98028ac` |
| `NSASS-011-META-COLOR-BLUE-FUNCTION-INVOCATION` | `VERIFIED` | Added provider-free native Zig invocation of distinct legacy-global and module-owned `sass:color` `blue` references through `meta.call()` for SCSS and indented Sass. Exact Dart Sass 1.101.0 probes establish global/default/aliased/unprefixed constructibility and identity, exact single-`$color` positional/named binding, list/map splats, transparent/RGB/HSL/HWB inputs, integer conversion rounding, number typing, once-only source order, and the distinct global-built-in/global-blue/module-blue deprecation sequence. Callable-only module ownership does not widen the direct qualified-call surface. Missing/extra/unknown/duplicate/empty-splat and non-color/list/map/callable/space/module/modern-color/limit/allocation paths fail closed without CSS; `alpha` remains unavailable. The red gate stopped at direct 306/307 and focused 439/440 before final Debug/ReleaseSafe each passed 23/23 steps and 440/440 tests; direct evaluator coverage passes 307/307, full compiler modes pass 60/60 and 935/935, and Windows x64 GNU plus Linux ARM64 GNU evaluator cross-compiles pass in both modes. Native contract 12/12 plus check adds only `built-in-meta-color-blue-function-invocation`; complete provider/reference/product matrices pass 151/151, package policy passes 6/6 plus check, runner selftest passes 59/59, and repository formatting, JavaScript/JSON syntax/parsing, and diff checks pass. Both native Sass rows remain private and production-unreachable, the release interlock stays closed, the SDK selector was removed, and no production or external state changed. | `916f570` |
| `NSASS-011-META-COLOR-GREEN-FUNCTION-INVOCATION` | `VERIFIED` | Added provider-free native Zig invocation of distinct legacy-global and module-owned `sass:color` `green` references through `meta.call()` for SCSS and indented Sass. Exact Dart Sass 1.101.0 probes establish global/default/aliased/unprefixed constructibility and identity, exact single-`$color` positional/named binding, list/map splats, transparent/RGB/HSL/HWB inputs, integer conversion rounding, number typing, once-only source order, and the distinct global-built-in/global-green/module-green deprecation sequence. Callable-only module ownership does not widen the direct qualified-call surface. Missing/extra/unknown/duplicate/empty-splat and non-color/list/map/callable/space/module/modern-color/limit/allocation paths fail closed without CSS; `blue` remains unavailable. The red gate stopped at direct 304/305 and focused 437/438 before final Debug/ReleaseSafe each passed 23/23 steps and 438/438 tests; direct evaluator coverage passes 305/305, full compiler modes pass 60/60 and 933/933, and Windows x64 GNU plus Linux ARM64 GNU evaluator cross-compiles pass in both modes. Native contract 12/12 plus check adds only `built-in-meta-color-green-function-invocation`; complete provider/reference/product matrices pass 151/151, package policy passes 6/6 plus check, runner selftest passes 59/59, and repository formatting, JavaScript/JSON syntax/parsing, and diff checks pass. Both native Sass rows remain private and production-unreachable, the release interlock stays closed, the SDK selector was removed, and no production or external state changed. | `9a8fb9f` |
| `NSASS-011-META-COLOR-RED-FUNCTION-INVOCATION` | `VERIFIED` | Added provider-free native Zig invocation of distinct legacy-global and module-owned `sass:color` `red` references through `meta.call()` for SCSS and indented Sass. Exact Dart Sass 1.101.0 probes establish global/default/aliased/unprefixed constructibility and identity, exact single-`$color` positional/named binding, list/map splats, transparent/RGB/HSL/HWB inputs, integer conversion rounding, number typing, once-only source order, and the distinct global-built-in/global-red/module-red deprecation sequence. Callable-only module ownership does not widen the direct qualified-call surface. Missing/extra/unknown/duplicate/empty-splat and non-color/list/map/callable/space/module/modern-color/limit/allocation paths fail closed without CSS; `green` remains unavailable. The red gate stopped at direct 302/303 and focused 435/436 before final Debug/ReleaseSafe each passed 23/23 steps and 436/436 tests; direct evaluator coverage passes 303/303, full compiler modes pass 60/60 and 931/931, and Windows x64 GNU plus Linux ARM64 GNU evaluator cross-compiles pass in both modes. Native contract 12/12 plus check adds only `built-in-meta-color-red-function-invocation`; complete provider/reference/product matrices pass 151/151, package policy passes 6/6 plus check, runner selftest passes 59/59, and repository formatting, JavaScript/JSON syntax/parsing, and diff checks pass. Both native Sass rows remain private and production-unreachable, the release interlock stays closed, the SDK selector was removed, and no production or external state changed. | `b6a3032` |
| `NSASS-011-META-COLOR-FUNCTION-INVOCATION` | `VERIFIED` | Added provider-free native Zig invocation of the global `color` reference through `meta.call()` for SCSS and indented Sass. Exact Dart Sass 1.101.0 reflection reports the callable in global and unprefixed scope and in default/aliased `sass:color` existence queries while module-owned lookup remains unavailable. Exact single-`$description` positional/named binding, list/map splats, all predefined spaces, `xyz-d65` normalization, unitless/percentage/static-variable channels, finite signed and out-of-range channels, alpha clamping, `none`, result typing, and once-only source order match fresh warning-free oracle bytes. The evaluated-description decoder reuses the direct constructor's color-space authority without allocation; deferred CSS values and invalid binding/value/unit/list/module/limit/allocation paths fail closed without CSS, while global `red` reference invocation remains unavailable. The red gate stopped at direct 300/301 and focused 433/434 before final Debug/ReleaseSafe each passed 23/23 steps and 434/434 tests; direct evaluator coverage passes 301/301, full compiler modes pass 60/60 and 929/929, and Windows x64 GNU plus Linux ARM64 GNU evaluator cross-compiles pass in both modes. Native contract 12/12 plus check adds only `built-in-meta-color-function-invocation`; complete provider/reference/product matrices pass 151/151, package policy passes 6/6 plus check, runner selftest passes 59/59, and repository formatting, JavaScript/JSON syntax/parsing, and diff checks pass. Both native Sass rows remain private and production-unreachable, the release interlock stays closed, the SDK selector was removed, and no production or external state changed. | `17f02a7` |
| `NSASS-011-META-COLOR-OKLCH-FUNCTION-INVOCATION` | `VERIFIED` | Added provider-free native Zig invocation of the global `oklch` reference through `meta.call()` for SCSS and indented Sass. Exact Dart Sass 1.101.0 reflection reports OKLCH in default and aliased `sass:color` existence queries while module-owned lookup and qualified calls remain unavailable; unprefixed import retains global identity. Single-`$channels` positional/named binding, list/map splats, unitless/percentage lightness and chroma, unitless/angle hue, alpha, clamping, normalization, `none`, result typing, and once-only source order match fresh oracle bytes without warnings. Static evaluated Oklch lists are decoded without allocation; deferred CSS values and invalid binding/value/unit/list/module/limit/allocation paths fail closed without CSS, while generic `color()` invocation remains unavailable. The red gate stopped at direct 298/299 and focused 431/432 before final Debug/ReleaseSafe each passed 23/23 steps and 432/432 tests; direct evaluator coverage passes 299/299, full compiler modes pass 60/60 and 927/927, and Windows x64 GNU plus Linux ARM64 GNU evaluator cross-compiles pass in both modes. Native contract 12/12 plus check adds only `built-in-meta-color-oklch-function-invocation`; complete provider/reference/product matrices pass 151/151, package policy passes 6/6 plus check, runner selftest passes 59/59, and repository formatting, JavaScript/JSON syntax/parsing, and diff checks pass. Both native Sass rows remain private and production-unreachable, the release interlock stays closed, the SDK selector was removed, and no production or external state changed. | `282ac11` |
| `NSASS-011-META-COLOR-OKLAB-FUNCTION-INVOCATION` | `VERIFIED` | Added provider-free native Zig invocation of the global `oklab` reference through `meta.call()` for SCSS and indented Sass. Exact Dart Sass 1.101.0 reflection reports Oklab in default and aliased `sass:color` existence queries while module-owned lookup and qualified calls remain unavailable; unprefixed import retains global identity. Single-`$channels` positional/named binding, list/map splats, unitless/percentage lightness and axes, negative axes, alpha, clamping, `none`, result typing, and once-only source order match fresh oracle bytes without warnings. Static evaluated Oklab lists are decoded without allocation; deferred CSS values and invalid binding/value/unit/list/module/limit/allocation paths fail closed without CSS, while `oklch` invocation remains unavailable. The red gate stopped at direct 296/297 and focused 429/430 before final Debug/ReleaseSafe each passed 23/23 steps and 430/430 tests; direct evaluator coverage passes 297/297, full compiler modes pass 60/60 and 925/925, and Windows x64 GNU plus Linux ARM64 GNU evaluator cross-compiles pass in both modes. Native contract 12/12 plus check adds only `built-in-meta-color-oklab-function-invocation`; complete provider/reference/product matrices pass 151/151, package policy passes 6/6 plus check, runner selftest passes 59/59, and repository formatting, JavaScript/JSON syntax/parsing, and diff checks pass. Both native Sass rows remain private and production-unreachable, the release interlock stays closed, the SDK selector was removed, and no production or external state changed. | `02c13b2` |
| `NSASS-011-META-COLOR-LCH-FUNCTION-INVOCATION` | `VERIFIED` | Added provider-free native Zig invocation of the global `lch` reference through `meta.call()` for SCSS and indented Sass. Exact Dart Sass 1.101.0 reflection reports LCH in default and aliased `sass:color` existence queries while module-owned lookup and qualified calls remain unavailable; unprefixed import retains global identity. Single-`$channels` positional/named binding, list/map splats, unitless/percentage lightness and chroma, unitless/angle hue, alpha, clamping, normalization, `none`, result typing, and once-only source order match fresh oracle bytes without warnings. Static evaluated LCH lists are decoded without allocation; deferred CSS values and invalid binding/value/unit/list/module/limit/allocation paths fail closed without CSS, while `oklab` invocation remains unavailable. The red gate stopped at direct 294/295 and focused 427/428 before final Debug/ReleaseSafe each passed 23/23 steps and 428/428 tests; direct evaluator coverage passes 295/295, full compiler modes pass 60/60 and 923/923, and Windows x64 GNU plus Linux ARM64 GNU evaluator cross-compiles pass in both modes. Native contract 12/12 plus check adds only `built-in-meta-color-lch-function-invocation`; complete provider/reference/product matrices pass 151/151, package policy passes 6/6 plus check, runner selftest passes 59/59, and repository formatting, JavaScript/JSON syntax/parsing, and diff checks pass. Both native Sass rows remain private and production-unreachable, the release interlock stays closed, the SDK selector was removed, and no production or external state changed. | `cef787d` |
| `NSASS-011-META-COLOR-LAB-FUNCTION-INVOCATION` | `VERIFIED` | Added provider-free native Zig invocation of the global `lab` reference through `meta.call()` for SCSS and indented Sass. Exact Dart Sass 1.101.0 reflection reports Lab in default and aliased `sass:color` existence queries while module-owned lookup and qualified calls remain unavailable; unprefixed import retains global identity. Single-`$channels` positional/named binding, list/map splats, unitless/percentage and negative channels, alpha, clamping, `none`, result typing, and once-only source order match fresh oracle bytes without warnings. Static evaluated Lab lists are decoded without allocation; deferred CSS values and invalid binding/value/unit/list/module/limit/allocation paths fail closed without CSS, while `lch` invocation remains unavailable. The red gate stopped at direct 292/293 and focused 425/426 before final Debug/ReleaseSafe each passed 23/23 steps and 426/426 tests; direct evaluator coverage passes 293/293, full compiler modes pass 60/60 and 921/921, and Windows x64 GNU plus Linux ARM64 GNU evaluator cross-compiles pass in both modes. Native contract 12/12 plus check adds only `built-in-meta-color-lab-function-invocation`; complete provider/reference/product matrices pass 151/151, package policy passes 6/6 plus check, runner selftest passes 59/59, and repository formatting, JavaScript/JSON syntax/parsing, and diff checks pass. Both native Sass rows remain private and production-unreachable, the release interlock stays closed, the SDK selector was removed, and no production or external state changed. | `ff4a17b` |
| `NSASS-011-META-COLOR-HWB-FUNCTION-INVOCATION` | `VERIFIED` | Added provider-free native Zig invocation of global and module-owned `sass:color` `hwb` references through `meta.call()` for SCSS and indented Sass. Global/module identity remains distinct; default, aliased, and unprefixed module references remain identical. Exact single-`$channels` binding, positional/named use, list/map splats, angle and percentage alpha channels, normalization, finite out-of-range channels, color typing, and once-only source order match fresh Dart Sass 1.101.0 at the strict-percentage, non-deferred boundary. The shared HWB constructor preserves out-of-range channels until canonical sum normalization and emits canonical hue-zero grayscale. Deprecated arbitrary-unit hue coercion, deferred calculations, `none`, arithmetic-shaped negative channels, and invalid binding/value/unit/list/limit/allocation cases expose no CSS. The focused red gate stopped at direct 289/291 and focused 422/424 before final Debug/ReleaseSafe each passed 23/23 steps and 424/424 tests; direct evaluator coverage passes 291/291, full compiler modes pass 60/60 and 919/919, and Windows x64 GNU plus Linux ARM64 GNU native preprocessor cross-compiles pass in both modes. Native contract 12/12 plus check adds only `built-in-meta-color-hwb-function-invocation`; complete provider/reference/product matrices pass 151/151, package policy passes 6/6 plus check, runner selftest passes 59/59, and repository formatting, JavaScript/JSON syntax/parsing, and diff checks pass. Both native Sass rows remain private and production-unreachable, the release interlock stays closed, the SDK selector was removed, and no production or external state changed. | `ec1b4eb` |
| `NSASS-011-META-COLOR-HSLA-FUNCTION-INVOCATION` | `VERIFIED` | Added provider-free native Zig invocation of the already-reflectable global `hsla` function reference through `meta.call()` for SCSS and indented Sass by reusing the exact bounded HSL invocation path. The reference remains global-only, preserves identity across unprefixed `sass:color` import, remains distinct from global `hsl`, emits no warning, and rejects module-qualified construction. Positional/named HSL channels, optional alpha, angle/percentage channels, percentage alpha, space/slash channel lists, list/map splats, omitted alpha, hue normalization, alpha clamping, out-of-range HSL preservation, color result typing, and once-only source order match fresh Dart Sass 1.101.0 bytes at the measured strict-percentage, non-deferred boundary. Deprecated unit coercions, deferred calculations, `none`, and invalid binding/value/unit/list/module/limit/allocation cases expose no CSS; global `hwb` invocation remains unavailable. The focused red gate stopped at direct 287/289 and focused 420/422 before final Debug/ReleaseSafe each passed 23/23 steps and 422/422 tests; direct evaluator coverage passes 289/289, full compiler modes pass 60/60 and 917/917, and Windows x64 GNU plus Linux ARM64 GNU native preprocessor cross-compiles pass in both modes. Native contract 12/12 plus check adds only `built-in-meta-color-hsla-function-invocation`; complete provider/reference/product matrices pass 151/151, package policy passes 6/6 plus check, runner selftest passes 59/59, and repository formatting, JavaScript/JSON syntax/parsing, and diff checks pass. Both native Sass rows remain private and production-unreachable, the release interlock stays closed, the SDK selector was removed, and no production or external state changed. | `223f4a8` |
| `NSASS-011-META-COLOR-HSL-FUNCTION-INVOCATION` | `VERIFIED` | Added provider-free native Zig invocation of the already-reflectable global `hsl` function reference through `meta.call()` for SCSS and indented Sass. The reference remains global-only, preserves identity across unprefixed `sass:color` import, emits no warning, and rejects module-qualified construction. Positional/named HSL channels, optional alpha, angle/percentage channels, space/slash channel lists, list/map splats, hue normalization, alpha clamping, out-of-range HSL preservation, color result typing, and once-only source order match fresh Dart Sass 1.101.0 bytes at the measured strict-percentage, non-deferred boundary. Deprecated unit coercions, deferred calculations, `none`, and invalid binding/value/unit/list/module/limit/allocation cases expose no CSS; global `hsla` invocation remains unavailable. The focused red gate stopped at direct 285/287 and focused 418/420 before final Debug/ReleaseSafe each passed 23/23 steps and 420/420 tests; direct evaluator coverage passes 287/287, full compiler modes pass 60/60 and 915/915, and Windows x64 GNU plus Linux ARM64 GNU native preprocessor cross-compiles pass in both modes. Native contract 12/12 plus check adds only `built-in-meta-color-hsl-function-invocation`; complete provider/reference/product matrices pass 151/151, package policy passes 6/6 plus check, runner selftest passes 59/59, and repository formatting, JavaScript/JSON syntax/parsing, and diff checks pass. Both native Sass rows remain private and production-unreachable, the release interlock stays closed, the SDK selector was removed, and no production or external state changed. | `e467854` |
| `NSASS-011-META-COLOR-RGBA-FUNCTION-INVOCATION` | `VERIFIED` | Added provider-free native Zig invocation of the already-reflectable global `rgba` function reference through `meta.call()` for SCSS and indented Sass. The reference remains global-only, preserves identity across unprefixed `sass:color` import, emits no warning, and rejects module-qualified construction. Positional/named channels, omitted/explicit alpha, percentages, legacy RGB/HSL/HWB color-plus-alpha overloads, space/slash channel lists, list/map splats, clamping, result typing, and once-only source order match fresh Dart Sass 1.101.0 bytes at the measured non-deferred boundary. Invalid binding, value, unit, overload, callable, module, argument-limit, and allocation-failure cases expose no CSS; deferred calculations remain fail-closed and global `hsl` invocation remains unavailable. The focused red gate stopped at direct 283/285 and focused 416/418 before final Debug/ReleaseSafe each passed 23/23 steps and 418/418 tests; direct evaluator coverage passes 285/285, full compiler modes pass 60/60 and 913/913, and Windows x64 GNU plus Linux ARM64 GNU native preprocessor cross-compiles pass in both modes. Native contract 12/12 plus check adds only `built-in-meta-color-rgba-function-invocation`; complete provider/reference/product matrices pass 151/151, package policy passes 6/6 plus check, runner selftest passes 59/59, and repository formatting, JavaScript/JSON syntax/parsing, and diff checks pass. Both native Sass rows remain private and production-unreachable, the release interlock stays closed, the SDK selector was removed, and no production or external state changed. | `5b54b41` |
| `NSASS-011-META-COLOR-RGB-FUNCTION-INVOCATION` | `VERIFIED` | Added provider-free native Zig invocation of the already-reflectable global `rgb` function reference through `meta.call()` for SCSS and indented Sass. The reference remains global-only, preserves identity across unprefixed `sass:color` import, emits no warning, and rejects module-qualified construction. Positional/named RGB channels, percentage channels, optional alpha, legacy RGB/HSL/HWB color-plus-alpha overloads, space/slash channel lists, list/map splats, clamping, color result typing, and once-only source order match fresh Dart Sass 1.101.0 oracle bytes at the measured non-deferred boundary. Invalid binding, value, unit, overload, calculation, callable, argument-limit, and allocation-failure cases expose no CSS; global `rgba` invocation remains unavailable. The focused red gate stopped at direct 281/283 and focused 414/416 before final Debug/ReleaseSafe each passed 23/23 steps and 416/416 tests; direct evaluator coverage passes 283/283, full compiler modes pass 60/60 and 911/911, and Windows x64 GNU plus Linux ARM64 GNU native preprocessor cross-compiles pass in both modes. Native contract 12/12 plus check adds only `built-in-meta-color-rgb-function-invocation`; complete provider/reference/product matrices pass 151/151, package policy passes 6/6 plus check, runner selftest passes 59/59, and repository formatting, JavaScript/JSON syntax and parsing, and diff checks pass. Both native Sass rows remain private and production-unreachable, the release interlock stays closed, the SDK selector was removed, and no production or external state changed. | `16f0b14` |
| `NSASS-011-META-COLOR-SCALE-FUNCTION-INVOCATION` | `VERIFIED` | Added provider-free native Zig invocation of the exact already-admitted module-owned `sass:color` `scale` function reference through `meta.call()` for SCSS and indented Sass, plus independently measured legacy-global `scale-color` reflection. Default, custom, aliased, and unprefixed module origins retain one identity without warnings; global and module references remain distinct, and legacy invocation preserves the normalized warning. One positional `$color`, measured optional named channels and `$space`, list/map splats, positive/negative RGB, HSL, HWB, Lab, LCH, display-p3, alpha, unchanged identity, named reordering, result type/inspection, and once-only source order match fresh Dart Sass 1.101.0 oracle bytes. Invalid binding/value/space/channel/calculation/function, percentage-unit/range, hue, argument-limit, and allocation-failure cases expose no CSS; reflected global `rgb` invocation remains unavailable. The focused red gate stopped at direct 278/281 and focused 411/414 before final Debug/ReleaseSafe each passed 23/23 steps and 414/414 tests; direct evaluator coverage passes 281/281, full compiler modes pass 60/60 and 909/909, and Windows x64 GNU plus Linux ARM64 GNU native preprocessor cross-compiles pass in both modes. Native contract 12/12 plus check adds only `built-in-meta-color-scale-function-invocation`; complete provider/reference/product matrices pass 151/151, package policy passes 6/6 plus check, runner selftest passes 59/59, and repository formatting, JavaScript/JSON syntax, and diff checks pass. Both native Sass rows remain private and production-unreachable, the release interlock stays closed, the SDK selector and validation caches were removed, and no production or external state changed. | `c62a117` |
| `NSASS-011-META-COLOR-CHANGE-FUNCTION-INVOCATION` | `VERIFIED` | Added provider-free native Zig invocation of the exact already-admitted module-owned `sass:color` `change` function reference through `meta.call()` for SCSS and indented Sass, plus independently measured legacy-global `change-color` reflection. Default, custom, aliased, and unprefixed module origins retain one identity without warnings; global and module references remain distinct, and legacy invocation preserves the normalized warning. One positional `$color`, measured optional named channels and `$space`, list/map splats, RGB unitless/percentage values, HSL, HWB, Lab, LCH, display-p3, alpha, omitted channels, named reordering, result type/inspection, and once-only source order match fresh Dart Sass 1.101.0 oracle bytes. Invalid binding/value/space/channel/calculation/function, alpha-range, argument-limit, and allocation-failure cases expose no CSS; `color.scale` remains explicitly unavailable through reflection. The focused red gate stopped at direct 276/278 and focused 409/411 before final Debug/ReleaseSafe each passed 23/23 steps and 411/411 tests; direct evaluator coverage passes 278/278, full compiler modes pass 60/60 and 906/906, and Windows x64 GNU plus Linux ARM64 GNU evaluator cross-compiles pass in both modes. Native contract 12/12 plus check adds only `built-in-meta-color-change-function-invocation`; complete provider/reference/product matrices pass 151/151, package policy passes 6/6 plus check, runner selftest passes 59/59, and repository formatting, JavaScript/JSON syntax, and diff checks pass. Both native Sass rows remain private and production-unreachable, the release interlock stays closed, the SDK selector and validation caches were removed, and no production or external state changed. | `94c5d3a` |
| `NSASS-011-META-COLOR-ADJUST-FUNCTION-INVOCATION` | `VERIFIED` | Added provider-free native Zig invocation of the exact already-admitted module-owned `sass:color` `adjust` function reference through `meta.call()` for SCSS and indented Sass, plus independently measured legacy-global `adjust-color` reflection. Default, custom, aliased, and unprefixed module origins retain one identity without warnings; global and module references remain distinct, and legacy invocation preserves the normalized warning. One positional `$color`, measured optional named channels and `$space`, list/map splats, RGB unitless/percentage values, HSL, HWB, Lab, LCH, display-p3, alpha, omitted channels, named reordering, result type/inspection, and once-only source order match fresh Dart Sass 1.101.0 oracle bytes. Invalid binding/value/space/channel/calculation/function, argument-limit, and allocation-failure cases expose no CSS; `color.change` remains explicitly unavailable through reflection. The focused red gate stopped at direct 273/275 and focused 406/408 before final Debug/ReleaseSafe each passed 23/23 steps and 408/408 tests; direct evaluator coverage passes 275/275, full compiler modes pass 60/60 and 903/903, and Windows x64 GNU plus Linux ARM64 GNU evaluator cross-compiles pass in both modes. Native contract 12/12 plus check adds only `built-in-meta-color-adjust-function-invocation`; complete provider/reference/product matrices pass 151/151, package policy passes 6/6 plus check, runner selftest passes 59/59, and repository formatting, JavaScript/JSON syntax, and diff checks pass. Both native Sass rows remain private and production-unreachable, the release interlock stays closed, the SDK selector was removed, and no production or external state changed. | `8d3450a` |
| `NSASS-011-META-STRING-LOWER-CASE-FUNCTION-INVOCATION` | `VERIFIED` | Added provider-free native Zig invocation of the exact already-admitted module-owned `sass:string` `to-lower-case` function reference through `meta.call()` for SCSS and indented Sass, plus independently measured legacy-global reflection. Default, custom, underscore-normalized, and unprefixed module origins retain one identity without warnings; global and module references remain distinct despite identical inspection, and legacy invocation preserves the normalized warning. Positional/named `$string`, one-item list/map splats, ASCII-only conversion, preserved Unicode/combining/ZWJ bytes, quoted/unquoted values, hexadecimal/simple escapes, empty strings, interpolation, result quoting/type, and once-only source order match fresh Dart Sass 1.101.0 oracle bytes. Invalid binding/value/calculation, temporary, argument-limit, and allocation-failure cases expose no CSS; direct `string.to-lower-case()` remains calculation-closed while `color.adjust` remains explicitly unavailable through reflection. The focused red gate stopped at direct 269/272 and focused 402/405 before final Debug/ReleaseSafe each passed 23/23 steps and 405/405 tests; direct evaluator coverage passes 272/272, full compiler modes pass 60/60 and 900/900, and Windows x64 GNU plus Linux ARM64 GNU evaluator cross-compiles pass in both modes. Native contract 12/12 plus check adds only `built-in-meta-string-lower-case-function-invocation`; complete provider/reference/product matrices pass 151/151, package policy passes 6/6 plus check, runner selftest passes 59/59, and repository formatting, JavaScript/JSON syntax, and diff checks pass. Both native Sass rows remain private and production-unreachable, the release interlock stays closed, the SDK selector was removed, and no production or external state changed. | `6678b6b` |
| `NSASS-011-META-STRING-UPPER-CASE-FUNCTION-INVOCATION` | `VERIFIED` | Added provider-free native Zig invocation of the exact already-admitted module-owned `sass:string` `to-upper-case` function reference through `meta.call()` for SCSS and indented Sass, plus independently measured legacy-global reflection. Default, custom, underscore-normalized, and unprefixed module origins retain one identity without warnings; global and module references remain distinct despite identical inspection, and legacy invocation preserves the normalized warning. Positional/named `$string`, one-item list/map splats, ASCII-only conversion, preserved Unicode/combining/ZWJ bytes, quoted/unquoted values, hexadecimal/simple escapes, empty strings, interpolation, result quoting/type, and once-only source order match fresh Dart Sass 1.101.0 oracle bytes. Invalid binding/value/calculation, temporary, argument-limit, and allocation-failure cases expose no CSS; the calculation guard also closes direct `string.to-upper-case()` while `string.to-lower-case` remains explicitly unavailable through reflection. The focused red gate stopped at direct 265/269 and focused 398/402 before final Debug/ReleaseSafe each passed 23/23 steps and 402/402 tests; direct evaluator coverage passes 269/269, full compiler modes pass 60/60 and 897/897, and Windows x64 GNU plus Linux ARM64 GNU evaluator cross-compiles pass in both modes. Native contract 12/12 plus check adds only `built-in-meta-string-upper-case-function-invocation`; complete provider/reference/product matrices pass 151/151, package policy passes 6/6 plus check, runner selftest passes 59/59, and repository formatting, JavaScript/JSON syntax, and diff checks pass. Both native Sass rows remain private and production-unreachable, the release interlock stays closed, the SDK selector was removed, and no production or external state changed. | `cd8fde7` |
| `NSASS-011-META-STRING-INSERT-FUNCTION-INVOCATION` | `VERIFIED` | Added provider-free native Zig invocation of the exact already-admitted module-owned `sass:string` `insert` function reference through `meta.call()` for SCSS and indented Sass, plus independently measured legacy-global `str-insert` reflection. Default, custom, and unprefixed module origins retain one identity without warnings; global and module references remain distinct, and legacy invocation preserves the normalized warning. Positional/named `$string`/`$insert`/`$index`, three-item list/map splats, ASCII and Unicode scalar insertion, combining/ZWJ sequences, quoted/unquoted values, hexadecimal/simple escapes, mixed quoting, negative/zero/out-of-range clamping, empty strings, interpolation, source-string result quoting/type, and once-only source order match fresh Dart Sass 1.101.0 oracle bytes. Invalid binding/value/index/calculation, temporary, argument-limit, and allocation-failure cases expose no CSS; operand-specific calculation guards also close direct `string.insert()` while `string.to-upper-case` remains explicitly unavailable through reflection. The focused red gate stopped at direct 262/266 and focused 395/399 before final Debug/ReleaseSafe each passed 23/23 steps and 399/399 tests; direct evaluator coverage passes 266/266, full compiler modes pass 60/60 and 894/894, and Windows x64 GNU plus Linux ARM64 GNU evaluator cross-compiles pass in both modes. Native contract 12/12 plus check adds only `built-in-meta-string-insert-function-invocation`; complete provider/reference/product matrices pass 151/151, package policy passes 6/6 plus check, runner selftest passes 59/59, and repository formatting, JavaScript/JSON syntax, and diff checks pass. Both native Sass rows remain private and production-unreachable, the release interlock stays closed, the SDK selector was removed, and no production or external state changed. | `1b55197` |
| `NSASS-011-META-STRING-SLICE-FUNCTION-INVOCATION` | `VERIFIED` | Added provider-free native Zig invocation of the exact already-admitted module-owned `sass:string` `slice` function reference through `meta.call()` for SCSS and indented Sass, plus independently measured legacy-global `str-slice` reflection. Default, custom, and unprefixed module origins retain one identity without warnings; global and module references remain distinct, and legacy invocation preserves the normalized warning. Positional/named `$string`/`$start-at`/optional `$end-at`, three-item list/map splats, ASCII and Unicode scalar slicing, combining/ZWJ sequences, quoted/unquoted values, hexadecimal/simple escapes, omitted-end semantics, negative/zero/reversed/out-of-range clamping, interpolation, result quoting/type, and once-only source order match fresh Dart Sass 1.101.0 oracle bytes. Invalid binding/value/index/calculation, temporary, argument-limit, and allocation-failure cases expose no CSS; the calculation-string guard also closes direct `string.slice()` while `string.insert` remains explicitly unavailable through reflection. The focused red gate stopped at direct 259/263 and focused 392/396 before final Debug/ReleaseSafe each passed 23/23 steps and 396/396 tests; direct evaluator coverage passes 263/263, full compiler modes pass 60/60 and 891/891, and Windows x64 GNU plus Linux ARM64 GNU evaluator cross-compiles pass in both modes. Native contract 12/12 plus check adds only `built-in-meta-string-slice-function-invocation`; complete provider/reference/product matrices pass 151/151, package policy passes 6/6 plus check, runner selftest passes 59/59, and repository formatting, JavaScript/JSON syntax, and diff checks pass. Both native Sass rows remain private and production-unreachable, the release interlock stays closed, the SDK selector was removed, and no production or external state changed. | `591a5f3` |
| `NSASS-011-META-STRING-INDEX-FUNCTION-INVOCATION` | `VERIFIED` | Added provider-free native Zig invocation of the exact already-admitted module-owned `sass:string` `index` function reference through `meta.call()` for SCSS and indented Sass, plus independently measured legacy-global `str-index` reflection. Default, custom, and unprefixed module origins retain one identity without warnings; global and module references remain distinct, and legacy invocation preserves the normalized warning. Positional/named `$string`/`$substring`, two-item list/map splats, ASCII and Unicode scalar positions, combining/ZWJ sequences, quoted/unquoted values, hexadecimal/simple escapes, empty substrings, null misses, interpolation, number/null typing, and once-only source order match fresh Dart Sass 1.101.0 oracle bytes. Invalid binding/value/calculation in either operand, temporary, argument-limit, and allocation-failure cases expose no CSS; operand-specific guards also close direct `string.index()` calculations while `string.slice` remains explicitly unavailable through reflection. The focused red gate stopped at direct 256/260 and focused 389/393 before final Debug/ReleaseSafe each passed 23/23 steps and 393/393 tests; direct evaluator coverage passes 260/260, full compiler modes pass 60/60 and 888/888, and Windows x64 GNU plus Linux ARM64 GNU evaluator cross-compiles pass in both modes. Native contract 12/12 plus check adds only `built-in-meta-string-index-function-invocation`; complete provider/reference/product matrices pass 151/151, package policy passes 6/6 plus check, runner selftest passes 59/59, and repository formatting, JavaScript/JSON syntax, and diff checks pass. Both native Sass rows remain private and production-unreachable, the release interlock stays closed, the SDK selector was removed, and no production or external state changed. | `cc9a58a` |
| `NSASS-011-META-STRING-LENGTH-FUNCTION-INVOCATION` | `VERIFIED` | Added provider-free native Zig invocation of the exact already-admitted module-owned `sass:string` `length` function reference through `meta.call()` for SCSS and indented Sass, plus independently measured legacy-global `str-length` reflection. Default, custom, and unprefixed module origins retain one identity without warnings; legacy string length, global list length, and module string length remain distinct despite inspected-name collisions, and only the legacy invocation preserves the normalized warning. Positional/named `$string`, one-item list/map splats, ASCII and Unicode scalars, combining/ZWJ sequences, quoted/unquoted values, hexadecimal and simple escapes, empty strings, interpolation, numeric typing, and once-only source order match fresh Dart Sass 1.101.0 oracle bytes. Invalid binding/value/calculation, temporary, argument-limit, and allocation-failure cases expose no CSS; a length-only guard also closes direct `string.length()` calculations, and bounded top-level escape splitting preserves hexadecimal terminators without regressing escaped plain-CSS functions. The focused red gate stopped at direct 254/257 and focused 387/390 before final Debug/ReleaseSafe each passed 23/23 steps and 390/390 tests; direct evaluator coverage passes 257/257, full compiler modes pass 60/60 and 885/885, and Windows x64 GNU plus Linux ARM64 GNU evaluator cross-compiles pass in both modes. Native contract 12/12 plus check adds only `built-in-meta-string-length-function-invocation`; complete provider/reference/product matrices pass 151/151, package policy passes 6/6 plus check, runner selftest passes 59/59, and repository formatting, JavaScript/JSON syntax, and diff checks pass. Both native Sass rows remain private and production-unreachable, `string.index` remains explicitly unavailable, the release interlock stays closed, the SDK selector was removed, and no production or external state changed. | `0853efb` |
| `NSASS-011-META-STRING-UNQUOTE-FUNCTION-INVOCATION` | `VERIFIED` | Added provider-free native Zig invocation of the exact already-admitted module-owned `sass:string` `unquote` function reference through `meta.call()` for SCSS and indented Sass, plus independently measured legacy-global `unquote` reflection. Default, custom, and unprefixed module origins retain one identity without warnings; global and module references remain distinct despite identical inspected spelling, and legacy invocation preserves the normalized warning. Positional/named `$string`, one-item list/map splats, quoted/unquoted strings, escaped-space normalization, interpolation, exact unquoted serialization, result typing, and once-only source order match fresh Dart Sass 1.101.0 oracle bytes. Invalid binding/value/calculation, invalid generated CSS, temporary, argument-limit, and allocation-failure cases expose no CSS; the calculation guard also closes direct `string.unquote()` while `string.length` remains explicitly unavailable through reflection. Dart Sass's invalid `foo"bar` quote-removal output is deliberately rejected by the recovery-disabled ZigCSS core. The focused red gate stopped at direct 252/254 and focused 385/387 before final Debug/ReleaseSafe each passed 23/23 steps and 387/387 tests; direct evaluator coverage passes 254/254, full compiler modes pass 60/60 and 882/882, and Windows x64 GNU plus Linux ARM64 GNU evaluator cross-compiles pass in both modes. Native contract 12/12 plus check adds only `built-in-meta-string-unquote-function-invocation`; complete provider/reference/product matrices pass 151/151, package policy passes 6/6 plus check, runner selftest passes 59/59, and repository formatting, JavaScript/JSON syntax, and diff checks pass. Both native Sass rows remain private and production-unreachable, the release interlock stays closed, the SDK selector was removed, and no production or external state changed. | `33bd709` |
| `NSASS-011-META-STRING-QUOTE-FUNCTION-INVOCATION` | `VERIFIED` | Added provider-free native Zig invocation of the exact already-admitted module-owned `sass:string` `quote` function reference through `meta.call()` for SCSS and indented Sass, plus independently measured legacy-global `quote` reflection. Default, custom, and unprefixed module origins retain one identity without warnings; global and module references remain distinct despite identical inspected spelling, and legacy invocation preserves the normalized warning. Positional/named `$string`, one-item list/map splats, quoted/unquoted strings, escaped content, interpolation, exact quoted serialization, result typing, and once-only source order match fresh Dart Sass 1.101.0 oracle bytes. Invalid binding/value/calculation, temporary, argument-limit, and allocation-failure cases expose no CSS; a quote-only guard also closes the direct `string.quote()` calculation sibling while `string.unquote` remains explicitly unavailable through reflection. The focused red gate stopped at direct 248/251 and focused 381/384 before the calculation mismatch was captured and closed; final Debug/ReleaseSafe each passed 23/23 steps and 384/384 tests, direct evaluator coverage passes 251/251, full compiler modes pass 60/60 and 879/879, and Windows x64 GNU plus Linux ARM64 GNU evaluator cross-compiles pass in both modes. Native contract 12/12 plus check adds only `built-in-meta-string-quote-function-invocation`; complete provider/reference/product matrices pass 151/151, package policy passes 6/6 plus check, runner selftest passes 59/59, and repository formatting, JavaScript/JSON syntax, and diff checks pass. Both native Sass rows remain private and production-unreachable, the release interlock stays closed, the SDK selector was removed, and no production or external state changed. | `4428e95` |
| `NSASS-011-META-SELECTOR-REPLACE-FUNCTION-INVOCATION` | `VERIFIED` | Added provider-free native Zig invocation of the exact already-admitted module-owned `sass:selector` `replace` function reference through `meta.call()` for SCSS and indented Sass, plus independently measured legacy-global `selector-replace` reflection. Default, custom, and unprefixed module origins retain one identity without warnings; global `replace` remains false without an unprefixed import, legacy `selector-replace` remains true, global and module references remain distinct, and legacy invocation preserves the normalized warning. Positional/named `$selector`/`$original`/`$replacement`, list/map splats, string/existing-selector/list values, interpolation, replacement-only results, exact serialization, and once-only source order match fresh Dart Sass 1.101.0 oracle bytes; extend's adjacent `$extendee`/`$extender` names remain invalid. Invalid binding/value/parent/malformed/unsupported-native-complex-replacement, selector-count/byte, temporary, argument-limit, and allocation-failure cases expose no CSS. The focused red gate stopped at direct 244/248 and focused 377/381 before Debug/ReleaseSafe each passed 23/23 steps and 381/381 tests; direct evaluator coverage passes 248/248, full compiler modes pass 60/60 and 876/876, and Windows x64 GNU plus Linux ARM64 GNU evaluator cross-compiles pass in both modes. Native contract 12/12 plus check adds only `built-in-meta-selector-replace-function-invocation`; complete provider/reference/product matrices pass 151/151, package policy passes 6/6 plus check, runner selftest passes 59/59, and repository formatting, JavaScript/JSON syntax, and diff checks pass. Both native Sass rows remain private and production-unreachable, the release interlock stays closed, the SDK selector was removed, and no production or external state changed. | `98d4bc3` |
| `NSASS-011-META-SELECTOR-EXTEND-FUNCTION-INVOCATION` | `VERIFIED` | Added provider-free native Zig invocation of the exact already-admitted module-owned `sass:selector` `extend` function reference through `meta.call()` for SCSS and indented Sass, plus independently measured legacy-global `selector-extend` reflection. Default, custom, and unprefixed module origins retain one identity without warnings; global `extend` remains false without an unprefixed import, legacy `selector-extend` remains true, global and module references remain distinct, and legacy invocation preserves the normalized warning. Positional/named `$selector`/`$extendee`/`$extender`, list/map splats, string/existing-selector/list values, interpolation, retained-original extension results, exact serialization, and once-only source order match fresh Dart Sass 1.101.0 oracle bytes. Invalid binding/value/parent/malformed/unsupported-native-extension, selector-count/byte, temporary, argument-limit, and allocation-failure cases expose no CSS; module-owned `selector.replace` remains explicitly unavailable. The focused red gate stopped at direct 241/245 and focused 374/378 before Debug/ReleaseSafe each passed 23/23 steps and 378/378 tests; direct evaluator coverage passes 245/245, full compiler modes pass 60/60 and 873/873, and Windows x64 GNU plus Linux ARM64 GNU evaluator cross-compiles pass in both modes. Native contract 12/12 plus check adds only `built-in-meta-selector-extend-function-invocation`; complete provider/reference/product matrices pass 151/151, package policy passes 6/6 plus check, runner selftest passes 59/59, and repository formatting, JavaScript/JSON syntax, and diff checks pass. Both native Sass rows remain private and production-unreachable, the release interlock stays closed, the SDK selector was removed, and no production or external state changed. | `bff2eed` |
| `NSASS-011-META-SELECTOR-NEST-FUNCTION-INVOCATION` | `VERIFIED` | Added provider-free native Zig invocation of the exact already-admitted module-owned `sass:selector` `nest` function reference through `meta.call()` for SCSS and indented Sass, plus independently measured legacy-global `selector-nest` reflection. Default, custom, and unprefixed module origins retain one identity without warnings; global `nest` remains false without an unprefixed import, legacy `selector-nest` remains true, global and module references remain distinct, and legacy invocation preserves the normalized warning. One-or-more positional arguments, list splats, string/existing-selector/list values, interpolation, Cartesian lists, repeated/chained parent substitution, leading combinators, exact serialization, and once-only source order match fresh Dart Sass 1.101.0 oracle bytes. Keyword/map-splat and invalid selector/value cases plus selector-count/byte, temporary, argument-limit, and allocation-failure paths expose no CSS; module-owned `selector.extend` and `selector.replace` remain explicitly unavailable. The focused red gate stopped at direct 238/242 and focused 371/375 before Debug/ReleaseSafe each passed 23/23 steps and 375/375 tests; direct evaluator coverage passes 242/242, full compiler modes pass 60/60 and 870/870, and Windows x64 GNU plus Linux ARM64 GNU evaluator cross-compiles pass in both modes. Native contract 12/12 plus check adds only `built-in-meta-selector-nest-function-invocation`; complete provider/reference/product matrices pass 151/151, package policy passes 6/6 plus check, runner selftest passes 59/59, and repository formatting, JavaScript/JSON syntax, and diff checks pass. Both native Sass rows remain private and production-unreachable, the release interlock stays closed, the SDK selector was removed, and no production or external state changed. | `955d44b` |
| `NSASS-011-META-SELECTOR-APPEND-FUNCTION-INVOCATION` | `VERIFIED` | Added provider-free native Zig invocation of the exact already-admitted module-owned `sass:selector` `append` function reference through `meta.call()` for SCSS and indented Sass. Default, custom, and unprefixed module origins retain one identity without warnings; global `append` remains the distinct legacy list callable and preserves its normalized warning. One-or-more positional arguments, list splats, string/existing-selector/list values, interpolation, variadic composition, and once-only source order match fresh Dart Sass 1.101.0 oracle bytes. Keyword/map-splat and invalid selector/value cases plus selector-count/byte, temporary, argument-limit, and allocation-failure paths expose no CSS; module-owned `selector.nest` remains explicitly unavailable. The focused red gate stopped at direct 235/239 and focused 368/372 before Debug/ReleaseSafe each passed 23/23 steps and 372/372 tests; direct evaluator coverage passes 239/239, full compiler modes pass 60/60 and 867/867, and Windows x64 GNU plus Linux ARM64 GNU evaluator cross-compiles pass in both modes. Native contract 12/12 plus check adds only `built-in-meta-selector-append-function-invocation`; complete provider/reference/product matrices pass 151/151, package policy passes 6/6 plus check, runner selftest passes 59/59, and repository formatting, JavaScript/JSON syntax, and diff checks pass. Both native Sass rows remain private and production-unreachable, the release interlock stays closed, the SDK selector was removed, and no production or external state changed. | `6adfbcc` |
| `NSASS-011-META-SELECTOR-UNIFY-FUNCTION-INVOCATION` | `VERIFIED` | Added provider-free native Zig invocation of the exact already-admitted module-owned `sass:selector` `unify` function reference through `meta.call()` for SCSS and indented Sass, plus independently measured legacy-global `selector-unify` reflection. Default, custom, and unprefixed module origins retain one identity without warnings; global `unify` remains false without an unprefixed import, legacy `selector-unify` remains true, global and module references remain distinct, and legacy invocation preserves the normalized warning. Positional/named `$selector1`/`$selector2`, list/map splats, string/existing-selector/list values, interpolation, list/null results, exact serialization, and once-only source order match fresh Dart Sass 1.101.0 oracle bytes. Invalid binding/value/parent/malformed/unsupported-native-unification, selector-count/byte, temporary, argument-limit, and allocation-failure cases expose no CSS; module-owned `selector.append` remains explicitly unavailable. The focused red gate stopped at direct 232/236 and focused 365/369 before Debug/ReleaseSafe each passed 23/23 steps and 369/369 tests; direct evaluator coverage passes 236/236, full compiler modes pass 60/60 and 864/864, and Windows x64 GNU plus Linux ARM64 GNU evaluator cross-compiles pass in both modes. Native contract 12/12 plus check adds only `built-in-meta-selector-unify-function-invocation`; complete provider/reference/product matrices pass 151/151, package policy passes 6/6 plus check, runner selftest passes 59/59, and repository formatting, JavaScript/JSON syntax, and diff checks pass. Both native Sass rows remain private and production-unreachable, the release interlock stays closed, the SDK selector was removed, and no production or external state changed. | `85c7dfc` |
| `NSASS-011-META-SELECTOR-IS-SUPERSELECTOR-FUNCTION-INVOCATION` | `VERIFIED` | Added provider-free native Zig invocation of the exact already-admitted module-owned `sass:selector` `is-superselector` function reference through `meta.call()` for SCSS and indented Sass, plus independently measured legacy-global reflection. Default, custom, and unprefixed module origins retain one identity without warnings; global reflection remains true, global and module references remain distinct, and legacy invocation preserves the normalized warning. Positional/named `$super`/`$sub`, list/map splats, string/existing-selector/list values, interpolation, true/false relation results, and once-only source order match fresh Dart Sass 1.101.0 oracle bytes. Invalid binding/value/parent/malformed/unsupported-relation, selector-count/byte, temporary, argument-limit, and allocation-failure cases expose no CSS; module-owned `selector.unify` remains explicitly unavailable. The focused red gate stopped at direct 230/233 and focused 363/366 before Debug/ReleaseSafe each passed 23/23 steps and 366/366 tests; direct evaluator coverage passes 233/233, full compiler modes pass 60/60 and 861/861, and Windows x64 GNU plus Linux ARM64 GNU evaluator cross-compiles pass in both modes. Native contract 12/12 plus check adds only `built-in-meta-selector-is-superselector-function-invocation`; complete provider/reference/product matrices pass 151/151, package policy passes 6/6 plus check, runner selftest passes 59/59, and repository formatting, JavaScript/JSON syntax, and diff checks pass. Both native Sass rows remain private and production-unreachable, the release interlock stays closed, the SDK selector was removed, and no production or external state changed. | `a4ad372` |
| `NSASS-011-META-SELECTOR-SIMPLE-SELECTORS-FUNCTION-INVOCATION` | `VERIFIED` | Added provider-free native Zig invocation of the exact already-admitted module-owned `sass:selector` `simple-selectors` function reference through `meta.call()` for SCSS and indented Sass, plus independently measured legacy-global reflection. Default, custom, and unprefixed module origins retain one identity without warnings; global reflection remains true, global and module references remain distinct, and legacy invocation preserves the normalized warning. Positional/named `$selector`, list/map splats, string/existing-selector values, interpolation, simple-selector ordering and serialization, exact item types, and once-only source order match fresh Dart Sass 1.101.0 oracle bytes. Invalid binding/value/complex/list/parent-selector, selector-count/byte, temporary, argument-limit, and allocation-failure cases expose no CSS; module-owned `selector.is-superselector` remains explicitly unavailable. The focused red gate stopped at 359/363 before Debug/ReleaseSafe each passed 23/23 steps and 363/363 tests; direct evaluator coverage passes 230/230, full compiler modes pass 60/60 and 858/858, and Windows x64 GNU plus Linux ARM64 GNU evaluator cross-compiles pass in both modes. Native contract 12/12 plus check adds only `built-in-meta-selector-simple-selectors-function-invocation`; complete provider/reference/product matrices pass 151/151, package policy passes 6/6 plus check, runner selftest passes 59/59, and repository formatting, JavaScript/JSON syntax, and diff checks pass. Both native Sass rows remain private and production-unreachable, the release interlock stays closed, the SDK selector was removed, and no production or external state changed. | `964d216` |
| `NSASS-011-META-SELECTOR-PARSE-FUNCTION-INVOCATION` | `VERIFIED` | Added provider-free native Zig invocation of the exact already-admitted module-owned `sass:selector` `parse` function reference through `meta.call()` for SCSS and indented Sass. Default, custom, and unprefixed module origins retain one identity without warnings; global reflection remains false and global lookup fails. Positional/named `$selector`, list/map splats, string/list/existing-selector values, interpolation, normalized serialization, leading combinators, and once-only source order match fresh Dart Sass 1.101.0 oracle bytes. Invalid binding/value/parent-selector, selector-count/byte, temporary, argument-limit, and allocation-failure cases expose no CSS; module-owned `selector.simple-selectors` remains explicitly unavailable. The focused red gate stopped at 357/360 before Debug/ReleaseSafe each passed 23/23 steps and 360/360 tests; direct evaluator coverage passes 227/227, full compiler modes pass 60/60 and 855/855, and Windows x64 GNU plus Linux ARM64 GNU evaluator cross-compiles pass in both modes. Native contract 12/12 plus check adds only `built-in-meta-selector-parse-function-invocation`; complete provider/reference/product matrices pass 151/151, package policy passes 6/6 plus check, runner selftest passes 59/59, and repository formatting, JavaScript/JSON syntax, and diff checks pass. Both native Sass rows remain private and production-unreachable, the release interlock stays closed, the SDK selector was removed, and no production or external state changed. | `7064ec8` |
| `NSASS-011-META-MATH-RANDOM-FUNCTION-INVOCATION` | `VERIFIED` | Added provider-free native Zig invocation of the exact already-admitted module-owned `sass:math` `random` function reference through `meta.call()` for SCSS and indented Sass, plus independently measured legacy-global reflection. Default, custom, and unprefixed module origins retain ownership without a legacy warning; global reflection remains true, global and module references remain distinct, and every legacy invocation preserves the normalized warning. Omitted/null, positional/named, list/map/empty-splat, map-override, full positive 32-bit limit, unit-warning, once-only ordering, range, and sequence behavior match fresh Dart Sass 1.101.0 probes; ZigCSS deliberately preserves deterministic identical-source replay while the oracle varies. Invalid type/range/binding, argument-limit, and allocation-failure paths expose no CSS. The focused red gate stopped at 354/357 before Debug/ReleaseSafe each passed 23/23 steps and 357/357 tests; direct evaluator coverage passes 224/224, full compiler modes pass 60/60 and 852/852, and Windows x64 GNU plus Linux ARM64 GNU evaluator cross-compiles pass in both modes. Native contract 12/12 plus check adds only `built-in-meta-math-random-function-invocation`; complete provider/reference/product matrices pass 151/151, package policy passes 6/6 plus check, runner selftest passes 59/59, and repository formatting, JavaScript/JSON syntax, and diff checks pass. Both native Sass rows remain private and production-unreachable, the release interlock stays closed, the SDK selector was removed, and no production or external state changed. | `c217d28` |
| `NSASS-011-META-MATH-MAX-FUNCTION-INVOCATION` | `VERIFIED` | Added provider-free native Zig invocation of the exact already-admitted module-owned `sass:math` `max` function reference through `meta.call()` for SCSS and indented Sass, plus independently measured legacy-global reflection. Default, custom, and unprefixed module origins retain ownership without a legacy warning; global reflection remains true, global and module references remain distinct, and every legacy invocation preserves the normalized warning. One-or-more positional arguments, list splats, converted units, unitless/unitful comparison, exact selected-argument unit spelling, and once-only source order match fresh Dart Sass 1.101.0 oracle bytes. Empty, keyword, map-splat, string, list, incompatible-unit, argument-limit, and allocation-failure cases fail closed; module-owned `math.random` remains explicitly unavailable. The focused red gate stopped at 350/354 before Debug/ReleaseSafe each passed 23/23 steps and 354/354 tests; direct evaluator coverage passes 221/221, full compiler modes pass 60/60 and 849/849, and Windows x64 GNU plus Linux ARM64 GNU evaluator cross-compiles pass in both modes. Native contract 12/12 plus check adds only `built-in-meta-math-max-function-invocation`; complete provider/reference/product matrices pass 151/151, package policy passes 6/6 plus check, runner selftest passes 59/59, and repository formatting, JavaScript/JSON syntax, and diff checks pass. Both native Sass rows remain private and production-unreachable, the release interlock stays closed, the SDK selector was removed, and no production or external state changed. | `5a23c98` |
| `NSASS-011-META-MATH-MIN-FUNCTION-INVOCATION` | `VERIFIED` | Added provider-free native Zig invocation of the exact already-admitted module-owned `sass:math` `min` function reference through `meta.call()` for SCSS and indented Sass, plus independently measured legacy-global reflection. Default, custom, and unprefixed module origins retain ownership without a legacy warning; global reflection remains true, global and module references remain distinct, and every legacy invocation preserves the normalized warning. One-or-more positional arguments, list splats, converted units, unitless/unitful comparison, exact selected-argument unit spelling, and once-only source order match fresh Dart Sass 1.101.0 oracle bytes. Empty, keyword, map-splat, string, list, incompatible-unit, argument-limit, and allocation-failure cases fail closed; module-owned `math.max` remains explicitly unavailable. The focused red gate stopped at 347/351 before Debug/ReleaseSafe each passed 23/23 steps and 351/351 tests; direct evaluator coverage passes 218/218, full compiler modes pass 60/60 and 846/846, and Windows x64 GNU plus Linux ARM64 GNU evaluator cross-compiles pass in both modes. Native contract 12/12 plus check adds only `built-in-meta-math-min-function-invocation`; complete provider/reference/product matrices pass 151/151, package policy passes 6/6 plus check, runner selftest passes 59/59, and repository formatting, JavaScript/JSON syntax, and diff checks pass. Both native Sass rows remain private and production-unreachable, the release interlock stays closed, the SDK selector was removed, and no production or external state changed. | `1a009fd` |
| `NSASS-011-META-MATH-HYPOT-FUNCTION-INVOCATION` | `VERIFIED` | Added provider-free native Zig invocation of the exact already-admitted module-owned `sass:math` `hypot` function reference through `meta.call()` for SCSS and indented Sass. Default, custom, and unprefixed module origins retain ownership without a legacy warning; direct CSS-compatible calls remain usable while global reflection now matches Dart Sass 1.101.0's false/not-found boundary. One-or-more positional arguments, list splats, converted units, exact first-argument unit spelling, and once-only source order match fresh oracle bytes. Empty, keyword, map-splat, string, list, mixed-unit, and incompatible-unit calls fail closed; module-owned `math.min` remains explicitly unavailable. The focused red gate stopped at 344/348 before Debug/ReleaseSafe each passed 23/23 steps and 348/348 tests; direct evaluator coverage passes 215/215, full compiler modes pass 60/60 and 843/843, and Windows x64 GNU plus Linux ARM64 GNU evaluator cross-compiles pass in both modes. Native contract 12/12 plus check adds only `built-in-meta-math-hypot-function-invocation`; complete provider/reference/product matrices pass 151/151, package policy passes 6/6 plus check, runner selftest passes 59/59, and repository formatting, JavaScript/JSON/Bash syntax, and diff checks pass. Both native Sass rows remain private and production-unreachable, the release interlock stays closed, the SDK selector was removed, and no production or external state changed. | `29bcfed` |
| `NSASS-011-META-MATH-UNITLESS-FUNCTION-INVOCATION` | `VERIFIED` | Added provider-free native Zig invocation of the exact already-admitted `sass:math` `is-unitless` function reference through `meta.call()` for SCSS and indented Sass. Module-owned default/custom/unprefixed origins avoid the legacy-global warning; the true legacy `unitless` origin preserves it. Canonical positional/named `$number` binding, list/map splats, dimensionless/unitful boolean results, and once-only source order match Dart Sass 1.101.0. Missing, extra, unknown, duplicate, and non-number arguments fail closed, while `math.unit` and every other sibling callable family remain unavailable. The focused test-first gate reproduced the former boundary at 312/314 before both modes passed 23/23 steps and 314/314 tests; direct evaluator coverage passes 181/181, full compiler modes pass 60/60 and 809/809, and Windows x64 GNU plus Linux ARM64 GNU cross-target evaluator compilation passes in both modes. Native contract 10/10 plus check adds only `built-in-meta-math-unitless-function-invocation`; complete provider/product matrices pass 151/151, package policy passes 6/6 plus check, runner selftest passes 58/58, and repository formatting, JavaScript/JSON syntax, and diff checks pass. Both native Sass rows remain private and production-unreachable, the release interlock stays closed, the SDK selector was removed, and no production or external state changed. | `934ad1f` |
| `NSASS-011-META-MATH-COMPATIBILITY-FUNCTION-INVOCATION` | `VERIFIED` | Added provider-free native Zig invocation of the exact already-admitted `sass:math` `compatible` function reference through `meta.call()` for SCSS and indented Sass. Module-owned default/custom/unprefixed origins avoid the legacy-global warning; the true legacy `comparable` origin preserves it. Canonical positional/named `$number1`/`$number2` binding, list/map splats, known/custom/compound units, boolean results, and once-only source order match Dart Sass 1.101.0. Missing, extra, unknown, duplicate, and non-number arguments fail closed, while `math.is-unitless` and every other sibling callable family remain unavailable. The focused test-first gate reproduced the former boundary at 310/312 before both modes passed 23/23 steps and 312/312 tests; direct evaluator coverage passes 179/179, full compiler modes pass 60/60 and 807/807, and Windows x64 GNU plus Linux ARM64 GNU cross-target evaluator compilation passes in both modes. Native contract 10/10 plus check adds only `built-in-meta-math-compatibility-function-invocation`; complete provider/product matrices pass 151/151, package policy passes 6/6 plus check, runner selftest passes 58/58, and repository formatting, JavaScript/JSON syntax, and diff checks pass. Both native Sass rows remain private and production-unreachable, the release interlock stays closed, the SDK selector was removed, and no production or external state changed. | `f6216b9` |
| `NSASS-011-META-MATH-UNARY-FUNCTION-INVOCATION` | `VERIFIED` | Added provider-free native Zig invocation of the exact already-admitted unary `sass:math` function-reference family through `meta.call()` for SCSS and indented Sass: `abs`, `ceil`, `floor`, `round`, and `percentage`. Module-owned default/custom/unprefixed origins avoid the legacy-global warning; true legacy origins preserve it. Canonical positional/named `$number` binding, list/map splats, numeric units, percentage conversion, and once-only source order match Dart Sass 1.101.0. Missing, extra, unknown, duplicate, non-number, and unitful-percentage arguments fail closed, while `math.compatible` and every other sibling callable family remain unavailable. The focused test-first gate reproduced the former boundary at 307/310 before both modes passed 23/23 steps and 310/310 tests; direct evaluator coverage passes 177/177, full compiler modes pass 60/60 and 805/805, and Windows x64 GNU plus Linux ARM64 GNU cross-target evaluator compilation passes in both modes. Native contract 10/10 plus check adds only `built-in-meta-math-unary-function-invocation`; complete provider/product matrices pass 151/151, package policy passes 6/6 plus check, runner selftest passes 58/58, and repository formatting, JavaScript/JSON syntax, and diff checks pass. Both native Sass rows remain private and production-unreachable, the release interlock stays closed, the SDK selector was removed, and no production or external state changed. | `798a3d8` |
| `NSASS-011-META-EXISTENCE-FUNCTION-INVOCATION` | `VERIFIED` | Added provider-free native Zig invocation of the exact `sass:meta` existence-query function-reference family through `meta.call()` for SCSS and indented Sass: `feature-exists`, `function-exists`, `mixin-exists`, `variable-exists`, and `global-variable-exists`. Module-owned default/custom/unprefixed origins avoid the legacy-global warning; true legacy origins preserve it, while `feature-exists` preserves its independent deprecation. Canonical positional/named/optional binding, list/map splats, lexical call-site scope, normalized names, loaded-module reflection, and once-only source order match Dart Sass 1.101.0. Missing, extra, unknown, type-invalid, and unloaded-module arguments fail closed, and every sibling callable family remains unavailable. The focused test-first gate reproduced the former boundary at 305/308 before both modes passed 23/23 steps and 308/308 tests; direct evaluator coverage passes 175/175, full compiler modes pass 60/60 and 803/803, and Windows x64 GNU plus Linux ARM64 GNU cross-target evaluator compilation passes in both modes. Native contract 10/10 plus check adds only `built-in-meta-existence-function-invocation`; complete provider/product matrices pass 151/151, package policy passes 6/6 plus check, runner selftest passes 58/58, and repository formatting, JavaScript/JSON syntax, and diff checks pass. Both native Sass rows remain private and production-unreachable, the release interlock stays closed, the SDK selector was removed, and no production or external state changed. | `272dc9f` |
| `NSASS-011-META-CALCULATION-FUNCTION-INVOCATION` | `VERIFIED` | Added provider-free native Zig invocation of the exact module-owned `sass:meta` `calc-name` and `calc-args` function references through `meta.call()` for SCSS and indented Sass. Default, custom, and unprefixed module access preserve ownership without a legacy warning; positional/named `$calc` binding, list/map splats, retained calculation values, and once-only source order match Dart Sass 1.101.0. Missing, extra, unknown, duplicate, and non-calculation arguments fail closed, while meta existence and every other sibling callable family remain unavailable. The focused test-first gate reproduced the former boundary at 304/306 before both modes passed 23/23 steps and 306/306 tests; direct evaluator coverage passes 173/173, full compiler modes pass 60/60 and 801/801, and Windows x64 GNU plus Linux ARM64 GNU cross-target evaluator compilation passes in both modes. Native contract 10/10 plus check adds only `built-in-meta-calculation-function-invocation`; complete provider/product matrices pass 151/151, package policy passes 6/6 plus check, runner selftest passes 58/58, and repository formatting, JavaScript/JSON syntax, and diff checks pass. Both native Sass rows remain private and production-unreachable, the release interlock stays closed, the SDK selector was removed, and no production or external state changed. | `8b103a7` |
| `NSASS-011-META-CONTENT-ACCEPTANCE-FUNCTION-INVOCATION` | `VERIFIED` | Added provider-free native Zig invocation of the exact module-owned `sass:meta` `accepts-content` function reference through `meta.call()` for SCSS and indented Sass. Default, custom, and unprefixed module access preserve ownership without a legacy warning; positional/named binding, list/map splats, and once-only source order match Dart Sass 1.101.0. User and built-in mixin references preserve their content contracts. Missing, extra, unknown, and non-mixin arguments fail closed, and every sibling callable family remains unavailable. The focused test-first gate reproduced the former boundary at 303/305 before both modes passed 23/23 steps and 305/305 tests; full compiler modes pass 60/60 and 800/800, cross-target evaluator compilation passes, contract 10/10 plus check adds only the named capability, complete provider/product matrices pass 151/151, package policy passes 6/6, runner selftest passes 58/58, and static checks pass. The SDK selector was removed and no production or external state changed. | `4559341` |
| `NSASS-011-META-KEYWORDS-FUNCTION-INVOCATION` | `VERIFIED` | Added provider-free native Zig invocation of the exact `sass:meta` `keywords` function reference through `meta.call()` for SCSS and indented Sass. The origin decoder validates global/module ownership; default, custom, and unprefixed imports retain module ownership, while the callable-only legacy global warns without widening direct bare-global dispatch. Evaluated forwarding preserves positional/named `$args`, named `$function`, list/map/argument-list splats, rest forwarding, normalized keyword names, once-only source order, and shared keyword-consumption state across deeply owned bounded collections. Global/module existence reflection stays coherent. Missing, extra, unknown, or type-invalid target arguments, non-callables, mixins, every other unsupported built-in family, special `if()`, limits, cancellation, and allocation failures fail closed; direct callable CSS serialization, plain-CSS references, broader built-in invocation, and mixin application remain unavailable. Fresh Dart Sass 1.101.0 probes match exact SCSS, indented-Sass, negative-binding, existence, and legacy-warning behavior. The test-first probe reproduced the old 0/1 boundary, and the first dispatch replay exposed semantic gaps at 11/13. Direct evaluator coverage passes 171/171; focused native Debug/ReleaseSafe each pass 23/23 steps and 304/304 tests; full exact Zig 0.15.2 Debug/ReleaseSafe each pass 60/60 steps and 799/799 tests; and direct evaluator no-emission cross-compiles pass for Windows x64 GNU and Linux ARM64 GNU in both modes. Native contract 10/10 plus check adds only `built-in-meta-keywords-function-invocation`, preserves both native Sass rows as private and production-unreachable, and retains the recursive repository-owned Zig/`std`/`builtin` closure with zero target package or optional runtime dependencies. Host 17/17, resolver 12/12, metadata 13/13, Sass 28/28, Less 27/27, Stylus 24/24, and five-language product 30/30 matrices pass for 151/151 total; preprocessor package policy passes 6/6 plus check. Autonomous-runner selftest passes 58/58, repository Zig formatting, JavaScript/JSON syntax, contract, and diff gates pass. The temporary SDK selector was removed; no production route, public claim, version, native graduation, release interlock, tag, artifact/package publication, deployment, or external state changed. | `ad4ac97` |
| `NSASS-011-META-INSPECTION-FUNCTION-INVOCATION` | `VERIFIED` | Added provider-free native Zig invocation of the exact `sass:meta` inspection function-reference family, `inspect` and `type-of`, through `meta.call()` for SCSS and indented Sass. The origin decoder validates every encoded callable against its global/module inventory; default, custom, and unprefixed imports retain module ownership, while only the two true legacy globals warn. Evaluated forwarding preserves positional/named binding, list/map/argument-list splats, rest forwarding, nested value and callable inspection, and once-only source order. Missing, extra, or unknown target arguments, non-callables, mixins, every other meta/math/selector/string/color/calculation or special-function family, special `if()`, limits, cancellation, and every allocation failure fail closed; direct callable CSS serialization, plain-CSS references, broader built-in invocation, and mixin application remain unavailable. Fresh Dart Sass 1.101.0 probes match exact SCSS, indented-Sass, negative-binding, and legacy-warning behavior. The focused test-first gate reproduced the old family boundary at 299/302. Direct evaluator coverage passes 169/169; focused native Debug/ReleaseSafe each pass 23/23 steps and 302/302 tests; full exact Zig 0.15.2 Debug/ReleaseSafe each pass 60/60 steps and 797/797 tests; and direct evaluator no-emission cross-compiles pass for Windows x64 GNU and Linux ARM64 GNU in both modes. Native contract 10/10 plus check adds only `built-in-meta-inspection-function-invocation`, preserves both native Sass rows as private and production-unreachable, and retains the recursive repository-owned Zig/`std`/`builtin` closure with zero target package or optional runtime dependencies. Host 17/17, resolver 12/12, metadata 13/13, Sass 28/28, Less 27/27, Stylus 24/24, and five-language product 30/30 matrices pass for 151/151 total; preprocessor package policy passes 6/6 plus check. Autonomous-runner selftest passes 58/58, repository Zig formatting, JavaScript/JSON syntax, contract, and diff gates pass. The temporary SDK selector was removed; no production route, public claim, version, native graduation, release interlock, tag, artifact/package publication, deployment, or external state changed. | `85d8929` |
| `NSASS-011-META-MAP-MUTATION-FUNCTION-INVOCATION` | `VERIFIED` | Added provider-free native Zig invocation of the complete immutable `sass:map` mutation function-reference family through `meta.call()` for SCSS and indented Sass: variadic `merge`, `remove`, `set`, and `deep-remove`, plus fixed `deep-merge`, with exact custom and unprefixed module ownership and the two legacy-global counterparts. The origin decoder validates every encoded callable against its global/module inventory; only true legacy globals warn. Evaluated forwarding preserves positional/measured named binding, list/map/argument-list splats, rest forwarding, nested key paths, immutable order, and once-only source order. Invalid target binding, unsupported variadic keywords, non-callables, mixins, all other built-in families, special `if()`, limits, cancellation, and every allocation failure fail closed; direct callable CSS serialization, plain-CSS references, broader built-in invocation, and mixin application remain unavailable. Fresh Dart Sass 1.101.0 probes match exact SCSS, indented-Sass, and legacy-warning output. The focused test-first gate reproduced the old family boundary at 297/300. Direct evaluator coverage passes 167/167; focused native Debug/ReleaseSafe each pass 23/23 steps and 300/300 tests; full exact Zig 0.15.2 Debug/ReleaseSafe each pass 60/60 steps and 795/795 tests; and direct evaluator no-emission cross-compiles pass for Windows x64 GNU and Linux ARM64 GNU in both modes. Native contract 10/10 plus check adds only `built-in-meta-map-mutation-function-invocation`, preserves both native Sass rows as private and production-unreachable, and retains the recursive repository-owned Zig/`std`/`builtin` closure with zero target package or optional runtime dependencies. Host 17/17, resolver 12/12, metadata 13/13, Sass 28/28, Less 27/27, Stylus 24/24, and five-language product 30/30 matrices pass for 151/151 total; preprocessor package policy passes 6/6 plus check. Autonomous-runner selftest passes 58/58, repository Zig formatting, JavaScript/JSON syntax, contract, and diff gates pass. The temporary SDK selector was removed; no production route, public claim, version, native graduation, release interlock, tag, artifact/package publication, deployment, or external state changed. | `3b559d6` |
| `NSASS-011-META-MAP-QUERY-FUNCTION-INVOCATION` | `VERIFIED` | Added provider-free native Zig invocation of the complete read-only `sass:map` function-reference family through `meta.call()` for SCSS and indented Sass: variadic `get` and `has-key`, plus fixed `keys` and `values`, with exact custom and unprefixed module ownership and all four legacy-global counterparts. The origin decoder validates every encoded callable against its global/module inventory; only true legacy globals warn. Evaluated forwarding preserves positional/named binding, list/map/argument-list splats, rest forwarding, variadic key paths, null/missing behavior, and once-only source order. Invalid target binding, variadic keywords, non-callables, mixins, map mutations, all other built-in families, special `if()`, limits, cancellation, and every allocation failure fail closed; direct callable CSS serialization, plain-CSS references, broader built-in invocation, and mixin application remain unavailable. Fresh Dart Sass 1.101.0 probes match exact SCSS, indented-Sass, and legacy-warning output. The focused test-first gate reproduced the old family boundary at 296/298. Direct evaluator coverage passes 165/165; focused native Debug/ReleaseSafe each pass 23/23 steps and 298/298 tests; full exact Zig 0.15.2 Debug/ReleaseSafe each pass 60/60 steps and 793/793 tests; and direct evaluator no-emission cross-compiles pass for Windows x64 GNU and Linux ARM64 GNU in both modes. Native contract 10/10 plus check adds only `built-in-meta-map-query-function-invocation`, preserves both native Sass rows as private and production-unreachable, and retains the recursive repository-owned Zig/`std`/`builtin` closure with zero target package or optional runtime dependencies. Host 17/17, resolver 12/12, metadata 13/13, Sass 28/28, Less 27/27, Stylus 24/24, and five-language product 30/30 matrices pass for 151/151 total; preprocessor package policy passes 6/6 plus check. Autonomous-runner selftest passes 58/58, repository Zig formatting, JavaScript/JSON syntax, contract, and diff gates pass. The temporary SDK selector was removed; no production route, public claim, version, native graduation, release interlock, tag, artifact/package publication, deployment, or external state changed. | `9822b9c` |
| `NSASS-011-META-LIST-FUNCTION-INVOCATION` | `VERIFIED` | Added provider-free native Zig invocation of the complete already-constructible `sass:list` function-reference family through `meta.call()` across default, custom, and unprefixed `sass:meta` namespaces in SCSS and indented Sass, plus all nine legacy-global counterparts. The origin decoder validates every encoded callable against its global/module inventory; aliases and unprefixed imports retain module ownership, while only true legacy globals warn. All ten members (`nth`, `length`, `index`, `separator`, `is-bracketed`, `append`, `set-nth`, `join`, `zip`, and module-only `slash`) preserve positional/named binding, omitted-option defaults, list/map/argument-list splats, rest forwarding, keyword-rest consumption, variadics, and once-only source order. Invalid target binding, variadic keywords, non-callables, mixins, unsupported built-in families, special `if()`, limits, cancellation, and every allocation failure fail closed; other callable families, direct callable CSS serialization, plain-CSS references, and mixin application remain unavailable. Fresh Dart Sass 1.101.0 probes match exact SCSS, indented-Sass, and legacy-warning output. The focused test-first gate reproduced the old boundary at 294/296. Direct evaluator coverage passes 163/163; focused native Debug/ReleaseSafe each pass 23/23 steps and 296/296 tests; full exact Zig 0.15.2 Debug/ReleaseSafe each pass 60/60 steps and 791/791 tests; and direct evaluator no-emission cross-compiles pass for Windows x64 GNU and Linux ARM64 GNU in both modes. Native contract 10/10 plus check adds only `built-in-meta-list-function-invocation`, preserves both native Sass rows as private and production-unreachable, and retains the recursive repository-owned Zig/`std`/`builtin` closure with zero target package or optional runtime dependencies. Host 17/17, resolver 12/12, metadata 13/13, Sass 28/28, Less 27/27, Stylus 24/24, and five-language product 30/30 matrices pass for 151/151 total; preprocessor package policy passes 6/6 plus check. Autonomous-runner selftest passes 58/58, repository Zig formatting, JavaScript/JSON syntax, contract, and diff gates pass. The temporary SDK selector was removed; no production route, public claim, version, native graduation, release interlock, tag, artifact/package publication, deployment, or external state changed. | `198aa1b` |
| `NSASS-011-META-USER-FUNCTION-INVOCATION` | `VERIFIED` | Added provider-free native Zig invocation of already-constructible user-function references through `meta.call()` in default, exact custom, and unprefixed `sass:meta` namespaces for SCSS and indented Sass, plus the legacy global warning. Declaration-stable old/new references, defaults, positional/named arguments, list/map/argument-list splats, rest forwarding, keyword-rest consumption, nested calls, and once-only source-order evaluation match fresh Dart Sass 1.101.0 probes. Non-callables, mixins, built-in and special-function references, invalid target binding, expanded limits, cancellation, and every allocation failure fail closed; direct callable CSS serialization, plain-CSS references, built-in invocation, and mixin application remain unavailable. The focused test-first gate reproduced the former 0/1 missing-member boundary. Direct evaluator coverage passes 161/161; focused native Debug/ReleaseSafe each pass 23/23 steps and 294/294 tests; full exact Zig 0.15.2 Debug/ReleaseSafe each pass 60/60 steps and 789/789 tests; and direct evaluator no-emission cross-compiles pass for Windows x64 GNU and Linux ARM64 GNU in both modes. Native contract 10/10 plus check adds only `built-in-meta-user-function-invocation`, preserves both native Sass rows as private and production-unreachable, and keeps the recursive target closure limited to repository-owned Zig/`std`/`builtin` with zero package or optional runtime dependencies, provider engines, child compilers, network, downloads, or non-system language runtimes. Host 17/17, resolver 12/12, metadata 13/13, Sass 28/28, Less 27/27, Stylus 24/24, and five-language product 30/30 matrices pass for 151/151 total; preprocessor package policy passes 6/6 plus check. Autonomous-runner selftest passes 58/58, repository Zig formatting, JavaScript/JSON syntax, contract, and diff gates pass. The temporary SDK selector was removed; no production route, public claim, version, native graduation, release interlock, tag, artifact/package publication, deployment, or external state changed. | `da8c09f` |
| `NSASS-011-META-CALLABLE-INSPECTION` | `VERIFIED` | Added provider-free native Zig inspection for every already-constructible first-class function and mixin reference through `meta.inspect()` in SCSS and indented Sass. User functions/mixins, legacy globals, loaded built-in module functions, special `if()`, and built-in mixins render canonical namespace-independent `get-function("name")` / `get-mixin("name")` strings; underscore aliases normalize to hyphens, stable module ownership decodes to exact member names, and nested collection inspection preserves punctuation. The decoder validates origins and members against the admitted global/module inventory and rejects invalid identities. Direct callable CSS serialization and invocation remain fail-closed. Fresh Dart Sass 1.101.0 probes cover all four callable kinds and every module naming family, including normalized global/module distinctions and nested list/map rendering. The targeted test-first gate reproduced the former 0/1 unsupported boundary. Direct evaluator coverage passes 158/158; focused native Debug/ReleaseSafe each pass 23/23 steps and 291/291 tests; full exact Zig 0.15.2 Debug/ReleaseSafe each pass 60/60 steps and 786/786 tests; and direct evaluator no-emission cross-compiles pass for Windows x64 GNU and Linux ARM64 GNU in both modes. Native contract 10/10 plus check adds only `built-in-meta-callable-inspection`, preserves both native Sass rows as private and production-unreachable, and keeps the recursive target closure limited to repository-owned Zig/`std`/`builtin` with zero package or optional runtime dependencies, provider engines, child compilers, network, downloads, or non-system language runtimes. Host 17/17, resolver 12/12, metadata 13/13, Sass 28/28, Less 27/27, Stylus 24/24, and five-language product 30/30 matrices pass for 151/151 total; preprocessor package policy passes 6/6 plus check. Autonomous-runner selftest passes 58/58, repository Zig formatting, JavaScript/JSON syntax, contract, and diff gates pass. The temporary SDK selector was removed; no production route, public claim, version, native graduation, release interlock, tag, artifact/package publication, deployment, or external state changed. | `4850e7e` |
| `NSASS-011-META-FUNCTION-REFERENCES` | `VERIFIED` | Added provider-free native Zig creation of first-class Sass function references through `meta.get-function()` in default, exact custom, and unprefixed `sass:meta` namespaces for SCSS and indented Sass, plus the normalized legacy global. User references retain declaration identity across underscore/hyphen and escaped spelling, become distinct after redefinition, and remain case-sensitive. Canonical `$name`, `$css`, and `$module` arguments evaluate exactly once in source order; false/null `$css` selects Sass lookup, explicit null selects global lookup, aliases of one loaded module member compare equal, unprefixed module members preserve that identity, and legacy globals remain distinct from corresponding module members. The special legacy `if()` and the exact native-admitted global/module inventory are typed as functions. Missing or later declarations, invalid names/types/arity/keywords, case or ownership mismatches, missing namespaces/members, truthy `$css` plain-CSS references, callable invocation or inspection, direct CSS serialization, temporary exhaustion, and every allocation failure fail closed without CSS. Fresh Dart Sass 1.101.0 probes reproduce exact SCSS `.values{type:function;same:true;marked:true;redefined:false;different:false;alias:function;star:function;global:function;global-alias:true;module:function;module-alias:true;star-module:true;global-distinct:false;own:function;own-alias:true;conditional:function;conditional-same:true;reflected:true;evaluations:3;trace:123}` and indented Sass `.sass{user:function;same:true;module:function;global-distinct:false}`. Test-first focused Debug stopped at 287/290 with three expected failures. Final direct evaluator coverage passes 157/157; focused native Debug/ReleaseSafe each pass 23/23 steps and 290/290 tests; full exact Zig 0.15.2 Debug/ReleaseSafe each pass 60/60 steps and 785/785 tests; and direct evaluator no-emission cross-compiles pass for Windows x64 GNU and Linux ARM64 GNU in both modes. Native contract 10/10 plus check adds only `built-in-meta-sass-function-references`, preserves both native Sass rows as private and production-unreachable, and keeps the recursive target closure limited to repository-owned Zig/`std`/`builtin` with zero package or optional runtime dependencies, provider engines, child compilers, network, downloads, or non-system language runtimes. Host 17/17, resolver 12/12, metadata 13/13, Sass 28/28, Less 27/27, Stylus 24/24, and five-language product 30/30 matrices pass for 151/151 total; preprocessor package policy passes 6/6 plus check. Autonomous-runner selftest passes 58/58, repository Zig formatting, JavaScript/JSON syntax, contract, and diff gates pass, and the native release interlock remains closed. Host gates reused the existing isolated macOS 15.4 SDK selector required by pinned Zig 0.15.2; no tracked or system state changed. No production route, public claim, version, native graduation, release interlock, tag, artifact/package publication, deployment, or external state changed. | `d41e1ab` |
| `NSASS-011-LEGACY-IF-DUAL-SPLAT` | `VERIFIED` | Added provider-free native Zig expansion for exactly one adjacent terminal positional-rest plus keyword-rest pair in legacy `if()` calls for SCSS and indented Sass. Fresh Dart Sass 1.101.0 probes establish first-rest, second-rest, then source-ordered direct-argument evaluation; the first rest expands scalar, list, map, or positional/keyword argument-list data, while the second rest must be an actual map with string keys. Direct keyword values are overridden by the first expanded map and then the second expanded map, exact map-key spelling remains significant, and positional/name conflicts fail. Valid pairs produce only the existing `if-function` warning with its five-warning throttle and combined summary; they do not produce `misplaced-rest`. Non-map scalars/lists/empty lists/argument lists, non-string map keys, underscore key mismatches, duplicate or missing bindings, expanded-argument exhaustion, nonterminal pairs, three or more splats, and every allocation failure fail closed without CSS. Test-first focused Debug stopped at the old multiple-splat branch with 284/287 tests and three expected failures. Final focused native Debug/ReleaseSafe each pass 23/23 steps and 287/287 tests; full exact Zig 0.15.2 Debug/ReleaseSafe each pass 60/60 steps and 782/782 tests; and direct evaluator no-emission cross-compiles pass for Windows x64 GNU and Linux ARM64 GNU in both modes. Native contract 10/10 plus check adds only `legacy-if-dual-splat-expansion`, preserves both native Sass rows as private and production-unreachable, and keeps the recursive target closure limited to repository-owned Zig/`std`/`builtin` with zero package or optional runtime dependencies, provider engines, child compilers, network, downloads, or non-system language runtimes. Host 17/17, resolver 12/12, metadata 13/13, Sass 28/28, Less 27/27, Stylus 24/24, and five-language product 30/30 matrices pass for 151/151 total; preprocessor package policy passes 6/6 plus check. Autonomous-runner selftest passes 58/58, repository Zig formatting, JavaScript/JSON syntax, contract, and diff gates pass, and the native release interlock remains closed. Host gates reused the existing isolated macOS 15.4 SDK selector required by pinned Zig 0.15.2; no tracked or system state changed. No production route, public claim, version, native graduation, release interlock, tag, artifact/package publication, deployment, or external state changed. | `c23672d` |
| `NSASS-011-LEGACY-IF-MISPLACED-REST` | `VERIFIED` | Added provider-free native Zig support for exactly one non-final legacy `if()` rest argument in SCSS and indented Sass. Fresh Dart Sass 1.101.0 probes establish rest-first evaluation, eager source-order direct evaluation, direct binding before expanded scalar/list/map positional or keyword rest values, and the exact positional-versus-named `misplaced-rest` warning before `if-function`. Native diagnostics retain both canonical warning sentences on one valid line; each deprecation type independently emits five warnings and the omitted counts share one final summary. Test-first focused Debug stopped at the old unsupported branch with 284/286 tests; final focused native Debug/ReleaseSafe each pass 23/23 steps and 286/286 tests, full exact Zig 0.15.2 Debug/ReleaseSafe each pass 60/60 steps and 781/781 tests, and direct evaluator no-emission cross-compiles pass for Windows x64 GNU and Linux ARM64 GNU in both modes. The exhaustive allocation fixture executes the new warning and evaluation path, while the valid exact two-terminal-splat form remains explicitly unsupported for the next measured slice. Native contract 10/10 plus check adds only `legacy-if-single-misplaced-rest-expansion`, preserves both native Sass rows as private and production-unreachable, and keeps the recursive target closure limited to repository-owned Zig/`std`/`builtin` with zero target package or optional runtime dependencies, provider engines, child compilers, network, downloads, or non-system language runtimes. Host 17/17, resolver 12/12, metadata 13/13, Sass 28/28, Less 27/27, Stylus 24/24, and five-language product 30/30 matrices pass for 151/151 total; preprocessor package policy passes 6/6 plus check. Autonomous-runner selftest passes 58/58, repository Zig formatting, JavaScript/JSON syntax, contract, and diff gates pass, and the native release interlock remains closed. The isolated macOS 15.4 SDK selector used by pinned Zig 0.15.2 was removed after validation. No production route, public claim, version, native graduation, release interlock, tag, artifact/package publication, deployment, or external state changed. | `a1d5782` |
| `NSASS-011-LEGACY-IF-FINAL-SPLAT` | `VERIFIED` | Added provider-free native Zig expansion for exactly one final legacy `if()` splat in SCSS and indented Sass. Fresh Dart Sass 1.101.0 probes establish that a splatted call evaluates the rest expression first and then every direct argument eagerly, unlike the unchanged splat-free lazy path. Native comma/space lists, scalar splats, exact string-key maps, and positional/keyword argument lists now bind the three special parameters; splat-map keywords remain exact rather than underscore/hyphen-normalized and override matching direct keywords. A side-effect trace pins exact rest-first order and closes speculative double execution of the first callable inside parenthesized collection syntax before arithmetic probing. Missing/extra arguments, non-string map keys, map-key spelling mismatches, eager undefined variables, expanded-argument limits, and every allocation failure fail without CSS. Non-final or multiple splats remain an explicit unsupported boundary because their separate misplaced-rest warnings have not been admitted. The test-first focused gate stopped at the old `SplatUnsupported` path with 149/152 evaluator tests; final focused native Debug/ReleaseSafe each pass 23/23 steps and 285/285 tests, full exact Zig 0.15.2 Debug/ReleaseSafe each pass 60/60 steps and 780/780 tests, and direct evaluator no-emission cross-compiles pass for Windows x64 GNU and Linux ARM64 GNU in both modes. Native contract 10/10 plus check adds only `legacy-if-final-splat-expansion`, preserves both native Sass rows as private and production-unreachable, and keeps the recursive target closure limited to repository-owned Zig/`std`/`builtin` with zero package or optional runtime dependencies, provider engines, child compilers, network, downloads, or non-system language runtimes. Host 17/17, resolver 12/12, metadata 13/13, Sass 28/28, Less 27/27, Stylus 24/24, and five-language product 30/30 matrices pass for 151/151 total; preprocessor package policy passes 6/6 plus check. The autonomous-runner selftest passes 58/58, repository Zig formatting, JavaScript/JSON syntax, contract, and diff gates pass, and the native release interlock remains closed. Host full builds selected the already-installed macOS 15.4 SDK through a temporary isolated wrapper because pinned Zig 0.15.2 cannot consume the host macOS 26.5 SDK's current link metadata; the wrapper was removed after both safety modes passed and no implementation byte changed. No production route, public claim, version, native graduation, release interlock, tag, artifact/package publication, deployment, or external state changed. | `dad88d5` |
| `NSASS-011-LEGACY-IF-FUNCTION` | `VERIFIED` | Added provider-free native Zig legacy `if($condition, $if-true, $if-false)` evaluation before user-defined and ordinary CSS functions. The condition evaluates first and only the selected branch evaluates; false and null are falsey, zero and other Sass values are truthy, and a user declaration named `if` cannot shadow the special built-in. Canonical positional and reordered keyword bindings, underscore/hyphen keyword equivalence, comment trivia, a trailing comma, exact lowercase and escaped-lowercase function spelling, nested calls, arithmetic, `calc()`, `min()`, plain CSS-function arguments, list/map/null results, and SCSS/indented-Sass syntax are executable. `meta.function-exists("if")` returns true. Exact Dart Sass 1.101.0 oracle probes established and tests pin the first-five warning throttle, exact deprecation text, and final `11 repetitive deprecation warnings omitted.` summary for 16 calls. Modern CSS `if(style(...): ...; else: ...)` is preserved independently without a legacy warning. Missing, extra, unknown, duplicate, or position-after-keyword arguments, unrecognized clauses, whitespace between the name and opening parenthesis, argument ceiling exhaustion, and every allocation failure are covered; argument-list splat expansion remains deliberately fail-closed. Test-first direct evaluation produced the expected 148/150 red boundary with only the new legacy semantics absent. Final direct evaluator Debug coverage passes 151/151; focused native Debug/ReleaseSafe each pass 23/23 steps and 284/284 tests; full exact Zig 0.15.2 Debug/ReleaseSafe each pass 60/60 steps and 779/779 tests. Direct evaluator no-emission cross-compiles pass for Windows x64 GNU and Linux ARM64 GNU in both modes. Native contract 10/10 plus check adds only `lazy-legacy-if-function`, preserves both native Sass rows as private and production-unreachable, and keeps the recursive target closure limited to repository-owned Zig/`std`/`builtin` with zero package or optional runtime dependencies, provider engines, child compilers, network, downloads, or non-system language runtimes. Host 17/17, resolver 12/12, metadata 13/13, Sass 28/28, Less 27/27, Stylus 24/24, and five-language product 30/30 matrices pass for 151/151 total; preprocessor package policy passes 6/6 plus check. The autonomous-runner selftest passes 58/58, repository Zig formatting, JavaScript/JSON syntax, contract, and diff gates pass, and the native release interlock remains closed. Host full builds selected the already-installed macOS 15.4 SDK through a temporary isolated wrapper because pinned Zig 0.15.2 cannot consume the host macOS 26.5 SDK's current link metadata; the wrapper was removed after both safety modes passed and no implementation byte changed. No production route, public claim, version, native graduation, release interlock, tag, artifact/package publication, deployment, or external state changed. | `313ea03` |
| `NSASS-011-PLAIN-CSS-FUNCTION-ARGUMENTS` | `VERIFIED` | Added provider-free native Zig evaluation of already-admitted Sass expressions inside otherwise unknown CSS-function arguments before the call is serialized. SCSS and indented Sass cover arithmetic, module and user-function calls, nested CSS calls, source-order single evaluation, list splats, interpolated and safely decoded escaped names, null gaps, empty and trailing arguments, empty `var()` fallbacks, data-URL commas, and quoted literals. Callable-producing module calls, callable-producing user functions, variables holding callables, callable values nested or in later arguments, maps, and keyword arguments fail closed rather than leaking Sass syntax into CSS. Existing known-wrapper paths for `rgb`, `lab`, `color`, `calc`, `min`, `sin`, and `opacity` also reject nested callables. Function-argument and temporary ceilings have direct regressions, and the exhaustive allocation-failure fixture executes the new path. Fresh Dart Sass 1.101.0 probes reproduce exact compressed SCSS `.values{arithmetic:outer(3px);module:outer(number);user:outer(3);nested:outer(inner(4));spread:outer(a, b);interpolated:outer(5);escaped:outer(6);nulls:outer(, 1, );empty:outer();trailing:outer(1);var-empty:var(--missing, );url-data:url(data:image/svg+xml,%3Csvg%3E);literal:outer("mark(8)");evaluations:4}` and indented Sass `.sass{arithmetic:outer(3px);module:outer(number);user:outer(3);evaluations:1}`. Test-first focused Debug produced the expected 279/281 red boundary before implementation. Direct evaluator coverage passes 148/148; focused native Debug/ReleaseSafe each pass 23/23 steps with 281/281 tests; full compiler Debug/ReleaseSafe each pass 60/60 steps with 776/776 tests. The evaluator cross-compiles without emission for Windows x64 GNU and Linux ARM64 GNU in both modes. Native contract 10/10 plus check adds only `plain-css-function-argument-evaluation` while keeping the recursive repository-owned Zig/`std`/`builtin` closure, both native Sass rows private, production-unreachable, and release-fail-closed. Canonical Sass reference tests pass 28/28, five-language product preprocessing passes 30/30, preprocessor package policy passes 6/6 plus check, and the autonomous-runner selftest passes 58/58. Repository Zig formatting, JavaScript/JSON syntax, and diff checks pass. This checkpoint admits no new callable creation, invocation, or inspection and makes no claim for lazy conditional-function semantics. No production route, public claim, version, native graduation, release interlock, tag, artifact/package publication, deployment, or external state changed. | `be99154` |
| `NSASS-011-META-CONTENT-ACCEPTANCE` | `VERIFIED` | Added provider-free native Zig `meta.accepts-content()` through default, exact custom, and unprefixed `sass:meta` namespaces in SCSS and indented Sass. Stable first-class user-mixin references retain their original content contract after a same-name redefinition; direct and control-flow-nested `@content` return true, a content-free declaration returns false, canonical `$mixin` binding evaluates once, underscore spelling resolves normally, and module reflection sees the function. The exact loaded built-in inventory is exhaustive: `load-css` returns false and `apply` returns true. Non-mixin values, missing or case-mismatched namespaces, missing or extra arguments, unknown keywords, and every allocation failure fail closed with no partial CSS; the shared evaluator operation and cancellation boundaries remain unchanged. Fresh pinned-oracle probes reproduce exact SCSS `.values{old:false;new:true;direct:true;nested:true;marked:true;keyword:true;alias:true;star:true;load:false;load-alias:false;apply:true;reflected:true;evaluations:1}` and indented Sass `.sass{user:true;load:false;apply:true}`. Test-first focused Debug stopped at the missing module member before implementation. Direct evaluator Debug/ReleaseSafe pass 146/146; focused native Debug/ReleaseSafe each pass 23/23 steps with 279/279 tests; full compiler Debug/ReleaseSafe each pass 60/60 steps with 774/774 tests. The evaluator cross-compiles for Windows x64 GNU and Linux ARM64 GNU in both modes. Native contract 10/10 plus check adds only `built-in-meta-content-acceptance` while keeping the recursive repository-owned Zig/`std`/`builtin` closure, both native Sass rows private, production-unreachable, and release-fail-closed. Canonical Sass reference tests pass 28/28, five-language product preprocessing passes 30/30, preprocessor package policy passes 6/6 plus check, and the autonomous-runner selftest passes 58/58. Repository Zig formatting, JavaScript/JSON syntax, and diff checks pass. The sibling scan found a pre-existing unknown-CSS-function path that preserves a nested callable-producing module call as raw bytes; it remains an explicit unclaimed future boundary and was not admitted here. No production route, public claim, version, native graduation, release interlock, tag, artifact/package publication, deployment, or external state changed. | `7da73bb` |
| `NSASS-011-META-MIXIN-REFERENCES` | `VERIFIED` | Added provider-free native Zig creation of first-class mixin references with `meta.get-mixin()` through default, exact custom, and unprefixed `sass:meta` namespaces in SCSS and indented Sass, plus the canonical legacy global. Declaration-order-visible user mixins preserve identity across stored references and underscore/hyphen or escaped spellings, while later redefinitions and different declarations compare unequal and case remains distinct. Canonical `$name` plus optional `$module` binding evaluates source arguments once; explicit null retains local lookup, and an exact loaded namespace exposes only the two mixins measured from Dart Sass 1.101.0's `sass:meta` module, `load-css` and `apply`. Built-in references are typed as mixins, remain distinct, follow custom namespaces, and align `meta.mixin-exists()` with the same exact set. The global call emits the exact deprecation warning. Missing or later declarations, case mismatches, local/module ownership mistakes, loaded modules without that mixin, missing namespaces, non-string values, missing or extra arguments, unknown keywords, direct callable CSS serialization, unavailable callable inspection, temporary exhaustion, and every allocation failure fail closed without partial CSS. Callable invocation, `meta.accepts-content()`, `meta.apply()` execution, first-class functions, module enumeration, dynamic loading, and user-module reflection remain explicit unavailable boundaries. Fresh pinned-oracle probes reproduce exact compressed SCSS `.values{type:mixin;same:true;redefined:false;different:false;alias:mixin;star:mixin;builtin:mixin;builtin-alias:true;builtin-apply:mixin;builtin-distinct:false;builtin-reflected:true;apply-reflected:true;reflected:true;evaluations:2}` and indented Sass `.sass{type:mixin;same:true;builtin:mixin}`, plus identity, declaration order, aliases, the exact built-in inventory, invalid bindings, and warning behavior. Test-first focused Debug recorded the expected 274/277 red boundary before implementation. Direct evaluator Debug/ReleaseSafe pass 144/144; focused native Debug/ReleaseSafe each pass 23/23 steps with 277/277 tests; full compiler Debug/ReleaseSafe each pass 60/60 steps with 772/772 tests. The evaluator cross-compiles without emission for Windows x64 GNU and Linux ARM64 GNU in both modes. Native contract 10/10 plus check adds only `built-in-meta-mixin-references` while retaining the recursive Zig/`std`/`builtin` production closure, private native Sass rows, zero package/optional/runtime dependencies, production unreachability, and the closed release interlock. Canonical Sass reference tests pass 28/28, five-language product preprocessing passes 30/30, preprocessor package policy passes 6/6 plus check, and the autonomous-runner selftest passes 58/58. Repository Zig formatting, JavaScript/JSON syntax, and diff checks pass. Exact-main Build `30111666747` passed comprehensive Test Suite `89542405044` and all five native archive/offline-install jobs on exact integrated commit `d78f25ea871c927bf46d61d4ca78e08f72cab598`: macOS ARM64 `89542405139`, Linux ARM64 `89542405096`, Windows x64 `89542405107`, Linux x64 `89542405114`, and macOS x64 `89542405146`. Artifacts are respectively `8603841333`, `8603883319`, `8603846064`, `8604069383`, and `8604010872`. Production routing, public capability claims, version, tag, release/artifact/package publication, deployment, native graduation, and the release interlock did not change. | `d78f25e` |
| `NSASS-011-META-CONTENT-EXISTENCE` | `VERIFIED` | Added provider-free native Zig `meta.content-exists()` through default, exact custom, and unprefixed `sass:meta` namespaces in SCSS and indented Sass, plus the canonical legacy global. A bounded runtime mixin-body context distinguishes legal reflection from merely retained content state: no-content and content-bearing invocations return false and true respectively; nested mixins isolate their own state; calls used as mixin or function arguments observe the caller mixin; and user-function bodies, mixin defaults, content-parameter defaults, and caller-provided content execution reject the call even when an enclosing invocation retained content. The fixed binder admits exactly zero arguments, underscore spelling resolves through normal Sass name equivalence, `meta.function-exists()` reflects the newly owned module function, and the global call emits the exact Dart Sass deprecation warning. Root calls, missing and case-mismatched namespaces, extra positional arguments, unknown keywords, and every allocation failure abort private staging without partial CSS. Fresh Dart Sass 1.101.0 black-box probes agree on compressed SCSS/indented-Sass output, nested isolation, caller argument behavior, all rejected callable/default/content contexts, namespace handling, arity, keywords, and warning text. A mechanical mapping comparison proves all 89 prior global built-ins unchanged and exactly one addition. Focused native Debug/ReleaseSafe each pass 23/23 steps with 274/274 tests; full compiler Debug/ReleaseSafe each pass 60/60 steps with 769/769 tests. The evaluator cross-compiles without emission for Windows x64 GNU and Linux ARM64 GNU in both modes. Native contract 10/10 plus check adds only `built-in-meta-content-existence` while keeping both native Sass rows private, production-unreachable, and release-fail-closed. Canonical Sass reference tests pass 28/28, five-language product preprocessing passes 30/30, preprocessor package policy passes 6/6 plus check, and the autonomous-runner selftest passes 58/58. Zig/JavaScript/JSON formatting and syntax plus diff checks pass. Exact-main Build `30107164340` passed comprehensive Test Suite `89527287638` and all five native archive/offline-install jobs on exact integrated commit `6f8300d6544a80af58e790c57d486cf1d36b7a0b`: macOS ARM64 `89527287719`, Linux x64 `89527287727`, Windows x64 `89527287767`, macOS x64 `89527287910`, and Linux ARM64 `89527288186`. Artifacts are respectively `8602121358`, `8602315877`, `8602153698`, `8602305479`, and `8602177053`. Production routing, public capability claims, version, tag, release/artifact/package publication, deployment, native graduation, and the release interlock did not change. | `6f8300d` |
| `NSASS-011-META-CALC-INTROSPECTION` | `VERIFIED` | Added provider-free native Zig `meta.calc-name()` and `meta.calc-args()` through default, exact custom, and unprefixed `sass:meta` namespaces in SCSS and indented Sass. The evaluator recognizes the 21 already-admitted retained calculation names, decodes CSS identifier escapes, canonicalizes name case and inspected body whitespace/comments, returns exact quoted names and ordered comma lists, materializes reducible numeric arguments as native numbers, preserves nested retained calculations as calculation values, and keeps deferred CSS arguments as strings. Canonical `$calc` binding evaluates once; non-calculation/reduced/quoted/composite/map values, missing or extra arguments, unknown or duplicate keywords, namespace-case mismatches, empty arguments, function-argument exhaustion, temporary/operation ceilings, cancellation, and every allocation failure abort without partial CSS. Exact Dart Sass 1.101.0 black-box probes agree on positive output and non-calculation rejection. Direct focused Debug/ReleaseSafe each pass 23/23 steps with 271/271 tests; full compiler Debug/ReleaseSafe each pass 60/60 steps with 766/766 tests. The unchanged source passes the full matrix again under official Zig 0.15.2 Linux ARM64 in `alpine@sha256:28bd5fe8b56d1bd048e5babf5b10710ebe0bae67db86916198a6eec434943f8b` as uid/gid 65534 with no network or capabilities, `no-new-privileges`, read-only source/toolchain/root, 512 pids, 4-GiB memory/swap, `-j1`, and tmpfs-only caches/output/temp. Native contract 10/10 plus check adds only `built-in-meta-calculation-introspection` and keeps all native rows private, production-unreachable, and release-fail-closed. Repository policy passes 141/141, autodevelop 47/47 before the independent runner repair, all 16 generated/check gates, production audits 0/0/0, product/reference preprocessing 30/30, format 2/2 plus exact matrix, and the isolated site 151/151 plus production build. Zig/JavaScript/JSON formatting, syntax, and diff checks pass. Exact-main Build `30103254468` passed comprehensive Test Suite `89514325720` and all five native archive/offline-install jobs on exact integrated commit `0f8b9dad50da18d9e1e68484a0d3350079812d9a`: Windows x64 `89514325835`, macOS ARM64 `89514325836`, macOS x64 `89514325837`, Linux ARM64 `89514325912`, and Linux x64 `89514326043`. Artifacts are respectively `8600581271`, `8600571320`, `8600731130`, `8600633095`, and `8600790130`. Production routing, public capability claims, version, tag, release/artifact/package publication, deployment, native graduation, and the release interlock did not change. | `7ed0127`, hosted closure `0f8b9da` |
| `SITE-001-TERMINAL-FUTURE-LANDING` | `VERIFIED` | Rebuilt the package front door from the operator-supplied truth pack as an eight-movement terminal-noir story: a once-per-session 1.2-second boot with click/reduced-motion bypass; an exact experimental/source-versus-npm hero boundary and copyable real install command; one adaptive, intersection-observed/rAF canvas convergence with accessible text; fail-closed security, deterministic replay, atomic-output, and one-owned-result manifesto gates; the existing five-language executable fixture corpus upgraded into keyboard-roving recorded-output tabs with exact pinned providers, UTF-8 byte counts, computed reduction, labeled deterministic digests, and no live-browser-compiler pretense; an evidence-locked benchmark panel with no timing/ranking/multiplier; an ADR-013 `TARGET STATE · NOT SHIPPED` corridor; real npm/source/Zig entry points; five delivery targets; terminal chrome/footer; a diagnostic route-not-found surface; animated-caret favicon; and the required console banner. The professional README now leads to the site and lab, makes current native/reference boundaries visible, and publishes the real 43-series/860-observation benchmark program while withholding comparative results until controlled Linux x64 evidence exists. The 1200×630, 824,714-byte social card was generated once, resized without meaningful crop, visually inspected, and wired through exact OG/Twitter metadata. Archivo and JetBrains Mono Latin variable WOFF2 files are self-hosted with their OFL texts and `font-display: swap`. Route splitting plus a new manifest-driven fail-closed budget gate measures the landing at 86.6 KiB gzip across three JavaScript chunks against a 160 KiB limit; no new runtime dependency was added. The site passes 151/151 tests across 26 files and production build; documentation passes 7/7 over 38 Markdown files, 69 fences/53 executable, eight site-rendered fences, 49 links, and 23 literal routes. The strengthened link gate now validates same-page and rooted JSX element IDs instead of exempting anchors. Five-language product evidence passes 30/30, the format contract passes 2/2 plus its exact matrix, capability status passes 25 rows/24 gates, root/docs production audits report zero, image dimensions/size and no-script/font/banned-claim/bundle contracts are executable, and diff checks pass. Initial checkpoint `c4b0fba54a6157f8fee38156061d4673557df3d8` deployed through Documentation run `29904509914`. Build run `29904510249` then failed closed before the main suite because the professional README reduced canonical `0.5.0-rc.1` copy from two exact occurrences to one; the subsequent local replay also exposed that the brief intentionally requires three exact homepage placements while the older gate required one. The repair restores exactly two README placements, strengthens the homepage expectation to exactly three, and updates its mutation fixture; release-version test/check now pass 7/7 and exact 28-surface verification. Exact-main closure checkpoint `7b7cbcd16c3b1904fca442949560ee3d0b22428c` passed Build `29906041968`, Test Suite `88877622623`, and all five native archive/offline-install jobs; the live Pages origin serves the exact title and social metadata. No benchmark result, package, release, or tag was created. | `c4b0fba` + `513f83b` + `f3bb620` + `7b7cbcd` |
| `NSASS-011-META-EXISTENCE-QUERIES` | `VERIFIED` | Added provider-free native Zig `meta.function-exists()`, `meta.mixin-exists()`, `meta.variable-exists()`, `meta.global-variable-exists()`, and deprecated `meta.feature-exists()` through default, exact custom, and unprefixed `sass:meta` namespaces in SCSS and indented Sass, plus their canonical legacy globals. The implementation reflects only current native authority: declaration-order-visible user functions and mixins, lexical versus global variable scope, all admitted global built-ins, explicitly loaded built-in modules, and unprefixed module imports. Sass names decode CSS escapes and normalize underscores to hyphens while remaining case-distinct; a leading `$` is not silently accepted. Module arguments accept null or an exact loaded namespace, built-in module functions and the admitted `sass:math` constant set are reflected, and unavailable names return false without widening authority. `feature-exists()` owns exactly `global-variable-shadowing`, `extend-selector-pseudoclass`, `units-level-3`, `at-error`, and `custom-property`; unlike declaration names, feature names do not receive underscore normalization. Global calls emit the exact global-built-in deprecation warning and both global/module feature calls emit the exact feature deprecation warning. Canonical `$name` and optional `$module` binding evaluates valid arguments once; non-string names/modules, missing or extra arguments, unknown keywords, missing namespaces, unsupported module-variable use, temporary/operation ceilings, and every allocation failure abort private staging with no partial CSS. A mechanical before/after mapping comparison proved all 84 prior global built-ins unchanged and exactly five additions. Direct evaluator Debug/ReleaseSafe pass 136/136; focused native Debug/ReleaseSafe each pass 23/23 steps with 269/269 tests, and full Debug/ReleaseSafe each pass 60/60 with 764/764 tests. The evaluator cross-compiles for Windows x64 GNU and Linux ARM64 GNU in both modes. Native contract schema v3 adds only `built-in-meta-existence-queries`; 10/10 plus check retains the recursive repository-owned Zig/`std`/`builtin` closure with zero target package or optional dependencies, provider engine, compile child process, network, download, or non-system runtime. Product/reference preprocessor matrices pass 151/151; autodevelop passes 47/47; every individual policy gate and all 16 generated/check gates pass. Documentation integrity passes 7/7 over 38 Markdown files, 68 fences/53 executable, 49 links, and 24 routes; the isolated site passes 139/139 plus production build. VS Code passes 13/13 and packages the exact five-file 120,473-byte VSIX; real Neovim 0.11.7 and 0.12.4 integrations pass. Package-consumer Debug/ReleaseSafe pass 6/6 with 2/2 tests, build integration passes 7/7 with 1/1, and benchmark smoke passes 3/3 in both modes. Canonical Linux ARM64 release-container and immutable-source Homebrew smokes pass; actionlint 1.7.12 passes with only the reviewed custom benchmark-runner label ignore. Shell, JavaScript, 36 JSON files, Ruby, Zig formatting, and diff gates pass; root/docs/VS Code production and full audits report zero. The npm dry pack remains exactly 28 files, 92,750 packed bytes, and 406,429 unpacked bytes. Exact source commit `62161bf3e4cf2958b3de8808c699e8e6e3001bb2` and Git archive SHA-256 `8d000ec8fae4eeb5a3c220da66a7cc83273640dee2425b718f84bb06e124d358` pass the same native/full/package/example/benchmark matrix under checksum-verified Zig 0.15.2 archive SHA-256 `958ed7d1e00d0ea76590d27666efbf7a932281b3d7ba0c6b01b0ff26498f667f` on Linux ARM64 `alpine@sha256:28bd5fe8b56d1bd048e5babf5b10710ebe0bae67db86916198a6eec434943f8b`. Confinement ran as uid/gid 65534 with no network or capabilities, `no-new-privileges`, read-only root/source/toolchain, 512 pids, 4-GiB memory with zero cgroup swap, `-j1`, and bounded executable tmpfs caches/output/temp; mount evidence proved `/src` and `/opt/zig` read-only, source/toolchain touch probes failed, network I/O remained zero, and the container exited 0 without OOM. Three environment-only retries preceded the final unchanged-archive run: BusyBox awk rejected a reserved loop variable, Zig test temporaries required explicit source-local cache overlays, and the overlays required empty mountpoint directories in the extracted staging tree; the archive hash remained unchanged. Local documentation/site automation also required only explicit `NVIM` selection and isolation from a concurrent VS Code artifact mutation before exact sequential reruns passed. Exact-main Build `29820295031` passed Test Suite `88601099673` plus macOS ARM64 `88601099689`, macOS x64 `88601099693`, Linux ARM64 `88601099720`, Linux x64 `88601099726`, and Windows x64 `88601099831`; artifacts are respectively `8491143192`, `8491221060`, `8491181475`, `8491326994`, and `8491123843`. GitHub's only annotation is the known non-failing immutable-action Node 20-to-24 warning. User-module reflection and all later `sass:meta` slices remain unavailable. The four direct provider-era dependencies and 86 locked packages remain quarantined development oracles, so the unpublished transitional package is not claimed as dependency-free; production routing, public capability claims, version, tag, release/artifact/package publication, deployment, native graduation, and the release interlock did not change. | `62161bf` |
| `NSASS-011-SELECTOR-DIR-FUNCTION-GRAMMAR` | `VERIFIED` | Test-first selector and evaluator work stopped at the prior `:dir()` unsupported boundary. Fresh Dart Sass 1.101.0 black-box probes established that decoded lowercase and escaped-lowercase `:dir()` use opaque component-value grammar rather than selector grammar: empty arguments, comma edges, multiple words, identifiers, numbers, stars, ranges, strings, `var()`, nested functions, comments, `.a`, `&`, `@x`, `#x`, and balanced square/curly blocks are admitted and normalized, while top-level semicolons, malformed comments, unbalanced delimiters, invalid UTF-8/control bytes, and exhausted limits fail without a partial result. Function-name case remains semantically distinct, but `:DIR( LTR )` and `:DiR( ltr   rtl )` still normalize argument components to `:DIR(LTR)` and `:DiR(ltr rtl)`. Relations are exact normalized identity only: direction and argument case remain distinct, escaped spellings converge, ordinary simple subjects still cover compounds such as `.a:dir(ltr)`, and no relation recurses into opaque argument bytes. Unification exact-deduplicates or coexists with distinct directions/name case; extension and replacement treat `:dir()` as one simple selector, combine ordinary compounds, rewrite exact whole functions, work beneath already-admitted outer selector-list pseudos, and never rewrite `.a` inside `:dir(.a)`. The direct parse expectation first failed red because authored spaces remained, and the evaluator integration first failed red at `UnsupportedSelectorRelation`; the implementation then added only one enum member plus exact-lowercase and ignore-case normalization mappings. The stale unsupported sentinel moved to arbitrary `:foo()` and remains fail-closed. Exact evaluator CSS is `.values{parsed::dir(LTR , rtl);uppercase::DIR(RTL);simple:.root,:dir(ltr/**/ rtl),:hover;exact:true;distinct:false;case-distinct:false;subject:true;reverse-subject:false;unified::dir(ltr):dir(rtl)}.extensions{partial:.a:dir(ltr),.x:dir(ltr);replaced:.x:dir(ltr);whole:.a:dir(ltr),.a.x;nested::is(:dir(ltr), .x, .a);opaque::dir(.a)}`. Direct selector/evaluator roots pass 31/31 and 132/132 in Debug and ReleaseSafe; focused native Debug/ReleaseSafe each pass 23/23 steps with 265/265 tests, and full Debug/ReleaseSafe each pass 60/60 with 760/760 tests. Selector/evaluator roots cross-compile for Windows x64 GNU and Linux ARM64 GNU in both modes. Exact source commit `85a346da346258c0fe9c7de100191bc0ebdf9796`, Git archive SHA-256 `1214d793b629b5cce0d2596181f3b710458946d799a90bfb32406caffbd20d4e`, and toolchain archive SHA-256 `958ed7d1e00d0ea76590d27666efbf7a932281b3d7ba0c6b01b0ff26498f667f` pass the same native/full matrix under Zig 0.15.2 Linux ARM64 confinement as uid/gid 65534 with no network or capabilities, `no-new-privileges`, read-only exact source/toolchain/root image, 512 pids, 4-GiB memory/swap, `-j1`, and uid/gid-owned executable tmpfs caches/output/temp on `alpine@sha256:28bd5fe8b56d1bd048e5babf5b10710ebe0bae67db86916198a6eec434943f8b`; mount-table evidence proves `/src` and `/opt/zig` are `ro`. In the same confinement, package-consumer Debug/ReleaseSafe passes 6/6 with 2/2 tests, build integration passes 7/7 with 1/1, and benchmark smoke passes 3/3 in both modes without an archive. Native contract schema v3 adds only `built-in-selector-dir-functional-pseudo-grammar`; 10/10 plus check retains the recursive Zig/`std`/`builtin` closure with zero target package/optional dependencies, provider engine, compile child process, network, download, or non-system runtime. Product/reference matrices pass 151/151, repository policy 141/141, and autodevelop 47/47. All 16 generated checks plus workflow/version/release/package/dependency/prefix/capability/documentation/format/compatibility/transform/benchmark/actionlint-with-reviewed-custom-label/shell/JavaScript/JSON/Ruby/formatting/diff gates pass; the isolated site passes 139/139 plus production build, VS Code passes 13/13 plus the exact five-file 120,473-byte VSIX, and real Neovim 0.11.7 and 0.12.4 integrations pass. Production audits report root/docs/VS Code 0/0/0. The npm dry pack remains exactly 28 files, 92,750 packed bytes, and 406,429 unpacked bytes. Local canonical Linux ARM64 release-container and immutable-source Homebrew formula smokes pass; Homebrew verifies `526002807edc856eb2dc391551ac3d5c1b77da00` at `dbab9f777b795742716841354e0bfe30555cc1d54b21f2a446fdc7dc523e26b1`. One post-review direct `zig test` invocation lacked the build graph's `native_preprocessor` module import; the exact build-owned native suites immediately passed both modes without a source change. Exact-main Build `29816048931` passed Test Suite `88587551718` plus macOS x64 `88587551696`, Linux x64 `88587551700`, Windows x64 `88587551726`, macOS ARM64 `88587551729`, and Linux ARM64 `88587551763`; artifacts are respectively `8489513372`, `8489682489`, `8489443077`, `8489455987`, and `8489489844`. GitHub's only annotations are the known non-failing immutable-action Node 20-to-24 warnings. Host selectors, arbitrary functional pseudos, pseudo-elements, broader selector inference, user-module reflection, imports/forwards, and later meta slices remain unavailable. The four direct provider-era dependencies and 86 locked packages remain quarantined development oracles, so the unpublished transitional package is not claimed as dependency-free; production routing, public capability claims, version, tag, release/artifact/package publication, deployment, native graduation, and the release interlock did not change. | `85a346d` |
| `NSASS-011-SELECTOR-NONSELECTOR-FUNCTION-GRAMMAR` | `VERIFIED` | Test-first selector and evaluator work stopped at one independently measured non-selector functional pseudo. Fresh Dart Sass 1.101.0 black-box probes admitted only decoded lowercase/escaped-lowercase `:lang()` semantics and measured its opaque component-value normalization across outer/internal whitespace, comments, escapes, strings, commas, numbers, ranges, nested functions and `[]`/`{}` blocks, parent/at/hash tokens, invalid UTF-8/control bytes, malformed delimiters/comments, relations, unification, extension, replacement, and every shared resource boundary. A late oracle recheck after the first green source checkpoint `7648a8f2615c33f89724e1a56c8b9cf972f05c5f` disproved the provisional uppercase-preservation assumption: `:LANG( EN )` serializes as `:LANG(EN)`. The expectation changed first and failed red with the authored spaces; final commit `2e29f04e5d0c285106d74ea9102cd4d0042943a2` classifies opaque functional normalization case-insensitively while keeping semantic admission exact-lowercase, so `:LANG()`/`:LaNg()` normalize their component bytes but remain case-distinct in relations. The implementation trims outer whitespace/comments, collapses internal whitespace, preserves significant comments, quote style, nested component blocks and measured opaque tokens, rejects top-level semicolons and malformed/unsafe input, skips comments correctly in compound/relation/paren scanners, and retains selector/byte/component/temporary/operation/allocation and 64-level depth ceilings. Relations are normalized exact identity only and never recurse into arguments; ordinary compound subjects still reason normally. Unification exact-deduplicates and otherwise coexists; extension/replacement treats the entire `:lang()` as one simple and never rewrites opaque argument bytes. Exact evaluator CSS is `.values{parsed::lang(EN , fr);uppercase::LANG(EN);simple:.root,:lang(en/**/, fr),:hover;exact:true;distinct:false;case-distinct:false;subject:true;reverse-subject:false;unified::lang(en):lang(fr)}.extensions{partial:.a:lang(en),.x:lang(en);replaced:.x:lang(en);whole:.a:lang(en),.a.x;nested::is(:lang(en), .x, .a);opaque::lang(.a)}`. `:dir()`, host selectors, arbitrary functions, pseudo-elements, and broader inference remain unavailable. Direct selector/evaluator roots pass 30/30 and 131/131 in Debug and ReleaseSafe; focused exact Zig 0.15.2 Debug/ReleaseSafe each pass 23/23 steps with 263/263 tests, and full Debug/ReleaseSafe each pass 60/60 with 758/758 tests. Selector/evaluator roots cross-compile for Windows x64 GNU and Linux ARM64 GNU in both modes. Exact source commit `2e29f04e5d0c285106d74ea9102cd4d0042943a2`, Git archive SHA-256 `467d1242f7ac0547f4e0ebe8789b0c7768e37308579073ad081bd0f503a9122e`, and toolchain archive SHA-256 `958ed7d1e00d0ea76590d27666efbf7a932281b3d7ba0c6b01b0ff26498f667f` pass the same focused/full matrix under Zig 0.15.2 Linux ARM64 confinement as uid/gid 65534 with no network or capabilities, `no-new-privileges`, read-only exact source/toolchain/root image, 512 pids, 4-GiB memory/swap, `-j1`, and uid/gid-owned executable tmpfs caches/output/temp on `alpine@sha256:28bd5fe8b56d1bd048e5babf5b10710ebe0bae67db86916198a6eec434943f8b`; mount-table evidence proves `/src` and `/opt/zig` are `ro`. In the same confinement, package-consumer Debug/ReleaseSafe passes 6/6 with 2/2 tests, build integration passes 7/7 with 1/1, and benchmark smoke passes 3/3 in both modes without an archive. The first all-check invocation omitted the pinned Zig path, and the first confinement verifier let `sh -u` expand awk positional fields before any build; exact environment/verifier reruns passed without source change. Native contract schema v3 adds only `built-in-selector-lang-functional-pseudo-grammar`; 10/10 plus check retains the recursive Zig/`std`/`builtin` closure with zero package/optional dependencies, provider engine, compile child process, network, download, or non-system runtime in the native target. Product/reference matrices pass 151/151, repository policy 141/141, and autodevelop 47/47. All 16 generated checks plus workflow/version/release/package/dependency/prefix/capability/documentation/format/compatibility/transform/benchmark/actionlint-with-reviewed-custom-label/shell/JavaScript/JSON/Ruby/formatting/diff gates pass; the isolated site passes 139/139 plus production build, VS Code passes 13/13 plus the exact five-file 120,473-byte VSIX, and real Neovim 0.11.7 and 0.12.4 integrations pass. Production audits report root/docs/VS Code 0/0/0. The npm dry pack remains exactly 28 files, 92,750 packed bytes, and 406,429 unpacked bytes. Local canonical Linux ARM64 release-container and immutable-source Homebrew formula smokes pass; Homebrew verifies `526002807edc856eb2dc391551ac3d5c1b77da00` at `dbab9f777b795742716841354e0bfe30555cc1d54b21f2a446fdc7dc523e26b1`. The initial source Build `29811888122` passed all six jobs on `7648a8f` but is superseded by the oracle correction. Final exact-main Build `29813147348` passed Test Suite `88578369943` plus Windows x64 `88578369853`, macOS ARM64 `88578369876`, Linux x64 `88578369905`, Linux ARM64 `88578369946`, and macOS x64 `88578369969`; artifacts are respectively `8488316307`, `8488322264`, `8488343321`, `8488348088`, and `8488521612`. GitHub's only annotation is the known non-failing immutable-action Node 20-to-24 warning. The four direct provider-era dependencies and 86 locked packages remain quarantined development oracles, so the unpublished transitional package is not claimed as dependency-free; production routing, public capability claims, version, tag, release/artifact/package publication, deployment, native graduation, and the release interlock did not change. | `7648a8f`, `2e29f04` |
| `NSASS-011-SELECTOR-NTH-FUNCTION-GRAMMAR` | `VERIFIED` | Test-first selector and evaluator evidence stopped at the prior conservative functional-grammar boundary. Fresh Dart Sass 1.101.0 black-box measurements corrected several provisional assumptions before implementation: comments participate in Sass An+B whitespace with exact `of` boundaries; an escaped initial `odd`/`even` letter is invalid while later letters and `n` may be escaped; ordinary simple selectors may cover the final subject of nth arguments with bogus leading/trailing combinators even though exact nth-to-nth relations remain false; same-kind/exact-formula nested nth wrappers flatten during extension while a different formula or kind is discarded; and vendor-prefixed arbitrary nth functions are outside this deliberately admitted lowercase core. Added bounded native parsing and normalization for lowercase/escaped-lowercase `:nth-child()` and `:nth-last-child()` with `odd`, `even`, signed/unsigned integers, An+B, ASCII case normalization, spelling-preserving leading zeros, Sass comment/whitespace tokenization, and optional case/escape-insensitive `of <selector-list>`. Uppercase function names remain opaque/case-distinct; parent selectors, malformed formulas/lists, invalid scalars, and exhausted limits fail without partial output. Relations compare canonical formula spelling rather than mathematical equivalence, recursively compare exact-kind/formula selector lists, distinguish no-`of` from `of *`, expose optional selector subjects to ordinary simple-superselector reasoning, and reject exact nth relations containing bogus leading/trailing combinators. Unification deduplicates exact normalized nth selectors, preserves distinct formulas, and returns null when either nth selector argument is bogus. Extension/replacement rewrite only the selector list after `of`, preserve the formula, handle exact whole extendees, same-component and complex/context paths, flatten same-kind/exact-formula nested nth selectors, preserve rewritten nth selectors under `:has()`, discard incompatible nested wrappers, leave no-`of` selectors unchanged, and preserve a whole complex unchanged when any nth argument branch is bogus. All paths retain shared selector-count, byte, component, temporary-allocation, operation, allocation, and 64-level recursion ceilings; exact relation/extension operation bounds and exhaustive allocation traversal are executable. Exact SCSS integration bytes are `.values{parsed::nth-child(2n+01 of .a, .b);simple:.root,:nth-last-child(-n+4 of .a, .b),:hover;exact:true;formula-equivalent:false;selector-subset:true;subject:true;reverse-subject:false;relative:false;unified::nth-child(odd):nth-child(2n+1)}` and `.extensions{extend::nth-child(2n+1 of .a, .x, .b)}`. Direct selector/evaluator roots pass 29/29 and 130/130 in Debug; focused exact Zig 0.15.2 Debug/ReleaseSafe each pass 23/23 steps with 261/261 tests, and full Debug/ReleaseSafe each pass 60/60 with 756/756 tests. Selector/evaluator roots cross-compile for Windows x64 GNU and Linux ARM64 GNU in both modes. Exact source commit `5dc654253e6294f8891dcf623bf44f325c8c09f5` passes the same focused/full Debug/ReleaseSafe matrix under checksum-verified Zig 0.15.2 Linux ARM64 confinement as uid/gid 65534 with no network or capabilities, `no-new-privileges`, read-only exact Git archive/toolchain/container, 512 pids, 4-GiB memory/swap, `-j1`, and uid/gid-owned executable tmpfs caches/output/temp on `alpine@sha256:28bd5fe8b56d1bd048e5babf5b10710ebe0bae67db86916198a6eec434943f8b`; the immutable toolchain archive SHA-256 is `958ed7d1e00d0ea76590d27666efbf7a932281b3d7ba0c6b01b0ff26498f667f`. The first confinement invocation proved Zig's test harness requires cwd-local `.zig-cache` scratch even with explicit build caches; the exact rerun added only nested tmpfs scratch overlays while leaving all source bytes read-only. In the same confinement, package-consumer Debug/ReleaseSafe passes 6/6 steps and 2/2 tests, build integration passes 7/7 and 1/1, and benchmark smoke passes 3/3 in both modes without an archive. Native contract schema v3 adds only `built-in-selector-nth-function-grammar`; 10/10 plus check retains the recursive Zig/`std`/`builtin` closure with zero package or optional dependencies, provider engine, compile child process, network, download, or non-system runtime in the native target. Product/reference matrices pass 151/151, repository policy 141/141, and autodevelop 47/47. Workflow/version/release/native-contract/package/dependency/prefix/capability/documentation/format/compatibility/transform/benchmark/actionlint/shell/JavaScript/JSON/Ruby/formatting/diff gates pass; the isolated site passes 139/139 plus production build, VS Code passes 13/13 plus the exact five-file 120,473-byte VSIX, and real Neovim 0.11.7 and 0.12.4 integrations pass. Initial host invocations that omitted the pinned Neovim version, exact Zig path, or isolated VS Code cleanup failed only their environment/tree-preservation preconditions; exact reruns passed without source change. Complete root/docs/VS Code production audits report zero vulnerabilities. The npm dry pack remains exactly 28 files, 92,750 packed bytes, and 406,429 unpacked bytes. Local canonical Linux ARM64 release-container and immutable-source Homebrew formula smokes pass; Homebrew verifies `526002807edc856eb2dc391551ac3d5c1b77da00` at `dbab9f777b795742716841354e0bfe30555cc1d54b21f2a446fdc7dc523e26b1`. Exact-main Build `29806255732` passed Test Suite `88557315158` and all five native archive/offline-install jobs on `5dc654253e6294f8891dcf623bf44f325c8c09f5`: Linux x64 `88557315201`, macOS ARM64 `88557315191`, macOS x64 `88557315096`, Linux ARM64 `88557315151`, and Windows x64 `88557315219`; artifacts are respectively `8485748864`, `8485646015`, `8485723375`, `8485671675`, and `8485633568`. GitHub's only annotation is the known non-failing immutable-action Node 20-to-24 warning. The four direct provider-era dependencies and 86 locked packages remain quarantined development oracles, so the unpublished transitional package is not claimed as dependency-free; production routing, public capability claims, version, tag, release/artifact/package publication, deployment, native graduation, and the release interlock did not change. | `5dc6542` |
| `NSASS-011-SELECTOR-FUNCTIONAL-PSEUDO-EXTEND-REPLACE` | `VERIFIED` | Test-first selector, evaluator, and exhaustive allocation-failure routes stopped at the prior `UnsupportedSelectorExtension` boundary. Added provider-free bounded extension/replacement inside decoded lowercase `:is()`, `:where()`, `:matches()`, `:any()`, `:-webkit-any()`, `:-moz-any()`, `:not()`, and `:has()` selector lists. Extension inserts the extender beside the matched extendee while replacement removes it; authored function context and argument order remain stable. Independently measured nested behavior flattens same-kind positive wrappers, applies the narrower `:not()` flatten/discard matrix, preserves recognized wrappers under `:has()`, recursively rewrites recognized functions inside larger compounds/complexes, chains a sole nested negation, and rejects an outer function whose arguments all disappear. Partial compounds, complex subjects, relative branches, whole-functional extendees, same-component cross-products, selector/extendee/extender lists, compatible functional extenders, incompatible recognized wrappers, exact case distinctions, and escaped lowercase names are executable. Relative branches retain exact `>`, `+`, and `~` leading combinators and intentionally remain non-reflexive for duplicate pruning, matching Dart Sass's repeated relative output. The extension preflight now uses a structural simple-selector upper bound rather than a source-byte quotient; generated nested members are recursively counted against `max_selectors`, and exact one-below selector, byte, component, temporary, operation, depth, and allocation limits fail without partial output. The evaluator's unsupported boundary moved from now-admitted `:is()` to `:host()`; an attempted `:nth-child()` placeholder correctly hit its still-conservative unimplemented grammar/operation boundary and was not used to widen scope. Exact Dart Sass 1.101.0 SCSS bytes are `.values{extend::is(.a, .x, .b);replace::is(.x, .b);context:.root:where(.a, .x, .b):hover;nested::not(.a, .x, .b, .c);partial::matches(.b.x, .c);complex::any(.a .b, .a .x, .c);relative::has(> .x, + .b);whole:.root:-webkit-any(.a, .b),.root.x;same-component:.a:is(.a, .x, .b),.x:is(.a, .x, .b);functional-extender::is(.a, .x, .y, .b);ignored-extender::is(.b);case-distinct::IS(.a,.b)}` and indented Sass bytes are `.sass{extended::where(.a, .x, .b);replaced::not(.x, .b);nested::not(.a, .x, .b, .c);relative::has(+ .a, + .x, ~ .b);whole:.root.x}`. Direct selector/evaluator suites pass 28/28 and 129/129 in Debug and ReleaseSafe; focused exact Zig 0.15.2 Debug/ReleaseSafe each pass 23/23 steps with 259/259 tests, and full Debug/ReleaseSafe each pass 60/60 with 754/754 tests. Selector/evaluator roots cross-compile for Windows x64 and Linux ARM64 in both modes. Exact source commit `cc9084b6a89ee2fae9d3462d45a788dc9044b175` passes the same focused/full matrix under checksum-verified Zig 0.15.2 Linux ARM64 confinement as uid/gid 65534 with no network or capabilities, `no-new-privileges`, read-only source/toolchain/container, 512 pids, 4-GiB memory/swap, `-j1`, and uid/gid-owned executable tmpfs caches/output/temp on `alpine@sha256:28bd5fe8b56d1bd048e5babf5b10710ebe0bae67db86916198a6eec434943f8b`; the immutable toolchain archive SHA-256 is `958ed7d1e00d0ea76590d27666efbf7a932281b3d7ba0c6b01b0ff26498f667f`. In the same confinement, package-consumer Debug/ReleaseSafe passes 6/6 steps and 2/2 tests, build integration passes 7/7 and 1/1, and benchmark smoke passes 3/3 in both modes without an archive. Native contract schema v3 adds only `built-in-selector-list-functional-pseudo-extend-replace`; 10/10 plus check retains the recursive Zig/`std`/`builtin` closure with zero package or optional dependencies, provider engine, compile child process, network, download, or non-system runtime in the native target. Product/reference matrices pass 151/151, repository policy 141/141, and autodevelop 47/47. Workflow/version/release/native-contract/package/dependency/prefix/capability/documentation/format/compatibility/transform/benchmark/actionlint/shell/JavaScript/JSON/Ruby/formatting/diff gates pass; the isolated site passes 139/139 plus production build, VS Code passes 13/13 plus the exact five-file 120,473-byte VSIX, and real Neovim 0.11.7 and 0.12.4 integrations pass. The first repository-policy invocation lacked the explicit pinned Neovim path, the first prefix check lacked the pinned Zig path, and direct host tests initially lacked the macOS SDK wrapper; each exact rerun passed with no source change. Complete root/docs/VS Code production audits report zero vulnerabilities. The npm dry pack remains exactly 28 files, 92,750 packed bytes, and 406,429 unpacked bytes. Local canonical Linux ARM64 release-container and immutable-source Homebrew formula smokes pass; Homebrew verifies `526002807edc856eb2dc391551ac3d5c1b77da00` at `dbab9f777b795742716841354e0bfe30555cc1d54b21f2a446fdc7dc523e26b1`. Exact-main Build `29800516173` passed Test Suite `88540528781` and all five native archive/offline-install jobs on `cc9084b6a89ee2fae9d3462d45a788dc9044b175`: Linux x64 `88540528759`, macOS ARM64 `88540528763`, macOS x64 `88540528766`, Linux ARM64 `88540528775`, and Windows x64 `88540528778`; artifacts are respectively `8483727819`, `8483633748`, `8483660208`, `8483638595`, and `8483605322`. The four direct provider-era dependencies and 86 locked packages remain quarantined development oracles, so the unpublished transitional package is not claimed as dependency-free; production routing, public capability claims, version, tag, release/artifact/package publication, deployment, native graduation, and the release interlock did not change. | `cc9084b` |
| `NSASS-011-SELECTOR-FUNCTIONAL-PSEUDO-RELATIONS` | `VERIFIED` | Test-first direct selector evidence stopped at the prior `UnsupportedSelectorRelation` boundary, and exhaustive allocation-failure traversal reached the same missing semantic path. Added provider-free bounded non-identical relations for decoded lowercase selector-list pseudos. The positive `:is()`, `:where()`, `:matches()`, `:any()`, `:-webkit-any()`, and `:-moz-any()` family shares union/subset semantics and cross-family equivalence; `:has()` compares positively only against `:has()`; and `:not()` reverses argument relation direction. Ordinary compounds/complexes compare against positive wrappers in both independently proven directions. Compound and complex arguments, nested functions, top-level alternative coverage, exact case distinctions, and legacy-prefix equivalence are supported. Explicit relative branches remain non-reflexive, while another selector alternative may cover them. A late Dart Sass oracle probe after the first green source push proved that a one-component non-relative positive-family superselector may cover the final subject of a relative argument, while a relative superselector, `:has()`, `:not()`, or multi-component superselector may not. The first follow-up red expected true but found false; the initial generalization then produced a second red by overclaiming `:is(.a .b)` over `:where(> .a .b)`. The final guard strips the subargument's one explicit leading combinator only when the superselector has exactly one non-relative component, matching `:is(.b.x)` over `:where(+ .a .b.x)` without widening the multi-component case. Both operands share selector-count accounting through `SelectorList.selector_count`, so nested members cannot escape `max_selectors`; semantic recursion retains the shared operation, component, temporary-allocation, byte, and 64-level depth ceilings, and ephemeral relation complexes release active counters on every path. Allocation injection traverses the complete semantic functional-relation route. Exact Dart Sass 1.101.0 SCSS bytes are `.values{is-subset:true;is-reverse:false;legacy-cross:true;plain-super:true;positive-super:true;not-subset:true;not-reverse:false;nested-not:false;nested-not-reverse:true;has-subset:true;has-reverse:false;has-nested:false;has-nested-reverse:true;relative:false;relative-covered:true;positive-relative:true;positive-relative-self:false;list-forward:false;list-reverse:true;case-distinct:false}` and indented Sass bytes are `.sass{positive:true;negated:true;relational:true;relative:false}`. Direct selector/evaluator suites pass 27/27 and 128/128 in Debug and ReleaseSafe; focused exact Zig 0.15.2 Debug/ReleaseSafe each pass 23/23 steps with 257/257 tests, and full Debug/ReleaseSafe each pass 60/60 with 752/752 tests. Selector/evaluator roots cross-compile for Windows x64 and Linux ARM64 in both modes. Exact final commit `8c4a09126a97ab7ebefdfa314a1c647f324edac7` passes the same focused/full Debug/ReleaseSafe matrix under checksum-verified Zig 0.15.2 Linux ARM64 confinement as uid/gid 65534 with no network or capabilities, `no-new-privileges`, read-only source/toolchain/container, 512 pids, 4-GiB memory/swap, `-j1`, and uid/gid-owned executable tmpfs caches/output/temp on `alpine@sha256:28bd5fe8b56d1bd048e5babf5b10710ebe0bae67db86916198a6eec434943f8b`; the immutable toolchain archive SHA-256 is `958ed7d1e00d0ea76590d27666efbf7a932281b3d7ba0c6b01b0ff26498f667f`. In the same confinement, package-consumer Debug/ReleaseSafe passes 6/6 steps and 2/2 tests, build integration passes 7/7 and 1/1, and benchmark smoke passes 3/3 in both modes without an archive. Native contract schema v3 adds only `built-in-selector-list-functional-pseudo-relations`; 10/10 plus check retains the recursive Zig/`std`/`builtin` closure with zero package or optional dependencies, provider engine, compile child process, network, download, or non-system runtime in the native target. Product/reference matrices pass 151/151, repository policy 141/141, and autodevelop 47/47. Workflow/version/release/native-contract/package/dependency/prefix/capability/documentation/format/compatibility/transform/benchmark/actionlint/shell/JavaScript/JSON/Ruby/formatting/diff gates pass; the isolated site passes 139/139 plus production build, VS Code passes 13/13 plus the exact five-file 120,473-byte VSIX, and real Neovim 0.11.7 and 0.12.4 integrations pass. The first site invocation observed only concurrent temporary VS Code output and passed in its required isolated lane; prefix generation and Homebrew first lacked the pinned Zig `PATH` and passed on exact rerun, with no source change. Complete root/docs/VS Code and production audits report zero vulnerabilities across 136/385/331 dependencies. The npm dry pack remains exactly 28 files, 92,750 packed bytes, and 406,429 unpacked bytes. Local canonical Linux ARM64 release-container and immutable-source Homebrew formula smokes pass; Homebrew verifies `526002807edc856eb2dc391551ac3d5c1b77da00` at `dbab9f777b795742716841354e0bfe30555cc1d54b21f2a446fdc7dc523e26b1`. The initial source commit's exact-main Build `29793744313` was fully green but became superseded when the late oracle edge was found; no pushed commit was rewritten. Final exact-main Build `29795483602` passed Test Suite `88525846562` and all five native archive/offline-install jobs on `8c4a09126a97ab7ebefdfa314a1c647f324edac7`: Linux x64 `88525846620`, macOS x64 `88525846664`, Windows x64 `88525846608`, Linux ARM64 `88525846623`, and macOS ARM64 `88525846593`; artifacts are respectively `8481981948`, `8481969867`, `8481879071`, `8481903694`, and `8481889089`. GitHub's only annotations are the known non-failing immutable-action Node 20-to-24 warnings. The four direct provider-era dependencies and 86 locked packages remain quarantined development oracles, so the unpublished transitional package is not claimed as dependency-free; production routing, public capability claims, version, tag, release/artifact/package publication, deployment, native graduation, and the release interlock did not change. | `0e22ad0`, `8c4a091` |
| `NSASS-011-SELECTOR-NORMALIZE-FUNCTIONAL-PSEUDOS` | `VERIFIED` | Test-first direct evidence expected `:not(.a, #b)` but retained authored escapes/spacing, while evaluator relation calls failed at the prior `UnsupportedFeature` boundary. Added provider-free recursive canonicalization for decoded lowercase `:not()`, `:is()`, `:where()`, `:has()`, `:matches()`, `:any()`, `:-webkit-any()`, and `:-moz-any()` selector-list functions. Escaped function names, nested recognized functions, selector escapes, attributes, leading relative combinators, and forgiving internal empty groups normalize deterministically; empty, leading-empty, trailing-empty, invalid-parent, invalid-scalar, excessive-depth, and exhausted-budget inputs fail without a partial result. Unknown/non-selector functions retain their existing argument grammar and established attribute normalization. Exact normalized relation identity and unification are admitted, while non-identical functional inference and all functional extension/replacement remain unavailable. Dart Sass 1.101.0's non-reflexive relative-`:has()` edge is reproduced recursively, but equal lists now fall through to ordinary cross-member coverage so `*, :has(> .a)` and `.a, .a:has(> .b)` remain true. A shared normalization context caps recursive selector members, 100,000,000 normalization operations, active nested argument bytes, final bytes, and 64 selector-list-function levels; exact one-below boundaries and exhaustive allocation failures are executable. Dart Sass agrees byte-for-byte with SCSS `.values{not::not(.a, #b);is::is(.a, .b);where::where(.a, :not(.b, .c));has::has(> .a, + #b);matches::matches(.a, .b);any::any(.a, .b);webkit::-webkit-any(.a, .b);nested::not(:is(.a, :where(.b, .c)));relation-not:true;relation-is:true;relation-where:true;relation-has:true;relation-relative:false;relation-covered:true;unify-not::not(.a, .b);unify-is::is(.a, .b);unify-has::has(> .a, + .b);type:list}` and indented Sass `.sass{parsed::is(.a, .b);nested::not(:where(.a, .b));relation:true;relative:false;unified::not(.a, .b)}`. Direct selector/evaluator suites pass 26/26 and 127/127; focused exact Zig 0.15.2 Debug/ReleaseSafe each pass 23/23 steps with 255/255 tests, and full Debug/ReleaseSafe each pass 60/60 with 750/750. Selector/evaluator roots cross-compile for Windows x64 and Linux ARM64 in both modes. Exact final commit `e9264b6417867af31902b2f05a862e76f952a01b` passes focused and full Debug/ReleaseSafe under checksum-verified Zig 0.15.2 Linux ARM64 confinement as uid/gid 65534 with no network or capabilities, `no-new-privileges`, read-only source/toolchain/container, 512 pids, 4-GiB memory/swap, `-j1`, and uid/gid-owned executable tmpfs caches/output/temp on `alpine@sha256:28bd5fe8b56d1bd048e5babf5b10710ebe0bae67db86916198a6eec434943f8b`; the immutable toolchain archive SHA-256 is `958ed7d1e00d0ea76590d27666efbf7a932281b3d7ba0c6b01b0ff26498f667f`. The first confinement attempt proved the read-only source boundary when `std.testing.tmpDir()` required the `.zig-cache` overlay, and the second stopped before compilation because the new tmpfs was root-owned; the corrected non-root overlay passed unchanged source. In the same confinement, package-consumer Debug/ReleaseSafe passes 6/6 steps and 2/2 tests, build integration passes 7/7 and 1/1, and benchmark smoke passes 3/3 in both modes without an archive. Native contract schema v3 adds only `built-in-selector-list-functional-pseudo-normalization`; 10/10 plus check retains the recursive Zig/`std`/`builtin` closure with zero package or optional dependencies, provider engine, compile child process, network, download, or non-system runtime in the native target. Product/reference matrices pass 151/151, repository policy 141/141, and autodevelop 47/47. Workflow/version/release/native-contract/package/dependency/prefix/capability/documentation/format/compatibility/transform/benchmark/actionlint/shell/JavaScript/JSON/Ruby/formatting/diff gates pass; the isolated site passes 139/139 plus production build, VS Code passes 13/13 plus the exact five-file 120,473-byte VSIX, and real Neovim 0.11.7 and 0.12.4 integrations pass. The first site run encountered only concurrent VS Code output cleanup and passed in the required isolated lane. Complete root/docs/VS Code and production audits report zero vulnerabilities across 136/385/331 dependencies. The npm dry pack remains exactly 28 files, 92,750 packed bytes, and 406,429 unpacked bytes. Local canonical Linux ARM64 release-container and immutable-source Homebrew formula smokes pass. Exact-main Build `29789292920` passed Test Suite `88507387611` and all five native archive/offline-install jobs: Linux x64 `88507387622`, macOS x64 `88507387629`, Windows x64 `88507387632`, Linux ARM64 `88507387639`, and macOS ARM64 `88507387693`; artifacts are respectively `8479712051`, `8479727020`, `8479672913`, `8479707862`, and `8479690864`. GitHub's only annotations are the known non-failing immutable-action Node 20-to-24 warnings. The four direct provider-era dependencies and 86 locked packages remain quarantined development oracles, so the unpublished transitional package is not claimed as dependency-free; production routing, public capability claims, version, tag, release/artifact/package publication, deployment, native graduation, and the release interlock did not change. | `e9264b6` |
| `NSASS-011-SELECTOR-NORMALIZE-SIMPLE-PSEUDOS` | `VERIFIED` | A test-first red proved native `selector.parse(":\\68 over")` still serialized the authored escape and evaluator relation calls failed with `UnsupportedFeature`. Added provider-free bounded canonicalization for escaped simple pseudo-class and pseudo-element identifiers across parsing, `simple-selectors()`, superselector relations, equality, and unification; ordinary simple pseudo-classes now also participate in `extend()` and `replace()`. Leading digits, Unicode scalars, emoji, punctuation, and escaped leading hyphens use the established deterministic identifier serializer. Legacy `:before` and `::before` are relation/unification-equivalent with the left spelling retained, while pseudo spelling remains case-distinct. Dart Sass 1.101.0 agrees byte-for-byte with SCSS `.values{simple::hover;digit::\31 23;unicode::café;emoji::💚;punctuation::foo\+bar;escaped-hyphen::\-foo;element:::before;element-emoji:::💚;relation:true;relation-element:true;case-distinct:false;unify::hover;unify-element::before;extend:.x:hover,.x.y;replace:.x.y;extend-context:.x:hover,:hover:focus;replace-context::hover:focus;type:list}` and indented Sass `.sass{parsed::hover;element:::before;relation:true;unified::before;extended:.x:hover,.x.y}`. Invalid scalar, newline, NUL, and output-ceiling cases fail without partial results; exhaustive allocation-failure traversal reaches pseudo parse/relation/unify/extend paths. Escaped functional names, all functional extension, `:host`/`:host-context`, functional pseudo-elements, and pseudo-element extension remain explicitly unavailable rather than inferred. Direct selector/evaluator suites pass 25/25 and 126/126. Focused exact Zig 0.15.2 Debug and ReleaseSafe each pass 23/23 steps with 253/253 tests, and full suites each pass 60/60 with 748/748. Selector/evaluator roots cross-compile for Windows x64 and Linux ARM64 in both modes. Exact final bytes pass focused and full Debug/ReleaseSafe under checksum-verified Zig 0.15.2 Linux ARM64 confinement as uid/gid 65534 with no network or capabilities, `no-new-privileges`, read-only source/toolchain/container, 512 pids, 4-GiB memory/swap, `-j1`, and executable tmpfs caches/output/temp on `alpine@sha256:28bd5fe8b56d1bd048e5babf5b10710ebe0bae67db86916198a6eec434943f8b`; the immutable toolchain archive SHA-256 is `958ed7d1e00d0ea76590d27666efbf7a932281b3d7ba0c6b01b0ff26498f667f`. Native contract schema v3 adds only `built-in-selector-simple-pseudo-normalization`; 10/10 plus check retains the recursive Zig/`std`/`builtin` closure with zero package or optional dependencies, provider engine, compile child process, network, download, or non-system runtime in the native target. Product/reference matrices pass 151/151, repository policy 141/141, and autodevelop 47/47. Workflow/version/release/native-contract/package/dependency/prefix/capability/documentation/format/compatibility/transform/benchmark/actionlint/shell/JavaScript/JSON/formatting/diff gates pass; the isolated site passes 139/139 plus production build, VS Code passes 13/13 plus the exact five-file 120,473-byte VSIX, and real Neovim 0.11.7 and 0.12.4 integrations pass. Complete root/docs/VS Code and production audits report zero vulnerabilities. The npm dry pack remains exactly 28 files, 92,750 packed bytes, and 406,429 unpacked bytes. Local canonical Linux ARM64 release-container and immutable-source Homebrew formula smokes pass. Sealed package-consumer Debug/ReleaseSafe passes 6/6 steps and 2/2 tests, build integration passes 7/7 and 1/1, and benchmark smoke passes 3/3 in both modes without an archive. Exact-main Build `29782996142` passed Test Suite `88488264507` and all five native archive/offline-install jobs on `2e2045785462364afd1c8de8f34e0205140afe88`: Linux x64 `88488264602`, macOS x64 `88488264588`, Windows x64 `88488264628`, Linux ARM64 `88488264622`, and macOS ARM64 `88488264619`; artifacts are respectively `8477558070`, `8477480755`, `8477437851`, `8477437989`, and `8477415607`. GitHub's only annotation is the known non-failing immutable-action Node 20-to-24 warning. The four direct provider-era dependencies and 86 locked packages remain quarantined development oracles, so the unpublished transitional package is not claimed as dependency-free; production routing, public capability claims, version, tag, release/artifact/package publication, deployment, native graduation, and the release interlock did not change. | `2e20457` |
| `NSASS-011-SELECTOR-NORMALIZE-ESCAPES` | `VERIFIED` | Added provider-free bounded CSS identifier escape decoding and canonical serialization for native Sass selector classes, IDs, placeholders, type names, qualified namespaces, attribute names, attribute namespaces, and quoted and unquoted attribute values. Equivalent hex/simple spellings now converge deterministically through selector parsing, relation matching, equality, unification, extension, and replacement; leading digits use lowercase terminating hex escapes, Unicode scalars serialize directly, punctuation remains canonically escaped, and escaped leading hyphens remain observably distinct from literal hyphens. Quoted invalid scalar escapes follow Dart Sass by becoming U+FFFD, while surrogate/out-of-range identifier escapes, escaped modifiers, escaped pseudo names requiring inference, simple string line continuations, and malformed escapes fail closed rather than inventing equivalence. Every new append is checked against the final byte ceiling before allocation, normalization expansion is bounded, post-normalization validation owns its buffers on every error, and exhaustive allocation-failure coverage includes escaped parse/relation/unify/extend paths. Pinned Dart Sass 1.101.0 agrees byte-for-byte with the canonical SCSS output `.values{class:.foo;digit:.\31 23;unicode:.café;emoji:.💚;punctuation:.foo\+bar;escaped-hyphen:.\-foo;id:#foo;placeholder:%\31 23;type:button;qualified:svg|a;escaped-star:\*;attribute-name:[x=y];attribute-namespace:[ns|x=y];attribute-value:[x=yes];attribute-digit:[x=\31 23];attribute-punctuation:[x=foo\+bar];attribute-quoted:[x=y];attribute-space:[x="a b"];relation-class:true;relation-attribute:true;escaped-distinct:false;unify-class:.foo;unify-attribute:[x=yes];extend-class:.foo.c,.c.bar;replace-attribute:.c.bar;type-of:list}` and indented Sass `.sass{parsed:.foo;attribute:[x=yes];relation:true;extended:[x=y].x,.x.z}`; direct edge guards cover NUL identity, U+FFFD distinction, invalid scalar replacement in strings, deterministic quote selection, malformed newline/NUL escapes, output expansion ceilings, and escaped modifier rejection. Direct selector/evaluator suites pass 24/24 and 125/125; focused exact Zig 0.15.2 Debug and ReleaseSafe each pass 23/23 steps with 251/251 tests, and full suites each pass 60/60 steps with 746/746 tests. Selector/evaluator roots cross-compile for Windows x64 and Linux ARM64 in both modes. Exact final bytes pass under checksum-verified Zig 0.15.2 Linux ARM64 confinement as uid/gid 65534 with no network or capabilities, `no-new-privileges`, read-only source/toolchain/container, 512 pids, 4-GiB memory/swap, `-j1`, and executable tmpfs caches/output/temp on `alpine@sha256:28bd5fe8b56d1bd048e5babf5b10710ebe0bae67db86916198a6eec434943f8b`; the immutable toolchain archive SHA-256 is `958ed7d1e00d0ea76590d27666efbf7a932281b3d7ba0c6b01b0ff26498f667f`. Native contract schema v3 adds only `built-in-selector-escape-normalization`; 10/10 plus check retains the recursive Zig/`std`/`builtin` closure with zero package or optional dependencies, provider engine, compile child process, network, download, or non-system runtime in the native target. Product/reference matrices pass 151/151, repository policy 141/141, and autodevelop 47/47. Workflow/version/release/native-contract/package/dependency/prefix/capability/documentation/format/compatibility/transform/benchmark/actionlint/shell/JavaScript/JSON/formatting/diff gates pass; the isolated site passes 139/139 plus production build, VS Code passes 13/13 plus the exact five-file 120,473-byte VSIX, and real Neovim 0.11.7 and 0.12.4 integrations pass. Complete root/docs/VS Code and production audits report zero vulnerabilities. The npm dry pack remains exactly 28 files, 92,750 packed bytes, and 406,429 unpacked bytes. Local canonical Linux ARM64 release-container and immutable-source Homebrew formula smokes pass. Sealed package-consumer Debug/ReleaseSafe passes 6/6 steps and 2/2 tests, build integration passes 7/7 and 1/1, and benchmark smoke passes 3/3 in both modes without an archive. Exact-main Build `29776646858` passed Test Suite `88467517239` and all five native archive/offline-install jobs on `878753589268f138574874f7b0181e6cb65fd2e8`: Linux x64 `88467517098`, macOS x64 `88467517178`, Windows x64 `88467517300`, Linux ARM64 `88467517235`, and macOS ARM64 `88467517075`; artifacts are respectively `8475157867`, `8475042917`, `8474965352`, `8475014213`, and `8474979578`. GitHub's only annotation is the known non-failing immutable-action Node 20-to-24 warning. The four direct provider-era dependencies and 86 locked packages remain quarantined development oracles, so the unpublished transitional package is not claimed as dependency-free; production routing, public capability claims, version, tag, release/artifact/package publication, deployment, native graduation, and the release interlock did not change. | `8787535` |
| `NSASS-011-SELECTOR-NORMALIZE-ATTRIBUTES` | `VERIFIED` | Added a provider-free bounded native Zig parser and serializer for unescaped CSS/Sass attribute selectors. Canonicalization now owns comments and whitespace; all six operators; plain, qualified, and namespaced attribute names; quoted and unquoted values; deterministic quote selection; identifier and `--` value rules; and one exact case-sensitive alphabetic modifier. The representation flows through selector parsing, simple-selector decomposition, relation matching, equality, unification, extension, and replacement. Name, namespace, modifier, operator, and value comparisons remain exact: wildcard/existence inference and other unproven semantic equivalence are not inferred. Escaped attributes preserve their exact raw bytes during ordinary parsing and fail closed for semantic relation operations; decoding them is the next independently gated package. Every input is size-checked before allocation. Pinned Dart Sass 1.101.0 oracle cases cover comment removal, whitespace normalization, every operator, deterministic quote normalization, qualified names, namespaces, modifiers, extension/replacement duplicates, and the closed escape boundary. The exact extension boundary passes with seven selectors, 50 bytes, 34 components, 862 temporary bytes, and 103 relation operations; one below each ceiling fails without a partial result. Direct selector tests pass 23/23 and evaluator tests pass 124/124 in Debug and ReleaseSafe. Focused exact Zig 0.15.2 Debug and ReleaseSafe each pass 23/23 steps with 249/249 tests; full suites each pass 60/60 steps with 744/744 tests. Selector/evaluator roots cross-compile for Windows x64 and Linux ARM64 in both modes. Exact final bytes pass focused native Debug/ReleaseSafe under checksum-verified Zig 0.15.2 Linux ARM64 confinement as uid/gid 65534 with no network or capabilities, `no-new-privileges`, read-only source/toolchain/container, 512 pids, 4-GiB memory/swap, `-j1`, and executable tmpfs caches/output/temp on `alpine@sha256:28bd5fe8b56d1bd048e5babf5b10710ebe0bae67db86916198a6eec434943f8b`; the immutable toolchain archive SHA-256 is `958ed7d1e00d0ea76590d27666efbf7a932281b3d7ba0c6b01b0ff26498f667f`. Native contract schema v3 adds only `built-in-selector-attribute-normalization`; 10/10 plus check retains the recursive Zig/`std`/`builtin` closure with zero package or optional dependencies, provider engine, compile child process, network, download, or non-system runtime in the native target. Product/reference matrices pass 151/151, repository policy 141/141, and autodevelop 47/47. Workflow/version/release/native-contract/package/dependency/prefix/capability/documentation/format/compatibility/transform/benchmark/actionlint/shell/JavaScript/JSON/formatting/diff gates pass; the isolated site passes 139/139 plus production build, VS Code passes 13/13 plus the exact five-file 120,473-byte VSIX, and real Neovim 0.11.7 and 0.12.4 integrations pass. Complete root/docs/VS Code and production audits report zero vulnerabilities and zero production dependencies. The npm dry pack remains exactly 28 files, 92,750 packed bytes, and 406,429 unpacked bytes. Local canonical Linux ARM64 release-container and immutable-source Homebrew formula smokes pass. Sealed package-consumer Debug/ReleaseSafe passes 6/6 steps and 2/2 tests, build integration passes 7/7 and 1/1, and benchmark smoke passes 3/3 in both modes without an archive. Exact-main Build `29767914070` passed Test Suite `88438522277` and all five native archive/offline-install jobs on `a6d5478f3026bf805c59caa601dec7eaac66a611`: Linux x64 `88438522262`, macOS x64 `88438522327`, Windows x64 `88438522335`, Linux ARM64 `88438522336`, and macOS ARM64 `88438522380`; artifacts are respectively `8471811308`, `8471713337`, `8471597979`, `8471651888`, and `8471605947`. GitHub's only annotations are the known non-failing immutable-action Node 20-to-24 warnings. The four direct provider-era dependencies and 86 locked packages remain quarantined development oracles, so the unpublished transitional package is not claimed as fully dependency-free; production routing, public capability claims, version, tag, release/artifact/package publication, deployment, native graduation, and the release interlock did not change. | `a6d5478` |
| `NSASS-011-SELECTOR-NORMALIZE-DUPLICATE-SIMPLES` | `VERIFIED` | Extended the provider-free native Zig `sass:selector.extend()` and `sass:selector.replace()` implementation through default, exact custom, and unprefixed module namespaces in SCSS and indented Sass so duplicate simple selectors and reordered equivalent target, extendee-list, and extender-list members participate in matching and trimming. An extendee compound is normalized to one occurrence of each exact simple token for matching; every matching occurrence is removed from the subject before its remainder is unified with the extender. Original selector bytes are never normalized in place: a true no-match preserves exact order and duplicate list entries, whole duplicate extenders preserve authored spelling, and reordered compounds remain observable where Dart Sass preserves them. Matched stages reproduce Dart Sass 1.101.0 trimming: generated semantic duplicates and covered selectors are removed, duplicate/equivalent extendee and extender members keep the first observable spelling, identity extension collapses only after a real relation applies, `replace()` preserves its exact-byte/provenance behavior, and stages above 100 candidates remain untrimmed. Repeated tokens no longer inflate the compound-product safety bound. Pinned oracle cases include `.a.a.c` extended from `.a` or `.a.a` by `.b` as `.a.a.c,.c.b`, replacement as `.c.b`; `.a.b,.b.a` extended from `.a` by `.c` as `.a.b,.b.c,.b.a`; `.a,.a` extended from `.a` by `.b` as `.a,.b,.a`; matched duplicate trimming `.a,.a,.c` as `.a,.b,.c`; repeated pattern `.a.a.a.a.a` against `.a` with `.b,.c` as `.a,.b,.c`; and equivalent extender list `.b.c,.c.b` retaining `.b.c`. The canonical four-result boundary passes at seven total selectors, 29 bytes, 34 complex/simple components, 850 temporary bytes, and 109 relation operations, while one below every ceiling fails without a prefix. Complex extendees/extenders, attributes, escapes, pseudos, namespaces/universals, and conflicting unification remain fail-closed boundaries. Direct selector/evaluator suites pass 21/21 and 123/123 in Debug, including exhaustive allocation failures; focused native Debug and ReleaseSafe each pass 23/23 steps with 245/246 tests and the one existing platform skip, and full Debug and ReleaseSafe each pass 60/60 steps with 740/741 tests and the same skip. Selector/evaluator roots compile for Windows x64 and Linux ARM64 in both modes. Exact final bytes pass under checksum-verified Zig 0.15.2 Linux ARM64 confinement as uid/gid 65534 with no network or capabilities, `no-new-privileges`, read-only source/toolchain/container, 512 pids, 4-GiB memory/swap, `-j1`, and tmpfs caches/output/temp on `alpine@sha256:28bd5fe8b56d1bd048e5babf5b10710ebe0bae67db86916198a6eec434943f8b`; the immutable toolchain archive SHA-256 is `958ed7d1e00d0ea76590d27666efbf7a932281b3d7ba0c6b01b0ff26498f667f`. Native contract schema v3 adds only `built-in-selector-extend-replace-normalized-compounds`; 10/10 plus check retains the recursive Zig/`std`/`builtin` closure with zero package or optional dependencies, provider engine, compile child process, network, download, or non-system runtime in the native target. Product/reference matrices pass 151/151, repository policy 141/141, and autodevelop 47/47. Workflow/version/release/native-contract/package/dependency/prefix/capability/documentation/format/compatibility/transform/benchmark/actionlint/shell/JavaScript/JSON/formatting/diff gates pass; the isolated site passes 139/139 plus production build, VS Code passes 13/13 plus the exact five-file 120,473-byte VSIX, and real Neovim 0.11.7 and 0.12.4 pass. Complete root/docs/VS Code and production audits report zero vulnerabilities and zero production dependencies. The npm dry pack remains exactly 28 files, 92,750 packed bytes, and 406,429 unpacked bytes; local canonical Linux ARM64 release-container and immutable-source Homebrew smokes pass. Exact-main Build `29718031524` passed Test Suite `88275006633` and all five native archive/offline-install jobs on `05ac96da92648818aa917b941ed9de4c6e772363`: Linux x64 `88275006614`, macOS x64 `88275006632`, Linux ARM64 `88275006635`, Windows x64 `88275006638`, and macOS ARM64 `88275006646`; artifacts are respectively `8451352900`, `8451353366`, `8451335482`, `8451319954`, and `8451314461`. The four direct provider-era dependencies and 86 locked packages remain quarantined development oracles, so the unpublished transitional package is not claimed as fully dependency-free; production routing, public capability claims, version, tag, release/artifact/package publication, deployment, native graduation, and the release interlock did not change. | `05ac96d` |
| `NSASS-011-SELECTOR-EXTEND-REPLACE-COMPOUND-LISTS` | `VERIFIED` | Extended the provider-free native Zig `sass:selector.extend()` and `sass:selector.replace()` implementation through default, exact custom, and unprefixed module namespaces in SCSS and indented Sass so both extendee and extender may be bounded selector lists whose members are single safe compounds. Extendee members are applied sequentially in source order, each extender list expands by exact mixed-radix product order, and original identity/order propagates exactly, including `replace()`'s provider-observable duplicate-original behavior. The implementation reproduces Dart Sass 1.101.0 `_trim()` behavior for candidate stages through 100 entries and its untrimmed behavior above 100 across repeated target components. A matched individual multi-simple extendee whose extender product would make Dart Sass's inner compound power set exceed 100 now fails closed with `SelectorLimitExceeded` rather than returning a silently incomplete trimmed result: `(extenders + 1)^tokens` for `extend()` and `extenders^tokens` for `replace()`, with checked arithmetic charged to the shared relation-operation budget; the same oversized pattern remains an exact no-op when it does not match. Duplicate or semantically reordered list members, complex extendees/extenders, duplicate simples, attributes, escapes, pseudos, namespaces/universals, and conflicting unification remain unavailable all-or-nothing boundaries. Pinned oracle bytes agree exactly with `.values{extendee-list:.a.c .a,.x .a,.a.c .x,.x .x;replace-extendee-list:.x .x;extender-list:.a .a,.d .a,.b .a,.a .d,.d .d,.b .d,.a .b,.d .b,.b .b;replace-extender-list:.d .d,.b .d,.d .b,.b .b;both-lists:.a.c,.b,.d;replace-both-lists:.b,.d,.b;ordered:.b.x;reversed:.x;typed:button.b,button.c,#id.b,#id.c;no-match:.none;type:list;separator:comma}` and indented Sass `.sass{extendee:.a.c,.x;extender:.d .d,.b .d,.d .b,.b .b;ordered:.b.x}`. Additional pinned guards prove `.a.b.c.d` with `.x,.y` trims to three extend results and two replace results, while five repeated target components produce the exact 243-entry untrimmed order and seven replace components produce 128; the exact list boundary passes at 13 selectors and 56 bytes and one below either limit fails without a prefix. Direct selector suites pass 20/20 in Debug and ReleaseSafe, including exhaustive allocation failures; full exact Zig 0.15.2 suites pass 60/60 steps and 739/739 tests in both modes, and selector/evaluator roots compile for Windows x64 and Linux ARM64 in both modes. The final bytes pass under checksum-verified Zig 0.15.2 Linux ARM64 archive SHA-256 `958ed7d1e00d0ea76590d27666efbf7a932281b3d7ba0c6b01b0ff26498f667f` in uid/gid 65534 confinement with no network or capabilities, `no-new-privileges`, read-only source/toolchain/container, 512 pids, 4-GiB memory/swap, `-j1`, and tmpfs caches/output/temp on `alpine@sha256:28bd5fe8b56d1bd048e5babf5b10710ebe0bae67db86916198a6eec434943f8b`. The native contract adds only `built-in-selector-extend-replace-compound-lists`; 10/10 plus check retains the recursive Zig/`std`/`builtin` closure with zero package or optional dependencies, provider engine, compile child process, network, download, or non-system runtime in the native target. Product/reference matrices pass 151/151, repository policy 141/141, and autodevelop 47/47. The isolated site passes 139/139 plus build; documentation passes 7/7; VS Code passes 13/13 plus the exact five-file 120,473-byte VSIX; Neovim 0.11.7/0.12.4 integrations, actionlint, workflow/release/native-contract/package/dependency/live-audit/prefix/capability/format/compatibility/transform/benchmark/syntax/JSON/formatting/diff gates, local canonical release-container, immutable-source Homebrew, package consumer, build integration, benchmark smoke, and zero-vulnerability complete and production audits all pass. The npm dry pack remains exactly 28 files, 92,750 packed bytes, and 406,429 unpacked bytes. Exact-main Build `29704100277` passed Test Suite `88238038806` and all five archive/offline-install jobs on `afbd6bbf3fa8b2cd747f8c4287ef13172466c856`: macOS x64 `88238038817`, Linux ARM64 `88238038820`, macOS ARM64 `88238038826`, Linux x64 `88238038831`, and Windows x64 `88238038862`; artifacts are respectively `8447393998`, `8447388491`, `8447365619`, `8447398875`, and `8447368450`. GitHub's only annotations are the known non-failing immutable-action Node 20-to-24 warnings. The four provider-era direct dependencies and 86 locked packages remain quarantined development oracles, so the unpublished transitional package is not claimed as zero dependency; production routing, public capability claims, version, tag, release/artifact/package publication, deployment, native graduation, and release interlock did not change. | `afbd6bb` |
| `NSASS-011-SELECTOR-EXTEND-REPLACE-COMPOUND` | `VERIFIED` | Added provider-free native Zig `sass:selector.extend()` and `sass:selector.replace()` through default, exact custom, and unprefixed module namespaces in SCSS and indented Sass. One safe compound extendee and one safe compound extender now operate across arbitrary target selector lists with descendant, child, adjacent-sibling, and following-sibling combinators byte-preserved. Exact compound-subset matching removes the extendee and unifies the remainder with the extender through the already-admitted bounded compound core. `extend()` retains every original path and emits the checked `2^matching_components` product in earliest-component-fastest order; `replace()` substitutes every match once. Global trimming preserves original selectors, prefers an original over a semantically equivalent generated selector, removes generated paths already covered by an original or another generated superselector, and deduplicates `replace()` only when rendered bytes are identical so reordered compounds remain observable exactly like Dart Sass. Typed selectors, IDs, placeholders, no-match paths, repeated target components, keyword reordering, exact aliases, unprefixed imports, selector-value inputs, evaluate-once arguments, and list/meta representation are covered. Extendee/extender lists, complex extendees/extenders, duplicate simples, attributes, escapes, pseudos, namespace/universal inference, semantically duplicate target entries, and conflicting replacement unification fail closed without a partial selector list; review specifically caught and closed quoted/unquoted attribute equivalence, duplicate-simple normalization, generated-vs-original superselector trimming, and reordered `replace()` dedup before commit. Inputs, candidate arrays, rendered bytes, complex/simple components, temporary allocations, relation comparisons, and power-set growth share cumulative checked ceilings. The canonical four-result boundary passes at seven total selectors, 29 bytes, 34 complex/simple components, 626 temporary bytes, and 147 relation operations, while one below every ceiling fails without a prefix. Pinned Dart Sass 1.101.0 agrees byte-for-byte with `.values{basic:.a,.b;replace:.b;compound:.a.c,.c.b.d;complex:.x > .a + .y,.x > .b + .y;repeated:.a.c .a.d,.c.b .a.d,.a.c .d.b,.c.b .d.b;list:.b,.x > .b,.none;no-match:.none;typed:button.b;keyword:.k1.k2,.k2.k3;alias:.m2.m3;star:.s1,.s2;marked:.q2;evaluations:3;type:list;separator:comma;inspect:.a, .b}` and `.sass{extend:.a.c,.c.b;replace:.x > .b;repeated:.a .a,.b .a,.a .b,.b .b}`; an additional exact oracle covers original/generated global pruning, reordered compounds, IDs, and placeholders. Direct selector/evaluator suites pass 19/19 and 121/121 in Debug and ReleaseSafe, including exhaustive allocation failures. Focused exact Zig 0.15.2 Debug/ReleaseSafe each pass 23/23 steps and 242/242 tests, and full Debug/ReleaseSafe each pass 60/60 and 737/737. Selector/evaluator roots cross-compile for Windows x64 and Linux ARM64 in both modes. The exact final source passes focused/full Debug/ReleaseSafe under checksum-verified Zig 0.15.2 Linux ARM64 confinement as uid/gid 65534, no network, no capabilities, `no-new-privileges`, read-only source/toolchain/container, 512 pids, 4-GiB memory/swap, `-j1`, and tmpfs-only `.zig-cache`, `zig-out`, and `/tmp` on `alpine@sha256:28bd5fe8b56d1bd048e5babf5b10710ebe0bae67db86916198a6eec434943f8b`; the immutable toolchain archive SHA-256 is `958ed7d1e00d0ea76590d27666efbf7a932281b3d7ba0c6b01b0ff26498f667f`. In the same confinement, package consumer passes 6/6 steps and 2/2 tests, build integration passes 7/7 and 1/1, and benchmark smoke passes 3/3 in both modes without archiving a result. Native contract schema v3 adds only `built-in-selector-extend-replace-compound`; 10/10 plus check retains the recursive repository Zig/`std`/`builtin` closure with zero package/optional dependencies, provider engine, compile child process, network, download, or non-system runtime in the native target. Repository policy passes 141/141, product/reference preprocessors pass 151/151, and autodevelop passes 47/47. Workflow/version/release/native-contract/package/dependency/live full-and-production-audit/prefix/capability/documentation/format/compatibility/transform/benchmark/actionlint/syntax/JSON/formatting/diff gates pass; the isolated website passes 139/139 plus production build, documentation integrity passes 7/7, VS Code passes 13/13 plus the exact five-file 120,473-byte VSIX, and real Neovim 0.11.7 and 0.12.4 pass. Complete audits report zero vulnerabilities across the 136-dependency root graph, 385-dependency docs graph, and 331-dependency VS Code graph. The npm dry pack remains exactly 28 files, 92,750 packed bytes, and 406,429 unpacked bytes; local canonical Linux ARM64 release-container and immutable-source Homebrew smokes pass. Exact-main Build `29700518574` passed Test Suite `88228697581` and all five native archive/offline-install jobs on `c8782d00417310488f56665109d39984e04c3698`: macOS ARM64 `88228697586`, Linux x64 `88228697590`, Linux ARM64 `88228697599`, Windows x64 `88228697617`, and macOS x64 `88228697752`; artifacts are respectively `8446325896`, `8446391145`, `8446343009`, `8446340127`, and `8446347674`. GitHub's only annotations are the known non-failing platform warnings that immutable Node-20-targeted action releases are forced onto Node 24; workflow security and actionlint remain green, and no ZigCSS runtime or package dependency changed. The unpublished transition still contains four direct provider-era manifest dependencies and 86 locked packages and is not claimed as zero dependency; production routing, public capability claims, version, tag, release/artifact/package publication, deployment, native graduation, and the release interlock did not change. | `c8782d0` |
| `NSASS-011-SELECTOR-UNIFY-WEAVE-SHARED-RIGID` | `VERIFIED` | Extended the provider-free native Zig `sass:selector.unify()` path so exact shared descendant anchors can coexist with one-sided rigid ancestry suffixes. LCS anchors now require both exact compounds and descendant combinators, preventing unusable rigid matches from hiding later valid anchors. At most one operand may globally own rigid child, adjacent-sibling, or following-sibling components; within each anchor-delimited segment its descendant prefix weaves as one whole chunk with the other operand's descendant chunk before a contiguous rigid suffix is appended nearest the anchor. Mixed rigid chains and multiple rigid segments are byte-preserved, the earliest varying segment still changes fastest, the subject unifies once, and left-outer/right-inner list order remains exact. Rendering now length-checks and writes each actual combinator rather than assuming a one-byte descendant separator. Dual-rigid ownership, any descendant after a rigid component inside one segment, mismatched-combinator anchors, cross-segment simple overlap, partial compounds, escapes, host semantics, and functional pseudo-elements remain fail-closed. Every result is independently revalidated and cumulatively charged against selectors, bytes, complex/simple components, temporary allocations, and relation operations; the canonical two-result boundary passes at four total selectors, 66 bytes, 31 complex components, 484 temporary bytes, and 91 operations, while one below every ceiling fails without a prefix. Test-first evidence captured exactly 234/239 focused tests with the intended five direct/evaluator/allocation/limit failures at the former unsupported boundary. Pinned Dart Sass 1.101.0 agrees byte-for-byte with `.rigid{child:.d .a > .b .c.e;adjacent:.d .a + .b .c.e;following:.d .a ~ .b .c.e;right:.a .d > .b .c.e;prefix:.p .q .d .a > .b .c.e,.q .d .p .a > .b .c.e;tail:.d .a > .b .x .y .c.e,.d .a > .b .y .x .c.e;chain:.d .a > .m + .b .c.e;multiple:.d .a > .x .e .b + .y .c.f;list:.d .a > .b .c.e,.u + .v .d .b .w.e,.d .b .u + .v .w.e}` and the exact indented-Sass child/prefix/chain subset; direct review also covers reversed ownership, right-side prefixes/chains, repeated child chains, two anchors, every rigid combinator, list products, and the explicit unavailable boundaries. Direct selector/evaluator suites pass 17/17 and 120/120, including exhaustive allocation failures; focused exact Zig 0.15.2 Debug/ReleaseSafe each pass 23/23 steps and 239/239 tests, and full Debug/ReleaseSafe each pass 60/60 and 734/734. Selector/evaluator roots cross-compile for Windows x64 and Linux ARM64 in both modes. The exact final source passes focused/full Debug/ReleaseSafe under checksum-verified Zig 0.15.2 Linux ARM64 confinement as uid/gid 65534, no network, no capabilities, `no-new-privileges`, read-only source/toolchain/container, 512 pids, 4-GiB memory/swap, `-j1`, and tmpfs-only `.zig-cache`, `zig-out`, and `/tmp` on `alpine@sha256:28bd5fe8b56d1bd048e5babf5b10710ebe0bae67db86916198a6eec434943f8b`; the immutable toolchain archive SHA-256 is `958ed7d1e00d0ea76590d27666efbf7a932281b3d7ba0c6b01b0ff26498f667f`. Two initial confinement attempts stopped before meaningful validation because `/tmp` lacked executable permission and redirected caches did not satisfy Zig 0.15.2 `std.testing.tmpDir()`; the established exact overlays corrected only the harness, after which unchanged source passed. In the same confinement, package consumer passes 6/6 steps and 2/2 tests, build integration passes 7/7 and 1/1, and benchmark smoke passes 3/3 in both modes without archiving a result. Native contract schema v3 adds only `built-in-selector-unify-complex-weave-shared-rigid`; 10/10 plus check retains the recursive repository Zig/`std`/`builtin` closure with zero package/optional dependencies, provider engine, compile child process, network, download, or non-system runtime in the native target. Repository policy passes 141/141, product/reference preprocessors pass 151/151, and autodevelop passes 47/47. Workflow/version/release/native-contract/package/dependency/live full-and-production-audit/prefix/capability/documentation/format/compatibility/transform/benchmark/actionlint/syntax/JSON/formatting/diff gates pass; the isolated website passes 139/139 plus production build, documentation integrity passes 7/7, VS Code passes 13/13 plus the exact five-file 120,473-byte VSIX, and real Neovim 0.11.7 and 0.12.4 pass. Complete audits report zero vulnerabilities across the 136-dependency root graph, 385-dependency docs graph, and 331-dependency VS Code graph. The npm dry pack remains exactly 28 files, 92,750 packed bytes, and 406,429 unpacked bytes; local canonical Linux ARM64 release-container and immutable-source Homebrew smokes pass. Exact-main Build `29696187877` passed Test Suite `88217258649` and all five native archive/offline-install jobs on `38e02533980a72f7517077945817f490fb8f8877`: Windows x64 `88217258642`, Linux x64 `88217258650`, Linux ARM64 `88217258653`, macOS ARM64 `88217258675`, and macOS x64 `88217258680`. GitHub's non-failing platform annotation forced the immutable Node-20-targeted action releases onto Node 24; workflow security and actionlint remain green, and no ZigCSS runtime or package dependency changed. The unpublished transition still contains four direct provider-era manifest dependencies and 86 locked packages and is not claimed as zero dependency; production routing, public capability claims, version, tag, release/artifact/package publication, deployment, native graduation, and the release interlock did not change. | `38e0253` |
| `DEP-001-DOCS-DEV-REFRESH` | `VERIFIED` | A live complete `npm audit` reproduced nine newly published documentation-tool findings—one low, two moderate, five high, and one critical—while the plan-defined production audit remained zero across root/docs/VS Code. The affected graph is development-only and never enters ZigCSS compiler, npm package, native archive, or runtime bytes, but it includes the local site/test servers and therefore received an immediate isolated refresh. Exact direct pins advance Vite 6.3.5 to 6.4.3 and Vitest 3.2.4 to 3.2.7; lifecycle-disabled lock regeneration advances 38 existing compatible package nodes, including the coherent Vitest/Babel families and fixed `form-data`, `lodash`, `picomatch`, `postcss`, `tar`, and `ws`, with no package additions or removals. A clean `npm ci --ignore-scripts` installs 314 packages and audits 316 with zero findings; the complete resolved graph reports 385 dependencies and zero info/low/moderate/high/critical findings. The site passes 23/23 files and 139/139 tests on Vitest 3.2.7, and Vite 6.4.3 produces the same five asset filenames and byte/gzip sizes as before. Dependency-policy tests pass 4/4; exact manifest/lock, bounded Dependabot, workflow, documentation, JSON, and diff checks pass; root and VS Code complete audits and all three production audits are zero. The root npm dry pack remains exactly 28 files, 92,750 packed bytes, and 406,429 unpacked bytes, proving no shipped inventory change. Exact-main Build `29680188010` passed Test Suite `88174817637` and all five native archive/offline-install jobs on `0daf1b559c5ba4951cf0d457ec255c8f6d5b2b4e`: macOS x64 `88174817639`, Windows x64 `88174817648`, macOS ARM64 `88174817664`, Linux ARM64 `88174817671`, and Linux x64 `88174817679`. Documentation run `29680187982` passed build `88174817559` and the already-authorized Pages deploy `88174858618`. No Zig source, production dependency, provider reference, package/archive byte, capability claim, version, tag, release, artifact publication, or native graduation state changed. | `0daf1b5` |
| `NSASS-011-SELECTOR-UNIFY-WEAVE-TERMINAL-SIBLINGS` | `VERIFIED` | Extended the provider-free native Zig `sass:selector.unify()` path with bounded two-component terminal sibling-combinator weaving through SCSS and indented Sass. The planner admits safe ancestry compounds only, unifies the subject once, and preserves exact left-outer/right-inner list order. Two following-sibling constraints emit both valid predecessor orders plus their merged predecessor; an incompatible predecessor merge omits only the merged form, while a merge equal to either input prunes both redundant ordered forms. Following plus adjacent emits the ordered sequence and compatible adjacent merge, emits only the merged selector when the adjacent predecessor already subsumes the following predecessor, and retains only the sequence on conflict. Remaining admitted mismatches use the provider-proven nearest-subject rank descendant, child, following sibling, adjacent sibling, including both operand directions; aligned adjacent constraints retain the strict path and incompatible aligned adjacent predecessors remain null. Longer sibling ancestries, partial-compound anchors, escapes, host semantics, functional pseudo-elements, and ambiguous multi-rigid/shared cases remain fail-closed. Every output is independently revalidated and cumulatively charged against selectors, bytes, complex/simple components, temporary allocations, and relation operations; the canonical three-result following/following boundary passes at five total selectors, 53 bytes, 22 components, 299 temporary bytes, and 34 operations, while one below every ceiling fails without a prefix. Test-first evidence captured 232/237 focused tests with exactly five intended direct/evaluator/allocation/limit failures at the former unsupported boundary. Final direct review strengthened every admitted rank pair and incompatible-predecessor path against pinned Dart Sass 1.101.0. Exact oracle bytes include `.siblings{following:.a ~ .c ~ .b.d,.c ~ .a ~ .b.d,.a.c ~ .b.d;following-adjacent:.a ~ .c + .b.d,.a.c + .b.d;adjacent-following:.c ~ .a + .b.d,.c.a + .b.d;child-adjacent:.a > .c + .b.d;adjacent-child:.c > .a + .b.d;child-following:.a > .c ~ .b.d;following-child:.c > .a ~ .b.d;descendant-following:.a .c ~ .b.d;following-descendant:.c .a ~ .b.d;conflict-following:a ~ c ~ .b.d,c ~ a ~ .b.d;conflict-following-adjacent:a ~ c + .b.d;conflict-adjacent:null;list:.a ~ .c ~ .b.d,.c ~ .a ~ .b.d,.a.c ~ .b.d,.c ~ .x + .y.d,.c.x + .y.d}` and the exact indented-Sass subset `.siblings{following:.a ~ .c ~ .b.d,.c ~ .a ~ .b.d,.a.c ~ .b.d;adjacent:.a ~ .c + .b.d,.a.c + .b.d;child:.a > .c + .b.d}`. Direct selector/evaluator suites pass 16/16 and 119/119; focused exact Zig 0.15.2 Debug/ReleaseSafe each pass 23/23 steps and 237/237 tests, and full Debug/ReleaseSafe each pass 60/60 and 732/732. Selector/evaluator roots cross-compile for Windows x64 and Linux ARM64 in both modes. The exact final source passes focused/full Debug/ReleaseSafe under checksum-verified Zig 0.15.2 Linux ARM64 confinement as uid/gid 65534, no network, no capabilities, `no-new-privileges`, read-only source/toolchain/container, 512 pids, 4-GiB memory/swap, `-j1`, and tmpfs-only caches/output/temp on `alpine@sha256:28bd5fe8b56d1bd048e5babf5b10710ebe0bae67db86916198a6eec434943f8b`; public API passes 8/8 steps and 4/4 tests, build integration passes 6/6 and 1/1, and benchmark smoke passes 3/3 in both modes without archiving a result. Native contract schema v3 adds only `built-in-selector-unify-complex-weave-terminal-siblings`; 10/10 plus check retains the recursive repository Zig/`std`/`builtin` closure with zero package/optional dependencies, provider engine, compile child process, network, download, or non-system runtime in the native target. Repository policy passes 141/141, product/reference preprocessors pass 151/151, and autodevelop passes 47/47. Workflow/version/release/native-contract/package/dependency/live full-and-production-audit/prefix/capability/documentation/format/compatibility/transform/benchmark/actionlint/syntax/JSON/formatting/diff gates pass; the isolated website passes 139/139 plus production build, documentation integrity passes 7/7, VS Code passes 13/13 plus the exact five-file 120,473-byte VSIX, and real Neovim 0.11.7 and 0.12.4 pass. Complete audits report zero vulnerabilities across the 136-dependency root graph, 385-dependency docs graph, and 331-dependency VS Code graph. The npm dry pack remains exactly 28 files, 92,750 packed bytes, and 406,429 unpacked bytes; local canonical Linux ARM64 release-container and immutable-source Homebrew smokes pass. Exact-main Build `29693598037` passed Test Suite `88210467696` and all five native archive/offline-install jobs on `b73d70a0439390e96362bc5678d320801f7890a4`: Linux ARM64 `88210467709`, macOS ARM64 `88210467720`, Windows x64 `88210467722`, Linux x64 `88210467726`, and macOS x64 `88210467729`. GitHub's non-failing platform annotation forced the immutable Node-20-targeted action releases onto Node 24; workflow security and actionlint remain green, and no ZigCSS runtime or package dependency changed. The unpublished transition still contains four direct provider-era manifest dependencies and 86 locked packages and is not claimed as zero dependency; production routing, public capability claims, version, tag, release/artifact/package publication, deployment, native graduation, and the release interlock did not change. | `b73d70a` |
| `NSASS-011-SELECTOR-UNIFY-WEAVE-SHARED-LCS` | `VERIFIED` | Extended the provider-free native Zig `sass:selector.unify()` path with exact shared descendant ancestry anchors through SCSS and indented Sass. The bounded solver admits only descendant ancestry whose simple tokens are already safe for native weaving, allocates a checked `(left + 1) * (right + 1)` `usize` LCS matrix plus an exact anchor array under the cumulative temporary-byte ceiling, and preserves Dart Sass's observable prefix-LCS tie break by moving left through the right ancestry when predecessor lengths are equal. Every region between anchors remains a whole ordered chunk; a checked `2^varying_segments` bound is proved before rendering, the earliest varying segment changes fastest, exact anchors emit once, tails participate in the same product, the compound subject remains unified once, and left-outer/right-inner list order is retained. Non-anchor cross-segment exact, qualified-name, or simple-superselector overlap fails closed, so partial-compound anchors, child/adjacent/following shared ancestry, escapes, IDs/placeholders/attributes/pseudos in ancestry, host semantics, and functional pseudo-elements remain explicit later slices. Every result is length-checked, bounds-checked during copy, revalidated, and cumulatively charged against selectors, bytes, complex/simple components, temporary allocations, and relation operations; the four-result two-anchor boundary passes at six total selectors, 136 bytes, 51 components, 682 temporary bytes, and 128 operations, while one below every ceiling fails without returning a prefix. Test-first evidence captured 230/235 focused tests with exactly five intended direct/evaluator/allocation/limit paths failing at the former `UnsupportedSelectorUnification` boundary. Pinned Dart Sass 1.101.0 oracle bytes include `.shared{one:.a .d .b .c.e,.d .a .b .c.e;prefix:.a .b .c.d;suffix:.d .a .b .c.e;multiple:.a .x .b .c .d.e,.x .a .b .c .d.e;segments:.a1 .b1 .x .a2 .b2 .y .s.t,.b1 .a1 .x .a2 .b2 .y .s.t,.a1 .b1 .x .b2 .a2 .y .s.t,.b1 .a1 .x .b2 .a2 .y .s.t;tail:.a .b .x .c .d .s.t,.b .a .x .c .d .s.t,.a .b .x .d .c .s.t,.b .a .x .d .c .s.t;tie:.a .b .a .c.d;list:.a .d .b .c.e,.d .a .b .c.e,.a .x .b .c.y,.x .a .b .c.y}` plus the exact indented-Sass one/multiple subset; reversed ambiguous ties and repeated-anchor LCS cases also match exact provider order. Direct selector/evaluator suites pass 15/15 and 118/118, focused exact Zig 0.15.2 Debug/ReleaseSafe each pass 23/23 steps and 235/235 tests, and full Debug/ReleaseSafe each pass 60/60 and 730/730. Selector/evaluator roots cross-compile for Windows x64 and Linux ARM64 in both modes. The unchanged source passes focused/full Debug/ReleaseSafe under checksum-verified Zig 0.15.2 Linux ARM64 confinement as uid/gid 65534, no network, no capabilities, `no-new-privileges`, read-only source/toolchain/container, 512 pids, 4-GiB memory/swap, `-j1`, and tmpfs-only caches/output/temp on `alpine@sha256:28bd5fe8b56d1bd048e5babf5b10710ebe0bae67db86916198a6eec434943f8b`; package consumer passes 6/6 and 2/2, build integration passes 7/7 and 1/1, and benchmark smoke passes 3/3 in both modes without archiving a result. Native contract schema v3 adds only `built-in-selector-unify-complex-weave-shared-lcs`; 10/10 plus check retains the recursive repository Zig/`std`/`builtin` closure with zero package/optional dependencies, provider engine, compile child process, network, download, or non-system runtime in the native target. Repository policy passes 141/141, product/reference preprocessors pass 151/151, and autodevelop passes 47/47. Workflow/version/release/native-contract/package/dependency/live full-and-production-audit/prefix/capability/documentation/format/compatibility/transform/benchmark/actionlint/syntax/JSON/formatting/diff gates pass; the isolated website passes 139/139 plus production build, documentation integrity passes 7/7, VS Code passes 13/13 plus the exact five-file 120,473-byte VSIX, and real Neovim 0.11.7 and 0.12.4 pass. Complete audits report zero vulnerabilities across the 136-dependency root graph, 385-dependency docs graph, and 331-dependency VS Code graph. The npm dry pack remains exactly 28 files, 92,750 packed bytes, and 406,429 unpacked bytes. Exact-main Build `29690321912` passed Test Suite `88201769927` and all five native archive/offline-install jobs on `44fbc8a786ce63908e6038f440959be4fb28dd04`: Windows x64 `88201769925`, macOS x64 `88201769926`, macOS ARM64 `88201769935`, Linux x64 `88201769936`, and Linux ARM64 `88201769969`. The unpublished transition still contains four direct provider-era manifest dependencies and 86 locked packages and is not claimed as zero dependency; production routing, public capability claims, version, tag, release/artifact/package publication, deployment, native graduation, and the release interlock did not change. | `44fbc8a` |
| `NSASS-011-SELECTOR-UNIFY-WEAVE-DISJOINT` | `VERIFIED` | Extended the provider-free native Zig `sass:selector.unify()` path with bounded multi-result disjoint ancestry weaving through SCSS and indented Sass. The pair evaluator now transfers zero, one, or multiple owned selectors directly into the cumulative result builder without partial-result leaks: two descendant ancestry chunks emit left/right then right/left order, one descendant plus child or adjacent-sibling chunk emits the sole provider-defined rigid-nearest-subject order, and internal child/adjacent links remain byte-preserved. Left-outer/right-inner list products retain exact provider order. Every output is length-checked, revalidated, and charged against shared selector, byte, complex/simple component, temporary-allocation, and relation-operation ceilings; the exact two-result boundary passes at four total selectors, 30 total bytes, 17 components, 222 temporary bytes, and 28 operations, while one below every ceiling fails without truncation. Ancestry compounds are admitted only when their simple tokens have no exact or qualified-name superselector anchor across operands; IDs, placeholders, attributes, pseudos, following-sibling chunks, dual-rigid chunks, partial/shared anchors, escapes, host selectors, and functional pseudo-elements remain explicit `UnsupportedFeature` boundaries for later packages. Test-first evidence captured 228/233 focused tests with all five intended direct/evaluator/allocation/limit paths failing at the former blanket `UnsupportedSelectorUnification` branch. Exact Dart Sass 1.101.0 development-oracle bytes with `charset: false` are `.weave{descendant:.a .c .b.d,.c .a .b.d;deep:.a > .b .d .c.e,.d .a > .b .c.e;left-rigid:.c .a > .b.d;right-rigid:.a .c > .b.d;list:.a .c .b.d,.c .a .b.d,.a .z .b.w,.z .a .b.w,.x .c .y.d,.c .x .y.d,.x .z .y.w,.z .x .y.w}` and `.weave{descendant:.a .c .b.d,.c .a .b.d;rigid:.c .a > .b.d}`. Direct selector/evaluator suites pass 14/14 and 117/117; focused exact Zig 0.15.2 Debug/ReleaseSafe each pass 23/23 steps and 233/233 tests, and full Debug/ReleaseSafe each pass 60/60 and 728/728. Selector/evaluator roots cross-compile for Windows x64 and Linux ARM64 in both modes. The same source passes focused/full Debug/ReleaseSafe with checksum-verified Zig 0.15.2 under Linux ARM64 confinement as uid/gid 65534, no network, no capabilities, `no-new-privileges`, read-only source/toolchain/container, 512 pids, 4-GiB memory/swap, `-j1`, and tmpfs-only cache/output/temp paths on `alpine@sha256:28bd5fe8b56d1bd048e5babf5b10710ebe0bae67db86916198a6eec434943f8b`; an initial mode-bit writability assertion stopped before compilation even though an actual write correctly failed with `Read-only file system`, and the next harness attempt proved Zig 0.15.2's `std.testing.tmpDir()` requires the established `.zig-cache` tmpfs overlay before the unchanged source passed. In the same confinement, package consumer Debug/ReleaseSafe pass 6/6 steps and 2/2 tests, build integration passes 7/7 and 1/1, and benchmark smoke passes 3/3 in both modes without archiving a result. Native contract schema v3 adds only `built-in-selector-unify-complex-weave-disjoint`; 10/10 plus check retains the recursive repository Zig/`std`/`builtin` closure with zero package/optional dependencies, provider engine, compile child process, network, download, or non-system runtime in the native target. Repository policy passes 141/141, product/reference preprocessors pass 151/151, and autodevelop passes 47/47. Workflow/version/release/native-contract/package/dependency/live full-and-production-audit/prefix/capability/documentation/format/compatibility/transform/benchmark/actionlint/syntax/JSON/formatting/diff gates pass; the isolated website passes 139/139 plus production build, documentation integrity passes 7/7, VS Code passes 13/13 plus the exact five-file 120,473-byte VSIX, and real Neovim 0.11.7 and 0.12.4 pass. Complete audits report zero vulnerabilities across the 136-dependency root graph, 385-dependency docs graph, and 331-dependency VS Code graph. The npm dry pack remains exactly 28 files, 92,750 packed bytes, and 406,429 unpacked bytes. Exact-main Build `29687567057` passed Test Suite `88194459537` and all five native archive/offline-install jobs on `dc6484df6f01bc779e8f5ef3a3292ced8339ba14`: macOS ARM64 `88194459511`, macOS x64 `88194459520`, Linux x64 `88194459529`, Linux ARM64 `88194459531`, and Windows x64 `88194459544`. The unpublished transition still contains four direct provider-era manifest dependencies and 86 locked packages and is not claimed as zero dependency; production routing, public capability claims, version, tag, artifact/package publication, deployment, native graduation, and the release interlock did not change. | `dc6484d` |
| `NSASS-011-SELECTOR-UNIFY-COMPLEX-STRICT` | `VERIFIED` | Extended the provider-free native Zig `sass:selector.unify()` path through default, exact custom, aliased, and unprefixed namespaces in SCSS and indented Sass. The bounded core now scans each canonical complex selector once, validates every simple token, replaces a complex subject from either operand with the compound intersection, preserves exact/shared ancestry, and pairwise-intersects equal-length ancestry whose links are exclusively aligned child or adjacent-sibling combinators. It retains left-outer/right-inner list order and partial-conflict omission, returns null for subject or aligned-ancestor conflicts, and renders only revalidated owned selectors. General descendant/following-sibling weaving, leading/trailing combinators, escaped identifiers, `:host`/`:host-context`, and functional pseudo-elements remain fail-closed `UnsupportedFeature` boundaries rather than guessed CSS. Shared selector, byte, complex-component, simple-component, temporary-allocation, and operation ceilings are cumulative across both inputs and every list pair; fixed-width overflow checks, cancellation, exact output validation, and exhaustive allocation-failure traversal prevent partial results. Test-first evidence captured direct selector 11/13, evaluator 114/116, and focused 227/231 with all four new positives failing at the former blanket complex-selector boundary. Exact Dart Sass 1.101.0 development-oracle bytes with `charset: false` are `.complex{left:.a .b.c;right:.b .a.c;exact:.a > .b;shared:.a .b.c;strict:.a.d > .b.e + .c.f;list:.a .b.c,.a .b.z,.x > .y.c,.x > .y.z;partial:.a b.z,.x c.y,.x .y.z;following-one:.a ~ .b.c;conflict:null}` and `.complex{left:.a .b.c;strict:.a.c > .b.d;list:.a .b.c,.x > .y.c;conflict:null}`; the exact strict type conflict `selector.unify("a > .b", "c > .d")` returns null. Direct selector/evaluator suites pass 13/13 and 116/116; focused exact Zig 0.15.2 Debug/ReleaseSafe each pass 23/23 steps and 231/231 tests, and full Debug/ReleaseSafe each pass 60/60 and 726/726. Selector/evaluator roots cross-compile for Windows x64 and Linux ARM64 in both modes. The same source passes focused/full Debug/ReleaseSafe with checksum-verified Zig 0.15.2 under Linux ARM64 confinement as uid/gid 65534, no network, no capabilities, `no-new-privileges`, read-only source/toolchain/container, bounded pids and 4-GiB memory/swap, and tmpfs-only cache/output/temp paths on `alpine@sha256:28bd5fe8b56d1bd048e5babf5b10710ebe0bae67db86916198a6eec434943f8b`; package consumer passes 6/6 steps and 2/2 tests, build integration passes 7/7 and 1/1, and benchmark smoke passes 3/3 in both modes without archiving a result. Native contract schema v3 adds only `built-in-selector-unify-complex-strict`; 10/10 plus check retains the recursive Zig/`std`/`builtin` closure with zero package/optional dependencies, provider engine, compile child process, network, download, or non-system runtime in the native target. Repository policy passes 141/141, product/reference preprocessors pass 151/151, and autodevelop passes 47/47. Workflow/version/release/native-contract/package/dependency/live-production-audit/prefix/capability/documentation/format/compatibility/transform/benchmark/syntax/JSON/formatting/diff gates pass; the isolated website passes 139/139 plus production build, VS Code passes 13/13 plus the exact five-file 120,473-byte VSIX, real Neovim 0.11.7 and 0.12.4 pass, and npm dry pack remains 28 files, 92,750 packed bytes, and 406,429 unpacked bytes. The plan-defined production audit is zero across root/docs/VS Code; a separate full docs development audit observed nine newly published, fixable tooling-only findings and is isolated to immediate `DEP-001-DOCS-DEV-REFRESH`, with no compiler or shipped-runtime finding. Exact-main Build `29678753063` passed Test Suite `88170921492` and all five native archive/offline-install jobs on `1e8acb03d948b513ca8ed69222d2d9d1d72d9caa`: Linux ARM64 `88170921526`, Linux x64 `88170921538`, macOS x64 `88170921546`, Windows x64 `88170921555`, and macOS ARM64 `88170921569`. The unpublished transition still contains four direct provider-era manifest dependencies and 86 locked packages and is not claimed as zero dependency; the exact providers remain quarantined development oracles until native graduation and `NATIVE-007` removes them from production closure. Production routing, version, tag, artifact/package publication, deployment, native graduation, release interlock, and public capability claims were not changed. | `1e8acb0` |
| `NSASS-011-SELECTOR-UNIFY-COMPOUND` | `VERIFIED` | Added provider-free native Zig `sass:selector.unify()` through default, exact custom, aliased, and unprefixed namespaces in SCSS and indented Sass. Canonical `$selector1`/`$selector2` binding accepts strings, compatible space/comma lists, and existing typed selector values, evaluates valid arguments exactly once in source order, and returns a typed comma selector list or native null. The bounded standard-library-only core performs deterministic left-outer/right-inner Cartesian compound intersection with duplicate-result retention; it owns type/universal/default/empty/named/wildcard namespace rules, exact and conflicting IDs, normalized safe attributes, class/placeholder ordering, functional pseudo-class coexistence and exact deduplication, pseudo-class placement, and legacy/modern pseudo-element equivalence and conflicts. Mixed compatible list pairs survive while conflicting pairs are omitted, and an all-conflict result becomes null. Complex selectors, escaped identifiers, `:host`/`:host-context`, and functional pseudo-elements fail closed as `UnsupportedFeature` rather than emitting guessed CSS. Shared selector, byte, component, temporary-allocation, and operation ceilings, cancellation, fixed-width overflow checks, every allocation-failure traversal, and exact output validation prevent partial or truncated results. Initial test-first evidence captured direct evaluator 112/114 and focused 224/226 with `InvalidExpression` at the former unsupported module boundary; the positive fixture and complex/escape/host boundary were both red. Review then added a 20-simple growing-compound regression and replaced a naive operation estimate with the exact safe sum of per-list-pair squared segment-length upper bounds. Exact Dart Sass 1.101.0 development-oracle bytes with `charset: false` are `.values{class:.a.b;same:.a;type-class:button.a;universal-type:button;namespace:svg|a;attributes:[x][y];pseudo:.a:hover;pseudo-element:.a::before;list:.a.x,.a.y,.b.x,.b.y;functional-exact:.b:not(.a);typed:.typed.more;from-list:.l.x,.r.x;keyword:.k1.k2;alias:.m1.m2;star:.m1.m2;marked:.m1.m2;evaluations:2;type:list;separator:comma;inspect:(.a.b,);type-conflict:null;id-conflict:null;namespace-conflict:null;pseudo-conflict:null}` and `.sass{value:.a.b;list:.a.x,.b.x;pseudo:.a::before;null:null}`. Direct selector and evaluator suites pass 12/12 and 115/115 in Debug, including exhaustive allocation failures; focused exact Zig 0.15.2 Debug/ReleaseSafe each pass 23/23 steps and 229/229 tests, and full Debug/ReleaseSafe each pass 60/60 steps and 724/724 tests. Direct selector/evaluator roots cross-compile for Windows x64 and Linux ARM64 in both modes. Exact source `641fb87` passes focused/full Debug/ReleaseSafe with the checksum-verified Zig 0.15.2 Linux ARM64 toolchain under non-root uid/gid 65534, no network, dropped capabilities, `no-new-privileges`, read-only source/toolchain/container, bounded pids and 4-GiB memory/swap, and tmpfs-only cache/output/temp paths on `alpine@sha256:28bd5fe8b56d1bd048e5babf5b10710ebe0bae67db86916198a6eec434943f8b`; default-parallel full ReleaseSafe first reached the exact container memory ceiling and was OOM-killed as harness evidence, while the identical source with `-j1` passed 60/60 and 724/724 below 800 MiB RSS. In the same confinement, package consumer Debug/ReleaseSafe pass 6/6 steps and 2/2 tests, build integration passes 7/7 and 1/1, and benchmark smoke passes 3/3 in both modes without archiving a result. Native contract schema v3 adds only `built-in-selector-unify-compound`; 10/10 plus check retains the recursive repository Zig/`std`/`builtin` closure with zero package/optional dependencies, provider engine, compile child process, network, download, or non-system runtime in the native target. Repository policy passes 141/141, product/reference preprocessors pass 151/151, and autodevelop passes 47/47. Workflow/version/release/native-contract/package/dependency/live-production-audit/prefix/capability/documentation/format/compatibility/transform/benchmark/syntax/JSON/formatting/diff gates pass; the isolated website passes 139/139 plus production build after a concurrent VS Code output-directory cleanup was identified as the sole transient first-run status delta, VS Code passes 13/13 plus an exact five-file 120,473-byte VSIX, real Neovim 0.11.7 and 0.12.4 pass, npm audit reports zero vulnerabilities, and npm dry pack remains 28 files, 92,750 packed bytes, and 406,429 unpacked bytes. Exact-main Build `29676374077` passed Test Suite `88164442115` and all five native archive/offline-install jobs on `641fb87cfd0c0b57901b8920004146c01ebd1f73`: Linux ARM64 `88164442118`, macOS x64 `88164442124`, Windows x64 `88164442125`, macOS ARM64 `88164442131`, and Linux x64 `88164442135`. The unpublished transition still contains four direct provider-era manifest dependencies and 86 locked packages and is not claimed as zero dependency; those exact engines remain quarantined development oracles until native graduation and `NATIVE-007` removes them from production closure. Production routing, version, tag, artifact/package publication, deployment, native graduation, release interlock, and public capability claims were not changed. | `641fb87` |
| `NSASS-011-SELECTOR-RELATIONS` | `VERIFIED` | Added provider-free native Zig `sass:selector.is-superselector()` through default, exact custom, and unprefixed namespaces in SCSS and indented Sass. Canonical `$super`/`$sub` binding accepts strings, compatible space/comma lists, and existing typed selector values, evaluates valid arguments exactly once in source order, and returns a native boolean. The bounded standard-library-only core parses both selector lists under shared selector/byte ceilings and implements every-subselector/any-superselector list semantics, structural compound containment, type/universal/namespace relations, classes, IDs, placeholders, exact attributes and pseudos, pseudo-element subject identity including legacy single-colon forms, and descendant/child plus following/adjacent-sibling supercombinators. Exact functional or escaped simple tokens remain usable when the subselector only adds constraints; non-identical functional-pseudo inference, escaped-identifier equivalence, and normalized attribute spelling remain explicit `UnsupportedFeature` boundaries rather than guessed results. Leading/trailing combinators return false, element selectors do not falsely subsume their pseudo-elements, incompatible namespaces/combinators return false, and selector/component/temporary/operation ceilings, cancellation, and every allocation failure return no partial CSS. Initial test-first evidence captured direct evaluator 112/113 and focused 222/223 with `InvalidExpression` at the former unsupported module boundary. Review-strengthening regressions then captured direct selector 9/10 and evaluator 112/113, focused 223/225, at the former blanket functional/escape guard before exact-token containment and pseudo-element subject behavior were corrected. Exact Dart Sass 1.101.0 development-oracle bytes with `charset: false` are `.values{same:true;class-extra:true;class-reverse:false;universal-type:true;type-universal:false;type-class:true;namespaced:true;any-namespace:true;wrong-namespace:false;descendant:true;descendant-child:true;child-descendant:false;sibling:true;adjacent:false;list-all:true;list-not-all:false;target-one:true;functional-exact:true;functional-extra:true;ordinary-functional-extra:true;pseudo-element-subject:false;pseudo-element-extra:true;legacy-pseudo-element:true;typed:true;from-list:true;alias:true;star:true;keyword:true;marked:true;evaluations:2}` and `.sass{same:true;complex:true;list:true}`; allocation traversal emits `.allocation{parsed:.a > .b,#c:hover;inspected:.a, .b;simple:[title=x],.foo,:hover;from-value:button,.primary;appended:.a.c,.b.c;nested:.a + .a,.a + .b,.b + .a,.b + .b;nested-value:.root.child;splat:.splat.item;relation:true}`. Direct selector and evaluator suites pass 10/10 and 113/113 in Debug and ReleaseSafe; focused exact Zig 0.15.2 Debug/ReleaseSafe each pass 23/23 steps and 225/225 tests, and full Debug/ReleaseSafe each pass 60/60 steps and 720/720 tests. Direct selector and evaluator semantic roots cross-compile for Windows x64 and Linux ARM64 in both modes. Exact source `197c9f7` passes the same focused/full matrix with the checksum-verified Zig 0.15.2 Linux ARM64 toolchain under non-root uid/gid 65534, no network, dropped capabilities, `no-new-privileges`, read-only source/toolchain/container, bounded pids/memory, and tmpfs-only cache/output/temp paths on `alpine@sha256:28bd5fe8b56d1bd048e5babf5b10710ebe0bae67db86916198a6eec434943f8b`. A first full-confinement harness attempt passed all 663 executable tests but correctly failed three install steps against read-only `/src/zig-out`; adding only a separately bounded tmpfs output mount produced 60/60 and 720/720 in both modes while source remained read-only. In the same confinement, package consumer Debug/ReleaseSafe pass 6/6 steps and 2/2 tests, build integration passes 7/7 and 1/1, and benchmark smoke passes 3/3 in both modes without archiving a result. Native contract schema v3 adds only `built-in-selector-relations`; 10/10 plus check retains the recursive repository Zig/`std`/`builtin` closure with zero package/optional dependencies, provider engine, compile child process, network, download, or non-system runtime in the native target. Repository policy passes 141/141, product/reference preprocessors pass 151/151, and autodevelop passes 47/47. Workflow/version/release/native-contract/package/dependency/live-production-audit/prefix/capability/documentation/format/compatibility/transform/benchmark/syntax/JSON/formatting/diff gates pass; the isolated website passes 139/139 plus production build, VS Code passes 13/13 plus an exact five-file 120,473-byte VSIX, real Neovim 0.11.7 and 0.12.4 pass, and npm dry pack remains 28 files, 92,750 packed bytes, and 406,429 unpacked bytes. Exact-main Build `29673865617` passed Test Suite `88157498257` and all five native archive/offline-install jobs on `197c9f7cb4a75581e2317a057f112c3b7489c852`: Windows x64 `88157498266`, Linux ARM64 `88157498281`, Linux x64 `88157498289`, macOS x64 `88157498291`, and macOS ARM64 `88157498315`. The unpublished transition still contains four direct provider-era manifest dependencies and 86 locked packages and is not claimed as zero dependency; those exact engines remain quarantined development oracles until native graduation. Production routing, version, tag, artifact/package publication, deployment, native graduation, release interlock, and public capability claims were not changed. | `197c9f7` |
| `NSASS-011-SELECTOR-COMPOSITION` | `VERIFIED` | Added provider-free native Zig `sass:selector.append()` and `selector.nest()` through default, exact custom, and unprefixed namespaces in SCSS and indented Sass. Variadic arguments retain source-order evaluation, list splats expand canonically, strings/lists/existing typed selector values are accepted through one owned conversion boundary, and results are typed comma selector lists observed correctly by `meta.type-of()`, `meta.inspect()`, and list functions. The bounded standard-library-only selector core implements deterministic Cartesian list composition, complex suffix append, descendant/explicit-combinator nesting, multiple composition stages, repeated `&` expansion, and parent substitution inside admitted selector-valued functional pseudos. It rejects invalid parent positions, parents in attributes/arithmetic or unadmitted functional contexts, leading/trailing combinator misuse during append, bad types/arity/keywords, selector/count/byte/temporary/operation ceilings, cancellation, and every allocation failure with no partial CSS. Unknown functional-pseudo parent contexts and valid-but-not-yet-modeled `nth-* of &` details remain deliberately unavailable rather than producing guessed semantics. Test-first evidence captured the new evaluator suite at 0/1 with `InvalidExpression` at the former unsupported `tryModuleBuiltin` boundary. Exact Dart Sass 1.101.0 development-oracle bytes with `charset: false` are `.values{append-one:.a;append-basic:.foo.bar;append-three:.accordion__copy.open;append-list:.a.c,.a.d,.b.c,.b.d;append-complex:.a > .b.c > .d;append-pseudo:.foo:hover;append-type:.foobutton;append-attr:.foo[x];append-universal:*.foo;append-namespaced:svg|a.foo;nest-one:.a,.b;nest-basic:.foo .bar;nest-parent:.foo:hover;nest-list:.a .c,.a .d,.b .c,.b .d;nest-repeat:.foo + .foo,.foo + .bar,.bar + .foo,.bar + .bar;nest-deep:.a:hover,.c .a,.b:hover,.c .b;nest-combinator:.a > .b;nest-three:.a.b:hover;append-typeof:list;nest-typeof:list;append-inspect:(.foo.bar,);nest-inspect:(.foo .bar,);append-length:2;from-value:.typed.value;list-splat:.splat.item;nest-splat:.root.child;alias:.alias.ok;star:.star.ok;marked:.marked.ok;evaluations:2}` and `.sass{type:list;append:.a.c,.b.c;nest:.a:hover,.b:hover;inspect:(.root > .child,)}`; allocation traversal emits `.allocation{parsed:.a > .b,#c:hover;inspected:.a, .b;simple:[title=x],.foo,:hover;from-value:button,.primary;appended:.a.c,.b.c;nested:.a + .a,.a + .b,.b + .a,.b + .b;nested-value:.root.child;splat:.splat.item}`. Direct selector and evaluator suites pass 8/8 and 112/112 in Debug and ReleaseSafe; focused exact Zig 0.15.2 Debug/ReleaseSafe each pass 23/23 steps and 222/222 tests, and full Debug/ReleaseSafe each pass 60/60 steps and 717/717 tests. Windows x64 and Linux ARM64 evaluator semantic cross-compiles pass in both modes. Exact committed source `4fc5726` passes the same focused/full matrix in a checksum-verified Zig 0.15.2 Linux ARM64 toolchain under non-root uid/gid 65534, no network, dropped capabilities, `no-new-privileges`, read-only source/toolchain/container, bounded pids/memory, and tmpfs-only cache/output/temp paths on `alpine@sha256:28bd5fe8b56d1bd048e5babf5b10710ebe0bae67db86916198a6eec434943f8b`; the first harness attempt correctly exposed that `std.testing.tmpDir` requires `.zig-cache/tmp`, and the corrected confinement overlays only `/src/.zig-cache` while all source files remain read-only. In the same confinement, package consumer Debug/ReleaseSafe pass 6/6 steps and 2/2 tests, build integration passes 7/7 and 1/1, and benchmark smoke passes 3/3 without archiving a result. Native contract schema v3 adds only `built-in-selector-composition`; 10/10 plus check retains the recursive repository Zig/`std`/`builtin` closure with zero package/optional dependencies, provider engine, compile child process, network, download, or non-system runtime in the native target. Repository policy passes 141/141, product/reference preprocessors pass 151/151, and autodevelop passes 47/47. Workflow/version/release/native-contract/package/dependency/live-production-audit/prefix/capability/documentation/format/compatibility/transform/benchmark/syntax/JSON/formatting/diff gates pass; the isolated website passes 139/139 plus production build, VS Code passes 13/13 plus an exact five-file 120,473-byte VSIX, real Neovim 0.11.7 and 0.12.4 pass, and npm dry pack remains 28 files, 92,750 packed bytes, and 406,429 unpacked bytes. Exact-main Build `29671397270` passed Test Suite `88150942412` and all five native archive/offline-install jobs on `4fc57267f08d4e1eda2e96786ec2b532f91d4c2a`: macOS ARM64 `88150942414`, Linux ARM64 `88150942418`, macOS x64 `88150942424`, Linux x64 `88150942499`, and Windows x64 `88150942502`. The unpublished transition still contains four direct provider-era manifest dependencies and 86 locked packages and is not claimed as zero dependency; those exact engines remain quarantined development oracles until native graduation. Production routing, version, tag, artifact/package publication, deployment, native graduation, release interlock, and public capability claims were not changed. | `4fc5726` |
| `NSASS-011-SELECTOR-PARSE-SIMPLE` | `VERIFIED` | Added a provider-free native Zig selector-value parser and the `sass:selector` functions `parse()` and `simple-selectors()` through default, exact custom, and unprefixed namespaces in SCSS and indented Sass. Strings, decoded quoted strings, compatible space/comma lists, and existing typed selector values become deeply owned canonical selector/list values; `meta.type-of()`, `meta.inspect()`, and list operations observe the required Sass value shape. The bounded parser understands selector lists, complex/compound structure, descendant and `>`, `+`, `~` combinators, namespaced types, classes, IDs, placeholders, attributes, pseudos, functional pseudos, strings, escapes, and forgiving non-leading empty groups. It accepts the pinned leading/trailing combinator behavior while rejecting leading empty groups, the Sass-unsupported column combinator, malformed delimiters/escapes, parent selectors, complex input to `simple-selectors()`, invalid value types, bad arity/binding/splats, missing or case-mismatched namespaces, selector/count/byte/temporary/operation ceilings, and every allocation failure without partial CSS. Test-first evidence recorded both new integration suites red at the former unsupported module boundary and later recorded the direct parser at 4/5 until leading combinators matched the pinned oracle. Exact Dart Sass 1.101.0 development-oracle bytes with `charset: false` are `.values{basic:.foo;list:.foo > .bar,#id:hover;normalized:.foo > .bar,#id:hover;functional:[data-x="a,b"]:not(.x, .y);leading:> .foo,+ .bar,~ .baz;from-list:a,b;interpolated:.item-2;type:list;parse-item-type:list;single-inspect:(.foo,);parse-length:2;parse-separator:comma;inspect:.foo > .bar, #id:hover;simple:a,.foo,#id,:hover,::before,[title=x];simple-item-type:string;functional-simple::not(.a, .b), .x;simple-len:3;simple-first:a;simple-second:.foo;named:.named;named-simple:button,.primary;alias:.alias;star:.star;marked:.marked;marked-simple:a,.marked;evaluations:2}` and `.sass{type:list;parse:.foo > .bar,#bar:hover;inspect:.foo > .bar, #bar:hover;simple:button,.primary,:hover}`; allocation traversal emits `.allocation{parsed:.a > .b,#c:hover;inspected:.a, .b;simple:[title=x],.foo,:hover;from-value:button,.primary}`. Direct parser passes 5/5 and direct evaluator passes 111/111 in Debug and ReleaseSafe. Focused Debug/ReleaseSafe each pass 23/23 steps and 218/218 tests; exact Zig 0.15.2 full Debug/ReleaseSafe each pass 60/60 steps and 713/713 tests. Windows x64 and Linux ARM64 evaluator cross-compiles pass in both modes. The checksum-verified official Linux ARM64 Zig toolchain in a non-root, no-network, capability-free, read-only `alpine@sha256:28bd5fe8b56d1bd048e5babf5b10710ebe0bae67db86916198a6eec434943f8b` container passes focused and full Debug/ReleaseSafe with only executable tmpfs cache/output mounts; package-consumer, build-integration, and benchmark-smoke confinement gates also pass. Native contract schema v3 adds only `built-in-selector-parse-simple`; 10/10 plus check retains the recursive repository-owned Zig/`std`/`builtin` target closure with zero package or optional dependencies, external engines, child processes, network, downloads, or non-system language libraries. A clean npm install audits 82 packages at zero vulnerabilities; repository policy passes 141/141, product/reference preprocessor tests pass 151/151, and autodevelop passes 47/47 in isolation. Dependency, live-production audit, package/provider/product/capability/format/version/release/publication, prefix, compatibility, transform, benchmark, documentation, workflow, formatting, syntax, JSON, and diff gates pass. The isolated website passes 139/139 plus production build; VS Code passes 13/13 plus a five-file 120,473-byte VSIX; real Neovim 0.11.7 and 0.12.4 integrations pass. The npm dry run remains 28 files, 92,750 packed bytes, and 406,429 unpacked bytes. Exact-main Build `29669392414` passed comprehensive Test Suite `88145587117` and all five native archive/offline-install jobs on `9c25dd4369092cc40c9ebe6501e9192d0a77f4f0`: Windows x64 `88145587146`, Linux x64 `88145587147`, macOS ARM64 `88145587149`, Linux ARM64 `88145587151`, and macOS x64 `88145587156`. The unpublished transition still contains four direct provider-era development dependencies and 86 locked packages and is not claimed as zero dependency. Production routing, native graduation, release interlock, version, tag, artifact publication, package publication, deployment, and public native capability claims were not changed. | `9c25dd4` |
| `NSASS-011-META-INSPECTION` | `VERIFIED` | Added provider-free native Zig `meta.type-of()` and `meta.inspect()` through default, exact custom, and unprefixed `sass:meta` namespaces in SCSS and indented Sass, plus the canonical legacy globals `type-of()` and `inspect()`. Canonical `$value` binding evaluates valid arguments exactly once. Type inspection covers null, booleans, numbers, strings, colors, lists, maps, calculations, argument lists, and the already modeled callable function/mixin tags; calculation recognition is case-insensitive only for a complete calculation value, so `var()` remains a string and a composite such as `foo calc(...)` remains a list. Recursive inspection owns exact null/boolean/number/color/string output, comma/space/bracket/slash lists, maps, calculations, and argument lists, including nested comma-list parentheses, singleton `(1,)`, empty `()`, and the upstream rule that inspected argument lists omit accessed keyword arguments. First-class callable inspection remains an explicit failing boundary until the later callable meta slice. Missing or case-mismatched namespaces, bad arity, unknown/duplicate keyword names, operation/temporary limits, unsupported callable inspection, and every allocation failure fail closed without partial CSS; allocation traversal includes keyword access, recursive values, diagnostics, output, and source maps. Test-first evidence recorded the focused 1/2 boundary with the positive suite failing at the former unsupported module member while the negative suite already failed closed. Exact Dart Sass 1.101.0 development-oracle bytes with `charset: false` are `.values{null-type:null;bool-type:bool;number-type:number;string-type:string;color-type:color;list-type:list;map-type:map;calculation-type:calculation;css-variable-type:string;uppercase-calculation-type:calculation;composite-type:list;empty-map-type:map;arglist-type:arglist;null-inspect:null;bool-inspect:true;number-inspect:1px;quoted-inspect:"hello world";unquoted-inspect:hello world;color-inspect:#a1b2c3;comma-inspect:1, 2;space-inspect:1 2;bracket-inspect:[1 2];slash-inspect:1 / 2;map-inspect:(a: 1, "b": (2, 3));empty-map-inspect:();null-list-inspect:null, 1, null;calculation-inspect:calc(1px + var(--x));arglist-inspect:1, 2;single-arglist-inspect:(1,);keyword-arglist-inspect:(1,);alias:color;star:(a: 1);global-type:number}` and `.sass{type:map;inspect:[1 2]}`. The canonical non-compressible `#a1b2c3` probe deliberately avoids overclaiming lexical color-spelling retention, which remains a later typed-value differential boundary. Direct evaluator passes 108/108 in Debug and ReleaseSafe. Focused Debug/ReleaseSafe each pass 21/21 steps and 210/210 tests; exact Zig 0.15.2 full Debug/ReleaseSafe each pass 58/58 steps and 705/705 tests. Direct evaluator cross-compiles pass for Windows x64 and Linux ARM64 in both modes. The checksum-verified official Linux ARM64 Zig toolchain in a non-root, no-network, capability-free, read-only `alpine@sha256:28bd5fe8b56d1bd048e5babf5b10710ebe0bae67db86916198a6eec434943f8b` container passes focused and full Debug/ReleaseSafe with only executable tmpfs cache/output mounts. In the same confinement, the package consumer passes 6/6 steps and 2/2 tests in both modes, the build-integration example passes 7/7 and 1/1 in both modes, and benchmark smoke passes 3/3. Native contract schema v3 adds only `built-in-meta-inspection`; 10/10 plus check retains the recursive repository-owned Zig/`std`/`builtin` target closure with zero package or optional dependencies, external engines, child processes, network, downloads, or non-system language libraries. Repository policy/tests pass 143/143, product/reference preprocessor tests pass 151/151, and autodevelop passes 47/47 in isolation. Dependency, policy, live-audit, package/provider/product/capability/format/version/release/publication, prefix, compatibility, transform, benchmark, documentation, workflow, formatting, syntax, JSON, and diff gates pass. The isolated website passes 139/139 plus production build; VS Code passes 13/13 plus a five-file 120,473-byte VSIX; real Neovim 0.11.7 and 0.12.4 integrations pass. The npm dry run remains 28 files, 92,750 packed bytes, and 406,429 unpacked bytes. Exact-main Build `29666600079` passed comprehensive Test Suite `88137925826` and all five native archive/offline-install jobs on `0b03095f0f6d901f74a68fc32741c9740adc6293`: Linux x64 `88137925824`, macOS ARM64 `88137925828`, Windows x64 `88137925829`, macOS x64 `88137925836`, and Linux ARM64 `88137925870`. The unpublished transition still contains four direct provider-era dependencies and 86 locked packages and is not claimed as zero dependency. Production routing, native graduation, release interlock, version, tag, artifact publication, package publication, deployment, and public native capability claims were not changed. | `0b03095` |
| `NSASS-011-MATH-CONSTANTS-DETERMINISTIC-RANDOM` | `VERIFIED` | Added all seven provider-free native Zig `sass:math` constants (`$e`, `$epsilon`, `$max-number`, `$max-safe-integer`, `$min-number`, `$min-safe-integer`, and `$pi`) through default, exact custom, and unprefixed namespaces in SCSS and indented Sass. Underscore/hyphen normalization, interpolation, direct serialization, arithmetic and built-in use, root binding conflicts, immutable global/star bindings, allowed local shadowing, missing/case-mismatched namespaces and members, and qualified mutation failure are executable. Built-in constants retain their complete finite IEEE `f64` payload until arithmetic consumes it, so epsilon scaling is correct even though direct epsilon and true-minimum serialization round to zero. Added module and legacy/global `random()` with no-argument/`null` unit-interval results, inclusive positive integer limits through `2^32`, named `$limit`, list/map/empty splats, canonical map override precedence, evaluate-once behavior, one unit-deprecation warning, and unbiased bounded selection. ZigCSS deliberately resolves upstream Sass nondeterminism in favor of the roadmap's reproducible-build requirement: a stable FNV-1a seed over root source bytes drives a fixed SplitMix64 stream, excluding paths, processes, clocks, and host entropy; identical bytes therefore produce identical CSS across filenames and hosts. This is a deterministic build primitive and is explicitly not cryptographic. The frozen native golden is `.golden{first:.7417619928;bounded:5;second:.2622934202}`. Decimal, zero, negative, over-`2^32`, nonnumeric/dynamic limits, excess positional arguments, unknown/duplicate keywords, invalid positional/map combinations, operation ceilings, and every allocation failure fail closed without partial CSS; allocation traversal owns constants, epsilon arithmetic, map splats, unit warnings, random state, diagnostics, CSS, and source maps. Test-first evidence recorded the expected 102/104 direct evaluator red boundary: the positive suite failed at unresolved constants and the negative suite reached the former undefined-variable boundary. A deliberate placeholder then froze the exact deterministic golden, and a qualified assignment probe established the parser-terminal `InvalidSyntax` boundary. Exact Dart Sass 1.101.0 development-oracle constant bytes with `charset: false` are `.constants{e:2.7182818285;pi:3.1415926536;epsilon-scaled:.2220446049;max-ratio:179769313.48623157;max-safe:9007199254740991;min-number:0;min-safe:-9007199254740991;alias:3.1415926536;star:2.7182818285;underscore:9007199254740991;trig:1;interpolated:3.1415926536}`; native adds only the separately validated allowed `local-shadow:1`. Oracle probes also established the accepted random ranges, integer/unit/null/keyword/splat behavior, `2^32` ceiling, and provider deprecation diagnostics; exact random bytes intentionally are not copied because upstream is nondeterministic. Direct evaluator and numeric suites pass 105/105 and 8/8 in Debug and ReleaseSafe. Focused Debug/ReleaseSafe each pass 21/21 steps and 207/207 tests; exact Zig 0.15.2 full Debug/ReleaseSafe each pass 58/58 steps and 702/702 tests. Direct evaluator cross-compiles pass for Windows x64 and Linux ARM64 in both modes. The checksum-verified official Linux ARM64 Zig toolchain in a non-root, no-network, capability-free, read-only `alpine@sha256:28bd5fe8b56d1bd048e5babf5b10710ebe0bae67db86916198a6eec434943f8b` container passes focused and full Debug/ReleaseSafe with only executable tmpfs cache/output mounts. In the same confinement, the package consumer passes 6/6 steps and 2/2 tests in both modes, the build-integration example passes 7/7 and 1/1 in both modes, and benchmark smoke passes 3/3. Native contract schema v3 adds only `built-in-math-constants-deterministic-random`; 10/10 plus check retains the recursive repository-owned Zig/`std`/`builtin` target closure with zero package or optional dependencies, external engines, child processes, network, downloads, or non-system language libraries. Repository policy/tests pass 143/143, product/reference preprocessor tests pass 151/151, and autodevelop passes 47/47 in isolation. Dependency, policy, live-audit, package/provider/product/capability/format/version/release/publication, prefix, compatibility, transform, benchmark, documentation, workflow, formatting, syntax, JSON, and diff gates pass. The isolated website passes 139/139 plus production build; VS Code passes 13/13 plus a five-file 120,473-byte VSIX; real Neovim 0.11.7 and 0.12.4 integrations pass. The npm dry run remains 28 files, 92,750 packed bytes, and 406,429 unpacked bytes. Exact-main Build `29664568798` passed comprehensive Test Suite `88132725894` and all five native archive/offline-install jobs on `16b4d2e63a04eca06f3796e8632f21e251f1d84c`: Linux ARM64 `88132725908`, macOS x64 `88132725909`, macOS ARM64 `88132725911`, Linux x64 `88132725912`, and Windows x64 `88132725925`. The first local website run intentionally exposed concurrent VS Code build-output interference and passed 139/139 when rerun in the required isolated lane; the first Neovim 0.11.7 invocation lacked its expected-version variable and both exact versions passed after correcting the harness invocation. The unpublished transition still contains the provider reference files and 86 locked packages and is not claimed as zero dependency. Production routing, release interlock, version, tag, artifact publication, package publication, deployment, and public native capability claims were not changed. | `16b4d2e` |
| `NSASS-011-MATH-EXTREMA-HYPOTENUSE` | `VERIFIED` | Added provider-free native Zig `min()`, `max()`, `clamp()`, and `hypot()` through default, exact custom, and unprefixed `sass:math` namespaces in SCSS and indented Sass, plus their canonical global CSS calculation names. Variadic module calls evaluate in source order and accept list splats; canonical `$min`, `$number`, and `$max` binding evaluates valid clamp expressions exactly once in source order. Strict extrema convert compatible known units for comparison while returning the selected original value, preserve ties and IEEE signed-zero selection, and retain unitless-versus-unitful Sass ordering. Strict clamp validates every dimension and follows canonical low/mid/high and tie selection. Hypotenuse folds with stable `std.math.hypot`, preserves the first argument's exact known, percentage, custom, or compound unit system, and rejects incompatible, unitless/unitful, or nonfinite inputs. Static global calls evaluate natively; unresolved `var()` or identifier arguments preserve already-evaluated global CSS calculations without double evaluation, and relative-unit global `hypot(1em, 2px)` remains deferred while provably incompatible known dimensions fail closed. Exact Sass frequency spelling is now `Hz`/`kHz`; lowercase variants remain incompatible as required. Bad arity, empty variadics, invalid or case-mismatched units, nonnumbers, unknown or duplicate keywords, unsupported splats, missing/case-mismatched namespaces, operation/argument limits, nonfinite results, and every allocation failure fail closed without partial CSS. Test-first work recorded the expected 100/102 direct evaluator baseline with only the two new suites failing at the former unsupported `math.min()` boundary. Oracle review then reproduced two additional red boundaries before correction: focused evaluator 0/2 and direct numeric 0/1 exposed inherited lowercase frequency spelling and premature rejection of a valid deferred relative-unit global hypot calculation. Exact Dart Sass 1.101.0 development-oracle bytes with `charset: false` are `.a{min:1px;min-converted:95px;min-unitless:1px;min-negative:-2px;max:3px;max-converted:97px;max-unitless:2;max-negative:-1px;clamp:2px;clamp-low:1px;clamp-high:3px;clamp-equal:96px;hypot:5px;hypot-converted:1.4142135624in;hypot-frequency:2000.00025Hz;hypot-percent:5%;hypot-custom:13foo;hypot-one:3px;hypot-compound-unit:"px/s";alias:3s;star:2em;splat:1px;named-clamp:2px;evaluations:3;min-zero-angle:0deg;max-zero-angle:180deg;clamp-zero-angle:180deg;hypot-zero-angle:0deg;global-min:2ms;global-max:4deg;global-clamp:2foo;global-hypot:17px}` for SCSS, `.sass{min:1px;max:2s;clamp:2em;hypot:5foo;globals:1px 2s 2em 5foo}` for indented Sass, `.plain{static-min:1px;static-max:2s;static-clamp:2em;static-hypot:5foo;relative-hypot:hypot(1em,2px);min:min(var(--x),1px);min-name:min(foo,1px);max:max(var(--x),2px);max-name:max(foo,2px);clamp:clamp(1px,var(--x),3px);clamp-name:clamp(1px,foo,3px);hypot:hypot(var(--x),4px);hypot-name:hypot(foo,4px)}` for global CSS, and `.dynamic{min:min(var(--a),1px);max:max(var(--b),2px);clamp:clamp(1px,var(--c),3px);hypot:hypot(var(--d),4px);evaluations:4}` for evaluate-once interpolation. Direct evaluator passes 102/102 in Debug and ReleaseSafe; direct numeric coverage passes 7/7. Focused Debug/ReleaseSafe each pass 21/21 steps and 203/203 tests; exact Zig 0.15.2 full Debug/ReleaseSafe each pass 58/58 steps and 698/698 tests. Checksum-verified official Zig 0.15.2 native Linux ARM64 in a non-root Docker container at `alpine@sha256:28bd5fe8b56d1bd048e5babf5b10710ebe0bae67db86916198a6eec434943f8b`, with read-only source/container/toolchain, executable cache/output tmpfs mounts, no capabilities, and no network, passes the focused and full suites in both modes on the exact checkpoint. Direct evaluator cross-compiles pass for Windows x64 and Linux ARM64 in Debug and ReleaseSafe; final hosted builds verify exact native archives and offline npm installation on all five supported targets. Native contract schema v3 adds only `built-in-math-extrema-hypotenuse`; 10/10 plus check retains the recursive repository-owned Zig/`std`/`builtin` closure and target zero package or optional dependencies, external engines, child processes, network, downloads, or non-system language libraries. Repository policy/tests pass 143/143, product preprocessor tests pass 151/151, package consumer Debug/ReleaseSafe pass 6/6 steps and 2/2 tests, build integration Debug/ReleaseSafe passes 7/7 and 1/1, benchmark smoke passes 3/3, autodevelop passes 47/47 in isolation, and repository-wide Zig/Bash/JavaScript/JSON formatting and syntax plus diff gates pass. Dependency/policy/live-production audits, package/provider/product/capability/format/version/release/publication checks, compatibility/transform/prefix checks, every benchmark check, documentation integrity 7/7 over 38 Markdown files and 68 fences, isolated website 139/139 plus build, VS Code 13/13 plus a five-file 120,473-byte VSIX, real Neovim 0.11.7 and 0.12.4 integration, and a 28-file/92,750-byte packed and 406,429-byte unpacked npm dry run pass. Exact-main Build `29662710949` passed comprehensive Test Suite `88127962156` and all five native archive/offline-install jobs on `2bc599c51309523cd19255e8c236d97bc0d8dac0`: Windows x64 `88127962168`, Linux x64 `88127962182`, Linux ARM64 `88127962183`, macOS ARM64 `88127962186`, and macOS x64 `88127962200`. The one parallel autodevelop fingerprint mismatch was reproduced as concurrent working-tree interference and passed 47/47 when rerun in the required isolated mode. Nonfinite reference results remain deliberately safe-narrowed to terminal errors until normalized compatibility diagnostics are implemented. Dart Sass, Less, and Stylus remain quarantined development-only black-box oracles rather than runtime dependencies or fallbacks; production routing, release interlock, version, tag, artifact publication, package publication, deployment, and public capability claims were not changed. | `2bc599c` |
| `NSASS-011-MATH-TRIGONOMETRY` | `VERIFIED` | Added provider-free native Zig `sin()`, `cos()`, `tan()`, `asin()`, `acos()`, `atan()`, and `atan2()` through default, exact custom, and unprefixed `sass:math` namespaces in SCSS and indented Sass, plus their canonical global CSS calculation names. Forward trigonometry accepts unitless radians or exact-case `deg`, `grad`, `rad`, and `turn`; inverse functions require unitless finite numbers and return degrees. `atan2()` converts compatible known, percent, exact custom, and compound dimensions into the first argument's unit system, rejects unitless/unitful and incompatible pairs, and preserves IEEE signed zero internally so `(0,-0)` and `(-0,-0)` produce `180deg` and `-180deg` while ordinary CSS number serialization still canonicalizes standalone negative zero. Canonical `$number` and `$y`/`$x` binding evaluates valid expressions exactly once in source order. Static global calls evaluate natively; dynamic unqualified CSS calculations retain their authored function name from already-evaluated owned values, while module and unprefixed-import calls stay strict. Bad arity, invalid or case-mismatched units, nonnumbers, lists/colors/quoted strings, unknown or duplicate keywords, selected splats, missing/case-mismatched namespaces, operation limits, inverse-domain failures, nonfinite results, and every allocation failure fail closed without partial CSS. Test-first work recorded the expected 98/100 direct evaluator baseline with only the two new suites failing at the former unsupported-member boundary. Semantic review then reproduced a signed-zero red boundary: all three zero-quadrant `atan2()` probes emitted `0deg` before native numeric and value ownership retained the sign bit; the final compound-unit and percentage additions were separately matched against the oracle. Exact Dart Sass 1.101.0 development-oracle bytes with `charset: false` are `.a{sin-zero:0;sin-deg:.5;sin-grad:1;sin-turn:1;sin-rad:.8414709848;cos-zero:1;cos-deg:.5;tan:1;asin:30deg;acos:60deg;atan:45deg;atan-negative:-45deg;atan2:45deg;atan2-units:45deg;atan2-percent:26.5650511771deg;atan2-compound:.0062169899deg;atan2-custom:63.4349488229deg;atan2-zero:0deg;atan2-negative-x-zero:180deg;atan2-negative-zero-pair:-180deg;atan2-negative-y-zero:0deg;atan2-negative:-135deg;alias:1;star:-1;named:45deg;evaluations:2;global-sin:.5;global-cos:.5;global-tan:1;global-asin:30deg;global-acos:60deg;global-atan:45deg;global-atan2:45deg}` for SCSS, `.sass{sin:.5;cos:.5;tan:1;asin:30deg;acos:60deg;atan:45deg;atan2:45deg;globals:.5 .5 1 30deg 60deg 45deg 45deg}` for indented Sass, `.plain{sin:sin(var(--number));sin-name:sin(foo);cos:cos(var(--number));cos-name:cos(foo);tan:tan(var(--number));tan-name:tan(foo);asin:asin(var(--number));asin-name:asin(foo);acos:acos(var(--number));acos-name:acos(foo);atan:atan(var(--number));atan-name:atan(foo);atan2:atan2(var(--y),1);atan2-name:atan2(foo,bar)}` for dynamic global CSS, and `.dynamic{sin:sin(foo);atan2:atan2(bar,1);evaluations:2}` for evaluate-once interpolation. Direct evaluator passes 100/100 in Debug and ReleaseSafe. Focused Debug/ReleaseSafe each pass 21/21 steps and 201/201 tests; exact Zig 0.15.2 full Debug/ReleaseSafe each pass 58/58 steps and 696/696 tests. Checksum-verified official Zig 0.15.2 native Linux ARM64 in a non-root Docker container at `alpine@sha256:28bd5fe8b56d1bd048e5babf5b10710ebe0bae67db86916198a6eec434943f8b`, with read-only source/container/toolchain, executable cache tmpfs mounts, no capabilities, and no network, passes the focused and full suites in both modes on the exact checkpoint. Direct evaluator cross-compiles pass for Windows x64 and Linux ARM64 in Debug and ReleaseSafe; controlled ReleaseSafe production builds verify as PE32+ x86-64, statically linked ELF ARM64, and Mach-O ARM64. Native contract schema v3 adds only `built-in-math-trigonometry`; 10/10 plus check retains the recursive repository-owned Zig/`std`/`builtin` closure and target zero package or optional dependencies, external engines, child processes, network, downloads, or non-system language libraries. Workflow, dependency/policy/live-production audits, package/provider/product/capability/format/version/release/publication/consumer checks, compatibility/transform/prefix checks, every benchmark check, package consumers/examples, autodevelop 47/0, repository-wide Zig/Bash/JavaScript/JSON formatting and syntax, documentation integrity 7/7 over 38 Markdown files and 68 fences, isolated website 139/139 plus build, VS Code 13/13 plus package, real Neovim 0.11.7 and 0.12.4 integration, benchmark smoke 3/3, a 28-file/92,750-byte packed and 406,429-byte unpacked npm dry run, and diff gates pass. Exact-main Build `29660277973` passed comprehensive Test Suite `88121651016` and all five native archive/offline-install jobs on `146f1c9e8be1a8e9b9df73548d9160b36f9ec3ff`: Linux ARM64 `88121651060`, macOS x64 `88121651063`, macOS ARM64 `88121651070`, Linux x64 `88121651077`, and Windows x64 `88121651093`. Nonfinite reference results remain deliberately safe-narrowed to terminal errors until normalized compatibility diagnostics are implemented. Dart Sass, Less, and Stylus remain quarantined development-only black-box oracles rather than runtime dependencies or fallbacks; production routing, release interlock, version, tag, artifact publication, package publication, deployment, and public capability claims were not changed. | `146f1c9` |
| `NSASS-011-MATH-POWERS-ROOTS-LOGARITHMS` | `VERIFIED` | Added provider-free native Zig `pow()`, `sqrt()`, and `log()` through default, exact custom, and unprefixed `sass:math` namespaces in SCSS and indented Sass, plus the canonical global calculation names. Canonical `$base`, `$exponent`, `$number`, and optional `$base` binding evaluates valid expressions exactly once in source order. Static calls require unitless native numbers and use bounded finite arithmetic; dynamic unqualified CSS calculations retain their authored function name with already-evaluated owned arguments, while module and unprefixed-import calls stay strict. The logarithm base-zero boundary canonically emits `0` like Dart Sass. Bad arity, unit-bearing or nonnumeric values, unknown/duplicate keywords, selected splats, missing/case-mismatched namespaces, operation limits, and nonfinite square-root, logarithm, or power results fail closed without partial CSS. Test-first work recorded the expected 96/98 direct evaluator baseline with only the two new suites failing at the former unsupported-member boundary; the earlier lazy unsupported-member assertions now use still-unimplemented `random()` rather than executing a newly supported call. Semantic review then reproduced a second red boundary: deferred interpolation evaluated twice and emitted `evaluations:6` where Dart Sass emits `evaluations:3`. Reconstructing deferred calculations from their already-evaluated values fixed the new functions and the shared one-argument `abs()`/`round()` path without changing their emitted bytes; exhaustive allocation-failure traversal covers both static and deferred results. Exact Dart Sass 1.101.0 development-oracle bytes with `charset: false` are `.a{pow:8;pow-negative:.125;pow-fraction:3;sqrt:1.4142135624;sqrt-perfect:9;log:2.302585093;log-base:3;log-half:2;log-zero-base:0;alias:9;star:4;named-pow:8;named-log:3;evaluations:4;legacy-pow:16;legacy-sqrt:5;legacy-log:2}` for SCSS, `.sass{pow:32;sqrt:7;log:3;globals:8 2 3}` for indented Sass, `.plain{pow:pow(var(--base),2);pow-name:pow(foo,2);sqrt:sqrt(var(--number));sqrt-name:sqrt(foo);log:log(var(--number),2);log-name:log(foo)}` for dynamic global CSS, and `.dynamic{pow:pow(foo,2);sqrt:sqrt(bar);log:log(baz,2);evaluations:3}` for evaluate-once interpolation. Direct evaluator passes 98/98 in Debug and ReleaseSafe. Focused Debug/ReleaseSafe each pass 21/21 steps and 199/199 tests; exact Zig 0.15.2 full Debug/ReleaseSafe each pass 58/58 steps and 694/694 tests. Checksum-verified official Zig 0.15.2 native Linux ARM64 in a non-root Docker container at `alpine@sha256:28bd5fe8b56d1bd048e5babf5b10710ebe0bae67db86916198a6eec434943f8b`, with read-only source/container/toolchain, executable cache tmpfs mounts, no capabilities, and no network, passes the focused and full suites in both modes. Direct evaluator cross-compiles pass for Windows x64 and Linux ARM64 in Debug and ReleaseSafe; controlled ReleaseSafe production builds verify as PE32+ x86-64, statically linked ELF ARM64, and Mach-O ARM64. Native contract schema v3 adds only `built-in-math-powers-roots-logarithms`; 10/10 plus check retains the recursive repository-owned Zig/`std`/`builtin` closure and target zero package or optional dependencies, external engines, child processes, network, downloads, or non-system language libraries. Workflow, dependency/policy/live-production audits, package/provider/product/capability/format/version/release/publication/consumer checks, compatibility/transform/prefix checks, every benchmark check, package consumers/examples, autodevelop 47/0, repository-wide Zig/Bash/JavaScript/JSON formatting and syntax, documentation integrity 7/7 over 38 Markdown files and 68 fences, isolated website 139/139 plus build, VS Code 13/13 plus package, real Neovim 0.11.7 and 0.12.4 integration, benchmark smoke 3/3, a 28-file/92,750-byte packed and 406,429-byte unpacked npm dry run, and diff gates pass. Exact-main Build `29659199516` passed comprehensive Test Suite `88118767035` and all five native archive/offline-install jobs on `3d97516c0a1f93cbce61c2890609582a664faf53`: macOS ARM64 `88118767040`, Windows x64 `88118767049`, Linux ARM64 `88118767053`, macOS x64 `88118767060`, and Linux x64 `88118767063`. Dart Sass currently serializes nonfinite results as CSS calculations; the native path deliberately safe-narrows those boundaries to terminal errors until normalized compatibility diagnostics are implemented. Dart Sass, Less, and Stylus remain quarantined development-only black-box oracles rather than runtime dependencies or fallbacks; production routing, release interlock, version, tag, artifact publication, package publication, deployment, and public capability claims were not changed. | `3d97516` |
| `NSASS-011-MATH-DIVISION` | `VERIFIED` | Added provider-free native Zig `math.div()` through default, exact custom, and unprefixed `sass:math` namespaces in SCSS and indented Sass; no nonexistent global alias was invented. Canonical `$number1`/`$number2` binding evaluates each valid expression exactly once in source order. Numeric division uses the bounded native unit algebra, reduces compatible dimensions, retains compound numerator and denominator units for module inspection, and fails closed if a compound value reaches direct CSS serialization. The legacy nonnumeric path produces one deeply owned unquoted slash string with canonical comma spacing, bracket and `null` behavior, including nested color/list values; it remains a one-item space value under `list.separator()` and `list.length()`. Direct left-color arithmetic follows Sass rejection while color/string and number/right-color legacy values remain representable. Empty unbracketed lists/maps, callable values, bad arity, unknown/duplicate keywords, selected splats, operation or temporary ceilings, division by zero, nonfinite results, and every allocation failure fail closed without partial CSS. Test-first work recorded the expected 94/96 direct evaluator baseline with only the two new suites failing at the former unsupported-member boundary. Exact Dart Sass 1.101.0 development-oracle bytes with `charset: false` are `.a{plain:3px;converted:1;decimal:.25;quotient-unit:"px/s";denominator-unit:"px^-1";custom-case:"Foo/foo";alias:3s;star:2em;named:5px;evaluations:2;strings:foo/bar;booleans:true/false;quoted:"a b"/c;left-color-string:red/foo;right-color:2/red;nested-color:red, x/foo;spaces:a b/c d;lists:a, b/c, d;brackets:[a, b]/[c d];null-left:/b;null-right:a/;legacy-separator:space;legacy-length:1}` for SCSS, `.sass{plain:4px;unit:"s/px";string:a/b;starless:div(6px, 2)}` for indented Sass, and `.plain{value:div(6px, 2)}` for ordinary CSS. Direct evaluator passes 96/96. Focused Debug/ReleaseSafe each pass 21/21 steps and 197/197 tests; exact Zig 0.15.2 full Debug/ReleaseSafe each pass 58/58 steps and 692/692 tests. Checksum-verified official Zig 0.15.2 native Linux ARM64 in a non-root Docker container at `alpine@sha256:28bd5fe8b56d1bd048e5babf5b10710ebe0bae67db86916198a6eec434943f8b`, with read-only source/container/toolchain, executable cache tmpfs mounts, no capabilities, and no network, passes the focused and full suites in both modes. Direct evaluator cross-compiles pass for Windows x64 and Linux ARM64 in Debug and ReleaseSafe; controlled ReleaseSafe production builds verify as PE32+ x86-64, statically linked ELF ARM64, and Mach-O ARM64. Native contract schema v3 adds only `built-in-math-division`; 10/10 plus check retains the recursive repository-owned Zig/`std`/`builtin` closure and target zero package or optional dependencies, external engines, child processes, network, downloads, or non-system language libraries. Workflow, dependency/policy/live-production audits, package/provider/product/capability/format/version/release/publication/consumer checks, compatibility/transform/prefix checks, every benchmark check, package consumers/examples, autodevelop 47/0, repository-wide Zig/Bash/JavaScript/JSON formatting and syntax, documentation integrity 7/7 over 38 Markdown files and 68 fences, isolated website 139/139 plus build, VS Code 13/13 plus package, real Neovim 0.11.7 and 0.12.4 integration, benchmark smoke 3/3, a 28-file/92,750-byte packed and 406,429-byte unpacked npm dry run, and diff gates pass. Exact-main Build `29657889774` passed comprehensive Test Suite `88115381425` and all five native archive/offline-install jobs on `b93e9c74f60f40df6b9df883718619a688397657`: Windows x64 `88115381427`, macOS ARM64 `88115381440`, Linux x64 `88115381447`, Linux ARM64 `88115381448`, and macOS x64 `88115381457`. Reference-engine deprecation warnings remain deferred to the normalized diagnostics slice, while nonfinite arithmetic is deliberately safe-narrowed to fail closed. Dart Sass, Less, and Stylus remain quarantined development-only black-box oracles rather than runtime dependencies or fallbacks; production routing, release interlock, version, tag, artifact publication, package publication, deployment, and public capability claims were not changed. | `b93e9c7` |
| `NSASS-011-MATH-UNARY-NUMERIC` | `VERIFIED` | Added provider-free native Zig `abs()`, `ceil()`, `floor()`, `round()`, and `percentage()` through default, exact custom, and unprefixed `sass:math` namespaces in SCSS and indented Sass, plus their canonical legacy globals. Exact authored unit spelling and case, compound units, canonical negative zero, half-away-from-zero rounding, and percentage conversion are retained; cancelled numeric expressions reduce before transformation. Canonical named `$number` binding evaluates exactly once in source order. Global one-argument `abs()` and `round()` preserve dynamic CSS calculations, and two/three-argument CSS `round()` remains deferred, while module and unprefixed-import calls stay strict. Unit-bearing percentages, nonnumbers, bad arity, missing/case-mismatched namespaces, unknown or duplicate keywords, selected fixed-builtin splats, operation ceilings, nonfinite percentage results, and every allocation failure fail closed without partial CSS. Test-first work recorded the expected 92/94 direct evaluator baseline with only the two new suites failing at the former unsupported-member boundary. Exact Dart Sass 1.101.0 development-oracle bytes with `charset: false` are `.a{abs:1.5px;abs-percent:2%;abs-compound-unit:"px*s/foo";ceil:2px;ceil-negative:-1px;ceil-zero:0px;floor:1s;floor-negative:-2s;floor-zero:0s;round-low:1em;round-half:2em;round-negative-half:-2em;round-zero:0em;round-case:2PX;percentage:12.5%;percentage-negative:-12.5%;percentage-cancelled:1.0416666667%;alias:3ms;star:-3deg;named:3foo;evaluations:1;legacy-abs:2px;legacy-ceil:2px;legacy-floor:1px;legacy-round:2px;legacy-percentage:12.5%}` for SCSS, `.sass{abs:1.5px;ceil:-1s;floor:1em;round:-2deg;percentage:25%;global-abs:2foo;global-ceil:2foo;global-floor:1foo;global-round:2foo;global-percentage:25%}` for indented Sass, and `.plain{abs:abs(var(--size));abs-name:abs(foo);round:round(var(--size),1px);round-name:round(foo)}` for intentionally deferred global CSS calls. Direct evaluator passes 94/94. Focused Debug/ReleaseSafe each pass 21/21 steps and 195/195 tests; exact Zig 0.15.2 full Debug/ReleaseSafe each pass 58/58 steps and 690/690 tests. Checksum-verified official Zig 0.15.2 native Linux ARM64 in a non-root Docker container at `alpine@sha256:28bd5fe8b56d1bd048e5babf5b10710ebe0bae67db86916198a6eec434943f8b`, with read-only source/container/toolchain, executable cache tmpfs mounts, no capabilities, and no network, passes 21/21 and 195/195 in both modes. Direct evaluator cross-compiles pass for Windows x64 and Linux ARM64 in Debug and ReleaseSafe; controlled ReleaseSafe production builds verify as PE32+ x86-64, statically linked ELF ARM64, and Mach-O ARM64. Native contract schema v3 adds only `built-in-math-unary-numeric`; 10/10 plus check retains the recursive repository-owned Zig/`std`/`builtin` closure and target zero package or optional dependencies, external engines, child processes, network, downloads, or non-system language libraries. Workflow, dependency/policy/live-production audits, package/provider/product/capability/format/version/release/publication/consumer checks, compatibility/transform/prefix checks, every benchmark check, package consumers/examples, autodevelop 47/0, repository-wide Zig/Bash/JavaScript/JSON formatting and syntax, documentation integrity 7/7, isolated website 139/139 plus build, VS Code 13/13 plus package, Neovim 4/4 plus real 0.11.7 integration, a 28-file/92,750-byte packed and 406,429-byte unpacked npm dry run, and diff gates pass. Exact-main Build `29656490011` passed comprehensive Test Suite `88111675718` and all five native archive/offline-install jobs on `4a7ad6e5bd1d165ded70276628d25b43faaf361e`: Linux ARM64 `88111675767`, Windows x64 `88111675777`, macOS x64 `88111675779`, Linux x64 `88111675787`, and macOS ARM64 `88111675806`. Reference-engine deprecation warnings and infinity-result compatibility remain intentionally outside this narrowed slice: diagnostics are scheduled separately and nonfinite arithmetic fails closed. Dart Sass, Less, and Stylus remain quarantined development-only black-box oracles rather than runtime dependencies or fallbacks; production routing, release interlock, version, tag, artifact publication, package publication, deployment, and public capability claims were not changed. | `4a7ad6e` |
| `NSASS-011-MATH-UNIT-SERIALIZATION` | `VERIFIED` | Added provider-free native Zig `unit()` through default, exact custom, and unprefixed `sass:math` namespaces in SCSS and indented Sass, plus the canonical legacy global. Results are always quoted strings: dimensionless and fully cancelled numbers become `""`; numerator instances retain exact authored spelling, case, order, and repetition joined by `*`; one denominator emits `/unit`; multiple denominators are parenthesized; and denominator-only values use `^-1`. Canonical named `$number` binding evaluates its expression exactly once. The numeric core independently checks at most 64 unit instances, rejects empty forged units, computes serialized length with checked arithmetic, writes only after exact capacity validation, and reports an insufficient buffer without mutation beyond the admitted slice. The evaluator charges unit-sensitive operations, bounds the checked output against `max_temporary_bytes`, allocates exactly one temporary serialization buffer only when nonempty, deep-owns the quoted result, and frees scratch bytes on every path. Missing or case-mismatched namespaces, bad arity, strings/lists/nulls, unknown or duplicate keywords, selected fixed-builtin splats, temporary overflow, malformed unit state, cancellation, and every allocation failure fail closed without partial CSS. Test-first work recorded the expected 90/92 direct evaluator baseline with only the two new suites failing at the former unsupported-member boundary, while the direct numeric test initially failed to compile because `unitStringLength` did not exist. Exact Dart Sass 1.101.0 development-oracle bytes with `charset: false` are `.a{unitless:"";px:"px";known-case:"PX";custom:"Foo";percent:"%";product:"px*s";reversed:"s*px";quotient:"px/s";denominator-only:"s^-1";powers:"px*px/(s*s)";denominator-many:"(s*foo)^-1";mixed-many:"foo*bar/(baz*qux)";cancelled:"";alias:"deg/s";star:"em/ms";named:"px/s";evaluations:1;legacy:"foo"}` for SCSS and `.sass{unitless:"";single:"px";product:"px*s";quotient:"px/s";denominator:"(s*foo)^-1";case:"PX/in";cancelled:"";global:"foo"}` for indented Sass. Direct evaluator passes 92/92 and direct numeric passes 7/7. Focused Debug/ReleaseSafe each pass 21/21 steps and 193/193 tests; exact Zig 0.15.2 full Debug/ReleaseSafe each pass 58/58 steps and 688/688 tests. Checksum-verified official Zig 0.15.2 native Linux ARM64 in a non-root Docker container at `alpine@sha256:28bd5fe8b56d1bd048e5babf5b10710ebe0bae67db86916198a6eec434943f8b`, with read-only source/container/toolchain, executable cache tmpfs mounts, no capabilities, and no network, passes 21/21 and 193/193 in both modes. Direct evaluator cross-compiles pass for Windows x64 and Linux ARM64 in Debug and ReleaseSafe; controlled ReleaseSafe production builds verify as PE32+ x86-64, statically linked ELF ARM64, and Mach-O ARM64. Native contract schema v3 adds only `built-in-math-unit-serialization`; 10/10 plus check retains the recursive repository-owned Zig/`std`/`builtin` closure and target zero package or optional dependencies, external engines, child processes, network, downloads, or non-system language libraries. Workflow 5/5 plus check, dependency 4/4 plus policy check and live 0/0/0 production audits, preprocessor package 6/6 plus check, Zig package 7/7, Node wrapper 4/4, provider-oracle Sass 28/28, Less 27/27, Stylus 24/24, host 17/17, resolver 12/12, metadata 13/13, product 30/30, capability 7/7 plus check, format matrix 2/2 plus validation, version 7/7 plus check, release metadata 6/6 plus check, npm-publication 7/7, release smoke 6/6, release consumers 17/17, documentation 7/7 plus check over 38 Markdown files and 68 fences, isolated website 139/139 plus production build, VS Code 13/13 plus package, Neovim 4/4 plus real 0.11.7 integration, compatibility/transform, prefix 4/4 plus check, every benchmark corpus/tool/output/mode/statistics/archive/publication test and check, benchmark smoke, package consumers/examples in Debug and ReleaseSafe, autodevelop 47/0, repository-wide Zig formatting, Bash/JavaScript/JSON syntax, a 28-file/92,750-byte packed and 406,429-byte unpacked npm dry run, and diff gates pass. One parallel website run correctly detected transient untracked VS Code build directories created by a simultaneous package gate; the clean isolated rerun passed all 139 tests and built with no source change. Exact-main Build `29655471302` passed comprehensive Test Suite `88108876388` and all five native archive/offline-install jobs on `fd54948b2a1b16252afd8e62b187148c7d70ef25`: Linux x64 `88108876351`, Windows x64 `88108876352`, macOS x64 `88108876366`, Linux ARM64 `88108876378`, and macOS ARM64 `88108876387`. Dart Sass, Less, and Stylus remain quarantined development-only black-box oracles rather than runtime dependencies or fallbacks; production routing, release interlock, version, tag, artifact publication, package publication, deployment, and public capability claims were not changed. | `fd54948` |
| `NSASS-011-MATH-UNIT-PREDICATES` | `VERIFIED` | Added provider-free native Zig loading for `compatible()` and `is-unitless()` through default, exact custom, and unprefixed `sass:math` namespaces in SCSS and indented Sass, plus the canonical legacy globals `comparable()` and `unitless()`. Compatibility treats either dimensionless operand as compatible, recognizes convertible known dimensions only under exact Sass unit spelling, keeps percentages separate from lengths, compares compound dimensions after canonical reduction, and preserves exact case for custom units. Unitless detection recognizes bare and fully cancelled dimensions while retaining percent and other units. Fixed arguments bind by canonical `$number1`, `$number2`, and `$number` names after every valid expression evaluates exactly once in source order. A dedicated numeric-function arithmetic context evaluates deprecated slash division only at the owned math-call boundary, so `1px / 1in` becomes dimensionless there without changing ordinary legacy slash-list or unknown CSS-function behavior. Missing or case-mismatched namespaces, duplicate namespaces, unsupported qualified members, known-but-unimplemented members imported `as *`, bad arity, unknown/duplicate keywords, strings/lists/nulls, selected splats, and argument limits fail closed without partial CSS; rejected unsupported members do not evaluate their arguments. Exhaustive allocation-failure traversal now evaluates and emits both predicates. Test-first work isolated the expected 88/90 evaluator baseline at the former unsupported-module boundary and a direct numeric compile failure before the smallest implementation. Exact Dart Sass 1.101.0 development-oracle bytes with `charset: false` are `.a{compatible:true;incompatible:false;unitless-mixed:true;percentage:true;percentage-length:false;compound:true;custom:true;custom-case:false;known-case:false;alias:true;star:true;is-unitless:true;has-unit:false;cancelled:true;known-case-cancelled:false;percent-unitless:false;named:true;first-evaluated:1in;legacy-comparable:true;legacy-unitless:true}` for SCSS, `.sass{compatible:true;incompatible:false;mixed:true;compound:true;unitless:true;unitful:false;cancelled:true;global:true}` for indented Sass, and `.plain{compatible:compatible(1px, 1in);unitless:is-unitless(1)}` for the intentionally unowned CSS globals without `as *`. Direct evaluator passes 90/90. Focused Debug/ReleaseSafe pass 21/21 steps and 190/190 tests; exact Zig 0.15.2 full Debug/ReleaseSafe each pass 58/58 steps and 685/685 tests. Checksum-verified official Zig 0.15.2 native Linux ARM64 in a non-root Docker container at `alpine@sha256:28bd5fe8b56d1bd048e5babf5b10710ebe0bae67db86916198a6eec434943f8b`, with read-only source/container/toolchain, executable cache tmpfs, no capabilities, and no network, passes 21/21 steps and 190/190 tests in both modes; an environment-only first attempt mounted the local build-runner cache non-executable, and the corrected confinement rerun required no source change. Direct evaluator cross-compiles pass for Windows x64 and Linux ARM64 in Debug and ReleaseSafe; controlled ReleaseSafe production builds verify as PE32+ x86-64 and statically linked ELF ARM64 while the host artifact remains verified Mach-O ARM64. Native contract schema v3 adds only `built-in-math-unit-predicates`; 10/10 plus check retains the recursive repository-owned Zig/`std`/`builtin` closure and target zero package or optional dependencies, external engines, child processes, network, downloads, or non-system language libraries. Workflow 5/5 plus check, dependency 4/4 plus policy check and live 0/0/0 production audits, preprocessor package 6/6 plus check, Zig package 7/7, Node wrapper 4/4, provider-oracle Sass 28/28, Less 27/27, Stylus 24/24, host 17/17, resolver 12/12, metadata 13/13, product 30/30, capability 7/7 plus check, format matrix 2/2 plus validation, version 7/7 plus check, release metadata 6/6 plus check, npm-publication 7/7, release smoke 6/6, release consumers 17/17, documentation 7/7 plus check over 38 Markdown files and 68 fences, website 139/139 plus production build, VS Code 13/13 plus package, Neovim 4/4 plus real 0.11.7 integration, compatibility/transform, prefix 4/4 plus check, every benchmark corpus/tool/output/mode/statistics/archive/publication test and check, benchmark smoke, package consumers/examples in Debug and ReleaseSafe, autodevelop 47/0, repository-wide Zig formatting, Bash/JavaScript/JSON syntax, a 28-file/92,750-byte packed and 406,429-byte unpacked npm dry run, and diff gates pass. A follow-up exact Dart Sass oracle expansion proved that even known Sass numeric units are case-sensitive: `PX` is incompatible with `px`, cannot be added to it, and does not cancel against `in`. The focused red baseline was exactly 188/190 with only the new direct and evaluator assertions failing; changing the known-unit lookup to exact spelling made both safety modes 190/190, both full modes 685/685, both confined Linux ARM64 modes 190/190, all four evaluator cross-compiles, the native contract, the 28-case provider oracle, production audits, formatting, and diff gates green. Exact-main corrective Build `29654312630` passed comprehensive Test Suite `88105829327` and all five native archive/offline-install jobs on `28d0cb9edae5a13004a3b79aa56ab9eec80e32be`: Linux x64 `88105829332`, Linux ARM64 `88105829334`, macOS x64 `88105829335`, Windows x64 `88105829336`, and macOS ARM64 `88105829340`. Dart Sass, Less, and Stylus remain quarantined development-only black-box oracles rather than runtime dependencies or fallbacks; production routing, release interlock, version, tag, artifact publication, package publication, deployment, and public capability claims were not changed. | `28d0cb9` |
| `NSASS-011-LIST-SLASH` | `VERIFIED` | Added provider-free native Zig `list.slash()` for default, exact custom, and unprefixed `sass:list` namespaces in SCSS and indented Sass. The module-only variadic requires at least two positional elements and returns a deeply owned unbracketed slash list without flattening grouped, comma, bracketed, nested slash, map, empty-map, argument-list, scalar, `null`, or literal legacy `a/b` elements; null elements remain queryable while CSS serialization omits them. Ordinary list and argument-list splats expand in source order, while a literal legacy slash value splats as one scalar and therefore still participates in canonical arity checking. Direct and splatted keywords are rejected after the canonical minimum-arity precedence, nonstring map-splat keys fail closed, direct and post-expansion argument ceilings remain enforced, and checked temporary byte accounting bounds the scratch value array. Every copied element consumes the shared operation budget, the value store clones the result graph deeply, scratch storage frees exactly once on both success and error, and exhaustive allocation-failure traversal now emits a slash-list result. No legacy global alias was invented: without `as *`, `slash(a, b)` remains an ordinary CSS function with its authored comma spacing. Test-first work recorded the expected 86/88 direct evaluator baseline with only the two new suites failing at the former unsupported-member boundary. Exact Dart Sass 1.101.0 development-oracle bytes with `charset: false` are `.a{basic:a/b/c;custom:a/b;star:a/b;groups:a b/c d;commas:a,b/c,d;bracket-input:[a b]/[c d];nested:a/b/c/d;nested-length:2;nested-first-length:2;nulls:a/b;all-null-length:2;map-length:2;map-first:1;empty-map-length:2;empty-map-first-length:0;spread:a b/c d/e;legacy:a/b/c;rest-list:a,b/x;forwarded:a b/c d;separator:slash;length:2;first-item:a b;bracketed:false;source-order:a/b;first-evaluated:a}` for SCSS and `.sass{value:a/b/c;groups:a b/c d;spread:a b/c d/e;legacy:a/b/c;separator:slash}` for indented Sass. Direct evaluator passes 88/88. Focused Debug/ReleaseSafe pass 21/21 steps and 187/187 tests; exact Zig 0.15.2 full Debug/ReleaseSafe each pass 58/58 steps and 682/682 tests. Checksum-verified official Zig 0.15.2 native Linux ARM64 in a non-root Docker container at `alpine@sha256:28bd5fe8b56d1bd048e5babf5b10710ebe0bae67db86916198a6eec434943f8b`, with read-only source/container/toolchain, executable cache tmpfs mounts, no capabilities, and no network, passes 21/21 steps and 187/187 tests in both modes; the first environment-only attempt correctly failed because the test harness cache was outside the writable tmpfs, and the corrected confinement rerun required no source change. Direct evaluator cross-compiles pass for Windows x64 and Linux ARM64 in Debug and ReleaseSafe; controlled ReleaseSafe production builds verify as PE32+ x86-64 and ELF ARM64, followed by restoration and verification of the Mach-O ARM64 host artifact. Native contract schema v3 adds only `built-in-list-slash`; 10/10 plus check retains the recursive repository-owned Zig/`std`/`builtin` closure and target zero package or optional dependencies, external engines, child processes, network, downloads, or non-system language libraries. Workflow 5/5 plus check, dependency 4/4 plus policy check and live 0/0/0 production audits, preprocessor package 6/6 plus check, Zig package 7/7, Node wrapper 4/4, provider-oracle Sass 28/28, Less 27/27, Stylus 24/24, host 17/17, resolver 12/12, metadata 13/13, product 30/30, capability 7/7 plus check, format matrix 2/2 plus validation, version 7/7 plus check, release metadata 6/6 plus check, npm-publication 7/7, release smoke 6/6, release consumers 17/17, documentation 7/7 plus check over 38 Markdown files and 68 fences, website 139/139 plus production build, VS Code 13/13 plus package, Neovim 4/4 plus real 0.11.7 integration, compatibility/transform, prefix 4/4 plus check, every benchmark corpus/tool/output/mode/statistics/archive/publication test and check, benchmark smoke, package consumers/examples in Debug and ReleaseSafe, autodevelop 47/0, repository-wide Zig formatting, Bash/JavaScript/JSON syntax, a 28-file/92,750-byte packed and 406,429-byte unpacked npm dry run, and diff gates pass. Exact-main Build `29652293230` passed comprehensive Test Suite `88100561831` and all five native archive/offline-install jobs on `6709c4261d0d491b8b4839dd37ed77702da0f770`: Linux ARM64 `88100561852`, macOS ARM64 `88100561854`, Windows x64 `88100561864`, Linux x64 `88100561909`, and macOS x64 `88100561912`. The canonical providers remain quarantined development-only black-box oracles rather than runtime dependencies or fallbacks; production routing, release interlock, version, tag, artifact publication, package publication, deployment, and public capability claims were not changed. | `6709c42` |
| `NSASS-011-LIST-ZIP` | `VERIFIED` | Added provider-free native Zig `list.zip()` for default, exact custom, and unprefixed `sass:list` namespaces in SCSS and indented Sass, plus legacy global `zip()`. The variadic result always owns an unbracketed comma outer list whose unbracketed space rows stop at the shortest input; zero inputs and any empty-map input produce the canonical empty comma list. Ordinary comma/space/slash and bracketed lists, scalar and `null` singleton views, positional argument lists, and ordered map key/value pair views retain exact Sass item semantics without retaining input separators or brackets. Literal legacy `a/b` remains one scalar item, including when splatted, while ordinary list and argument-list splats expand in source order. Direct or splatted keyword arguments fail closed because the canonical variadic signature has no named parameters; nonstring map-splat keys, direct and expanded argument ceilings, and temporary-storage ceilings also fail without partial CSS. Checked row/item/map-pair arithmetic bounds all scratch bytes, every selected item consumes the shared operation budget, the value store deeply owns the completed graph, and scratch cleanup frees exactly once on both error and success paths. Exhaustive allocation-failure traversal emits a zipped result. Test-first work recorded the expected 84/86 direct evaluator baseline with only the two new ZIP suites failing at the former member boundary. Exact Dart Sass 1.101.0 development-oracle bytes with `charset: false` are `.a{basic:a 1,b 2,c 3;shortest:a 1,b 2;comma-input:a 1,b 2;bracket-input:a 1,b 2;slash-input:a 1,b 2;scalar:a b;single:a,b;map:a 1 c 3,b 2 d 4;empty-map-length:0;zero-length:0;zero-separator:comma;outer-separator:comma;outer-bracketed:false;inner-separator:space;inner-bracketed:false;null-value:a;legacy:a/b c;legacy-length:1;legacy-first:a/b;spread:a c,b d;legacy-splat:a/b;rest-list:a x,b y;forwarded:a c,b d;legacy-zip:a c,b d;source-order:a c,b d;first:a b}` for SCSS and `.sass{value:a 1,b 2;shortest:a 1;spread:a c,b d;legacy:a/b c;global:a c,b d}` for indented Sass. Direct evaluator passes 86/86. Focused Debug/ReleaseSafe pass 21/21 steps and 185/185 tests; exact Zig 0.15.2 full Debug/ReleaseSafe each pass 58/58 steps and 680/680 tests. Checksum-verified official Zig 0.15.2 native Linux ARM64 Docker at `alpine@sha256:28bd5fe8b56d1bd048e5babf5b10710ebe0bae67db86916198a6eec434943f8b`, with read-only source/container/toolchain, executable workspace/global/temp cache tmpfs mounts, no capabilities, and no network, passes 21/21 steps and 184/185 tests plus the existing platform-intentional skip in both modes. Direct evaluator cross-compiles pass for Windows x64 and Linux ARM64 in Debug and ReleaseSafe; controlled ReleaseSafe production builds verify as PE32+ x86-64 and statically linked ELF ARM64 artifacts. Native contract schema v3 adds only `built-in-list-zip`; 10/10 plus check retains the recursive repository-owned Zig/`std`/`builtin` closure and target zero package or optional dependencies, external engines, child processes, network, downloads, or non-system language libraries. Workflow 5/5 plus check, dependency 4/4 plus policy check and live 0/0/0 production audits, preprocessor package 6/6 plus check, Zig package 7/7, Node wrapper 4/4, provider-oracle Sass 28/28, Less 27/27, Stylus 24/24, host 17/17, resolver 12/12, metadata 13/13, product 30/30, capability 7/7 plus check, format matrix 2/2 plus validation, version 7/7 plus check, release metadata 6/6 plus check, npm-publication 7/7, release smoke 6/6, release consumers 17/17, documentation 7/7 plus check over 38 Markdown files and 68 fences, website 139/139 plus production build, VS Code 13/13 plus package, Neovim 4/4 plus real 0.11.7 integration, compatibility/transform, prefix 4/4 plus check, all benchmark corpus/tool/output/mode/statistics/archive/publication tests and checks, package consumers/examples in Debug and ReleaseSafe, autodevelop 47/0, repository-wide Zig formatting, Bash/JavaScript/JSON syntax, a 28-file/92,750-byte packed and 406,429-byte unpacked npm dry run, and diff gates pass. Cross-target and documentation/editor reruns required only restoration of the native macOS artifact and explicit pinned Zig/Neovim tool paths; no source change followed. Exact-main Build `29651028179` passed comprehensive Test Suite `88097287104` and all five native archive/offline-install jobs on `91474e5f91285dc2d6638d18bad600bb3866b6ef`: Windows x64 `88097287115`, Linux ARM64 `88097287126`, macOS ARM64 `88097287128`, macOS x64 `88097287135`, and Linux x64 `88097287151`. The provider packages remain quarantined development-oracle evidence rather than the self-contained target; production routing, release interlock, version, tag, artifact publication, package publication, deployment, and public capability claims were not changed. | `91474e5` |
| `NSASS-011-LIST-JOIN` | `VERIFIED` | Added provider-free native Zig `list.join()` for default, exact custom, and unprefixed `sass:list` namespaces in SCSS and indented Sass, plus legacy global `join()`. The immutable result flattens ordinary and constructed slash lists, ordered map pair views, argument-list positional views, and scalar singleton views while retaining literal legacy `a/b` as one scalar item. Automatic separator selection uses the first known separator, then the second, then space; nonempty maps and argument lists are comma, while scalars, legacy slash values, undecided lists, and empty maps defer. Exact case-sensitive quoted or unquoted `auto`, `space`, `comma`, and `slash` overrides are admitted. Automatic bracket identity comes only from the first input; explicit `auto` defers and every other value follows Sass truthiness. Empty map query/append behavior was corrected to canonical space semantics. Fixed arguments bind before evaluation and every valid expression evaluates exactly once in source order; a regression found during red-to-green work now densely initializes an omitted `$separator` when named `$bracketed` is present, preventing undefined stack memory from entering dispatch. Missing, duplicate, unknown, excessive, invalid separator, selected splat, and temporary-limit calls fail closed without partial CSS. Combined item/map-pair temporary storage uses checked byte accounting, every copied item consumes the operation budget, the value store deeply owns the result, and exhaustive allocation-failure traversal emits a joined list. Test-first work recorded the expected 82/84 direct evaluator baseline with only the two new join suites failing at the former member boundary. Exact Dart Sass 1.101.0 development-oracle bytes with `charset: false` are `.a{space:a b c d;comma:a,b,c,d;first-separator:a b c d;second-separator:a,c,d;bracket-left:[a b c d];original:[a b];bracket-right:a b c d;bracket-defer:[a,b,c];unbracket:a b c d;force-bracket:[a b c d];truthy-bracket:[a b];null-unbracket:a b;quoted-auto:a b c d;force-comma:a,b,c,d;force-space:a b c d;force-slash:a/b/c/d;slash-auto:a/b/c/d/e;slash-length:5;slash-separator:slash;legacy:a/b,c,d;legacy-length:3;legacy-first:a/b;map:x 1,y 2,z 3;empty-map-separator:space;empty-map-append-separator:space;empty-map-join:b,c;rest:a,b,z;empty-length:0;empty-separator:space;legacy-join:a,b,c,d;source-order:a,b,c,d;first:false}` for SCSS and `.sass{value:[a b c d];original:[a b];comma:a,b,c;bracket:[a b c d];legacy:a,b,c,d}` for indented Sass. Direct evaluator passes 84/84. Focused Debug/ReleaseSafe pass 21/21 steps and 183/183 tests; exact Zig 0.15.2 full Debug/ReleaseSafe each pass 58/58 steps and 678/678 tests. Checksum-verified official Zig 0.15.2 native Linux ARM64 Docker at `alpine@sha256:28bd5fe8b56d1bd048e5babf5b10710ebe0bae67db86916198a6eec434943f8b`, with read-only source/container/toolchain, executable workspace/global/temp cache tmpfs mounts, and no network, passes 21/21 steps and 182/183 tests plus the existing platform-intentional skip in both modes. Direct evaluator cross-compiles pass for Windows x64 and Linux ARM64 in Debug and ReleaseSafe; controlled ReleaseSafe production builds verify as PE32+ x86-64 and statically linked ELF ARM64 artifacts. Native contract schema v3 adds only `built-in-list-join`; 10/10 plus check retains the recursive repository-owned Zig/`std`/`builtin` closure and target zero package or optional dependencies, external engines, child processes, network, downloads, or non-system language libraries. Workflow 5/5 plus check, dependency 4/4 plus policy check and live 0/0/0 production audits, preprocessor package 6/6 plus check, Zig package 7/7, Node wrapper 4/4, provider-oracle Sass 28/28, Less 27/27, Stylus 24/24, host 17/17, resolver 12/12, metadata 13/13, product 30/30, capability 7/7 plus check, format matrix 2/2 plus validation, version 7/7 plus check, release metadata 6/6 plus check, documentation 7/7 plus check, website 139/139 plus production build, editor integrations, compatibility/transform and all benchmark policy gates, repository-wide Zig formatting, JavaScript/JSON syntax, a 28-file/92,750-byte packed and 406,429-byte unpacked npm dry run, and diff gates pass. Exact-main Build `29649705013` passed comprehensive Test Suite `88093807532` and all five native archive/offline-install jobs on `5099d97dbbcc68c52d37bd88424bccf88343336c`: Windows x64 `88093807535`, macOS ARM64 `88093807547`, Linux x64 `88093807550`, macOS x64 `88093807555`, and Linux ARM64 `88093807556`. The provider-backed package remains quarantined development-oracle evidence rather than the self-contained target; production routing, release interlock, version, tag, artifact publication, package publication, deployment, and public capability claims were not changed. | `5099d97` |
| `NSASS-011-LIST-TRANSFORMATIONS` | `VERIFIED` | Added provider-free native Zig `list.append()` and `list.set-nth()` for default, exact custom, and unprefixed `sass:list` namespaces in SCSS and indented Sass, plus legacy global `append()` and `set-nth()`. Both transformations return deeply owned immutable lists across ordinary lists, scalar singleton views, ordered map pair views, and argument-list positional views; bracket identity and canonical source separators are retained, negative indexes resolve from the end, map/argument-list views use comma, and scalar/undecided views use space. `append()` admits exact case-sensitive `auto`, `space`, `comma`, and `slash` separator strings in both quoted and unquoted forms. A distinct private `legacy_slash` representation preserves source `a/b` bytes as a scalar singleton while function-constructed slash lists remain true queryable multi-item lists; bracketed legacy slash values unwrap only their scalar item for `nth()`, `index()`, append, and replacement. Fixed arguments bind before evaluation, then every valid expression evaluates exactly once in source order. Missing, duplicate, unknown, and excessive arguments, invalid separator names/types, invalid index types/zero/bounds, and selected splats fail closed without partial CSS. Every copied item consumes the shared operation budget; separately byte-accounted temporary item and map-pair arrays enforce `max_temporary_bytes`; the value store owns the finished graph deeply; exhaustive allocation-failure traversal executes both transformations and emits their results. Test-first work recorded the expected 80/82 direct evaluator baseline with only the two new transformation suites failing at the former member boundary. Exact Dart Sass 1.101.0 development-oracle bytes with `charset: false` are `.a{scalar-auto:solo next;space-auto:a b c;comma-auto:a,b,c;explicit-auto:a,b,c;quoted-comma:a,b,c;comma-override:a,b,c;space-override:a b c;slash:a/b/c;slash-length:3;slash-separator:slash;slash-index:2;bracketed:[a b c];original:[a b];map:a 1,b 2,c;empty:x;rest:a,b,z;legacy-append:a,b,c;set-space:a x c;set-comma:x,b,c;set-bracket:[a b x];set-map:a 1,x;set-scalar:x;set-slash:a/x/c;set-rest:a,x,c;legacy-set:a x c;legacy-auto:a/b c;legacy-auto-length:2;legacy-slash-length:2;legacy-set-single:x;legacy-bracket-append:[a/b c];legacy-bracket-length:1;legacy-bracket-nth:a/b;legacy-bracket-index:1;legacy-bracket-set:[x];source-append:a b c;append-first:c;source-set:a x c;set-first:x}` for SCSS and `.sass{append:[a b c];set:a b x;legacy-append:a,b,c;legacy-set:a x c;slash:a/b/c;original:[a b]}` for indented Sass; selected invalid arity, type, name, keyword, and index boundaries are oracle-confirmed rejections. Direct evaluator passes 82/82. Focused Debug/ReleaseSafe pass 21/21 steps and 181/181 tests; exact Zig 0.15.2 full Debug/ReleaseSafe each pass 58/58 steps and 676/676 tests. Checksum-verified official Zig 0.15.2 native Linux ARM64 Docker at `alpine@sha256:28bd5fe8b56d1bd048e5babf5b10710ebe0bae67db86916198a6eec434943f8b`, with read-only source/container/toolchain, executable workspace/global/temp cache tmpfs mounts, and no network, passes 21/21 steps and 180/181 tests plus the existing platform-intentional skip in both modes. Direct evaluator cross-compiles pass for Windows x64 and Linux ARM64 in Debug and ReleaseSafe; controlled ReleaseSafe production builds verify as PE32+ x86-64 and statically linked ELF ARM64 artifacts. Native contract schema v3 adds only `built-in-list-transformations`; 10/10 plus check retains the recursive repository-owned Zig/`std`/`builtin` closure and target zero package or optional dependencies, external engines, child processes, network, downloads, or non-system language libraries. Workflow 5/5 plus check, dependency 4/4 plus policy check and live 0/0/0 production audits, preprocessor package 6/6 plus check, Zig package 7/7, Node wrapper 4/4, provider-oracle Sass 28/28, Less 27/27, Stylus 24/24, host 17/17, resolver 12/12, metadata 13/13, product 30/30, capability 7/7 plus check, format matrix 2/2 plus validation, version 7/7 plus check, release metadata 6/6 plus check, documentation 7/7 plus check, website 139/139 plus production build, repository-wide Zig formatting, JavaScript/JSON syntax, a 28-file/92,750-byte packed and 406,429-byte unpacked npm dry run, and diff gates pass. Exact-main Build `29648443862` passed comprehensive Test Suite `88090550804` and all five native archive/offline-install jobs on `16a40e9948c38fa52b3077da45ea9912d01ec696`: Windows x64 `88090550816`, Linux x64 `88090550818`, Linux ARM64 `88090550827`, macOS ARM64 `88090550831`, and macOS x64 `88090550838`. The provider-backed package remains quarantined development-oracle evidence rather than the self-contained target; production routing, release interlock, version, tag, artifact, package publication, deployment, and public capability claims were not changed. | `16a40e9` |
| `NSASS-011-LIST-QUERIES` | `VERIFIED` | Added provider-free native Zig `list.index()`, `list.separator()`, and `list.is-bracketed()` for default, exact custom, and unprefixed `sass:list` namespaces in SCSS and indented Sass, plus legacy global `index()`, `list-separator()`, and `is-bracketed()`. Index returns the first one-based Sass-equal match or `null` across lists, map pair views, argument-list positional items, and scalar singleton views; each scan step consumes the shared operation budget. Separator returns canonical `comma` for maps/argument lists and comma lists, otherwise `space`; bracket inspection retains only actual square-bracket identity. Literal legacy `/` expressions remain byte-preserved but now behave canonically as scalar singletons for `length()`, `nth()`, `index()`, and `separator()` instead of exposing the evaluator's internal preservation representation. Calls evaluate once in source order and bind fixed names before dispatch. Missing/duplicate/unknown arguments, invalid arity, and selected splats fail closed with no partial CSS. Allocation-failure traversal evaluates every query and threads their results through emitted declarations. Test-first work recorded the expected 78/80 direct evaluator baseline with only the two new query suites failing at the former member boundary. Exact Dart Sass 1.101.0 development-oracle bytes with `charset: false` are `.a{index:1;missing:true;map-index:2;separator-space:space;separator-comma:comma;separator-undecided:space;bracketed:true;unbracketed:false;global-index:2;global-separator:comma;global-bracketed:true;slash-length:1;slash-separator:space;slash-index:1;slash-nth:a/b;source-order:2;first:b}` for SCSS and `.sass{index:2;missing:true;separator:space;bracketed:true}` for indented Sass. Direct evaluator passes 80/80. Focused Debug/ReleaseSafe pass 21/21 steps and 179/179 tests; exact Zig 0.15.2 full Debug/ReleaseSafe each pass 58/58 steps and 674/674 tests. Checksum-verified official Zig 0.15.2 native Linux ARM64 Docker at the pinned Alpine digest, with read-only source/container/toolchain, executable isolated cache tmpfs mounts, and no network, passes 21/21 steps and 178/179 tests plus the existing platform-intentional skip in both modes. Direct evaluator cross-compiles pass for Windows x64 and Linux ARM64 in Debug and ReleaseSafe; controlled ReleaseSafe production builds pass as PE32+ x86-64 and statically linked ELF ARM64 artifacts. Native contract schema v3 adds only `built-in-list-queries`; 10/10 plus check retains the recursive repository-owned Zig/`std`/`builtin` closure and target zero package or optional dependencies, external engines, child processes, network, downloads, or non-system language libraries. Workflow 5/5 plus check, dependency 4/4 plus policy check and live 0/0/0 production audits, preprocessor package 6/6 plus check, Zig package 7/7, Node wrapper 4/4, provider-oracle Sass 28/28, Less 27/27, Stylus 24/24, host 17/17, resolver 12/12, metadata 13/13, product 30/30, capability 7/7 plus check, format matrix 2/2 plus validation, version/release metadata, documentation 7/7 plus check, website 139/139 plus production build in an isolated rerun, repository formatting, JavaScript/JSON syntax, a 28-file/406,429-byte npm dry run, and diff gates pass. Exact-main Build `29646847717` passed comprehensive Test Suite `88086429253` and all five native archive/offline-install jobs on `12db3e53c67b867cf5ae62a82ab38227a1a757dc`: macOS ARM64 `88086429283`, Windows x64 `88086429293`, Linux ARM64 `88086429294`, macOS x64 `88086429305`, and Linux x64 `88086429334`. The provider-backed candidate remains quarantined migration evidence rather than the self-contained target; production routing, release interlock, version, tag, artifact, package publication, deployment, and public capability claims were not changed. | `12db3e5` |
| `NSASS-011-LIST-MODULE-ALIASES` | `VERIFIED` | Added provider-free native Zig loading for `@use "sass:list"` with default, exact custom, and unprefixed namespaces in SCSS and indented Sass. The admitted module surface aliases `nth()` and `length()` to the existing bounded typed-collection implementation across lists, maps, and scalar singleton views; every other list member remains an explicit closed boundary, and the next canonical transformation `append()` rejects before its arguments are evaluated. Missing, case-mismatched, and duplicate namespaces, unknown members, invalid arity/index type/index bounds, and selected splats fail closed with no partial CSS. The shared fixed-builtin binder now evaluates every valid argument exactly once in source order before reordering values by canonical parameter name, retaining fixed color/filter dispatch and all previous keyword behavior. Allocation-failure traversal loads the list module and uses both aliases in emitted results. Test-first work recorded the expected 76/78 direct evaluator baseline with only the two new positive/negative suites failing at the former module boundary. Exact Dart Sass 1.101.0 development-oracle bytes with `charset: false` are `.a{nth:c;length:3;scalar:1;map-nth:b 2;source-order:b;first:2}` for SCSS and `.sass{nth:b;length:3;scalar:1}` for indented Sass, covering default/custom/star namespaces, negative indexes, maps, scalar singleton length, named binding, and source-order side effects. Direct evaluator passes 78/78. Focused Debug/ReleaseSafe pass 21/21 steps and 177/177 tests; exact Zig 0.15.2 full Debug/ReleaseSafe each pass 58/58 steps and 672/672 tests. Checksum-verified official Zig 0.15.2 native Linux ARM64 Docker at `alpine@sha256:28bd5fe8b56d1bd048e5babf5b10710ebe0bae67db86916198a6eec434943f8b`, with read-only source/container/toolchain, executable isolated cache tmpfs mounts, and no network, passes 21/21 steps and 176/177 tests plus the existing platform-intentional skip in both modes. Direct evaluator cross-compiles pass for Windows x64 and Linux ARM64 in Debug and ReleaseSafe; controlled ReleaseSafe production builds pass as PE32+ x86-64 and statically linked ELF ARM64 artifacts. Native contract schema v3 adds only `built-in-list-module-aliases`; 10/10 plus check retains the recursive repository-owned Zig/`std`/`builtin` closure and target zero package or optional dependencies, external engines, child processes, network, downloads, or non-system language libraries. Workflow 5/5 plus check, dependency 4/4 plus policy check and live 0/0/0 production audits, preprocessor package 6/6 plus check, Zig package 7/7, Node wrapper 4/4, provider-oracle Sass 28/28, Less 27/27, Stylus 24/24, host 17/17, resolver 12/12, metadata 13/13, product 30/30, capability 7/7 plus check, format matrix 2/2 plus validation, version/release metadata, documentation 7/7 plus check, website 139/139 plus production build, VS Code and Neovim integration, consumer examples, independent CSS/transform validation, all benchmark policy/smoke gates without archival, repository formatting, JavaScript/JSON syntax, a 28-file/406,429-byte npm dry run, and diff gates pass. Exact-main Build `29645815180` passed comprehensive Test Suite `88083763347` and all five native archive/offline-install jobs on `e10a6130f8c7e49275c5c2704f437b13ed6f72c5`: macOS x64 `88083763381`, Linux ARM64 `88083763386`, Linux x64 `88083763391`, Windows x64 `88083763403`, and macOS ARM64 `88083763410`. The provider-backed candidate remains quarantined migration evidence rather than the self-contained target; production routing, release interlock, version, tag, artifact, package publication, deployment, and public capability claims were not changed. | `e10a613` |
| `NSASS-011-STRING-MODULE-ALIASES` | `VERIFIED` | Added provider-free native Zig loading for `@use "sass:string"` with default, exact custom, and unprefixed namespaces in SCSS and indented Sass. The admitted module surface aliases `quote()`, `unquote()`, `length()`, `index()`, `slice()`, `insert()`, `to-upper-case()`, and `to-lower-case()` to the already bounded native string implementation; `unique-id()` and every other member remain explicit unsupported boundaries and reject before argument evaluation. Missing, case-mismatched, and duplicate namespaces, invalid type/arity, and selected splats fail closed with no partial CSS. Fixed string calls now evaluate valid named arguments exactly once in source order before reordering them for canonical parameter binding. Allocation-failure traversal loads the module and exercises every alias. Test-first work recorded the expected 74/76 direct evaluator baseline with only the two new positive/negative suites failing at the former module boundary. Exact Dart Sass 1.101.0 development-oracle bytes with `charset: false` are `.a{quote:"foo";unquote:foo bar;length:2;index:2;slice:"💚";insert:"aXb";upper:"ABC-é";lower:"abc-É";source-order:2;first:b}` for SCSS and `.sass{length:2;index:2;slice:"lo";insert:"aXb";upper:"ABC-é"}` for indented Sass, covering quoted/unquoted behavior, Unicode code-point length/index/slice/insert, ASCII case conversion, custom/star namespaces, star collision resolution, named binding, and source-order side effects. Focused Debug/ReleaseSafe pass 21/21 steps and 175/175 tests; exact Zig 0.15.2 full Debug/ReleaseSafe each pass 58/58 steps and 670/670 tests. Checksum-verified official Zig 0.15.2 native Linux ARM64 Docker at the pinned Alpine digest, with read-only source/container/toolchain, executable isolated cache tmpfs mounts, and no network, passes 21/21 steps and 174/175 tests plus the existing platform-intentional skip in both modes. Direct evaluator cross-compiles pass for Windows x64 and Linux ARM64 in Debug and ReleaseSafe; controlled ReleaseSafe production builds pass for both targets. Native contract schema v3 adds only `built-in-string-module-aliases`; 10/10 plus check retains the recursive repository-owned Zig/`std`/`builtin` closure and target zero package or optional dependencies, external engines, child processes, network, downloads, or non-system language libraries. Workflow 5/5 plus check, dependency 4/4 plus policy check and live 0/0/0 production audits, preprocessor package 6/6 plus check, Zig package 7/7, Node wrapper 4/4, provider-oracle Sass 28/28, Less 27/27, Stylus 24/24, host 17/17, resolver 12/12, metadata 13/13, product 30/30, capability 7/7 plus check, format matrix 2/2 plus validation, version/release metadata, documentation 7/7 plus check, website 139/139 plus production build, repository formatting, JavaScript/JSON syntax, a 28-file/406,429-byte npm dry run, and diff gates pass. Exact-main Build `29644727462` passed comprehensive Test Suite `88080959605` and all five native archive/offline-install jobs on `5efd88e2a87efef679af333e3095fdcc7462ccf6`: macOS ARM64 `88080959615`, Linux ARM64 `88080959617`, Linux x64 `88080959622`, Windows x64 `88080959623`, and macOS x64 `88080959626`. The provider-backed candidate remains quarantined migration evidence rather than the self-contained target; production routing, release interlock, version, tag, artifact, package publication, deployment, and public capability claims were not changed. | `5efd88e` |
| `NSASS-011-NESTED-DEEP-MAP-MUTATIONS` | `VERIFIED` | Added provider-free native Zig nested `map.merge()`/legacy `map-merge()` and `map.set()` plus recursive `map.deep-merge()` and `map.deep-remove()` for default, exact custom, and unprefixed `sass:map` namespaces in SCSS and indented Sass. Nested merge/set create missing intermediate maps and replace scalar intermediates exactly where canonical Sass does; deep merge recurses only when both colliding values are map-like, otherwise replaces from the right; deep remove leaves missing or scalar paths unchanged and retains an emptied parent map. Every operation reconstructs a deeply owned immutable result, preserves existing key positions, appends new keys in source order at each level, and leaves the original map unchanged. Positional calls evaluate exactly once in source order; bounded fixed named calls bind before source-ordered evaluation. Root/final map types, arity, duplicate/unknown/missing arguments, selected splats, recursive evaluation depth, shared operation budget, value ownership, and a separately byte-accounted temporary arena all fail closed with no partial CSS. Exhaustive allocation injection now traverses every new mutation and output query. Test-first work recorded the expected 72/74 direct evaluator baseline with only the two new positive/negative suites failing against the former shallow boundary. Exact Dart Sass 1.101.0 development-oracle bytes match both syntaxes for existing/missing/scalar paths, recursive replacement, root/child ordering, empty parents, unchanged misses, immutability, named source-order side effects, types, arity, and selected unsupported splats. Focused Debug/ReleaseSafe pass 21/21 steps and 173/173 tests; exact Zig 0.15.2 full Debug/ReleaseSafe each pass 58/58 steps and 668/668 tests. Checksum-verified official Zig 0.15.2 native Linux ARM64 Docker at the pinned Alpine digest, with read-only source/container/toolchain, executable isolated cache tmpfs mounts, and no network, passes 21/21 steps and 172/173 tests plus the existing platform-intentional skip in both modes. Direct evaluator cross-compiles pass for Windows x64 and Linux ARM64 in Debug and ReleaseSafe; controlled ReleaseSafe production builds pass for both targets. Native contract schema v3 adds only `nested-deep-map-mutations`; 10/10 plus check retains the recursive repository-owned Zig/`std`/`builtin` closure and target zero package or optional dependencies, external engines, child processes, network, downloads, or non-system language libraries. Workflow 5/5 plus check, dependency 4/4 plus policy check and live 0/0/0 production audits, preprocessor package 6/6 plus check, Zig package 7/7, Node wrapper 4/4, provider-oracle Sass 28/28, Less 27/27, Stylus 24/24, host 17/17, resolver 12/12, metadata 13/13, product 30/30, capability 7/7 plus check, format matrix 2/2 plus validation, version/release metadata, documentation 7/7 plus check, website 139/139 plus production build, repository formatting, JavaScript/JSON syntax, a 28-file/406,429-byte npm dry run, and diff gates pass. Exact-main Build `29643971889` passed comprehensive Test Suite `88078941577` and all five native archive/offline-install jobs on `0da62b8e9547b939af58f39d5f4b2103e8d9abee`: macOS ARM64 `88078941579`, Windows x64 `88078941582`, Linux ARM64 `88078941583`, Linux x64 `88078941585`, and macOS x64 `88078941828`. The current provider-backed candidate package remains quarantined migration evidence rather than the self-contained target; production routing, release interlock, version, tag, artifact, package publication, deployment, and public capability claims were not changed. | `0da62b8` |
| `NSASS-011-SHALLOW-MAP-MUTATIONS` | `VERIFIED` | Added provider-free native Zig `merge()`, `remove()`, and top-level `set()` members for `@use "sass:map"` with default, exact custom, and unprefixed namespaces, plus legacy global `map-merge()` and `map-remove()`. All operations return deeply owned immutable maps. Shallow merge retains the first map's key order, replaces equal-key values in place, and appends new second-map keys in source order; remove accepts zero or more top-level keys; set replaces in place or appends. Typed and canonical empty maps are admitted. Fixed and variadic argument paths bind before evaluation, reject selected splats, and evaluate every valid argument exactly once in source order even when named arguments are reordered. Map comparisons and copies consume the shared operation budget; allocation failure and every invalid path retain no partial CSS. Root non-map inputs, missing/unknown/duplicate arguments, invalid arity, and unsupported rest-keyword forms fail closed. Canonical nested `map.merge()`/`map.set()` forms and deep-map members report the explicit `UnsupportedFeature` boundary for the next package rather than silently applying shallow behavior. Test-first work recorded the expected 70/72 evaluator red baseline with only the two new mutation tests failing. Exact Dart Sass 1.101.0 development-oracle bytes matched default/custom/star and legacy calls, both syntaxes, replacement/append/removal order, zero-key removal, named binding and source-order side effects, empty maps, equality, immutability, and selected errors. Focused Debug/ReleaseSafe pass 21/21 steps and 171/171 tests; exact Zig 0.15.2 full Debug/ReleaseSafe each pass 58/58 steps and 666/666 tests. Checksum-verified official Zig 0.15.2 native Linux ARM64 Docker at the pinned Alpine digest, with read-only source/container/toolchain, isolated writable cache tmpfs mounts, and no network, passes 21/21 steps and 170/171 tests plus the existing platform-intentional skip in both modes. Direct evaluator cross-compiles pass for Windows x64 and Linux ARM64 in Debug and ReleaseSafe; controlled ReleaseSafe production builds pass for both targets. Native contract schema v3 adds only `shallow-map-mutations`; 10/10 plus check retains the recursive repository-owned Zig/`std`/`builtin` closure and target zero package or optional dependencies, external engines, child processes, network, downloads, or non-system language libraries. Workflow 5/5 plus check, dependency 4/4 plus policy check and live 0/0/0 production audits, preprocessor package 6/6 plus check, Zig package 7/7, Node wrapper 4/4, provider-oracle Sass 28/28, Less 27/27, Stylus 24/24, host 17/17, resolver 12/12, metadata 13/13, product 30/30, capability 7/7 plus check, format matrix 2/2 plus validation, version/release metadata, documentation 7/7 plus check, website 139/139 plus production build, repository formatting, JavaScript/JSON syntax, and diff gates pass. Exact-main Build `29640416248` passed comprehensive Test Suite `88069892785` and all five native archive/offline-install jobs on `2fcee347303adf87ce74ad92eeb7169d96e99b87`: macOS x64 `88069892773`, macOS ARM64 `88069892776`, Windows x64 `88069892777`, Linux ARM64 `88069892792`, and Linux x64 `88069892796`. The canonical providers remain quarantined as development-only black-box oracles; production routing, release interlock, version, tag, artifact, package publication, deployment, and public capability claims were not changed. | `2fcee34` |
| `NSASS-011-MAP-QUERIES` | `VERIFIED` | Added provider-free native Zig loading for the admitted `get()`, `has-key()`, `keys()`, and `values()` members of `@use "sass:map"` with default, exact custom, and unprefixed namespaces, plus legacy global `map-get()`, `map-has-key()`, `map-keys()`, and `map-values()`. Variadic `get` and `has-key` retain fixed-name binding for `$map`/`$key`; `keys` and `values` accept exact `$map` binding. Typed maps preserve insertion order and Sass value equality. Every zero-item list, including bracketed lists and positional-empty argument lists, is viewed as the canonical empty map without exposing argument-list keywords. Root non-map inputs are type errors; a missing or non-map nested path returns `null` for `get` and `false` for `has-key`. Key and value lists are comma-separated even when empty or singleton. Lookups and list extraction consume the shared operation budget, deep ownership remains bounded by the value store, and allocation/resource failures retain no partial CSS. Selected built-in splats, mutation/deep-map members, and every other `sass:map` member remain closed. Missing modules, unknown members, duplicate namespaces, invalid type/arity, and rejected splats fail before producing CSS; unknown members reject before argument evaluation. Test-first work recorded the expected 167/169 focused red baseline with only the two new query tests stopping at the existing `UnsupportedFeature` module boundary. Exact Dart Sass 1.101.0 development-oracle bytes then matched default/custom/star use, both syntaxes, nested retrieval/presence, named arguments, legacy globals, source-ordered keys/values, empty `()`/`[]`/argument-list behavior, nested non-map/missing results, and declaration omission for `null`. Focused Debug/ReleaseSafe pass 21/21 steps and 169/169 tests; exact Zig 0.15.2 full Debug/ReleaseSafe each pass 58/58 steps and 664/664 tests. Checksum-verified official Zig 0.15.2 native Linux ARM64 Docker at the pinned Alpine digest, with read-only source/container, isolated writable caches, and no network, passes 21/21 steps and 168/169 tests plus the existing platform-intentional skip in both modes. Direct native evaluator cross-compiles pass for Windows x64 and Linux ARM64 in Debug and ReleaseSafe; controlled ReleaseSafe production builds pass for both targets. Native contract schema v3 adds only `built-in-map-queries`; 10/10 plus check retains the recursive repository-owned Zig/`std`/`builtin` closure and target zero package or optional dependencies, external engines, child processes, network, downloads, or non-system language libraries. Workflow 5/5 plus check, dependency 4/4 plus policy check and live 0/0/0 production audits, preprocessor package 6/6 plus check, Zig package 7/7, Node wrapper 4/4, provider-oracle Sass 28/28, Less 27/27, Stylus 24/24, host 17/17, resolver 12/12, metadata 13/13, product 30/30, capability 7/7 plus check, format matrix 2/2 plus validation, version/release metadata, documentation 7/7 plus check, website 139/139 plus production build, repository formatting, JavaScript/JSON syntax, and diff gates pass. Exact-main Build `29639454065` passed comprehensive Test Suite `88067393270` and all five native archive/offline-install jobs on `a2c2e083ec59d02e46e23a504b5cb216dad9a9fc`: macOS x64 `88067393279`, Windows x64 `88067393283`, Linux x64 `88067393284`, macOS ARM64 `88067393292`, and Linux ARM64 `88067393313`. The canonical providers remain quarantined as development-only black-box oracles; production routing, release interlock, version, tag, artifact, package publication, deployment, and public capability claims were not changed. | `a2c2e08` |
| `NSASS-011-META-KEYWORDS` | `VERIFIED` | Added provider-free native Zig loading for the admitted `keywords()` member of `@use "sass:meta"` with default, exact custom, and unprefixed namespaces in SCSS and indented Sass. `meta.keywords()` accepts only the owned typed argument-list value, marks its shared keyword state as observed, excludes positional items, and returns a deeply owned insertion-ordered map. Explicit keyword names normalize underscores to hyphens while map-splat names remain exact. The behavior is shared by functions, mixins, forwarded rest arguments, and parameterized content blocks, including empty and positional-only argument lists. Call expressions still evaluate exactly once in source order, but explicit keywords are collected before keyword splats as Sass requires; splats then merge in their own order, later splats override matching values while preserving the first key position, and exact map spelling remains distinct from normalized explicit spelling. The same merge semantics now feed fixed callables instead of falsely reporting dynamic duplicates. Missing modules, unknown members, unloaded or duplicate namespaces, wrong types, bad arity, and unimplemented `sass:meta` members fail closed; an unknown member rejects before evaluating its arguments. Normalized temporary name bytes and argument/module/value/transaction limits remain bounded, and exhaustive allocation failure produces no partial CSS. Test-first work recorded the expected 165/167 focused red baseline with only the two new module tests stopping at the existing `UnsupportedFeature` boundary. Exact Dart Sass 1.101.0 development-oracle bytes then matched default/custom/star namespace use, direct and forwarded arbitrary keywords, exact versus normalized key spelling, explicit/splat and multi-splat override order, fixed-call binding, empty/positional-only inspection, function/mixin/content use, and both syntaxes. Focused Debug/ReleaseSafe pass 21/21 steps and 167/167 tests; exact Zig 0.15.2 full Debug/ReleaseSafe each pass 58/58 steps and 662/662 tests. Checksum-verified official Zig 0.15.2 native Linux ARM64 Docker at `alpine@sha256:28bd5fe8b56d1bd048e5babf5b10710ebe0bae67db86916198a6eec434943f8b`, with read-only source/container, isolated writable caches, and no network, passes 21/21 steps and 166/167 tests plus the existing platform-intentional skip in both modes. Direct native evaluator cross-compiles pass for Windows x64 and Linux ARM64 in Debug and ReleaseSafe; controlled ReleaseSafe production builds pass for both targets. Native contract schema v3 adds only `built-in-meta-keywords`; 10/10 plus check retains the recursive repository-owned Zig/`std`/`builtin` closure and target zero package or optional dependencies, external engines, child processes, network, downloads, or non-system language libraries. Workflow 5/5 plus check, dependency 4/4 plus policy check and live 0/0/0 production audits, preprocessor package 6/6 plus check, Zig package 7/7, Node wrapper 4/4, provider-oracle Sass 28/28, Less 27/27, Stylus 24/24, host 17/17, resolver 12/12, metadata 13/13, product 30/30, capability 7/7 plus check, format matrix 2/2 plus validation, version/release metadata, documentation 7/7 plus check, website 139/139 plus production build, repository formatting, JavaScript/JSON syntax, npm dry-run inventory, and diff gates pass. Exact-main Build `29638739842` passed comprehensive Test Suite `88065503406` and all five native archive/offline-install jobs on `4551181575d5ea3672cc6855a8f901984e174a71`: macOS x64 `88065503392`, macOS ARM64 `88065503395`, Linux ARM64 `88065503415`, Linux x64 `88065503417`, and Windows x64 `88065503428`. The canonical providers remain quarantined as development-only black-box oracles; production routing, release interlock, version, tag, artifact, package publication, deployment, and public capability claims were not changed. | `4551181` |
| `NSASS-011-CALLABLE-ARGUMENT-LISTS` | `VERIFIED` | Added provider-free native Zig callable rest parameters and parameterized content blocks for SCSS and indented Sass. One shared signature parser admits only a final non-defaulted rest parameter; calls evaluate arguments once in source order and expand bounded scalar, list, map, and argument-list splats. Explicit keywords retain Sass underscore/hyphen identity while map-splat names remain exact, string-only map keys are enforced, duplicates and post-expansion count overflow fail closed, and fixed native built-ins continue rejecting splats they do not yet own. Rest values are deeply owned typed argument lists with positional items, ordered keyword entries, spelling provenance, and shared usage state. `@each`, destructuring, `nth()`, `length()`, list equality, forwarding, and CSS emission consume their positional side. Arbitrary keywords are rejected lazily unless the argument list is forwarded, preserving the exact future observation point for `meta.keywords()`. `@include ... using (...)` supports required/default/rest parameters; `@content(...)` evaluates arguments in mixin scope while the content body retains the include caller's lexical scope, including nested synchronous forwarding. Test-first work recorded the expected missing-`.splat` parser compilation failures and 155/158 evaluator baseline with three intended unsupported/old-error failures. The pinned Dart Sass 1.101.0 development-only oracle then matched byte-for-byte on direct/rest/forwarded function and mixin calls, scalar/list/map/argument-list expansion, exact map-key spelling, unused-keyword rejection, content defaults/rest/keywords, lexical scope, forwarding, both syntaxes, and all selected negative boundaries. Focused Debug/ReleaseSafe pass 21/21 steps and 165/165 tests. Exact Zig 0.15.2 full Debug/ReleaseSafe each pass 58/58 steps and 660/660 tests. Checksum-verified official Zig 0.15.2 native Linux ARM64 Docker with read-only source, read-only container, isolated writable caches, and no network passes 21/21 steps and 164/165 tests plus the existing platform-intentional skip in both modes. Direct native Sass evaluator cross-compiles pass for Windows x64 and Linux ARM64 in Debug and ReleaseSafe; ReleaseSafe production builds pass for both targets. Native contract schema v3 adds `callable-rest-splat-content-parameters`; 10/10 plus check retains the recursive repository-owned Zig/`std`/`builtin` closure and target zero package or optional dependencies, external engines, child processes, network, downloads, or non-system language libraries. Workflow, dependency policy and live 0/0/0 production audits, package, host/resolver/metadata/product, all three provider-oracle suites, capability/format/version/release metadata, website, documentation, formatting, syntax, JSON, and diff gates pass. Exact-main Build `29637776957` passed comprehensive Test Suite `88063012404` and all five native archive/offline-install jobs on `4e8c08c45cc3a473b00433731fd8fbf8a06e419c`: Linux x64 `88063012405`, macOS x64 `88063012411`, Linux ARM64 `88063012420`, macOS ARM64 `88063012426`, and Windows x64 `88063012436`. The reference providers remain quarantined as development-only oracle evidence; production routing, release interlock, version, tag, artifact, package publication, deployment, and public capability claims were not changed. | `4e8c08c` |
| `NSASS-011-COLOR-MODULE-ALIASES` | `VERIFIED` | Added provider-free native Zig loading for the admitted transform portion of `@use "sass:color"` in SCSS and indented Sass. Default `color`, exact case-sensitive custom namespaces, repeated loading under distinct aliases, and `as *` bind only `adjust()`, `change()`, and `scale()` to the already verified allocation-free color-transform core; `@use` emits no CSS and may follow top-level variable declarations. A bounded source-backed module registry owns a separate validated module limit. Module directives are restricted to the stylesheet root and must precede rules; duplicate namespaces, unloaded or case-mismatched namespaces, unknown members, malformed aliases, late/nested uses, unsupported modules, and `@forward` fail closed without evaluating rejected call arguments or exposing partial CSS. The capability is deliberately named `built-in-color-transform-aliases` and does not claim the rest of `sass:color`, general modules, imports, or forwards. Test-first work produced the expected 160/162 red focused baseline: module loading stopped at `UnsupportedFeature`, while an unresolved qualified call reached the pre-existing undefined-variable path. Exact Dart Sass 1.101.0 development-oracle bytes then matched the combined default/custom/star SCSS fixture and custom-namespace indented-Sass fixture. Focused Debug/ReleaseSafe pass 21/21 steps and 162/162 tests, including module-limit and exhaustive evaluator allocation-failure coverage; checksum-verified official Zig 0.15.2 native Linux ARM64 Docker with read-only source, isolated writable caches, and no network passes 21/21 steps and 161/162 tests plus the existing platform-intentional skip in both modes. Exact Zig 0.15.2 full Debug/ReleaseSafe each pass 58/58 steps and 657/657 tests. Native contract schema v3 adds `built-in-color-transform-aliases`; 10/10 plus check retains the recursive repository-owned Zig/`std`/`builtin` import closure and target zero package or optional dependencies, external engines, child processes, network, downloads, or non-system language libraries. Workflow 5/5 plus check, dependency 4/4 plus policy check and live 0/0/0 production audits, Zig package 7/7, preprocessor package 6/6 plus check, documentation 7/7 plus check over 38 Markdown files/68 fences, website 139/139 plus production build, provider-oracle Sass 28/28, Less 27/27, Stylus 24/24, host 17/17, resolver 12/12, metadata 13/13, product 30/30, capability 7/7 plus check, format matrix 2/2 plus validation, release version/metadata, repository formatting, JavaScript/JSON syntax, and diff gates pass. Windows x64 and Linux ARM64 production builds and direct native Sass evaluator cross-compiles pass in Debug and ReleaseSafe. Exact-main Build `29636586523` passed comprehensive Test Suite `88059892377` and all five native archive/offline-install jobs on `ba64d8351c337bbc8cb342dc22cb011a80c920a2`: macOS ARM64 `88059892376`, Windows x64 `88059892402`, Linux x64 `88059892426`, Linux ARM64 `88059892564`, and macOS x64 `88059892571`. The temporary reference-provider package remains quarantined as development-only oracle evidence until `NATIVE-007`; production routing, release interlock, version, tag, artifact, package publication, deployment, and public claims were not changed. | `ba64d83` |
| `NSASS-011-MODERN-COLOR-TRANSFORMS` | `VERIFIED` | Added provider-free native Zig global `adjust-color()`, `change-color()`, and `scale-color()` semantics for Lab/LCH, Oklab/Oklch, sRGB, linear sRGB, Display P3, A98 RGB, ProPhoto RGB, Rec. 2020, XYZ D50/D65, and explicit transforms through legacy RGB/HSL/HWB. Channel inference follows the typed input space; explicit space aliases are closed and channel membership is validated before evaluation. Transforms execute in the selected space, preserve alpha, convert typed inputs back to their original space, and retain existing legacy inference/output behavior. Scaling uses Sass's nominal per-space bounds, signed axes move toward their negative bound, and channels already beyond the requested outward bound remain unchanged. Adjusted polar chroma clamps at zero while negative changed chroma is equivalently normalized by flipping hue 180 degrees. Out-of-range changed modern lightness is never silently clamped: an allocation-free canonical `color-mix(in <space>, color(xyz ...) 100%, ...)` serializer preserves the exact color in both compressed and expanded output. Missing-component color transforms, ambiguous/mixed channels, invalid units/ranges/spaces, hue scaling, and selected argument splats fail closed with no CSS; alpha-only transforms preserve typed missing components. Test-first work recorded the expected missing-`transformModern` compile failure plus a 61/62 evaluator failure while the remaining focused tests stayed green. Direct and evaluator regressions now cover every admitted space, positive/negative/outward scaling, percentages, hue wrapping, negative chroma, all four out-of-range modern serializers, legacy/modern cross-space round trips, both SCSS and indented Sass, rejection boundaries, and the exhaustive allocation-failure path against exact Dart Sass 1.101.0 development-oracle bytes. Focused Debug/ReleaseSafe pass 21/21 steps and 160/160 tests; checksum-verified official Zig 0.15.2 native Linux ARM64 Docker with read-only source, only an isolated writable `.zig-cache`, and no network passes 21/21 steps and 159/160 tests plus the existing platform-intentional skip in both modes. Exact Zig 0.15.2 full Debug/ReleaseSafe each pass 58/58 steps and 655/655 tests. Native contract schema v3 adds `modern-color-transforms`; 10/10 plus check retains the recursive repository-owned Zig/`std`/`builtin` import closure and target zero package or optional dependencies, external engines, child processes, network, downloads, or non-system language libraries. Workflow 5/5 plus check, dependency 4/4 plus policy check and live 0/0/0 production audits, Zig package 7/7, preprocessor package 6/6 plus check, documentation 7/7 plus check over 38 Markdown files/68 fences, website 139/139 plus production build, provider-oracle Sass 28/28, Less 27/27, Stylus 24/24, host 17/17, resolver 12/12, metadata 13/13, product 30/30, capability 7/7 plus check, format matrix 2/2 plus validation, repository formatting, JavaScript/JSON syntax, and diff gates pass. Windows x64 and Linux ARM64 production builds and direct color-core cross-compiles pass in Debug and ReleaseSafe. Exact-main Build `29635686794` passed comprehensive Test Suite `88057498059` and all five native archive/offline-install jobs on `870fdb24cf2c782dcd4bee5d568477c42e171bf8`: Linux x64 `88057498067`, Windows x64 `88057498074`, macOS ARM64 `88057498082`, Linux ARM64 `88057498090`, and macOS x64 `88057498101`. The temporary reference-provider package still owns its quarantined 86-package oracle graph until `NATIVE-007`; public routing, release interlock, version, tag, artifact, package publication, deployment, and public claims were not changed. | `870fdb2` |
| `NSASS-011-COLOR-CONVERSION` | `VERIFIED` | Added one allocation-free native Zig conversion pipeline across every color space owned by the private Sass value model: legacy RGB/HSL/HWB, Lab/LCH, Oklab/Oklch, sRGB, linear sRGB, Display P3, A98 RGB, ProPhoto RGB, Rec. 2020, XYZ D50, and canonical XYZ D65. Fixed compiled matrices and sign-preserving extended-gamut transfer functions route through XYZ D65, with exact Bradford D50/D65 adaptation, rectangular/polar conversion, alpha preservation, no gamut clipping, and no heap or provider access. Identity conversion preserves missing masks; cross-space conversion with any missing component deliberately fails closed until exact propagation semantics are implemented, while computed near-neutral polar hues become missing. Legacy manipulation APIs remain legacy-only rather than silently accepting modern colors, and same-space typed equality is unchanged. The Rec. 2020 piecewise transfer is explicitly pinned to Dart Sass 1.101.0 and the CSS Color 4 April 2025 CRD compatibility target rather than silently following later draft churn. Test-first work produced the expected missing-`convert` compilation baseline with the other 145 tests green. Twelve direct color tests now include exact Dart Sass oracle vectors for `#369` in all 12 modern/predefined destinations, alpha preservation, every inverse round trip, missing-channel rejection, and legacy/modern boundary retention. Focused Debug/ReleaseSafe pass 21/21 steps and 157/157 tests; checksum-verified official Zig 0.15.2 native Linux ARM64 Docker with a read-only source mount and isolated writable cache passes 21/21 steps and 156/157 tests plus the existing one platform-intentional skip in both modes. Exact Zig 0.15.2 full Debug/ReleaseSafe each pass 58/58 steps and 652/652 tests. Native contract schema v3 adds `cross-space-color-conversion`; 10/10 plus check retains the recursive repository-owned Zig/`std`/`builtin` import closure and target zero package or optional dependencies, external engines, child processes, network, downloads, or non-system language libraries. Workflow 5/5 plus check, dependency 4/4 plus policy check and live 0/0/0 production audits, Zig package 7/7, preprocessor package 6/6 plus check, documentation 7/7 plus check over 38 Markdown files/68 fences, website 139/139 plus production build, provider-oracle Sass 28/28, host 17/17, resolver 12/12, metadata 13/13, product 30/30, capability 7/7 plus check, format matrix 2/2 plus validation, repository formatting, JavaScript/JSON syntax, and diff gates pass. Windows x64 and Linux ARM64 production builds and direct color-core cross-compiles pass in Debug and ReleaseSafe. Exact-main Build `29634551781` passed comprehensive Test Suite `88054452294` and all five native archive/offline-install jobs on `d7aafbbd27518692861fbd2b36e5667381d9f847`: Linux ARM64 `88054452302`, Linux x64 `88054452303`, Windows x64 `88054452319`, macOS x64 `88054452326`, and macOS ARM64 `88054452336`. The temporary reference-provider package still owns its quarantined 86-package oracle graph until `NATIVE-007`; public routing, release interlock, version, tag, artifact, package, deployment, and claims were not changed. | `d7aafbb` |
| `NSASS-011-MIXINS-CONTENT` | `VERIFIED` | Added provider-free native Zig execution for bounded source-ordered Sass mixins at stylesheet and style-rule lexical scope in SCSS and indented Sass. Declarations support zero, required, and earlier-parameter-dependent defaults; includes support parenthesized or bare calls, positional and keyword arguments, underscore/hyphen identity, caller-order expression evaluation, local/global lookup, redefinition, nested includes, variables, declarations, comments, rules, lazy conditionals, and bounded loops. Content blocks retain the include caller's lexical scope, may be optional or repeated, forward through nested includes without recursion, never leak into an include that received no block, and are rejected when the target mixin does not lexically accept content. A mixin named `using` remains valid while the distinct `using (...)` content-parameter form fails closed. The complete structural pass rejects declarations inside control/mixin/function contexts, functions inside mixins or include content, `@content` outside a mixin, and parse-level duplicate/ordered argument errors even in unselected branches while preserving lazy unknown calls and argument splats in unselected branches. Rest parameters, selected argument splats, and content arguments/parameters deliberately remain unsupported; modules/imports and remaining directives retain the existing closed boundary. Combined function/mixin count, argument, environment/value/temporary/expression/evaluation-depth/operation, cumulative call-count/depth, cancellation, output, and allocation ceilings are terminal with no partial CSS. Test-first work produced the expected 151/155 red baseline before implementation. The pinned Dart Sass 1.101.0 development-only oracle then agreed byte-for-byte on source order, global/local capture, redefinition, defaults/keywords, nested rule output, optional/repeated/forwarded content, no-content isolation, a literal `using` name, unselected call syntax/laziness, and both syntaxes. Focused Debug/ReleaseSafe pass 21/21 steps and 155/155 tests; checksum-verified native Linux ARM64 Docker Debug/ReleaseSafe each pass 21/21 steps with 154/155 tests and the existing one platform-intentional skip. Exact Zig 0.15.2 full Debug/ReleaseSafe each pass 58/58 steps and 650/650 tests, including the expanded exhaustive evaluator allocation-failure path. Native contract schema v3 adds `bounded-user-mixins-content`; 10/10 plus check retains the recursive repository-owned Zig/`std`/`builtin` import closure and target zero package or optional dependencies, external engines, child processes, network, downloads, or non-system language libraries. Workflow 5/5 plus check, dependency 4/4 plus policy check and live 0/0/0 production audits, Zig package 7/7, preprocessor package 6/6 plus check, documentation 7/7 plus check over 38 Markdown files/68 fences, website 139/139 plus production build, provider-oracle Sass 28/28, host 17/17, resolver 12/12, metadata 13/13, product 30/30, repository formatting, JavaScript/JSON syntax, and diff gates pass. Windows x64 and Linux ARM64 production builds and direct native Sass semantic cross-compiles pass in Debug and ReleaseSafe. Exact-main Build `29633632202` passed comprehensive Test Suite `88051913989` and all five native archive/offline-install jobs on `d33358600752a138e27f7f5582b14a5e6e47d559`: Linux x64 `88051913922`, macOS x64 `88051913927`, macOS ARM64 `88051913959`, Windows x64 `88051913971`, and Linux ARM64 `88051913994`. The temporary reference-provider package still owns its quarantined 86-package oracle graph until `NATIVE-007`; public routing, release interlock, version, tag, artifact, package, deployment, and claims were not changed. | `d333586` |
| `NSASS-011-USER-FUNCTIONS` | `VERIFIED` | Added provider-free native Zig execution for bounded source-ordered Sass user functions at global and style-rule lexical scope in SCSS and indented Sass. Signatures support zero, required, and earlier-parameter-dependent default values; calls support positional and keyword arguments, underscore/hyphen identity, trailing commas, call-site source-order evaluation, nested calls, recursion, redefinition, and user overrides of legacy global built-ins. Calls before definition and local calls outside their definition scope remain preserved CSS rather than receiving accidental forward/global visibility. Function bodies own local variables, comments, lazy conditionals, all three bounded loop forms, and early `@return` propagation through nested control flow. A complete structural pass rejects functions in control/mixin/function contexts, `@return` outside functions, malformed returns, and forbidden output/rules/includes/extends even when the affected branch or function is never executed. Rest parameters and argument splats fail closed; mixins/content, arbitrary embedded custom calls inside unknown CSS functions, modules/imports, and remaining body directives deliberately remain pending. Callable-count, argument, environment/value/temporary/expression/evaluation-depth/operation, cumulative call-count/depth, cancellation, output, and allocation ceilings are terminal with no partial CSS. Test-first work produced the expected 146/149 red baseline before implementation. The pinned Dart Sass 1.101.0 development-only oracle then agreed byte-for-byte on source order, global/local capture, redefinition, override, parameter/default/keyword/trailing behavior, recursion, loop returns, and both syntaxes. Focused Debug/ReleaseSafe and native Linux ARM64 Docker Debug/ReleaseSafe each pass 21/21 steps and 151/151 tests; exact Zig 0.15.2 full Debug/ReleaseSafe each pass 58/58 steps and 646/646 tests, including the expanded exhaustive evaluator allocation-failure path. Native contract schema v3 adds `bounded-user-functions`; 10/10 plus check retains the recursive repository-owned Zig/`std`/`builtin` import closure and target zero package or optional dependencies, external engines, child processes, network, downloads, or non-system language libraries. Workflow 5/5 plus check, dependency 4/4 plus policy check and live 0/0/0 production audits, Zig package 7/7, preprocessor package 6/6 plus check, documentation 7/7 plus check over 38 Markdown files/68 fences, repository formatting, JavaScript/JSON syntax, and diff gates pass. Windows x64 and Linux ARM64 production builds and direct native Sass semantic cross-compiles pass in Debug and ReleaseSafe. Exact-main Build `29632429496` passed comprehensive Test Suite `88048632334` and all five native archive/offline-install jobs on `4e20c9cca76217399389eb318a069e2ae8ea6e22`: macOS ARM64 `88048632333`, Windows x64 `88048632337`, Linux x64 `88048632339`, macOS x64 `88048632342`, and Linux ARM64 `88048632376`. The temporary reference-provider package still owns its quarantined 86-package oracle graph until `NATIVE-007`; public routing, release interlock, version, tag, artifact, package, deployment, and claims were not changed. | `4e20c9c` |
| `NSASS-011-LOOPS` | `VERIFIED` | Added native Zig execution for Sass `@for`, `@each`, and `@while` at stylesheet, style-rule, selected-conditional, and nested-loop scope in SCSS and indented Sass. `@for` evaluates its bounds once, requires finite integers with compatible units, implements ascending/descending `through` and `to` ranges, preserves the start unit, and rejects non-advancing IEEE-754 steps. `@each` covers scalar, list, map-pair, multi-variable destructuring, duplicate normalized names, missing-slot `null`, and variable iterables. `@while` reevaluates Sass truthiness in one persistent flow frame. Every real nested iteration consumes the shared cumulative transaction budget; variable-count, expression, evaluation-depth, operation, environment, value, temporary, output, cancellation, and allocation ceilings remain terminal with no partial CSS. Loop variables and fresh body locals persist only for the loop lifetime while assignments to existing lexical/global owners follow the preceding verified scope model. Zero-iteration bodies stay lazy, including unsupported directives. Test-first work produced the expected 143/145 red `UnsupportedFeature` baseline before implementation; the pinned Dart Sass 1.101.0 development-only oracle then agreed byte-for-byte on the admitted range, unit, iterable, destructuring, duplicate-name, nested-rule, scope, laziness, and indented-Sass fixtures. Focused Debug/ReleaseSafe and native Linux ARM64 Docker Debug/ReleaseSafe each pass 21/21 steps and 146/146 tests; exact Zig 0.15.2 full Debug/ReleaseSafe each pass 58/58 steps and 641/641 tests, including exhaustive evaluator allocation failure. Native contract schema v3 adds `bounded-control-flow-loops`; 10/10 plus check retains the recursive repository-owned Zig/`std`/`builtin` import closure and target zero package or optional dependencies, external engines, child processes, network, downloads, or non-system language libraries. Dependency 4/4 plus policy check and live 0/0/0 production audits, preprocessor package 6/6 plus check, formatting, JavaScript/JSON syntax, and diff gates pass; the earlier unchanged workflow, Zig-package, documentation, Windows x64/Linux ARM64 production-build, and direct semantic cross-compile gates remain green. Exact-main Build `29631310550` passed comprehensive Test Suite `88045316225` and all five native archive/offline-install jobs on `01629b0c2b3f0cc0d1695647ffe402ca41907d32`: macOS ARM64 `88045316231`, Linux x64 `88045316237`, Windows x64 `88045316239`, macOS x64 `88045316244`, and Linux ARM64 `88045316250`. Callables, modern cross-space colors/module aliases, remaining built-ins/modules/imports, diagnostics/dependencies/maps, and differential graduation remain pending. The temporary reference-provider package still owns its quarantined 86-package oracle graph until `NATIVE-007`; public routing, release interlock, version, tag, artifact, package, deployment, and claims were not changed. | `01629b0` |
| `NSASS-011-FLOW-SCOPE` | `VERIFIED` | Replaced fail-closed flow mutation with native Zig semantic scope frames that distinguish actual variable ownership from immutable environment snapshots. Existing globals update through root flow control, existing non-global owners update across nested flow and style-rule frames, and fresh branch or nested-rule variables remain confined; owner updates mirror only through active descendants while global lookup always observes the current global cursor. `!global`, `!default`, null, local shadows, new-global warnings, underscore/hyphen identity, indented Sass, nested-rule propagation, nested branch ownership, and the upstream dart-sass#1250 regression match the exact Dart Sass 1.101.0 development oracle. An added ordered `!global`/normal-assignment regression first failed red (142/143) because a visibility mirror was mistaken for ownership; the final owner-frame design produces the oracle's branch-local value while preserving the new global outside the branch. Fresh root/rule/nested-flow/nested-rule variables fail with `UndefinedVariable`; scope/binding ceilings, cancellation, operation/temporary/value/environment ceilings, and every allocation failure remain terminal with no partial CSS. Focused Debug/ReleaseSafe and native Linux ARM64 Docker Debug/ReleaseSafe each pass 21/21 steps and 143/143 tests; exact Zig 0.15.2 full Debug/ReleaseSafe each pass 58/58 steps and 638/638 tests. Native contract schema v3 adds `flow-control-variable-scope`; 10/10 plus check retains the recursive repository-owned Zig/`std`/`builtin` import closure and target zero package or optional dependencies, external engines, child processes, network, downloads, or non-system language libraries. Workflow 5/5 plus check, dependency 4/4 plus policy check and live 0/0/0 production audits, Zig package 7/7, preprocessor package 6/6 plus check, documentation 7/7 plus check over 38 Markdown files/68 fences, formatting, JavaScript/JSON syntax, and diff gates pass. Windows x64 and Linux ARM64 production builds plus direct native Sass semantic cross-compiles pass in Debug and ReleaseSafe. Exact-main Build `29630488045` passed comprehensive Test Suite `88042965299` and all five native archive/offline-install jobs on `ce1e0ff792e63761e0b08cdff84d52ce04db4660`: Windows x64 `88042965286`, Linux x64 `88042965290`, macOS x64 `88042965293`, Linux ARM64 `88042965302`, and macOS ARM64 `88042965303`. Loops, callables, modules/imports, remaining built-ins, diagnostics/dependencies/maps, and differential graduation remain pending. The temporary reference-provider graph, public capability boundary, release interlock, version, tag, artifact, package, deployment, and claims were not changed. | `ce1e0ff` |
| `NSASS-011-CONDITIONALS` | `VERIFIED` | Added native Zig evaluation for contiguous Sass `@if`/`@else if`/`@else` chains at stylesheet and style-rule scope for both SCSS and indented Sass. Only the selected condition and block execute; unselected undefined conditions, declarations, and unsupported loops remain lazy. Sass truthiness treats only `false` and `null` as false, so zero selects its branch. Nested conditions and nested style rules preserve parent declaration chunks and deterministic source order. Stray/multiple/post-`@else` branches, missing conditions/blocks, invalid `@else` preludes, selected unsupported directives, evaluation-depth exhaustion, cancellation, operation/temporary/value/environment ceilings, and every allocation failure abort private staging with no partial CSS. Flow-control assignments deliberately remained fail-closed in this slice pending their exact scope package. Two new evaluator regressions pass inside the 141/141 focused native suite in Debug and ReleaseSafe; exact Zig 0.15.2 full Debug/ReleaseSafe each pass 58/58 steps and 636/636 tests. The pinned Dart Sass 1.101.0 development oracle agrees byte-for-byte with the complete admitted SCSS and indented-Sass outputs, including nested branch ordering and lazy undefined expressions. Native contract schema v3 adds `lazy-conditional-emission`; 10/10 plus check retains the recursive repository-owned Zig/`std`/`builtin` import closure and target zero package or optional dependencies, external engines, child processes, network, downloads, or non-system language libraries. Workflow 5/5 plus check, dependency 4/4 plus check, Zig package 7/7, preprocessor package 6/6 plus check, documentation 7/7 plus check over 38 Markdown files/68 fences, formatting, JavaScript/JSON syntax, and diff gates pass. Windows x64 and Linux ARM64 production builds plus direct native Sass semantic cross-compiles pass in Debug and ReleaseSafe. Initial exact-main Build `29629124469` provided real red portability evidence when Linux ARM64 job `88039293955` reported Zig `checkAllAllocationFailures` heap-layout-dependent `NondeterministicMemoryUsage`; production semantics were green on the other native hosts. Repair `0d9dd87` makes test backing growth use explicit injectable allocations instead of nondeterministic in-place remaps, then passes native Linux ARM64 Docker Debug/ReleaseSafe and the complete local matrix. Replacement exact-main Build `29629395876` passed comprehensive Test Suite `88040014603` and all five native archive/offline-install jobs on `0d9dd87c500cfabd622a1df2d16be11746f52f6d`: Linux x64 `88040014624`, macOS ARM64 `88040014625`, macOS x64 `88040014633`, Linux ARM64 `88040014647`, and Windows x64 `88040014652`. Flow-variable propagation, loops, callables, modules/imports, remaining built-ins, diagnostics/dependencies/maps, and differential graduation remain pending. The temporary reference-provider graph, public capability boundary, release interlock, version, tag, artifact, package, deployment, and claims were not changed. | `15a9359`, `0d9dd87` |
| `NSASS-011-PREDEFINED-COLORS` | `VERIFIED` | Extended the provider-free typed color model and evaluator with Sass/CSS Color 4 `color()` construction for `srgb`, `srgb-linear`, `display-p3`, `a98-rgb`, `prophoto-rgb`, `rec2020`, `xyz-d50`, and canonical D65 `xyz`; `xyz-d65` parses to the same identity and serializes as `xyz`. Native Zig owns case-insensitive closed space recognition, unitless/percentage channels, alpha clamping, case-sensitive `none` channel masks, canonical positive/negative fractional spelling, and exact same-space/mask equality without claiming unsupported cross-space conversion. The syntax-only layer binds the single `$description` overload before values execute; comma syntax, quoted/custom/unknown spaces, missing/extra channels, alpha without slash, bad units/types, unknown/duplicate/ordered arguments, multiple alpha separators including deferred forms, and resource ceilings fail closed. Unknown space identity rejects before deliberately undefined channel expressions execute. Positional CSS-deferred descriptions/channels remain preserved, while keyword-deferred and splat forms remain explicitly unsupported rather than leaking Sass syntax. Ten direct color tests and 46 evaluator tests pass inside the 139/139 focused native suite in Debug and ReleaseSafe, including every evaluator allocation failure. Exact Zig 0.15.2 full Debug/ReleaseSafe each pass 58/58 steps and 634/634 tests. The pinned Dart Sass 1.101.0 development oracle agrees byte-for-byte with all 16 admitted predefined-space construction, canonicalization, missing-channel, percentage, keyword, case, and equality outputs. Native contract schema v3 adds `predefined-wide-gamut-colors`; 10/10 plus check retains the recursive repository-owned Zig/`std`/`builtin` import closure and target zero package or optional dependencies, external engines, child processes, network, downloads, or non-system language libraries. Workflow 5/5 plus check, dependency 4/4 plus check, Zig package 7/7, preprocessor package 6/6 plus check, documentation 7/7 plus check over 38 Markdown files/68 fences, formatting, JavaScript/JSON syntax, and diff gates pass. Windows x64 and Linux ARM64 production builds plus direct native Sass semantic cross-compiles pass in Debug and ReleaseSafe. Exact-main Build `29628639663` passed comprehensive Test Suite `88037960193` and all five native archive/offline-install jobs on `861bf5cf5fd16a13e06ff77aefb719363383e724`: macOS ARM64 `88037960194`, Linux x64 `88037960196`, Linux ARM64 `88037960202`, macOS x64 `88037960211`, and Windows x64 `88037960215`. Modern cross-space conversion/manipulation and module aliases, conditional/callable/control flow, remaining collection/string/selector/built-ins, modules/imports, diagnostics/dependencies/maps, and differential graduation remain pending. The temporary reference-provider graph, public capability boundary, release interlock, version, tag, artifact, package, deployment, and claims were not changed. | `861bf5c` |
| `NSASS-011-MODERN-LAB-COLORS` | `VERIFIED` | Added provider-free typed construction and deterministic serialization for Sass `lab()`, `lch()`, `oklab()`, and `oklch()`. Native Zig owns space-specific percentage scaling, lightness/alpha clamping, cylindrical chroma clamping, angle normalization, case-sensitive `none` channel masks, canonical positive/negative fractional spelling, and exact same-space equality without pretending unsupported cross-space conversion. The syntax-only argument layer binds the single `$channels` overload before value execution; comma syntax, missing/extra channels, bad units/types, unknown/duplicate/ordered arguments, multiple alpha separators (including deferred forms), and resource ceilings fail closed. Positional CSS-deferred forms remain preserved, while keyword-deferred and splat forms remain explicitly unsupported rather than leaking Sass syntax. The shared CSS-value gate now validates every typed color through bounded serialization. Eight direct color tests and 44 evaluator tests pass inside the 135/135 focused native suite in Debug and ReleaseSafe, including every evaluator allocation failure. Exact Zig 0.15.2 full Debug/ReleaseSafe each pass 58/58 steps and 630/630 tests. The pinned Dart Sass 1.101.0 development oracle agrees byte-for-byte with all ten admitted construction, normalization, missing-channel, and equality outputs. Native contract schema v3 adds `modern-lab-color-constructors`; 10/10 plus check retains the recursive repository-owned Zig/`std`/`builtin` import closure and target zero package or optional dependencies, external engines, child processes, network, downloads, or non-system language libraries. Workflow 5/5 plus check, dependency 4/4 plus check, Zig package 7/7, preprocessor package 6/6 plus check, documentation 7/7 plus check over 38 Markdown files/68 fences, formatting, JavaScript/JSON syntax, and diff gates pass. Windows x64 and Linux ARM64 production builds plus direct native Sass semantic cross-compiles pass in Debug and ReleaseSafe. Exact-main Build `29628272426` passed comprehensive Test Suite `88036878985` and all five native archive/offline-install jobs on `806142fc878f418ed637f71fc485fadc87a51f44`: macOS x64 `88036879009`, Linux ARM64 `88036879024`, Windows x64 `88036879030`, macOS ARM64 `88036879034`, and Linux x64 `88036879038`. Wide-gamut `color()` spaces, modern cross-space conversion/manipulation and module aliases, remaining collection/string/selector/built-ins, callables/control flow/modules/imports, diagnostics/dependencies/maps, and differential graduation remain pending. The temporary reference-provider graph, public capability boundary, release interlock, version, tag, artifact, package, deployment, and claims were not changed. | `806142f` |
| `NSASS-011-VARIADIC-MAP-GET` | `VERIFIED` | Routed the last generic eager-evaluation built-in path through the bounded syntax-only argument parser. Native `map-get()` retains arbitrary bounded positional nested-key traversal while keyword-bearing calls bind only canonical `$map` and `$key` parameters before any value executes; reordered and one-positional-plus-keyword forms work without weakening the variadic positional path. Missing, unknown, duplicate, positional-after-keyword, named-rest, and argument-list splat forms fail closed, and an unknown keyword is rejected before deliberately undefined values can execute. Forty-two evaluator tests pass inside the 131/131 focused native suite in Debug and ReleaseSafe; the every-allocation-failure fixture traverses the reordered map-keyword path. Exact Zig 0.15.2 full Debug/ReleaseSafe each pass 58/58 steps and 626/626 tests. The pinned Dart Sass 1.101.0 development oracle agrees byte-for-byte with all three admitted reordered, mixed, and variadic nested outputs and rejects the measured ambiguity matrix. Native contract schema v3 adds `variadic-map-get-binding`; 10/10 plus check retains the recursive repository-owned Zig/`std`/`builtin` import closure and target zero package or optional dependencies, external engines, child processes, network, downloads, or non-system language libraries. Workflow 5/5 plus check, dependency 4/4 plus check, Zig package 7/7, preprocessor package 6/6 plus check, documentation 7/7 plus check over 38 Markdown files/68 fences, formatting, JavaScript/JSON syntax, and diff gates pass. Windows x64 and Linux ARM64 production builds plus direct native Sass semantic cross-compiles pass in Debug and ReleaseSafe. Exact-main Build `29627551315` passed comprehensive Test Suite `88034821332` and all five native archive/offline-install jobs on `67c52bd7a8437f0425ea85f07c276a612aaa1201`: macOS x64 `88034821363`, Linux x64 `88034821365`, Linux ARM64 `88034821370`, macOS ARM64 `88034821373`, and Windows x64 `88034821374`. Modern/wide-gamut color spaces and module aliases, remaining collection/string/selector/built-ins, callables/control flow/modules/imports, diagnostics/dependencies/maps, and differential graduation remain pending. The temporary reference-provider graph, public capability boundary, release interlock, version, tag, artifact, package, deployment, and claims were not changed. | `67c52bd` |
| `NSASS-011-COLOR-CONSTRUCTOR-KEYWORDS` | `VERIFIED` | Routed all five admitted Sass color-constructor names through the bounded syntax-only argument parser before evaluation. Native Zig now owns reordered legacy RGB/RGBA `$red`/`$green`/`$blue` plus optional `$alpha`, HSL/HSLA `$hue`/`$saturation`/`$lightness` plus optional `$alpha`, RGB alias `$color`/`$alpha`, modern single `$channels` overloads for RGB/RGBA/HSL/HSLA/HWB, and positional-prefix plus keyword completion. Overload selection is closed and binding completes before values execute; missing, duplicate, unknown, mixed-family, positional-after-keyword, legacy-named HWB, malformed-channel, argument-list splat, and deferred keyword forms fail closed without preserving invalid keyword syntax. Existing positional and CSS-deferred constructor behavior remains unchanged, including `rgb(color, alpha)` alias parity. Forty evaluator tests pass inside the 129/129 focused native suite in Debug and ReleaseSafe; the every-allocation-failure fixture traverses the modern keyword path. Exact Zig 0.15.2 full Debug/ReleaseSafe each pass 58/58 steps and 624/624 tests. The pinned Dart Sass 1.101.0 development oracle agrees byte-for-byte with all 14 admitted output declarations. Native contract schema v3 adds `color-constructor-keyword-overloads`; 10/10 plus check retains the recursive repository-owned Zig/`std`/`builtin` import closure and target zero package or optional dependencies, external engines, child processes, network, downloads, or non-system language libraries. Workflow 5/5 plus check, dependency 4/4 plus check, Zig package 7/7, preprocessor package 6/6 plus check, documentation 7/7 plus check over 38 Markdown files/68 fences, formatting, JavaScript/JSON syntax, and diff gates pass. Windows x64 and Linux ARM64 production builds plus direct native Sass semantic cross-compiles pass in Debug and ReleaseSafe. Exact-main Build `29627202081` passed comprehensive Test Suite `88033845393` and all five native archive/offline-install jobs on `e4693b49ee08ca03defbab3c36f7d203a875298a`: macOS ARM64 `88033845371`, macOS x64 `88033845378`, Linux ARM64 `88033845389`, Linux x64 `88033845396`, and Windows x64 `88033845425`. Variadic `map-get()`, modern `mix()` methods, wide-gamut color spaces and module aliases, remaining built-ins, callables/control flow/modules/imports, diagnostics/dependencies/maps, and differential graduation remain pending. The temporary reference-provider graph, public capability boundary, release interlock, version, tag, artifact, package, deployment, and claims were not changed. | `e4693b4` |
| `NSASS-011-FIXED-BUILTIN-KEYWORDS` | `VERIFIED` | Routed all already-admitted fixed-arity collection and legacy color built-ins through the bounded syntax-only argument binder before evaluation: `nth()`/`length()`; RGB/HSL/alpha channel accessors; `mix()`; lighten/darken/saturate/desaturate and hue/complement/grayscale/invert manipulation; opacify/fade-in/transparentize/fade-out; `opacity()`; and `ie-hex-str()`. Positional and reordered keyword calls, canonical names, required and trailing optional parameters, duplicates, unknowns, missing arguments, ordering, positional ceilings, and splats share one fail-closed path. Positional CSS filter conflicts remain deferred exactly as before, while named non-color `saturate()`/`grayscale()`/`invert()`/`opacity()` calls fail as Sass type errors rather than leaking keyword syntax into CSS. Existing positional, dynamic-filter, and allocation-failure behavior remains green. Thirty-eight evaluator tests pass inside the 127/127 focused native suite in Debug and ReleaseSafe; the every-allocation-failure fixture exercises reordered collection and color calls. Exact Zig 0.15.2 full Debug/ReleaseSafe each pass 58/58 steps and 622/622 tests. The pinned Dart Sass 1.101.0 development oracle agrees byte-for-byte with all 25 admitted fixed-builtin keyword outputs. Native contract schema v3 adds `fixed-builtin-keyword-binding`; 10/10 plus check retains the recursive repository-owned Zig/`std`/`builtin` import closure and target zero package or optional dependencies, external engines, child processes, network, downloads, or non-system language libraries. Workflow 5/5 plus check, dependency 4/4 plus check, Zig package 7/7, preprocessor package 6/6 plus check, documentation 7/7 plus check over 38 Markdown files/68 fences, formatting, JavaScript/JSON syntax, and diff gates pass. Windows x64 and Linux ARM64 production builds plus direct native Sass semantic cross-compiles pass in Debug and ReleaseSafe. Exact-main Build `29626653525` passed comprehensive Test Suite `88032313934` and all five native archive/offline-install jobs on `0b214eaa93b68010b4f5b8b19156a36c3090fa0e`: macOS x64 `88032313955`, Windows x64 `88032313956`, Linux x64 `88032313978`, Linux ARM64 `88032313979`, and macOS ARM64 `88032313980`. Variadic `map-get()`, legacy/modern constructor keyword overloads, modern `mix()` methods, module aliases, remaining built-ins, callables/control flow/modules/imports, diagnostics/dependencies/maps, and differential graduation remain pending. No current provider dependency, public capability, release interlock, version, tag, artifact, package, deployment, or claim changed. | `0b214ea` |
| `NSASS-011-HWB-COLOR-TRANSFORMS` | `VERIFIED` | Added allocation-free RGB/HSL-to-HWB conversion and native HWB `adjust-color()`, `change-color()`, and `scale-color()` semantics over the bounded keyword binder. Implicit channel-based selection and explicit `$space: hwb` own hue/whiteness/blackness grouping, input-space hue selection, unitless/percentage/angle conversion, raw changed and adjusted channels, signed percentage scaling, hue normalization, and CSS Color normalization when whiteness plus blackness exceeds 100%. The serializer admits HSL only when shorter for HWB-derived values, preserving exact grayscale, normalized, fractional, and out-of-range oracle output. Cross-space mixing, selected-space mismatches, hue scaling, bad units/types/ranges, duplicate/unknown/missing arguments, splats, cancellation, and every allocation failure fail closed with no partial CSS. Six direct color tests and 36 evaluator tests pass inside the 125/125 focused native suite in Debug and ReleaseSafe. Exact Zig 0.15.2 full Debug/ReleaseSafe each pass 58/58 steps and 620/620 tests. The pinned Dart Sass 1.101.0 development oracle agrees byte-for-byte with all 11 admitted HWB transform outputs, including reordered calls, implicit/explicit selection, RGB conversion, normalization over 100%, and signed scaling. Native contract schema v3 adds `hwb-color-transforms`; 10/10 plus check retains the recursive repository-owned Zig/`std`/`builtin` import closure and target zero package or optional dependencies, external engines, child processes, network, downloads, or non-system language libraries. Workflow 5/5 plus check, dependency 4/4 plus check, Zig package 7/7, preprocessor package 6/6 plus check, documentation 7/7 plus check over 38 Markdown files/68 fences, formatting, JavaScript/JSON syntax, and diff gates pass. Windows x64 and Linux ARM64 production builds plus direct native Sass semantic cross-compiles pass in Debug and ReleaseSafe. Exact-main Build `29626201095` passed comprehensive Test Suite `88031002862` and all five native archive/offline-install jobs on `e12b3e491432852a53c212e5ed7113b38a243cb4`: Linux ARM64 `88031002877`, macOS x64 `88031002879`, macOS ARM64 `88031002889`, Windows x64 `88031002908`, and Linux x64 `88031002913`. Lab/LCH/OKLab/OKLCH/XYZ and wide-gamut RGB spaces, module aliases, remaining keyword built-ins, callables/control flow/modules/imports, diagnostics/dependencies/maps, and differential graduation remain pending. No current provider dependency, public capability, release interlock, version, tag, artifact, package, deployment, or claim changed. | `e12b3e4` |
| `NSASS-011-KEYWORD-COLOR-TRANSFORMS` | `VERIFIED` | Extended the allocation-free native color core and bounded argument evaluator with legacy RGB/HSL `adjust-color()`, `change-color()`, and `scale-color()`. The implementation owns one-positional-argument enforcement, reordered and canonical keyword binding, explicit `rgb`/`hsl` space selection, RGB/HSL group exclusivity, unitless/percentage/angle conversion, Sass alpha percentage behavior, adjustment clamping, raw changed-channel preservation, signed percentage scaling, hue normalization, and deterministic out-of-gamut HSL serialization. Binding succeeds before any value evaluation; malformed, duplicate, unknown, missing, mixed-space, typed, unit-bearing, out-of-range, quoted-space, hue-scale, splat, cancellation, and allocation failures abort private staging with no partial CSS. Five direct color tests and 34 evaluator tests pass inside the 122/122 focused native suite in Debug and ReleaseSafe, including every evaluator allocation failure. Exact Zig 0.15.2 full Debug/ReleaseSafe each pass 58/58 steps and 617/617 tests. The pinned Dart Sass 1.101.0 development oracle agrees byte-for-byte with all 28 admitted keyword-transform outputs, including reordered calls, every RGB channel, HSL transforms, percentages, alpha, explicit spaces, cross-space input, and out-of-gamut serialization. Native contract schema v3 adds `keyword-color-transforms`; 10/10 plus check retains the recursive repository-owned Zig/`std`/`builtin` import closure and target zero package or optional dependencies, external engines, child processes, network, downloads, or non-system language libraries. Workflow 5/5 plus check, dependency 4/4 plus check, Zig package 7/7, preprocessor package 6/6 plus check, documentation 7/7 plus check over 38 Markdown files/68 fences, formatting, JavaScript/JSON syntax, and diff gates pass. Windows x64 and Linux ARM64 production builds plus direct native Sass semantic cross-compiles pass in Debug and ReleaseSafe. Exact-main Build `29625881144` passed comprehensive Test Suite `88030067781` and all five native archive/offline-install jobs on `31d3dc86420e674f95a571a0959a35619828a224`: macOS x64 `88030067801`, Windows x64 `88030067803`, macOS ARM64 `88030067804`, Linux x64 `88030067808`, and Linux ARM64 `88030067810`. HWB and modern/wide-gamut transform spaces, module aliases, remaining keyword built-ins, callables/control flow/modules/imports, diagnostics/dependencies/maps, and differential graduation remain pending. No current provider dependency, public capability, release interlock, version, tag, artifact, package, deployment, or claim changed. | `31d3dc8` |
| `NSASS-011-KEYWORD-ARGUMENTS` | `VERIFIED` | Added a reusable bounded native Zig call-argument layer that retains source ranges without evaluating values, enforces positional-before-keyword ordering, Sass underscore/hyphen parameter-name equivalence, duplicate/unknown/missing argument rejection, optional parameters, per-call positional ceilings, and a fail-closed boundary for argument-list splats. Binding completes before any argument is evaluated. All eight admitted global string built-ins now use the binder for positional and keyword calls, including reordered keywords and canonical `$start-at`/`$end-at` aliases. Four direct binder tests and 32 evaluator tests pass inside the 119/119 focused native suite in Debug and ReleaseSafe, including every allocation failure in the parser, binder, and evaluator paths. Exact Zig 0.15.2 full Debug/ReleaseSafe each pass 58/58 steps and 614/614 tests. Native contract schema v3 binds `sass_arguments.zig`, its focused test, and `keyword-argument-binding`; 10/10 plus check retains the recursive import-closure guarantee that every transitive native import is repository-owned Zig or Zig `std`/`builtin`. The native target remains at zero package or optional dependencies, external engines, child processes, network, downloads, or non-system language libraries. Exact Dart Sass 1.101.0 development-oracle output agrees for admitted positional, named, reordered, optional, canonical-name, duplicate, unknown, missing, ordering, and splat probes. Workflow 5/5 plus check, dependency 4/4 plus check, Zig package 7/7, preprocessor package 6/6 plus check, documentation 7/7 plus check over 38 Markdown files/68 fences, formatting, JavaScript/JSON syntax, and diff gates pass. Windows x64 and Linux ARM64 production builds plus direct native Sass semantic cross-compiles pass in Debug and ReleaseSafe. Exact-main Build `29625248645` passed comprehensive Test Suite `88028239061` and all five native archive/offline-install jobs on `8db4212c9c2a5bc19b7759e164ac981d8b89735f`: macOS ARM64 `88028239040`, Linux x64 `88028239051`, Windows x64 `88028239067`, Linux ARM64 `88028239088`, and macOS x64 `88028239107`. Remaining native built-ins still require binder integration; callable definitions, rest arguments and argument-list splats remain pending. No current provider dependency, public capability, release interlock, version, tag, artifact, package, deployment, or claim changed. | `8db4212` |
| `NSASS-011-STRINGS` | `VERIFIED` | Added a separate bounded native Zig string core with owned UTF-8 validation, Unicode code-point length/index/slice/insert behavior, positive/zero/negative and out-of-range index clamping, empty-substring semantics, CSS hexadecimal escape decoding, quoted versus unquoted simple-escape behavior, continuation handling, replacement of invalid escaped scalars, deterministic quoted/unquoted re-encoding, and ASCII-only case folding that leaves non-ASCII code points unchanged. The shared evaluator now owns the admitted positional global `quote()`/`unquote()`, `str-length()`, `str-index()`, `str-slice()`, `str-insert()`, `to-upper-case()`, and `to-lower-case()` API; null search misses omit declarations, while bad arity, non-string values, fractional or unit-bearing indexes, malformed UTF-8/escapes, raw NUL, temporary ceilings, cancellation, and allocation failure abort private staging with no partial CSS. Four direct string tests and 32 evaluator tests pass inside the 115/115 focused native suite in Debug and ReleaseSafe; the every-allocation-failure fixture traverses every newly allocating string operation. Exact Zig 0.15.2 full Debug/ReleaseSafe each pass 56/56 steps and 610/610 tests. Native contract schema v3 now binds `sass_string.zig`, its focused test, and `unicode-string-core`/`legacy-string-builtins`; 10/10 plus check also recursively closes every transitive native Zig import to repository-owned `.zig` files or Zig `std`/`builtin`, rejecting external modules, non-literal imports, symlinks, and path escapes. SCSS/Sass remain private, production-unreachable, and release-fail-closed; the native target retains zero package/optional dependencies, external engines, compile child processes, network, downloads, or non-system language libraries. The unchanged current provider packages remain the existing reference migration path, never enter or execute inside the native implementation, and are removed only after the independent native graduation gates reach `NATIVE-007`. Workflow 5/5 plus check, dependency 4/4 plus check, Zig package 7/7, preprocessor package 6/6 plus check, documentation 7/7 plus check over 38 Markdown files/68 fences, repository formatting, JavaScript/JSON syntax, and diff gates pass. Windows x64 and Linux ARM64 production builds plus direct native Sass semantic cross-compiles pass in Debug and ReleaseSafe. Exact Dart Sass 1.101.0 development-oracle output agrees for the complete admitted fixture, escape/Unicode/index boundary probes, and rejection cases. Exact-main Build `29624804794` passed comprehensive Test Suite `88026950697` and all five native archive/offline-install jobs on `b4de04dad058dc22e7cc39116fa261726355c4e7`: macOS x64 `88026950695`, Windows x64 `88026950705`, Linux x64 `88026950710`, Linux ARM64 `88026950720`, and macOS ARM64 `88026950727`. Generic keyword invocation, `sass:string` module aliases, remaining string inspection/serialization details, selector functions, modern/wide-gamut colors/modules, callables/control flow/modules/imports, diagnostics/dependencies/maps, and differential graduation remain pending. No current production dependency, public capability, release interlock, version, tag, artifact, package, deployment, or claim changed. | `db12877`, `ab371d3`, `3158d12`, `b4de04d` |
| `NSASS-011-COLOR-MANIPULATION` | `VERIFIED` | Extended the native allocation-free color core with Sass's positional legacy manipulation API: exact alpha-weighted `mix()`, `lighten()`/`darken()`, `saturate()`/`desaturate()`, `adjust-hue()`/`complement()`, `grayscale()`, weighted `invert()`, `opacify()`/`fade-in()`, `transparentize()`/`fade-out()`, alpha-channel `opacity()`, and `ie-hex-str()`. Adjustments clamp at the semantic boundary, hue wraps deterministically, and the fixed serializer selects the same stable legacy RGB/HSL representation as the pinned oracle without allocating. CSS filter conflicts such as percentage `saturate()`, `grayscale()`, `invert()`, and `opacity()` remain valid deferred CSS rather than being stolen by Sass built-ins; malformed arity, types, units, ranges, and non-finite values fail closed with no partial CSS. Four direct color tests and 30 evaluator tests, including expanded every-allocation-failure traversal, pass inside the 109/109 focused native suite in Debug and ReleaseSafe; exact Zig 0.15.2 full Debug/ReleaseSafe each pass 54/54 steps and 604/604 tests. Native contract schema v3 binds `legacy-color-manipulation`, passing 9/9 plus check while SCSS/Sass stay private, production-unreachable, and release-fail-closed with zero target package/optional dependencies, external engines, compile child processes, network, downloads, or non-system language libraries. Workflow 5/5 plus check, dependency 4/4 plus check, Zig package 7/7, preprocessor package 6/6 plus check, documentation 7/7 plus check over 38 Markdown files/68 fences, Windows x64 and Linux ARM64 Debug/ReleaseSafe production builds, JavaScript/JSON syntax, formatting, and diff gates pass. Exact Dart Sass 1.101.0 development-oracle probes agree for ordinary, weighted, and alpha-mixed colors; all admitted adjustment, inversion, alpha, legacy-filter, serialization, dynamic-preservation, and rejection cases. Exact-main Build `29623700077` passed comprehensive Test Suite `88023747522` and all five native archive/offline-install jobs on `88182af4b7973caf0ff9ab6c51ca818dd40ac8f5`: Linux ARM64 `88023747538`, macOS x64 `88023747546`, macOS ARM64 `88023747551`, Windows x64 `88023747556`, and Linux x64 `88023747573`. Keyword-argument transforms including `adjust-color()`/`change-color()`/`scale-color()`, modern/wide-gamut color spaces and modules, escaped identifiers, strings/selectors, callables/control flow/modules/imports, diagnostics/dependencies/maps, and differential graduation remain pending. No production dependency, public capability, release interlock, version, tag, artifact, package, deployment, or claim changed. | `88182af` |
| `NSASS-011-COLORS` | `VERIFIED` | Added a separate allocation-free finite Zig color core compiled directly into ZigCSS: all 148 closed CSS named colors plus canonical aliases, 3/4/6/8-digit hex, legacy and modern `rgb()`/`rgba()`/`hsl()`/`hsla()`/`hwb()` construction, `rgba(color, alpha)`, unitless/percentage channels, degree/grad/radian/turn hues, clamping, hue/HWB normalization, RGB/HSL/HWB conversion, fuzzy cross-space equality, legacy channel accessors, and deterministic shortest RGB/HSL serialization through a fixed 512-byte ceiling. Static invalid arity, types, units, mixed syntax, duplicate alpha separators, and argument-limit overflow fail closed with no partial CSS; dynamic `var()`/`calc()` forms remain CSS-deferred after bounded structural admission, and outer commas normalize without mutating nested custom-property fallbacks. Named and hex literals now own typed colors, so quoted strings remain distinct while interpolation and CSS emission serialize canonically. Three direct color tests and 28 evaluator tests, including expanded every-allocation-failure traversal, pass inside the 106/106 focused native suite in Debug and ReleaseSafe; exact Zig 0.15.2 full Debug/ReleaseSafe each pass 54/54 steps and 601/601 tests. Native contract schema v3 binds `sass_color.zig`, its focused test, and `legacy-color-core`/`closed-named-colors`/`color-space-equality`/`color-channel-accessors`, passing 9/9 plus check while SCSS/Sass stay private, production-unreachable, and release-fail-closed with zero target package/optional dependencies, external engines, compile child processes, network, downloads, or non-system language libraries. Workflow 5/5 plus check, dependency 4/4 plus check, Zig package 7/7, preprocessor package 6/6 plus check, documentation 7/7 plus check over 38 Markdown files/68 fences, Windows x64 and Linux ARM64 Debug/ReleaseSafe production builds, JavaScript/JSON syntax, formatting, and diff gates pass. Exact Dart Sass 1.101.0 development-oracle probes agree for every embedded named color and the admitted constructor, alpha, serialization, equality, channel, dynamic-preservation, and rejection cases. Exact-main Build `29623125829` passed comprehensive Test Suite `88022008907` and all five native archive/offline-install jobs on `474f7cc61c3d1a46bf17eaddf49d49a110056252`: Linux x64 `88022008900`, macOS x64 `88022008903`, macOS ARM64 `88022008945`, Windows x64 `88022008948`, and Linux ARM64 `88022008950`. Richer color manipulation, modern/wide-gamut color spaces and modules, escaped color identifiers, richer strings/selectors, callables/control flow/modules/imports, diagnostics/dependencies/maps, and differential graduation remain pending. No production dependency, public capability, release interlock, version, tag, artifact, package, deployment, or claim changed. | `474f7cc` |
| `NSASS-011-CALCULATIONS` | `VERIFIED` | Added allocation-free finite number serialization matching the pinned Dart Sass non-inspect precision boundary: shortest decimals remain stable, long fractional forms round to ten places, fuzzy integral/zero normalization is canonical, and compressed output safely omits leading zeroes. Native `calc()`, `min()`, `max()`, and `clamp()` reduce compatible numeric expressions and absolute-unit conversions while preserving valid relative-unit, percentage, custom-property, and modern CSS calculation forms for recovery-disabled validation. Direct `calc()` and `clamp()` enforce CSS dimensional rules; `min()`/`max()` retain pinned Sass unitless comparison behavior; incompatible relative dimensions defer without swallowing undefined variables, allocation/cancellation/resource failures, malformed expressions, non-finite math, or invalid compound values. Function arguments have a separate 4,096 default/65,536 hard ceiling. Deferred-function recognition ignores strings, comments, escapes, and identifier substrings, and only outer calculation argument commas normalize without mutating nested fallbacks. Arity, type, syntax, dimensional mismatch, zero division, spoofing, argument-limit, and every-allocation-failure regressions fail closed with no partial CSS. Five direct numeric tests and 26 evaluator tests pass inside the 101/101 focused native suite in Debug and ReleaseSafe; exact Zig 0.15.2 full Debug/ReleaseSafe each pass 52/52 steps and 596/596 tests. Native contract schema v3 binds `canonical-number-serialization` and `css-calculations`, passing 9/9 plus check while SCSS/Sass stay private, production-unreachable, and release-fail-closed. Workflow 5/5 plus check, dependency 4/4 plus check, Zig package 7/7, preprocessor package 6/6 plus check, documentation 7/7 plus check over 38 Markdown files/68 fences, Windows x64 and Linux ARM64 Debug/ReleaseSafe production builds, formatting, syntax, and diff gates pass. Exact Dart Sass 1.101.0 development-oracle probes agree for admitted reductions, conversions, preserved calculations, arity/type failures, and dimensional edge cases. Exact-main Build `29621956715` passed comprehensive Test Suite `88018612000` and all five native jobs on `f55c1e108f0ed4e2626f6efaffd6421ca3b71f47`: macOS ARM64 `88018612026`, Linux x64 `88018612035`, Windows x64 `88018612040`, Linux ARM64 `88018612047`, and macOS x64 `88018612082`. Colors, richer strings/selectors, callables/control flow/modules/imports, diagnostics/dependencies/maps, and differential graduation remain pending. No production dependency, public capability, release interlock, version, tag, artifact, package, deployment, or claim changed. | `f55c1e1` |
| `NSASS-011-UNITS` | `VERIFIED` | Added a separate private allocation-free Zig numeric algebra with at most 16 distinct unit terms/64 unit instances. It preserves source unit spelling while canonical dimensions convert absolute length (`px`/`in`/`cm`/`mm`/`q`/`pt`/`pc`), angle, time, frequency, and resolution values; unitless arithmetic follows Sass result-unit behavior; equality/ordering compare canonical magnitudes on Dart Sass's exact `1e-11` fuzzy grid; multiplication/division cancel exact or convertible numerator/denominator units with scale correction; modulo converts compatible divisors; division by zero, non-finite values, incompatible relative/custom units, oversized unit sets, and non-CSS compound results fail closed. The evaluator now owns converted addition/subtraction/comparison/equality and safely emits only dimensionless or one-dimensional numbers. The slice also fixes spaced `%` modulo without regressing adjacent percentage units and normalizes conversion noise at fuzzy integral boundaries. Four direct numeric regressions plus 23 evaluator regressions pass inside the 97/97 focused native suite; expanded evaluator allocation injection traverses conversions/cancellation. Exact Zig 0.15.2 Debug and ReleaseSafe each pass 52/52 full steps and 592/592 tests; Windows x64 and Linux ARM64 Debug/ReleaseSafe builds pass. Native contract schema v3 binds both native sources/tests and passes 9/9 plus check while SCSS/Sass remain private, production-unreachable, and release-fail-closed. Workflow 5/5 plus check, dependency 4/4 plus check, Zig package 7/7, preprocessor package 6/6 plus check, documentation 7/7 plus check over 38 Markdown files/68 fences, formatting, and diff gates pass. Exact Dart Sass 1.101.0 development-oracle probes agree for left/right unit conversion, all admitted unit families, unitless arithmetic, modulo, equality/ordering, and compound cancellation. Exact-main Build `29620804773` passed comprehensive Test Suite `88015332231` and all five native jobs on `735e7c1e7d1f796ad84ca20b84b269fd6b36bde3`: Linux x64 `88015332254`, macOS ARM64 `88015332260`, macOS x64 `88015332285`, Windows x64 `88015332287`, and Linux ARM64 `88015332292`. CSS calculations, complete number serialization, colors, richer strings/selectors, callables/control flow/modules/imports, diagnostics/dependencies/maps, and differential graduation remain pending. No production dependency, public capability, release interlock, version, tag, artifact, package, deployment, or claim changed. | `f83fa3d`, `735e7c1` |
| `NSASS-011-VALUES` | `VERIFIED` | Extended the private zero-runtime-dependency Zig Sass evaluator with token-bounded `!default`/`!global` assignment semantics, null-default behavior, global propagation, duplicate/trailing modifier rejection, quote isolation, and the upstream-compatible new-global warning. The owned value store now receives recursively bounded space/comma/slash/bracket lists and ordered nested maps; map keys are Sass-compared, duplicates fail closed, null list items are omitted during CSS rendering, and maps/empty unbracketed lists/callables cannot escape as invalid CSS or interpolation. Native `map-get()` supports nested keys and null misses; `nth()` handles lists, maps, scalars, positive/negative indexes, and bounds; `length()` handles lists/maps/scalars. Short-circuit `or`/`and`, unary `not`, equality/inequality, numeric ordering, and smart comma/space-list precedence are evaluated natively. Recursive expression depth, token, operation, temporary, value, and collection ceilings remain enforced; cancellation and any allocation failure poison private staging. Twenty-one evaluator regressions, including expanded all-allocation-failure traversal, pass inside the 91/91 focused native suite. Exact Zig 0.15.2 Debug and ReleaseSafe each pass 50/50 full steps and 586/586 tests; native contract schema v3 passes 9/9 plus check while keeping SCSS/Sass private, production-unreachable, and the target production boundary at zero package/optional dependencies, external engines, child processes, network, downloads, and non-system dynamic language libraries. Workflow 5/5 plus check, dependency 4/4 plus check, Zig package 7/7, preprocessor package 6/6 plus check, documentation 7/7 plus check over 38 Markdown files/68 fences, JavaScript syntax, formatting, and diff gates pass. Exact Dart Sass 1.101.0 development-oracle probes agree with the positive modifier, collection, accessor, logical/comparison, and mixed list-precedence fixtures. Exact-main Build `29619866031` passed comprehensive Test Suite `88012594791` and all five native jobs on `43928586e456a5ebb124a1584f6416e7b638494d`: Linux ARM64 `88012594778`, Linux x64 `88012594795`, macOS ARM64 `88012594826`, Windows x64 `88012594852`, and macOS x64 `88012594895`. Full unit conversion/calculation/color/string/selector behavior, callable/control flow/module/import coverage, normalized diagnostics/dependencies/maps, corpus differential/fuzz/concurrency graduation, public routing, and provider removal remain pending; no production dependency, public capability, release interlock, version, tag, artifact, package, deployment, or claim changed. | `4392858` |
| `NSASS-011` | `IN_PROGRESS` | Added the first private self-contained Sass semantic core in Zig over the verified parser, persistent environment, owned values, confined transaction, and recovery-disabled CSS validator. Both SCSS and indented Sass now share one implementation for ordered lexical variables with Sass underscore/hyphen canonicalization, booleans/null/quoted and unquoted strings, interpolation, simple finite numeric `+`/`-`/`*`/`/`/`%` arithmetic with units, literal slash-list preservation versus forced division, Cartesian selector lists including repeated-parent cross products, parent `&` replacement, descendant nesting, cascade-preserving declaration chunks around nested rules, Sass custom-property interpolation behavior, nested properties, loud-comment preservation, complete private staging, and coarse frontend mappings. Unsupported directives and variable modifiers fail closed rather than passing through. Independent selector/count/byte, expression-token, temporary-byte, value, environment, evaluation-depth, transaction-operation/output/diagnostic/map, and core-result ceilings remain enforced; cancellation and every evaluator allocation failure poison the transaction and expose no result. Allocation injection found and closed an ownership leak when a completed nested selector could not be appended. Fifteen focused semantic regressions pass; the complete native frontend step is 85/85. Exact Zig 0.15.2 Debug and ReleaseSafe each pass 50/50 steps and 580/580 tests; the evaluator semantically cross-compiles for Windows x64 and Linux ARM64 in both modes. Native contract schema v3 binds the separate `NSASS-011` implementation and keeps SCSS/Sass `reference-only`, public-unavailable, production-unreachable, and release-fail-closed; 9/9 plus check pass. Workflow 5/5 plus check, dependency 4/4 plus check, Zig package 7/7, preprocessor package 6/6 plus check, documentation 7/7 plus check over 38 Markdown files, JavaScript/JSON syntax, formatting, and diff gates pass. Exact Dart Sass 1.101.0 development-oracle probes match the positive semantic outputs used by the native tests, including the mixed-declaration, repeated-parent, slash, and custom-property edge cases. Exact-main Build `29618466243` passed Test Suite `88008488536` and all five native jobs on `639ca4cdb4dd893ee001cec396563cefd3cb4967`: Linux ARM64 `88008488572`, macOS ARM64 `88008488576`, macOS x64 `88008488586`, Linux x64 `88008488620`, and Windows x64 `88008488623`. Lists/maps, full calculation/color/selector behavior, mixins/functions/content, control flow, modules/imports/built-ins, full diagnostics/dependencies/maps, and corpus/differential/fuzz/concurrency graduation remain pending, so no provider, production dependency graph, public API, package, capability row, version, release interlock, tag, artifact, or public claim changed. | `639ca4c` |
| `NSASS-010` | `VERIFIED` | Added a private self-contained Zig parser for SCSS and indented Sass over the verified lossless lexer and immutable syntax foundation. It builds one complete owned document or fails terminally with no partial tree; preserves exact source spans and raw composite text; classifies variables, declarations, selectors, nested property blocks, style blocks, strings, comments, interpolation, mixins/functions, control flow, returns/content, imports/modules, and generic at-rules; handles optional final SCSS semicolons; and derives indented-Sass blocks from physical columns while retaining delimiter, comma/operator, and directive-prelude continuations. Input, token, indentation, delimiter, statement, node, edge, depth, diagnostic, cancellation, and allocation failures are bounded and fail closed. Fifteen parser regressions cover SCSS/Sass structure, ownership after caller mutation, ambiguity, multiline indentation, deterministic replay, malformed input, UTF-8, inconsistent indentation, independent ceilings, invalid limits, both cancellation phases, and every allocation failure. The native parser accepts all 60 pinned successful Sass-spec entries and rejects seven pinned lexical/delimiter failures; the remaining evaluation and complete differential obligations stay in `NSASS-011`/`NSASS-012`. Focused Debug passes 70/70; exact Zig 0.15.2 Debug and ReleaseSafe each pass 48/48 steps and 565/565 tests. Windows x64 and Linux ARM64 semantic cross-compiles pass in Debug and ReleaseSafe. Native contract schema v3 binds the exact implementation/test inventory, proves it unavailable and production-unreachable, retains five rows with SCSS/Sass `reference-only`, and passes 9/9 plus check. Workflow 5/5 plus check, dependency 4/4 plus check, Zig package 7/7, preprocessor package 6/6 plus check, documentation 7/7 plus check over 38 Markdown files, JavaScript/JSON syntax, formatting, and diff gates pass. Exact-main Build `29616810952` passed Test Suite `88003569523` and all five native jobs on `3e6f46adbe37c6d40f4fb0f12b501d30720d1303`: macOS x64 `88003569505`, macOS ARM64 `88003569509`, Windows x64 `88003569521`, Linux ARM64 `88003569529`, and Linux x64 `88003569562`. No provider, dependency, production API, package, capability, version, tag, release, or public claim changed. | `3e6f46a` |
| `NATIVE-005` | `VERIFIED` | Added one private single-owner Zig evaluation transaction over the verified semantic foundation and confined resolver. Language evaluators can charge operations, loops, calls, diagnostics, mappings, and generated bytes; append only normalized-LF valid UTF-8; stage at most 20 MiB; retain exact generated UTF-16 positions; and explicitly abort. Every mutating failure is terminal, clears private output, and closes its resolver session. Finish rejects native errors, unbalanced calls, cancellation before/after core phases, all recovered/core diagnostics, oversized diagnostic sets, and independently capped 40 MiB validated CSS/maps; no staged bytes are observable. A successful commit deep-owns native diagnostics and related labels, ordered dependencies/edges/stats, the frontend map, and the existing core's CSS/diagnostics/map beyond evaluator, resolver, and source-table cleanup. Twelve evaluator regressions cover deterministic replay, ownership after caller mutation/deinit, candidate misses, malformed output, all budget classes, late cancellation, core rejection with retained diagnostics, independent result ceilings, invalid limits, closed sessions, explicit abort, and every allocation failure across resolver, staging, core validation, cloning, maps, and result cleanup. Focused Debug and ReleaseSafe pass 55/55; full exact Zig 0.15.2 suites each pass 46/46 steps and 550/550 tests; Windows x64 and Linux ARM64 semantic cross-compiles pass. Contract schema v2 binds four foundation inventories and passes 8/8 plus check; workflow 5/5 plus check, dependency 4/4 plus check, Zig package 7/7, preprocessor package 6/6 plus check, documentation 7/7 plus check over 38 Markdown files, JavaScript/JSON syntax, formatting, and diff gates pass. Exact-main Build `29614839695` passed Test Suite `87997549492` and all five native jobs: Linux ARM64 `87997549514`, macOS x64 `87997549521`, Windows x64 `87997549531`, macOS ARM64 `87997549541`, and Linux x64 `87997549557`. Adapter rows, provider-backed product bytes, and fail-closed native release readiness remain unchanged. | `90947e6` |
| `NATIVE-004` | `VERIFIED` | Added a synchronous single-owner Zig resolver for concrete candidates under at most 64 explicit canonical directory capabilities. Strict bounded `file:` URLs reject schemes, authorities, credentials, query/fragment aliases, malformed escapes, encoded separators/NUL, Windows network shares, and lexical/canonical escapes. Every descendant component and final file is opened no-follow; directory, parent, special-file, unreadable, symlink, root-replacement, canonical-handle mismatch, and unstable size/identity/time paths fail without dependency admission. Volume/inode identity, exact positional reads, 10 MiB/file, 40 MiB/session, 4,096 unique files, 8,192 attempts, 128 hard/64 default depth, root/URL/path limits, cancellation checkpoints, ancestry and graph-level cycles, ordered first-success facts/edges, duplicate reads, owned results, session closure, deterministic replay, Unicode URL round-trips, and every allocation failure are executable. Initial exact-main Build `29612525873` exposed Windows overlapped EOF behavior; `6ee67d7` handles the kernel completion result directly and owns an empty-file boundary regression. Replacement `29613017436` exposed a valid high-bit signed macOS ARM64 device identity; `9e0cab9` preserves its raw bits before widening. Final exact-main Build `29613305552` passed Test Suite `87992721006` and all five native jobs: Windows x64 `87992721100`, Linux ARM64 `87992721101`, macOS x64 `87992721115`, Linux x64 `87992721126`, and macOS ARM64 `87992721128`. Fourteen resolver tests plus lexer/foundation pass 43/43 in Debug and ReleaseSafe; full exact Zig 0.15.2 suites each pass 44/44 steps and 538/538 tests. Native contract 8/8 plus check, workflow 5/5 plus check, dependency 4/4 plus check, Zig package 7/7, preprocessor package 6/6 plus check, documentation 7/7 plus check over 38 Markdown files, formatting, and diff gates pass. Adapter rows and release readiness remain unchanged. | `fa4ee99`, `6ee67d7`, `9e0cab9` |
| `NATIVE-003` | `VERIFIED` | Added seven private Zig modules for owned source identities/spans/UTF-16 positions, immutable acyclic syntax documents, deep-owned typed values, persistent deterministic lexical environments, monotonic evaluation budgets, bounded structured diagnostics, and ordered multi-source Source Map segments/VLQ encoding. All input-derived collections have explicit count/depth/byte ceilings; malformed IDs/spans/order/UTF-8/non-finite values fail closed; no hash iteration, recursion beyond the value-depth gate, language runtime, external process, network, or runtime data is used. Fourteen focused regressions cover ownership after caller mutation, structural comparison, snapshot isolation/shadowing, independent ceilings, multi-source UTF-16 mappings, ordering/lookup, and a combined every-allocation-failure path. Together with the lexer, focused Debug and ReleaseSafe pass 29/29; full suites each pass 42/42 steps and 524/524 tests. Contract schema v2 binds both source/test inventories. Exact integrated-main Build `29610608006` passed Test Suite `87984110033` and all five native jobs: Windows x64 `87984109992`, Linux ARM64 `87984109996`, macOS x64 `87984110014`, macOS ARM64 `87984110043`, and Linux x64 `87984110054`. Adapter rows and public behavior remain unchanged. | `f6d3f74` |
| `NATIVE-002` | `VERIFIED` | Added an internal allocation-free Zig token stream for CSS, SCSS, indented Sass, Less, and Stylus with exact original-byte spans; explicit LF/CR/CRLF, horizontal trivia, virtual indent/dedent, identifiers/variables/numbers, segmented strings, nested Sass/Less interpolation, syntax-aware comments, delimiters, and deterministic longest-match operators. Unterminated strings/comments/interpolations are explicit and byte-preserving. Source, token, indentation, interpolation, nesting, and tab-width limits are validated; fixed-capacity stacks prevent input-driven allocator or recursion growth. The materializing helper has complete injected allocation-failure coverage while streaming proves zero allocation. Fifteen focused regressions include all 256 byte values across all five syntax modes, significant/blank indentation, interpolation isolation from comments, escapes/continuations, malformed bytes, fail-closed ceilings, deterministic reruns, and idempotent EOF. Build exposes a dedicated internal test step, full Debug and ReleaseSafe each pass 40/40 steps and 510/510 tests, and schema-v2 contract/workflow gates pass 8/8 and 5/5. Exact integrated-main Build `29609313443` passed Test Suite `87979961672` and all five native jobs (`87979961546`, `87979961562`, `87979961601`, `87979961606`, `87979961753`). Adapter rows and public behavior remain unchanged. | `71b4a13` |
| `NATIVE-001` | `VERIFIED` | Owner cancelled the provider-backed rc.1 release before tag creation and accepted ADR-013. Plan v1.2 adds dependency-ordered Milestone 10 targeting `0.6.0`: shared native lexer/syntax/value/resolver/evaluator foundations, separate Sass/Less/Stylus parser/evaluator/corpus packages, product migration, zero-dependency packaging, public graduation, and a final native release gate. The closed schema-v1 contract owns five ordered rows, exact development-only oracles, zero target production/optional dependencies, zero external engines/compile child processes/network/runtime downloads/non-system language libraries, current versus target states, native source inventories, owner packages, and release identity. Six regressions reject boundary widening, premature native/release claims, reference drift, invalid tags, and release-workflow bypass; the exact check passes. Build CI owns the focused gate, while tag release invokes it before npm authentication and rejects all tags until graduation. README and format guidance disclose the cancelled candidate and migration. Exact integrated-main Build run `29608089256` passed Test Suite job `87976061585` and five native platform jobs (`87976061605`, `87976061606`, `87976061608`, `87976061625`, `87976061638`); Documentation run `29608089326` passed build `87976061977` and deploy `87976165254`. Production behavior and current package bytes remain unchanged. | `6db7320` |
| `PRE-001` | `VERIFIED` | Owner-approved canonical integration program. Accepted ADR-012 pins Dart Sass `sass` 1.101.0/MIT, Less 4.6.7/Apache-2.0, and Stylus 0.64.0/MIT behind `zigcss-preprocessor-v1`; separates trusted project-code extensions; and keeps every adapter unavailable until independent graduation. Plan v1.1 adds dependency-ordered Milestone 9, `0.5.0` stages, host/adapter/product packages, security and full-language criteria, and an exact autonomous sequence parallel to `BENCH-007`. Schema-v2 matrix/provider validation and human documentation are executable. Focused ADR/matrix tests pass 24/24; the live format gate passes 8 adapters, 9 rejected extension probes, complete source coverage, and CSS Modules evidence. Full site tests pass 137/137 with production build; documentation integrity, version/release metadata, workflow/dependency policy, JSON/JavaScript/Bash/diff checks pass. No compiler behavior or public availability changed. | `195429d` |
| `PRE-002` | `VERIFIED` | Added one-request-per-process `zigcss-preprocessor-v1`: exact four-byte big-endian JSON frames, closed provider/syntax/options schemas, 10 MiB source and 20 MiB output ceilings, exact success-or-css-free-failure responses, production IDs without installed implementations, provider abort/deadline handling, and sanitized errors. The outer supervisor uses absolute Node/host paths, `shell: false`, framed stdin, no ambient `PATH`/`HOME`/`NODE_OPTIONS`/`NODE_PATH`, child-runtime environment cleanup, bounded stdout/stderr/time, close-before-resolution, and request-ID verification. A hermetic fake host proves metacharacters are data, provider throw/timeout/invalid/overflow behavior, real child timeout kill, stdout flood, any stderr, malformed/extra/wrong-ID response, nonzero exit, symlink-host rejection, and environment containment. Fourteen focused tests pass and CI owns the gate. Debug and ReleaseSafe each pass 36/36 steps and 491/491 tests; full site tests pass 137/137 with production build; workflow, release version/metadata, dependency, format, seven documentation-integrity tests over 32 Markdown files, syntax, generated-state, and diff checks pass. The npm dry run remains exactly the five rc.3 files, so the unfinished host is not published and all preprocessors remain unavailable. | `0f0134e` |
| `PRE-003` | `VERIFIED` | Added a provider-independent loader for concrete canonical local candidates without reimplementing language-specific import search. It accepts only absolute `file:` URLs under explicit regular directory roots; rejects schemes, credentials, query/fragment aliases, encoded separators, lexical/canonical escapes, every file or parent symlink, missing/unreadable paths, directories/special files, invalid kinds/options/roots/limits, cycles, and excessive depth; opens canonical files read-only/no-follow; and validates stable inode/size/time identity around a bounded positional read. Per-session ceilings cover 10 MiB/file, 40 MiB cumulative bytes, 4,096 unique files, 8,192 attempts, and 64 default ancestry levels. Results own bytes and canonical URLs; first-success dependencies are deterministic and deduplicated while repeated reads still consume budgets. Nine resolver and fourteen host tests pass, and separate CI steps own both gates. Debug and ReleaseSafe each pass 36/36 steps and 491/491 tests; full site tests pass 137/137 with build; workflow, format, release version/metadata, dependency, seven documentation-integrity tests over 33 Markdown files, syntax, generated-state, and diff checks pass. npm remains exactly five rc.3 files; no provider, format, or public claim is enabled. | `826edd6` |
| `PRE-004` | `VERIFIED` | Added closed normalization for exact Dart Sass/Less/Stylus diagnostic shapes, ANSI/control sanitization, canonical local source identities, severity/location/code limits, deterministic first-seen dependency facts, and owned provider failures. The wire now permits validated diagnostics on provider failures but never CSS or partial results; generic and oversized failures collapse to bounded diagnostic-free host errors. Added a dependency-free strict non-indexed Source Map v3 parser/composer with safe-integer VLQ bounds, exact schema/index/order validation, one-intermediate-source ownership, greatest-lower-bound tracing, deterministic encoding, and preservation of UTF-16 columns, generated/unmapped regions, signed cross-line deltas, names, source roots, and source content. Thirteen metadata/map, sixteen host, and nine resolver tests pass in separately ordered CI gates. Debug and ReleaseSafe each pass 36/36 steps and 491/491 tests; the site passes 137/137 plus production build; documentation integrity covers 33 Markdown files; format, compatibility, transform, workflow, version, release-metadata, capability, package, wrapper, prefix, dependency, syntax, formatting, generated-state, and diff gates pass; live production audits are 0/0/0. The npm dry run remains exactly five rc.3 files and excludes the unfinished host; no provider or public format row changed. | `833bb3f` |
| `SASS-010` | `VERIFIED` | Pinned `sass` 1.101.0/MIT as an internal development dependency and connected one statically registered modern `compileStringAsync` provider for protocol `scss` and `sass` (`indented`) syntax. Exact expanded/compressed output, provider maps, warning/error spans, built-in `sass:` modules, pre/mid-flight cancellation ownership, repeated and 12-way mixed parallel determinism, end-to-end framed host success/failure, and no-partial-CSS behavior are executable. Measurement proved a normal file entry URL silently enables Dart Sass's adjacent-filesystem importer, so this stage uses a stable non-file virtual entry, a per-compilation rejecting importer, result/map identity checks, and a spoof-resistant import signal; adjacent, absolute, dynamic `meta.load-css`, and nonempty load-path probes all fail before CSS. The 24-package transitive/optional closure is exact and license-reviewed (MIT, Apache-2.0, BSD-3-Clause); the provider requires future Node >=20.19.0, but stays dev-only and excluded from the public package until `PRE-006`. Ten Sass, sixteen host, nine resolver, and thirteen metadata/map tests pass; a clean `npm ci --ignore-scripts` passes and both full and production advisory reads report zero findings. Debug and ReleaseSafe each pass 36/36 steps and 491/491 tests; the site passes 137/137 plus build; documentation, workflow, format, version, release, dependency, compatibility, transform, package, wrapper, prefix, syntax, formatting, generated-state, and diff gates pass. npm remains five rc.3 files/36,246 unpacked bytes; SCSS and Sass public rows remain unavailable/unverified. | `6fb6bd2` |
| `SASS-011-OPTIONS` | `VERIFIED` | Extended the closed wire with exact provider-owned option objects. Dart Sass accepts only boolean `charset`, `quietDeps`, and `verbose`; Less and Stylus accept only `{}` until their adapters define reviewed fields. Missing, extra, cross-provider, non-boolean, and executable-extension fields fail at host validation, and the adapter independently validates its exact object. Dart Sass receives those booleans directly and always disables colored alerts; a byte-exact UTF-8 regression proves `charset` controls the compressed-output BOM. The documentation publishes the exact boundary. All 49 preprocessor host, resolver, Sass, diagnostics, dependency, and source-map tests pass with diff checks. Imports and public format admission remain deliberately unavailable pending the rest of `SASS-011`. | `91321aa` |
| `SASS-011` | `VERIFIED` | Added a full custom Dart Sass importer whose canonical identities never grant native entry-file or load-path authority. Every dependency candidate is read through one `PRE-003` session; entry-relative lookup is allowed only when the source URL is already inside an explicit root, ordered global importers retain caller load-path precedence and correct `quietDeps` ownership, and provider-owned SHA-256 canonical identities preserve ancestry without exposing filesystem reads to Dart Sass. Resolution reproduces partial/full ambiguity groups, `.sass`/`.scss` before `.css`, explicit extensions, indexes, and legacy `.import` precedence for `@use`, `@forward`, `@import`, and dynamic `meta.load-css`. First-seen canonical dependency facts use `reference` for the public API's indistinguishable module loads and `import` for legacy loads. Imported warning/error spans, source content, entry/dependency map identities, Unicode two-stage composition, charset/quietDeps/verbose behavior, cancellation cleanup, serial/repeated/eight-way parallel determinism, and the real framed host all pass. Negative coverage includes missing and malformed source URLs, root requirement/order, partial/extension ambiguity, network and unknown schemes, lexical/canonical escape, absolute URLs outside roots, links, unreadable and oversized files, invalid UTF-8, cycles, and no-partial-CSS ownership. A red 65-level callback failure amplified Dart Sass memory before a controlled terminal error, so the adapter safely lowers `PRE-003` ancestry to 32 and rejects there deterministically. Resolver candidate probing now distinguishes absent directories/non-directory parents from special-file failures without weakening confinement. Twenty-three Sass, sixteen host, nine resolver, and thirteen metadata/map tests pass (61 total). Debug and ReleaseSafe each pass 36/36 steps and 491/491 tests using exact checksum-verified Zig 0.15.2 with the macOS 15.4 SDK isolation. The site passes 137/137 plus production build; documentation integrity covers 33 Markdown files and 64 fences; format, compatibility, transform, workflow, dependency, capability, version, release-metadata, JavaScript, and diff gates pass. npm remains exactly five rc.3 files/36,246 unpacked bytes and excludes the host. SCSS/Sass public rows remain unavailable pending `SASS-012` and later product/package graduation. | `1414070`, `6277a06` |
| `SASS-012` | `VERIFIED` | Pinned official Sass-spec revision `1b03109a6205c8cff146defeae8488094b147c88` as a reviewed 80-case/175-file corpus with exact archive, tree, manifest, selection, and license checksums. The corpus covers 41 SCSS and 19 indented-Sass successes plus 13 SCSS and seven indented-Sass failures across 33 positive and negative language categories. Direct differential tests require byte-exact provider CSS, failure, diagnostic, source, and dependency order; eight-way reruns prove determinism. Every one of the 60 successful outputs is parsed and emitted twice through ZigCSS for idempotence and reparsed by pinned Lightning CSS with error recovery disabled. A 1,001-warning case fails at the 1,000-diagnostic ceiling without CSS. Combined host, resolver, metadata, and Sass tests pass 65/65; Debug and ReleaseSafe each pass 36/36 steps and 491/491 tests. The site passes 137/137 plus production build; documentation integrity covers 34 Markdown files and 65 fences. Format, compatibility, transform, workflow, dependency, capability, prefix, version, release, package, wrapper, VS Code, both supported Neovim versions, syntax, audits, and diff gates pass. Production audits are 0/0/0 and npm remains exactly five files/36,246 unpacked bytes, excluding the private host and corpus. The autonomous pass explicitly sequences Sass, Less, Stylus, then product graduation. Public SCSS/Sass rows remain unavailable until `PRE-005`, `PRE-006`, and `PRE-008`. | `219f373`, `9f5112f`, `405916d` |
| `LESS-010` | `VERIFIED` | Pinned exact `less` 4.6.7/Apache-2.0 plus its 15-package transitive/optional closure with registry integrity, license, engine, and no-lifecycle assertions. Added one static canonical `less.render` provider for protocol `less`, with byte-exact expanded/compressed output, deterministic Source Map v3 entry ownership, normalized CSS-free failures, pre/post cancellation checks, repetition, 12-way parallel determinism, and a real framed-process success. The provider never sees the request's entry filename: one stable virtual entry and a highest-priority per-request file manager reject asynchronous and synchronous reads, including adjacent/absolute/optional/inline/network imports, `data-uri`, and image functions; suppressed optional reads are rejected after render. Plain CSS imports emit without I/O. Nonempty load paths fail until `LESS-011`. `javascriptEnabled: false` and `disablePluginRule: true` prevent inline JavaScript and `@plugin` execution; exact request fields prohibit injected executable options. Ten Less plus 16 host, nine resolver, 13 metadata/map, and 27 Sass tests pass (75 total) after a clean lifecycle-disabled install. Debug and ReleaseSafe each pass 36/36 steps and 491/491 tests. The site passes 137/137 plus build; documentation integrity passes over 34 Markdown files/65 fences. Format, compatibility, transform, workflow, dependency, capability, prefix, version, release, package, wrapper, VS Code, Neovim 0.11.7/0.12.4, autodevelop 47/0, syntax, audit, formatting, and diff gates pass. Full install and production audits are zero; npm remains exactly five public files/36,348 unpacked bytes and excludes the host. The public Less row remains unavailable pending `LESS-011`, `LESS-012`, `PRE-005`, `PRE-006`, and `PRE-008`. | `061cc10` |
| `LESS-011` | `VERIFIED` | Added one confined Less file manager whose asynchronous stylesheet loads and supported synchronous asset reads all pass through one serialized `PRE-003` session. Canonical load-path and entry-relative order, extension inference, nested and absolute-inside-root imports, reference/inline/optional/multiple/forced Less/CSS behavior, convergent ancestry, URL rewriting, dependencies, imported diagnostics, provider maps, cancellation, and repeated/eight-way determinism are executable. Missing inputs remain distinct from policy failures. Query/fragment aliases, encoded separators/NUL, schemes/network, UNC/package prefixes, escapes, every file or directory symlink, unreadable/special/unstable files, invalid stylesheet UTF-8, cycles, excessive ancestry, bytes, files, attempts, and diagnostics fail closed without partial CSS or internal sentinels. Closed options are exactly `math`, `quietDeprecations`, `rewriteUrls`, and `strictUnits`; JavaScript and plugins stay disabled. `data-uri()` and image size helpers use resolver-owned buffers, with exact internal `image-size` 0.5.5 rather than native project-path reopening. Twenty-three Less, 11 resolver, 16 host, 13 metadata/map, and 27 Sass tests pass (90 total). Exact Zig 0.15.2 Debug and ReleaseSafe each pass 36/36 steps and 491/491 tests; all product, documentation, editor, package, release, benchmark-policy, audit, syntax, formatting, and deterministic-output gates pass. npm remains exactly five public files/36,375 unpacked bytes and excludes the private host/importer. Public Less remains unavailable pending `LESS-012`, `PRE-005`, `PRE-006`, and `PRE-008`. | `b447c7f` |
| `LESS-012` | `VERIFIED` | Pinned canonical Less 4.6.7 test data from official `less/less.js` revision `8ae2cc3bfa79f0718ad6fe5f263a1d6819fe9d5c`, tree `77299b2e4390d6e2b8592d6ca2dbb495189149b1`, and archive SHA-256 `9c53e2e65ce1b73fb192e735d3b267c590139212bc76b88151ec546793260577`. The reviewed snapshot owns 88 cases/217 exact files under Apache-2.0: 68 exact CSS successes and 20 exact parser/evaluator failures across at-rules, calculations, colors, imports, functions, layers, mixins, nesting, operations, selectors, strings, variables, and negative language behavior. Direct Less CSS, official formatted errors, normalized diagnostics, sources, explicit complete dependencies, and eight-way adapter reruns agree exactly. Every success parses with recovery disabled, round-trips twice through ZigCSS, and reparses independently. The differential exposed ancestry-derived virtual filenames breaking ordinary once-only import deduplication across parent branches; canonical URL filenames plus per-invocation ancestry contexts now preserve both default deduplication and convergent `(multiple)` behavior. Less passes 27/27; the combined preprocessor gate passes 94/94. Exact Zig Debug/ReleaseSafe each pass 36/36 and 491/491; all main/package/example/benchmark, product, documentation, editor, release, syntax, formatting, generated-state, diff, audit, and packaging gates pass. Documentation integrity covers 35 Markdown files/66 fences; production and full audits are zero; npm remains exactly five files/36,375 unpacked bytes and excludes every private provider/corpus file. Public Less remains unavailable pending `PRE-005`, `PRE-006`, and `PRE-008`; Stylus is next. | `e1f6e3d` |
| `STYLUS-010` | `VERIFIED` | Pinned exact `stylus` 0.64.0/MIT as an internal development dependency and integrity/license-reviewed its complete 47-package resolved closure (MIT, ISC, BSD-3-Clause, and BlueOak-1.0.0) with no install lifecycle scripts. Added one static callback-based `renderer.render` provider behind an owned Promise boundary. A stable non-file virtual entry admits exactly Stylus's immutable package-owned `functions/index.styl`; every source-authored relative, absolute, CSS, URL, or network import and every nonempty load path fails before CSS pending `STYLUS-011`. `json()`, `image-size()`, and `embedurl()` cannot read project paths; direct or aliased `use()` cannot execute plugins; request fields cannot inject functions, globals, evaluators, plugins, imports, paths, or hooks. `warn()`, `p()`, and `trace()` become ordered owned diagnostics instead of corrupting framed stdout/stderr. Exact expanded/compressed output includes the canonical standard library; maps own only the virtual entry and request source bytes; formatted errors lose excerpts, internal paths, and stacks while retaining one-based locations. Ten Stylus and 94 shared tests pass (104 total), including the 1,000-diagnostic ceiling, pre/post cancellation, 12-way determinism, and real framed success. A lifecycle-disabled clean install audits 82 packages with zero findings. Exact Zig Debug/ReleaseSafe each pass 36/36 and 491/491; main builds, package consumers, complete examples, benchmark smoke, all product/release/workflow/benchmark/editor/autodevelop gates, 137/137 site tests/build, 35-file/66-fence documentation integrity, syntax/format/diff checks, and both supported Neovim versions pass. Production audits are 0/0/0 and the full audit is zero. npm remains exactly five public files/36,484 unpacked bytes and excludes the provider. Public Stylus remains unavailable pending `STYLUS-011`, `STYLUS-012`, `PRE-005`, `PRE-006`, and `PRE-008`. | `fc0c7bf` |
| `STYLUS-010-HARDENING` | `VERIFIED` | A source-level red regression exposed that Stylus's default parser cache is module-global. The provider now passes `cache: false`, preventing request ASTs and source text from surviving across framed compilations while preserving canonical rendering. Focused Stylus tests pass 10/10, protocol tests pass 16/16, and JavaScript syntax, documentation integrity, and diff checks remain green. | `b8f3b64` |
| `STYLUS-011` | `VERIFIED` | Measured Stylus 0.64.0 directly before admitting confined imports. One custom evaluator and import authority preserve entry/nested/load-path precedence, optional `.styl`, directory index and same-name lookup, confined package `main`, sorted bounded filename globs, ordinary repeated imports, `@require` once-only behavior, local CSS inclusion, and literal CSS/URL imports. Every project stylesheet, package manifest, JSON, image, and embedded asset byte traverses one serialized `PRE-003` session; no provider-native project read, shell, glob CLI, child process, network, plugin, or injected hook is available. Exact lock-bound patched `glob` 10.5.0 is used only as a library for single-directory filename expansion; recursive/directory-segment globs stay fail-closed. Closed booleans `includeCss` and `hoistAtrules`, resolver-backed `json()`, `image-size()`, and `embedurl()`, ordered dependency facts, imported diagnostics/maps, two-stage map composition, cancellation, UTF-8/JSON/image/glob/byte/count/depth/cycle limits, CSS-free failures, eight-way determinism, and real framed imports pass. Stylus passes 20/20 and the combined preprocessor suite passes 114/114. Exact Zig Debug/ReleaseSafe each pass 36/36 and 491/491; main builds, package consumers, examples, benchmark smoke, all product/release/workflow/benchmark/editor/autodevelop gates, 137/137 site tests/build, 35-file/66-fence documentation integrity, syntax/format/diff checks, and Neovim 0.11.7/0.12.4 pass. Clean lifecycle-disabled install, production audits 0/0/0, and the full audit report zero findings. npm remains exactly five files/36,484 unpacked bytes and excludes the private host/importer. Public Stylus remains unavailable pending `STYLUS-012`, `PRE-005`, `PRE-006`, and `PRE-008`. | `5c9df0d` |
| `STYLUS-012` | `VERIFIED` | Pinned official Stylus 0.64.0 tag/revision `1086c6c1fbd7a7fd0ce9ad94f6cf4a62fc79a6e9`, tree `295654c66ed48ff47a4aeca95b1935edf7a612ba`, archive SHA-256 `140a722893f0f05b501a01ce1ff27042b6d7c0769ad746f4b5d04429ec8d429e`, and MIT license evidence. The reproducible manifest owns 346 cases and 816 exact files: 326 admitted official successes plus 20 project-owned exact diagnostic failures. Twenty-nine named official exclusions cover executable extension/custom-resolver callbacks, an undocumented renderer prefix option, and legacy generated CSS rejected by strict Lightning CSS; they are explicit and never treated as passing compatibility. Direct provider CSS/failure/diagnostic/source/dependency results agree exactly and eight workers reproduce them deterministically. Every success parses with recovery disabled, round-trips twice through ZigCSS, and reparses independently. The differential exposed missing recursive/directory glob semantics and package `.styl` fallback; bounded `globIterateSync` now checks confined static roots, rejects traversal/symlink escape, uses no shell or child process, caps depth and matches, sorts results, and routes every matched file through `PRE-003`. Stylus passes 23/23 and the complete preprocessor matrix passes 117/117 after a clean lifecycle-disabled install. Exact Zig Debug/ReleaseSafe each pass 36/36 and 491/491; all main/package/example/benchmark, product, documentation, editor, release, syntax, formatting, generated-state, diff, audit, and packaging gates pass. Documentation integrity covers 36 Markdown files and 67 fences; production audits and the full audit are zero; npm remains exactly five files/36,484 unpacked bytes and excludes every private provider/corpus file. Public SCSS, Sass, Less, and Stylus remain unavailable pending `PRE-005`, `PRE-006`, and `PRE-008`. | `d184914` |
| `PRE-005` | `VERIFIED` | Added a closed npm-facing `compileString`/`compileFile` API and product CLI for CSS plus exact Dart Sass 1.101.0 SCSS/indented Sass, Less 4.6.7, and Stylus 0.64.0. One length-prefixed `zigcss-core-v1` process boundary validates all generated CSS with recovery disabled and owns bounded source, response, diagnostics, dependencies, maps, timeouts, cancellation, stderr, request ID, and no-partial-result behavior. File loads reuse `PRE-003`; provider maps compose through the Zig emitter. The installed launcher detects only admitted extensions or explicit syntax while native Zig remains CSS-only. CLI coverage owns stdin/stdout, files, confined load paths, inline maps, minification/optimization closure, deterministic stem collisions, compile-before-commit batches, atomic files, a 4,096-task queue with at most eight workers, cancellation, exit codes, and exact terminal signals. Watch hashes one directly compiled root snapshot, deduplicates at most 4,096 confined dependencies under the 40 MiB resolver budget, retains the last good output, reports unchanged failures once, recovers after deletion/restoration, and passes every admitted syntax. Twenty-nine product, 40 common host/resolver/map, and 77 canonical adapter tests pass (146 distinct tests). Debug and ReleaseSafe each pass 38/38 steps and 495/495 tests; site 137/137 plus production build, documentation integrity 7/7 over 36 Markdown files/67 fences, workflow/version/release/dependency/prefix/capability/Zig-package/wrapper/format/compatibility/transform gates, JavaScript/Zig formatting and diff checks, and live production audits 0/0/0 pass. CI owns the product suite. The npm dry run remains exactly five files/38,030 unpacked bytes and deliberately excludes this private host/API pending `PRE-006`; no public format row changed. | `09dc46d` |
| `PRE-006` | `VERIFIED` | Promoted the verified API, product CLI, host, resolver, maps, providers, and canonical engines into one exact 28-file npm archive under Node >=20.19.0. Five production dependencies, an 86-package exact lock closure, five reviewed licenses, lifecycle/deprecation exceptions, runtime imports, archive inventory/size, installed inventory, five native targets, workflow provenance, SPDX 2.3 SBOM, and third-party notices are fail-closed and reproducible. Provider children deny HTTP(S), sockets, DNS, UDP, `fetch`, and WebSocket at runtime. The final local matrix passed 150/150 preprocessor tests after hosted remediation, 495/495 Zig tests in both modes, 79/79 policy tests, 137/137 site tests plus build, 7/7 documentation-integrity tests, zero full and production audit findings, exact 28-file npm pack (90,761 packed/400,960 unpacked bytes), and a real macOS ARM64 lifecycle-disabled clean-install plus offline postinstall/four-format/API smoke. Hosted run `29551818190` exposed mixed Windows 8.3/native canonical-path identity plus three ignored upstream Stylus lookup fixtures; `e06fa65` switched every provider authority to native canonical paths, added a tracked-corpus invariant, and force-added only the required official files. Run `29552211694` then exposed filename-free Stylus AST nodes on Windows; `978a501` made path identity null-safe with simulated-Windows coverage. Run `29552375699` passed the full Test Suite and four native rows but reproduced Dart Sass's RFC-unreserved `%7E`/`~` spelling difference for the public API's synthetic Windows entry; `ceeb991` added a red exact reproduction and narrowly normalizes unreserved spelling only inside the closed `zigcss-entry://entry` identity, including source maps. Final main Build run `29552671372` passed Test Suite job `87798404127` and all five native smoke jobs: macOS x64 `87798404152`, Windows x64 `87798404163`, Linux ARM64 `87798404167`, macOS ARM64 `87798404195`, and Linux x64 `87798404204`. Every row installed the packed production dependency graph, then compiled SCSS, Sass, Less, Stylus, and the public API offline. | `bf91c11`, `e06fa65`, `978a501`, `ceeb991` |
| `PRE-008` | `VERIFIED` | Graduated four independent canonical rows: SCSS and indented Sass through Dart Sass 1.101.0, Less through Less 4.6.7, and Stylus through Stylus 0.64.0, with native CSS remaining the direct ZigCSS path. Schema-v3 format evidence executes four real combined-CLI probes and five rejected extension/plugin probes; the capability contract owns 25 rows/24 gates. Combined help and the npm-facing API expose only exact admitted syntax/provider boundaries. Five versioned website examples are recompiled through the real provider-plus-ZigCSS product path and byte-check their displayed CSS. README, package metadata, changelog, compatibility/recovery/build guides, status, and the five-tab site now present the 0.5 source snapshot while explicitly distinguishing the published CSS-only rc.3 and denying arbitrary plugins, custom functions/importers, or executable project code. Browser QA exercised every tab and found a mobile grid min-content escape; a regression plus `min-w-0`/one-column track containment and root clipping reduce the 390 px document width from 691 px to exactly 390 px. Local evidence: 151/151 host/resolver/map/Sass/Less/Stylus/product tests; 138/138 site tests plus production build; documentation 7/7 over 37 Markdown files, 68 fences (53 executable; nine site-rendered), 48 links, and 24 routes; exact Zig 0.15.2 Debug and ReleaseSafe each 38/38 steps and 495/495 tests; package 6/6 with 22 runtime sources, 28 archive files, 86 dependencies, and five targets; wrapper 4/4; workflow 5/5; version 7/7; release metadata 6/6; dependency 4/4; capability 7/7; format, prefix, compatibility, transform, generated-state, and production audits 0/0/0. Exact-main Build `29554740002` passed Test Suite `87804419252` and all five native smoke jobs (`87804419258`, `87804419280`, `87804419288`, `87804419300`, `87804419432`). Documentation/Pages `29554740003` passed build `87804419414` and deploy `87804471198`; the public site served the exact new title and `index-DtZkXU-n.js`. No package, tag, release, or artifact was published. | `d92827d` |
| `PRE-009` | `VERIFIED` | Selected the roadmap-staged `0.5.0-rc.1` identity as the safest reversible candidate. A red version-integrity regression proved the inherited rc.3 drift before all 28 release surfaces, VS Code `0.5.0` mapping, candidate fixtures, package lock, generated runtime SPDX 2.3 SBOM, README/site/status claims, Docker labels, changelog, and publication guide were synchronized. The live registry proves the candidate unused. Exact Zig 0.15.2 Debug and ReleaseSafe each pass 38/38 steps and 495/495 tests; the complete preprocessor/product gate passes 151/151; site 138/138 and build, documentation 7/7, production audits 0/0/0, local linux/arm64 release container, package consumers/examples, benchmark policy/mode smoke, workflow, version, npm, release metadata/smoke/consumer, package, capability, prefix, editor, format, compatibility, transform, syntax, formatting, autodevelop 47/0, and diff gates pass. The exact npm dry run contains 28 files, 92,512 packed and 405,664 unpacked bytes. Source anchor `526002807edc856eb2dc391551ac3d5c1b77da00` produces the exact 2,187,412-byte archive with SHA-256 `dbab9f777b795742716841354e0bfe30555cc1d54b21f2a446fdc7dc523e26b1`; the formula-bound real download/build/version/CSS smoke passes. Binding `376ace8d6cbcc5eb30ac95a8313254b6168f63ec` produced a 561,400-byte native aarch64-macOS archive (SHA-256 `c4bc6a68cbbf3bf74019ae677ca0c8fd37604e648ae02639dfce75120484fce0`), deterministic binary SPDX SBOM (SHA-256 `0df210683ffc9329ea607672c4ee99cfc53406523c88e9dd1687e12e062fe2d5`), exact checksum manifest, direct execution, lifecycle-disabled install, offline postinstall, four canonical provider compiles, public API smoke, and 1,183-entry/19,125,624-byte installed-state validation. Exact-main Build `29555992594` passed Test Suite `87808031910` plus Windows x64 `87808031913`, Linux ARM64 `87808031923`, Linux x64 `87808031930`, macOS ARM64 `87808031932`, and macOS x64 `87808031940`; every native job passed architecture verification, archive metadata, offline npm installation, canonical five-language/API smoke, and artifact upload. Documentation/Pages `29555992555` passed build `87808031885` and deploy `87808092086`, and the public homepage serves the five-language deployment. Signed provenance remains correctly deferred to an authorized tag workflow; no package, tag, GitHub Release, signed attestation, or npm publication was created. | `5260028`, `376ace8`, `d026b9b` |
| `REL-006-RC2` | `VERIFIED` | Advanced all 28 release surfaces from the unusable public rc.1 identity to `0.4.0-rc.2` without moving the old tag. Debug and ReleaseSafe each pass 491/491; version 7/7, capability 7/7, release metadata 5/5, release smoke 5/5, release consumers 17/17, workflow 5/5, dependency 4/4, production audits 0/0/0, documentation 126/126 plus production build, npm dry run, and real Homebrew immutable-source smoke pass. | `f498f7d`, `f6eda45` |
| `REL-006-SITE` | `VERIFIED` | Replaced the internal-first README with install/use/language/API/release guidance while retaining compiled Zig examples and exact release claims. The existing React/Vite site is now the consumer package website at the real GitHub Pages project base, with direct-route recovery, package metadata, favicon, and one checked 1200×630 social card. CSS is labeled experimental and matrix-tested rather than complete; SCSS, Sass, and Less are explicitly unsupported. Full docs pass 130/130 and build; documentation integrity passes 7/7 over 31 Markdown files, 60 fences (50 executable; 7 site-rendered), 51 links, and 24 routes. Capability 7/7, version 7/7, package 7/7, wrapper 2/2, release metadata 5/5, release smoke 5/5, release consumers 17/17, workflow 5/5, dependency 4/4, format/compatibility/transform gates, live 0/0/0 production audits, and the exact five-file/34,429-byte npm dry run pass. | `d40c84c` |
| `REL-006-RC2-PUBLICATION` | `FAILED CLOSED` | Lightweight tag `v0.4.0-rc.2` points immutably to exact green integrated commit `221a973`. Release run `29512583531` passed npm authentication/version availability and four architecture jobs, while Windows passed version synchronization but failed the CRLF-sensitive release-metadata workflow parser before building. The matrix prevented GitHub release creation and npm publication; registry versions remain 0.2.0, 0.2.1, and 0.3.0. The public tag is preserved and will not be moved. | `221a973` |
| `REL-006-RC3-PORTABILITY` | `VERIFIED` | Added a red Windows-CRLF reproduction for both release/build workflow validators, plus bare-carriage-return rejection and a temporary checkout fixture. Normalization now occurs once at each bounded validator entry before LF-sensitive policy checks. Every active release surface is synchronized to rc.3. Debug and ReleaseSafe each pass 36/36 steps and 491/491 tests; version 7/7, capability 7/7, release metadata 6/6, release smoke 5/5, release consumers 17/17, workflow 5/5, dependency 4/4, prefix 4/4, package 7/7, wrapper 2/2, site 130/130 plus production build, documentation integrity 7/7, editor/format/compatibility/transform gates, all benchmark policy and smoke gates, production audits 0/0/0, the local non-root release container, repository formatting, and the exact five-file/34,429-byte npm dry run pass. | `18deb7c` |
| `REL-006-RC3-BINDING` | `VERIFIED` | Published the immutable rc.3 source checkpoint to the authorized recovery branch, downloaded the exact 1,522,571-byte GitHub archive, verified its canonical rc.3 `VERSION`, and bound commit `18deb7c34e5a2d13d57e07459138d925aed5a6e3` plus SHA-256 `f7dcb180bbe466f4f4269d699c56110889313c228723aedd1aed22b0c00a19b6` into the formula and closed release validators. Version 7/7, release consumers 17/17, release metadata 6/6, all checks, and the real checksum-bound Homebrew download/build/version/CSS smoke pass. | `8e78705` |
| `REL-006-RC3-PUBLICATION` | `PUBLISHED` | Exact-main Build `29514268622` and Documentation `29514268695` pass on `14c2bc8`; immutable tag `v0.4.0-rc.3` points there. Release run `29514712511` passed authority plus all five native signed-artifact jobs and created the public prerelease with 25 assets. npm logged successful five-file provenance publication and transparency index `2186856475`; the registry now proves immutable rc.3, `next=rc.3`, and unchanged `latest=0.3.0`. A clean public consumer install, native version probe, and CSS minification smoke pass. The run conclusion is failure only because its one-shot readback hit npm's transient 404 0.39 seconds after successful publication. | `14c2bc8` |
| `REL-007-NPM-READBACK` | `VERIFIED` | The hosted propagation race is the red reproduction. Added a dependency-free registry readback that validates exact immutable version, exact `next`, a retained stable `latest`, bounded JSON/tag structure, fixed public registry, command time/output limits, and at most 12 attempts separated by five seconds. Seven publication tests cover convergence and exhaustion; metadata 6/6, workflow 5/5, version 7/7, dependency 4/4, wrapper 3/3, documentation 7/7, live readback, diff checks, and a warning-free five-file/34,471-byte npm dry run pass. npm package metadata is canonical before publication instead of relying on registry auto-correction. Exact-main Build `29515939157` and Documentation/Pages `29515939319` are green on ledger head `d5c645d`. | `dc39e0f`, `d5c645d` |
| `REL-008-SITE-FORMAT-LAB` | `VERIFIED` | Replaced the static language-boundary table with a keyboard-accessible five-tab input/output lab and sharpened the hero around native Zig, deterministic output, and fail-closed release gates. CSS displays real input and exact rc.3 minified output; SCSS, indented Sass, Less, and Stylus display no output, an explicit unsupported state, and the required preprocess-to-CSS pipeline. Three component regressions exercise the real CSS default, every unsupported tab, and the authoritative policy link; package-claim and home tests own the surface. Full site tests pass 133/133 and the production Vite build passes. Documentation integrity passes 7/7 over 31 Markdown files, 63 fences (52 executable; 10 rendered by the site), 51 links, and 24 routes; both rendered CSS panels are independently parsed, while preprocessor inputs are presentation-only. The exact CSS sample was also run through the local rc.3 binary and matched byte-for-byte. Exact-main Build `29517581488` and Documentation/Pages `29517581341` are green on ledger head `c75ef3e`; the deployed public site is reachable. | `43fde82`, `c75ef3e` |
| `REL-009-README-FRONT-DOOR` | `VERIFIED` | Added the deployed 1200×630 ZigCSS social card as a clickable README banner; promoted “Compile CSS. Keep the meaning.” plus the native/deterministic/semantics-first contract; placed website, getting-started, documentation, compatibility, npm, and release links before installation; and added four bounded, truthful differentiators without restoring withdrawn speed or completeness claims. A regression owns the banner and above-the-fold website/npm links. Focused package/claim tests pass 22/22; the full site passes 134/134, production Vite build and documentation integrity pass, diff checks pass, and the isolated npm dry run remains exactly five files with the updated README. | `6e7799a` |
| `BENCH-007` | `BLOCKED` | The 2026-07-16 live GitHub Actions runner API still reports `total_count: 0`; therefore no runner matches `[self-hosted, linux, x64, zigcss-benchmark-v1]`, and the schedule-only workflow cannot produce provenance-bound evidence. The next eligible cron is 2026-07-20 04:17 UTC. GitHub-hosted, emulated, manually dispatched, or synthetic results would violate the verified `BENCH-006` contract and are deliberately rejected. No timing, memory, throughput, ranking, or ratio claim is published. | — |

## Milestone 0 package ledger

| Package | State | Evidence / decision | Commit |
|---|---|---|---|
| `SEC-001` | `VERIFIED` | HTTP regressions reject encoded slash/backslash traversal and escaping symlinks, malformed encoding returns 400 without terminating the server, safe assets and SPA fallback remain available; full docs tests and build pass. | `14e8878` |
| `SEC-002` | `DEFERRED` | Public compile routes return 503 and contain no process-spawn path. Bounded execution remains deferred until body/time/output/process/concurrency/cleanup isolation is implemented. Focused 9/9 and full docs 46/46 tests pass. | `ed5b2ff` |
| `SEC-003` | `VERIFIED` | The final M0 docs image builds from a 42.79 kB filtered context, contains only the built site and static server under `/app`, carries no compiler/download stage, runs as uid 1000 (`node`), and serves on 8080. Static regressions, image inspection, and a live container smoke test pass. | `8d8d81b` |
| `SAFE-001` | `VERIFIED` | CLI, package, docs, editor, formula, release, and benchmark surfaces identify the compiler as experimental. Alternate-format inputs fail before writes, unsafe/absent features are listed explicitly, unsupported public guides are withdrawn, and claim regressions pass. Debug/ReleaseSafe pass 105/105; docs pass 59/59 and build. | `6a2b594` |
| `TEST-001` | `VERIFIED` | Added 18 isolated compiler/CLI characterization regressions covering every non-server Milestone 0 audit case; fixed server regressions remain active assertions. Debug and ReleaseSafe each pass 94/94 tests. | `0334d05` |
| `OPT-001` | `VERIFIED` | Stable code generation rejects optimize, autoprefix, dead-code, and critical-CSS requests before AST mutation or emission. All optimizer corruption/crash inputs now assert explicit containment; Debug and ReleaseSafe pass 95/95. | `1f8323a` |
| `PROF-001` | `VERIFIED` | Timing handles end idempotently and transfer/free their name exactly once. CLI profiling exits 0, emits CSS, and reports each stage once; Debug and ReleaseSafe pass 97/97. | `0c7e56d` |
| `CLI-001` | `VERIFIED` | Canonical/real-path and inode-aware planning rejects input aliases, symlink/hard-link aliases, duplicate batch destinations, and unsafe default batch naming before writes. Debug and ReleaseSafe pass 99/99. | `5d2fc1d` |
| `CLI-002` | `VERIFIED` | Unknown/duplicate options, missing values, invalid batch contracts, and unavailable features exit 2 with explicit diagnostics. Help separates available from rejected recovery features. Debug and ReleaseSafe pass 102/102. | `4719d02` |
| `CI-001` | `VERIFIED` | PRs run locked docs install, 49 tests, and Vite build; Pages artifact uses `docs/dist`; deployment is isolated to non-PR events. Workflow regression tests and YAML parse pass. | `e39a674` |
| `CI-002` | `VERIFIED` | Build/release pass every matrix target to Zig after native tests and verify headers before upload. Five real cross-builds verified: ELF x86_64/aarch64, Mach-O x86_64/aarch64, PE x86_64. | `fcc1658` |
| `CI-003` | `VERIFIED` | Initial upstream-main Build run `29225800919` proved `macos-latest` had moved the ARM64 job to macOS 26.4, whose SDK symbols are incompatible with the reviewed Zig 0.15.2 baseline. The build and release matrices, generated target policy, native-smoke map, and regression expectation now pin the supported ARM64 `macos-15` image while retaining `macos-15-intel` for x86_64. Four smoke tests, five release-metadata tests/check, five workflow tests/check, 16 consumer tests, actionlint 1.7.12, and local Debug/ReleaseSafe 491/491 pass. Replacement Build run `29226557612` completed the ARM64 macOS job and its native/archive/npm smokes successfully. | `32fa523` |
| `CI-004` | `VERIFIED` | Initial upstream-main Windows job `86739467959` reproduced 34 CLI integration failures because Zig 0.15.2 explicitly does not implement `std.process.Child.cwd_dir` on Windows, leaving temporary fixture inputs outside the child cwd. Portable absolute `cwd` reduced replacement jobs `86741746820` and `86742704682` from 34 failures to three and then two. The remaining diagnostics proved Windows returns `IsDir` while collision planning identifies directory destinations, before output creation owns the stable error. Directory paths now retain normalized collision identity without a file inode; all direct children use absolute confined paths, and output inspection/creation share one stable contract. Build run `29227098324`, Windows job `86743377449`, passed all 491 native tests plus both target builds and architecture checks before reaching an unrelated archive-smoke fault. | `dfe6d4e`, `712058f`, `9595e4c` |
| `CI-005` | `VERIFIED` | Initial upstream-main Test Suite run `29225800919` reached the live Homebrew source gate but its default shallow checkout could not prove that immutable source commit `3fada235…` exists and is an ancestor. Only the release-consumer test job now fetches complete history; the verifier retains both object and ancestry checks, and the release policy fails closed if `fetch-depth: 0` drifts. The regression failed before the workflow change, then five metadata tests/check, 16 consumer tests, and a live checksum-bound Homebrew download/build/smoke passed. Replacement run `29226557612` passed the complete release-consumer step and continued through compiler, editor, dependency, package, wrapper, and documentation gates. | `0a4c102` |
| `CI-006` | `VERIFIED` | Replacement Test Suite run `29226557612` reached the experimental format matrix and proved a fresh checkout cannot represent the intentionally empty, untracked `src/formats` directory. Legacy-source discovery now defines absence as the canonical empty inventory, accepts only a regular non-symlink directory and regular optional Tailwind source, and returns a deterministic sorted closed set. Two hermetic regressions cover a fresh absent directory, filtering/order, the optional source, and symlink rejection; the complete eight-adapter/nine-extension/five-output matrix passes. The docs policy pins the expanded regression command; all 126 docs tests and the production build pass. Replacement Build run `29227098324`, Test Suite job `86743377333`, completed the format gate and every subsequent job step successfully. | `b382807`, `f054884` |
| `CI-007` | `VERIFIED` | Replacement Test Suite run `29226868221` reproduced a Linux scheduling race in an invalid `stdin + --watch` regression: the child correctly rejected its arguments and closed stdin before the parent finished writing the unused fixture, so the harness surfaced `BrokenPipe` instead of asserting the owned exit/diagnostic contract. The harness now ignores only this early-close condition and still propagates every other write error; any unexpected early close in a valid compile remains visible through the child result assertions. Two consecutive local Debug runs and ReleaseSafe each pass 491/491; replacement Build run `29227098324`, Test Suite job `86743377333`, passed the full hosted compiler test gate and every subsequent step. | `27cd262` |
| `CI-008` | `VERIFIED` | Build run `29227098324`, Windows job `86743377449`, passed all native compiler tests/builds and then proved the release smoke resolved Git Bash's GNU `tar`, which interprets an absolute `D:` archive path as a remote host and cannot own the Windows ZIP contract. The smoke harness and published npm installer now select Windows' built-in ZIP-capable libarchive reader by an absolute `${SystemRoot}\System32\tar.exe` path, reject absent or UNC system roots, and retain ambient `tar` on POSIX. Red regressions failed on both missing selectors; the repaired release smoke passes 5/5 and consumers 17/17, a real local libarchive ZIP list/stream round trip passes, and release metadata/workflow/version/dependency, live Homebrew, Debug/ReleaseSafe 491/491, docs 126/126/build, syntax, formatting, and diff gates pass. Replacement Build run `29251213253`, Windows job `86819948056`, passed all 491 tests, both target builds and header checks, ZIP inspection/extraction, the real offline npm postinstall/wrapper smoke, and artifact upload; all five native matrix jobs and the full Test Suite completed successfully. | `918b2b7` |

## Baseline commands and results

Baseline captured on base commit `2d2c0d9` with Zig 0.15.2, Node 24.16.0, and npm 11.13.0. The host is macOS 26.5; Zig 0.15.2's bundled linker could not consume the macOS 26.5 SDK stubs, so native commands used an isolated `xcrun` wrapper pointing at the already-installed macOS 15.4 SDK plus `MACOSX_DEPLOYMENT_TARGET=15.4`. A fresh-cache retry against the 26.5 SDK reproduced the linker failure, while the 15.4 SDK completed unchanged builds and tests.

| Surface | Command | Result |
|---|---|---|
| Zig Debug build | `zig build --summary all` | `PASS`; 3/3 steps succeeded. |
| Zig ReleaseSafe build | `zig build -Doptimize=ReleaseSafe --summary all` | `PASS`; 3/3 steps succeeded. |
| Zig tests | `zig build test --summary all` | `PASS`; 76/76 tests. |
| Zig formatting | `zig fmt --check build.zig build_helpers.zig src examples` | `FAIL`; 19 inherited `.zig` files require formatting. No bulk formatting was applied because it would mix unrelated churn into containment packages. |
| Docs tests | `cd docs && npm run test:run` | `PASS`; 6 files and 37 tests. |
| Docs build | `cd docs && npm run build` | `PASS`; Vite emitted `docs/dist`. |
| Docs dependency install/audit | `cd docs && npm ci --ignore-scripts` | Install passed; npm reported 10 advisories: 1 low, 2 moderate, 6 high, 1 critical. Resolution belongs to `DEP-001`. |
| npm package dry run | `npm pack --dry-run --ignore-scripts` | `PASS`; package contains 5 files, 53.0 kB unpacked. |
| Audit reproductions | Temporary corpus and live docs-server requests | `FAIL` as expected; concrete evidence below. |

## Milestone 0 exit validation

Completed on recovery commit `6a2b594`.

| Gate | Result |
|---|---|
| Clean isolated branch | `PASS`; `vale/zigcss-recovery` was clean before and after validation. |
| Debug / ReleaseSafe builds | `PASS`; each completed 3/3 build steps. |
| Debug / ReleaseSafe tests | `PASS`; each completed 105/105 tests (80 unit, 25 CLI integration). |
| Documentation | `PASS`; 59/59 tests and the Vite production build completed. |
| Workflow / script syntax | `PASS`; JSON, workflow YAML, `docs/server.js`, and the artifact inspector parsed successfully. |
| Native target matrix | `PASS`; real ReleaseSafe builds inspected as ELF x86_64/aarch64, Mach-O x86_64/aarch64, and PE x86_64. |
| Documentation container | `PASS`; filtered 42.79 kB build context, uid 1000 runtime, live HTTP response, only `/app/dist` and `/app/server.js`, no compiler binary. |
| Editor integration | `PASS`; VSCode extension dependencies installed with no reported advisory and TypeScript completed with `--noEmit`. |
| npm package surfaces | `PASS`; five-file dry run (11.7 kB unpacked) and a temporary wrapper-to-native-binary help smoke test completed. |
| Zig formatting | `KNOWN DEBT`; the same 19 inherited files reported at baseline remain. Focused newly added/edited regression and format files pass. Formatting debt must be isolated from semantic packages. |
| Dependency audit | `KNOWN DEBT`; docs report one high production advisory in direct `react-router` 7.13.0 and 10 total advisories including dev tooling. Owned by `DEP-001`. |

Milestone 0 exit criteria pass: static serving is contained; the public compiler endpoint is disabled; destructive CLI output paths and accepted no-op flags are rejected; unsafe transforms are unreachable from stable paths; every audit crash/corruption has an executable regression or explicit containment; public documentation describes the current experimental boundary.

## Operator-requested tooling ledger

| Package | State | Evidence / decision | Commit |
|---|---|---|---|
| `OPS-001` | `VERIFIED` | Added a canonical single-agent Codex loop protocol and executable read-only `scripts/autodevelop/orient.sh`. Tests prove it preserves Git status while reporting branch, ledger work, blockers, validation, checkpoints, recovery delta, durable-file hashes, and local tool versions from a nested directory. Bash syntax, 61/61 docs tests, claims checks, and Vite build pass. | `a648ad6` |
| `OPS-002` | `VERIFIED` | The app persistent-goal state had stopped at `blocked`, so it did not provide the requested unattended continuation even though roadmap implementation was progressing interactively. Added an Alvo-style repository-owned single-lane Bash supervisor with `doctor`, foreground test/run, background start, graceful pause/resume/stop, status, logs, and orientation controls. Every ephemeral pass explicitly selects `gpt-5.6-sol` plus `ultra`, admits at most one dependency-eligible package, repeats the no-delegation/no-fallback/no-outward-action contract, requires explicit-path staging and no fetch/integration, and proves `PROGRESS` with both a new local commit and a clean tree. An atomic directory lock rejects a second loop; ignored state/caches/logs are confined to real non-symlink mode-700 `.autodevelop` directories; interrupted recovery is bound to the exact dirty-tree SHA-256 fingerprint. Bounded timeouts, rate-limit backoff, immediate auth pause, five-error pause, and three-identical-blocker pause prevent hot loops; `COMPLETE` stops for review without publishing. The control surface deliberately has no LaunchAgent/system-startup installer. Twenty-four hermetic shell assertions cover classification, prompt-echo rejection, exact CLI model/effort/ephemeral arguments, auth/rate/timeout precedence, atomic state, umask restoration, confinement/symlink/numeric negatives, WIP binding, single-loop locking, and graceful stop without a model call or tracked mutation. Three operations tests pass; full docs are 126/126 and build; documentation integrity is 31 files/61 fences/51 executable/41 internal links/24 routes; version, capability, seven package tests, Bash syntax, and diff checks pass. Root npm remains five files/28,214 unpacked bytes. Fresh package `zigcss-0.4.0-rc.1-Bz3pcRE3GwBpTveEW6YWkZKglcWtvMQXabl0HzJptI_p` contains 75 files/1,937,408 disk bytes, builds 3/3 without mutation, and its consumer passes 6/6 steps with 2/2 tests in Debug and ReleaseSafe. | `e10d7e7` |
| `OPS-003` | `VERIFIED` | The first live unattended start reproduced two supervisor faults without touching tracked work: user-local Codex 0.142.5 rejected the required `gpt-5.6-sol`, while prompt echo made that startup error look like both `COMPLETE` and a rate limit. A direct read-only preflight proved the ChatGPT-bundled Codex 0.144.0-alpha.4 runs the exact approved model at ultra reasoning. Resolution now prefers the current app-bundled CLI, and `--output-last-message` isolates the sole trusted status channel from transport diagnostics and echoed instructions; rate-limit matching also requires an actual exhaustion signature. Twenty-seven hermetic assertions, three operations tests, Bash syntax, 126/126 docs tests, the production docs build, documentation integrity, and diff checks pass. | `a519e85` |
| `OPS-004` | `VERIFIED` | The operator explicitly authorized automatic pushes after every green autonomous pass. Model passes remain unable to push; the outer supervisor acts only after `run-pass.sh` proves a clean checkpoint. A dedicated helper admits only the reviewed `vyakymenko/zigcss` `origin`, the unchanged current `vale/*` branch, ordinary non-force push semantics, a five-minute non-interactive bound, and an exact remote object-ID readback before recording success. Rejection, timeout, mismatch, local mutation, or remote drift stops the loop with the checkpoint local. `PROGRESS` and `COMPLETE` both use this gate. Thirty-one hermetic assertions include a real temporary bare-remote push and unapproved-remote rejection. Three operations tests, Bash syntax, 126/126 docs tests, the production docs build, documentation integrity, diff checks, approved-origin doctor preflight, and a five-file/43,583-byte npm dry run pass. The first real helper invocation created `origin/vale/zigcss-recovery` and read back exact head `e11dffd`. Its initial launchd transport was rejected by live `OPS-005` evidence before any model pass and is superseded. | `c880ff8` |
| `OPS-005` | `VERIFIED` | The first transient launchd start failed closed before model execution because macOS privacy isolation denied its `/usr/bin/env` process access to linked-worktree Git metadata under `~/Documents`; the submitted retrying job was removed immediately. A detached system `screen` probe survived a separate command boundary and read exact Git head `e11dffd` with the caller's access. The control path now prefers a stable worktree-derived detached screen session and repository-confined launch wrapper, retaining `nohup` only as a non-screen fallback and installing no startup item. A real paused `ctl.sh start` remained live and clean as PID 93460/phase `PAUSED` across a separate status command at head `5edccab`, then stopped cleanly. The clean-tree full suite also exposed and fixed a selftest assumption that had depended on unrelated dirty work; clean and dirty states now each assert the correct WIP-marker contract. Thirty-one hermetic runner assertions, 3/3 operations tests, Bash and diff checks, 126/126 docs tests, the production docs build, documentation integrity, and a five-file/43,702-byte npm dry run pass. | `5edccab`, `a29696b` |
| `OPS-006` | `VERIFIED` | Live paused-runner state reproduced the same `BENCH-007` external archive condition repeatedly remaining at attempt 1/3 because free-form reason wording produced a different hash on each pass. Blocked results now require one validated lowercase kebab-case code plus a separate human reason; missing, malformed, empty, overlong, or superseded invalid markers fail closed as runner errors. The pass result records the code explicitly, and the supervisor counts only that stable identity, so changing explanatory prose cannot reset the three-pass pause threshold while a genuinely different code resets to one. The prompt and operator documentation define the contract. Forty-four hermetic assertions cover parsing, last-marker precedence, stable-code validation and bounds, reason separation, same-code increments to three, different-code reset, isolated result persistence, and all prior model/push/state/lock controls. Bash syntax, 3/3 operations tests, 126/126 docs tests, the production docs build, seven documentation-integrity tests/check, diff checks, and an exact five-file/45,908-byte npm dry run pass. Production compiler and publication behavior remain unchanged from the fully validated `QUAL-001` checkpoint. | `6004f31`, `1902b36` |
| `OPS-007` | `VERIFIED` | Initial upstream-main Documentation run `29225800907` executed the hermetic supervisor selftest from a normal `main` checkout and failed the production-only `vale/*` branch guard before producing test output. Repository admission now has an explicit test-mode branch predicate: production still admits only `vale/*`, while the already-confined selftest may run from main or a detached CI checkout. Three new assertions pin production acceptance/rejection and isolated test-mode admission; the supervisor passes 47/47, docs pass 126/126 and build, and documentation integrity passes 7/7. Replacement run `29226557568` completed its build job, including locked install, all tests, production build, and Pages artifact upload; the separate deploy job reached GitHub and failed only because Pages was not enabled. After explicit operator authorization, workflow-based Pages was enabled with enforced HTTPS. Dispatch `29358869275` on exact integrated head `e2eef58` passed the locked build, 126 documentation tests, production build, artifact upload, and deployment jobs; `https://vyakymenko.github.io/zigcss/` then returned HTTP 200. | `b18657e`, external run `29358869275` |
| `OPS-008` | `VERIFIED` | Live status reproduced a stopped, paused supervisor whose last three passes reused the stale `controlled-benchmark-archive` blocker even though the current plan makes `BENCH-007` non-blocking for eligible native frontend work. Source review found a second stale boundary: the embedded pass prompt still selected completed provider-era Milestone 9 packages after ADR-013 had made Milestone 10 authoritative. The prompt now pins the exact `NATIVE-001` through `NATIVE-009` dependency order, requires an in-progress umbrella package's smallest status-designated slice, and explicitly prevents the external benchmark runner from pausing native correctness work. Model passes still cannot push. Under the operator's existing authorization, the outer supervisor now pushes a clean green object to the exact `vale/*` recovery branch and `main` in one ordinary non-force atomic transaction, then reads both object IDs back before recording success; any diverged remote ref rejects the entire transaction and leaves both refs unchanged. The approved repository allowlist, fixed model/effort, clean-tree requirement, timeout, no-publication boundary, and all prior confinement controls remain intact. Fifty-eight hermetic assertions include exact native prompt-order guards, stale-prompt rejection, successful dual-ref integration, diverged-main rejection with atomic no-partial-update evidence, and state readback. Bash syntax, ShellCheck when available, documentation 7/7 plus check, and diff checks pass. Exact-main Build `30103254468` on `0f8b9dad50da18d9e1e68484a0d3350079812d9a` passed Test Suite `89514325720` and all five platform archive/offline-install jobs. A clean live restart remains the operator-side activation step. | `0f8b9da` |

## Foundational ADR ledger

| Decision | State | Evidence / decision | Commit |
|---|---|---|---|
| `ADR-001` | `VERIFIED` | Stable scope is standards-oriented CSS library/CLI parsing, diagnostics, spans, deterministic emission, and only evidence-backed transforms; ecosystem adapters remain explicit experiments. | `cd5c45f` |
| `ADR-002` | `VERIFIED` | CSS Syntax algorithms define token behavior; original byte spans, retained trivia, lossless nested component values, recoverable diagnostics, and transform-free syntax boundaries are mandatory. | `cd5c45f` |
| `ADR-003` | `VERIFIED` | A per-compilation arena owns sources through AST; returned CSS/maps/diagnostics have independent result ownership and one cleanup path with allocation-failure coverage. | `cd5c45f` |
| `ADR-004` | `VERIFIED` | Transform passes use immutable root handoff, exact deny-by-default safety classes, deterministic dependency plans, compilation-arena outputs, scratch validation storage, explicit acceptance evidence, and transactional root/diagnostic installation. | `e8176cb` |
| `ADR-005` | `VERIFIED` | Every inherited alternate-format adapter remains unavailable until strict evidence exists. SCSS/Sass, Less, Stylus, CSS-in-JS, PostCSS-like, and Tailwind-like behavior use `remove-until-funded`; CSS Modules alone may pursue a closed `limited-native-subset`. Unsupported syntax must diagnose without partial CSS, and transformed bytes may not enter the inherited parser. | `90d51a1` |
| `ADR-006` | `VERIFIED` | Browser compatibility uses a closed deterministic target grammar and pinned generated BCD data; qualifiers, gaps, partial support, and unsupported forms remain explicit rather than guessed. | `84fa0e6` |
| `ADR-007` | `VERIFIED` | Generated syntax carries an explicit causal source span; only its first generated byte maps to the authored construct start, no false inner precision or offset-zero fallback is allowed, and generated payloads require structured closed serialization. | `249632f` |
| `ADR-010` | `VERIFIED` | The configured `gpt-5.6-sol` xhigh runtime and one implementation agent are hard gates; fallback models and delegated implementation are prohibited. The owner changed the approved effort from ultra to xhigh on 2026-07-27. | `cd5c45f`, amended 2026-07-27 |
| `ADR-011` | `VERIFIED` | Native plugins are a trusted Zig-only experimental source API, not a sandbox or stable ABI. Definitions and user state are borrowed synchronously, IDs stay in an isolated `plugin.` namespace, execution reuses bounded deterministic pass plans, and configuration/execution failures preserve compiler transaction boundaries while caller-owned side effects remain the plugin's responsibility. Renumbered from the provisional ADR-005 identifier when Milestone 5 activated the roadmap-reserved preprocessor ADR. | `05879af`, `90d51a1` |

## Milestone 1 package ledger

| Package | State | Evidence / decision | Commit |
|---|---|---|---|
| `ARCH-001` | `VERIFIED` | Public `zigcss` module now roots at `src/lib.zig`. `SourceManager` owns copied names/bytes/line indexes; spans are source-bound half-open byte ranges; locations handle CR/LF/CRLF/form-feed and Unicode scalar columns; diagnostics own structured messages; `Compilation` owns per-input sources/diagnostics and validates spans. Debug and ReleaseSafe pass 113/113. | `4f290c1` |
| `TOK-001` | `VERIFIED` | Defined every CSS Syntax token category plus comment trivia/EOF and an on-demand progress-safe dispatcher for punctuation, CDO/CDC, whitespace, basic ASCII ident-like tokens, strings, UTF-8 delimiters, and EOF. All 256 byte values make progress; Debug and ReleaseSafe pass 120/120. | `a9d8a2c` |
| `TOK-002` | `VERIFIED` | Added CSS input preprocessing over original-byte spans; escape decoding; Unicode identifiers; number, percentage, dimension, URL/bad-URL, and opt-in unicode-range consumers; numeric representation metadata; raw/decoded token access; and bounded EOF/malformed-input recovery. Tests cover distinct string/name EOF escapes, quoted URL dispatch, escaped bad-URL recovery, incomplete exponents, invalid UTF-8 progress, all escape scalar limits, and every decoded-value allocation failure. Debug and ReleaseSafe pass 135/135. | `ece86be` |
| `TOK-003` | `VERIFIED` | Retains terminated and unterminated comments as explicit trivia with content/raw spans, identifies whitespace/comment trivia uniformly, and derives token start/end locations through the source line index. Tests prove bounded comment EOF recovery, contiguous lossless spans, and original-byte locations across CRLF, form feed, and multibyte UTF-8. Debug and ReleaseSafe pass 139/139. | `ccfa315` |
| `SYN-001` | `VERIFIED` | Added arena-owned lossless component values with preserved tokens, recursive simple blocks, functions, optional closing tokens, and original-byte node spans. Tests prove exact top-level round trips, independent `()[]{}` nesting, quoted/unquoted URL behavior, retained trivia, mismatched-closer preservation, bounded truncated-input recovery, unknown-source errors, exhaustive allocation-failure cleanup, and a configurable default nesting cap. Debug and ReleaseSafe pass 152/152. | `396b6b9` |
| `AST-001` | `VERIFIED` | Introduced the new `src/css/ast.zig` selector model: selector lists, complex selectors with optional leading and explicit inter-compound combinators, ordered compounds/simple selectors, namespace states, type/universal/id/class/nesting selectors, fully representable attribute operators/quoted values/case flags, and pseudo class/element arguments retaining both raw component values and optional typed selector lists. Constructors validate source/span containment and impossible shapes. Tests prove `.a.b` differs structurally from `.a .b`, every combinator/namespace is representable, functional pseudo syntax stays lossless, and attribute metadata survives. Debug and ReleaseSafe pass 161/161. | `ef810d5` |
| `AST-002` | `VERIFIED` | Added contiguous lossless component-value lists, ordered declaration lists, explicit name/colon/value/terminator spans, custom-property identity, and validated top-level `!important` metadata that retains its raw marker/trivia while exposing the semantic value prefix. Tests prove nested semicolons/colons stay inside component nodes, comments/case in `!important` are preserved, invalid markers are rejected, and duplicate fallback declarations retain order. Debug and ReleaseSafe pass 164/164. | `2869b7c` |
| `AST-003` | `VERIFIED` | Added exact terminated/unterminated block envelopes; ordered style/at-rule lists; declaration, nested-rule, keyframe, raw, and no-block at-rule variants; raw preludes; keyframe rules; and source-contained constructors. Tests cover every category, nested unknown syntax, percentage keyframe preludes, rule order, invalid block gaps, and semicolon-free EOF forms. Debug and ReleaseSafe pass 170/170. | `78bc6d1` |
| `MEM-001` | `VERIFIED` | `Compilation` now owns a heap-stable, caller-backed arena used by sources, diagnostics, and temporary syntax storage. Foundational `CompileResult` storage deeply owns CSS, optional source-map bytes, and cloned diagnostic messages independently of compilation lifetime; `take` makes moves explicit and leaves one safe cleanup path. Tests cover compilation moves, unfreed arena temporaries, post-compilation result access, empty/double cleanup, diagnostic-bearing results, and every constructor allocation failure. Debug and ReleaseSafe pass 143/143. | `3330a57` |
| `DIAG-001` | `VERIFIED` | Token recovery metadata and syntax parsing now append compilation-owned diagnostics for invalid UTF-8 and escapes, bad/unterminated strings and URLs, unterminated comments/blocks/functions, mismatched or top-level closers, and nesting limits while preserving a usable lossless document whenever recovery is possible. Diagnostic spans remain source-valid (including zero-width EOF spans), allocation failures propagate separately, and result cloning already deep-copies messages. Debug and ReleaseSafe pass 155/155, including exhaustive diagnostic-allocation failure injection. | `182aa5c` |

## Milestone 1 exit validation

Milestone 1 is `PASS` at gate checkpoint `31a00c1`. The gate adds a representative modern-CSS round-trip corpus, exercises every truncation boundary of nested escaped input (including cuts inside multibyte UTF-8), and passes every single byte through the full lossless syntax boundary.

| Gate | Result |
|---|---|
| Package closure | `PASS`; `ARCH-001`, `TOK-001` through `TOK-003`, `SYN-001`, `AST-001` through `AST-003`, `MEM-001`, and `DIAG-001` are verified and checkpointed. |
| Debug / ReleaseSafe tests | `PASS`; each completed 173/173 tests (80 legacy unit, 68 new library/core, 25 CLI integration). |
| Lossless corpus and truncation recovery | `PASS`; representative custom-property, selector, keyframe, nesting, Unicode/escape, URL, and unknown-syntax inputs round-trip exactly; every tested prefix and byte does likewise without a leak or panic. |
| Diagnostics and positions | `PASS`; malformed/truncated input remains usable and carries source-valid structured spans; LF/CRLF/form-feed, multibyte columns, EOF spans, invalid UTF-8, comments, strings, URLs, escapes, mismatched closers, and nesting limits are covered. |
| AST invariants | `PASS`; adjacency/combinators, attributes, functional pseudos, nested declaration values, importance/fallback ordering, at-rule categories, keyframe preludes, block EOF state, and rule order have independent tests. |
| Ownership / allocation failure | `PASS`; compilation arena, result cloning, decoded tokens, syntax construction, and diagnostics pass exhaustive allocation-failure injection. |
| Debug / ReleaseSafe builds | `PASS`; each completed 3/3 build steps. |
| Documentation | `PASS`; 69/69 tests and the Vite production build completed. |
| Editor and package surfaces | `PASS`; VS Code TypeScript completed with `--noEmit`; npm dry-run contains the expected five files (12.2 kB unpacked). |
| Focused formatting | `PASS`; all Milestone 1 files pass `zig fmt --check`. Repository-wide checking reports only the same 19 inherited files recorded at baseline. |
| Compatibility boundary | `PASS`; the new public source/token/syntax/CSS-AST modules coexist separately from the inherited parser; stable CLI transformation paths remain contained until Milestone 2 replaces them. |

## Milestone 2 package ledger

| Package | State | Evidence / decision | Commit |
|---|---|---|---|
| `PAR-001` | `VERIFIED` | Added arena-owned selector lowering from lossless component values into selector lists, compounds, standard descendant/child/sibling combinators, namespaces, universal/type/class/ID selectors, attributes with every matcher and `i`/`s` flag, legacy/modern pseudo-elements, raw functional pseudos, and typed selector arguments for `:is`, `:where`, `:not`, and `:has`. Comments and whitespace retain distinct semantics; `:is`/`:where` use silent forgiving lists (including empty results), while strict/relative pseudo lists, nested `:has`, pseudo-element contexts, unsupported column combinators, and pre-`NEST-001` `&` are rejected. Names decode into the compilation arena while full spans preserve spelling. Debug and ReleaseSafe pass 185/185, including official Selectors Level 4 grammar examples, negative recovery, recursion limits, and exhaustive allocation-failure injection. | `8c6a9cf` |
| `PAR-002` | `VERIFIED` | Added arena-owned declaration-list lowering and top-level semicolon recovery. Property names decode with raw spans; values remain exact contiguous component lists; functions, blocks, strings, and custom-property syntax cannot be split by nested delimiters; final trivia-separated `!important` markers are case/escape aware while raw marker spelling remains retained; empty values, duplicate fallbacks, and missing final semicolons are preserved. Invalid candidates append diagnostics and resume at the next top-level semicolon. Debug and ReleaseSafe pass 194/194, including count limits and exhaustive valid/recovery allocation-failure injection. | `a5d65f9` |
| `PAR-003` | `VERIFIED` | Added stylesheet/rule-list lowering that composes selector and declaration parsers, ignores CDO/CDC only at top level, preserves style/at-rule order, recurses through rule-list at-rules, classifies declaration/keyframe/raw/no-block bodies, and retains keyframe bodies for PAR-004. Invalid qualified rules synchronize at a top-level semicolon or complete curly block; unterminated blocks keep declarations and explicit missing-close metadata. Escaped at-rule names classify by decoded value. Debug and ReleaseSafe pass 203/203, including rule/depth limits and exhaustive valid/recovery allocation-failure injection. | `01cbce3` |
| `PAR-004` | `VERIFIED` | Added a lossless specialization layer for media query lists; supports conditions with unary/conjunction/disjunction structure; named container queries; statement, named-block, and anonymous layers; declaration-backed property/font-face details; typed keyframe names, selectors, percentages, and declaration blocks; and page selectors, declarations, and margin boxes. Invalid structures append diagnostics while leaving the generic raw AST available; invalid keyframes recover frame-by-frame; page scanning distinguishes margin at-rules from at-keywords inside declaration values. Debug and ReleaseSafe pass 212/212, including mixed/empty/percentage recovery, raw-retention assertions, and exhaustive allocation-failure injection; both build modes and focused formatting pass. | `51b1ce4` |
| `ERR-001` | `VERIFIED` | Added a shared top-level semicolon/curly/EOF synchronization scanner with explicit progress metadata and reused it for qualified rules, at-rules, keyframes, and invalid page children. Selector diagnostic checkpoints suppress redundant outer messages after precise nested failures; syntax-diagnosed stray closers are isolated so they cannot consume the next valid rule. Recovery tests preserve ordered valid siblings, nested rules, declarations, frames, and page descriptors across ten independent errors; every typed-corpus truncation, all 256 bytes, and exhaustive allocation failures remain usable with valid diagnostic spans. Debug and ReleaseSafe pass 220/220; both build modes and focused formatting pass. | `e46a74d` |
| `EMIT-001` | `VERIFIED` | Added deterministic pretty emission over the typed CSS AST with configurable indentation/final newline, canonical selector/rule/declaration formatting, ordered page/keyframe children, duplicate fallback and importance retention, raw component-value preservation for property-specific/unknown syntax, and CSSOM-compatible identifier/string escaping. Every possible byte serializes into one valid identifier and one terminated string token. Emission refuses source mismatches, missing closers, invalid known at-rule structures, discarded rules/declarations/frames, and other recovery gaps rather than inventing or silently deleting syntax. Representative output reparses cleanly; Debug and ReleaseSafe pass 231/231, exhaustive allocation failures, both builds, and focused formatting pass. | `0c91387` |
| `EMIT-002` | `VERIFIED` | Pretty and minified modes now share one emitter, escaping implementation, recovery gate, and ordered traversal. Minified mode removes structural indentation/newlines/optional block-final semicolons, selector-list spaces, and explicit-combinator spaces while retaining descendant combinators, the separator between at-rule names and preludes, CSS escape terminators, comment separators, and one normalized whitespace token everywhere an unknown/property-specific component stream originally contained whitespace. Nested functions/blocks are serialized recursively without flattening. Pretty/minified outputs are deterministic, minified output is smaller, representative style/conditional/keyframe/page/font/raw output reparses cleanly, and both modes refuse the same recovery gaps. Debug and ReleaseSafe pass 234/234 with exhaustive allocation failures; both builds and focused formatting pass. | `c1dcbae` |
| `ROUND-001` | `VERIFIED` | Added an allocation-safe semantic AST comparator across ordered rules, selectors/combinators/namespaces/attributes/pseudos, declarations and importance, decoded token/component trees, typed at-rule tags, keyframes, and page item order. It ignores spans, trivia width, escape spelling, comments, and string quote style only where they are representational; it preserves raw whitespace-token edges for unknown syntax and detects compound-vs-descendant collapse, fallback/importance/rule changes, raw token merging, keyframe selector changes, and page declaration/margin reordering. A five-family modern CSS corpus passes original→pretty/minified→parse structural equivalence and exact second-emission idempotence. ROUND also corrected unknown/raw emission so prelude, block, and nested-function edge whitespace presence survives minification. Debug and ReleaseSafe pass 240/240 with exhaustive end-to-end allocation failures; both builds and focused formatting pass. | `96e43f6` |
| `MAP-001` | `VERIFIED` | Added an ECMA-426/Source Map v3 builder with ordered four-field mappings, relative Base64 VLQ encode/strict decode, duplicate generated-position refinement, deterministic JSON, optional generated file and embedded source content, and independent ownership. Original and generated CSS columns count zero-based UTF-16 code units; CR/LF/CRLF/form-feed line tracking matches the source model. The emitter records rule, selector, declaration, keyframe/page, and raw component starts without changing CSS bytes. Tests verify exact positive/negative VLQ deltas, malformed grouping rejection, CRLF and BMP/astral columns, JSON escaping/content omission, representative pretty/minified decoded positions, deterministic maps, empty-map cleanup, and exhaustive mapped-pipeline allocation failures. Debug and ReleaseSafe pass 247/247; both builds and focused formatting pass. | `d3ef270` |
| `NEST-001` | `VERIFIED` | Implemented CSS Syntax block-content disambiguation and CSS Nesting Level 1 structures: style blocks retain leading declarations plus ordered child rules, later declaration runs become explicit nested-declarations rules, and nested media/supports/container/layer/scope-style group blocks accept interleaved declarations and relative rules without selector expansion. Selectors represent `&` and whether a parent reference is implied, including delimiter discovery inside raw functional arguments; type-selector ordering rejects `&div` while accepting `div&`, multiple `&`, leading combinators, and top-level scope use. Known non-group at-rules are rejected in nesting context while unknown raw at-rules remain forward-compatible. Pretty/minified emission, structural equivalence, recovery-gap refusal, source-map positions, arbitrary-depth grammar examples, the current WPT parsing matrix/recovery/invalid-inner-rule cases, nesting limits, truncations, and exhaustive allocation failures pass. The WPT corpus also exposed and fixed unprefixed `[lang|="zh"]` being misread as a namespace. Debug and ReleaseSafe pass 258/258; both builds and focused formatting pass. | `fca7093` |
| `M2-CLI` | `VERIFIED` | Added an ownership-safe CSS pipeline that composes source management, lossless syntax, typed parsing, structured diagnostics, deterministic emission, and optional maps. The stable single-file, watch, and bounded batch compilation functions now use only that pipeline; legacy parsing/code generation remain reachable solely from labeled internal prototype tests and non-stable boundaries. Replaced all six parser/minifier audit quarantines with target behavior, and added fallback/importance, nesting, pretty output, filename/location diagnostics, successful parallel batch, all-or-nothing batch failure, result-lifetime, source-map, and exhaustive allocation-failure coverage. Error strings and compile results have explicit ownership in batch cleanup. Debug and ReleaseSafe pass 268/268 (80 legacy unit, 157 library/core, 31 CLI integration); both builds, focused formatting, and diff checks pass. The repository-wide formatter reports only the same 19 inherited files. | `46ed011` |
| `M2-COMPAT` | `VERIFIED` | Published one machine-readable and public 20-feature grammar matrix with nine versioned fixtures and explicit `Supported`/`Preserved`/`Rejected` meanings. The locked validation script compiles seven valid corpora in pretty and minified modes, then requires all 14 outputs to parse in Lightning CSS 1.30.1 with error recovery disabled and the nesting draft enabled; two unsupported corpora must reject in both modes with diagnostics and empty stdout. Expected unknown-at-rule warnings are named rather than suppressed. Schema, IDs, statuses, case links, fixture containment, modes, validator version/options, documentation rows, and dependency pins are executable assertions. PR CI installs the root lockfile and runs the gate after native tests. Debug and ReleaseSafe pass 268/268; the independent gate reports 20 features/14 parsed/4 rejected; documentation passes 75/75 and builds; root npm audit reports zero vulnerabilities and the five-file package dry run remains contained. | `56d662b` |

## Milestone 2 exit validation

Milestone 2 is `PASS` at gate checkpoint `d4a16d3`.

| Gate | Result |
|---|---|
| Audit parser/minifier regressions | `PASS`; all six legacy quarantines are target-contract assertions, fallback/importance and nesting coverage are active, and `tests/regressions/audit.zig` contains no remaining quarantine. |
| Published grammar boundary | `PASS`; one public and machine-readable matrix defines 20 tested `Supported`, `Preserved`, and `Rejected` features and is guarded against documentation drift. |
| Independent output parsing | `PASS`; exact Lightning CSS 1.30.1 parses 14 actual pretty/minified CLI outputs with recovery disabled; four unsupported-mode runs reject deterministically without partial output. |
| Source mappings | `PASS`; decoded representative pretty/minified mappings cover rules, selectors, declarations, nested rules, Unicode/CRLF columns, and deterministic repeated emission. |
| Stable CLI migration | `PASS`; single, watch, and parallel batch compilation use `css/pipeline.zig`, with structured diagnostics and all-or-nothing batch writes. |
| Legacy stable-path removal | `PASS`; source inspection of stable compile functions finds no inherited parser, format-parser, or code-generation calls; legacy modules remain only behind prototype tests/non-stable boundaries. |
| Debug / ReleaseSafe | `PASS`; each completes 11/11 test steps and 268/268 tests (80 legacy unit, 157 library/core, 31 CLI integration), plus 3/3 build steps. |
| Documentation / packaging / CI | `PASS`; docs pass 75/75 and build, workflow YAML and validator/JSON parse, root locked install audits clean, and package dry run remains five files/12.7 kB. |
| Formatting | `KNOWN DEBT`; every M2-owned file passes focused checks. Repository-wide checking reports the same 19 inherited files recorded at baseline, with no new formatting debt. |

## Milestone 3 package ledger

| Package | State | Evidence / decision | Commit |
|---|---|---|---|
| `PASS-001` | `VERIFIED` | Added the public rebuilt transform pass manager and accepted `ADR-004`. Passes declare stable ID/revision, matched phase/safety class, maturity, dependency graph, pre/post/no-op contracts, nesting/order/size behavior, and acceptance evidence. Verified output-changing passes require semantic+differential evidence; size and reorder claims have separate gates. Planning bounds registries, closes dependencies, rejects invalid/duplicate/missing/cyclic/later-phase graphs and requests, authorizes every dependency through exact class booleans, and orders ready passes by phase/priority/ID independent of registration/request order. Execution accepts const roots, rejects recovered input, uses compilation-arena candidates plus scratch validation allocation, invokes pre/post and optional second-run idempotence hooks, prevents analysis root replacement, rejects error diagnostics, rolls failed diagnostics back, and publishes a pipeline root only after the complete plan succeeds. Tests cover source mismatch, no-op/clone paths, transitive ordering, all policy denials, failed-validator rollback, diagnostic rollback, semantic equivalence hooks, and every planning/application allocation failure. `--optimize` remains rejected and no inherited optimizer is registered. Debug and ReleaseSafe pass 278/278 (80 legacy unit, 167 library/core, 31 CLI integration); both builds, focused formatting, 77/77 docs tests/build, and independent CSS validation pass. | `e8176cb` |
| `OPT-010` | `VERIFIED` | Added the public verified `empty-rule-cleanup` pass. It immutably removes empty style rules and empty `media`, `supports`, `container`, or `starting-style` groups at every supported nesting depth while preserving survivor order and returning the exact input root without allocation when no candidate exists. Empty layers, scopes, keyframes, document/descriptor/page rules, and unknown syntax remain observable; declaration runs, fallbacks, importance, custom values, and token-separating comments remain byte-stable. Rule deletion uses source-ordered proof ASTs rather than trusted byte spans, so constructors, the pass validator, and emission reject forged/nonempty omissions. Tests cover recursive parents, nested declarations, all-empty output, exact no-op/idempotence, diagnostic-free PASS-001 integration, rejected omission proofs, surviving source-map positions, and every allocation failure. A test-only driver and reviewed pretty/minified goldens are independently canonicalized against the original by pinned Lightning CSS 1.30.1, with warning policy, byte-idempotence, order, and size checks in CI. Debug and ReleaseSafe pass 288/288 (80 legacy unit, 177 library/core, 31 CLI integration); both builds, the existing compatibility matrix, package dry run, benchmark smoke, focused formatting, and 77/77 docs tests/build pass. The stable CLI remains transform-free. | `fe821f5` |
| `OPT-011` | `VERIFIED` | Added the public bounded duplicate-declaration report API and verified `duplicate-declaration-analysis` PASS-001 pass. Reports deeply own copied property names and ordered occurrence/span facts, outlive compilation, and are deterministic across runs. Standard properties match decoded names ASCII case-insensitively; custom properties remain case-sensitive. Groups never cross a declaration-list segment, so runs split by nested rules stay distinct. Every occurrence records normal/important state and its exact transition from the previous same-property declaration; same-importance chains remain explicitly potential fallbacks and mixed-importance chains are never removal candidates. Traversal covers nested styles/groups, descriptor blocks, keyframes, pages, and page margins while preserving first-occurrence order. Group/occurrence/property-byte/depth limits, invalid ASTs, empty/idempotent cleanup, escaped names, logical and unsupported properties, layer/media contexts, PASS-001 root identity, exact emission, and every allocation failure are tested. Debug and ReleaseSafe pass 293/293 (80 legacy unit, 182 library/core, 31 CLI integration); both builds, existing independent grammar/transform gates, package dry run, and focused formatting pass. No declaration is rewritten or deleted, and the stable CLI remains transform-free. | `0ee5a5e` |
| `VAL-001` | `VERIFIED` | Added public immutable numeric-value parsing through `css.numeric_value`: a caller-owned flat postfix instruction stream retains literal/operator/function spans, numeric spelling metadata, typed results, and safe idempotent cleanup independently of compilation lifetime. The reviewed unit taxonomy covers absolute/font/viewport/container lengths, angles, times, frequencies, resolutions, flex, percentages, and numbers; static conversion factors stop at context-dependent units. CSS Typed OM-style dimension maps implement percentage hints plus checked addition, multiplication, division, and inversion while preserving compound intermediates and distinguishing incompatible/unknown types from parser failures. Recursive descent enforces CSS math precedence, left associativity, mandatory whitespace around binary `+`/`-`, comment semantics, current constants, `min`/`max`/`clamp` arity, and clamp-only `none`; unresolved functions such as `var()` conservatively reject. The boundary validates complete source-owned component trees, termination and delimiter structure, and independent instruction/argument/nesting limits before returning owned output. Tests cover all declared units and percentage hints, escaped/case-insensitive units, root-vs-calculation grammar, compatibility/compound algebra, checked exponent overflow, malformed and foreign spans, unterminated syntax, OOM, cleanup, limits, and no-rewrite behavior. Debug and ReleaseSafe pass 302/302 (80 legacy unit, 191 library/core, 31 CLI integration); both builds, focused formatting, 20-feature/14-output/four-rejection grammar validation, the existing transform differential, five-file package dry run, and 77/77 docs tests/build pass. The stable CLI and AST output are unchanged. | `ff5970d` |
| `MATH-001` | `VERIFIED` | Added public verified `numeric-math-folding` over the typed postfix IR plus a shared closed unit taxonomy and structured generated-numeric AST payload. Evaluation is finite and conservative: add/subtract/comparisons require identical units, multiplication requires a unitless side, division requires a unitless divisor or identical-unit cancellation, and context/cross-unit conversion, substitutions, comments, compound algebra, ambiguous signed zero, and non-finite results are no-ops. A closed property policy admits only category/range-correct whole math-function values whose generated spelling is strictly shorter; custom properties and declaration-backed descriptors remain untouched. Immutable traversal preserves declarations, importance, fallbacks, and order through nested styles/groups/layers, keyframes, pages, and margin boxes with explicit depth/declaration budgets. Generated values carry the whole math function as their causal span, serialize only through the emitter, and receive one mapping at the generated segment start. Pre/post/idempotence validators recompute expected output and its source map; forged results, every allocation failure, unsupported/range/mixed-unit cases, and exact-root no-ops are covered. Reviewed pretty/minified goldens independently canonicalize against the original with Lightning CSS 1.30.1, remain byte-idempotent, and reduce the transform-free minified fixture from 511 to 370 bytes. Debug and ReleaseSafe pass 313/313 (80 legacy unit, 202 library/core, 31 CLI integration); both builds, the 20-feature/14-output/four-rejection compatibility gate, both transform differentials, benchmark smoke, five-file package dry run, focused formatting/diff checks, and 79/79 docs tests/build pass. The same 19 inherited format failures remain; the stable CLI still rejects optimization. | `f44b3ad` |
| `OPT-012` | `VERIFIED` | Added public exact 8-bit sRGB typing for 3/4/6/8-digit hex, the standardized basic named colors/aliases, and integer or endpoint-percentage `rgb()`/`rgba()` channels. Source ownership, complete component spans, termination, nesting limits, escaped/case-insensitive names, malformed syntax, conservative unsupported forms, and every allocation failure are covered. Generated numeric and color output now share one closed AST union and emitter/source-map path. Added a bounded immutable whole-value traversal that preserves ordered style/nested/group/layer/keyframe/page/margin paths while skipping declaration-backed descriptors. The verified `typed-color-zero-shortening` pass uses closed color-property and unambiguous length-property policies, requires strict byte reduction, preserves custom values/fallbacks/importance/order, and retains the exact root for contextual/system/wider colors, missing/calculated/fractional/clamped channels, comments, unsupported properties, percentages/angles/times, signed zero, and number-or-length grammars. Compatibility is explicit: non-opaque `rgba()` and `transparent` are not raised to alpha-hex without target data; only authored alpha-hex shortens within that feature. Validators recompute generated CSS plus source maps, reject forged results, and prove idempotence. Reviewed pretty/minified goldens independently canonicalize against the original with Lightning CSS 1.30.1 and reduce the transform-free minified fixture from 419 to 378 bytes. Debug and ReleaseSafe pass 325/325 (80 legacy unit, 214 library/core, 31 CLI integration); both builds, the 20-feature/14-output/four-rejection compatibility gate, all three transform differentials, benchmark smoke, five-file package dry run, focused formatting/diff checks, and 79/79 docs tests/build pass. The same 19 inherited format failures remain; stable optimization is still rejected. | `f5241f6` |
| `OPT-013` | `VERIFIED` | Added the public verified `margin-shorthand-synthesis` declaration-phase pass with an explicit dependency on `OPT-011`. Eligibility is deliberately one-value and proof-complete: exactly four adjacent physical longhands in top/right/bottom/left order, identical importance, identical authored token bytes, and one finite length, finite percentage, unitless zero, or decoded `auto`. Mixed values/order/importance/spelling, comments, functions, substitutions, CSS-wide keywords, non-length dimensions, unknown units, non-finite values, already-generated inputs, declaration-backed descriptors, and page/page-margin contexts are exact no-ops. The AST retains all four authored declarations plus ordered non-overlapping proof metadata; constructors validate the canonical reset set and causal span, the emitter re-runs CSS-level eligibility before closed `margin` emission, and forged or incomplete proofs fail. Generated declarations receive one mapping at the multi-input causal start and no false inner mappings; authored siblings keep their mappings. Immutable traversal preserves fallback, logical-property, layer, media, nesting, keyframe, rule, and importance boundaries, prevents later value rewrites from discarding a declaration proof, and remains budgeted/OOM-safe. Logical equivalence compares generated declaration views against reparsed shorthand output. Reviewed pretty/minified fixtures independently canonicalize to the original, remain byte-idempotent, preserve order, and reduce the transform-free minified fixture from 936 to 631 bytes with Lightning CSS 1.30.1. Debug and ReleaseSafe each pass 340/340 tests (80 legacy unit, 229 library/core, 31 CLI integration); both builds, the 20-feature/14-output/four-rejection compatibility gate, all four transform differentials, benchmark smoke, five-file/12.8 kB npm dry run, focused formatting/diff checks, and 79/79 docs tests/build pass. The independent gate exposed that page-margin descriptor shorthand equivalence is not currently provable, so those contexts remain authored. The same 19 inherited format failures remain; stable optimization is still rejected. | `014fac4` |
| `OPT-014` | `VERIFIED` | Added the public verified `adjacent-selector-rule-merge` rule-phase pass plus a reusable bounded immutable rule-list traversal. Eligibility is deliberately narrow: exactly two authored-adjacent style rules in one list, a whitespace/terminated-comment gap, nonempty declaration-only blocks, no recovery, omissions, nested content, generated values/declarations/rules, or foreign spans, and structurally equivalent ordered declarations. Equivalence compares decoded token text, numeric representation type/sign, nested component structure, importance, meaningful whitespace, and exact authored value comments; it preserves property/custom-property identity and declines token-boundary or comment changes. The cascade proof recorded in `ADR-004` is the reverse of selector-list expansion: selector alternatives retain order and use the most specific selector that actually matches, both equivalent blocks lie on the same side of every outside rule, and declaration-only eligibility prevents changing a descendant nesting-selector context. Both authored rules remain as ordered non-overlapping proof metadata; constructors validate structure and causal spans, while the emitter independently recomputes semantic eligibility before composing typed selector lists with the first authored block. Forged, incomplete, overlapping, omitted, non-style, nested, or semantically different proofs fail or remain exact no-ops. Logical equivalence understands the generated rule view. Pretty/minified source maps anchor the causal start, retain mappings for both authored selector lists, the surviving declaration block, and later siblings, assign no mapping to the inserted separator, and expose none from the duplicate block; `ADR-007` now records this compositional policy. Reviewed top-level/layer/media/native-nesting fixtures cover mixed selector specificity, custom-property token boundaries, representation-only block differences, nested-content guards, and at-rule boundaries. Lightning CSS 1.30.1 independently canonicalizes both modes to the original, repeated parse-transform-emit is byte-idempotent, and the transform reduces its minified baseline from 528 to 439 bytes. Exhaustive allocation failures exercise decoded-token comparison, traversal depth/rule budgets are enforced, and validators recompute CSS plus maps. Debug and ReleaseSafe each pass 354/354 tests (80 legacy unit, 243 library/core, 31 CLI integration); both builds, the 20-feature/14-output/four-rejection compatibility gate, all five transform differentials, benchmark smoke, five-file/12.8 kB npm dry run, focused formatting/diff checks, and 79/79 docs tests/build pass. The same 19 inherited format failures remain; stable optimization is still rejected. | `fe7bdf6` |
| `OPT-015` | `VERIFIED` | Added the public verified `adjacent-at-rule-merge` rule-phase pass. Eligibility is closed to exactly two authored-adjacent, nonempty, terminated typed `media`, `supports`, `container`, or identically named non-anonymous `layer` rule blocks in one list, with matching nesting mode and structurally equivalent preludes. Different kinds/queries/layers, anonymous layers, untyped or recovered syntax, non-rule blocks, omissions at the inspected parent boundary, existing parent proofs, and direct nested-declaration streams are exact no-ops. A shared source-validating component comparator preserves token kind, numeric representation type/sign, nested structure, meaningful whitespace, token boundaries, and exact comments while allowing equivalent decoded spelling. Both authored groups remain in ordered proof metadata; AST constructors validate shape and causal spans, and the emitter independently re-proves kind, typed details, prelude, adjacency, termination, child eligibility, source ownership, and proof span before retaining the first header and composing both logical child streams. Nested generated child proofs and inner omissions compose without escaping their parent. Logical equivalence compares concatenated generated streams with reparsed authored groups. Pretty/minified maps retain the first header, both child streams, and later sibling mappings while omitting the removed second header/braces; `ADR-007` records that policy. Named-layer first occurrence and child order are unchanged, while anonymous layers remain distinct under the Cascade specification. Reviewed fixtures cover all four admitted kinds, nested layers and query groups, unsafe boundaries, custom-property token separation, omissions, direct nested declarations, source maps, determinism, forged/incomplete proofs, budgets, and every allocation failure. Lightning CSS 1.30.1 independently canonicalizes both modes to the original, repeated transform emission is byte-idempotent, and the fixture falls from 1,190 to 1,044 minified bytes. Its current direct nested-declaration serialization loses a required separator, so those candidates remain authored rather than relying on an unsound oracle. Debug and ReleaseSafe each pass 369/369 tests (80 legacy unit, 258 library/core, 31 CLI integration); both builds, the 20-feature/14-output/four-rejection compatibility gate, all six transform differentials, benchmark smoke, five-file/12.8 kB npm dry run, focused formatting/validator/diff checks, and 79/79 docs tests/build pass. The same 19 inherited format failures remain; stable optimization is still rejected. | `168b9d4` |
| `CUSTOM-001` | `VERIFIED` | Added a public default custom-property preservation boundary shared by rebuilt value and declaration traversals. Pass metadata now declares a custom-property effect that defaults to preservation. Static resolution is valid only as an experimental semantic value-pass classification, cannot be marked verified, and requires independent semantic-rewrite, experimental, and static-resolution policy authority; no rebuilt pass registers that effect and the stable CLI still rejects optimization. The guard protects every custom-property definition and every ordinary declaration containing a decoded `var()` at any nested component depth. It validates source ownership and termination, recognizes escaped/case-varied function spellings, enforces a depth limit, skips whole-value generators for protected declarations, and rejects generated-declaration proofs that consume them. Tests cover malicious generators, shorthand proofs, foreign spans, depth limits, every allocation failure, case-sensitive names, fallback lists, cycles, token-separating comments, empty custom values, and exact stable-CLI output. One composed plan actively runs every verified analysis/cleanup/value/declaration/rule pass: safe ordinary candidates still fold, shorten, synthesize, clean, and merge while protected definitions/substitutions remain authored; the result reparses semantically equivalent. The machine-readable compatibility matrix now gives custom properties a dedicated fixture and asserts exact pretty/minified preservation fragments before Lightning CSS validation. `ADR-004` records the CSS Custom Properties computed-value/cascade rationale and the separate future-mode requirement. Debug and ReleaseSafe each pass 377/377 tests (80 legacy unit, 265 library/core, 32 CLI integration); both builds, the 20-feature/16-output/four-rejection compatibility gate, all six transform differentials, benchmark smoke, five-file/12.8 kB npm dry run, focused formatting/JSON/validator/diff checks, and 79/79 docs tests/build pass. The same 19 inherited format failures remain. | `270e33f` |
| `LOGICAL-001` | `VERIFIED` | Removed logical-to-physical conversion from the rebuilt product decision rather than retaining a context-free mode. Pass metadata now declares a logical-property effect that defaults to preservation; the reserved `physical_conversion` effect is registry-invalid for every maturity/safety/phase combination and has no policy escape hatch. The rebuilt AST has no generated property-name payload, all registered passes assert preservation, and the stable CLI rejects the inherited optimizer entry point. `ADR-004` records the CSS Logical Properties proof: physical mapping depends on used `writing-mode`, `direction`, and `text-orientation`, while logical/physical declarations in one property group cascade together by source order after mapping, so a global direction flag is insufficient. A composed all-pass regression covers RTL and vertical writing modes, direction-sensitive `float`/`text-align` values, logical box properties, and an interrupted physical shorthand set. Safe numeric/color values on logical properties still fold or shorten, while property names, directional values, cascade position, and the logical boundary remain authored; the full output reparses equivalent. Stable CLI regressions assert exact RTL/vertical output and no optimization mode. The compatibility matrix adds a dedicated logical-properties case with required fragments across horizontal LTR, root RTL, vertical-rl, and vertical-lr modes before independent parsing. Debug and ReleaseSafe each pass 379/379 tests (80 legacy unit, 265 library/core, 34 CLI integration); both builds, the 21-feature/18-output/four-rejection compatibility gate, all six transform differentials, benchmark smoke, five-file/12.8 kB npm dry run, focused formatting/JSON/validator/diff checks, and 79/79 docs tests/build pass. The same 19 inherited format failures remain. | `34fa77e` |
| `PREFIX-001` | `VERIFIED` | Added the public library-only `prefixing` boundary and accepted `ADR-006`. Its deliberately non-Browserslist grammar accepts only comma-separated explicit minimums for a closed six-browser enum, owns normalized targets, sorts them by a fixed order, serializes canonically, reports structured byte offsets, and enforces byte/target limits. Empty, dynamic, aliased, duplicate, malformed, zero/overflow/leading-zero, and forged noncanonical queries fail; every byte value, allocation failure, and canonical ordering path is covered. Exact `@mdn/browser-compat-data` 8.0.0 package bytes, timestamp, provenance, license, browser mapping, and eight reviewed property/value/selector/at-rule paths are pinned in a manifest and lockfile. The generator uses locale-independent ASCII ordering, records manifest/selected-data SHA-256 digests, validates exact versions and increasing intervals, rejects mirrors, runtime flags, ambiguous forms, invalid qualifiers, or browser drift, and retains partial-support plus note-presence facts without copying or interpreting caveat text. Four generator unit tests and a read-only byte regeneration check run in CI before Zig tests. The bounded resolver proves continuous support from each minimum forward, deterministically selects the longest complete/unannotated chain, deduplicates nonstandard forms, exposes partial/annotated facts, and reports the first unsupported gap instead of guessing. Tests cover modern/legacy unions, prefixes, alternative names, removals, qualifiers, initial and later gaps, forged inputs, table structure, and every allocation failure. Documentation exposes only this eight-feature experimental library subset; stable `--browsers` and `--autoprefix` remain rejected until `PREFIX-002` proves ordered rewrites. Debug and ReleaseSafe each pass 394/394 tests (80 legacy unit, 280 library/core, 34 CLI integration); both builds, 4/4 generator tests plus byte regeneration, the 21-feature/18-output/four-rejection compatibility gate, all six transform differentials, benchmark smoke, five-file/13,237-byte npm dry run, focused formatting/JSON/script/diff checks, and 82/82 docs tests/build pass. Repository-wide formatting reports exactly the same 19 inherited files. | `84fa0e6` |
| `PREFIX-002` | `VERIFIED` | Added the public verified `target-prefix-rewrite` compatibility pass for the pinned eight-feature property/value/selector/at-rule subset. Per-target configuration admits only complete unannotated BCD forms, retains partial/annotated/unsupported facts without guessing, and makes a reviewed legacy query materially expand output while modern Chrome/Edge/Firefox returns the exact input root. Closed proof-carrying AST payloads and independent emitter analysis serialize vendor declarations immediately before the retained standard declaration, separate unforgiving selector variants into adjacent rules, and clone prefixed keyframes before standard keyframes; no raw generated CSS is accepted. Manual vendor syntax makes that feature a conservative list-level no-op, and mixed/functional selectors, descriptors, page contexts, supports preludes, recovered/unsupported/conflicting syntax, custom-property definitions, and logical names remain authored. `var()` consumers are copied byte-for-byte without resolution. Immutable bounded traversal covers nested style/group/declaration/keyframe paths, composes with existing cleanup/value/declaration/rule proofs, preserves importance/fallback/order, and passes exhaustive allocation-failure tests. Validators recompute the candidate, logical standard semantics, exact CSS and source map, and second-run idempotence; generated segments follow `ADR-007`. The independent Lightning CSS 1.30.1 fixture verifies reviewed pretty/minified goldens, standard-semantic projection, exact reparse idempotence, structured invalid-query rejection, legacy-vs-modern target differences, and the required minified expansion from 591 to 2,111 bytes. The fixture exposed and fixed typed `@-moz-keyframes` reparsing. Debug and ReleaseSafe each pass 409/409 tests (80 legacy unit, 295 library/core, 34 CLI integration); both builds, 4/4 generator tests plus byte regeneration, the 21-feature/18-output/four-rejection compatibility gate, all seven transform differentials, benchmark smoke, five-file/13,503-byte npm dry run, focused formatting/script/diff checks, and 82/82 docs tests/build pass. Repository-wide formatting reports the same 19 inherited files. Stable `--browsers` and `--autoprefix` remain explicitly unavailable pending later public API/CLI wiring. | `6b3b642` |
| `TREE-001` | `VERIFIED` | Replaced the inherited token-presence/`swapRemove` heuristic with distinct public `conservative-dead-code-extraction` and `conservative-critical-css-extraction` passes. Both remain experimental and require separate `allow_extraction` plus `allow_experimental` grants; the stable CLI and legacy optimizer remain unreachable. A non-null class/ID category is an exhaustive decoded inventory for a closed document snapshot or closed critical selector-matching tree, while null is unknown and a non-null empty slice is known empty. Configurations deeply own, byte/count bound, ASCII-fold sort, duplicate-check, and binary-search their inventory; rule/depth/proof budgets and exhaustive allocation-failure coverage bound execution. Only a direct positive class or ID absent from a complete category can prove one complex selector impossible, and a style rule emits nothing only when every selector-list alternative is proven impossible. Types, attributes, namespaces, functional pseudo arguments, possible alternatives, generated-rule ranges, and unsupported semantics default to authored output. Traversal preserves source order through groups and native nesting while retaining imports, layers, descriptors, keyframes, custom-property dependencies, unknown at-rules, and other dependency-bearing non-style rules. Nonempty extracted rules remain in the AST under closed zero-output proofs carrying the canonical inventory/mode; the emitter independently re-proves absence, forged/unsorted proofs fail, removed rules receive no mappings, logical equivalence skips only validated zero-output proofs, and cleanup/merge proofs compose without overlap. Reviewed dead-code and critical fixtures cover fallbacks, `!important`, custom properties, logical RTL/vertical declarations, future values, layers, media/supports, nesting, selector-list/functional/type/attribute no-op boundaries, font faces, and keyframes. Lightning CSS 1.30.1 independently derives each selector subset; both modes remain byte-idempotent, reject incomplete/duplicate inventories without output, and reduce a 762-byte transform-free fixture to 628 and 524 minified bytes. Debug and ReleaseSafe each pass 420/420 tests (80 legacy unit, 306 library/core, 34 CLI integration); both builds, 4/4 generator tests plus byte regeneration, the 21-feature/18-output/four-rejection compatibility gate, all nine transform differentials, benchmark smoke, five-file/14,059-byte npm dry run, focused formatting/script/diff checks, and 82/82 docs tests/build pass. Repository-wide formatting reports the same 19 inherited files. | `72c2829` |

## Milestone 3 exit validation

Milestone 3 is `PASS` at gate checkpoint `89417a8`.

| Gate | Result |
|---|---|
| Package closure | `PASS`; `PASS-001`, `OPT-010` through `OPT-015`, `VAL-001`, `MATH-001`, `CUSTOM-001`, `LOGICAL-001`, `PREFIX-001`, `PREFIX-002`, and `TREE-001` are verified and checkpointed. |
| Stable optimizer admission | `PASS`; `--optimize` invokes one static `verified-optimizer` registry containing exactly seven verified definitions: duplicate analysis, empty cleanup, numeric math folding, typed color/zero shortening, margin shorthand synthesis, adjacent at-rule merge, and adjacent selector-rule merge. Policy grants only lossless cleanup and semantic rewriting. Compatibility, extraction, experimental maturity, proven reorder, static custom-property resolution, and logical-to-physical conversion have no registry path. The inherited optimizer and legacy code generator remain unreachable. |
| Cross-pass fixed point | `PASS`; proof-carrying nodes cannot silently defer a second safe candidate to a later CLI invocation. The preset runs no more than 32 parse-transform-emit rounds and succeeds only when consecutive bytes match. Tests cover cleanup→merge, math→zero, math→shorthand, value→selector merge, and at-rule→inner-selector merge; every allocation failure is handled and non-convergence produces no output. Fixed-point source maps remain explicitly unavailable pending composed-map evidence. |
| Source order and nesting | `PASS`; rebuilt transform and prefix sources contain no `swapRemove`; all admitted passes are recursively nested and order-preserving by validated metadata. CLI target contracts preserve importance/fallback chains, non-adjacent boundaries, custom-property cascade, logical RTL/vertical names and values, unsupported selectors/values, group/layer contexts, and batch determinism. Legacy `src/optimizer.zig` still contains order-unstable operations but is not imported by the preset. |
| Browser targets and diagnostics | `PASS`; the reviewed legacy query materially expands the pinned eight-feature fixture from 591 to 2,111 minified bytes, while modern Chrome/Edge/Firefox is the exact transform-free root. Dynamic/unknown target input fails at its byte offset without CSS, and target rewriting remains separately authorized rather than part of `--optimize`. |
| Independent semantics and size | `PASS`; Lightning CSS 1.30.1 independently canonicalizes all ten reviewed pretty/minified transform fixtures, projects target-only compatibility forms and both selector-extraction subsets, checks exact goldens/order, rejects bad query/inventory input, and proves reparse byte stability. The combined optimizer fixture preserves cascade-sensitive boundaries and falls from 1,143 to 866 minified bytes. The grammar matrix remains 21 features, 18 independently parsed outputs, and four deterministic rejections. |
| Debug / ReleaseSafe | `PASS`; each completes 13/13 test steps and 425/425 tests (80 legacy unit, 310 library/core, 35 CLI integration), plus 3/3 build steps. The preset registry, dependency closure, fixed-point execution, batch/single CLI paths, every allocation failure, parser/emitter equivalence, maps for ordinary single plans, and all prior regressions pass. |
| Generated data / docs / packaging | `PASS`; four prefix-data generator tests and byte-exact BCD 8.0.0 regeneration pass; 82/82 docs tests and the Vite build pass; benchmark smoke completes; root production audit reports zero vulnerabilities; npm dry-run remains five files and 14,517 bytes unpacked. |
| Formatting and determinism | `PASS` for every M3-owned clean-format file, JavaScript and shell syntax, generated bytes, and `git diff --check`. Repository-wide checking reports exactly the same 19 inherited Zig files recorded at baseline; no formatting-only churn was included. |

## Milestone 4 package ledger

| Package | State | Evidence / decision | Commit |
|---|---|---|---|
| `API-001` | `VERIFIED` | Audited the `src/lib.zig` foundation created by `ARCH-001` and kept one explicit external module name, `zigcss`, registered from `build.zig`. The public root exports source/span, diagnostic, compilation/result, tokenizer, component-syntax, typed CSS/parser/emitter, source-map, transform, and target-query foundations without importing any inherited parser/optimizer surface. Added an independent `tests/public-api/consumer.zig` module that imports only `zigcss`, not implementation-relative files, and a focused `zig build test-public-api` step. The consumer proves all root modules and aliases compile externally, CSS/map/diagnostic results remain owned after compilation cleanup, source-map JSON names the source, the closed verified optimizer is reachable, and strict target queries own and validate their data. README and active docs label this as an experimental foundation and explicitly defer final `CompileOptions`, dependencies/module exports, plugin policy, package metadata, and installed-consumer integration to `API-002`, `API-003`, and `BUILD-001+`. Debug and ReleaseSafe each pass 427/427 tests (80 legacy unit, 310 library/core, two external API, 35 CLI integration); focused consumer tests pass 2/2 in both modes; both builds, all ten transform differentials, the 21-feature/18-output/four-rejection compatibility gate, 82/82 docs tests/build, focused formatting/diff checks, and the five-file/15,652-byte npm dry run pass. | `1f063aa` |
| `API-002` | `VERIFIED` | Added `zigcss.compile` with explicit CSS syntax, pretty/minified format, separate source-map, closed optimizer, verified target-prefix, borrowed canonical target-query, and dependency-limit options. The move-only-by-convention result owns CSS, optional map bytes, located structured diagnostics, ordered decoded top-level `@import` facts, and optional module-export state through one allocator-recording idempotent `deinit`; `take` makes transfers explicit and no compilation-arena view escapes. Stable CSS returns null module exports. Invalid transform/map/target combinations return owned `API0001` diagnostics without partial CSS; dependency bounds return a resource diagnostic and no partial facts; operational allocation/source-size/internal failures remain a small explicit error set. Import reporting preserves authored order and duplicates, supports decoded string/URL operands, ignores malformed or dynamic preludes, and owns source names/specifiers beyond compilation cleanup. A separately compiled consumer exercises the high-level facade, while one exact README/docs example is extracted, compared with `examples/public_api.zig`, and compiled in CI-facing build steps. Exhaustive allocation-failure tests cover successful transformed/dependency results and diagnostic results. Debug and ReleaseSafe each pass 437/437 tests (80 legacy unit, 320 library/core, two external API, 35 CLI integration); focused external API/example gates pass 2/2 in both modes; both builds, all ten transform differentials, the 21-feature/18-output/four-rejection compatibility gate, exact BCD 8.0.0 generation, benchmark smoke, 84/84 docs tests/build, focused formatting/diff checks, and the five-file/16,459-byte npm dry run pass. | `3b3e80b` |
| `API-003` | `VERIFIED` | Replaced any public reliance on the inherited mutable `PluginRegistry` with `PluginOptions.experimental` over the rebuilt immutable pass contract and accepted ADR-011 (originally recorded under the provisional ADR-005 identifier). Native plugins are trusted in-process Zig callbacks only; the default API, recovery CLI, verified optimizer, and HTTP service contain no plugin path. Definitions, metadata, requests, callbacks, and `user_data` are borrowed until synchronous `compile` returns, while AST output remains compilation-arena-owned and no plugin pointer enters the result. Every definition/dependency/request uses the isolated `plugin.` namespace; complete registries plus request/dependency lists are capped at 256, validated before callbacks, and ordered independently of registration/request order by dependencies, phase, priority, then ID. The optimizer fixed point completes first; selected plugins and optional target prefixing share one final plan after plugin selection passes the caller's exact deny-by-default policy. Active plugin maps are rejected pending mapping evidence. Invalid graph/metadata/policy produces owned `API0002` with no callbacks or CSS; callback/validator failure rolls back the root and plan diagnostics and produces owned `API0003` with no CSS while original import facts remain; successful warnings remain, caller-state side effects do not roll back, and OOM stays operational. Tests prove namespace isolation, bounds, deterministic order, exact cleanup authority, successful and failed plugin-plus-prefix composition, warning rollback/retention, no callback on option/config failure, external borrowed-state lifetime, and exhaustive plan/result/diagnostic allocation failures. Debug and ReleaseSafe each pass 445/445 tests (80 legacy unit, 327 library/core, three external API, 35 CLI integration); focused external API/example gates pass 3/3 in both modes; both builds, all ten transform differentials, the 21-feature/18-output/four-rejection compatibility gate, exact BCD 8.0.0 generation, benchmark smoke, zero root production advisories, 86/86 docs tests/build, focused formatting/diff/reachability checks, and the five-file/17,292-byte npm dry run pass. | `05879af` |
| `BUILD-001` | `VERIFIED` | Added the Zig 0.15.2 package manifest with stable name `.zigcss`, version 0.3.0 synchronized to npm, generated identity fingerprint `0xae272a4871e93d07`, no dependencies, and an explicit five-entry content allowlist: `build.zig`, `build.zig.zon`, `src`, `README.md`, and `LICENSE`. The legacy helper, tests, docs, wrappers, editor integration, and installed dependencies are excluded until their owning packages are verified. Added a committed package-manager consumer that declares ZigCSS as a path dependency, resolves only `zigcss.module("zigcss")`, and exercises the owned compile API without relative source imports; it passes 1/1 in Debug and ReleaseSafe. A fresh isolated `zig fetch .` produced `zigcss-0.3.0-Bz3pcVTjGQC3mpM_M5vmSR--tbruLQpuTg2ShHm0kKa0`, containing 76 allowlisted files/1.8 MiB; that pruned copy builds the CLI and passes its own consumer in both modes. Four metadata regressions pin identity/version/fingerprint/minimum toolchain, exact paths, package-manager use, and every active Zig 0.15.2 source/CI claim; the build workflow runs those checks plus the real consumer before CSS oracles. The installer now says tested with 0.15.2 rather than promising all later versions, and docs advertise local/fetched consumption without inventing a remote URL/hash. Root Debug and ReleaseSafe remain 445/445; both root builds, the package consumer, fresh fetched package, all ten transform differentials, 21-feature/18-output/four-rejection compatibility, exact BCD generation, benchmark smoke, zero production advisories, 87/87 docs tests/build, manifest/workflow/JavaScript parsing, focused formatting/diff checks, and the five-file/18,025-byte npm dry run pass. Repository-wide formatting remains exactly the same 19 inherited files. | `042fcd4` |
| `BUILD-002` | `VERIFIED` | Reproduced the legacy helper's Zig 0.15 failure at its obsolete three-argument `@fieldParentPtr` and replaced the custom step that mutated the graph during `make`. `@import("zigcss").helpers.addCssCompile` now configures an ordinary `addRunArtifact` during graph construction, declares exactly one input with `addFileArg`, declares one generated output with `addOutputFileArg`, bounds captured stdio, and returns that output `LazyPath` plus an opaque step handle for downstream checks/installs. Output names are limited to portable ASCII `.css` basenames of 1-128 bytes. The helper exposes only minification and the closed verified optimizer; source maps, prefix/target flags, alternate formats, arbitrary arguments, and batch directories have no API path. Callers must provide a host-runnable compiler artifact. The root and real package dependency both exact-check optimized/minified CSS with `CheckFile`; helper basename tests cover valid/invalid/path/NUL/non-ASCII/over-limit names. Unchanged root, path-package, and fetched-package runs are cached; changing only input bytes reruns compilation and preserves exact output. The dependency build module exports `helpers`, so the committed consumer imports it by package name rather than a relative file. `build_helpers.zig` is now the sixth manifest path. Fresh `zig fetch .` hash `zigcss-0.3.0-Bz3pcab3GQBiHxMhPPypn1VWGh4xRQO1-ijU0Bv0YN6n` contains 77 allowlisted files/1.8 MiB; its default build and API/helper consumer pass in Debug and ReleaseSafe. Root Debug and ReleaseSafe each pass 446/446 across 20/20 steps (80 legacy unit, 327 library/core, three external API, 35 CLI, one helper unit); focused helper and package consumers pass 6/6 steps and 1/1 tests in both modes. Five package regressions, all ten transform differentials, 21-feature/18-output/four-rejection compatibility, exact BCD generation, benchmark smoke, zero production advisories, 87/87 docs tests/build, focused formatting/diff checks, and the five-file/18,889-byte npm dry run pass. Repository-wide formatting remains exactly the same 19 inherited files. | `19781b3` |
| `BUILD-003` | `VERIFIED` | Reproduced the inherited build example's missing relative helper import, unused module, and obsolete build API, then replaced it with a self-contained path-dependency project. The example resolves separate application-target and host-tool ZigCSS instances, imports only the public library module and dependency build module, compiles exact-checked optimized/minified CSS through `helpers.addCssCompile`, installs the generated stylesheet, builds an executable, and runs an owned-API test. Its Debug and ReleaseSafe test steps each pass 7/7 with 1/1 test; the default install passes 6/6, the installed CSS is exact, and the executable emits exact public-API CSS. CI runs both safe modes before independent CSS validation. A recursive static gate enumerates every authored `.zig` example while excluding generated/cache directories and requires either the complete project or the root public-API build gate, preventing uncompiled snippets. Root Debug and ReleaseSafe remain 446/446 across 20/20 steps. Six package regressions, all ten transform differentials, the 21-feature/18-output/four-rejection compatibility gate, exact BCD generation, benchmark smoke, zero production advisories, 88/88 docs tests/build, YAML parsing, focused formatting/diff checks, and the five-file/19.3 kB npm dry run pass. Fresh `zig fetch .` hash `zigcss-0.3.0-Bz3pcWT5GQCB0p1nMq9Wt50m6QT2OxQCfBMhS3RwdPif` contains 77 allowlisted files/1.8 MiB and builds 3/3 in Debug and ReleaseSafe. Replacing the obsolete file reduces inherited repository formatting debt from 19 files to 18 without formatting unrelated sources. | `e6dd72e` |
| `CLI-010` | `VERIFIED` | Reproduced two independent executable implementations of parse, fixed-point optimize, and emit across the single/watch and batch paths, then removed both in favor of one `zigcss.compile` call site. The executable and its test modules now receive the same public `zigcss` module as external consumers. Runtime code constructs explicit CSS/pretty-or-minified/verified-optimizer options, consumes owned `CompileResult` values, renders public located diagnostics, propagates operational errors, preserves no-partial-write batch behavior, and deinitializes every single or task-owned result exactly once. A static call-graph gate requires exactly one facade call and rejects direct runtime parser/pipeline/optimizer/format-adapter/autoprefixer imports; inherited compiler imports remain confined after a test-only boundary. Lightweight CLI suffix classification preserves all eight rejected experimental format names without importing their adapters. A real child-process regression compares exact CLI bytes with direct public API results in pretty, minified, and optimized-minified modes; all existing diagnostic, ordering, path, batch, and containment regressions remain active. Profiling now honestly measures the end-to-end public compile call once; unobservable internal stage and allocator metrics remain deferred to `PROF-010`. Debug and ReleaseSafe each pass 449/449 across 20/20 steps (82 executable units, 327 library/core, three external API, 36 CLI integration, one helper unit). Path-package consumers pass 6/6 with 1/1 tests and complete examples pass 7/7 with 1/1 in both modes. All five release targets build and inspect as ELF x86_64/aarch64, Mach-O x86_64/aarch64, and PE x86_64. Seven package regressions, all ten transform differentials, the 21-feature/18-output/four-rejection compatibility gate, exact BCD generation, benchmark smoke, zero production advisories, 88/88 docs tests/build, focused formatting/diff checks, and the five-file/19.8 kB npm dry run pass. Fresh `zig fetch .` hash `zigcss-0.3.0-Bz3pcVn7GQCV_aauChGkGrKsBPoK3TYtzx-ryuME2wi_` contains 77 allowlisted files/1.8 MiB and builds 3/3 in Debug and ReleaseSafe. Repository-wide formatting remains the same 18 inherited files. | `2d52056` |
| `CLI-011` | `VERIFIED` | Added a strict public command contract without expanding the stable compiler surface. `--version`/`-V` prints `zigcss 0.3.0` from a constant statically synchronized to npm and Zig package metadata; help and version must be alone, exit 0, and use stdout. `--syntax` is non-repeatable and admits only explicit `css`, which is also the default and maps to the public `Syntax` option. Every existing boolean and valued flag is now one-shot; missing/unknown/duplicate/unavailable/incoherent options exit 2 without CSS. One `-` input reads at most 10 MiB from stdin, reports owned diagnostics under `<stdin>`, and may emit to stdout or one file; `-o -` explicitly selects stdout without creating a filesystem entry. Stdin cannot be duplicated, watched, or mixed with file/batch inputs; batch output cannot be stdout and batch profiling is rejected rather than ignored. Help documents the exact contract. Success/info exits 0, CSS and read/write failures exit 1 with stderr diagnostics, and usage/configuration failures exit 2. Child-process tests cover help/version streams, synchronized bytes, no-input/mixed-info failures, missing values/files, parser diagnostics, unwritable output, CSS syntax acceptance/rejection/duplication, all repeated booleans, stdin optimization, stdin-to-file, explicit stdout, `<stdin>` locations, duplicate/mixed/watched stdin, batch stdout, and batch profiling. The npm launcher now inherits all three streams, preserves native 0/1/2 statuses, maps unexpected null status to failure, and re-raises POSIX child signals instead of converting them to success; two local fixture tests run in CI with no download. Debug and ReleaseSafe each pass 453/453 across 20/20 steps (82 executable units, 327 library/core, three external API, 40 CLI integration, one helper unit). Path-package consumers pass 6/6 with 1/1 tests and complete examples pass 7/7 with 1/1 in both modes. All five release targets build and inspect as ELF x86_64/aarch64, Mach-O x86_64/aarch64, and PE x86_64. Seven package regressions, two wrapper tests, all ten transform differentials, the 21-feature/18-output/four-rejection compatibility gate, exact BCD generation, benchmark smoke, zero production advisories, workflow YAML, 88/88 docs tests/build, focused formatting/diff checks, and the five-file/20.7 kB npm dry run pass. Fresh `zig fetch .` hash `zigcss-0.3.0-Bz3pcecOGgCYS7Y1TVdFwrzb2yLsOTpYg17PPTqjcut8` contains 77 allowlisted files/1.8 MiB and builds 3/3 in Debug and ReleaseSafe. Repository-wide formatting remains the same 18 inherited files. | `45e10c7` |
| `CLI-012` | `VERIFIED` | Reproduced direct-write failures for missing output parents, followed symlink/hard-link destinations, non-atomic overwrite under a read-only parent, and rejected same-basename batches. File commits now use Zig's same-directory `atomicFile`: missing parents are created only when a successful result is committed, existing regular-file permissions are retained, destination link entries are replaced rather than followed, and every write/rename failure removes its temporary file while preserving the old destination. Batch parse/transform failure still commits nothing and now leaves no output directory. Unique stems map to `<stem>.css`; colliding stems receive an order-independent 16-lowercase-hex XxHash64 suffix over the normalized working-directory-relative input path, with conservative case folding on Windows/Apple targets and a `zigcss-<hash>.css` fallback that bounds every final basename to 128 bytes. The existing canonical-path/inode collision gate validates all final names and catches exact-input or hash collisions before writes. Per-destination atomicity is explicit; a batch is not falsely claimed as one multi-file transaction after compilation succeeds. Regressions cover nested parent creation, complete replacement and mode retention, symlink/hard-link target preservation, read-only prepare failure, rename failure, temporary-file cleanup, collision naming in both argument orders, long-name fallback, no directory on compile failure, and exhaustive naming allocation failures. Debug and ReleaseSafe each pass 458/458 across 20/20 steps (83 executable units, 327 library/core, three external API, 44 CLI integration, one helper unit). Path-package consumers pass 6/6 with 1/1 tests and complete examples pass 7/7 with 1/1 in both modes. All five release targets build and inspect as ELF x86_64/aarch64, Mach-O x86_64/aarch64, and PE x86_64. Seven package regressions, two wrapper tests, all ten transform differentials, the 21-feature/18-output/four-rejection compatibility gate, four prefix-data tests and exact generation, benchmark smoke, zero production advisories, workflow YAML, and 88/88 docs tests/build pass. The five-file npm dry run is 21.4 kB unpacked. Fresh `zig fetch .` hash `zigcss-0.3.0-Bz3pcfojGgDY43mGGucJ2rWYTivvWhmOOf9EoMCdNZQQ` contains 77 allowlisted files/1,888,256 bytes and builds 3/3 in Debug and ReleaseSafe. Focused formatting/diff checks pass; repository-wide formatting remains the same 18 inherited files. | `1a59375` |
| `WATCH-001` | `VERIFIED` | Reproduced the inherited watcher reading the root once to hash and again inside `compileFile`, ignoring every reported `@import`, and recompiling unchanged invalid CSS every 500 ms (four identical `CSS0007` diagnostics in the first 1.8 seconds, with further repeats until termination). Compilation orchestration now has one `compileLoadedFile` path that consumes the exact already-read root bytes and returns its owned public result; the ordinary single-file path retains identical ownership and output behavior. A tagged root fingerprint records content hashes or stable read-error identities before each compile attempt, so unchanged read, parse, transform, or write failures wait for a real transition instead of looping or adding a generic duplicate error. Successful owned dependency evidence is converted into an ordered, allocation-safe watch set: direct local relative imports resolve from the root stylesheet, query/fragment suffixes are excluded, remote/scheme/protocol-relative/origin-absolute/filesystem-absolute paths are never opened, normalized platform-equivalent paths and self-imports are deduplicated, and each unique dependency is content-hashed under the existing 10 MiB per-file bound. Missing/unreadable identities remain observable states, so creation/recovery triggers once. Rebuilding the set reuses prior fingerprints, and failed compilation retains the last successful dependency coverage. Unit tests cover URL/path policy, order/deduplication, missing/readable transitions, state-before-attempt semantics, and every dependency-planning allocation failure. Real child-process tests prove one dependency-only edit causes exactly one atomic output replacement followed by stable polls, while unchanged invalid CSS reports one diagnostic and later recovers when the root actually changes. A static runtime gate requires watch to pass its loaded slice into `compileLoadedFile` and forbids a call back through the rereading wrapper. Debug and ReleaseSafe each pass 463/463 across 20/20 steps (86 executable units, 327 library/core, three external API, 46 CLI integration, one helper unit). Path-package consumers pass 6/6 with 1/1 tests and complete examples pass 7/7 with 1/1 in both modes. All five release targets build and inspect as ELF x86_64/aarch64, Mach-O x86_64/aarch64, and PE x86_64. Seven package regressions, two wrapper tests, all ten transform differentials, the 21-feature/18-output/four-rejection compatibility gate, four prefix-data tests and exact generation, benchmark smoke, zero production advisories, workflow YAML, and 88/88 docs tests/build pass. The five-file npm dry run is 22.0 kB unpacked. Fresh `zig fetch .` hash `zigcss-0.3.0-Bz3pcb9VGgA089eUXvxnYPhf-c67x3_4TUc1jUB7vYfq` contains 77 allowlisted files/1,900,544 bytes and builds 3/3 in Debug and ReleaseSafe. Focused formatting/diff checks pass; repository-wide formatting remains the same 18 inherited files. | `c0142f4` |
| `PARALLEL-001` | `VERIFIED` | Reproduced the inherited static CPU-sized chunks sharing the CLI planning allocator, continuing all chunks after failure, and using `try Thread.spawn` before retaining/joining every live handle, so a spawn failure could unwind while started workers still referenced task/stack state. Replaced it with a mutex-protected dynamic index and explicit `pending/running/succeeded/failed/cancelled` task states. Worker count is the minimum of inputs, detected CPUs, and a hard cap of eight. Each stable task slot owns a non-thread-safe GPA used by exactly one worker for input, diagnostics, error strings, and public compile results; main-thread cleanup deinitializes the result/error first and asserts the allocator is leak-free before releasing the planned output path. The first observed task failure atomically closes the queue; in-flight public compile calls finish at their safe boundary, no later task is claimed, and remaining pending entries become cancelled. Spawn failure closes the queue and joins every successfully created thread before returning. A dedicated `compileBatch` scope now unwinds every task before main maps exit 1/2, including output-collision and planning failures. Only a joined all-success state reaches writes; destinations and `Compiled:` status lines are committed in original argument order, and failure diagnostics are rendered in that same stable order. Per-destination atomicity retains CLI-012's explicit non-transactional operational-write boundary. Unit tests prove the cap, claim/cancel state machine, distinct allocator contexts, leak-free failed-batch unwind, and no output on failure. A ten-input child regression exceeds the cap and exact-checks all CSS plus status order; another uses seven slow in-flight inputs to prove a missing ninth input remains unclaimed after the first parser failure and no output directory is created. Static gates reject the former chunk counters/slices and require cap, task allocator, cancellation, join, and scoped cleanup structures. Debug and ReleaseSafe each pass 469/469 across 20/20 steps (90 executable units, 327 library/core, three external API, 48 CLI integration, one helper unit). Path-package consumers pass 6/6 with 1/1 tests and complete examples pass 7/7 with 1/1 in both modes. All five release targets build and inspect as ELF x86_64/aarch64, Mach-O x86_64/aarch64, and PE x86_64. Seven package regressions, two wrapper tests, all ten transform differentials, the 21-feature/18-output/four-rejection compatibility gate, four prefix-data tests and exact generation, benchmark smoke, zero production advisories, workflow YAML, and 88/88 docs tests/build pass. The five-file npm dry run is 22.5 kB unpacked. Fresh `zig fetch .` hash `zigcss-0.3.0-Bz3pcQ9uGgCXbDf7_ud5ktsbbm2jvKrxEVURDtkjj2PA` contains 77 allowlisted files/1,904,640 bytes and builds 3/3 in Debug and ReleaseSafe. Focused formatting/diff checks pass; repository-wide formatting remains the same 18 inherited files. | `fdbda26` |
| `PROF-010` | `VERIFIED` | Replaced the CLI's legacy outer-call profiler and fabricated zero-valued stage/memory display with one opt-in session inside `zigcss.compile`. The public result now owns optional `CompileMetrics` containing one monotonic end-to-end duration, actual parse, validation/plugin-plan, dependency, optimizer, plugin/prefix transform, emit, result-promotion, and temporary-cleanup intervals, plus allocator-requested memory counters. A mutex-protected forwarding allocator counts successful allocations, deallocations, resizes/remaps, cumulative allocated/freed bytes, peak live bytes, and result bytes retained after cleanup while forwarding exact caller-allocator pointers; result buffers therefore remain valid after the stack-owned wrapper is gone and free normally through idempotent `deinit`. Disabled calls install no wrapper, sub-resolution/skipped intervals may be zero, timer unavailability is an explicit `ProfilingUnavailable` operational error, and the executable consumes only result metrics. Metrics survive `take`, clear on repeated cleanup, accompany structured diagnostic results, and remain covered through exhaustive successful/diagnostic allocation failures. Unit, external-consumer, static reachability, and child-process tests prove real stages, one report, total/stage and allocated/freed/retained invariants, default null metrics, no runtime legacy-profiler import, and leak-free ownership. Debug and ReleaseSafe each pass 473/473 across 20/20 steps (90 executable units, 331 library/core, three external API, 48 CLI integration, one helper unit). Path-package consumers pass 6/6 with 1/1 tests and complete examples pass 7/7 with 1/1 in both modes. All five release targets build and inspect as ELF x86_64/aarch64, Mach-O x86_64/aarch64, and PE x86_64. Seven package regressions, two wrapper tests, all ten transform differentials, the 21-feature/18-output/four-rejection compatibility gate, four prefix-data tests and exact generation, benchmark smoke, zero production advisories, workflow YAML, and 88/88 docs tests/build pass. The five-file npm dry run is 23.2 kB unpacked. Fresh `zig fetch .` hash `zigcss-0.3.0-Bz3pcdCyGgCF6Ov91Eawpe2bRWTFZLLQgrfY4jrNGWrY` contains 78 allowlisted files/1,921,024 bytes and builds 3/3 in Debug and ReleaseSafe. Focused formatting/diff checks pass; repository-wide formatting remains the same 18 inherited files. | `3be527e` |

## Milestone 4 exit validation

Milestone 4 is `PASS` at gate checkpoint `3be527e`.

| Gate | Result |
|---|---|
| Package closure | `PASS`; `API-001` through `API-003`, `BUILD-001` through `BUILD-003`, `CLI-010` through `CLI-012`, `WATCH-001`, `PARALLEL-001`, and `PROF-010` are verified and checkpointed. |
| Public API and ownership | `PASS`; one external `zigcss.compile` facade exposes explicit options and owned CSS, maps, located diagnostics, dependency facts, plugin policy, module-export state, and opt-in profile metrics. Successful, diagnostic, transformed, and allocation-failure paths retain one idempotent result cleanup authority; README/docs examples and the independently compiled consumer pass. |
| Build and package integration | `PASS`; root and fresh fetched-package Debug/ReleaseSafe builds each complete 3/3 steps, path-package consumers complete 6/6 with 1/1 tests, complete build examples complete 7/7 with 1/1 tests, and cacheable helper outputs are exact-checked. Zig 0.15.2 identity, allowlist, helpers, consumers, and examples are held by seven package regressions. |
| CLI contract and deterministic execution | `PASS`; the executable delegates CSS compilation through one public call site. Forty-eight integration tests plus executable units cover all admitted/rejected options, CSS format/optimization selection, stdin/stdout, exit streams/statuses, atomic destinations, collision-safe batch names, watch dependency transitions, bounded parallel cancellation/ownership, no-output failure paths, signal propagation, and argument-ordered diagnostics/writes. Single and parallel compilation exact-check identical argument-ordered results. |
| Profiling | `PASS`; one API-owned monotonic session measures actual compile stages through temporary cleanup. Forwarding-allocator requested-byte counters prove allocation/free/peak/retained invariants without changing returned pointer ownership; disabled, successful, diagnostic, moved, repeated-cleanup, and exhaustive allocation-failure cases pass without leaks or fabricated metrics. |
| Debug / ReleaseSafe and targets | `PASS`; each native mode completes 20/20 test steps and 473/473 tests (90 executable units, 331 library/core, three external API, 48 CLI integration, one helper unit). ReleaseSafe artifacts inspect as ELF x86_64/aarch64, Mach-O x86_64/aarch64, and PE x86_64. |
| Independent oracles and project surfaces | `PASS`; the 21-feature/18-output/four-rejection grammar gate, all ten transform differentials, four prefix-data tests and exact BCD generation, two npm-wrapper tests, benchmark smoke, 88/88 docs tests/Vite build, VS Code TypeScript compilation, workflow YAML, zero root production advisories, and the five-file/23.2 kB npm dry run pass. The extension has no lockfile, so no unsupported extension-local npm audit claim is made. |
| Formatting and generated state | `PASS`; package-owned Zig files, changed JavaScript syntax, and `git diff --check` pass; validation leaves no generated artifacts. Repository-wide formatting reports exactly the same 18 inherited Zig files as before Milestone 4. |

## Milestone 5 package ledger

| Package | State | Evidence / decision | Commit |
|---|---|---|---|
| `FMT-001` | `VERIFIED` | Published one machine-readable and human-readable inventory for all seven non-CSS legacy format tags plus the separate Tailwind-like `@apply` surface. Each entry names recognized extensions, current availability, compatibility evidence level, exact heuristic boundary, known risks, owning packages, accepted strategy, representative input, source files, and source anchors. The validator requires exact enum/display/extension coverage, inventories every `src/formats/*.zig` file plus `src/tailwind.zig`, proves public `Syntax` remains CSS-only, binds every strategy to accepted ADR-005, and runs nine real CLI probes that must exit 2 with the correct adapter diagnostic, empty stdout, and no output file. ADR-005 chooses removal until funded for SCSS/Sass, Less, Stylus, CSS-in-JS, PostCSS-like, and Tailwind-like behavior; only CSS Modules may pursue a closed native subset. It prohibits transformed bytes entering the inherited parser, silent deletion/fallback, implicit network/process execution, and compatibility claims without strict negative/dependency/generated-CSS/differential evidence. The previously provisional native-plugin ADR-005 is durably renumbered ADR-011. A new published guide, status/CLI/README links, sidebar entry, claims allowlist, ADR and matrix consistency tests, package script, and CI step keep the decision executable. Debug and ReleaseSafe remain 473/473 across 20/20 steps. The format gate reports 8 adapters/9 rejected probes/complete legacy-source coverage; path-package consumers pass 6/6 and examples 7/7 in both modes; all five release architectures inspect correctly. Seven package tests, two wrapper tests, 21-feature/18-output/four-rejection grammar validation, all ten transform differentials, four prefix-data tests and exact generation, benchmark smoke, VS Code TypeScript, zero production advisories, workflow YAML, and 94/94 docs tests/build pass. The five-file npm dry run is 23.3 kB unpacked. Fresh `zig fetch .` hash `zigcss-0.3.0-Bz3pcTezGgD8Jyekh-qmE6FniteDTokLOfa7bfuQ1F3X` contains 78 allowlisted files/1,921,024 bytes and builds 3/3 in Debug and ReleaseSafe. Diff/script/JSON/generated-state checks pass; repository-wide formatting remains the same 18 inherited files. | `90d51a1` |
| `SCSS-001` | `VERIFIED` | Applied ADR-005's removal strategy to both brace SCSS and indented Sass rather than declaring a heuristic subset. Deleted 3,667 lines across both parser sources and removed their legacy `Format` tags, display names, parser dispatch, and positive/vacuous tests. The test-only filename detector is now nullable: `.scss`, `.sass`, and unknown extensions return null rather than silently becoming CSS, while known remaining legacy extensions retain explicit tags. The stable CLI keeps its lightweight pre-read `.scss`/`.sass` rejection, and the matrix now distinguishes `Removed` from `LegacyCharacterized`, pins deleted paths, requires zero implementation sources/tags, binds containment anchors, and still runs both real rejection probes with no output. Tracing the nullable result exposed the experimental LSP as a hidden legacy dispatcher; removed/unknown document URIs now receive an explicit diagnostic. An explicit LSP test artifact activates 20 parser/format/protocol units, including both removed Sass requests. That test exposed and fixed an inherited double-free where one URI allocation was owned as both map key and document value; the map key is now the sole URI owner. Debug and ReleaseSafe each pass 463/463 across 22/22 steps (60 executable units, 20 LSP/legacy units, 331 library/core, three external API, 48 CLI integration, one helper unit). The format gate reports 8 adapters/2 removed implementations/9 rejected probes/complete remaining-source coverage. Consumers/examples pass in both modes; all five release targets inspect correctly. Seven package tests, two wrapper tests, all CSS/transform/prefix oracles, benchmark smoke, VS Code TypeScript, zero production advisories, workflow YAML, and 94/94 docs tests/build pass. The npm dry run remains five files/23.4 kB. Fresh `zig fetch .` hash `zigcss-0.3.0-Bz3pceF8GACkWxsnNhTe7bHX0pAa3eGS5TOmGR-9M55P` contains 76 allowlisted files/1,773,568 bytes and builds 3/3 in both modes. Focused formatting/diff/script/JSON/generated-state checks pass; deleting the two inherited unformatted sources reduces repository formatting debt from 18 to 16 files. | `79e7f36` |
| `SCSS-002` | `NOT_APPLICABLE` | ADR-005 selected removal rather than a native Sass scope, so there is no lexical/module/evaluation subset to extend. `SCSS-001` removes both implementations and proves strict CLI/LSP rejection; reintroduction requires a new funded canonical-integration decision and evidence program. | `79e7f36` |
| `LESS-001` | `VERIFIED` | Applied ADR-005's removal strategy instead of naming the global at-variable byte substitution a Less subset. Deleted the parser source and its three positive tests, then removed the legacy `Format` tag, display/dispatch path, and parser wrapper. `.less` now returns null from the test-only detector and receives the same explicit unsupported-format LSP diagnostic as both removed Sass syntaxes; stable CLI rejection remains pre-read and no-output. The executable matrix pins the absent source/tag, zero implementation files, owning removal package, containment anchors, and real exit-2 probe while preserving the historical risk statement. Debug and ReleaseSafe each pass 457/457 across 22/22 steps (57 executable units, 17 LSP/legacy units, 331 library/core, three external API, 48 CLI integration, one helper unit). The format gate reports 8 adapters/3 removed implementations/9 rejected probes/complete remaining-source coverage. Consumers/examples pass in both modes; all five release targets inspect correctly. All package/wrapper/CSS/transform/prefix/docs/editor/security/workflow/benchmark gates pass; docs remain 94/94 and npm remains five files/23.4 kB. Fresh `zig fetch .` hash `zigcss-0.3.0-Bz3pcdNTGAC9guMm53f37wx7Bypt9zc8VJN8n5EGfHAH` contains 75 allowlisted files/1,757,184 bytes and builds 3/3 in both modes. Focused checks pass; deleting the unformatted source reduces repository formatting debt from 16 to 15 files. | `fda59bc` |
| `STYLUS-001` | `VERIFIED` | Applied ADR-005's removal strategy rather than naming indentation and global dollar substitution a Stylus subset. Deleted the parser source and its two positive tests, removed the legacy `Format` tag/display/dispatch wrapper, and made `.styl` a null detector result. Stable CLI exit-2 rejection and the unsupported-format LSP diagnostic remain; the LSP request regression now covers all four removed preprocessor syntaxes. The matrix pins the absent source/tag, owning package, zero implementation files, containment anchors, and real no-output probe. Debug and ReleaseSafe each pass 453/453 across 22/22 steps (55 executable units, 15 LSP/legacy units, 331 library/core, three external API, 48 CLI integration, one helper unit). The format gate reports 8 adapters/4 removed implementations/9 rejected probes/complete remaining-source coverage. All consumers, examples, release architectures, package/wrapper/CSS/transform/prefix/docs/editor/security/workflow/benchmark gates pass; docs remain 94/94 and npm five files/23.4 kB. Fresh `zig fetch .` hash `zigcss-0.3.0-Bz3pcdcqGAB1sa5v_yNuuZ5gyk6oiaofthG-yaBt7spo` contains 74 allowlisted files/1,744,896 bytes and builds 3/3 in both modes. Focused checks pass; deleting the unformatted source reduces repository formatting debt from 15 to 14 files. | `19b79b6` |
| `MODULE-001` | `VERIFIED` | Replaced the name-only legacy FNV adapter with an explicit library-only `.css_modules` syntax over the rebuilt typed pipeline. Local-by-default classes are collected in first-authored order across nested rules and typed `:is`/`:where`/`:not`/`:has` selectors, receive versioned file-specific full SHA-256 identifiers, and are emitted through a validated sorted lookup while the owned result retains unique decoded name/value exports. Source identity, export count, and owned export bytes are bounded; empty identity, deferred local/global/import/export/composition/value behavior, raw functional pseudos, and syntax outside the closed at-rule list fail without CSS or partial exports. Pretty/minified output, exact source maps, direct CSS import facts, and profiling compose; optimizer, prefixing, and native plugins remain strict option errors. The old legacy parser/tag/dispatch are removed while CLI/LSP `.module.css` containment remains. Debug and ReleaseSafe each pass 461/461 across 24/24 steps (53 executable units, 13 LSP/legacy units, 342 library/core, four external API, 48 CLI integration, one helper unit); package consumers pass 2/2 and examples 1/1 in both modes. The independent format gate recomputes names with Node SHA-256 and recovery-disabled Lightning CSS 1.30.1 validates three outputs plus one strict rejection. All five release targets inspect correctly; all CSS/transform/prefix/package/wrapper/docs/editor/workflow/benchmark gates pass; docs are 99/99, production audit has zero vulnerabilities, and npm dry-run is five files/23,983 bytes unpacked. Fresh `zig fetch .` hash `zigcss-0.3.0-Bz3pcbOnGACJ0CjU_NHYr93CgF_mYgbpjjbC9nhNXoBg` contains 75 allowlisted files/1,777,664 disk bytes and builds plus external consumption pass in Debug and ReleaseSafe. Focused format/diff/script/JSON/generated-state checks pass; deleting the inherited adapter reduces repository formatting debt from 14 to 13 files. | `dbdf866` |
| `MODULE-002` | `VERIFIED` | Added occurrence-sensitive functional `:local(...)`/`:global(...)`, owned local/global/dependency composition references, CSS-module dependency facts, and token-aware local `@value` definitions/substitution to the closed native subset. Value definitions are top-level terminated single-name rules, resolve sequentially against earlier definitions, share first-authored export order and uniqueness with classes, and are omitted from CSS/maps. Complete-table CSS uses rewrite exact decoded identifier tokens recursively in declaration values and approved at-rule preludes while strings, URLs, comments, property/function names, and unsupported selector positions remain opaque or fail closed. One-identifier values may alias class selectors; one-string values may alias external composition paths. Imported values, raw ICSS, duplicate/nested/empty/unterminated/importance-bearing definitions, missing/cyclic composition, ambiguous scope, unsupported at-rules/selectors, and every combined resource limit produce structured no-partial-output failures. Debug and ReleaseSafe each pass 472/472 across 24/24 steps (53 executable units, 13 LSP/legacy units, 353 library/core, four external API, 48 CLI integration, one helper unit); package consumers pass 2/2 and examples 1/1 in both modes. The independent format gate validates five generated outputs including two value fixtures, one Lightning CSS 1.30.1 composition differential, and two strict rejections. All five release architectures inspect correctly; package/wrapper/CSS/transform/prefix/docs/editor/security/benchmark gates pass; docs are 99/99 and npm dry-run is five files/24,233 bytes. Fresh `zig fetch .` hash `zigcss-0.3.0-Bz3pcQL9GQAVz1YbXid7s74v7s79zog3ZdHbESDb862Q` contains 75 allowlisted files/1,855,488 disk bytes, builds 3/3, and passes direct external consumption in Debug and ReleaseSafe. Changed Zig/JavaScript/JSON/diff/generated-state checks pass; repository-wide formatting remains exactly the same 13 inherited files. | `5be06e7` |
| `JS-001` | `VERIFIED` | Applied ADR-005's removal strategy rather than treating backtick/brace scanning as JavaScript or TypeScript parsing. The red detector regression first received `.css_in_js`, the LSP regression received no unavailable-format diagnostic, and the executable matrix rejected the still-present source. Deleted the parser and its positive/vacuous expression tests, removed the legacy `Format` tag/display/dispatch function, and made both `.css.js` and `.css.ts` null detector results. Stable CLI exit-2 rejection remains pre-read/no-output; the LSP now returns the common unsupported/removed diagnostic for both extensions. The matrix pins the absent source/tag, owning removal package, zero implementation files, containment anchors, and both real extension probes. Debug and ReleaseSafe each pass 468/468 across 24/24 steps (51 executable units, 11 LSP/legacy units, 353 library/core, four external API, 48 CLI integration, one helper unit). Package consumers pass 2/2 and examples 1/1 in both modes. The format gate reports eight adapters, five removed implementations, one limited native implementation, nine rejected probes, five independent CSS Modules outputs, one composition differential, two strict module rejections, and complete source coverage. All five release architectures inspect correctly; package/wrapper/CSS/transform/prefix/docs/editor/security/benchmark gates pass; docs are 99/99 and npm dry-run is five files/24,244 bytes. Fresh `zig fetch .` hash `zigcss-0.3.0-Bz3pcZDoGQCJxC-GFYaQgepxxAumor5ZC8moivCgWAt2` contains 74 allowlisted files/1,847,296 disk bytes, builds 3/3, and passes direct external consumption in Debug and ReleaseSafe. Focused format/JSON/diff/generated-state checks pass; repository-wide formatting remains exactly the same 13 inherited files. | `f7eeb8b` |
| `POSTCSS-001` | `VERIFIED` | Applied ADR-005's removal strategy rather than renaming directive deletion as PostCSS compatibility or introducing an unfunded JavaScript host. Red detector/LSP regressions first accepted `.postcss`, and the matrix rejected the still-present scanner. Deleted the parser containing four weak positive tests, removed the legacy `Format` tag/display/dispatch, and made `.postcss` a null detector result. This also removes the only import and runtime path to the separate fixed Tailwind-like registry without deleting that next package's source. Stable CLI exit-2/no-output rejection and the LSP unsupported-format diagnostic remain. The matrix pins the absent PostCSS source/tag, containment anchors, and real probe while explicitly characterizing the utility table as unreachable. Debug and ReleaseSafe each pass 456/456 across 24/24 steps (45 executable units, five LSP/legacy units, 353 library/core, four external API, 48 CLI integration, one helper unit); the 12-test reduction is the former scanner's four tests plus the imported utility registry's two tests disappearing from each legacy-root artifact. Package consumers pass 2/2 and examples 1/1 in both modes. The format gate reports eight adapters, six removed implementations, one limited native implementation, nine rejected probes, five independent module outputs, one composition differential, two strict module rejections, and complete source coverage. All five architectures and all package/wrapper/CSS/transform/prefix/docs/editor/security/benchmark gates pass; docs are 99/99 and npm is five files/24,258 bytes. Fresh `zig fetch .` hash `zigcss-0.3.0-Bz3pcWvBGQDkGzS2hIkehTCLLQZD9t9gTCGOFeSLsuRA` contains 73 files/1,835,008 disk bytes, builds 3/3, and passes direct external consumption in both modes. Focused format/JSON/diff/generated-state checks pass; deleting the inherited unformatted scanner reduces repository formatting debt from 13 to 12 files. | `2e659aa` |
| `TAILWIND-001` | `VERIFIED` | Applied ADR-005's removal strategy instead of retaining a fixed table under the Tailwind name. The matrix's red removed-source contract failed while `src/tailwind.zig` existed; a complete import search and unchanged 456-test root proved `POSTCSS-001` had already removed its only executable path. Deleted the registry and its two unreachable positive tests, removed the hard-coded inventory exception, and now derive remaining legacy sources only from files that exist. No compiler-side Tailwind-like source, scanner, tag, or dispatch remains. Stable `.postcss` CLI/LSP rejection stays intact, while the documentation site's separate pinned canonical Tailwind dependency still builds successfully. Debug and ReleaseSafe remain 456/456 across 24/24 steps (45 executable units, five LSP/legacy units, 353 library/core, four external API, 48 CLI integration, one helper unit). Consumers pass 2/2 and examples 1/1 in both modes. The format gate reports all seven unavailable adapters physically removed, one limited native CSS Modules implementation, nine real rejection probes, five module outputs, one composition differential, two strict rejections, and complete coverage. Five architectures and all package/wrapper/CSS/transform/prefix/docs/editor/security/benchmark gates pass; docs are 99/99 and npm is five files/24,266 bytes. Fresh `zig fetch .` hash `zigcss-0.3.0-Bz3pcX9ZGQBSMsalkgBtcY0qctmu6vMm3yhZF4HQPnru` contains 72 files/1,806,336 disk bytes, builds 3/3, and passes direct consumption in both modes. JavaScript/JSON/diff/generated-state checks pass; formatting debt remains exactly 12 inherited files. | `8ee38ab` |

### MODULE-002 execution ledger

| Phase | State | Evidence / decision | Commit |
|---|---|---|---|
| Functional scope | `VERIFIED` | Test-first regression initially returned one diagnostic for canonical `:local(...)`/`:global(...)`. Emission is now keyed by exact class-selector spans, so the same decoded name can be preserved globally and generated locally in one selector list. Functional wrappers accept exactly one inline-safe compound containing a class, inherit scope through typed selector pseudos, disappear from output, and retain exact inner-class source-map anchors. Bare modes, lists/combinators, nesting, pseudo-element forms, IDs, class-free arguments, and rewrite-limit overflow produce one structured no-output failure. Silent adapter fragment parsing does not leak ordinary CSS diagnostics. Debug and ReleaseSafe each pass 465/465 across 24/24 steps; the independent Node SHA-256/Lightning CSS oracle exercises mixed scope, all 21/18/4 stable CSS compatibility cases and ten transform differentials pass, and docs remain 99/99 plus production build. Focused format, JavaScript/JSON syntax, and diff checks pass. | `70d4a56` |
| Composition and dependency references | `VERIFIED` | Added an owned `ModuleReference` union for local generated names, global authored names, and external `{ name, specifier }` references while retaining each class export's generated `value`. Only one plain local class selector may own canonical `composes`; declarations precede ordinary properties, disappear from CSS/maps, and preserve reference/dependency order. External declarations add one `.css_module` fact each to the same authored-order, count, and owned-byte boundary as CSS imports without filesystem loading or resolution. Missing locals, cycles, complex/pseudo/explicit-wrapper targets, late/important/malformed forms, `composes-with`, reference limits, and combined dependency limits fail without partial CSS/facts/exports. Move/repeated cleanup and exhaustive allocation failures cover nested reference ownership. Debug and ReleaseSafe each pass 467/467 across 24/24 steps; the format gate adds a Lightning CSS 1.30.1 composition-reference differential to three independently parsed outputs and one strict rejection. Stable CSS, all ten transform differentials, 99/99 docs plus build, focused formatting, JavaScript/JSON syntax, and diff checks pass. | `1153006` |
| Values and final package gate | `VERIFIED` | Test-first local-value coverage initially returned `CSS0009` before implementation. ZigCSS now accepts exactly one top-level terminated `@value <ident>: <component-values>;` definition, resolves definitions sequentially, rewrites complete-table CSS uses by decoded identifier token, and exports normalized owned values in first-authored order with classes. Strings/URLs stay opaque, comments normalize as safe separators, functions/blocks recurse, one-ident values may alias classes, and one-string values may alias external composition paths. Definitions and composition declarations are omitted without fabricated mappings; rewritten output maps to the authored use token. Imported/raw ICSS, duplicate/nested/empty/unterminated/importance-bearing forms and unsafe selector uses fail without partial results. Export/reference/owned-byte/dependency/rewrite boundaries remain combined and allocation-failure-clean. Full Debug/ReleaseSafe, consumers, examples, five targets, fresh fetched-package consumption, all independent oracles, benchmark, docs, editor, audit, package, formatting, syntax, generated-state, and diff gates pass with the evidence recorded in the package row above. | `5be06e7` |

## Milestone 5 exit validation

Milestone 5 is `PASS` at gate checkpoint `8ee38ab`.

| Gate | Result |
|---|---|
| Strategy and package closure | `PASS`; accepted ADR-005 chooses one explicit strategy for all eight inventoried adapters. SCSS/Sass, Less, Stylus, CSS-in-JS, PostCSS-like behavior, and Tailwind-like behavior are physically removed; CSS Modules alone exposes a closed experimental library syntax. Every listed package is verified or explicitly not applicable under that decision. |
| Unsupported behavior | `PASS`; nine real alternate-extension probes exit 2 with the adapter diagnostic, empty stdout, and no output file. Removed/unknown LSP document formats receive one explicit unsupported-format diagnostic rather than CSS fallback. The native CSS Modules subset rejects unsupported scope, composition, value, selector, at-rule, option, dependency, and resource-limit forms without partial CSS or metadata. |
| Strings, comments, dependencies, and generated CSS | `PASS`; removed adapters have no byte preprocessor capable of corrupting quoted or URL text. CSS Modules uses tokenizer/typed-AST identity, keeps strings/URLs opaque, normalizes comments only inside its documented local-value serialization, owns bounded dependency facts, and validates five pretty/minified fixture projections through the stable parser/emitter and recovery-disabled Lightning CSS 1.30.1. |
| Fixture, negative, differential, and ownership evidence | `PASS`; the matrix runs nine containment probes, five independently parsed module outputs including two value fixtures, one composition differential, and two strict module rejections. Root tests cover limits, moves, repeated deinit, and exhaustive allocation failure; package and fetched-cache consumers prove result ownership outside the implementation tree. |
| Documentation and graduation boundary | `PASS`; README, status, ADR-005, the executable matrix, and the published guide state exact availability and ownership. No broad upstream compatibility claim graduates; canonical-suite-level evidence remains required. The docs site's real Tailwind dependency is distinct from the removed compiler adapter and its production build passes. |
| Release regression surface | `PASS`; Debug and ReleaseSafe are 456/456, consumers/examples pass both modes, five release architectures inspect correctly, the fresh 72-file package builds and consumes in both modes, all independent CSS/transform/prefix/docs/editor/audit/benchmark gates pass, and npm remains a five-file dry run. |

## Milestone 6 package ledger

| Package | State | Evidence / decision | Commit |
|---|---|---|---|
| `NEOVIM-001` | `VERIFIED` | Red characterization proved the inherited file still used deprecated `require('lspconfig').…setup()`, claimed Neovim 0.5, attached unsupported SCSS/Sass/Less/Stylus, and mapped declaration/implementation/signature/code-action/formatting methods the server does not advertise. Replaced it with a built-in `vim.lsp.config`/`vim.lsp.enable` runtime file requiring Neovim 0.11.7+, attaching only CSS, using `.git` or single-file roots without filesystem scanning, and enabling omnifunc only after the real client advertises completion. Explicit `ZIGCSS_LSP_PATH` must be a bounded absolute regular executable and fails without fallback; automatic discovery examines at most 1,024 absolute `PATH` directories and 64 validated Windows `PATHEXT` forms, ignoring empty/relative entries before passing exactly `[absolute_path, "--lsp"]`. Four static config/claims/workflow tests plus isolated real headless smokes run against the supported minimum Neovim 0.11.7 and current signed stable 0.12.4. Both verify the live command, CSS attachment, UTF-16 and full sync, pull diagnostic provider plus seven editor methods, six absent methods, completion omnifunc, an actual empty full diagnostic report, an exact `color` hover, clean shutdown, and live rejection of a relative configured executable. CI downloads both official Linux archives and verifies SHA-256 `38a7c631…f7991` and `012bf3fc…78628`; local macOS arm64 validation verified `9c84686c…9a126` and `51ab83af…24c89`. Debug and ReleaseSafe pass 489/489 across 30/30; docs pass 115/115 and build; the VS Code extension remains 13/13 with an exact five-file/120,473-byte VSIX. Seven package, two wrapper, prefix, eight-format, 21-feature/18-output/four-rejection CSS, ten transform, benchmark, workflow syntax, and all three production-audit gates pass. Root npm dry run is five files/25,652 bytes. Final package `zigcss-0.3.0-Bz3pcVgtGwC43k52obcDIv9gmTtqNfcInX351qH46Vq1` contains 75 allowlisted files/1,933,312 disk bytes, builds 3/3 in both modes, and its dependency consumer passes 6/6 steps with 2/2 tests in Debug and ReleaseSafe. Generated state/diff checks pass and formatting debt remains exactly 12 inherited files. | `0f31340` |
| `LSP-001` | `VERIFIED` | Reproduced a valid small frame exiting 1 with `UnexpectedEndOfInput`, then added a child transcript whose 9 KiB initialization body failed at the inherited fixed request array with `BufferTooSmall`. Replaced delimiter/array parsing with a dedicated bounded transport that consumes exact CRLF headers, accepts case-insensitive `Content-Length` plus optional HTTP-style fields, dynamically owns each body up to 16 MiB, handles sequential frames, and treats EOF at a frame boundary as clean completion. Missing, duplicate, signed, non-decimal, overflowing, over-limit, bare-LF, overlong-header, and truncated framing fail before JSON handling. Exact response framing and all allocation failures are covered. The four transport tests are an explicit build artifact, and a real two-request child transcript proves the >8 KiB body, response boundaries, second request, and clean exit. Debug and ReleaseSafe each pass 461/461 across 26/26 steps (45 executable units, five legacy LSP units, four transport units, 353 library/core, four external API, 49 CLI integration, one helper unit). Consumers pass 2/2 and examples 1/1 in both modes. All five architectures and package/wrapper/CSS/format/transform/prefix/docs/editor/security/benchmark gates pass; docs are 101/101 and npm is five files/24,446 bytes. Fresh `zig fetch .` hash `zigcss-0.3.0-Bz3pcfV0GQD0C2wXLAj9fdh-wkFLpCCxhutwY5bgxuHM` contains 73 files/1,814,528 disk bytes, builds 3/3, and passes direct consumption in both modes. Focused formatting/diff/generated-state checks pass; repository formatting debt remains exactly 12 inherited files. JSON-RPC serialization/errors, request/notification and shutdown lifecycle, versions, cancellation, UTF-16 positions, compiler diagnostics, and syntax-aware features remain explicitly deferred to `LSP-002` through `LSP-007`. | `8776f25` |
| `LSP-002` | `VERIFIED` | Test-first child transcripts reproduced malformed framed JSON terminating the server, hostile string IDs producing invalid JSON, and array-shaped parameters panicking through unchecked union access. Each request now parses in a caller-backed arena with the standard JSON parser, and every response/error/handler value uses `std.json.Stringify`; strings and dynamic object keys are escaped, arbitrary-precision numeric IDs remain JSON numbers, and only JSON-RPC string/number/null IDs are admitted. Parse, invalid-request, method-not-found, and invalid-params responses use `-32700`, `-32600`, `-32601`, and `-32602`; malformed JSON returns one framed error and the next request is processed. All method parameters are type-checked before handler use. The stateful allocation regression then reproduced a real use-after-free segfault when response serialization failed after document insertion; scoped ownership transfer now keeps open/change state valid on every serializer failure, with exhaustive parse/response/document allocation checks. A complete-response table across all ten dispatch paths subsequently exposed 69 leaked allocations in successful legacy diagnostics; the returned stylesheet now deinitializes its AST and transferred string pool. Request-versus-notification silence and shutdown/document/version/cancellation lifecycle deliberately remain `LSP-003`. Debug and ReleaseSafe each pass 468/468 across 26/26 steps (45 executable, ten LSP JSON/legacy, four transport, 353 library/core, four external API, 51 CLI integration, one helper). Consumers pass 2/2 and examples 1/1 in both modes; five release targets inspect correctly. All CSS/format/transform/prefix/package/wrapper/docs/editor/security/benchmark gates pass; docs are 102/102, both root and docs production audits are clean, and npm remains five files/24,602 bytes. The gate exposed the direct `react-router` advisory and pinned its fixed 7.18.1 release in `2a660f1`; `DEP-001` update automation remains later work. Fresh package `zigcss-0.3.0-Bz3pceidGQBZ5tyOUueNEYWKB71sq0L85p0ELHnVdBnK` contains 73 files/1,826,816 disk bytes, builds 3/3, and passes external consumption in both modes. Diff/generated-state checks pass and formatting debt remains exactly 12 inherited files. | `8fe77f1` |
| `LSP-003` | `VERIFIED` | Pinned the implementation boundary to the official LSP 3.17 lifecycle used by the current VS Code client. Test-first child transcripts reproduced valid notifications emitting `id:null` responses, pre-initialize requests reaching ordinary dispatch, `shutdown`/`exit` being unknown methods, and exit without shutdown returning status 0. A typed `MessageResult` now separates owned responses, silent notifications, and exit control from transport; the executable writes only response variants. Pre-initialize notifications are dropped, requests receive `-32002`, initialize succeeds once, shutdown returns `result:null` without exiting, all later requests return `-32600`, post-shutdown notifications are dropped, and exit returns 0 only after shutdown or 1 otherwise. Notification dispatch owns `initialized`, balanced open/change/close, and validated `$/cancelRequest`; unknown and invalid notifications are silent. Cancellation is intentionally ignored after validation because the single-threaded server finishes a request before reading another frame, so no request is in flight when cancellation arrives. Initialize advertises only `{ openClose: true, change: 1 }`, removing premature diagnostic/hover/completion claims. Open requires URI/language/version/full text and rejects duplicate ownership; change requires an already-open document, exactly one range-free full replacement, an in-range 32-bit version strictly above the stored version, and commits only after allocation; close removes and frees the sole URI/text ownership. Stale, duplicate, incremental, unopened, and unbalanced notifications do not mutate state. Unit tests cover every handler JSON result, full-sync boundaries, close/reopen, and exhaustive parse/response/document/lifecycle allocation failure; real transcripts cover notification silence, cancellation, initialization, shutdown, post-shutdown behavior, and both exit statuses. Debug and ReleaseSafe each pass 470/470 across 26/26 steps (45 executable, ten LSP, four transport, 353 library/core, four external API, 53 CLI integration, one helper). Consumers pass 2/2 and examples 1/1 in both modes; all five release targets inspect correctly. All CSS/format/transform/prefix/package/wrapper/docs/editor/security/benchmark gates pass; docs are 103/103, both production audits are clean, and npm remains five files/24,594 bytes. Fresh package `zigcss-0.3.0-Bz3pcYa_GQBA60Wg1E22zDPZ82leHidy66KfYaZATcNU` contains 73 files/1,835,008 disk bytes, builds 3/3, and passes external consumption in both modes. Diff/generated-state checks pass and formatting debt remains exactly 12 inherited files. | `8388205` |
| `LSP-004` | `VERIFIED` | Test-first direct coverage reproduced byte-oriented navigation returning `null` when a requested symbol followed non-BMP and multibyte text. Added one allocation-free shared position module that validates UTF-8, recognizes LF/CRLF/lone-CR lines, maps zero-based UTF-16 code units to UTF-8 byte boundaries and back, counts astral scalars as two units, clamps overlong client characters to the line end, and rejects mid-surrogate, mid-scalar, and inside-CRLF offsets. JSON positions are bounded to the LSP `uinteger` range; initialization explicitly advertises `positionEncoding: utf-16`. Hover, completion, definition, references, rename, unsupported-format diagnostics, and legacy parse diagnostics now use the converter rather than byte columns, while these feature capabilities remain unadvertised pending syntax-aware replacement. Focused tests cover every line ending, BMP/astral conversion in both directions, invalid UTF-8 and protocol positions, exact hover/navigation/diagnostic ranges, and a real child transcript across an astral prefix plus CRLF. Debug and ReleaseSafe each pass 476/476 across 28/28 steps (45 executable, 13 LSP, four transport, two standalone position, 353 library/core, four external API, 54 CLI integration, one helper). Consumers pass 2/2 and examples 1/1 in both modes; all five release targets inspect correctly. All CSS/format/transform/prefix/package/wrapper/docs/editor/security/benchmark gates pass; docs are 104/104, both production audits are clean, and npm remains five files/24,526 bytes. Fresh package `zigcss-0.3.0-Bz3pca7pGQCVhcXki_24GmTUfWnyTcgsr75WErSmlJ3W` contains 74 files/1,847,296 disk bytes, builds 3/3 in both modes, and passes external consumption in both modes. Diff/generated-state checks pass and formatting debt remains exactly 12 inherited files. | `f3254ca` |
| `LSP-005` | `VERIFIED` | Pinned the diagnostic surface to the official LSP 3.17 pull model. The red initialization regression crashed because `diagnosticProvider` was absent, and the inherited plural method could return only one legacy-parser error. Initialization now advertises provider identifier `zigcss` with no inter-file or workspace diagnostics; the standard singular `textDocument/diagnostic` request returns a full report and the plural prototype method returns `-32601`. CSS documents compile through the external `zigcss` module and the same owned public facade as the CLI, eliminating the LSP's direct parser/error-module imports without duplicating the 353-test core graph. Every recoverable compiler diagnostic retains authored order, exact message, stable code label, mapped error/warning/information severity, and byte-span-derived UTF-16 range; a three-error fixture proves recovery across two lines. Matching identifiers and string previous-result IDs are validated, unopened/malformed requests return `-32602`, operational compiler failures return `-32803`, and full reports deliberately omit result IDs until caching has measured value. Removed, library-only, and unknown formats produce one `CSS0009` diagnostic without CSS fallback, including empty/non-BMP/line-ending-safe ranges. Focused tests cover the full schema, provider fields, valid empty results, legacy-method rejection, every invalid parameter class, all severity mappings, unsupported formats, deterministic prevalidation before JSON emission, and exhaustive allocation failures. A real child transcript proves capability negotiation and all three compiler diagnostics through the executable. Debug and ReleaseSafe each pass 478/478 across 28/28 steps (45 executable, 14 LSP, four transport, two standalone position, 353 library/core, four external API, 55 CLI integration, one helper). Consumers pass 2/2 and examples 1/1 in both modes; all five release targets inspect correctly. All CSS/format/transform/prefix/package/wrapper/docs/editor/security/benchmark gates pass; docs are 105/105, both production audits are clean, and npm remains five files/24,491 bytes. Fresh package `zigcss-0.3.0-Bz3pcb8KGgBV2UuMntJ3r5CTctNnJCx6FUZIdVWQp4kv` contains 74 files/1,855,488 disk bytes, builds 3/3 in both modes, and passes external consumption in both modes. Diff/generated-state checks pass and formatting debt remains exactly 12 inherited files. | `0dd724c` |
| `LSP-006` | `VERIFIED` | The red initialization regression first crashed because `hoverProvider` was absent, pinning the rule that no editor feature is advertised before its replacement exists. Replaced unrestricted byte/substring navigation with a dedicated index built from `zigcss.css.pipeline.parse`. It owns typed class/ID selectors including parsed selector-list pseudos, custom-property declarations/`@property` definitions and `var()` references, keyframe definitions and grammar-validated `animation-name` references, declaration properties/contexts, and opaque token spans. Quoted keyframe names remain grammar-aware while unrelated strings, comments, URLs, invalid recovered lists, and ambiguous animation shorthands are excluded. Cursor-boundary regressions prefer the token beginning at a shared endpoint. Lazy per-version indexes invalidate only after an allocated full change and free on close; cross-document operations inspect only open CSS documents and sort URI then source order without filesystem scanning, import loading, or project-graph inference. Class/ID names are external DOM references: hover/symbol/reference/syntax-only rename work, but definition deliberately returns null. Custom properties/keyframes support deterministic open-document definition/reference/rename. Completion returns a closed property-name list only in declaration-name contexts with exact UTF-16 edits; value, selector, string, and comment positions remain empty. Rename validates kind/UTF-8/NUL/reserved names and serializes one safe CSS identifier spelling. Initialize now advertises the standard LSP 3.17 hover, completion, document/workspace symbol, definition, references, and rename capabilities. Persistent state is bounded to 4,096 documents, 256 MiB text, 16 MiB aggregate/64 KiB individual URIs, and 128 MiB allocator-measured index storage; each index is capped at 32 MiB plus count/name/depth/scan budgets. Workspace query/result/rename sizes are bounded, case-insensitive query matching is linear-time, sorting is `O(n log n)`, and every dynamic editor response is preflighted against a 16 MiB worst-case escaped-JSON budget before serialization. Transactional state/resource-limit, invalid-name, false-positive, cache invalidation, close, deterministic ordering, exact range, adjacent-token, quoted-keyframe, and exhaustive index/handler allocation-failure tests pass. A real child transcript proves all advertised capabilities and multi-document hover/completion/symbol/navigation/rename behavior; the migrated LSP-004 transcript now uses a real custom-property definition rather than the removed class-definition guess. Debug and ReleaseSafe each pass 487/487 across 30/30 steps (45 executable, 19 LSP, four transport, two standalone position, three standalone index, 353 library/core, four external API, 56 CLI integration, one helper). Consumers pass 2/2 and examples 1/1 in both modes; all five release targets inspect correctly. The 21-feature/18-output/four-rejection CSS gate, all ten transform differentials, eight-format matrix, four prefix tests/exact generation, seven package tests, two wrapper tests, benchmark smoke, VS Code TypeScript, workflow/docs/security gates, and both production audits pass; docs are 106/106 and npm is five files/24,554 bytes. Fresh package `zigcss-0.3.0-Bz3pcUQeGwBAH4kwVSBgRxGeS40Wqv6B2OWEHU1sNU9y` contains 75 files/1,929,216 disk bytes, builds 3/3, and passes 4/4 direct external-consumer tests in Debug and ReleaseSafe. Generated state/diff checks pass and repository formatting debt remains exactly the same 12 inherited files. | `e930091` |
| `LSP-007` | `VERIFIED` | Audited the separately completed framing, JSON recovery, lifecycle, UTF-16, diagnostics, syntax-index, allocation-failure, and child-transcript coverage, then added the missing system-level stress proof without changing production behavior. One real executable transcript frames malformed JSON and an invalid array envelope before initialization, continues normally, synchronizes a CSS document above 1 MiB, resolves a non-ASCII custom property across an astral prefix and CRLF to exact UTF-16 coordinates, returns exact property hover and an empty compiler-backed full diagnostic report, silently processes close, rejects a subsequent positional request with `-32602`, returns null for shutdown, exits 0, consumes every response frame exactly, and keeps output below 64 KiB. A separate allocator-checked server test repeats Unicode open/index/full-change/re-index/close 64 times; each cycle builds both index versions and proves document count, synchronized text bytes, URI bytes, and allocator-measured index bytes return to zero. Existing exhaustive frame/JSON/document/editor allocation injection, framing rejection, malformed-request continuation, non-BMP, and lifecycle transcripts complete the matrix required by the roadmap. Debug and ReleaseSafe each pass 489/489 across 30/30 steps (45 executable, 20 LSP, four transport, two standalone position, three standalone index, 353 library/core, four external API, 57 CLI integration, one helper). All five release artifacts inspect correctly; the 21-feature/18-output/four-rejection CSS gate, ten transform differentials, eight-format matrix, prefix/package/wrapper gates, VS Code TypeScript, and both production audits pass. Docs are 107/107 and build; npm is five files/24,626 bytes. Fresh package `zigcss-0.3.0-Bz3pcTYqGwClVRaT1SNHl9UaASqZOMRzpD97q90LY2S7` contains 75 files/1,929,216 disk bytes, builds 3/3, and passes 4/4 direct external-consumer tests in Debug and ReleaseSafe. Generated state/diff checks pass and formatting debt remains exactly 12 inherited files. | `fc6cc22` |
| `VSCODE-001` | `VERIFIED` | The red baseline had no extension test script or lockfile, version 0.1.0 diverged from the 0.3.0 Zig/npm package, VS Code 1.74 conflicted with the language client's 1.82 engine, activation included unsupported preprocessors, executable launch was unchecked, and an unused recursive watcher widened scope. Version 0.3.0 now has an exact npm lockfile and direct pins for TypeScript 5.9.3, esbuild 0.28.1, official `@vscode/vsce` 3.9.2, VS Code/Node types, and `vscode-languageclient` 9.0.1. The workspace extension activates only for file-backed CSS and is disabled in untrusted/virtual workspaces. Binary discovery fails closed: explicit absolute executables are verified, explicit failures never fall back, bare commands search only bounded absolute `PATH` entries, empty configuration prefers a future extension-local binary, POSIX requires a regular executable with execute permission, Windows honors `Path`/`PATHEXT`, relative/workspace paths are never executed, and path/argument count/length/NUL limits are enforced again at runtime. One esbuild CommonJS entry replaces shipped dependencies; the exact pre-release VSIX contains only `package.json`, README, MIT license, exact runtime dependency notices, and `dist/extension.js`, with no native binary, source map, lockfile, test, or build tool. Thirteen discovery/real-file/manifest/version/workflow tests pass; the pinned verifier reports five files/120,473 VSIX bytes and removes all generated state. CI uses Node 22 plus `npm ci --ignore-scripts`; no publish/release action was added. Debug and ReleaseSafe remain 489/489 because production Zig is unchanged. Docs pass 111/111 and build; seven package, two wrapper, prefix, eight-format, 21-feature/18-output/four-rejection CSS, and ten transform gates pass. Root/docs/extension production audits are zero, root npm dry run is five files/25,142 bytes, syntax/diff/generated-state checks pass, and formatting debt remains exactly 12 inherited files. Final fresh package `zigcss-0.3.0-Bz3pccorGwDJKFEnRD0pbnqB5CEiZKMJc2q_IlWe4w9k` contains 75 allowlisted files/1,933,312 disk bytes, builds 3/3 in both modes, and its real dependency consumer passes 6/6 steps with 2/2 tests in Debug and ReleaseSafe. | `76765e2` |

## Milestone 6 exit validation

Completed on recovery commit `0f31340`.

| Gate | Result |
|---|---|
| Requests, notifications, and lifecycle | `PASS`; bounded transcripts cover framing, malformed JSON/envelopes, notification silence, initialization, shutdown, exit, cancellation validation, document versions, close invalidation, and both process statuses. |
| Large documents and ownership | `PASS`; a real transcript synchronizes more than 1 MiB and the repeated allocator-checked Unicode/index lifecycle returns all persistent counters and owned bytes to zero. |
| UTF-16 positions | `PASS`; shared conversion and real ZigCSS/Neovim requests prove non-ASCII, astral, CRLF, clamping, invalid-boundary, diagnostic, definition, completion, and hover coordinates. |
| Advertised capabilities | `PASS`; server unit/transcript tests and two independent Neovim clients require exactly full sync, pull diagnostics, hover, completion, document/workspace symbols, definition, references, and rename while six unimplemented editor methods remain absent. |
| Syntax-aware workspace behavior | `PASS`; typed bounded indexes cover only synchronized open CSS documents, reject opaque/ambiguous syntax, preserve deterministic URI/source order, and never scan the filesystem or fabricate class/ID definitions. |
| Editor clients | `PASS`; the VS Code client compiles/tests/packages from an exact lockfile and the built-in Neovim config passes real 0.11.7/0.12.4 attach, diagnostic, hover, rejection, and shutdown smokes in isolated state. Neither artifact bundles a native binary or publishes. |
| Full regression surface | `PASS`; Debug/ReleaseSafe are 489/489, docs are 115/115, all independent CSS/transform/prefix/package/wrapper/audit/benchmark gates pass, the final fetched package builds and consumes in both modes, and unchanged production binaries retain the prior five-target inspection evidence. |

Milestone 6 exit criteria pass: protocol composition, body sizing, UTF-16 coordinates, capability truthfulness, syntax-aware bounded workspace operations, and both local editor integrations have executable evidence.

## Milestone 7 package ledger

| Package | State | Evidence / decision | Commit |
|---|---|---|---|
| `DOC-001` | `VERIFIED` | Red baselines proved there was no capability-status generator or gate and the site still claimed LSP parser migration and editor features were future work after both editor packages had passed. Added one schema-versioned `docs/src/data/capabilities.json` authority with a closed four-kind status vocabulary, 19 unique public surfaces, and 13 referenced executable evidence gates. Each gate must resolve to a real root/docs npm script or real Zig build step and every content anchor must be a regular file whose canonical path remains inside the repository; duplicate rows/evidence, unknown or contradictory status, malformed inline code, unused gates, symlink escapes, missing/duplicate generation markers, and stale tables fail closed. One deterministic generator owns byte-identical marked tables in README and the status guide, while the React feature page consumes the same metadata directly and renders validated inline code without literal Markdown. Seven generator tests, four docs metadata tests, six feature-page tests, and a CI drift gate prevent divergence. Debug and ReleaseSafe remain 489/489 across 30/30; docs pass 121/121 and build; VS Code remains 13/13 with a five-file/120,473-byte VSIX and both real Neovim versions pass four static gates plus integration. Prefix generation, seven package tests, two wrapper tests, eight-format, 21-feature/18-output/four-rejection CSS, ten transform, benchmark, syntax, diff, and all three production-audit gates pass. Root npm remains five files/25,939 unpacked bytes. Isolated package `zigcss-0.3.0-Bz3pcW8tGwBkfoEoLEjkUVh-xoK142KGmr0MldSKGVOM` contains 75 allowlisted files/1,933,312 disk bytes, builds 3/3, and its external dependency consumer passes 6/6 steps with 2/2 tests in Debug and ReleaseSafe. Formatting debt remains exactly 12 inherited files. | `8317d09` |
| `DOC-002` | `VERIFIED` | The red repository-wide gate found the published CSS Modules Zig snippet was an uncompiled fragment while only the duplicated basic API snippet had canonical source coverage. Added one closed documentation policy and a bounded validator over all 31 tracked Markdown files plus rendered React examples. It classifies 59 code/presentation fences, of which 49 are executable and three are rendered directly by the site: shell is syntax-checked under Bash; JSON is parsed; ordinary and CSS Modules examples compile through real ZigCSS binaries and their emitted CSS is reparsed by recovery-disabled Lightning CSS; Lua and Ex commands are parsed by an explicit absolute Neovim 0.11.7-or-later executable in clean headless state; two exact canonical Zig programs compile and run through `zig build test-documentation-examples` in Debug and ReleaseSafe. The approved roadmap's evolving API-shape sketch is the sole SHA-256-pinned non-executable Zig fence. The CSS Modules fragment is now a self-contained canonical program that verifies owned exports and runs with exact empty stdout; the existing public API example now runs with exact CSS stdout. Inline, image, reference-definition, reference-use, HTML, relative-file, heading-fragment, docs-route, and literal React routes are resolved; malformed encoding, missing/duplicate references, unknown routes, lexical escapes, and symlink escapes fail closed. Every child has a 15-second/4 MiB bound, editor discovery has no fallback, and invalid Bash/JSON/CSS/Lua/Vim regressions prove nonzero failures propagate. Seven validator tests pass on both Neovim 0.11.7 and 0.12.4; the gate resolves 41 internal Markdown links and 24 literal site routes. Debug and ReleaseSafe pass 489/489 across 34/34; docs pass 123/123 and build; VS Code remains 13/13 with a five-file/120,473-byte VSIX. Capability evidence now includes 14 used gates. Prefix, seven package, two wrapper, eight-format, 21-feature/18-output/four-rejection CSS, ten transform, benchmark, syntax, generated-state, diff, and all three production-audit gates pass. Root npm is five files/26,233 unpacked bytes. Fresh package `zigcss-0.3.0-Bz3pcdIxGwCc1tlYjTLFyVk6XNxbG9yAkxTbeaWggdlS` contains 75 files/1,937,408 disk bytes, builds 3/3, and its consumer passes 6/6 steps with 2/2 tests in both modes. Formatting debt remains exactly 12 inherited files. | `b247067` |
| `DEP-001` | `VERIFIED` | Live red baselines found zero production vulnerabilities in the root, documentation, and VS Code lock graphs but no repository update automation or executable policy preserving that state. Added a closed inventory of exactly three npm manifests and version-3 lockfiles. Root identity and dependencies/dev/optional/peer maps must match each lock root; every direct dependency/dev/optional specifier is exact except the one reviewed `docs -> file:..` package edge; a new unowned lockfile or version range fails. A bounded parser accepts only consistent npm audit report v2 counts and rejects any high/critical finding or malformed/offline result. Four tests cover live inventory, unpinned/extra-lock negatives, report version/count/high severity negatives, and exact automation. CI audits each production graph directly from its lockfile with lifecycle scripts absent and a high threshold; current root/docs/extension results are each 0 total, 0 high, and 0 critical. The official Dependabot v2 configuration uses one npm entry for `/`, `/docs`, and `/vscode-extension`, plus GitHub Actions and root Docker entries, all weekly with version-update PR limits 3/2/1. Exact rendering forbids registry credentials, target-branch changes, reviewers/assignees, and automerge; it creates no publishing or deployment path. Capability evidence now uses 15 gates. Debug and ReleaseSafe remain 489/489 across 34/34; docs pass 124/124 and build; VS Code stays 13/13 with a five-file/120,473-byte VSIX; both real Neovim integrations pass. Documentation integrity remains seven tests over 31 Markdown files, 59 fences, 41 links, and 24 routes. Prefix, package, wrapper, format, compatibility, transform, benchmark, syntax, generated-state, and diff gates pass. Root npm is five files/26,564 unpacked bytes. Fresh package `zigcss-0.3.0-Bz3pcSAyGwADM0J9-GHXsywUvbTT7o9F_73bo1zFatyI` contains 75 files/1,937,408 disk bytes, builds 3/3, and its consumer passes 6/6 steps with 2/2 tests in both modes. Formatting debt remains exactly 12 inherited files. | `d5b2d2b` |
| `REL-001` | `VERIFIED` | The red inventory found root npm/Zig/CLI/editor claims at 0.3.0, the linked docs lock at 0.2.1, Homebrew at 0.1.0, a stale 0.3 homepage claim, and no canonical authority or tag preflight. Added root `VERSION` at the roadmap target `0.4.0-rc.1` and synchronized npm, Zig, CLI, Docker OCI labels, formula, changelog, Neovim, public docs, generated capability claims, and tests. VS Marketplace's three-part manifest maps to 0.4.0 and is required to package with `--pre-release`. A dependency-free validator owns exactly 26 regular non-symlink surfaces, strict SemVer, exact literal counts, lock/manifest agreement, formula/Docker/runtime claims, the plan target, CI order, and tag `v0.4.0-rc.1`; six regressions cover drift, invalid versions/tags, inventory growth, CI/package policy, and symlink substitution. The build workflow gates version integrity before npm installation and release jobs reject mismatched tags before artifact construction. Debug and ReleaseSafe pass 489/489 across 34/34; docs pass 125/125 and build; VS Code passes 13/13 and packages an exact five-file/120,473-byte prerelease VSIX; both real Neovim versions pass. All five targets inspect correctly after both ReleaseSafe and ReleaseFast builds. Production audits remain 0/0/0; documentation, capability (19 rows/16 gates), prefix, package, wrapper, format, compatibility, transform, benchmark, syntax, generated-state, and diff gates pass. Root npm is five files/26,952 unpacked bytes. Fresh package `zigcss-0.4.0-rc.1-Bz3pcdcyGwCUSsVNZwy680fwINFYZrcEX2bgxkLUfo8B` contains 75 files/1,937,408 disk bytes, builds 3/3, remains byte-count invariant, and its external consumer passes 6/6 steps with 2/2 tests in Debug and ReleaseSafe. The docs image build/check and OCI label inspection pass; the root Dockerfile's live check reached only a Docker Hub base-metadata `DeadlineExceeded`, while the closed static version gate passes and full asset verification remains owned by `REL-003`. Formatting debt remains exactly 12 inherited files. | `aef12af` |
| `REL-002` | `VERIFIED` | The red workflow produced five native archives but no checksum manifest, SBOM, signature, or provenance asset, and had no consumer-verifiable closed inventory. Added a dependency-free generator that accepts only the five reviewed target/archive/binary identities, strict canonical version and 40-character commit identity, bounded epoch and file sizes, repository-confined regular non-symlink inputs, and the exact `release-assets` output directory. It streams SHA-256/SHA-1 hashing, emits deterministic SPDX 2.3 JSON for the one executable actually contained in each archive, computes the SPDX package verification code, emits a two-subject SHA-256 manifest, and writes only new-or-byte-identical metadata. A bounded local Sigstore parser validates v0.3 bundle structure, signing material, DSSE/in-toto envelopes, exact basenames/digests, SLSA v1 shape, and byte-equivalent SPDX predicates before accepting exactly five regular assets. The tag matrix now creates one archive, manifest, SBOM, pinned `actions/attest` v4.1.1 SLSA bundle, and signed SBOM bundle per target; its release build job adds only the documented read, attestation, and OIDC grants, preserves the bundles, runs two `gh attestation verify` cryptographic checks, and uploads only the closed inventory with fail-closed seven-day retention. CI runs five metadata regressions and the workflow check before dependency installation; permission/action policy now owns 21 immutable invocations. A real native ReleaseFast archive regenerated and independently verified both manifest subjects and parsed as SPDX-2.3. No tag, attestation, artifact, or release was created locally. Debug/ReleaseSafe pass 489/489 across 34/34; docs pass 125/125 and build; capability metadata is 20 rows/17 gates; VS Code remains 13/13 with its exact five-file/120,473-byte VSIX; both real Neovim versions pass; all five targets inspect after ReleaseSafe and ReleaseFast builds; production audits remain 0/0/0. Documentation, prefix, package, wrapper, format, compatibility, transform, benchmark, syntax, generated-state, version/tag, and diff gates pass. Root npm is five files/27,463 unpacked bytes. Fresh package `zigcss-0.4.0-rc.1-Bz3pcSI0GwCPrekKhSZVkE2YVJxHCBiaz2o3myjhjJV4` contains 75 files/1,937,408 disk bytes, builds 3/3 without mutation, and its dependency consumer passes 6/6 steps with 2/2 tests in both modes. Formatting debt remains exactly 12 inherited files. | `6e79bfc` |
| `REL-003` | `VERIFIED` | Closed the npm, Docker, and Homebrew consumer trust boundaries without publishing an artifact or tap. npm retains exact five-target naming, bounded credential-free HTTPS, canonical manifest parsing, constant-time archive verification, one-entry extraction, independent executable-header checks, symlink rejection, cleanup, and late replacement. Docker consumes the complete five-file local inventory, binds archive and SBOM digests, admits only a matching Linux ELF, and copies one mode-0555 binary into a non-root, one-layer `scratch` image. Homebrew now pins the immutable production-source checkpoint `3fada2359ab1fa262b782a33e9ab2a7bab2c46ca` and its real SHA-256 `2fc630a4…c906`, requires the reviewed `zig@0.15` build tool instead of ambient Zig 0.16, removes the unverified head path, and exact-checks version plus compiled CSS. Sixteen hermetic consumer regressions reject mutable source URLs, empty/wrong trust data, unpinned toolchains, second downloads, archive/SBOM tampering, target mismatch, extra entries, inventory drift, symlinks, and CI reordering. The live Homebrew gate downloads over bounded HTTPS, validates the exact archive shape/digest/version, builds with isolated Zig 0.15.2 caches, and runs exact version/compiler smokes; the Docker live gate retains its cross-build, no-network runtime, architecture/configuration/layer inspection, and cleanup evidence. Debug and ReleaseSafe pass 489/489; docs pass 126/126 and build; documentation integrity is 7/7 over 31 Markdown files, 60 fences/50 executable, 41 links, and 24 routes; production audits are 0/0/0; release/version/workflow/dependency/package/wrapper/capability/Ruby/JavaScript/diff checks pass; npm remains five files/43,856 unpacked bytes. | `6ac89dc`, `d31ce95`, `1d3a47a` |
| `REL-004` | `VERIFIED` | The red inventory found 19 mutable major-tag action invocations across three workflows, no explicit default token denial in the build workflow, and release-wide `contents: write` plus OIDC authority inherited by every release job. Resolved eight unique actions from tags in their official repositories and pinned every invocation to the reviewed full commit SHA with an exact human-readable release comment. All workflows now declare `permissions: {}` at top level. Five ordinary jobs receive only `contents: read`; the dedicated Pages deploy job alone receives `pages: write` and `id-token: write`; the release-creation job alone receives `contents: write`. A dependency-free closed validator owns exactly three workflows, seven jobs, their permission maps, action order and placement, eight approved pins, and 19 invocations. It rejects mutable/malformed/stale/unknown actions, new workflows/jobs, permission growth, alternate YAML anchors/aliases/merges, and a missing or reordered self-gate. Five regressions pass, and CI runs them plus the exact check after pinned Node setup but before dependency installation. Official action documentation confirms the Pages and release grants; same-run artifact transfer needs no added repository permission. Debug and ReleaseSafe remain 489/489 across 34/34; docs pass 125/125 and build; VS Code passes 13/13 and its exact five-file/120,473-byte VSIX check; both real Neovim versions pass. All five native targets inspect correctly as ELF, Mach-O, or PE after both ReleaseSafe and ReleaseFast builds. Production audits remain 0/0/0 across root/docs/extension. Documentation, capability, prefix, package, wrapper, format, compatibility, transform, benchmark, YAML/JSON/JavaScript, generated-state, and diff gates pass. Root npm is five files/26,707 unpacked bytes. Fresh package `zigcss-0.3.0-Bz3pcSAyGwADM0J9-GHXsywUvbTT7o9F_73bo1zFatyI` remains 75 files/1,937,408 bytes, builds 3/3, and its external consumer passes 6/6 steps with 2/2 tests in both modes. Formatting debt remains exactly 12 inherited files. | `5f8c852` |
| `REL-005` | `VERIFIED` | Replaced header-only release evidence with a closed native execution gate. Both ordinary-build and tag matrices use five architecture-matched runner labels; each builds ReleaseFast, creates the exact release archive/manifest/SBOM, extracts and executes the archive for synchronized version and minified CSS, packs the exact five-file npm package, runs its real postinstall offline against only the two expected local release URLs, proves the installed binary is byte-identical, and executes the packaged wrapper. Unexpected HTTPS, host/target mismatch, package inventory drift, archive shape, checksum, byte identity, output, shim, or cleanup residue fails before upload or attestation. Four focused policy/preload regressions pass, and the existing 16 consumer, five release-metadata, five workflow-policy, six version, seven capability, four dependency-policy, and documentation gates remain green. A real aarch64-macOS ReleaseFast archive passed the complete direct and npm lifecycle locally; the other four targets are mechanically required on matching CI runners. Debug and ReleaseSafe each pass 489/489 across 34/34, docs pass 126/126 and build, production audits remain 0/0/0, documentation integrity covers 31 files/60 fences/41 links/24 routes, workflow policy owns 22 pinned actions, capability metadata owns 20 rows/18 gates, and the npm dry run remains five files/44,011 unpacked bytes. No tag, artifact, release, package, image, formula, or other external system was changed. | `db4ab56` |
| `REL-006` | `IMPLEMENTED` | Red review proved the tag workflow would run `npm version 0.4.0-rc.1` against an already synchronized package (which npm rejects as unchanged), would use unqualified `npm publish` (which can move stable `latest` to an experimental prerelease), and would not test npm authority until after GitHub Release creation. Added a read-only `npm-preflight` job that rejects tag/version drift, invalid credentials, malformed or duplicate registry inventories, stable versions, and any already-published immutable target before native release work. Publication now performs no source mutation, uses exact `npm publish --tag next --provenance`, grants OIDC only to the publish job, and reads back both the immutable version and `dist-tags.next`; the documented consumer contract keeps `latest` stable. The closed validators now own 28 synchronized release surfaces, four workflows, nine least-privilege jobs, 28 reviewed immutable actions, exactly two bounded token uses, and mandatory preflight/build/create/publish ordering. Forty-one focused release/version/workflow/consumer regressions, all exact checks, pinned actionlint 1.7.12, YAML parsing, seven documentation-integrity tests, 126/126 site tests and build, two wrapper tests, a five-file/15,464-byte npm dry run, and zero production audit findings pass. Live npm evidence shows three existing immutable versions, no `0.4.0-rc.1`, and `latest=0.3.0`; the current-head Linux/ARM64 archive-to-non-root-image smoke and checksum-bound Homebrew Zig 0.15.2 build/compiler smoke pass. Direct host `zig build test` remains unavailable because the macOS 26.5 Command Line Tools linker cannot link Zig 0.15.2's build runner; production Zig is unchanged, the repository-owned 15.4-SDK release wrappers pass, prior integrated Build run `29359138902` is green, and the new hosted Build run is required before tagging. No tag, GitHub release, or npm version was created by this implementation checkpoint. | `a0115c5` |
| `REL-006-WIN` | `VERIFIED` | Release run `29435369415` attempt 1 failed closed on the expired npm credential. After the operator replaced only `NPM_TOKEN`, attempt 2 passed npm identity/version availability, then four native jobs completed their archive, metadata, native/npm smoke, dual attestation, cryptographic verification, and artifact upload paths. Windows alone failed before build because Git checkout converted `VERSION` and other text surfaces to CRLF while the platform-neutral validator expected LF. A red full-inventory fixture reproduced the failure; source ingestion now canonicalizes CRLF for every synchronized text surface and still rejects any bare carriage return. Forty-two focused npm/version/workflow/metadata/smoke/consumer tests, all exact checks, syntax, and diff hygiene pass. Exact-main Build run `29436541028` completed the full Test Suite plus all five native archive/npm-smoke jobs, including Windows. The failed release run skipped both outward publication jobs; live readback confirms no GitHub prerelease, no npm `0.4.0-rc.1`, and unchanged `latest=0.3.0`. The public tag remains immutably observable at old checkpoint `0348ddc`, so a new release-identity decision is required before another closed tag run. | `9fde324` |

## Milestone 7 exit validation

Milestone 7 implementation criteria remain `PASS`; `REL-006` is implemented at `a0115c5` and awaits its authorized hosted publication evidence. The public compile service remains disabled under the verified `SEC-002` containment decision, so service re-enablement packages `DOC-003` and `WEB-001` remain deferred until the full request/process isolation dependency is implemented; no disabled route is treated as a release capability.

| Gate | Result |
|---|---|
| Documentation truthfulness and integrity | `PASS`; generated capability metadata drives both Markdown tables and the site, 126/126 docs tests and the production build pass, and seven integrity tests cover 31 Markdown files, 60 fences/50 executable, 41 links, and 24 routes. |
| Dependency security | `PASS`; exact lock/manifest policy and live production audits report 0/0/0 vulnerabilities across root, docs, and VS Code, including zero high or critical findings. |
| Release identity and trust metadata | `PASS`; version/tag checks, five target-specific archives, SHA-256 manifests, deterministic SPDX 2.3 SBOMs, local Sigstore binding, and mandatory GitHub cryptographic verification remain fail-closed before release creation. |
| Consumer and installation paths | `PASS`; npm, Docker, and Homebrew trust boundaries retain 16 hermetic regressions and their live host evidence. Every tag target now additionally requires direct archive execution and a real offline npm pack/postinstall/wrapper lifecycle on a matching runner. |
| Workflow authority | `PASS`; four workflows, nine least-privilege jobs, and 28 immutable action references pass the closed policy. Release creation alone owns repository-content write; npm publication alone owns package provenance OIDC; no model-pass publication path exists. |
| Compiler regression surface | `PASS`; Debug and ReleaseSafe each complete 34/34 steps and 489/489 tests. Production Zig code is unchanged by `REL-005`. |
| Publication boundary | `PASS`; npm authority and immutable-version availability must pass before native work, and all five release jobs must finish metadata, native smoke, attestation, local binding, and cryptographic verification before GitHub release creation and exact `next`-channel provenance publication become reachable. A source checkout does not claim that hosted publication completed; registry and release evidence must be recorded after the tag run. |

## Milestone 8 package ledger

| Package | State | Evidence / decision | Commit |
|---|---|---|---|
| `BENCH-001` | `VERIFIED` | Added one dependency-free generated `v1` inventory containing exact 95-byte, 12,383-byte, and 198,366-byte flat CSS corpora. The manifest binds every path to its SHA-256 and mechanically derived qualified-rule, at-rule, declaration, and maximum-depth counts: 210,844 bytes, 2,177 qualified rules, and 10,885 declarations in total. Six regressions prove byte-for-byte generation, exact recorded complexity, recovery-disabled parsing by pinned Lightning CSS 1.30.1, closed inventory/content/manifest checks, symlink rejection, active Zig embedding, and command output. The benchmark smoke consumes only those three embedded files and reports actual byte counts; CI checks generation before running it. Debug and ReleaseSafe each pass 489/489 across 34/34 and both builds pass 3/3; the benchmark smoke, 21-feature compatibility gate, ten transform differentials, workflow/dependency/package policy, 126/126 docs tests/build, documentation integrity, YAML/JSON/JavaScript/diff checks, and a five-file/44,275-byte npm dry run pass. Formatting the package-owned benchmark source reduces inherited repository formatting debt from 12 to 11 files. Competitor installation, output acceptance, execution-mode separation, statistics, scheduling, and publication remain owned by `BENCH-002` through `BENCH-007`; no timing is accepted or publishable yet. | `9035398` |
| `BENCH-002` | `VERIFIED` | Replaced the historical networked comparison script with one closed two-tool manifest: exact `esbuild` 0.28.1/MIT and `lightningcss-cli` 1.30.1/MPL-2.0 development pins. The version-3 npm lock binds each direct archive and all 36 optional native packages to HTTPS registry URLs plus SHA-512 integrity. A clean `npm ci --ignore-scripts` preinstalls the host packages without lifecycle authority; validation resolves exactly one owned platform package per competitor, requires a repository-confined regular executable, invokes its absolute native path with no shell or ambient `PATH`, and checks exact runtime output. The upstream Lightning CSS binary's `lightningcss 1.0.0-alpha.66` output is recorded explicitly rather than reinterpreted as the npm package version. Five focused regressions and the exact check enforce manifest, package, lock, range, version, platform, license, archive, executable, workflow-order, and legacy-runner boundaries; the old `npx`/global-install/random-input runner and both unverifiable timing JSON files are removed. CI verifies the toolchain after the corpus gate and before the Zig smoke. Debug and ReleaseSafe each pass 489/489 across 34/34, both builds pass 3/3, and the exact corpus/toolchain, benchmark smoke, compatibility, transform, format, workflow, dependency, Zig-package, capability, version, documentation-integrity, syntax, production-audit, formatting, and diff gates pass. Documentation remains 126/126 plus the production build; npm remains five files/44,509 unpacked bytes. Output acceptance, comparable invocation modes, samples/statistics, controlled scheduling, and public reports remain `BENCH-003` through `BENCH-007`; no timing sample is accepted or published. | `e025d2b` |
| `BENCH-003` | `VERIFIED` | Added a closed three-tool/three-corpus output-acceptance manifest and validator. ZigCSS, esbuild 0.28.1, and Lightning CSS CLI 1.30.1 run through exact bounded file-output arguments, absolute repository-confined executables, isolated environment variables, exact stdout/stderr contracts, regular-file checks, cleanup, and output-size/time limits. Pinned Lightning CSS 1.30.1 reparses every emitted file with recovery disabled and requires its minified semantic canonical form to equal the exact corpus input; nine real outputs pass. Six focused regressions prove equivalent normalization, malformed/empty/truncated/changed/extra-rule rejection before sample insertion, positive-duration admission, manifest/symlink drift failure, workflow ordering, and removal of every active legacy timing path. The Zig smoke now uses `zigcss.compile`, independently reparses input/output through the rebuilt typed pipeline, requires structural equivalence, reports exact input/output bytes, and emits no timings; the legacy parser/optimizer/codegen benchmark and `benchmarkCompilation` result API are removed. Debug and ReleaseSafe each pass 491/491 across 36/36, both builds pass 3/3, and the validated smoke completes 3/3. Corpus/toolchain, compatibility, ten transform differentials, eight-format, workflow/dependency/Zig-package/capability/version/prefix/wrapper, production-audit, documentation, JSON/JavaScript, formatting, and diff gates pass; docs remain 126/126 plus the production build, npm remains five files/44.7 kB unpacked, and inherited formatting debt falls from 11 to 10 files. No timing sample or result was accepted, archived, or published. | `b78bcbb` |
| `BENCH-004` | `VERIFIED` | Added one closed five-mode manifest and execution gate. Cold CLI means one new process with startup included and no mode-local compile warmup; warm CLI means the same new-process command after one separately validated mode-local invocation. Both modes use identical three-tool/three-corpus contracts and admit 9/9 samples only after recovery-free semantic validation. Direct in-process API latency, allocator-requested memory, and two-pass corpus throughput are ZigCSS-only metrics, preventing esbuild's serviced JavaScript API or other unlike boundaries from being mislabeled as in-process comparisons. The Zig harness warms and times only `zigcss.compile`, retains every throughput result until the measured region closes, validates every timed output structurally before admission, and checks allocator-memory ownership invariants without admitting a duration. Five regressions prove exact mode/tool/metric/lifecycle closure, cache preparation, manifest/symlink failure, all-output throughput admission, CI ordering, and no raw timing/memory printing. Debug and ReleaseSafe each pass 491/491 across 36/36; both builds pass 3/3 and both mode smokes pass 3 API, 3 memory, and 1 throughput observations. Corpus/toolchain/output/mode, compatibility, transform, format, workflow, dependency, Zig-package, wrapper, prefix, capability, version, documentation, audit, formatting, and diff gates pass; docs remain 126/126 plus build, npm remains five files/44.8 kB, and formatting debt remains exactly 10 inherited files. No raw sample, statistic, comparison, archive, or public claim was retained or published. | `6d6dcdb` |
| `BENCH-005` | `VERIFIED` | Added one closed 20-observation reporting contract and a fail-closed report generator for all 43 mode/tool/corpus/field series. Cold and warm CLI retain 180 validated durations each; ZigCSS retains 60 direct-API durations, 420 allocator-field observations, and 20 two-pass throughput values, for 860 unsigned-decimal raw samples total. Every timed CSS result is validated before its value enters a series. Reports recompute exact sorted-middle median, nearest-rank p95, and population variance from retained samples, reject missing/duplicate/reordered/tampered series or statistics, and bind results to corpus/executable SHA-256 identities plus platform, release, architecture, CPU, logical CPUs, memory, Node, Zig, ReleaseFast, and monotonic-clock metadata. The private Zig runner serializes only after measured regions close; the public script writes only a new explicit report path, while CI creates, re-reads, and removes a temporary report without archival. Six focused statistics regressions and five mode regressions pass; the real gate validates 43 x 20 observations. Debug and ReleaseSafe each pass 491/491 across 36/36, both builds and the separated-mode smoke pass, and all corpus/tool/output/statistics, compatibility, transform, format, workflow, dependency, package, wrapper, prefix, capability, version, release-metadata, documentation, audit, formatting, and diff gates pass. Docs remain 126/126 plus build; npm remains five files/45.0 kB. Fresh package `zigcss-0.4.0-rc.1-Bz3pcbYmGwC7HT14gqtTwce3yhfJvSY8mdeFzhJARxnC` contains 75 allowlisted files/1,779,382 source bytes and builds 3/3 in Debug and ReleaseSafe. Formatting debt remains exactly 10 inherited files. No report was archived or published. | `454f0a1` |
| `BENCH-006` | `VERIFIED` | Added one schedule-only Monday 04:17 UTC workflow confined to the exact upstream repository/default branch and a dedicated `[self-hosted, linux, x64, zigcss-benchmark-v1]` runner label. One non-cancelling concurrency group, a 45-minute bound, read-only contents, disabled checkout credentials, exact Zig/Node/tool dependencies, and four immutable actions close the execution surface. The job revalidates every BENCH-001 through BENCH-005 contract, builds ReleaseFast executables, collects the complete 43-series/860-sample report, then uploads only `benchmark-report.json` plus `benchmark-archive.json` for 90 days and unconditionally removes both from the persistent runner. The create-only archive manifest binds the repository, main ref, full commit, scheduled event/cron, run ID/attempt, exact artifact name, report digest/size/time, and copied CPU/core/memory/platform/architecture fields under a SHA-256 hardware fingerprint. Non-Linux/x64 reports, altered provenance/report/statistics, extra entries, symlinks, duplicate output, trigger/runner/retention/order/cleanup drift, and workflow permission/action growth fail closed. Five archive and five workflow regressions plus exact checks pass; the complete benchmark dependency chain, Debug/ReleaseSafe 491/491 across 36/36, both builds, mode smoke, independent CSS/transform/format/prefix/package/version/release-metadata/dependency/documentation gates, 126/126 docs tests/build, three zero-vulnerability production audits, syntax/diff checks, and a five-file/45.2 kB npm dry run pass. The first integrated push exposed GitHub's workflow-parse rejection because `runner.temp` is unavailable at job-level `env`; the archive path now resolves only in the four runner-assigned steps, and the policy pins that exact count. Five archive regressions/check and pinned actionlint 1.7.12 pass. No benchmark job or artifact was dispatched; public performance claims remain withdrawn for `BENCH-007`. | `2e4b56e`, `c22c1db` |
| `BENCH-007` | `IMPLEMENTED` | The red baseline had only a hand-written withdrawal notice and no consumer from the exact BENCH-006 archive to public Markdown. Added a closed two-state publication contract. The current `withdrawn` state requires byte-identical no-claims text and null archive authority. A future `published` state must commit exactly one repository-confined `benchmark-report.json`/`benchmark-archive.json` pair under its verified artifact name, bind an exact GitHub run/artifact URL, revalidate all 43 ordered series and 860 raw observations plus statistics, report digest/size, source/run identity, controlled Linux x64 hardware fingerprint, and canonical inventory, then reproduce `BENCHMARK_REPORT.md` byte-for-byte. Rendering keeps cold/warm CLI modes separate, labels API/memory/throughput as ZigCSS-only, escapes bounded runner text, links permanent committed raw data, states corpus/host limits, emits no ranking or ratios, and writes only to a new explicit output. Seven regressions cover exact withdrawal, deterministic rendering, archive/report and artifact-link tampering, Markdown/HTML safety, create-only output, committed-path/generated-byte confinement, and build/scheduled workflow order. The real ReleaseFast gate regenerated, revalidated, and removed all 860 temporary samples. Debug and ReleaseSafe remain 491/491 across 36/36; both builds and the mode smoke pass. The complete benchmark chain, compatibility, ten transform, eight-format, prefix, package/consumer/example, wrapper, version, release-metadata, dependency, capability, workflow, documentation, syntax, and diff gates pass. Docs remain 126/126 plus build and 31-file/60-fence/42-link/24-route integrity; production audits are 0/0/0; capability metadata is 21 rows/19 gates; npm is five files/45.8 kB; formatting debt remains ten inherited Zig files. No real scheduled artifact exists or was dispatched, so no timing, memory, throughput, ranking, ratio, archive, or public result was selected; final verification requires that external controlled input. | `509e2ef` |

## Milestone 8 activation boundary

Milestone 8 remains `IN_PROGRESS` at implementation checkpoint `509e2ef`. `BENCH-001` through `BENCH-006` are verified, and the complete `BENCH-007` publication implementation is green. The repository deliberately remains in the exact withdrawn state because a synthetic or manually invented archive cannot satisfy the roadmap's controlled scheduled evidence requirement. The first eligible archive must be produced by the schedule-only workflow on integrated upstream `main`, retained and reviewed, then committed and selected under the new fail-closed publication contract before `BENCH-007`, Milestone 8, or the autonomous program can become `VERIFIED`/`PASS`/complete.

## Final quality closure ledger

| Package | State | Evidence / decision | Commit |
|---|---|---|---|
| `QUAL-001` | `VERIFIED` | Applied only the pinned Zig 0.15.2 formatter to the exact ten inherited files still failing the repository-wide gate, isolated from every semantic package. `zig fmt --check build.zig build_helpers.zig src examples` and `git diff --check` now pass with zero formatting debt. Debug and ReleaseSafe each pass 36/36 steps and 491/491 tests. All five Linux/macOS/Windows targets inspect as the expected ELF, Mach-O, or PE architecture after both ReleaseSafe and ReleaseFast builds. The complete benchmark chain passes 40 focused regressions plus every exact check, including regeneration and validation of all 43 x 20 temporary observations while retaining the withdrawn publication state. Compatibility, ten transform differentials, eight-format, prefix, package, wrapper, release smoke/consumer/container/Homebrew, version, release metadata, workflow, dependency, capability, documentation, VS Code, both supported Neovim versions, syntax, production-audit, and generated-state gates pass. Documentation is 126/126 plus the production build and its seven-test integrity gate. A fresh package `zigcss-0.4.0-rc.1-Bz3pcT4iGwADMiFbRjkSB1R98GCMA9kpdd-K0IvHEgMp` contains the exact 75 allowlisted files/1,778,238 source bytes and builds 3/3 in Debug and ReleaseSafe; the committed external consumer passes 6/6 with 2/2 tests and the build-integration example passes 7/7 with 1/1 test in both modes. The npm dry run remains exactly five files/45,808 unpacked bytes. The sole remaining roadmap gate is the external controlled `BENCH-007` archive described above. | `3eced86` |

## Completed work packages

- `SEC-001`: static serving is contained within the configured real root and malformed URL encoding is handled without a process crash. Focused result: 7/7 security tests; integration result: 44/44 docs tests plus successful Vite build.
- `SEC-002` containment decision: the public compile service is disabled rather than exposed without its required resource and isolation limits. Re-enablement requires the full package gates.
- `TEST-001`: the audit corpus runs the actual CLI as a child process, safely captures crash signatures, and maps every quarantine to the package that must replace it with a target-contract assertion.
- `OPT-001`: legacy code generation no longer invokes the inherited optimizer and still rejects every transform-bearing field with `UnsafeTransformsDisabled`. The rebuilt CLI now owns its separate acceptance-gated preset; minification remains an independent emission mode.
- `PROF-001`: explicit and deferred `end()` calls are safe; profiler cleanup no longer double-frees timing names.
- `CLI-001`: every planned output is checked against every input and prior output before directory creation or compilation; rejected plans preserve all sources.
- `CLI-002`: the CLI accepts only functional recovery-scope options. Source maps, target/prefix queries, and extraction modes still fail before reading input; Milestone 3 separately admits only the closed verified `--optimize` preset.
- `CI-001`: documentation validation and deployment are separate jobs; pull requests cannot execute the deploy job and the uploaded artifact matches Vite's verified output directory.
- `CI-002`: target builds happen after native tests so tests cannot replace cross artifacts; a shared tested inspector validates architecture and executable format before upload or release archive creation.
- `SEC-003`: the public docs runtime is static-only, non-root, high-port, and root-owned; a filtered build context excludes repository metadata, generated output, dependency trees, and environment files.
- `SAFE-001`: the recovery CLI emits an experimental warning and rejects legacy format adapters; active public documentation is limited to the tested status, source-build, and CLI contracts, while prior performance comparisons are explicitly invalidated.
- `TOK-002`: semantic token values decode CSS escapes on demand while raw original spelling remains source-addressable; URL and malformed-token recovery always terminates, and unicode-range recognition is enabled only for its descriptor-specific entry point.
- `TOK-003`: lossless consumers receive comment and whitespace tokens instead of implicit gaps; normalized consumers can discard both through `Token.isTrivia()`, while unterminated-comment metadata remains available for later structured diagnostics.
- `MEM-001`: compilation-lifetime objects allocate from one arena, while result-lifetime CSS, maps, and diagnostic messages are deep-copied into a recorded result allocator and released only through `CompileResult.deinit`.
- `SYN-001`: component values retain every top-level source byte while representing nested blocks and functions explicitly; missing closers remain `null` metadata for recovery/diagnostics instead of being synthesized into the source model.
- `DIAG-001`: CSS parse problems are structured values on `Compilation.diagnostics`; only allocator exhaustion, unknown sources, and explicit resource limits leave the parse API as operational errors.
- `AST-001`: the new standards-oriented selector AST represents adjacency inside a compound separately from explicit combinators between compounds; typed pseudo arguments never replace their lossless component-value backing.
- `AST-002`: declaration values retain the complete raw component stream (including `!important` and trivia); `ImportantAnnotation.value_end` identifies the semantic prefix without deleting spelling or collapsing duplicate declarations.
- `AST-003`: at-rule block interpretation is a tagged choice rather than a universal rule/declaration assumption; every category retains the original prelude and exact brace/EOF envelope.
- `PAR-001`: selector lowering follows the current CSSWG Selectors Level 4 Editor's Draft grammar; style-rule lists are strict, only `:is()`/`:where()` are forgiving, and their discarded items do not create compilation errors.
- `PAR-002`: declaration parsing never searches raw delimiters inside nested syntax; only top-level semicolon component values synchronize candidates, and malformed candidates remain source-preserved gaps in the ordered declaration-list span.
- `PAR-003`: rule parsing recognizes only top-level semicolon and curly-block component boundaries; qualified rules compose the typed selector/declaration parsers, while each at-rule stores a decoded name, exact raw prelude, and explicit body category.
- `PAR-004`: structured at-rule details are additive views over the generic lossless at-rule. Typed lowering never deletes raw preludes/bodies; invalid typed syntax leaves `details` null or skips only the invalid keyframe/page child while retaining source-addressable raw syntax and diagnostics.
- `ERR-001`: lowering synchronizes only at top-level component-value boundaries, so nested delimiters cannot split an outer rule. Precise nested diagnostics suppress generic parent fallbacks, while matching syntax containers consume their own closers and any visible stray closer is isolated before the next rule.
- `EMIT-001`: pretty emission is structural where the grammar is typed and lossless where values/unknown at-rules remain component streams. It canonicalizes identifiers and strings, never invokes transforms, preserves declaration/rule order, and returns `UnrepresentableRecovery` or `UnterminatedSyntax` whenever formatting would otherwise discard or synthesize meaningful source.
- `EMIT-002`: minification is aggressive only across typed structural grammar. Raw component streams conservatively collapse existing whitespace runs to one token instead of guessing that whitespace is semantically removable; this protects custom syntax, descendant selectors in raw pseudo arguments, list separation, and whitespace-sensitive operators while still removing all structural formatting.
- `ROUND-001`: structural equivalence compares semantic tags, decoded values, ordered children, and normalized component streams rather than source spans or output text. Untyped raw syntax retains leading/trailing whitespace-token presence recursively; typed grammar may normalize representational edge trivia without changing equivalence.
- `MAP-001`: source maps are emitted as separate owned artifacts; CSS bytes never receive an implicit source-map comment. Mappings are captured at write time from generated UTF-16 positions to original span starts, use source index zero for the compilation source, and refine duplicate generated starts to the most specific later node.
- `NEST-001`: style-rule block contents follow declaration-first disambiguation without flattening cascade order. The initial declaration run remains on the style rule; every later run is an ordered nested-declarations child. Nested group rules use the same block-content algorithm, and emitter modes preserve that sequence while omitting only block-final declaration semicolons.
- `M2-CLI`: stable CSS compilation enters through `css/pipeline.zig`; it never constructs the inherited AST, calls the inherited parser, or invokes inherited code generation. Stable CLI source-map flags remain rejected until a file/comment output contract is specified even though the library pipeline can return a separately owned map.
- `M2-COMPAT`: the public Markdown matrix and machine-readable fixture manifest are cross-checked, while the independent parser consumes actual stable-CLI output rather than fixture input. Validator recovery is forbidden so acceptance cannot come from silently discarding malformed output.
- `PASS-001`: plans borrow pass definitions but own deterministic ordered pointer slices. Output-changing passes reconstruct const AST paths in the compilation arena; `ParsedStylesheet.applyPassPlan` installs a root only after the full plan succeeds, while failed candidates and diagnostic side effects remain unobservable.
- `OPT-010`: cleanup eligibility is intentionally closed: empty style rules and empty `media`, `supports`, `container`, or `starting-style` groups only. Empty layer/scope/keyframe/document/descriptor/page/unknown rules are preserved, as are comments that separate value tokens and all ordered declarations.
- `OPT-011`: duplicate declarations are exposed as owned, bounded analysis facts only. Standard names are ASCII case-insensitive, custom-property names are case-sensitive, list segments are hard scope boundaries, and importance transitions/fallback chains do not imply deletion safety.
- `VAL-001`: numeric interpretation is an immutable typed view over lossless component values. It retains source envelopes and context-relative units, reports invalid dimension algebra without rewriting syntax, and by itself grants no folding or shortening authority.
- `MATH-001`: numeric folding is an explicitly authorized semantic rewrite over a closed property/category policy. It retains one exact unit, declines context-dependent conversion and incomplete evidence, and emits only structured finite numerics when strictly shorter; the stable CLI reaches it only through the closed accepted preset.
- `OPT-012`: color typing is limited to exact 8-bit sRGB evidence, and unitless zero is limited to positive zero lengths on grammars without a competing number interpretation. Shortening never raises alpha-hex compatibility requirements without explicit target data.
- `OPT-013`: shorthand synthesis is limited to one exact safe token repeated across the canonical adjacent physical margin reset set. Authored declarations remain as proof; page and page-margin descriptor contexts stay unchanged because the current independent oracle cannot prove their shorthand equivalence.
- `OPT-014`: selector-rule merging is the reverse of expanding one selector list into adjacent identical declaration blocks. It never crosses a rule-list, at-rule, omission, recovery, or nesting boundary; blocks containing child rules are excluded so merging cannot change the parent selector list used by a descendant `&`. Representation-only declaration differences may merge only after typed component comparison, while distinct comments and custom-property token boundaries remain authored.
- `OPT-015`: typed group merging retains one identical conditional or named-layer context and concatenates only its two adjacent child streams. It never crosses a parent rule-list, omission, recovery, anonymous-layer identity, different query, or nesting boundary. Direct nested-declaration streams remain authored because Lightning CSS 1.30.1 cannot currently provide a sound independent canonical oracle for that shape; nested style and generated-proof streams remain eligible and ordered.
- `CUSTOM-001`: custom-property definitions and `var()` consumers are authored protected values on every default rebuilt value/declaration traversal. General static evaluation is not a source-only optimization because names are case-sensitive and substitution depends on element cascade, inheritance, fallbacks, registrations, and computed-value cycle handling. Any future mode must be a separate experimental value pass with three explicit policy grants and new DOM/cascade evidence; it cannot hitchhike on a verified pass or the stable CLI.
- `LOGICAL-001`: no logical-to-physical conversion mode is retained. Physical mapping requires used writing-mode, direction, text-orientation, the controlling box, inheritance, and the ordered cascade between logical and physical declarations; a stylesheet-global target is not sufficient. The reserved metadata effect is rejected at registry validation, so a future proposal requires a new package, context model, computed-style oracle, and ADR revision.
- `PREFIX-001`: target queries are a small deterministic ZigCSS language, not a partial Browserslist implementation. A fixed BCD release supplies reviewed facts rather than ambient policy; partial support and upstream-note presence remain conservative result qualifiers. No compatibility rewrite or stable CLI flag is enabled until `PREFIX-002` proves target-dependent syntax, ordering, idempotence, source maps, and independent semantic validation.
- `PREFIX-002`: compatibility generation is a closed, target-dependent library pass rather than the inherited target-invariant autoprefixer. Vendor declarations precede and cannot replace the authored standard, selector variants are separate rules because ordinary selector lists are unforgiving, prefixed keyframes are complete clones, and every partial, annotated, unsupported, manual, mixed, functional, descriptor, recovered, or conflicting boundary defaults to authored output. Stable target flags remain unavailable until later public option work can carry the strict query; this compatibility pass cannot hitchhike into `--optimize`.
- `TREE-001`: dead-code and critical-CSS extraction are explicit experimental subset operations over caller-declared closed selector domains, not source-only optimization guesses. Only complete class/ID inventories and direct positive requirements grant absence authority; every functional, document-language-dependent, dependency-bearing, dynamic, incomplete, or generated-overlap boundary remains authored. The recovery CLI continues to reject extraction flags.
- Milestone 3 exit: `--optimize` now reaches only the closed verified preset and never the inherited optimizer. Bounded fixed-point rounds make the composed CLI result byte-stable even when one proof-backed transformation exposes another candidate after reparse; separately authorized prefix and extraction behavior remains outside the preset.
- `API-001`: the public foundation is exercised from a separate consumer module rather than only by tests rooted inside `src/lib.zig`. The external name is `zigcss`; final high-level compile options/results and package consumption remain dependency-ordered later packages rather than accidental claims of this scaffold.
- `API-002`: the public compile facade has one explicit ownership boundary. Callers borrow input/options only for the call and receive independently owned CSS, maps, diagnostics, dependency facts, and future module-export storage; incoherent options and bounded dependency failures are structured result diagnostics, while operational failures remain errors.
- `API-003`: native plugins are an explicitly experimental trusted-code adapter over `PASS-001`, not a second mutable transform engine. Plugin registration order is irrelevant, policy and namespace boundaries are mechanical, compiler state is transactional, and callback-owned external side effects remain outside rollback.
- `BUILD-001`: the Zig package now has a stable identity and minimum compiler declaration backed by real path and fetched-cache consumers. Its initial allowlist exports only the build entry point, manifest, compiler sources, README, and license; remote publication and build helpers remain unavailable until their own gates pass.
- `BUILD-002`: the dependency build module exports a narrow supported helper rather than the inherited mutable custom step. Each invocation is a cacheable one-input/one-output graph node, downstream consumers receive the generated `LazyPath`, and unavailable CLI features cannot be smuggled through helper options.
- `BUILD-003`: every authored Zig example now has an executable build gate. The complete path-dependency project separates application and host-tool targets, consumes the public API and supported build helper, exact-checks and installs generated CSS, and passes Debug/ReleaseSafe CI.
- `CLI-010`: all supported CLI CSS modes now share the exact owned public compile contract used by Zig consumers. The executable owns only command, path, I/O, diagnostic-rendering, and write orchestration; direct parser/optimizer/emitter implementations are absent from its runtime boundary.
- `CLI-011`: the public command surface now has synchronized version/syntax metadata, bounded stdin and explicit stdout, non-repeatable options, mechanically disjoint stream/batch/watch modes, and documented 0/1/2 statuses. The npm launcher preserves the same streams, statuses, and POSIX signal semantics.
- `CLI-012`: successful file output now commits by same-directory atomic replacement with late parent creation; deterministic bounded batch names disambiguate equal stems without depending on argument order, while canonical/inode collision checks remain authoritative.
- `WATCH-001`: watch mode now compiles the root snapshot it already hashed, observes direct local relative imports from owned public dependency evidence, retains dependency coverage across failed attempts, and waits after unchanged failures instead of recompiling twice per second.
- `PARALLEL-001`: batch compilation now uses at most eight dynamically queued workers, one stable allocator lifetime per task, cancellation before further claims, unconditional joins, leak-checked unwind, and argument-ordered diagnostics and atomic commits.
- `MODULE-001`: the Zig library now exposes one closed CSS Modules slice with local-by-default typed class scoping, versioned source-specific names, owned first-seen exports, bounded metadata, strict no-partial-output failures, generated-CSS reparsing, and an independent naming/Lightning CSS oracle; CLI and LSP exposure remain contained.

- `VSCODE-001`: the editor artifact is a reproducible client package, not an implicit binary distribution channel. Its empty-path lookup cannot execute from the workspace/current directory, explicit invalid configuration fails rather than silently changing authority, runtime checks mirror manifest limits, and the temporary packaging gate admits an exact reviewed file inventory before cleanup. Publication and native-binary bundling remain unauthorized.

- `NEOVIM-001`: the plugin-framework prototype is replaced by a CSS-only built-in configuration whose minimum/current Neovim clients launch the real absolute command, prove exact advertised/absent methods and compiler-backed requests, and cleanly stop without touching user editor state.

- `DOC-001`: one evidence-linked metadata source now drives the repository README, published status guide, and site feature table; validation and CI reject unsupported status claims, missing or escaping evidence, invented commands, malformed presentation markup, and generated-table drift.

- `DOC-002`: every tracked Markdown/rendered-site code example is classified and syntax-checked, compiled, executed, or explicitly hash-pinned as a non-executable design sketch; every internal Markdown and literal site route is resolved under canonical repository confinement with fragment and reference-label validation.

- `DEP-001`: all npm manifests/lockfiles and exact direct pins are inventory-owned, every production lock graph is audited in CI with high/critical findings fatal, and bounded weekly Dependabot version PRs cover npm, GitHub Actions, and Docker without merge, credential, publish, or deploy authority.

- `REL-001`: one roadmap-bound `VERSION` authority now closes every current release identity claim, package/lock/runtime/container/formula/editor surface, generated capability row, CI preflight, and exact release tag; Marketplace's three-part version is explicitly mapped to the core prerelease and cannot package without its prerelease marker.

- `REL-002`: every release target now has a closed five-file identity—archive, manifest, SPDX SBOM, signed SLSA provenance bundle, and signed SPDX bundle—whose names, bytes, predicates, permissions, action pins, verification order, and upload scope fail closed before release creation. Local structural bundle tests do not claim cryptographic proof; the tag workflow must obtain GitHub OIDC and pass `gh attestation verify` before its artifacts can move downstream.

- `REL-003`: npm, Docker, and Homebrew now consume immutable, checksum-bound inputs under closed target and inventory contracts. The npm installer and Docker image consume the signed-release asset names; the unpublished Homebrew source formula pins an existing commit archive, its verified digest, and Zig 0.15, with exact live compiler output and no mutable head or second-download path.

- `REL-004`: every workflow defaults to no token authority, each job receives its exact minimum grant, and every external action is a full reviewed commit pin guarded by a closed dependency-free policy before npm installation.

- `REL-005`: every release archive is executed on a matching architecture before attestation, and the same checksum-bound bytes must survive an offline real npm pack/postinstall/wrapper lifecycle without network fallback or residue before publication can become reachable.

- `BENCH-001`: benchmark inputs now come from one generated versioned inventory whose exact bytes, hashes, rule/declaration complexity, file types, and active Zig embedding are checked in CI; changing corpus identity is an explicit reviewed input change rather than runtime generation.
- `BENCH-002`: competitor identity now comes from one closed manifest plus exact npm package/lock records. Only the checksum-locked native optional package selected by `npm ci --ignore-scripts` may execute, and it is invoked by an absolute repository-confined path; the historical networked runner and its unverifiable timing archives are gone.
- `BENCH-003`: output acceptance is a mandatory fail-closed boundary, not a post-processing check. Every tool/corpus emission must be nonempty, bounded, recovery-free, warning-free at the oracle, and semantically canonical to the exact input before a positive timing duration may enter a sample collection. The current Zig smoke adds an independent typed-AST structural check and deliberately records no duration; execution-mode and measurement design remain `BENCH-004`.
- `BENCH-004`: cross-tool comparison is confined to identical new-process CLI boundaries, with startup included in both cold/no-local-warmup and warm/one-validated-local-warmup modes. ZigCSS direct API latency, allocator-requested memory, and in-process throughput are separate non-comparative metrics; every timed output is validated before an in-memory admission, and no raw value survives the smoke.
- `BENCH-005`: every report series now retains exactly 20 raw observations and derives median, nearest-rank p95, and population variance from those exact values. The complete report is valid only when all 43 ordered CLI/API/memory/throughput series, corpus and executable digests, and closed host/toolchain metadata agree; collection or validation failure produces no report. CI proves the contract with a temporary ReleaseFast report and deletes it, leaving controlled scheduling and archival to `BENCH-006` and public rendering to `BENCH-007`.
- `BENCH-006`: benchmark collection is schedule-only on one dedicated self-hosted Linux x64 label and one non-cancelling concurrency group. Each accepted archive contains exactly the complete validated report and a provenance manifest binding source/run identity, report bytes, and a stable hardware fingerprint; persistent-runner cleanup is unconditional. GitHub artifact retention is the maximum reviewed 90-day handoff window, not publication or a permanent performance claim. `BENCH-007` may render only a separately obtained archive that passes this verifier.
- `BENCH-007` implementation: public Markdown is now a deterministic view of one committed verified archive rather than editable timing prose. Until that exact controlled input exists, the selected state is a byte-exact withdrawal with no sample, statistic, comparison, ranking, or ratio. Synthetic fixture archives prove the renderer and rejection boundaries but cannot authorize a public result.

## Active blockers

- `BENCH-007` final verification requires the first retained archive from the schedule-only benchmark workflow on integrated upstream `main`. Direct non-force integration is green through code/ledger head `c34deaf`: Build run `29251213253` completed the full Test Suite and all five native compiler/archive/npm matrix jobs successfully. No actionable repository or integration fault remains ahead of controlled archive collection.
- The GitHub repository Actions runner API returned `total_count: 0`. Therefore no runner currently satisfies the required `[self-hosted, linux, x64, zigcss-benchmark-v1]` label set. Registering and keeping a dedicated non-emulated Linux x64 runner online requires operator-owned hardware and GitHub registration authority; substituting GitHub-hosted or emulated hardware would invalidate `BENCH-006` evidence and is not an acceptable workaround. The 2026-07-13 04:17 UTC schedule has passed; after green replacement CI, the next policy-conforming scheduled opportunity is 2026-07-20 04:17 UTC. No qualifying archive currently exists.

## Known regressions and risks

The authoritative regression list remains the Milestone 0 list in `DEVELOPMENT_PLAN.md`. Base-commit reproductions established:

- The previously recorded nested callable-producing module call inside an otherwise unknown CSS function is closed by `NSASS-011-PLAIN-CSS-FUNCTION-ARGUMENTS`: arguments now evaluate before serialization and non-CSS callable values fail closed. The private native path remains production-unreachable and publicly unclaimed; lazy conditional-function semantics remain an explicit future boundary.

| Area | Base-commit evidence |
|---|---|
| Static traversal | `GET /..%2fpackage.json` served `docs/package.json`; double traversal served repository `package.json`. |
| Malformed URL | `GET /%E0%A4%A` threw `URIError: URI malformed`, terminated the docs server, and returned an empty reply. |
| Compile-service limits | Code inspection found unbounded request accumulation, no timeout/output/concurrency/process limit, synchronous temp I/O, and incomplete error-path cleanup. |
| Compound selectors | `.a.b` and `.a .b` both emitted `.a .b`, proving semantic collapse. |
| Functional/attribute selectors and nested delimiters | A selector containing `:not(...)` and `[data-x=...]` failed at byte 1 instead of parsing. |
| Declaration-bearing at-rules | `@font-face` failed as an invalid identifier. |
| Percentage keyframes and at-rule spacing | The combined at-rule corpus could not parse; optimized media output elsewhere emitted invalid `@mediascreen`. |
| Important/fallback ordering | Three `color` declarations collapsed to only the last important value. |
| Rule/at-rule ordering | Optimization merged non-adjacent `.keep` rules and non-adjacent `@media` blocks, moved the media block, reversed nested rule order, and reported allocator leaks. |
| Custom-property cascade | Optimization replaced `var(--theme)` with a global/static value and shortened custom property values, ignoring dynamic cascade semantics. |
| Logical/reset behavior | Optimization converted `margin-inline-start` to `margin-left` despite RTL/vertical modes and synthesized a background shorthand. |
| Source-map/browser flags | `--source-map` and `--browsers chrome100` were accepted but output was unchanged and no source map was produced. |
| Unknown/missing CLI arguments | An unknown flag and a missing `-o` value both exited 0 and compiled normally. |
| Profiling lifecycle | `--profile` printed a report and then crashed with SIGABRT/segmentation fault (exit 134), confirming the double-end path. |
| Input overwrite | `input.css -o input.css --minify` exited 0 and replaced the input in place. |
| Batch collision | Two different `shared.css` inputs mapped to the same output, both reported success, and the second silently overwrote the first. |

## Decisions

- The approved plan is authoritative.
- Security and semantic containment precede feature work.
- Inherited work is preserved and will not be rewritten or discarded.
- `NATIVE-004` treats each canonical root as an already-open filesystem capability; a candidate URL names a path but grants no authority. Traversal is relative to that capability with no-follow opens, while canonical path rebinding before and after the read proves the returned URL still names the same regular-file identity. Every load request, including malformed or missing probes, consumes the attempt ceiling. Nonempty file-URL authorities and Windows network-share roots remain fail-closed; language-specific partial, extension, package, glob, and load-path search belongs to later native adapters and cannot widen this shared loader.
- `NATIVE-005` makes language evaluation a private transaction rather than an output buffer. Generated bytes become a result only after the complete stylesheet passes the existing core with no error/recovery diagnostic; every evaluator, cancellation, limit, allocation, or validation failure closes the resolver and exposes no CSS. Language-specific candidate search remains outside the transaction, while successful results deep-own the resolver facts needed by later watch and product integration. Phase-boundary cancellation is cooperative; the core remains the sole CSS grammar authority.
- `OPS-002` replaces unreliable unattended continuity with one serialized outer Bash loop, not concurrent agents. Exactly one continuation owner may touch this worktree at a time; the background runner uses fresh ephemeral context from the approved roadmap and durable ledger, while local ignored state owns only scheduling evidence. System-startup installation remains outside authority, so `start` lasts for the current login and must be reissued after logout/reboot. A dirty restart is admitted only when its exact fingerprint was recorded by an interrupted runner pass.
- `OPS-003` trusts result markers only from Codex's isolated final-message file, never from the mixed diagnostic transcript. On macOS the current app-bundled CLI precedes an optional user-local installation because the selected model/runtime compatibility contract is stricter than path preference; model failure remains an error and never authorizes fallback.
- `OPS-004` makes automatic pushing a supervisor privilege, not a model capability. Only a clean green `HEAD` may move the identically named current `vale/*` branch on the closed approved `origin`; ordinary push plus exact readback is required, force and alternate refspecs/remotes remain absent, and any failure stops locally.
- `OPS-005` uses a detached system `screen` session for per-login durability because it retains the caller's authorized access to linked-worktree Git metadata. A standalone launchd process does not inherit that macOS privacy authority and is therefore not a valid runner transport. No LaunchAgent, login item, or system-startup state is installed.
- `OPS-006` treats blocker identity as a protocol value rather than human prose. One lowercase kebab-case code names the underlying authority or external-state condition across passes; the reason remains readable and may change. Invalid identity pauses fail closed, the same code counts toward the existing three-pass bound, and a different code alone starts a new blocker count.
- `OPS-007` separates production branch authority from hermetic runner testing. Only explicit test mode may bypass the `vale/*` requirement; production controls never set that mode, so a normal `main` checkout cannot start the autonomous loop.
- GitHub expression contexts are resolved at their narrowest valid scope. `runner.temp` belongs only to steps after runner assignment, never job-level `env`, and the archive policy owns all four uses.
- The reviewed Zig 0.15.2 platform baseline uses explicit `macos-15` and `macos-15-intel` runners. Floating `macos-latest` is not an acceptable release identity because its SDK can change independently of the compiler baseline.
- CLI integration children use absolute, repository-confined temporary-directory paths through `Child.cwd`; Zig 0.15.2's directory-handle `cwd_dir` remains unavailable on Windows. Release-consumer CI fetches full history only where the immutable Homebrew source ancestry proof requires it.
- The compile API will be disabled after `SEC-001` unless `SEC-002` isolation is implemented and verified immediately.
- Legacy compiler failures are executable characterization quarantines, not accepted contracts. Each owning fix must replace its quarantine with the correct behavior assertion in the same commit.
- Legacy optimizer functions remain reachable only to explicitly labeled internal tests. They are not part of the stable CLI/code-generation path and must not be re-enabled without pass-specific acceptance evidence.
- Operator-requested post-Milestone-0 tooling: add a ZigCSS autonomous-loop protocol plus a read-only `orient.sh`, modeled on Alvo's protocol/orient split and adapted to this repository's no-push/no-deploy authority boundary.
- Alternate-format parsers remain available only as experimental internals for characterization; `.scss`, `.sass`, `.less`, `.styl`, `.postcss`, `.module.css`, and CSS-in-JS extensions are rejected by the recovery CLI before any output is written.
- The persistent Codex goal remains the scheduler. `scripts/autodevelop/orient.sh` is deliberately read-only and cannot launch a model, wake a task, or mutate repository/external state; `docs/operations/codex-loop-protocol.md` is the canonical resume procedure.
- Foundational implementation must conform to accepted `ADR-001`, `ADR-002`, `ADR-003`, `ADR-004`, and `ADR-010`; changing stable scope, source/syntax boundaries, ownership, transform authority, or autonomous runtime requires a superseding approved ADR.
- CSS strings use their distinct trailing-backslash-at-EOF rule when producing decoded values; identifiers, dimensions, hashes, at-keywords, and URLs retain the general escaped-code-point replacement behavior.
- The arena control block is separately allocated from its caller-provided backing allocator so allocators retained by movable child containers never point into a relocated `Compilation` value.
- Component-value recursion defaults to a 256-container nesting budget and returns `NestingLimitExceeded` before exceeding it; resource-limit diagnostics are added by `DIAG-001` rather than conflated with allocation failures.
- The allocation-free tokenizer records bounded recovery metadata on tokens; the compilation-backed syntax layer converts it to owned diagnostics so no allocation error can be swallowed inside `Tokenizer.next`.
- The new `src/css/ast.zig` model coexists with the inherited `src/ast.zig` prototype until Milestone 2 parsing/emission migration; stable recovery paths do not consume the legacy selector representation.
- Declaration lists are ordered slices rather than property maps, preserving fallback declarations and leaving cascade/importance decisions to later explicit passes.
- Rule lists remain ordered and recursive through pointers, preventing non-adjacent at-rules/rules from being merged by the data model itself.
- Column combinators remain representable in the AST but are not accepted by the stable parser because the current Selectors Level 4 grammar lists only descendant, child, next-sibling, and subsequent-sibling combinators. Native nesting accepts explicit `&`, leading combinators, and implicit parent references; a recursively encountered ampersand delimiter suppresses implicit-parent insertion metadata.
- Parsed declarations retain the complete raw value (including importance trivia/marker) while `valueWithoutImportance()` exposes the semantic prefix; no property map or early value interpretation may remove fallback order.
- Generic rule-list at-rules (`media`, `supports`, `container`, `layer`, `scope`, and related forms) recurse immediately; keyframes retain raw frame syntax for PAR-004, and page/unknown mixed grammars stay raw until their dedicated classifier can preserve all nested forms.
- Structured at-rule specialization keeps generic blocks as the recovery boundary. Keyframes retain the full raw frame stream alongside typed valid frames; page remains a raw mixed-content block alongside ordered typed declarations/margin boxes; malformed typed preludes never erase the underlying rule.
- Rule recovery uses the shared `css/recovery.zig` boundary contract: semicolon and complete curly-block boundaries always advance one component, EOF is stable, nested components remain opaque, and syntax diagnostics remain the sole report for stray closing tokens.
- Pretty emission follows CSSOM identifier/string escaping and CSS Syntax round-trip constraints. Comments inside raw component values are retained as token separators; generic structural whitespace is canonicalized; incomplete or partially lowered constructs are rejected before a successful output is returned.
- Pretty and minified emission are modes of the same implementation. Minified mode defaults to no final newline, may omit only a declaration immediately before its closing brace, and retains a semicolon when a following page item still needs an explicit declaration boundary.
- Unknown at-rule preludes and raw blocks preserve adjacency and edge whitespace-token presence in both emission modes. Pretty formatting may not insert a cosmetic separator into untyped syntax, and minification may collapse raw whitespace width but not erase the token.
- CSS source-map columns are zero-based UTF-16 code-unit offsets per ECMA-426. The map JSON includes `version: 3`, one ordered source, optional `sourcesContent`/generated file, no names, and strict four-field mappings; generated output and source-map ownership remain separate.
- Style blocks partition their source span between leading declarations and ordered child rules. Invalid block-bearing child rules still split declaration runs as CSS Syntax requires; their source gap remains explicit so both emitter modes reject recovered output rather than silently deleting authored bytes.
- Known declaration/keyframe/page/top-level-only at-rules are invalid in a nesting context and recover at their complete at-rule boundary. Raw unknown at-rules remain represented for forward compatibility; newly standardized forms require an explicit classifier/context decision.
- Stable parse diagnostics are emitted only after the complete typed parse and successful compilation produces no partial CSS. Parallel batch compilation likewise writes no destination until every task has compiled successfully.
- Independent grammar validation is pinned to exact Lightning CSS package bytes through `package-lock.json`. An upgrade changes the evidence baseline and must update the declared version and rerun both output modes for every fixture.
- Transform safety classes are independent permissions, not an ordinal hierarchy. Policy defaults to verified analysis only; transitive dependencies need the same explicit authority as requested passes. Stable `--optimize` is now confined to the static seven-definition preset whose complete membership has passed the acceptance suite.
- Transform deletions are proof-carrying AST entries, not trusted source spans. The emitter independently validates that an omitted rule is structurally empty and belongs to the closed cleanup set before skipping it; recursive omission proofs retain nested source coverage and surviving source maps never point into deleted rules.
- `zigcss-transform-test-driver` is installed only by `zig build test` for independent pass validation and does not enter the normal install artifact. Its individual compatibility/extraction routes remain test-only; its `verified-optimizer` route mirrors the stable CLI preset for independent differential validation.
- Duplicate analysis uses deterministic first-occurrence output order and bounded hash-backed counting, but copies report names and spans into caller-owned storage so results can safely outlive compilation. Default group, occurrence, property-byte, and recursion limits prevent report construction from becoming an unbounded resource sink.
- Numeric grammar and unit behavior are frozen by fixtures against the reviewed CSS Values and Units Level 4 and CSS Conditional Rules Level 5 drafts; dimension/percentage algebra follows CSS Typed OM Level 1. Living-spec changes require an intentional fixture and decision update rather than silently changing an optimizer result.
- `VAL-001` stores postfix typing evidence only. Context-dependent conversion, finite-value handling, precision policy, folding, and shortening remain `MATH-001` or later semantic-rewrite responsibilities and default to no rewrite when evidence is incomplete.
- Generated nodes use the smallest complete authored construct as their causal source span. Emission adds one mapping at the generated segment start and never invents inner mappings, borrows an adjacent span, or defaults to source offset zero.
- `MATH-001` folds only complete supported math-function values under an exact property category/range policy. Identical-unit finite arithmetic is eligible; cross-unit conversion, comments, substitutions, descriptors, unsupported properties, invalid ranges, ambiguous signed zero, and non-shorter output retain the exact input root.
- `OPT-012` treats compatibility as part of equivalence: opaque basic named/hex/RGB forms share the oldest closed output surface, while non-opaque functional/keyword colors remain authored unless alpha-hex support was already required. Unitless-zero shortening excludes percentage/dimension categories and every number-or-length ambiguity.
- `OPT-013` treats shorthand expansion as a reset-and-cascade proof, not a textual compression. Only an adjacent top/right/bottom/left group with identical importance and raw value spelling may become one `margin` declaration; logical properties, fallbacks, layers, media queries, and nested segments remain on the same side of the replacement boundary. Page and page-margin descriptor contexts are excluded until an independent computed/canonical oracle can verify them.
- `PREFIX-002` treats compatibility syntax as structured proof output. Complete unannotated forms may be added in deterministic target order, but the standard form remains last and no qualifier, unsupported gap, author-supplied vendor policy, or untyped selector context is interpreted into authority. Stable CLI exposure is a separate public API decision, not an automatic consequence of pass verification.
- `TREE-001` treats selector extraction as a closed-snapshot proof. Null inventory categories never mean empty, critical input must include the full selector-matching render tree, ASCII case variants conservatively retain rules, and one possible selector alternative preserves the entire style rule. The two modes remain experimental despite full package evidence and require two independent policy grants.
- Milestone 3 stable optimization is a closed preset, not a maturity wildcard. Because proof-carrying AST nodes deliberately block overlapping rewrites inside one source snapshot, the CLI reaches a bounded emitted-byte fixed point through validated reparses. Source maps are not exposed across those intermediate snapshots until a composed-map proof exists.
- `API-001` reuses the ownership-safe library root established by `ARCH-001` instead of creating a parallel facade. External-consumer compilation is the export gate; a symbol used only through implementation-relative imports does not count as public. The existing low-level modules remain experimental until `API-002` defines the stable compile facade.
- `API-002` keeps target queries caller-owned and borrowed only for the duration of `compile`; no target-query pointer enters a result. Dependencies are facts, not fetch instructions: only decoded top-level string/URL `@import` operands are reported, authored order and duplicates remain observable, unsupported preludes are not guessed, and resource-limit failure discards partial facts. Fixed-point optimization remains incompatible with source maps until intermediate mappings can be composed. Stable CSS deliberately returns `module_exports = null` pending `MODULE-001`.
- `API-003` confines native plugins to `plugin.` IDs and borrowed synchronous lifetimes. The closed optimizer finishes before the plugin plan; target prefixing participates in that final plan under global phase/priority/ID order. Plugin-only policy validation happens before compatibility authority is added for the built-in prefix pass, so no plugin can gain a class transitively. Active plugin source maps remain unavailable, `API0002` identifies invalid configuration, `API0003` identifies transactional execution failure, and native panic/UB/nontermination requires consumer process isolation rather than a false in-process recovery promise.
- `BUILD-001` assigns package identity fingerprint `0xae272a4871e93d07`; changing it would change package identity and requires an explicit ownership decision, not version maintenance. Version 0.3.0 matches the current npm manifest, and `minimum_zig_version = "0.15.2"` names the earliest tested baseline while CI remains pinned to that exact toolchain. The initial `.paths` list deliberately excludes `build_helpers.zig` until `BUILD-002`, and no remote URL/hash is documented before a published artifact exists.
- `BUILD-002` treats CSS compilation as a normal `Step.Run` assembled before execution: `LazyPath` input, generated output argument, tool dependency, bounded stdio, and exact downstream check. Multiple files require multiple independent helper calls, which preserves per-file cache keys and declared outputs. Portable output basenames prevent caller-selected paths from escaping the build cache. The artifact is a host tool; application cross targets must not be executed as build tools. Unsupported CLI features are absent rather than represented as accepted no-ops.
- `BUILD-003` treats examples as executable consumer contracts, not illustrative snippets. The build project obtains a separate host dependency for the compiler tool and a target dependency for application imports; CI compiles both supported safe modes, while a repository enumeration test fails when an authored Zig example has no declared build path. Generated caches and install outputs are never considered authored examples.
- `CLI-010` defines the executable/library boundary at `zigcss.compile`: option selection and owned-result consumption belong to the CLI, while parsing, transforms, dependencies, and emission stay in the library. The separately experimental LSP route retains its explicitly documented legacy boundary until Milestone 6. Because the public facade has no truthful internal timing hooks, `--profile` reports one end-to-end compile duration instead of inventing stage precision; `PROF-010` owns accurate stage and allocator metrics.
- `CLI-011` reserves `-` exclusively as the standard stream sentinel. Stdin is bounded by the same 10 MiB CLI input limit, receives the stable diagnostic name `<stdin>`, and is deliberately single-shot rather than watch/batch input. Only CSS syntax is accepted; future syntax tags require their owning format packages. Exit 0 means success/info, 1 means compilation or operational I/O failure, and 2 means caller usage/configuration failure. Informational/CSS bytes use stdout while warnings/errors use stderr. A POSIX signal remains a signal through the npm launcher rather than being flattened into an invented numeric success.
- `CLI-012` defines atomicity at one destination, using a temporary sibling and rename rather than in-place truncation. Parent creation is deliberately delayed until a successful result is committed, but a batch does not promise rollback of earlier independently committed files after a later operational failure. Existing destination link entries are replaced rather than followed. Batch names derive only from the final stem and, when needed, a normalized relative-path hash; order never participates, basename length is bounded, and the final path-identity gate remains the collision authority.
- `WATCH-001` treats the root's already-read bytes as the one compile snapshot and records that snapshot before attempting compilation. Error state is a watch state rather than a retry request. Direct dependency facts are watched conservatively only when they are local relative filesystem paths; URL schemes and absolute/origin-relative forms are not reinterpreted into local access, queries/fragments are not path bytes, imports are not recursively parsed or inlined, and failed roots retain the last valid direct set. Path deduplication follows the same platform case policy as output planning, while content and read-error transitions remain deterministic.
- `PARALLEL-001` bounds concurrency at eight even on larger hosts and schedules through one locked monotonic index rather than static chunks. Cancellation is cooperative at the public compile-call boundary: already-running tasks finish and are cleaned, but no new task is opened after the queue observes failure. Per-task GPAs deliberately disable their own mutex because ownership never crosses workers; task slots are fully planned before any allocator is first used and remain stable through main-thread deinit. Thread-creation failure is a cancellation path, not an unwind past live handles. Completion order never grants write or diagnostic order; the input plan is the sole commit order.
- `PROF-010` defines profile memory as allocator-requested ownership evidence, not RSS, heap capacity, or a platform memory claim. One public compile session owns the timer and stage boundaries; the CLI renders its returned metrics instead of maintaining a second clock. The forwarding allocator adds no headers and returns the caller allocator's exact pointers, allowing result-owned allocations to outlive the stack wrapper; its final live-byte snapshot is therefore the retained result size before caller cleanup. Skipped and sub-timer-resolution stages may be zero, while unavailable timing support is an explicit operational failure rather than fabricated data.
- `FMT-001` treats every inherited alternate-format implementation as characterized legacy code, not a positive compatibility claim. Availability, evidence, strategy, sources, and owning packages live in one executable matrix. Real extension probes and the CSS-only public enum are the current product contract. ADR-005 chooses removal for every heuristic ecosystem adapter except a future closed CSS Modules subset; ADR-011 now owns the unrelated native-plugin decision without an identifier collision.
- `SCSS-001` makes ADR-005 removal physical: neither Sass syntax has a parser source, legacy dispatch tag, or CSS fallback. The stable CLI remains the extension-facing rejection authority; the experimental LSP diagnoses unsupported/removed URIs. Activating its previously dormant test artifact also fixes URI ownership so one allocation is never freed as both map key and value. With no chosen native scope, `SCSS-002` is not applicable unless a new decision funds canonical integration.
- `LESS-001` removes the global at-variable substitution path rather than treating a handful of examples as a Less grammar. The source, tag, and dispatch are absent; `.less` remains a real CLI rejection and a null detector/LSP diagnostic path. Any future Less work requires a new funded scope or canonical-integration decision.
- `STYLUS-001` removes line/indent guessing and swallowed variable errors rather than advertising a Stylus subset. The source, tag, and dispatch are absent; `.styl` remains an explicit CLI and LSP rejection. Future Stylus work requires a new funded language/integration decision.
- `MODULE-001` treats the compile-call source name as a required module identity, normalizes only path separators, and hashes length-delimited identity plus decoded class bytes under the versioned `zigcss.css-modules.v1` domain. Full SHA-256 output avoids order-dependent suffixes; a distinct-name digest collision is an internal failure, not permission to weaken determinism. Export collection owns first occurrence order while emission uses a separate byte-sorted lookup. The initial grammar is local-by-default and deny-by-default: unsupported module constructs or selector-bearing raw syntax produce `CSS0009` with no CSS, and optimizer/prefix/plugin composition awaits its own semantic evidence.
- `MODULE-002` scope uses only canonical functional `:local(...)` and `:global(...)` wrappers. Bare scope-state switching is intentionally rejected as ambiguous. Class rewrites are keyed by exact source occurrence rather than decoded name, wrapper fragments are parsed silently into the compilation arena, and the emitter removes only adapter-validated wrapper spans. The initial class-only contract rejects IDs rather than preserving them under a false locality claim.
- `MODULE-002` composition mirrors the useful unresolved-reference shape of the pinned Lightning CSS oracle rather than flattening external module state the compiler does not own. Local references point to owned generated names, global references retain decoded identifiers, and dependency references retain decoded export/specifier pairs. One external declaration yields one bounded `.css_module` dependency fact; ZigCSS never opens, resolves, or executes a dependency. Plain-class targets and acyclic local graphs are stricter than permissive ecosystem behavior by design.
- `MODULE-002` local values are a deliberately closed single-file substitution pass. Definitions resolve only earlier definitions so their serialized exports are deterministic; ordinary CSS is scanned after collection so authored CSS may use a later definition. Exact tokenizer identity, not substring matching, controls replacement. Strings and URLs are never opened, and imported values/raw ICSS remain strict errors because this compiler call owns neither a resolver nor external module state. Class and value exports intentionally share one namespace and first-authored order.
- `JS-001` makes ADR-005 removal physical: the repository contains no JavaScript/TypeScript byte scanner, expression-deletion path, legacy tag, or parser dispatch. `.css.js` and `.css.ts` stay recognizable only at the stable CLI rejection boundary and deliberately become null in the legacy detector so the LSP cannot reinterpret application code as CSS. Any future extraction requires a funded, version-pinned JavaScript/TypeScript parser and a non-executing static contract for tagged templates and dynamic expressions.
- `POSTCSS-001` removes the directive scanner rather than laundering fixed deletion/expansion behavior under the PostCSS name. `.postcss` remains only an explicit CLI rejection and null legacy-detector result; there is no plugin host, tag, or dispatch. Removing the scanner also makes the separate fixed utility registry unreachable, but that source remains characterized until `TAILWIND-001` owns its deletion and evidence.
- `TAILWIND-001` removes the unreachable fixed utility registry rather than preserving the Tailwind name for a finite table that omitted unknown utilities. Compiler-side removal does not alter the documentation site's separate pinned canonical Tailwind build dependency. Any future ZigCSS utility preset needs its own non-Tailwind name and exact semantics; claiming Tailwind requires canonical, versioned compilation evidence.
- `LSP-001` treats framing as a bounded owned byte transport, not JSON or method semantics. Exact CRLF header termination, one decimal `Content-Length`, 8 KiB aggregate headers, 16 MiB bodies, sequential frames, and clean boundary EOF are the complete admitted scope. Malformed transport input remains an operational error until `LSP-002` defines structured protocol errors; method and document lifecycle belongs to `LSP-003`.
- `LSP-002` admits only a JSON-RPC 2.0 object envelope with a string method and a string, number, or null ID. Standard parsing and serialization own all escaping and number spelling; parse/envelope/method/parameter faults are protocol results rather than process crashes. Handler validation occurs before response mutation, while committed document/root state remains owned even if later serialization fails. Notifications intentionally still receive the inherited response until `LSP-003` introduces a request/notification result type and lifecycle state machine.
- `LSP-003` uses an explicit synchronous lifecycle rather than pretending cancellation concurrency. A valid notification never owns response bytes. Initialization, running, and shutdown are disjoint states; only an exit notification produces process control, and EOF at a frame boundary remains a clean transport completion. Full document synchronization is the sole advertised capability: client-owned open text, strictly increasing full replacements, and balanced close ownership. Legacy diagnostic/navigation handlers remain unadvertised and are scheduled for replacement by `LSP-004` through `LSP-006`.
- `LSP-004` fixes the server's protocol coordinate system at UTF-16, which every LSP client and server must support and which remains the default when no alternate encoding is negotiated. One converter owns both directions between client positions and source bytes, with strict scalar/CRLF boundaries and specified line-end clamping. Existing editor handlers use it for positional integrity but remain unadvertised until compiler diagnostics and syntax/workspace indexes replace their legacy semantics in `LSP-005` and `LSP-006`.
- `LSP-005` uses document diagnostic pulls rather than inventing a push scheduler in the synchronous server. It advertises no inter-file/workspace claim, emits no result ID, and returns a complete current report for the client-owned document version on every standard singular request. Compiler diagnostics are consumed only through the external public module; unsupported document kinds remain a separate containment diagnostic. No response bytes are begun until parameters, compilation, and every UTF-16 range have been validated.
- `LSP-006` treats a workspace as deterministic synchronized state, not permission to crawl the filesystem. One typed index is lazily owned by each open CSS document version; full replacement and close are its only invalidation boundaries. Class and ID selectors refer to external DOM symbols and therefore have no fabricated stylesheet definition, while custom properties and keyframe names have grammar-owned definition/reference relationships. Ambiguous shorthand/value syntax and opaque content default to no result. All workspace state, index allocation, search work, result counts, rename input, and escaped response bytes have explicit fail-closed budgets, and an over-budget build or update leaves the previous owned state intact.
- `LSP-007` closes the server package with composition evidence rather than another implementation fork. The final child transcript combines malformed-request recovery, a genuinely large synchronized document, Unicode/CRLF coordinate conversion, compiler diagnostics, index navigation, notification silence, close invalidation, shutdown/exit, and exact frame consumption. Repeated allocator-checked index lifecycles prove persistent counters and ownership return to zero; component-level exhaustive allocation tests remain the stronger proof for every injected failure point. Editor distribution, discovery, and client integration remain separately owned by `VSCODE-001` and `NEOVIM-001`.

- `VSCODE-001` treats the extension as a reproducible client artifact, not implicit binary distribution authority. Discovery never executes a relative/workspace path, an invalid explicit setting fails without fallback, runtime validation mirrors schema limits, and package verification admits an exact reviewed inventory before deleting generated output. Publishing and native-binary bundling remain separate unauthorized decisions.

- `NEOVIM-001` treats editor configuration as process-launch authority. Explicit paths are absolute verified executables and never fall back; automatic lookup ignores relative/empty `PATH` entries and is bounded on both directories and Windows extensions. The workspace root controls client grouping only, not file discovery. Configuration publishes no mapping for an unadvertised method, and pinned real clients—not Lua text alone—are the graduation gate.

- `DOC-001` treats public capability status as generated test evidence rather than prose copied across surfaces. Metadata may cite only a closed status kind and executable gate with repository-confined content anchors; Markdown tables are generated views, and the site consumes the same rows directly. Unknown, contradictory, stale, or unanchored claims are build failures.

- `DOC-002` treats code formatting as an executable claim and links as repository-bound references. Canonical Zig examples are exact source files that compile and run in the main safe-mode graph; CSS output must also parse independently; shell, JSON, Lua, and Vim snippets use their real syntax parsers without executing publication instructions or loading user editor state. New languages, dynamic rendered examples, missing labels/routes/fragments, and paths that escape lexically or through symlinks fail closed.

- `DEP-001` treats dependency state as the union of an exact manifest/lock inventory, live production advisory evidence, and review-only update proposals. Direct versions stay exact, new lock roots require an explicit policy update, audit transport/schema errors cannot masquerade as clean results, and automation may open bounded pull requests but never gains registry credentials, target override, merge, publication, or deployment authority.

- `REL-001` treats `VERSION` and Milestone 9's approved `0.5.0-rc.1` candidate as one identity contract. Core npm, Zig, CLI, Docker, formula, docs, and changelog surfaces use the full SemVer; VS Marketplace uses base version 0.5.0 only because its manifest rejects prerelease identifiers and therefore must package with `--pre-release`. Formula checksum completion remains dependency-ordered under `REL-003`, and no synchronized version authorizes publication.

- `REL-002` follows the official [GitHub artifact-attestation workflow](https://docs.github.com/en/actions/how-tos/secure-your-work/use-artifact-attestations/use-artifact-attestations) and immutable [`actions/attest` v4.1.1](https://github.com/actions/attest/releases/tag/v4.1.1): the release build receives only read contents plus the required attestation/OIDC grants, while release creation retains the sole repository-content write grant. The SPDX document follows the fixed [SPDX 2.3 package fields and verification-code algorithm](https://spdx.github.io/spdx-spec/v2.3/package-information/) and describes only the binary actually shipped. Signature verification stays two-layered: bounded local subject/predicate binding plus GitHub CLI cryptographic verification; only the latter establishes trust. Actual signing is deliberately deferred to the authorized tag workflow, not simulated or claimed by local tests.

- `REL-003` treats release filenames, target headers, generated checksums, and consumer-specific trust roots as closed contracts. HTTPS alone never authorizes installation: npm and Docker must validate their exact archive/manifest/inventory and executable target before late installation, while Homebrew must bind one immutable source commit, its downloaded SHA-256, the reviewed Zig 0.15 toolchain, and exact compiler behavior. The Docker image remains a verified local artifact and the Homebrew formula remains an unpublished local source-build path; neither is a publication claim.

- `REL-004` treats a workflow action update as a reviewed supply-chain change, not a tag-following convenience. The full SHA, version comment, approved action identity, job placement, and exact permission map must advance together; a Dependabot proposal remains red until that policy evidence is reviewed. OIDC is granted only to the Pages deployment and release-attestation jobs, repository write belongs only to release creation, and all other unspecified token permissions are mechanically `none`.

- `REL-005` treats header inspection as necessary but insufficient release evidence. Each target must run on a host whose Node platform/architecture exactly matches the artifact, execute the extracted archive, and then prove the packaged npm postinstall plus wrapper using only repository-local checksum-bound assets. The smoke preload is test-only, excluded from the npm package, admits exactly the expected archive and manifest URLs, and blocks every other HTTPS request. A failed target prevents attestation/upload and therefore prevents release creation and npm publication.

- `BENCH-001` treats corpus identity as generated source evidence. Version `v1` is a closed regular-file inventory with exact bytes, lowercase SHA-256, and mechanically derived rule complexity; the active Zig smoke embeds those files and cannot substitute approximate or runtime-generated inputs. The historical competitor script and archived result JSON are not evidence. Binary pinning, output validation, mode comparability, raw samples, statistical reporting, controlled execution, and public claims remain separate dependency-ordered packages.

- `BENCH-002` treats competitor selection as a supply-chain contract, not permission to discover tools at runtime. Exact direct and platform package versions, licenses, registry archives, and SHA-512 integrity live in the npm lock; lifecycle scripts remain disabled, and only the one host-matched optional native executable may run by absolute path. Lightning CSS's mismatched internal version string is preserved as observed evidence. Tool presence and identity do not make output or timings valid; `BENCH-003` must reject semantically invalid output before any sample can enter later mode/statistics work.
- `BENCH-003` treats a successful process exit as insufficient benchmark evidence. Exact file output from each closed invocation is bounded and reparsed with recovery disabled, then compared to the canonical semantics of the exact versioned corpus. A duration is appended only after validation succeeds, so empty, malformed, warning-bearing, truncated, changed, or extra output remains a hard failure rather than a missing sample. The active smoke uses only the rebuilt public compiler and typed equivalence; the inherited benchmark timing API is removed. No current gate claims comparable modes or publishes measurements before `BENCH-004` through `BENCH-007`.
- `BENCH-004` defines cold CLI as a fresh process with startup included and zero mode-local warmups, and warm CLI as the identical command after one separately validated mode-local invocation; neither name claims that the operating system page cache was purged. Those are the only cross-tool comparison modes. ZigCSS direct API latency and throughput exclude process startup, while memory reports only allocator-requested ownership fields and admits no timing metric. Esbuild's serviced JavaScript API is not mislabeled as in-process. At that checkpoint all measured CSS remained unobservable until post-timing validation succeeded, and raw values remained unavailable pending `BENCH-005`.
- `BENCH-005` defines a complete report as 43 ordered series with 20 retained unsigned-decimal observations each. Median uses the sorted middle average, p95 uses nearest rank, and variance is the exact population value; all three are recomputed during validation so copied prose or altered summaries cannot become evidence. Reports bind to the exact corpus manifest, three CLI executables, private in-process runner, and closed host/toolchain environment. The local output path is create-only, CI keeps the report temporary, and no statistic is a public comparison until `BENCH-006` controls and archives execution and `BENCH-007` renders only those archives.
- `BENCH-006` defines controlled execution as the exact upstream main branch running on one dedicated self-hosted Linux x64 label under a serialized weekly schedule. The archive is an exact report/manifest pair whose source, run, byte digest, and hardware fingerprint must revalidate before consumption. A 90-day GitHub Actions artifact is a review handoff, not publication; `BENCH-007` must reject unverified, expired/unavailable, differently fingerprinted, or manually invented data rather than filling a report gap.
- `BENCH-007` defines publication as an exact generated view over a permanently committed copy of one retained controlled archive. The GitHub run/artifact link must agree with archive provenance, but raw samples remain available through the committed report after artifact expiry. Cross-tool tables contain only identical cold/warm CLI boundaries; ZigCSS direct API, allocator-requested memory, and throughput remain separately labeled non-comparative evidence. Repository state cannot transition from the exact withdrawal unless archive inventory, provenance, statistics, digests, bounded presentation text, selected path, and Markdown bytes all revalidate.

## Last full validation

`NSASS-011-LOCAL-USE-THIRTY-FIFTH-MODULE-REEXPORTED-CALLABLE-CONFIGURATION-OWNERSHIP-FOUNDATION` is verified at local implementation checkpoint `56e5b3fd327988340a46191f7c6f198d8c509bce`. Fresh Dart Sass 1.101.0 probes accept thirty-fifth- and thirty-sixth-module configuration in SCSS and indented Sass with function invocation and mixin application inside and after the configured modules, original-owner live state, identical compressed CSS, and zero warnings. Native Zig admits only the scheduled thirty-fifth-module hop by centralizing a maximum configurable re-export depth of thirty-three: depth thirty-three may configure the thirty-fifth module, depth thirty-four remains fail-closed at the thirty-sixth, and callable-argument/content remappers retain their stricter boundaries. The prior thirty-fourth-module fixture remains intact, while the additive thirty-fifth-module fixture covers direct/enumerated/nested and returned values, calls and mixins inside and after the thirty-fifth module, aliases, identity, state, dependencies, maps, exact thirty-sixth-hop diagnostics, no partial CSS, limits, and every allocation failure. The test-first focused Debug gate stopped at 639/642 on the former positive rejection, allocation path, and diagnostic position. Final focused native Debug/ReleaseSafe pass 23/23 steps and 642/642 tests, and full compiler Debug/ReleaseSafe pass 60/60 steps and 1137/1137 tests. Evaluator-test-root and preprocessor-root no-emission cross-compiles pass for Windows x64 GNU and Linux ARM64 GNU in both modes; native contract 12/12 plus check, provider/reference/product 153/153, package policy 6/6 plus check, capability status 7/7 plus check, runner selftest 59/59, repository-wide Zig formatting, changed JavaScript syntax, all tracked JSON parsing, and diff gates pass. The private native contract adds only `local-use-thirty-fifth-module-reexported-callable-configuration-ownership-foundation`; production reachability, native graduation, release state, and external systems are unchanged. Thirty-sixth-module configuration is the next already measured slice; thirty-seventh-and-later configuration, callable arguments/content, transitive modules, forwards/imports, `meta.load-css()`, and broader resolution remain separate.

`NSASS-011-LOCAL-USE-THIRTY-FOURTH-MODULE-REEXPORTED-CALLABLE-CONFIGURATION-OWNERSHIP-FOUNDATION` is verified at local implementation checkpoint `d1b53ac52704337ca5a5fc61f7395f2f433b3f8c`. Fresh Dart Sass 1.101.0 probes accept thirty-fourth- and thirty-fifth-module configuration in SCSS and indented Sass with function invocation and mixin application inside and after the configured modules, original-owner live state, identical compressed CSS, and zero warnings. Native Zig admits only the scheduled thirty-fourth-module hop by centralizing a maximum configurable re-export depth of thirty-two: depth thirty-two may configure the thirty-fourth module, depth thirty-three remains fail-closed at the thirty-fifth, and callable-argument/content remappers retain their stricter boundaries. The prior thirty-third-module fixture remains intact, while the additive thirty-fourth-module fixture covers direct/enumerated/nested and returned values, calls and mixins inside and after the thirty-fourth module, aliases, identity, state, dependencies, maps, exact thirty-fifth-hop diagnostics, no partial CSS, limits, and every allocation failure. The test-first focused Debug gate stopped at 638/641 on the former positive rejection, allocation path, and diagnostic position. Final focused native Debug/ReleaseSafe pass 23/23 steps and 641/641 tests, and full compiler Debug/ReleaseSafe pass 60/60 steps and 1136/1136 tests. Evaluator-test-root and preprocessor-root no-emission cross-compiles pass for Windows x64 GNU and Linux ARM64 GNU in both modes; native contract 12/12 plus check, provider/reference/product 153/153, package policy 6/6 plus check, capability status 7/7 plus check, runner selftest 59/59, repository-wide Zig formatting, changed JavaScript syntax, all tracked JSON parsing, and diff gates pass. The private native contract adds only `local-use-thirty-fourth-module-reexported-callable-configuration-ownership-foundation`; production reachability, native graduation, release state, and external systems are unchanged. Thirty-fifth-module configuration is the next already measured slice; thirty-sixth-and-later configuration, callable arguments/content, transitive modules, forwards/imports, `meta.load-css()`, and broader resolution remain separate.
`NSASS-011-LOCAL-USE-THIRTY-THIRD-MODULE-REEXPORTED-CALLABLE-CONFIGURATION-OWNERSHIP-FOUNDATION` is verified at local implementation checkpoint `304648d3538567589411911dc64611f474003e7e`. Fresh Dart Sass 1.101.0 probes accept thirty-third- and thirty-fourth-module configuration in SCSS and indented Sass with function invocation and mixin application inside and after the configured modules, original-owner live state, identical compressed CSS, and zero warnings. Native Zig admits only the scheduled thirty-third-module hop by centralizing a maximum configurable re-export depth of thirty-one: depth thirty-one may configure the thirty-third module, depth thirty-two remains fail-closed at the thirty-fourth, and callable-argument/content remappers retain their stricter boundaries. The prior thirty-second-module fixture remains intact, while the additive thirty-third-module fixture covers direct/enumerated/nested and returned values, calls and mixins inside and after the thirty-third module, aliases, identity, state, dependencies, maps, exact thirty-fourth-hop diagnostics, no partial CSS, limits, and every allocation failure. The test-first focused Debug gate stopped at 637/640 on the former positive rejection, allocation path, and diagnostic position. Final focused native Debug/ReleaseSafe pass 23/23 steps and 640/640 tests, and full compiler Debug/ReleaseSafe pass 60/60 steps and 1135/1135 tests. Evaluator-test-root and preprocessor-root no-emission cross-compiles pass for Windows x64 GNU and Linux ARM64 GNU in both modes; native contract 12/12 plus check, provider/reference/product 153/153, package policy 6/6 plus check, capability status 7/7 plus check, runner selftest 59/59, repository-wide Zig formatting, changed JavaScript syntax, all tracked JSON parsing, and diff gates pass. The private native contract adds only `local-use-thirty-third-module-reexported-callable-configuration-ownership-foundation`; production reachability, native graduation, release state, and external systems are unchanged. Thirty-fourth-module configuration is the next already measured slice; thirty-fifth-and-later configuration, callable arguments/content, transitive modules, forwards/imports, `meta.load-css()`, and broader resolution remain separate.
`NSASS-011-LOCAL-USE-THIRTY-SECOND-MODULE-REEXPORTED-CALLABLE-CONFIGURATION-OWNERSHIP-FOUNDATION` is verified at local implementation checkpoint `7229cefa359f9cd1feb99ae50535b51998d7ccec`. Fresh Dart Sass 1.101.0 probes accept thirty-second- and thirty-third-module configuration in SCSS and indented Sass with function invocation and mixin application inside and after the configured modules, original-owner live state, identical compressed CSS, and zero warnings. Native Zig admits only the scheduled thirty-second-module hop by centralizing a maximum configurable re-export depth of thirty: depth thirty may configure the thirty-second module, depth thirty-one remains fail-closed at the thirty-third, and callable-argument/content remappers retain their stricter boundaries. The prior thirty-first-module fixture remains intact, while the additive thirty-second-module fixture covers direct/enumerated/nested and returned values, calls and mixins inside and after the thirty-second module, aliases, identity, state, dependencies, maps, exact thirty-third-hop diagnostics, no partial CSS, limits, and every allocation failure. The test-first focused Debug gate stopped at 636/639 on the former positive rejection, allocation path, and diagnostic position. Final focused native Debug/ReleaseSafe pass 23/23 steps and 639/639 tests, and full compiler Debug/ReleaseSafe pass 60/60 steps and 1134/1134 tests. Evaluator-test-root and preprocessor-root no-emission cross-compiles pass for Windows x64 GNU and Linux ARM64 GNU in both modes; native contract 12/12 plus check, provider/reference/product 153/153, package policy 6/6 plus check, capability status 7/7 plus check, runner selftest 59/59, repository-wide Zig formatting, changed JavaScript syntax, all tracked JSON parsing, and diff gates pass. The private native contract adds only `local-use-thirty-second-module-reexported-callable-configuration-ownership-foundation`; production reachability, native graduation, release state, and external systems are unchanged. Thirty-third-module configuration is the next already measured slice; thirty-fourth-and-later configuration, callable arguments/content, transitive modules, forwards/imports, `meta.load-css()`, and broader resolution remain separate.
`NSASS-011-LOCAL-USE-THIRTY-FIRST-MODULE-REEXPORTED-CALLABLE-CONFIGURATION-OWNERSHIP-FOUNDATION` is verified at local implementation checkpoint `7bbe368c3f0875d5d9ffd92727c3e78710574399`. Fresh Dart Sass 1.101.0 probes accept thirty-first- and thirty-second-module configuration in SCSS and indented Sass with function invocation and mixin application inside and after the configured modules, default/custom aliases, original-owner live state, identical compressed CSS, and zero warnings. Native Zig admits only the scheduled thirty-first-module hop by centralizing a maximum configurable re-export depth of twenty-nine: depth twenty-nine may configure the thirty-first module, depth thirty remains fail-closed at the thirty-second, and callable-argument/content remappers retain their stricter boundaries. The prior thirtieth-module fixture remains intact, while the additive thirty-first-module fixture covers direct/enumerated/nested and returned values, calls and mixins inside and after the thirty-first module, aliases, identity, state, dependencies, maps, exact thirty-second-hop diagnostics, no partial CSS, limits, and every allocation failure. The test-first focused Debug gate stopped at 635/638 on the former positive rejection, allocation path, and diagnostic position. Final focused native Debug/ReleaseSafe pass 23/23 steps and 638/638 tests, and full compiler Debug/ReleaseSafe pass 60/60 steps and 1133/1133 tests. Evaluator-test-root and preprocessor-root no-emission cross-compiles pass for Windows x64 GNU and Linux ARM64 GNU in both modes; native contract 12/12 plus check, provider/reference/product 153/153, package policy 6/6 plus check, capability status 7/7 plus check, runner selftest 59/59, repository-wide Zig formatting, changed JavaScript syntax, all tracked JSON parsing, and diff gates pass. The private native contract adds only `local-use-thirty-first-module-reexported-callable-configuration-ownership-foundation`; production reachability, native graduation, release state, and external systems are unchanged. Thirty-second-module configuration is the next already measured slice; thirty-third-and-later configuration, callable arguments/content, transitive modules, forwards/imports, `meta.load-css()`, and broader resolution remain separate.
`NSASS-011-LOCAL-USE-THIRTIETH-MODULE-REEXPORTED-CALLABLE-CONFIGURATION-OWNERSHIP-FOUNDATION` is verified at local implementation checkpoint `ef1540270ea26391d45b999765df9f0f8b466b3f`. Fresh Dart Sass 1.101.0 probes accept thirtieth- and thirty-first-module configuration in SCSS and indented Sass with function invocation and mixin application inside and after the configured modules, original-owner live state, identical compressed CSS, and zero warnings. Native Zig admits only the scheduled thirtieth-module hop by centralizing a maximum configurable re-export depth of twenty-eight: depth twenty-eight may configure the thirtieth module, depth twenty-nine remains fail-closed at the thirty-first, and callable-argument/content remappers retain their stricter boundaries. The prior twenty-ninth-module fixture remains intact, while the additive thirtieth-module fixture covers direct/enumerated/nested and returned values, calls and mixins inside and after the thirtieth module, aliases, identity, state, dependencies, maps, exact thirty-first-hop diagnostics, no partial CSS, limits, and every allocation failure. The corrected test-first focused Debug gate stopped at 634/637 on the former positive rejection, allocation path, and diagnostic position. Final focused native Debug/ReleaseSafe pass 23/23 steps and 637/637 tests, and full compiler Debug/ReleaseSafe pass 60/60 steps and 1132/1132 tests. Evaluator-test-root and preprocessor-root no-emission cross-compiles pass for Windows x64 GNU and Linux ARM64 GNU in both modes; native contract 12/12 plus check, provider/reference/product 153/153, package policy 6/6 plus check, capability status 7/7 plus check, runner selftest 59/59, repository-wide Zig formatting, changed JavaScript syntax, all tracked JSON parsing, and diff gates pass. The private native contract adds only `local-use-thirtieth-module-reexported-callable-configuration-ownership-foundation`; production reachability, native graduation, release state, and external systems are unchanged. Thirty-first-module configuration is the next already measured slice; thirty-second-and-later configuration, callable arguments/content, transitive modules, forwards/imports, `meta.load-css()`, and broader resolution remain separate.
`NSASS-011-LOCAL-USE-TWENTY-NINTH-MODULE-REEXPORTED-CALLABLE-CONFIGURATION-OWNERSHIP-FOUNDATION` is verified at local implementation checkpoint `dcbfe4ea3af82ebd7f63f3916dd61e69a3302be1`. Prior fresh Dart Sass 1.101.0 probes establish the expected twenty-ninth-module behavior in SCSS and indented Sass with function invocation and mixin application inside and after the configured module, original-owner live state, identical compressed CSS, and zero warnings. Native Zig admits only the scheduled twenty-ninth-module hop by centralizing a maximum configurable re-export depth of twenty-seven: depth twenty-seven may configure the twenty-ninth module, depth twenty-eight remains fail-closed at the thirtieth, and callable-argument/content remappers retain their stricter boundaries. The prior twenty-eighth-module fixture remains intact, while the additive twenty-ninth-module fixture covers direct/enumerated/nested and returned values, calls and mixins inside and after the twenty-ninth module, aliases, identity, state, dependencies, maps, exact thirtieth-hop diagnostics, no partial CSS, limits, and every allocation failure. The test-first focused Debug gate stopped at 633/636 on the former positive rejection, allocation path, and diagnostic position. Final focused native Debug/ReleaseSafe pass 23/23 steps and 636/636 tests, and full compiler Debug/ReleaseSafe pass 60/60 steps and 1131/1131 tests. Evaluator-test-root and preprocessor-root no-emission cross-compiles pass for Windows x64 GNU and Linux ARM64 GNU in both modes; native contract 12/12 plus check, provider/reference/product 153/153, package policy 6/6 plus check, capability status 7/7 plus check, runner selftest 59/59, repository-wide Zig formatting, changed JavaScript syntax, all tracked JSON parsing, and diff gates pass. The private native contract adds only `local-use-twenty-ninth-module-reexported-callable-configuration-ownership-foundation`; production reachability, native graduation, release state, and external systems are unchanged. Thirtieth-module configuration is the next measurement slice; thirty-first-and-later configuration, callable arguments/content, transitive modules, forwards/imports, `meta.load-css()`, and broader resolution remain separate.
`NSASS-011-LOCAL-USE-TWENTY-EIGHTH-MODULE-REEXPORTED-CALLABLE-CONFIGURATION-OWNERSHIP-FOUNDATION` is verified at local implementation checkpoint `4a7034893a38d9e4fd550696bae674eb17635ce8`. Fresh Dart Sass 1.101.0 probes accept twenty-eighth- and twenty-ninth-module configuration in SCSS and indented Sass with function invocation and mixin application inside and after the configured modules, original-owner live state, identical compressed CSS, and zero warnings. Native Zig admits only the scheduled twenty-eighth-module hop by centralizing a maximum configurable re-export depth of twenty-six: depth twenty-six may configure the twenty-eighth module, depth twenty-seven remains fail-closed at the twenty-ninth, and callable-argument/content remappers retain their stricter boundaries. The prior twenty-seventh-module fixture remains intact, while the additive twenty-eighth-module fixture covers direct/enumerated/nested and returned values, calls and mixins inside and after the twenty-eighth module, aliases, identity, state, dependencies, maps, exact twenty-ninth-hop diagnostics, no partial CSS, limits, and every allocation failure. The test-first focused Debug gate stopped at 632/635 on the former positive rejection, allocation path, and diagnostic position. Final focused native Debug/ReleaseSafe pass 23/23 steps and 635/635 tests, and full compiler Debug/ReleaseSafe pass 60/60 steps and 1130/1130 tests. Evaluator-test-root and preprocessor-root no-emission cross-compiles pass for Windows x64 GNU and Linux ARM64 GNU in both modes; native contract 12/12 plus check, provider/reference/product 153/153, package policy 6/6 plus check, capability status 7/7 plus check, runner selftest 59/59, repository-wide Zig formatting, changed JavaScript syntax, all tracked JSON parsing, and diff gates pass. The private native contract adds only `local-use-twenty-eighth-module-reexported-callable-configuration-ownership-foundation`; production reachability, native graduation, release state, and external systems are unchanged. Twenty-ninth-module configuration is the next already measured slice; thirtieth-and-later configuration, callable arguments/content, transitive modules, forwards/imports, `meta.load-css()`, and broader resolution remain separate.
`NSASS-011-LOCAL-USE-TWENTY-SEVENTH-MODULE-REEXPORTED-CALLABLE-CONFIGURATION-OWNERSHIP-FOUNDATION` is verified at local implementation checkpoint `3d013790ecec5306dd7107e002a64b55b7d12759`. Fresh Dart Sass 1.101.0 probes accept twenty-seventh- and twenty-eighth-module configuration in SCSS and indented Sass with function invocation and mixin application inside and after the configured modules, original-owner live state, identical compressed CSS, and zero warnings. Native Zig admits only the scheduled twenty-seventh-module hop by centralizing a maximum configurable re-export depth of twenty-five: depth twenty-five may configure the twenty-seventh module, depth twenty-six remains fail-closed at the twenty-eighth, and callable-argument/content remappers retain their stricter boundaries. The prior twenty-sixth-module fixture remains intact, while the additive twenty-seventh-module fixture covers direct/enumerated/nested and returned values, calls and mixins inside and after the twenty-seventh module, aliases, identity, state, dependencies, maps, exact twenty-eighth-hop diagnostics, no partial CSS, limits, and every allocation failure. The test-first focused Debug gate stopped at 631/634 on the former positive rejection, allocation path, and diagnostic position. Final focused native Debug/ReleaseSafe pass 23/23 steps and 634/634 tests, and full compiler Debug/ReleaseSafe pass 60/60 steps and 1129/1129 tests. Evaluator-test-root and preprocessor-root no-emission cross-compiles pass for Windows x64 GNU and Linux ARM64 GNU in both modes; native contract 12/12 plus check, provider/reference/product 153/153, package policy 6/6 plus check, capability status 7/7 plus check, runner selftest 59/59, repository-wide Zig formatting, changed JavaScript syntax, all tracked JSON parsing, and diff gates pass. The private native contract adds only `local-use-twenty-seventh-module-reexported-callable-configuration-ownership-foundation`; production reachability, native graduation, release state, and external systems are unchanged. Twenty-eighth-module configuration is the next already measured slice; twenty-ninth-and-later configuration, callable arguments/content, transitive modules, forwards/imports, `meta.load-css()`, and broader resolution remain separate.
`NSASS-011-LOCAL-USE-TWENTY-SIXTH-MODULE-REEXPORTED-CALLABLE-CONFIGURATION-OWNERSHIP-FOUNDATION` is verified at local implementation checkpoint `2322aec3622bdf47294e20dc5b30679245855421`. Fresh Dart Sass 1.101.0 probes accept twenty-sixth- and twenty-seventh-module configuration in SCSS and indented Sass with function invocation and mixin application inside and after the configured modules, original-owner live state, identical compressed CSS, and zero warnings. Native Zig admits only the scheduled twenty-sixth-module hop by centralizing a maximum configurable re-export depth of twenty-four: depth twenty-four may configure the twenty-sixth module, depth twenty-five remains fail-closed at the twenty-seventh, and callable-argument/content remappers retain their stricter boundaries. The prior twenty-fifth-module fixture remains intact, while the additive twenty-sixth-module fixture covers direct/enumerated/nested and returned values, calls and mixins inside and after the twenty-sixth module, aliases, identity, state, dependencies, maps, exact twenty-seventh-hop diagnostics, no partial CSS, limits, and every allocation failure. The test-first focused Debug gate stopped at 630/633 on the former positive rejection, allocation path, and diagnostic position. Final focused native Debug/ReleaseSafe pass 23/23 steps and 633/633 tests, and full compiler Debug/ReleaseSafe pass 60/60 steps and 1128/1128 tests. Evaluator-test-root and preprocessor-root no-emission cross-compiles pass for Windows x64 GNU and Linux ARM64 GNU in both modes; native contract 12/12 plus check, provider/reference/product 153/153, package policy 6/6 plus check, capability status 7/7 plus check, runner selftest 59/59, repository-wide Zig formatting, changed JavaScript syntax, all tracked JSON parsing, and diff gates pass. The private native contract adds only `local-use-twenty-sixth-module-reexported-callable-configuration-ownership-foundation`; production reachability, native graduation, release state, and external systems are unchanged. Twenty-seventh-module configuration is the next already measured slice; twenty-eighth-and-later configuration, callable arguments/content, transitive modules, forwards/imports, `meta.load-css()`, and broader resolution remain separate.
`NSASS-011-LOCAL-USE-TWENTY-FIFTH-MODULE-REEXPORTED-CALLABLE-CONFIGURATION-OWNERSHIP-FOUNDATION` is verified at local implementation checkpoint `ff92cdcef768b77cbc186e09b6e0eabbabe6e475`. Fresh Dart Sass 1.101.0 probes accept twenty-fifth- and twenty-sixth-module configuration in SCSS and indented Sass with function invocation and mixin application inside and after the configured modules, original-owner live state, identical compressed CSS, and zero warnings. Native Zig admits only the scheduled twenty-fifth-module hop by centralizing a maximum configurable re-export depth of twenty-three: depth twenty-three may configure the twenty-fifth module, depth twenty-four remains fail-closed at the twenty-sixth, and callable-argument/content remappers retain their stricter boundaries. The prior twenty-fourth-module fixture remains intact, while the additive twenty-fifth-module fixture covers direct/enumerated/nested and returned values, calls and mixins inside and after the twenty-fifth module, aliases, identity, state, dependencies, maps, exact twenty-sixth-hop diagnostics, no partial CSS, limits, and every allocation failure. The test-first focused Debug gate stopped at 629/632 on the former positive rejection, allocation path, and diagnostic position. Final focused native Debug/ReleaseSafe pass 23/23 steps and 632/632 tests, and full compiler Debug/ReleaseSafe pass 60/60 steps and 1127/1127 tests. Evaluator-test-root and preprocessor-root no-emission cross-compiles pass for Windows x64 GNU and Linux ARM64 GNU in both modes; native contract 12/12 plus check, provider/reference/product 153/153, package policy 6/6 plus check, capability status 7/7 plus check, runner selftest 59/59, repository-wide Zig formatting, changed JavaScript syntax, all tracked JSON parsing, and diff gates pass. The private native contract adds only `local-use-twenty-fifth-module-reexported-callable-configuration-ownership-foundation`; production reachability, native graduation, release state, and external systems are unchanged. Twenty-sixth-module configuration is the next already measured slice; twenty-seventh-and-later configuration, callable arguments/content, transitive modules, forwards/imports, `meta.load-css()`, and broader resolution remain separate.
`NSASS-011-LOCAL-USE-TWENTY-FOURTH-MODULE-REEXPORTED-CALLABLE-CONFIGURATION-OWNERSHIP-FOUNDATION` is verified at local implementation checkpoint `eb04bc207e5027ad2e58f5405b3df08a20ca1ead`. Fresh Dart Sass 1.101.0 probes accept twenty-fourth- and twenty-fifth-module configuration in SCSS and indented Sass with function invocation and mixin application inside and after the configured modules, original-owner live state, identical compressed CSS, and zero warnings. Native Zig admits only the scheduled twenty-fourth-module hop by centralizing a maximum configurable re-export depth of twenty-two: depth twenty-two may configure the twenty-fourth module, depth twenty-three remains fail-closed at the twenty-fifth, and callable-argument/content remappers retain their stricter boundaries. The prior twenty-third-module fixture remains intact, while the additive twenty-fourth-module fixture covers direct/enumerated/nested and returned values, calls and mixins inside and after the twenty-fourth module, aliases, identity, state, dependencies, maps, exact twenty-fifth-hop diagnostics, no partial CSS, limits, and every allocation failure. The test-first focused Debug gate stopped at 628/631 on the former positive rejection, allocation path, and diagnostic position. Final focused native Debug/ReleaseSafe pass 23/23 steps and 631/631 tests, and full compiler Debug/ReleaseSafe pass 60/60 steps and 1126/1126 tests. Evaluator-test-root and preprocessor-root no-emission cross-compiles pass for Windows x64 GNU and Linux ARM64 GNU in both modes; native contract 12/12 plus check, provider/reference/product 153/153, package policy 6/6 plus check, capability status 7/7 plus check, runner selftest 59/59, repository-wide Zig formatting, changed JavaScript syntax, all tracked JSON parsing, and diff gates pass. The private native contract adds only `local-use-twenty-fourth-module-reexported-callable-configuration-ownership-foundation`; production reachability, native graduation, release state, and external systems are unchanged. Twenty-fifth-module configuration is the next already measured slice; twenty-sixth-and-later configuration, callable arguments/content, transitive modules, forwards/imports, `meta.load-css()`, and broader resolution remain separate.
`NSASS-011-LOCAL-USE-TWENTY-THIRD-MODULE-REEXPORTED-CALLABLE-CONFIGURATION-OWNERSHIP-FOUNDATION` is verified at local implementation checkpoint `fe4588b07ccc8c7915d3025db221e69bd8dd3007`. Fresh Dart Sass 1.101.0 probes accept twenty-third- and twenty-fourth-module configuration in SCSS and indented Sass with function invocation and mixin application inside and after the configured modules, original-owner live state, identical compressed CSS, and zero warnings. Native Zig admits only the scheduled twenty-third-module hop by centralizing a maximum configurable re-export depth of twenty-one: depth twenty-one may configure the twenty-third module, depth twenty-two remains fail-closed at the twenty-fourth, and callable-argument/content remappers retain their stricter boundaries. The prior twenty-second-module fixture remains intact, while the additive twenty-third-module fixture covers direct/enumerated/nested and returned values, calls and mixins inside and after the twenty-third module, aliases, identity, state, dependencies, maps, exact twenty-fourth-hop diagnostics, no partial CSS, limits, and every allocation failure. The test-first focused Debug gate stopped at 627/630 on the former positive rejection, allocation path, and diagnostic position. Final focused native Debug/ReleaseSafe pass 23/23 steps and 630/630 tests, and full compiler Debug/ReleaseSafe pass 60/60 steps and 1125/1125 tests. Evaluator-test-root and preprocessor-root no-emission cross-compiles pass for Windows x64 GNU and Linux ARM64 GNU in both modes; native contract 12/12 plus check, provider/reference/product 153/153, package policy 6/6 plus check, capability status 7/7 plus check, runner selftest 59/59, repository-wide Zig formatting, changed JavaScript syntax, all tracked JSON parsing, and diff gates pass. The private native contract adds only `local-use-twenty-third-module-reexported-callable-configuration-ownership-foundation`; production reachability, native graduation, release state, and external systems are unchanged. Twenty-fourth-module configuration is the next already measured slice; twenty-fifth-and-later configuration, callable arguments/content, transitive modules, forwards/imports, `meta.load-css()`, and broader resolution remain separate.
`NSASS-011-LOCAL-USE-TWENTY-SECOND-MODULE-REEXPORTED-CALLABLE-CONFIGURATION-OWNERSHIP-FOUNDATION` is verified at local implementation checkpoint `df7d1066d4042878ad2b2b10aba5becf790735ff`. Fresh Dart Sass 1.101.0 probes accept twenty-second- and twenty-third-module configuration in SCSS and indented Sass with function invocation and mixin application inside and after the configured modules, original-owner live state, identical compressed CSS, and zero warnings. Native Zig admits only the scheduled twenty-second-module hop by centralizing a maximum configurable re-export depth of twenty: depth twenty may configure the twenty-second module, depth twenty-one remains fail-closed at the twenty-third, and callable-argument/content remappers retain their stricter boundaries. The prior twenty-first-module fixture remains intact, while the additive twenty-second-module fixture covers direct/enumerated/nested and returned values, calls and mixins inside and after the twenty-second module, aliases, identity, state, dependencies, maps, exact twenty-third-hop diagnostics, no partial CSS, limits, and every allocation failure. The test-first focused Debug gate stopped at 626/629 on the former positive rejection, allocation path, and diagnostic position. Final focused native Debug/ReleaseSafe pass 23/23 steps and 629/629 tests, and full compiler Debug/ReleaseSafe pass 60/60 steps and 1124/1124 tests. Evaluator-test-root and preprocessor-root no-emission cross-compiles pass for Windows x64 GNU and Linux ARM64 GNU in both modes; native contract 12/12 plus check, provider/reference/product 153/153, package policy 6/6 plus check, capability status 7/7 plus check, runner selftest 59/59, repository-wide Zig formatting, changed JavaScript syntax, all tracked JSON parsing, and diff gates pass. The private native contract adds only `local-use-twenty-second-module-reexported-callable-configuration-ownership-foundation`; production reachability, native graduation, release state, and external systems are unchanged. Twenty-third-module configuration is the next already measured slice; twenty-fourth-and-later configuration, callable arguments/content, transitive modules, forwards/imports, `meta.load-css()`, and broader resolution remain separate.
`NSASS-011-LOCAL-USE-TWENTY-FIRST-MODULE-REEXPORTED-CALLABLE-CONFIGURATION-OWNERSHIP-FOUNDATION` is verified at local implementation checkpoint `c9e51c083d16021497d5d8b5401237bf551dcfb3`. Fresh Dart Sass 1.101.0 probes accept twenty-first- and twenty-second-module configuration in SCSS and indented Sass with function invocation and mixin application inside and after the configured modules, original-owner live state, identical compressed CSS, and zero warnings. Native Zig admits only the scheduled twenty-first-module hop by centralizing a maximum configurable re-export depth of nineteen: depth nineteen may configure the twenty-first module, depth twenty remains fail-closed at the twenty-second, and callable-argument/content remappers retain their stricter boundaries. The prior twentieth-module fixture remains intact, while the additive twenty-first-module fixture covers direct/enumerated/nested and returned values, calls and mixins inside and after the twenty-first module, aliases, identity, state, dependencies, maps, exact twenty-second-hop diagnostics, no partial CSS, limits, and every allocation failure. The test-first focused Debug gate stopped at 625/628 on the former positive rejection, allocation path, and diagnostic position. Final focused native Debug/ReleaseSafe pass 23/23 steps and 628/628 tests, and full compiler Debug/ReleaseSafe pass 60/60 steps and 1123/1123 tests. Evaluator-test-root and preprocessor-root no-emission cross-compiles pass for Windows x64 GNU and Linux ARM64 GNU in both modes; native contract 12/12 plus check, provider/reference/product 153/153, package policy 6/6 plus check, capability status 7/7 plus check, runner selftest 59/59, repository-wide Zig formatting, changed JavaScript syntax, all tracked JSON parsing, and diff gates pass. The private native contract adds only `local-use-twenty-first-module-reexported-callable-configuration-ownership-foundation`; production reachability, native graduation, release state, and external systems are unchanged. Twenty-second-module configuration is the next already measured slice; twenty-third-and-later configuration, callable arguments/content, transitive modules, forwards/imports, `meta.load-css()`, and broader resolution remain separate.
`NSASS-011-LOCAL-USE-TWENTIETH-MODULE-REEXPORTED-CALLABLE-CONFIGURATION-OWNERSHIP-FOUNDATION` is verified at local implementation checkpoint `4388a40bfbc987b49c5659ac7bff22d5ffaa4a59`. Fresh Dart Sass 1.101.0 probes accept twentieth- and twenty-first-module configuration in SCSS and indented Sass with function invocation and mixin application inside and after the configured modules, original-owner live state, identical compressed CSS, and zero warnings. Native Zig admits only the scheduled twentieth-module hop by centralizing a maximum configurable re-export depth of eighteen: depth eighteen may configure the twentieth module, depth nineteen remains fail-closed at the twenty-first, and callable-argument/content remappers retain their stricter boundaries. The prior nineteenth-module fixture remains intact, while the additive twentieth-module fixture covers direct/enumerated/nested and returned values, calls and mixins inside and after the twentieth module, aliases, identity, state, dependencies, maps, exact twenty-first-hop diagnostics, no partial CSS, limits, and every allocation failure. The test-first focused Debug gate stopped at 624/627 on the former positive rejection, allocation path, and diagnostic position. Final focused native Debug/ReleaseSafe pass 23/23 steps and 627/627 tests, and full compiler Debug/ReleaseSafe pass 60/60 steps and 1122/1122 tests. Evaluator-test-root and preprocessor-root no-emission cross-compiles pass for Windows x64 GNU and Linux ARM64 GNU in both modes; native contract 12/12 plus check, provider/reference/product 153/153, package policy 6/6 plus check, capability status 7/7 plus check, runner selftest 59/59, repository-wide Zig formatting, changed JavaScript syntax, all tracked JSON parsing, and diff gates pass. The private native contract adds only `local-use-twentieth-module-reexported-callable-configuration-ownership-foundation`; production reachability, native graduation, release state, and external systems are unchanged. Twenty-first-module configuration is the next already measured slice; twenty-second-and-later configuration, callable arguments/content, transitive modules, forwards/imports, `meta.load-css()`, and broader resolution remain separate.
`NSASS-011-LOCAL-USE-NINETEENTH-MODULE-REEXPORTED-CALLABLE-CONFIGURATION-OWNERSHIP-FOUNDATION` is verified at local implementation checkpoint `986176d081e9afad873d2ae04c1234faba2738c3`. Fresh Dart Sass 1.101.0 probes accept nineteenth- and twentieth-module configuration in SCSS and indented Sass with function invocation and mixin application inside the terminal modules, original-owner live state, identical compressed CSS, and zero warnings. Native Zig admits only the scheduled nineteenth-module hop by centralizing a maximum configurable re-export depth of seventeen: depth seventeen may configure the nineteenth module, depth eighteen remains fail-closed at the twentieth, and callable-argument/content remappers retain their stricter boundaries. The prior eighteenth-module fixture remains intact, while the additive nineteenth-module fixture covers direct/enumerated/nested and returned values, calls and mixins inside and after the nineteenth module, aliases, identity, state, dependencies, maps, exact twentieth-hop diagnostics, no partial CSS, limits, and every allocation failure. The test-first focused Debug gate stopped at 623/626 on the former positive rejection, allocation path, and diagnostic position. Final focused native Debug/ReleaseSafe pass 23/23 steps and 626/626 tests, and full compiler Debug/ReleaseSafe pass 60/60 steps and 1121/1121 tests. Evaluator-test-root and preprocessor-root no-emission cross-compiles pass for Windows x64 GNU and Linux ARM64 GNU in both modes; native contract 12/12 plus check, provider/reference/product 153/153, package policy 6/6 plus check, capability status 7/7 plus check, runner selftest 59/59, repository-wide Zig formatting, changed JavaScript syntax, all tracked JSON parsing, and diff gates pass. The private native contract adds only `local-use-nineteenth-module-reexported-callable-configuration-ownership-foundation`; production reachability, native graduation, release state, and external systems are unchanged. Twentieth-module configuration is the next already measured slice; twenty-first-and-later configuration, callable arguments/content, transitive modules, forwards/imports, `meta.load-css()`, and broader resolution remain separate.
`NSASS-011-LOCAL-USE-EIGHTEENTH-MODULE-REEXPORTED-CALLABLE-CONFIGURATION-OWNERSHIP-FOUNDATION` is verified at local implementation checkpoint `bc8d944946acafbad9a860ceab9e424945d274cd`. Fresh Dart Sass 1.101.0 probes accept eighteenth- and nineteenth-module configuration in SCSS and indented Sass with function invocation and mixin application inside the terminal modules, original-owner live state, identical compressed CSS, and zero warnings. Native Zig admits only the scheduled eighteenth-module hop by centralizing a maximum configurable re-export depth of sixteen: depth sixteen may configure the eighteenth module, depth seventeen remains fail-closed at the nineteenth, and callable-argument/content remappers retain their stricter boundaries. The prior seventeenth-module fixture remains intact, while the additive eighteenth-module fixture covers direct/enumerated/nested and returned values, calls and mixins inside and after the eighteenth module, aliases, identity, state, dependencies, maps, exact nineteenth-hop diagnostics, no partial CSS, limits, and every allocation failure. The test-first focused Debug gate stopped at 622/625 on the former positive rejection, allocation path, and diagnostic position. Final focused native Debug/ReleaseSafe pass 23/23 steps and 625/625 tests, and full compiler Debug/ReleaseSafe pass 60/60 steps and 1120/1120 tests. Evaluator-test-root and preprocessor-root no-emission cross-compiles pass for Windows x64 GNU and Linux ARM64 GNU in both modes; native contract 12/12 plus check, provider/reference/product 153/153, package policy 6/6 plus check, capability status 7/7 plus check, runner selftest 59/59, repository-wide Zig formatting, changed JavaScript syntax, all tracked JSON parsing, and diff gates pass. The private native contract adds only `local-use-eighteenth-module-reexported-callable-configuration-ownership-foundation`; production reachability, native graduation, release state, and external systems are unchanged. Nineteenth-module configuration is the next already measured slice; twentieth-and-later configuration, callable arguments/content, transitive modules, forwards/imports, `meta.load-css()`, and broader resolution remain separate.
`NSASS-011-LOCAL-USE-SEVENTEENTH-MODULE-REEXPORTED-CALLABLE-CONFIGURATION-OWNERSHIP-FOUNDATION` is verified at local implementation checkpoint `460c130f0459b185ae84c243f8cc0776dc2b39ec`. Fresh Dart Sass 1.101.0 probes accept seventeenth- and eighteenth-module configuration in SCSS and indented Sass with invocation and application inside and after the configured modules, original-owner live state, identical compressed CSS, and zero warnings. Native Zig admits only the scheduled seventeenth-module hop by centralizing a maximum configurable re-export depth of fifteen: depth fifteen may configure the seventeenth module, depth sixteen remains fail-closed at the eighteenth, and callable-argument/content remappers retain their stricter boundaries. The prior sixteenth-module fixture remains intact, while the additive seventeenth-module fixture covers direct/enumerated/nested and returned values, calls and mixins inside and after the seventeenth module, aliases, identity, state, dependencies, maps, exact eighteenth-hop diagnostics, no partial CSS, limits, and every allocation failure. The test-first focused Debug gate stopped at 621/624 on the former positive rejection, allocation path, and diagnostic position. Final focused native Debug/ReleaseSafe pass 23/23 steps and 624/624 tests, and full compiler Debug/ReleaseSafe pass 60/60 steps and 1119/1119 tests. Evaluator-test-root and preprocessor-root no-emission cross-compiles pass for Windows x64 GNU and Linux ARM64 GNU in both modes; native contract 12/12 plus check, provider/reference/product 153/153, package policy 6/6 plus check, capability status 7/7 plus check, runner selftest 59/59, repository-wide Zig formatting, changed JavaScript syntax, all tracked JSON parsing, and diff gates pass. The private native contract adds only `local-use-seventeenth-module-reexported-callable-configuration-ownership-foundation`; production reachability, native graduation, release state, and external systems are unchanged. Eighteenth-module configuration is the next already measured slice; nineteenth-and-later configuration, callable arguments/content, transitive modules, forwards/imports, `meta.load-css()`, and broader resolution remain separate.
`NSASS-011-LOCAL-USE-SIXTEENTH-MODULE-REEXPORTED-CALLABLE-CONFIGURATION-OWNERSHIP-FOUNDATION` is verified at local implementation checkpoint `3555679d81979e5c61eeefb8c80589e63eca7ea5`. Fresh Dart Sass 1.101.0 probes accept sixteenth- and seventeenth-module configuration in SCSS and indented Sass with invocation and application inside and after the configured modules, original-owner identity and live state, identical compressed CSS, and zero warnings. Native Zig admits only the scheduled sixteenth-module hop by centralizing a maximum configurable re-export depth of fourteen: depth fourteen may configure the sixteenth module, depth fifteen remains fail-closed at the seventeenth, and callable-argument/content remappers retain their stricter boundaries. The prior fifteenth-module fixture remains intact, while the additive sixteenth-module fixture covers direct/enumerated/nested and returned values, calls and mixins inside and after the sixteenth module, aliases, identity, state, dependencies, maps, exact seventeenth-hop diagnostics, no partial CSS, limits, and every allocation failure. The test-first focused Debug gate stopped at 620/623 on the former positive rejection, allocation path, and diagnostic position. Final focused native Debug/ReleaseSafe pass 23/23 steps and 623/623 tests, and full compiler Debug/ReleaseSafe pass 60/60 steps and 1118/1118 tests. Evaluator-test-root and preprocessor-root no-emission cross-compiles pass for Windows x64 GNU and Linux ARM64 GNU in both modes; native contract 12/12 plus check, provider/reference/product 153/153, package policy 6/6 plus check, capability status 7/7 plus check, runner selftest 59/59, repository-wide Zig formatting, changed JavaScript syntax, all tracked JSON parsing, and diff gates pass. The private native contract adds only `local-use-sixteenth-module-reexported-callable-configuration-ownership-foundation`; production reachability, native graduation, release state, and external systems are unchanged. Seventeenth-module configuration is the next already measured slice; eighteenth-and-later configuration, callable arguments/content, transitive modules, forwards/imports, `meta.load-css()`, and broader resolution remain separate.
`NSASS-011-LOCAL-USE-FIFTEENTH-MODULE-REEXPORTED-CALLABLE-CONFIGURATION-OWNERSHIP-FOUNDATION` is verified at local implementation checkpoint `0bd18400fb3459c8d86b17df483a0147aba86df2`. Fresh Dart Sass 1.101.0 probes accept fifteenth- and sixteenth-module configuration in SCSS and indented Sass with invocation and application inside and after the configured modules, original-owner live state, exact compressed CSS, and zero warnings. Native Zig admits only the scheduled fifteenth-module hop by centralizing a maximum configurable re-export depth of thirteen: depth thirteen may configure the fifteenth module, depth fourteen remains fail-closed at the sixteenth, and callable-argument/content remappers retain their stricter boundaries. The prior fourteenth-module fixture remains intact, while the additive fifteenth-module fixture covers direct/enumerated/nested and returned values, calls and mixins inside and after the fifteenth module, aliases, identity, state, dependencies, maps, exact sixteenth-hop diagnostics, no partial CSS, limits, and every allocation failure. The test-first focused Debug gate stopped at 618/621 on the former positive rejection, allocation path, and diagnostic position. Final focused native Debug/ReleaseSafe pass 23/23 steps and 622/622 tests, and full compiler Debug/ReleaseSafe pass 60/60 steps and 1117/1117 tests. Evaluator-test-root and preprocessor-root no-emission cross-compiles pass for Windows x64 GNU and Linux ARM64 GNU in both modes; native contract 12/12 plus check, provider/reference/product 153/153, package policy 6/6 plus check, capability status 7/7 plus check, runner selftest 59/59, repository-wide Zig formatting, changed JavaScript syntax, all tracked JSON parsing, and diff gates pass. The private native contract adds only `local-use-fifteenth-module-reexported-callable-configuration-ownership-foundation`; production reachability, native graduation, release state, and external systems are unchanged. Sixteenth-module configuration is the next measurement slice; seventeenth-and-later configuration, callable arguments/content, transitive modules, forwards/imports, `meta.load-css()`, and broader resolution remain separate.
`NSASS-011-LOCAL-USE-FOURTEENTH-MODULE-REEXPORTED-CALLABLE-CONFIGURATION-OWNERSHIP-FOUNDATION` is verified at local implementation checkpoint `1cd606bada383c14649cf9faa8359fbefa135d1e`. Fresh Dart Sass 1.101.0 probes accept fourteenth- and fifteenth-module configuration in SCSS and indented Sass with invocation and application inside and after the configured modules, original-owner live state, exact compressed CSS, and zero warnings. Native Zig admits only the scheduled fourteenth-module hop by centralizing a maximum configurable re-export depth of twelve: depth twelve may configure the fourteenth module, depth thirteen remains fail-closed at the fifteenth, and callable-argument/content remappers retain their stricter boundaries. The strengthened fixture covers direct/enumerated/nested and returned values, calls and mixins inside and after the fourteenth module, aliases, identity, state, dependencies, maps, exact fifteenth-hop diagnostics, no partial CSS, limits, and every allocation failure. The test-first focused Debug gate stopped at 618/621 on the former positive rejection, allocation path, and diagnostic position. Final focused native Debug/ReleaseSafe pass 23/23 steps and 621/621 tests, and full compiler Debug/ReleaseSafe pass 60/60 steps and 1116/1116 tests. Evaluator-test-root and preprocessor-root no-emission cross-compiles pass for Windows x64 GNU and Linux ARM64 GNU in both modes; native contract 12/12 plus check, provider/reference/product 153/153, package policy 6/6 plus check, capability status 7/7 plus check, runner selftest 59/59, repository-wide Zig formatting, changed JavaScript syntax, all tracked JSON parsing, and diff gates pass. The private native contract adds only `local-use-fourteenth-module-reexported-callable-configuration-ownership-foundation`; production reachability, native graduation, release state, and external systems are unchanged. Fifteenth-module configuration is the next measurement slice; sixteenth-and-later configuration, callable arguments/content, transitive modules, forwards/imports, `meta.load-css()`, and broader resolution remain separate.
`NSASS-011-LOCAL-USE-THIRTEENTH-MODULE-REEXPORTED-CALLABLE-CONFIGURATION-OWNERSHIP-FOUNDATION` is verified at local implementation checkpoint `cf52f4f6e6793f17096076633f7b6278cb74169a`. Fresh Dart Sass 1.101.0 probes accept thirteenth- and fourteenth-module configuration in SCSS and indented Sass with invocation and application inside and after the configured modules, original-owner live state, exact compressed CSS, and zero warnings. Native Zig admits only the scheduled thirteenth-module hop by centralizing a maximum configurable re-export depth of eleven: depth eleven may configure the thirteenth module, depth twelve remains fail-closed at the fourteenth, and callable-argument/content remappers retain their stricter boundaries. The strengthened fixture covers direct/enumerated/nested and returned values, calls and mixins inside and after the thirteenth module, aliases, identity, state, dependencies, maps, exact fourteenth-hop diagnostics, no partial CSS, limits, and every allocation failure. The test-first focused Debug gate stopped at 618/620 on the former positive rejection and diagnostic position. Final focused native Debug/ReleaseSafe pass 23/23 steps and 620/620 tests, and full compiler Debug/ReleaseSafe pass 60/60 steps and 1115/1115 tests. Evaluator-test-root and preprocessor-root no-emission cross-compiles pass for Windows x64 GNU and Linux ARM64 GNU in both modes; native contract 12/12 plus check, provider/reference/product 153/153, package policy 6/6 plus check, capability status 7/7 plus check, runner selftest 59/59, repository-wide Zig formatting, changed JavaScript syntax, all tracked JSON parsing, and diff gates pass. The private native contract adds only `local-use-thirteenth-module-reexported-callable-configuration-ownership-foundation`; production reachability, native graduation, release state, and external systems are unchanged. Fourteenth-module configuration is the next measurement slice; fifteenth-and-later configuration, callable arguments/content, transitive modules, forwards/imports, `meta.load-css()`, and broader resolution remain separate.
`NSASS-011-LOCAL-USE-TWELFTH-MODULE-REEXPORTED-CALLABLE-CONFIGURATION-OWNERSHIP-FOUNDATION` is verified at local implementation checkpoint `314f5f3569dbd32b2d7db92ac7732124e350945a`. Fresh Dart Sass 1.101.0 probes accept twelfth- and thirteenth-module configuration in SCSS and indented Sass with invocation and application inside and after the configured modules, original-owner live state, exact compressed CSS, and zero warnings. Native Zig admits only the scheduled twelfth-module hop by centralizing a maximum configurable re-export depth of ten: depth ten may configure the twelfth module, depth eleven remains fail-closed at the thirteenth, and callable-argument/content remappers retain their stricter boundaries. The strengthened fixture covers direct/enumerated/nested and returned values, calls and mixins inside and after the twelfth module, aliases, identity, state, dependencies, maps, exact thirteenth-hop diagnostics, no partial CSS, limits, and every allocation failure. The test-first focused Debug gate stopped at 616/619 on the former positive rejection, allocation path, and twelfth-hop diagnostic position. Final focused native Debug/ReleaseSafe pass 23/23 steps and 619/619 tests, and full compiler Debug/ReleaseSafe pass 60/60 steps and 1114/1114 tests. Evaluator-test-root and preprocessor-root no-emission cross-compiles pass for Windows x64 GNU and Linux ARM64 GNU in both modes; native contract 12/12 plus check, provider/reference/product 153/153, package policy 6/6 plus check, capability status 7/7 plus check, runner selftest 59/59, repository-wide Zig formatting, changed JavaScript syntax, all tracked JSON parsing, and diff gates pass. The private native contract adds only `local-use-twelfth-module-reexported-callable-configuration-ownership-foundation`; production reachability, native graduation, release state, and external systems are unchanged. Thirteenth-module configuration is the next measurement slice; fourteenth-and-later configuration, callable arguments/content, transitive modules, forwards/imports, `meta.load-css()`, and broader resolution remain separate.
`NSASS-011-LOCAL-USE-ELEVENTH-MODULE-REEXPORTED-CALLABLE-CONFIGURATION-OWNERSHIP-FOUNDATION` is verified at local implementation checkpoint `b5d795236721139aec24de955bef593798b1c9bf`. Fresh Dart Sass 1.101.0 probes accept eleventh- and twelfth-module configuration in SCSS and indented Sass with invocation inside and after the configured modules, original-owner live state, exact compressed CSS, and zero warnings. Native Zig admits only the scheduled eleventh-module hop by centralizing a maximum configurable re-export depth of nine: depth nine may configure the eleventh module, depth ten remains fail-closed at the twelfth, and callable-argument/content remappers retain their stricter boundaries. The strengthened fixture covers direct/enumerated/nested and returned values, calls and mixins inside and after the eleventh module, aliases, identity, state, dependencies, maps, exact twelfth-hop diagnostics, no partial CSS, limits, and every allocation failure. The test-first focused Debug gate stopped at 615/618 on the former positive rejection, allocation path, and eleventh-hop diagnostic position. Final focused native Debug/ReleaseSafe pass 23/23 steps and 618/618 tests, and full compiler Debug/ReleaseSafe pass 60/60 steps and 1113/1113 tests. Evaluator-test-root and preprocessor-root no-emission cross-compiles pass for Windows x64 GNU and Linux ARM64 GNU in both modes; native contract 12/12 plus check, provider/reference/product 153/153, package policy 6/6 plus check, capability status 7/7 plus check, runner selftest 59/59, repository-wide Zig formatting, changed JavaScript syntax, all tracked JSON parsing, and diff gates pass. The private native contract adds only `local-use-eleventh-module-reexported-callable-configuration-ownership-foundation`; production reachability, native graduation, release state, and external systems are unchanged. Twelfth-module configuration is the next measurement slice; thirteenth-and-later configuration, callable arguments/content, transitive modules, forwards/imports, `meta.load-css()`, and broader resolution remain separate.
`NSASS-011-LOCAL-USE-TENTH-MODULE-REEXPORTED-CALLABLE-CONFIGURATION-OWNERSHIP-FOUNDATION` is verified at local implementation checkpoint `ae0890e589099dc53afbcc7efa42920d2473d34f`. Fresh Dart Sass 1.101.0 probes accept tenth- and eleventh-module configuration in SCSS and indented Sass with invocation inside and after the configured modules, original-owner live state, exact compressed CSS, and zero warnings. Native Zig admits only the scheduled tenth-module hop by centralizing a maximum configurable re-export depth of eight: depth eight may configure the tenth module, depth nine remains fail-closed at the eleventh, and callable-argument/content remappers retain their stricter boundaries. The strengthened fixture covers direct/enumerated/nested and returned values, calls and mixins inside and after the tenth module, aliases, identity, state, dependencies, maps, exact eleventh-hop diagnostics, no partial CSS, limits, and every allocation failure. The test-first focused Debug gate stopped at 614/617 on the former positive rejection, allocation path, and tenth-hop diagnostic position. Final focused native Debug/ReleaseSafe pass 23/23 steps and 617/617 tests, and full compiler Debug/ReleaseSafe pass 60/60 steps and 1112/1112 tests. Evaluator-test-root and preprocessor-root no-emission cross-compiles pass for Windows x64 GNU and Linux ARM64 GNU in both modes; native contract 12/12 plus check, provider/reference/product 153/153, package policy 6/6 plus check, capability status 7/7 plus check, runner selftest 59/59, repository-wide Zig formatting, changed JavaScript syntax, all tracked JSON parsing, and diff gates pass. The private native contract adds only `local-use-tenth-module-reexported-callable-configuration-ownership-foundation`; production reachability, native graduation, release state, and external systems are unchanged. Eleventh-module configuration is the next measurement slice; twelfth-and-later configuration, callable arguments/content, transitive modules, forwards/imports, `meta.load-css()`, and broader resolution remain separate.
`NSASS-011-LOCAL-USE-NINTH-MODULE-REEXPORTED-CALLABLE-CONFIGURATION-OWNERSHIP-FOUNDATION` is verified at local implementation checkpoint `9c8d8e9107c9a73f5cf7e177aad4f4ce715ca7e7`. Fresh Dart Sass 1.101.0 probes accept ninth- and tenth-module configuration in SCSS and indented Sass with invocation inside and after the configured modules, original-owner live state, exact compressed CSS, and zero warnings. Native Zig admits only the scheduled ninth-module hop by centralizing a maximum configurable re-export depth of seven: depth seven may configure the ninth module, depth eight remains fail-closed at the tenth, and callable-argument/content remappers retain their stricter boundaries. The strengthened fixture covers direct/enumerated/nested and returned values, calls and mixins inside and after the ninth module, aliases, identity, state, dependencies, maps, exact tenth-hop diagnostics, no partial CSS, limits, and every allocation failure. The test-first focused Debug gate stopped at 613/616 on the former positive rejection, allocation path, and ninth-hop diagnostic position. Final focused native Debug/ReleaseSafe pass 23/23 steps and 616/616 tests, and full compiler Debug/ReleaseSafe pass 60/60 steps and 1111/1111 tests. Evaluator-test-root and preprocessor-root no-emission cross-compiles pass for Windows x64 GNU and Linux ARM64 GNU in both modes; native contract 12/12 plus check, provider/reference/product 153/153, package policy 6/6 plus check, capability status 7/7 plus check, runner selftest 59/59, repository-wide Zig formatting, changed JavaScript syntax, all tracked JSON parsing, and diff gates pass. The private native contract adds only `local-use-ninth-module-reexported-callable-configuration-ownership-foundation`; production reachability, native graduation, release state, and external systems are unchanged. Tenth-module configuration is the next measurement slice; eleventh-and-later configuration, callable arguments/content, transitive modules, forwards/imports, `meta.load-css()`, and broader resolution remain separate.
`NSASS-011-LOCAL-USE-EIGHTH-MODULE-REEXPORTED-CALLABLE-CONFIGURATION-OWNERSHIP-FOUNDATION` is verified at local implementation checkpoint `be22a893da17a670d77a2823c172bd1ef28d3848`. A fresh Dart Sass 1.101.0 probe accepts eighth-module configuration in SCSS and indented Sass with invocation inside and after the configured module, original-owner identity and live state, exact compressed CSS, and zero warnings. Native Zig admits only the scheduled eighth-module hop by centralizing a maximum configurable re-export depth of six: depth six may configure the eighth module, depth seven remains fail-closed at the ninth, and callable-argument/content remappers retain their stricter boundaries. The strengthened fixture covers direct/enumerated/nested and returned values, calls and mixins inside and after the eighth module, aliases, identity, state, dependencies, maps, exact ninth-hop diagnostics, no partial CSS, limits, and every allocation failure. The test-first focused Debug gate stopped at 612/615 on the former positive rejection, allocation path, and eighth-hop diagnostic position. Final focused native Debug/ReleaseSafe pass 23/23 steps and 615/615 tests, and full compiler Debug/ReleaseSafe pass 60/60 steps and 1110/1110 tests. Evaluator-test-root and preprocessor-root no-emission cross-compiles pass for Windows x64 GNU and Linux ARM64 GNU in both modes; native contract 12/12 plus check, provider/reference/product 153/153, package policy 6/6 plus check, capability status 7/7 plus check, runner selftest 59/59, repository-wide Zig formatting, changed JavaScript syntax, all tracked JSON parsing, and diff gates pass. The private native contract adds only `local-use-eighth-module-reexported-callable-configuration-ownership-foundation`; production reachability, native graduation, release state, and external systems are unchanged. Ninth-module configuration is the next measurement slice; tenth-and-later configuration, callable arguments/content, transitive modules, forwards/imports, `meta.load-css()`, and broader resolution remain separate.
`NSASS-011-LOCAL-USE-SEVENTH-MODULE-REEXPORTED-CALLABLE-CONFIGURATION-OWNERSHIP-FOUNDATION` is verified at local implementation checkpoint `b5c214a929f11ae70db12054bb5c0ba0f6873da7`. A fresh Dart Sass 1.101.0 probe accepts seventh- and eighth-module configuration in SCSS and indented Sass with invocation inside and after the configured modules, original-owner identity and live state, exact compressed CSS, and zero warnings. Native Zig admits only the scheduled seventh-module hop by centralizing a maximum configurable re-export depth of five: depth five may configure the seventh module, depth six remains fail-closed at the eighth, and callable-argument/content remappers retain their stricter boundaries. The strengthened fixture covers direct/enumerated/nested and returned values, calls and mixins inside and after the seventh module, aliases, identity, state, dependencies, maps, exact eighth-hop diagnostics, no partial CSS, limits, and every allocation failure. The test-first focused Debug gate stopped at 611/614 on the former positive rejection, allocation path, and seventh-hop diagnostic position. Final focused native Debug/ReleaseSafe pass 23/23 steps and 614/614 tests, and full compiler Debug/ReleaseSafe pass 60/60 steps and 1109/1109 tests. Evaluator-test-root and preprocessor-root no-emission cross-compiles pass for Windows x64 GNU and Linux ARM64 GNU in both modes; native contract 12/12 plus check, provider/reference/product 153/153, package policy 6/6 plus check, capability status 7/7 plus check, runner selftest 59/59, repository-wide Zig formatting, changed JavaScript syntax, all tracked JSON parsing, and diff gates pass. The private native contract adds only `local-use-seventh-module-reexported-callable-configuration-ownership-foundation`; production reachability, native graduation, release state, and external systems are unchanged. Eighth-module configuration is the next measured slice; ninth-and-later configuration, callable arguments/content, transitive modules, forwards/imports, `meta.load-css()`, and broader resolution remain separate.
`NSASS-011-LOCAL-USE-SIXTH-MODULE-REEXPORTED-CALLABLE-CONFIGURATION-OWNERSHIP-FOUNDATION` is verified at local implementation checkpoint `6d7e4c21f9f0950f3bce3f81069d1ed02e3b718f`. A fresh Dart Sass 1.101.0 probe accepts sixth- and seventh-module configuration in SCSS and indented Sass with invocation inside and after the configured modules, original-owner identity and live state, exact compressed CSS, and zero warnings. Native Zig admits only the scheduled sixth-module hop by centralizing a maximum configurable re-export depth of four: depth four may configure the sixth module, depth five remains fail-closed at the seventh, and callable-argument/content remappers retain their stricter boundaries. The strengthened fixture covers direct/enumerated/nested and returned values, calls and mixins inside and after the sixth module, aliases, identity, state, dependencies, maps, exact seventh-hop diagnostics, no partial CSS, limits, and every allocation failure. The test-first focused Debug gate stopped at 610/613 on the former positive rejection, allocation path, and sixth-hop diagnostic position. Final focused native Debug/ReleaseSafe pass 23/23 steps and 613/613 tests, and full compiler Debug/ReleaseSafe pass 60/60 steps and 1108/1108 tests. Evaluator-test-root and preprocessor-root no-emission cross-compiles pass for Windows x64 GNU and Linux ARM64 GNU in both modes; native contract 12/12 plus check, provider/reference/product 153/153, package policy 6/6 plus check, capability status 7/7 plus check, runner selftest 59/59, repository-wide Zig formatting, changed JavaScript syntax, all tracked JSON parsing, and diff gates pass. The private native contract adds only `local-use-sixth-module-reexported-callable-configuration-ownership-foundation`; production reachability, native graduation, release state, and external systems are unchanged. Seventh-module configuration is the next measured slice; eighth-and-later configuration, callable arguments/content, transitive modules, forwards/imports, `meta.load-css()`, and broader resolution remain separate.
`NSASS-011-LOCAL-USE-FIFTH-MODULE-REEXPORTED-CALLABLE-CONFIGURATION-OWNERSHIP-FOUNDATION` is verified at local implementation checkpoint `fa83d74af2e35e5c2fa83b5d9fa9e3bcc5b0b35d`. A fresh Dart Sass 1.101.0 probe accepts fifth- and sixth-module configuration in SCSS and indented Sass with invocation inside and after the configured modules, original-owner identity and live state, exact compressed CSS, and zero warnings. Native Zig admits only the scheduled fifth-module hop by centralizing a maximum configurable re-export depth of three: depth three may configure the fifth module, depth four remains fail-closed at the sixth, and callable-argument/content remappers retain their stricter boundaries. The strengthened fixture covers direct/enumerated/nested and returned values, calls and mixins inside and after the fifth module, aliases, identity, state, dependencies, maps, exact sixth-hop diagnostics, no partial CSS, limits, and every allocation failure. The test-first focused Debug gate stopped at 609/612 on the former positive rejection, allocation path, and fifth-hop diagnostic position. Final focused native Debug/ReleaseSafe pass 23/23 steps and 612/612 tests, and full compiler Debug/ReleaseSafe pass 60/60 steps and 1107/1107 tests. Evaluator-test-root and preprocessor-root no-emission cross-compiles pass for Windows x64 GNU and Linux ARM64 GNU in both modes; native contract 12/12 plus check, provider/reference/product 153/153, package policy 6/6 plus check, capability status 7/7 plus check, runner selftest 59/59, repository-wide Zig formatting, changed JavaScript syntax, all tracked JSON parsing, and diff gates pass. The private native contract adds only `local-use-fifth-module-reexported-callable-configuration-ownership-foundation`; production reachability, native graduation, release state, and external systems are unchanged. Sixth-module configuration is the next measured slice; seventh-and-later configuration, callable arguments/content, transitive modules, forwards/imports, `meta.load-css()`, and broader resolution remain separate.
`NSASS-011-LOCAL-USE-RECURSIVE-REEXPORTED-CALLABLE-CONFIGURATION-OWNERSHIP-FOUNDATION` is verified at local implementation checkpoint `f174916fb2c516e454148200ec20ca8575da0cd8`. A fresh Dart Sass 1.101.0 probe accepts recursive fourth- and fifth-module configuration in SCSS and indented Sass with canonical aliases, invocation/application, original-owner identity and live state, exact dependency order and compressed CSS, and zero warnings. Native Zig admits only the scheduled fourth-module hop by centralizing a maximum configurable re-export depth of two: depth two may configure the fourth module, depth three remains fail-closed at the fifth, and callable-argument/content remappers retain their stricter boundaries. The strengthened fixture covers direct/enumerated/nested and returned values, calls and mixins inside and after the fourth module, aliases, identity, state, dependencies, maps, exact fifth-hop diagnostics, no partial CSS, limits, and every allocation failure. The test-first focused Debug gate stopped at 608/611 on the former positive rejection, allocation path, and fourth-hop diagnostic position. Final focused native Debug/ReleaseSafe pass 23/23 steps and 611/611 tests, and full compiler Debug/ReleaseSafe pass 60/60 steps and 1106/1106 tests. Evaluator-test-root and preprocessor-root no-emission cross-compiles pass for Windows x64 GNU and Linux ARM64 GNU in both modes; native contract 12/12 plus check, provider/reference/product 153/153, package policy 6/6 plus check, capability status 7/7 plus check, runner selftest 59/59, repository-wide Zig formatting, changed JavaScript syntax, all tracked JSON parsing, and diff gates pass. The private native contract adds only `local-use-recursive-reexported-callable-configuration-ownership-foundation`; production reachability, native graduation, release state, and external systems are unchanged. Fifth-module configuration is the next measured slice; sixth-and-later configuration, callable arguments/content, transitive modules, forwards/imports, `meta.load-css()`, and broader resolution remain separate.
`NSASS-011-LOCAL-USE-REEXPORTED-CALLABLE-CONFIGURATION-OWNERSHIP-FOUNDATION` is verified at local implementation checkpoint `7eaae26499cdcec71689e77283ff7af202bb9669`. Dart Sass 1.101.0 probes establish third-sibling configuration from re-exported function/mixin identities across direct/enumerated/nested values, canonical aliases, invocation/application inside and after the configured module, original-owner identity and live state, dependency order, zero warnings, and SCSS/indented-Sass parity; separate bounded probes confirm that the provider accepts the configuration chain through fourth and fifth modules. Native Zig records bounded non-semantic re-export depth while preserving the exact packed original owner identity: depth one may configure the third module, depth two remains fail-closed at the fourth module, and argument/content remappers retain their stricter boundaries. The test-first focused gate stopped at 609/611 on the former positive rejection and moved recursive diagnostic; exact no-partial-CSS failure and exhaustive allocation coverage now pass. Final focused native Debug/ReleaseSafe pass 23/23 steps and 611/611 tests, and full compiler Debug/ReleaseSafe pass 60/60 steps and 1106/1106 tests. Evaluator-root and preprocessor-root no-emission cross-compiles pass for Windows x64 GNU and Linux ARM64 GNU in both modes; native contract 12/12 plus check, provider/reference/product 153/153, package policy 6/6 plus check, capability status 7/7 plus check, runner selftest 59/59, repository-wide Zig formatting, changed JavaScript syntax, all tracked JSON parsing, and diff gates pass. The private native contract adds only `local-use-reexported-callable-configuration-ownership-foundation`; production reachability, native graduation, release state, and external systems are unchanged. Recursive fourth-module configuration is the next measured slice; fifth-and-later configuration, callable arguments/content, transitive modules, forwards/imports, `meta.load-css()`, and broader resolution remain separate.

`NSASS-011-LOCAL-USE-CONFIGURED-CALLABLE-REEXPORT-OWNERSHIP-FOUNDATION` is verified at local implementation checkpoint `5b1a29f2c079c3e3470da5d62f6c96d82778c2d9`. Dart Sass 1.101.0 probes establish direct/enumerated/nested configured-callable re-exports, module results, canonical aliases and identity, invocation/application after return, live owner state, zero warnings, dependency order, and SCSS/indented-Sass parity. Native Zig deep-owns the aggregate into the parent, preserves exact packed owner identity, and records re-export provenance that is invisible to Sass equality but prevents unmeasured configuration, argument, and `@content` chaining. The test-first gate stopped at the former recursive re-export rejection; exact terminal chaining diagnostics and exhaustive allocation coverage now pass. Final focused native Debug/ReleaseSafe pass 23/23 steps and 610/610 tests, and full compiler Debug/ReleaseSafe pass 60/60 steps and 1105/1105 tests. Evaluator-root and preprocessor-root no-emission cross-compiles pass for Windows x64 GNU and Linux ARM64 GNU in both modes; native contract 12/12 plus check, provider/reference/product 153/153, package policy 6/6 plus check, capability status 7/7 plus check, runner selftest 59/59, repository-wide Zig formatting, changed JavaScript syntax, all tracked JSON parsing, and diff gates pass. The private native contract adds only `local-use-configured-callable-reexport-ownership-foundation`; production reachability, native graduation, release state, and external systems are unchanged. Re-exported-callable configuration of a third sibling is the next measured slice; recursive fourth-module configuration, callable arguments/content, transitive modules, forwards/imports, `meta.load-css()`, and broader resolution remain separate.

`NSASS-011-LOCAL-USE-CROSS-MODULE-CALLABLE-CONFIGURATION-OWNERSHIP-FOUNDATION` is verified at local implementation checkpoint `1814902701e8e735f71dabc32cb61aa8fa9ca283`. Dart Sass 1.101.0 probes establish exact-parent sibling function/mixin references across one-hop module configuration, nested aggregates, target-internal and caller-triggered invocation/application, canonical aliases, shared owner state, dependency order, zero warnings, and SCSS/indented-Sass parity. Native Zig admits only packed callable identities owned by an already retained earlier sibling in the configured evaluator's exact parent, deep-owns aggregate configuration into the target, and resolves invocation, application, inspection, and content acceptance through that retained owner. Raw caller, other-parent, later/nonexistent-owner, and recursively re-exported identities remain fail-closed. The initial focused gate stopped at the former blanket local-callable rejection; exhaustive allocation coverage then exposed and closed a partial `cloneEdges()` cleanup defect. Final focused native Debug/ReleaseSafe pass 23/23 steps and 608/608 tests, and full compiler Debug/ReleaseSafe pass 60/60 steps and 1103/1103 tests. Evaluator-root and preprocessor-root no-emission cross-compiles pass for Windows x64 GNU and Linux ARM64 GNU in both modes; native contract 12/12 plus check, provider/reference/product 153/153, package policy 6/6 plus check, capability status 7/7 plus check, runner selftest 59/59, repository-wide Zig formatting, changed JavaScript syntax, all tracked JSON parsing, and diff gates pass. The private native contract adds only `local-use-cross-module-callable-configuration-ownership-foundation`; production reachability, native graduation, release state, and external systems are unchanged. One-hop configured-callable re-export ownership is the next measured slice; third-module configuration, transitive modules, forwards/imports, `meta.load-css()`, and broader resolution remain separate.

`NSASS-011-LOCAL-USE-BUILTIN-CALLABLE-CONFIGURATION-OWNERSHIP-FOUNDATION` is verified at local implementation checkpoint `b2a035d0b6f2d7f1e41ab6552c33507a8fb324ea`. Dart Sass 1.101.0 probes establish globally stable built-in function/mixin references across one-hop module configuration, nested aggregates, direct/reflected use, canonical aliases, warnings, and SCSS/indented-Sass parity. Native Zig recursively admits only built-in callable kinds and deep-owns their enclosing values into the retained module store; user and local-module callable identities remain fail-closed. The initial focused Debug gate stopped at 604/607 on the former blanket rejection plus the exact earlier caller-user placement classification. Final focused native Debug/ReleaseSafe pass 23/23 steps and 607/607 tests, and full compiler Debug/ReleaseSafe pass 60/60 steps and 1102/1102 tests. Evaluator-root and preprocessor-root no-emission cross-compiles pass for Windows x64 GNU and Linux ARM64 GNU in both modes; native contract 12/12 plus check, provider/reference/product 153/153, package policy 6/6 plus check, capability status 7/7 plus check, runner selftest 59/59, repository-wide Zig formatting, changed JavaScript syntax, all tracked JSON parsing, and diff gates pass. The private native contract adds only `local-use-built-in-callable-configuration-ownership-foundation`; production reachability, native graduation, release state, and external systems are unchanged. Exact-parent cross-module user-callable configuration ownership is the next measured slice; raw caller callables, recursive re-exports, transitive modules, forwards/imports, `meta.load-css()`, and broader resolution remain separate.

`NSASS-011-LOCAL-USE-CONFIGURATION-FOUNDATION` is verified at local implementation checkpoint `6c543caf827ac6b749f1d960ad7503f3bc3b3bb8`. Dart Sass 1.101.0 probes establish public `!default` configuration, caller evaluation, null/default behavior, canonical aliases, first-load order, one-time/repeated configuration, strict invalid forms, and SCSS/indented-Sass parity. Native Zig evaluates bounded named configuration values in caller source order, rejects callable-bearing and unsafe forms, validates public top-level `!default` declarations, deep-owns values into the retained module store, and applies them before one complete module evaluation. The initial focused Debug gate stopped at 603/606 on the absent syntax and semantics. Final focused native Debug/ReleaseSafe pass 23/23 steps and 606/606 tests, and full compiler Debug/ReleaseSafe pass 60/60 steps and 1101/1101 tests. Evaluator-root and preprocessor-root no-emission cross-compiles pass for Windows x64 GNU and Linux ARM64 GNU in both modes; native contract 12/12 plus check, provider/reference/product 153/153, package policy 6/6 plus check, capability status 7/7 plus check, runner selftest 59/59, repository-wide Zig formatting, changed JavaScript syntax, all tracked JSON parsing, and diff gates pass. The private native contract adds only `local-use-configuration-foundation`; production reachability, native graduation, release state, and external systems are unchanged. Globally owned built-in callable configuration is the next measured ownership slice; all cross-module callable identities, transitive modules, forwards/imports, `meta.load-css()`, and broader resolution remain separate.

`NSASS-011-LOCAL-USE-CALLABLE-RESULT-OWNERSHIP-FOUNDATION` is verified at local implementation checkpoint `05efba1fc0030c6494d7249f53d2b5f688cf4c2d`. Fresh Dart Sass 1.101.0 probes establish one-hop local-module results containing module-local and built-in function/mixin references, nested lists/maps, canonical aliases, direct and reflected calls, safe argument passthrough, invocation/application after return, SCSS and indented Sass, exact compressed CSS, and zero warnings. Native Zig recursively deep-owns those results into the caller store while translating retained-module user callable identities to stable packed module/callable owners and preserving built-in identities. Already-packed local-module identities remain an explicit recursive re-export boundary; an unmappable owner fails with one exact diagnostic and no partial CSS. The initial focused Debug regression stopped at the former blanket callable-result rejection. Final focused native Debug/ReleaseSafe pass 23/23 steps and 603/603 tests, and full compiler Debug/ReleaseSafe pass 60/60 steps and 1098/1098 tests. Evaluator-root and preprocessor-root no-emission cross-compiles pass for Windows x64 GNU and Linux ARM64 GNU in both modes; native contract 12/12 plus check, provider/reference/product 153/153, package policy 6/6 plus check, runner selftest 59/59, repository-wide Zig formatting, changed JavaScript syntax, all tracked JSON parsing, and diff gates pass. The private native contract adds only `local-use-callable-result-ownership-foundation`; production reachability, native graduation, release state, and external systems are unchanged. One-hop local module configuration is the next measured dependency slice; recursive callable re-exports, forwards/imports, dynamic/transitive loading, `meta.load-css()`, and broader resolution remain separate.

`NSASS-011-LOCAL-USE-CALLABLE-ARGUMENT-OWNERSHIP-FOUNDATION` is verified at local implementation checkpoint `baf420c03e3a0245e8ae12a7edf92041b5c64ee4`. Fresh Dart Sass 1.101.0 probes establish one-hop callable-valued module arguments across target-module local references, built-in references, caller-owned and other-module references, nested lists/maps, positional/named/map-splat binding, direct/reflected functions and mixins, module-to-caller content round trips, SCSS and indented Sass, exact compressed CSS, and zero warnings. Native Zig admits only identities with an unambiguous owner: built-ins remain global, exact target-module packed identities recursively map into that retained evaluator, and content arguments map the retained evaluator's user identities back to the exact parent module identity. Caller-owned and other-module values remain fail-closed with one exact diagnostic and no partial CSS, while callable-valued results remain separately rejected. The initial focused Debug gate stopped at 600/601 on the former blanket rejection. Final focused native Debug/ReleaseSafe pass 23/23 steps and 602/602 tests, and full compiler Debug/ReleaseSafe pass 60/60 steps and 1097/1097 tests. Evaluator-root and preprocessor-root no-emission cross-compiles pass for Windows x64 GNU and Linux ARM64 GNU in both modes; native contract 12/12 plus check, provider/reference/product 153/153, package policy 6/6 plus check, runner selftest 59/59, repository-wide Zig formatting, changed JavaScript syntax, all tracked JSON parsing, and diff gates pass. The private native contract adds only `local-use-callable-argument-ownership-foundation`; production reachability, native graduation, release state, and external systems are unchanged. Callable-valued module-result ownership is the next measured dependency slice; forwards/imports, dynamic/transitive/configured loading, `meta.load-css()`, and broader resolution remain separate.

`NSASS-011-LOCAL-USE-CALLABLE-VARIABLE-OWNERSHIP-FOUNDATION` is verified at local implementation checkpoint `fa3bbedd6be0ef3aea1b6943187c687f74b6bf34`. Fresh Dart Sass 1.101.0 probes establish one-hop callable-valued module variables containing local and built-in function/mixin references through direct qualified reads and `meta.module-variables()`, nested lists/maps, canonical aliases, reassignment snapshots, default/custom namespaces, invocation/application, type/inspection/equality, SCSS and indented Sass, exact compressed CSS, and zero warnings. Native Zig recursively deep-owns those values into the caller store while translating retained-module user function/mixin identities to canonical packed module/callable owners and preserving built-in identities. Aliases compare equal, nested references remain invocable/applicable, private members remain absent, and enumerated maps remain immutable snapshots after later variable reassignment. Built-in-only `sass:*` use inside a retained local module is closed over the existing supported set; local transitive modules, already-translated re-exports, and caller-to-module callable arguments/results stay fail-closed. The initial focused Debug gate stopped at 599/600 on the former local-module built-in-use boundary. Final focused native Debug/ReleaseSafe pass 23/23 steps and 600/600 tests, and full compiler Debug/ReleaseSafe pass 60/60 steps and 1095/1095 tests. Evaluator-root and preprocessor-root no-emission cross-compiles pass for Windows x64 GNU and Linux ARM64 GNU in both modes; native contract 12/12 plus check, provider/reference/product 153/153, package policy 6/6 plus check, runner selftest 59/59, repository-wide Zig formatting, changed JavaScript syntax, tracked JSON parsing, and diff gates pass. The private native contract adds only `local-use-callable-variable-ownership-foundation`; production reachability, native graduation, release state, and external systems are unchanged. Callable-valued module-argument ownership is the next measured dependency slice; callable results, forwards/imports, dynamic/transitive/configured loading, `meta.load-css()`, and broader resolution remain separate.

`NSASS-011-LOCAL-USE-VARIABLE-ENUMERATION-FOUNDATION` is verified at local implementation checkpoint `5aa506e1037251e92be601992aec67dfeabb1e59`. Fresh Dart Sass 1.101.0 probes establish one-hop local-module variable enumeration through `meta.module-variables()` for public/private variables, final reassignment, canonical first-definition order, underscore/hyphen normalization, default/custom namespaces, canonical aliases, direct/reflected invocation, SCSS and indented Sass, nested non-callable values, immutable snapshots across later module mutation, exact compressed CSS, zero warnings, and exact unknown namespaces. Native Zig resolves only an already-loaded local target, walks its existing public variable inventory in provider order, reads final live values from the retained module environment, and deep-owns the completed map in the caller value store without module mutation or cross-arena pointer retention. Earlier maps remain unchanged after a module function mutates global state, later maps observe the new value, private members remain absent, and callable-bearing values retain the explicit unsupported boundary. Exact direct/reflected unknown-namespace cases expose no CSS; the direct diagnostic owns the full `meta.module-variables()` call span, generated position remains zero, and finish remains failed. Argument/collection/temporary/operation/callable limits and every allocation failure are terminal. The initial test-first focused Debug gate stopped at 596/599 on the former `UnsupportedFeature` boundary across the positive, collection-limit, and allocation paths. Final focused native Debug/ReleaseSafe pass 23/23 steps and 599/599 tests, and full compiler Debug/ReleaseSafe pass 60/60 steps and 1094/1094 tests. Evaluator-root and preprocessor-root no-emission cross-compiles pass for Windows x64 GNU and Linux ARM64 GNU in both modes; native contract 12/12 plus check, provider/reference/product 153/153, product replay 30/30, package policy 6/6 plus check, runner selftest 59/59, repository-wide Zig formatting, changed JavaScript syntax, tracked JSON parsing, and diff gates pass. The private native contract adds only `local-use-variable-enumeration-foundation`; production reachability, native graduation, release state, and external systems are unchanged. Callable-valued module-variable ownership is the next measured dependency slice; forwards/imports, dynamic/transitive/configured loading, `meta.load-css()`, and broader resolution remain separate.

`NSASS-011-LOCAL-USE-MIXIN-ENUMERATION-FOUNDATION` is verified at local implementation checkpoint `8ae034c595bf8ceffec1ba93ee7b8d67e051851e`. Fresh Dart Sass 1.101.0 probes establish one-hop local-module mixin enumeration through `meta.module-mixins()` for public/private mixins, final redefinitions, canonical first-definition order, underscore/hyphen normalization, contentful/contentless references, default/custom namespaces, canonical aliases, direct/reflected invocation, SCSS and indented Sass, exact compressed CSS, zero warnings, and exact unknown namespaces. Native Zig resolves only an already-loaded local target, enumerates public root mixins in provider order, binds each retained key to the final module-owned callable identity, and deep-owns the completed map in the caller value store without callable cloning, mixin execution, or module mutation. Canonical aliases produce equal maps, private mixins remain absent, and returned references retain exact content acceptance and invoke through the verified cross-evaluator path. Exact direct/reflected unknown-namespace cases expose no CSS; the direct diagnostic owns the full `meta.module-mixins()` call span, generated position remains zero, and finish remains failed. Argument/collection/temporary/operation/callable limits and every allocation failure are terminal. The initial test-first focused Debug gate stopped at 594/597 on the former `UnsupportedFeature` boundary. The first implementation replay reached 596/597 and showed only that a one-mixin fixture correctly fit a one-item collection limit; the fixture was strengthened to two public mixins without weakening the limit. Final focused native Debug/ReleaseSafe pass 23/23 steps and 597/597 tests, and full compiler Debug/ReleaseSafe pass 60/60 steps and 1092/1092 tests. Preprocessor-root no-emission cross-compiles pass for Windows x64 GNU and Linux ARM64 GNU in both modes; native contract 12/12 plus check, provider/reference/product 153/153, product replay 30/30, package policy 6/6 plus check, runner selftest 59/59, repository-wide Zig formatting, changed JavaScript syntax, tracked JSON parsing, and diff gates pass. The private native contract adds only `local-use-mixin-enumeration-foundation`; production reachability, native graduation, release state, and external systems are unchanged. Local module variable enumeration through `meta.module-variables()` is the next measured dependency slice; callable-valued cross-arena data, imports/forwards, dynamic/transitive/configured loading, and broader resolution remain separate.

`NSASS-011-LOCAL-USE-FUNCTION-ENUMERATION-FOUNDATION` is verified at local implementation checkpoint `42899a39d2abfe463157302f22b0cc947112bc25`. Fresh Dart Sass 1.101.0 probes establish one-hop local-module function enumeration through `meta.module-functions()` for public/private functions, final redefinitions, canonical first-definition order, underscore/hyphen normalization, default/custom namespaces, canonical aliases, direct/reflected invocation, SCSS and indented Sass, exact compressed CSS, zero warnings, and exact unknown namespaces. Native Zig resolves only an already-loaded local target, enumerates public root functions in provider order, binds each retained key to the final module-owned callable identity, and deep-owns the completed map in the caller value store without callable cloning, function execution, or module mutation. Canonical aliases produce equal maps, returned references invoke through the verified cross-evaluator path, and private functions remain absent. Exact direct/reflected unknown-namespace cases expose no CSS; the direct diagnostic owns the full `meta.module-functions()` call span, generated position remains zero, and finish remains failed. Argument/collection/temporary/operation/callable limits and every allocation failure are terminal. The initial test-first focused Debug gate stopped at 592/595 on the former `UnsupportedFeature` boundary. The first implementation replay reached 594/595 and showed only that a one-function fixture correctly fit a one-item collection limit; the fixture was strengthened to two public functions without weakening the limit. Final focused native Debug/ReleaseSafe pass 23/23 steps and 595/595 tests, and full compiler Debug/ReleaseSafe pass 60/60 steps and 1090/1090 tests. Preprocessor-root no-emission cross-compiles pass for Windows x64 GNU and Linux ARM64 GNU in both modes; native contract 12/12 plus check, provider/reference/product 153/153, product replay 30/30, package policy 6/6 plus check, runner selftest 59/59, repository-wide Zig formatting, changed JavaScript syntax, tracked JSON parsing, and diff gates pass. The private native contract adds only `local-use-function-enumeration-foundation`; production reachability, native graduation, release state, and external systems are unchanged. Local module mixin enumeration through `meta.module-mixins()` is the next measured dependency slice; variable reflection, callable-valued cross-arena data beyond the returned function map, imports/forwards, dynamic/transitive/configured loading, and broader resolution remain separate.

`NSASS-011-LOCAL-USE-MIXIN-CONTENT-INSPECTION-FOUNDATION` is verified at local implementation checkpoint `9a63d80ad7a20f490b25fa8c49a977e241c2e3f0`. Fresh Dart Sass 1.101.0 probes establish one-hop local-module mixin content inspection through `meta.accepts-content()` for public contentful and contentless mixins, last-definition replacement in both directions, default/custom/unprefixed namespaces, canonical aliases, direct and reflected invocation, SCSS and indented Sass, exact compressed CSS, zero warnings, and private/missing/unknown/ambiguous/non-mixin failures. Native Zig decodes the existing local mixin-reference identity, validates its canonical module and final callable indices, and reads the retained module evaluator's immutable `accepts_content` fact without cloning a callable across arenas, evaluating module code, or mutating state. Exact ambiguous lookup owns the inner `meta.get-mixin()` diagnostic span and exposes no CSS; argument limits and every direct inspection allocation failure remain terminal. The initial test-first focused Debug gate stopped at 591/593 on the former `UnsupportedFeature` boundary. The first implementation replay reached 592/593 and isolated the pre-existing injected-allocation reclassification for reflected built-in reference lookup; nested and pre-bound variants both reproduced it, so the allocation fixture was narrowed to the package-owned direct branch while reflected behavior remains covered by positive tests. Final focused native Debug/ReleaseSafe pass 23/23 steps and 593/593 tests, and full compiler Debug/ReleaseSafe pass 60/60 steps and 1088/1088 tests. Preprocessor-root no-emission cross-compiles pass for Windows x64 GNU and Linux ARM64 GNU in both modes; native contract 12/12 plus check, provider/reference/product 153/153, product replay 30/30, package policy 6/6 plus check, runner selftest 59/59, repository-wide Zig formatting, changed JavaScript syntax, tracked JSON parsing, and diff gates pass. The private native contract adds only `local-use-mixin-content-inspection-foundation`; production reachability, native graduation, release state, and external systems are unchanged. Local module function enumeration through `meta.module-functions()` is the next measured dependency slice; mixin/variable enumeration, callable-valued cross-arena data beyond that exact map, imports/forwards, dynamic/transitive/configured loading, and broader resolution remain separate.

`NSASS-011-LOCAL-USE-MIXIN-REFERENCE-APPLICATION-FOUNDATION` is verified at local implementation checkpoint `e0763bd91bcfe396a2d454a1e75f3e570cd206c2`. Fresh Dart Sass 1.101.0 probes establish one-hop local-module mixin-reference application through `meta.apply()` for default, custom, and unprefixed namespaces; canonical aliases; last-definition replacement; positional/named/list/map splats; later-map override; mutable module state and lexical defaults; caller-owned parameterized `@content`; SCSS and indented Sass; exact compressed CSS; zero warnings; and exact missing, extra, unknown, duplicate, non-mixin, and unexpected-content errors. Native Zig now decodes local mixin-reference identities and shares the direct local-include bounded cross-evaluator path: once-evaluated caller values are deep-cloned into the retained module engine, the module-owned binder and evaluator run against live lexical state, and caller content executes in the caller document and scope. Callable-valued arguments, invalid binding, expanded limits, transaction failure, and every allocation failure expose no partial CSS. The initial test-first focused Debug gate stopped at 587/591; the first implementation replay reached 589/591 and isolated the unchanged rest-argument serialization boundary plus generic function wording in the shared binder, so the fixture was narrowed to fixed arity and the binder was contextualized without broadening either feature surface. Final focused native Debug/ReleaseSafe pass 23/23 steps and 591/591 tests, and full compiler Debug/ReleaseSafe pass 60/60 steps and 1086/1086 tests. Preprocessor-root no-emission cross-compiles pass for Windows x64 GNU and Linux ARM64 GNU in both modes; native contract 12/12 plus check, provider/reference/product 153/153, product replay 30/30, package policy 6/6 plus check, runner selftest 59/59, repository-wide Zig formatting, changed JavaScript syntax, tracked JSON parsing, and diff gates pass. The private native contract adds only `local-use-mixin-reference-application-foundation`; production reachability, native graduation, release state, and external systems are unchanged. Local mixin-reference content inspection through `meta.accepts-content()` is the next measured dependency slice; enumeration, callable-valued cross-arena data, imports/forwards, dynamic/transitive/configured loading, and broader resolution remain separate.

`NSASS-011-LOCAL-USE-FUNCTION-REFERENCE-INVOCATION-FOUNDATION` is verified at local implementation checkpoint `ad4780ebb8e2cedf4a575fdabb1999d434d03fb5`. Fresh Dart Sass 1.101.0 probes establish one-hop local-module function-reference invocation through `meta.call()` for default, custom, and unprefixed namespaces; canonical aliases; last-definition replacement; positional/named/list/map/mixed splats; later-map override; mutable module state and lexical defaults; nested aggregate return ownership; SCSS and indented Sass; exact compressed CSS; zero warnings; and exact missing, extra, unknown, duplicate, non-function, and mixin-as-function errors. Native Zig now decodes local function-reference identities and shares the direct local-call bounded cross-evaluator path: caller values are deep-cloned into the retained module engine, the module-owned binder and evaluator run against live lexical state, callable-bearing results fail closed, and ordinary results are deep-cloned back into caller ownership. Callable-valued arguments/results, invalid binding, expanded limits, transaction failure, and every allocation failure expose no partial CSS. The initial test-first focused Debug gate stopped at 584/588; two fixtures initially reached the unchanged transitive-module guard and were narrowed to one-hop modules rather than broadening the package. Final focused native Debug/ReleaseSafe pass 23/23 steps and 588/588 tests, and full compiler Debug/ReleaseSafe pass 60/60 steps and 1083/1083 tests. Preprocessor-root no-emission cross-compiles pass for Windows x64 GNU and Linux ARM64 GNU in both modes; native contract 12/12 plus check, provider/reference/product 153/153, product replay 30/30, package policy 6/6 plus check, runner selftest 59/59, repository-wide Zig formatting, changed JavaScript syntax, tracked JSON parsing, and diff gates pass. The private native contract adds only `local-use-function-reference-invocation-foundation`; production reachability, native graduation, release state, and external systems are unchanged. Local mixin-reference application through `meta.apply()` is the next measured dependency slice; content inspection, enumeration, callable-valued cross-arena data, imports/forwards, dynamic/transitive/configured loading, and broader resolution remain separate.

`NSASS-011-LOCAL-USE-CALLABLE-REFERENCE-FOUNDATION` is verified at local implementation checkpoint `ad01fe5d7c66c63144b0166c870f2b4b5cc243a5`. Fresh Dart Sass 1.101.0 probes establish public, private, missing, and redefined local module function/mixin references through default, custom, and unprefixed namespaces; canonical equality across URL and namespace aliases; underscore/hyphen normalization; type and inspection; SCSS and indented Sass; and missing, unknown-namespace, competing-local, and built-in/local ambiguity. Native Zig now represents local function and mixin references with distinct packed canonical-module/final-definition identities, exposes only public top-level members, deduplicates aliases of one module, and accumulates every unprefixed module candidate before returning a reference. Type, inspection, and equality are provider-compatible without crossing value arenas. Private or missing members, unknown namespaces, competing local modules, and built-in/local star collisions fail without CSS. Invocation through `meta.call()`, mixin application through `meta.apply()`, content inspection, enumeration, callable-valued exports/variables/arguments/results, and broader user-module reflection remain unavailable with explicit separation regressions. The exact regression owns compressed CSS, zero-warning success, provider-aligned error classification, exact native ambiguity diagnostic identity/span, generated position zero, failed completion, argument limits, canonical alias identity, built-in/local collision behavior, and exhaustive allocation failure. The initial test-first focused Debug gate stopped at 581/585 on former local-reference rejection, and the sibling collision regression stopped at 584/585 before canonical candidate accumulation was implemented. Final focused native Debug/ReleaseSafe pass 23/23 steps and 585/585 tests, and full compiler Debug/ReleaseSafe pass 60/60 steps and 1080/1080 tests. Preprocessor-root no-emission cross-compiles pass for Windows x64 GNU and Linux ARM64 GNU in both modes; native contract 12/12 plus check, provider/reference/product 153/153, product replay 30/30, package policy 6/6 plus check, runner selftest 59/59, repository-wide Zig formatting, changed JavaScript syntax, tracked JSON parsing, and diff gates pass. The private native contract adds only `local-use-callable-reference-foundation`; production reachability, native graduation, release state, and external systems are unchanged. Local function-reference invocation through `meta.call()` is the next measured dependency slice; mixin application/content behavior, enumeration, callable-valued cross-arena data, imports/forwards, dynamic/transitive/configured loading, and broader resolution remain separate.

`NSASS-011-LOCAL-USE-CALLABLE-EXISTENCE-FOUNDATION` is verified at local implementation checkpoint `bf1ab47dc985d12c23adefa87602b861883a7b9b`. Fresh Dart Sass 1.101.0 probes establish public, private, missing, and redefined local module function/mixin existence through default, custom, and unprefixed namespaces; underscore/hyphen normalization and case sensitivity; nullable module lookup; reflected predicate invocation; SCSS and indented-Sass behavior; and distinct unknown-namespace and competing-star-module errors. Native Zig now resolves only `meta.function-exists()` and `meta.mixin-exists()` module targets against each canonical local module engine, exposes only public top-level presence, recognizes repeated aliases of one module as one identity, and fails ambiguous unprefixed lookups without CSS. Default/custom namespaces pass in SCSS and indented Sass and unprefixed lookup passes in SCSS; the existing indented-Sass `as *` parsing boundary remains unchanged and fail-closed. Local first-class references, reference invocation/application, enumeration, variable reflection, and broader user-module reflection remain unavailable with explicit separation regressions. The exact regression owns provider-aligned booleans/errors, compressed CSS, zero-warning success, exact diagnostic identity/span, generated position zero, failed completion, argument limits, and exhaustive allocation failure. The test-first focused Debug gate stopped at 578/582 on the former local-module rejection and false-result paths. Final focused native Debug/ReleaseSafe pass 23/23 steps and 582/582 tests, and full compiler Debug/ReleaseSafe pass 60/60 steps and 1077/1077 tests. Preprocessor-root no-emission cross-compiles pass for Windows x64 GNU and Linux ARM64 GNU in both modes; native contract 12/12 plus check, provider/reference/product 153/153, package policy 6/6 plus check, runner selftest 59/59, repository-wide Zig formatting, changed JavaScript syntax, tracked JSON parsing, and diff gates pass. The private native contract adds only `local-use-callable-existence-foundation`; production reachability, native graduation, release state, and external systems are unchanged. First-class local callable reference construction is the next measured dependency slice; invocation/application, enumeration, callable-valued cross-arena data, imports/forwards, dynamic/transitive/configured loading, and broader resolution remain separate.

`NSASS-011-LOCAL-USE-CALLABLE-MEMBER-FOUNDATION` is verified at local implementation checkpoint `3905f7cba035491f66be6f0fa4f353bf590c5299`. Fresh Dart Sass 1.101.0 probes establish public/private local module functions and mixins through default/custom/unprefixed namespaces; module lexical scope and defaults; positional/named/content binding; shared state and once-only evaluation across aliases; last-definition replacement; first-load CSS order; nested/arithmetic calls; SCSS/indented-Sass cross-loading; and invalid, missing, ambiguous, malformed, and over-limit closure. Native Zig now retains each loaded module's document, evaluator, environment, and values behind its canonical cache identity, uses stable top-level scope ownership for callable definitions, deep-clones non-callable cross-engine values, and bridges content execution back to the caller's document and lexical scope. Qualified and unprefixed variables observe live module state after callable mutation, root callables shadow star imports, aliases share identity, distinct star imports remain ambiguous, and callable-valued cross-arena/reflection surfaces remain unavailable. The exact regression owns compressed CSS, zero-warning success, provider-aligned private/error behavior, exact native diagnostic identity and span, zero generated output after failure, failed completion, callable limits, and exhaustive allocation failure. The test-first focused Debug gate stopped at 575/577 on the former function/mixin boundaries. Final focused native Debug/ReleaseSafe pass 23/23 steps and 579/579 tests, and full compiler Debug/ReleaseSafe pass 60/60 steps and 1074/1074 tests. Preprocessor-root no-emission cross-compiles pass for Windows x64 GNU and Linux ARM64 GNU in both modes; native contract 12/12 plus check, provider/reference/product 153/153, package policy 6/6 plus check, runner selftest 59/59, repository-wide Zig formatting, changed JavaScript syntax, tracked JSON parsing, and diff gates pass. The private native contract adds only `local-use-callable-member-foundation`; production reachability, native graduation, release state, and external systems are unchanged. Local callable existence queries are the next measured dependency slice; first-class references/application, enumeration, callable-valued cross-arena data, imports/forwards, dynamic/transitive/configured loading, and broader resolution remain separate.

`NSASS-011-STRING-UNIQUE-ID-DETERMINISTIC-COMPATIBILITY-DECISION` is verified at local implementation checkpoint `a8fce613ef0376b7a1c12f00c535fb1742fa44ef`. Fresh Dart Sass 1.101.0 probes establish that `sass:string.unique-id()` returns an unquoted `u[0-9a-z]{6}` string while advancing randomized process-global state: repeated calls and compilations, eight fresh processes, and eight concurrent compilations do not preserve deterministic bytes. Default, aliased, and unprefixed module calls; direct, list-splat, empty-map-splat, module-reflected, legacy-global, and globally reflected calls; SCSS and indented Sass; string typing and quoted interpolation; exact zero-argument binding; and legacy `global-builtin` deprecation ownership are executable provider evidence. Native Zig now recognizes every loaded `sass:string` and legacy-global unique-id identity and returns the exact dedicated error `native Sass string.unique-id() is unavailable because Dart Sass 1.101.0 produces nondeterministic output`; direct and reflected legacy-global paths first emit the existing source-owned warning, and no value or CSS is exposed. The regression owns repeated identical native compilations, exact diagnostic severity/code/message/span/order, generated position zero, failed transaction completion, function-argument limits, and exhaustive allocation failure. The test-first contract gate stopped at 6/12 on the absent capability and focused Debug stopped at 21/23 steps with 570/571 tests on the former generic diagnostic path. Final focused native Debug/ReleaseSafe pass 23/23 steps and 571/571 tests, and full compiler Debug/ReleaseSafe pass 60/60 steps and 1066/1066 tests. Preprocessor-root no-emission cross-compiles pass for Windows x64 GNU and Linux ARM64 GNU in both modes; native contract 12/12 plus check, provider/reference/product 153/153, package policy 6/6 plus check, runner selftest 59/59, repository-wide Zig formatting, changed JavaScript syntax, tracked JSON parsing, and diff gates pass. The private native contract adds only `built-in-string-unique-id-deterministic-rejection`; production reachability, native graduation, release state, and external systems are unchanged. The next dependency foundation is one-hop local `@use` module loading; user-module reflection, `@forward`, legacy imports, and `meta.load-css()` remain separate.

`NSASS-011-COLOR-LEGACY-ALPHA-MODERN-COLOR-REJECTION-SEMANTICS` is verified at local implementation checkpoint `59fe32f05974c0823ae0ee74811168f0861e339b`. Fresh Dart Sass 1.101.0 probes establish a uniform 312-case rejection matrix across 13 modern-color spellings, legacy `opacify()`/`fade-in()`/`transparentize()`/`fade-out()`, ordinary direct/direct-splat/reflected calls, and SCSS/indented Sass: one global-built-in warning precedes the function-specific legacy-colors-only error, no color-function warning is emitted, and no CSS is exposed. Native Zig now emits the existing normalized global warning before returning the exact function-specific provider error from the one shared legacy-alpha color guard, preserving closed modern-transform behavior across all three call surfaces. The exhaustive regression owns all 312 combinations, exact warning/error identity and order, exact call spans, generated position zero, failed transaction completion, a one-diagnostic limit boundary, and exhaustive allocation failure on a representative indented-Sass splat. The test-first Debug gate stopped at 21/23 steps with 569/570 tests on the missing warning; the first green replay then exposed and corrected only swallowed allocator-injected failure in the test harness. Final focused native Debug/ReleaseSafe pass 23/23 steps and 570/570 tests, and full compiler Debug/ReleaseSafe pass 60/60 steps and 1065/1065 tests. Preprocessor-root no-emission cross-compiles pass for Windows x64 GNU and Linux ARM64 GNU in both modes; native contract 12/12 plus check, provider/reference/product 151/151, package policy 6/6 plus check, runner selftest 59/59, repository-wide formatting, JavaScript syntax, JSON parsing, and diff gates pass. The private native contract adds only `built-in-color-legacy-alpha-modern-color-rejection-semantics`; production reachability, native graduation, release state, and external systems are unchanged. The next distinct measured boundary is an explicit deterministic compatibility decision for `sass:string.unique-id()`.

`NSASS-011-COLOR-LEGACY-ALPHA-COMPOUND-UNIT-SEMANTICS` is verified at local implementation checkpoint `027ddbfb113c6ce5f26f1d83904474c3cc562ec6`. Fresh Dart Sass 1.101.0 probes establish completed numeric unit algebra across ordinary direct, direct-splat, and reflected legacy `opacify()`/`fade-in()`/`transparentize()`/`fade-out()` calls: compatible-unit cancellation and conversion retain the evaluated scalar; remaining simple, denominator, product, percentage-product, and custom compound inventories are admitted; zero and one are accepted; finite values within the provider's fuzzy `1e-11` equality tolerance clamp into range; and non-numbers, deferred calculations, non-finite values, and magnitudes outside the closed range fail. Native ordinary direct calls now share the bounded legacy-alpha amount kernel, admit completed unit inventories without allocation or serialization, preserve finite/range rejection, apply fuzzy equality before clamping, and emit the exact global-built-in and function-specific warning pair on success. Four stale per-function negative inventories and fixed-keyword aggregate warning ownership were corrected without weakening failure boundaries. The test-first Debug gate stopped at 21/23 steps with 568/569 tests on the former `colorNumber()` rejection. Final focused native Debug/ReleaseSafe pass 23/23 steps and 569/569 tests, and full compiler Debug/ReleaseSafe pass 60/60 steps and 1064/1064 tests. Preprocessor-root no-emission cross-compiles pass for Windows x64 GNU and Linux ARM64 GNU in both modes; native contract 12/12 plus check, provider/reference/product 151/151, package policy 6/6 plus check, runner selftest 59/59, repository-wide formatting, JSON parsing, and diff gates pass. The private native contract adds only `built-in-color-legacy-alpha-compound-unit-semantics`; production reachability, native graduation, release state, and external systems are unchanged. Modern-color transformation remained unavailable, with its legacy-alpha rejection diagnostic designated as the next separate measured boundary.

`NSASS-011-COLOR-LEGACY-ALPHA-MISSING-CHANNEL-SEMANTICS` is verified at local implementation checkpoint `8845c604e0bbedbc0e544d5489346b1a445f2ad6`. Fresh Dart Sass 1.101.0 probes establish the selected missing-channel matrix for direct and reflected `opacify()`/`fade-in()`/`transparentize()`/`fade-out()`: every RGB channel, HSL hue/saturation/lightness, HWB hue/whiteness/blackness, all three channels, and alpha; SCSS and indented Sass; exact compressed output; exact global-built-in and function-specific warnings; and rejection of comma-form `none`. Missing legacy channels become zero and clear their missing bits before the alpha adjustment, while missing alpha begins at zero and clamps deterministically. Native Zig admits that representation only through whitespace/slash legacy constructors and applies one legacy-alpha kernel across ordinary direct, direct-splat, and reflected calls. Existing non-missing HSL/HWB conversion remains intact. Retained negative coverage keeps every sibling legacy transform, keyword transform, IE hex serialization, and legacy `color.is-powerless()` missing-channel path closed without changing admitted modern missing-channel queries. Allocation failures expose no partial CSS. The test-first Debug gate stopped at 21/23 steps with 566/567 tests at the former color-number boundary. Final focused native Debug/ReleaseSafe pass 23/23 steps and 568/568 tests, and full compiler Debug/ReleaseSafe pass 60/60 steps and 1063/1063 tests. Preprocessor-root no-emission cross-compiles pass for Windows x64 GNU and Linux ARM64 GNU in both modes; native contract 12/12 plus check, provider/reference/product 151/151, package policy 6/6 plus check, runner selftest 59/59, repository-wide formatting, JavaScript syntax, JSON parsing, and diff gates pass. The private native contract adds only `built-in-color-legacy-alpha-missing-channel-semantics`; production reachability, native graduation, release state, and external systems are unchanged. The next distinct measured boundary is legacy-alpha compound-unit amount semantics; modern-color transforms and nondeterministic `sass:string.unique-id()` remain separate and unavailable.

`NSASS-011-DIAGNOSTIC-DEPRECATION-SOURCE-DEDUPLICATION` is verified at local implementation checkpoint `27dfd2d7be6e9bcebb08434f482a1ff1336d28fe`. Fresh Dart Sass 1.101.0 probes establish exact once-per-code/source/span/message behavior for repeated direct calls in loops, repeatedly invoked user-function bodies, reflected static and dynamic calls, legacy globals, legacy `if()`, distinct textual locations, SCSS, and indented Sass. Native evaluation owns a bounded transaction-level deprecation-key inventory, refines diagnostic spans from source-backed expression slices, and routes every admitted compatibility warning through that shared policy. Distinct messages at one dynamic reflected call span remain independently observable. Legacy-`if()` and misplaced-rest first-five throttling plus their omitted-warning summary remain intact; exact diagnostic budgets count only emitted warnings; the unique-key limit defaults to 1,000, rejects zero and values above 65,536, and fails closed when exhausted. Fresh provider probes correct the inherited direct legacy `grayscale()` and reflected meta content-existence expectations to 9 and 1 warnings. The test-first filtered Debug gate failed 0/1 with 29 warnings against 14 at the old per-execution boundary; the final strengthened regression owns 15 unique warnings including legacy `if()`. Focused native Debug/ReleaseSafe pass 23/23 steps and 566/566 tests, and full compiler Debug/ReleaseSafe pass 60/60 steps and 1061/1061 tests. Preprocessor-root no-emission cross-compiles pass for Windows x64 GNU and Linux ARM64 GNU in both modes; native contract 12/12 plus check, provider/reference/product 151/151, package policy 6/6 plus check, runner selftest 59/59, repository-wide formatting, JavaScript syntax, JSON parsing, and diff gates pass. Sibling SCSS/indented-Sass probes designate deterministic missing-channel behavior in the admitted legacy-alpha family as the next separate measured boundary. Modern-color semantics and nondeterministic `sass:string.unique-id()` remain separate and unavailable. Production reachability, native graduation, release state, and external systems are unchanged.

`NSASS-011-COLOR-LEGACY-FADE-OUT-DIRECT-SPLAT-EXPANSION` is verified at local implementation checkpoint `4f35ca9fbb2aa7d8b62845704142a85f4d7fc317`. Fresh Dart Sass 1.101.0 probes establish legacy-global direct `fade-out()` positional-list, keyword-map, bracketed-list, and positional/keyword argument-list splats; later-map override of an earlier named color; positional/map duplicate rejection; once-only source order; SCSS/indented-Sass behavior; legacy RGB/HSL/HWB plus transparent, opaque, alpha-clamping, zero/full amount results; color typing; and one global-built-in plus one `fade-out()` deprecation per successful source call. Native dispatch selects the reflected path's evaluated-splat expansion, canonical bounded binder, global callable identity, and shared legacy-alpha kernel only for global `fade-out()` calls containing a syntactic splat; ordinary global and module-owned calls retain their prior paths. Empty/short/overfull splats, unknown or non-string map keys, duplicates, invalid values, deferred calculations, range failures, missing-channel constructors, modern colors, expanded argument ceilings, and allocation paths expose no CSS. The provider accepts selected compound-unit and missing-channel inputs, but the native slice remains deliberately narrower and fail-closed. The test-first Debug evaluator gate stopped at 21/23 steps and 563/565 tests on the former `SplatUnsupported` and unsupported negative-classification boundaries before final focused native Debug/ReleaseSafe passed 23/23 steps and 565/565 tests and full compiler Debug/ReleaseSafe passed 60/60 steps and 1060/1060 tests. Root no-emission cross-compiles pass for Windows x64 GNU and Linux ARM64 GNU in both modes; native contract 12/12 plus check, provider/reference/product 151/151, package policy 6/6 plus check, runner selftest 59/59, repository-wide formatting, JavaScript syntax, JSON parsing, and diff gates pass. A sibling SCSS/indented-Sass loop probe establishes that three evaluations of one legacy direct-call source location emit only one warning of each deprecation type, designating source-location deprecation deduplication as the next separate measured boundary. Missing-channel and modern-color semantics and nondeterministic `sass:string.unique-id()` remain separate and unavailable. Production reachability, native graduation, release state, and external systems are unchanged.

`NSASS-011-COLOR-LEGACY-TRANSPARENTIZE-DIRECT-SPLAT-EXPANSION` is verified at local implementation checkpoint `07eab662807631ae5648d526a1ca86c99c205510`. Fresh Dart Sass 1.101.0 probes establish legacy-global direct `transparentize()` positional-list, keyword-map, bracketed-list, and positional/keyword argument-list splats; later-map override of an earlier named color; positional/map duplicate rejection; once-only source order; SCSS/indented-Sass behavior; legacy RGB/HSL/HWB plus transparent, opaque, alpha-clamping, zero/full amount results; color typing; and one global-built-in plus one `transparentize()` deprecation per successful source call. Native dispatch selects the reflected path's evaluated-splat expansion, canonical bounded binder, global callable identity, and shared legacy-alpha kernel only for global `transparentize()` calls containing a syntactic splat; ordinary global calls, module-owned calls, and direct `fade-out()` retain their prior paths. Empty/short/overfull splats, unknown or non-string map keys, duplicates, invalid values, deferred calculations, range failures, missing-channel constructors, modern colors, expanded argument ceilings, and allocation paths expose no CSS. The provider accepts selected compound-unit and missing-channel inputs, but the native slice remains deliberately narrower and fail-closed. The test-first Debug evaluator gate stopped at 21/23 steps and 562/564 tests on the former `SplatUnsupported` boundary before final focused native Debug/ReleaseSafe passed 23/23 steps and 564/564 tests and full compiler Debug/ReleaseSafe passed 60/60 steps and 1059/1059 tests. Preprocessor-root no-emission cross-compiles pass for Windows x64 GNU and Linux ARM64 GNU in both modes; native contract 12/12 plus check, provider/reference/product 151/151, package policy 6/6 plus check, runner selftest 59/59, repository-wide formatting, JavaScript syntax, JSON parsing, and diff gates pass. Sibling probes designate deterministic legacy-global direct `fade-out()` splats next with the same two warning classes; repeated-call warning deduplication, missing-channel and modern-color semantics, and nondeterministic `sass:string.unique-id()` remain separate and unavailable. Production reachability, native graduation, release state, and external systems are unchanged.

`NSASS-011-COLOR-LEGACY-FADE-IN-DIRECT-SPLAT-EXPANSION` is verified at local implementation checkpoint `19027afbf8524c0116c6b445fe04d0a11454f32b`. Fresh Dart Sass 1.101.0 probes establish legacy-global direct `fade-in()` positional-list, keyword-map, bracketed-list, and positional/keyword argument-list splats; later-map override of an earlier named color; positional/map duplicate rejection; once-only source order; SCSS/indented-Sass behavior; legacy RGB/HSL/HWB plus transparent, opaque, alpha-clamping, zero/full amount results; color typing; and one global-built-in plus one `fade-in()` deprecation per successful source call. Native dispatch selects the reflected path's evaluated-splat expansion, canonical bounded binder, global callable identity, and shared legacy-alpha kernel only for global `fade-in()` calls containing a syntactic splat; ordinary global calls, module-owned calls, and sibling alpha functions retain their prior paths. Empty/short/overfull splats, unknown or non-string map keys, duplicates, invalid values, deferred calculations, range failures, missing-channel constructors, modern colors, expanded argument ceilings, and allocation paths expose no CSS. The provider accepts selected compound-unit and missing-channel inputs, but the native slice remains deliberately narrower and fail-closed. The test-first Debug evaluator gate stopped at 21/23 steps and 561/563 tests on the former `SplatUnsupported` boundary before final focused native Debug/ReleaseSafe passed 23/23 steps and 563/563 tests and full compiler Debug/ReleaseSafe passed 60/60 steps and 1058/1058 tests. Evaluator no-emission cross-compiles pass for Windows x64 GNU and Linux ARM64 GNU in both modes; native contract 12/12 plus check, provider/reference/product 151/151, package policy 6/6 plus check, runner selftest 59/59, repository-wide formatting, JavaScript syntax, JSON parsing, and diff gates pass. Sibling probes designate deterministic legacy-global direct `transparentize()` splats next with the same two warning classes; direct `fade-out()` splats, repeated-call warning deduplication, missing-channel and modern-color semantics, and nondeterministic `sass:string.unique-id()` remain separate and unavailable. Production reachability, native graduation, release state, and external systems are unchanged.

`NSASS-011-COLOR-LEGACY-OPACIFY-DIRECT-SPLAT-EXPANSION` is verified at local implementation checkpoint `573031f0d93030d767551369b91907dc73c331d2`. Fresh Dart Sass 1.101.0 probes establish legacy-global direct `opacify()` positional-list, keyword-map, bracketed-list, and positional/keyword argument-list splats; later-map override of an earlier named color; positional/map duplicate rejection; once-only source order; SCSS/indented-Sass behavior; legacy RGB/HSL/HWB plus transparent, opaque, alpha-clamping, zero/full amount results; color typing; and one global-built-in plus one `opacify()` deprecation per successful source call. Native dispatch selects the reflected path's evaluated-splat expansion, canonical bounded binder, global callable identity, and shared legacy-alpha kernel only for global `opacify()` calls containing a syntactic splat; ordinary global calls, module-owned calls, and sibling alpha functions retain their prior paths. Empty/short/overfull splats, unknown or non-string map keys, duplicates, invalid values, deferred calculations, range failures, missing-channel constructors, modern colors, expanded argument ceilings, and allocation paths expose no CSS. The provider accepts selected compound-unit and missing-channel inputs, but the native slice remains deliberately narrower and fail-closed. The test-first Debug evaluator gate stopped at 21/23 steps and 560/562 tests on the former `SplatUnsupported` boundary before final focused native Debug/ReleaseSafe passed 23/23 steps and 562/562 tests and full compiler Debug/ReleaseSafe passed 60/60 steps and 1057/1057 tests. Evaluator no-emission cross-compiles pass for Windows x64 GNU and Linux ARM64 GNU in both modes; native contract 12/12 plus check, provider/reference/product 151/151, package policy 6/6 plus check, runner selftest 59/59, repository-wide formatting, JavaScript syntax, JSON parsing, and diff gates pass. Sibling probes designate deterministic legacy-global direct `fade-in()` splats next with the same two warning classes; direct `transparentize()`/`fade-out()` splats, repeated-call warning deduplication, missing-channel and modern-color semantics, and nondeterministic `sass:string.unique-id()` remain separate and unavailable. Production reachability, native graduation, release state, and external systems are unchanged.

`NSASS-011-COLOR-LEGACY-INVERT-DIRECT-SPLAT-EXPANSION` is verified at local implementation checkpoint `eedc79b437a15b6a4fb44f5114bfba663f7c3039`. Fresh Dart Sass 1.101.0 probes establish legacy-global direct `invert()` comma-list, keyword-map, scalar, bracketed-list, and positional/keyword argument-list splats; later-map override of an earlier named binding; positional/map duplicate rejection; once-only source order; SCSS/indented-Sass behavior; legacy RGB/HSL/HWB plus transparent and alpha results; color/filter typing; one global-built-in deprecation per successful color source call; and warning-free numeric, unit-bearing, deferred, and calculated CSS filters. Native dispatch selects the reflected path's evaluated-splat expansion, canonical bounded binder, global callable identity, and shared invert kernel only for global calls containing a syntactic splat; ordinary global calls and module-owned direct calls retain their prior paths. Empty/overfull splats, unknown or non-string map keys, duplicates, invalid values, unitless/unit-bearing weights, optional spaces, direct missing-channel constructors, modern colors, expanded argument ceilings, and allocation paths expose no CSS. The provider accepts selected unit coercions, spaces, and missing-channel inputs, but the native slice remains deliberately narrower and fail-closed. The test-first Debug evaluator gate stopped at 21/23 steps and 560/561 tests on the former `SplatUnsupported` boundary before final focused native Debug/ReleaseSafe passed 23/23 steps and 561/561 tests and full compiler Debug/ReleaseSafe passed 60/60 steps and 1056/1056 tests. Evaluator no-emission cross-compiles pass for Windows x64 GNU and Linux ARM64 GNU in both modes; native contract 12/12 plus check, provider/reference/product 151/151, package policy 6/6 plus check, runner selftest 59/59, repository-wide formatting, JavaScript syntax, JSON parsing, and diff gates pass. Sibling probes designate deterministic legacy-global direct `opacify()` splats next, preserving one global-built-in and one `opacify()` deprecation per successful source call; related alpha functions, repeated-call warning deduplication, missing-channel and modern-color semantics, and nondeterministic `sass:string.unique-id()` remain separate and unavailable. Production reachability, native graduation, release state, and external systems are unchanged.

`NSASS-011-COLOR-MODULE-INVERT-DIRECT-SPLAT-EXPANSION` is verified at local implementation checkpoint `f4625ce9d996901c19ad266304bf3adccd3afce3`. Fresh Dart Sass 1.101.0 probes establish direct `sass:color.invert()` default/alias/unprefixed access; ordinary/named calls; comma-list, keyword-map, scalar, bracketed-list, and positional/keyword argument-list splats; later-map override of an earlier named binding; positional/map duplicate rejection; once-only source order; SCSS/indented-Sass behavior; legacy RGB/HSL/HWB plus transparent and alpha results; color/filter typing; and one module compatibility warning per numeric filter source call. Native dispatch recognizes only a loaded color-module member and selects the reflected path's evaluated-splat expansion, canonical bounded binder, module callable identity, and shared invert kernel. Empty/overfull splats, unknown or non-string map keys, duplicates, invalid/deferred values, unitless/unit-bearing weights, optional spaces, direct missing-channel constructors, modern colors, expanded argument ceilings, and allocation paths expose no CSS; legacy-global direct splats retain their former unsupported path. The test-first Debug evaluator gate stopped at 21/23 steps and 558/560 tests on the former undefined-module boundary before final focused native Debug/ReleaseSafe passed 23/23 steps and 560/560 tests and full compiler Debug/ReleaseSafe passed 60/60 steps and 1055/1055 tests. Evaluator no-emission cross-compiles pass for Windows x64 GNU and Linux ARM64 GNU in both modes; native contract 12/12 plus check, provider/reference/product 151/151, package policy 6/6 plus check, runner selftest 59/59, repository-wide formatting, JavaScript syntax, JSON parsing, and diff gates pass. Sibling probes designate deterministic legacy-global direct `invert()` splats next, preserving one global-built-in deprecation per color result and warning-free numeric/unit/deferred CSS filters; optional spaces, missing-channel and modern-color semantics, and nondeterministic `sass:string.unique-id()` remain separate and unavailable. Production reachability, native graduation, release state, and external systems are unchanged.

`NSASS-011-COLOR-LEGACY-GRAYSCALE-DIRECT-SPLAT-EXPANSION` is verified at local implementation checkpoint `edb7e792a930f85ddb535a6babd78a237e946068`. Fresh Dart Sass 1.101.0 probes establish legacy-global direct `grayscale()` comma-list, keyword-map, scalar, bracketed-list, and positional/keyword argument-list splats; later-map override of an earlier named binding; positional/map duplicate rejection; once-only source order; SCSS/indented-Sass behavior; legacy RGB/HSL/HWB plus transparent and alpha results; color typing; warning-free numeric, unit-bearing, and deferred CSS filters; and one global-built-in deprecation per color result. Native dispatch selects the reflected path's evaluated-splat expansion, canonical bounded binder, global callable identity, and shared grayscale kernel only for global calls containing a syntactic splat; ordinary global calls and module-owned direct calls retain their prior paths. Empty/overfull splats, unknown or non-string map keys, duplicates, invalid values, direct missing-channel constructors, modern colors, expanded argument ceilings, and allocation paths expose no CSS. The provider accepts selected missing-channel and modern-color inputs, but the native slice remains deliberately narrower and fail-closed. The test-first filtered Debug evaluator gate failed 0/1 at the former `SplatUnsupported` boundary before final focused native Debug/ReleaseSafe passed 23/23 steps and 559/559 tests and full compiler Debug/ReleaseSafe passed 60/60 steps and 1054/1054 tests. Evaluator no-emission cross-compiles pass for Windows x64 GNU and Linux ARM64 GNU in both modes; native contract 12/12 plus check, provider/reference/product 151/151, package policy 6/6 plus check, runner selftest 59/59, repository-wide formatting, JavaScript syntax, JSON parsing, and diff gates pass. Sibling probes designate deterministic direct `sass:color.invert()` splats next, with numeric filter calls retaining their distinct module compatibility warning; legacy-global invert splats, optional interpolation spaces, missing-channel and modern-color semantics, and nondeterministic `sass:string.unique-id()` remain separate and unavailable. Production reachability, native graduation, release state, and external systems are unchanged.

`NSASS-011-COLOR-MODULE-GRAYSCALE-DIRECT-SPLAT-EXPANSION` is verified at local implementation checkpoint `b5d8278abb8e7ce749f820c0be12dee73ddcfbeb`. Fresh Dart Sass 1.101.0 probes establish direct `sass:color.grayscale()` default/alias/unprefixed access; ordinary/named calls; comma-list, keyword-map, scalar, bracketed-list, and positional/keyword argument-list splats; later-map override of an earlier named binding; positional/map duplicate rejection; once-only source order; SCSS/indented-Sass behavior; legacy RGB/HSL/HWB plus transparent and alpha results; color typing; and numeric CSS-filter preservation with one module deprecation per numeric source call. Native dispatch recognizes only a loaded color-module member and selects the reflected path's evaluated-splat expansion, canonical bounded binder, module callable identity, and shared grayscale kernel. Empty/overfull splats, unknown or non-string map keys, duplicates, invalid or deferred values, direct missing-channel constructors, modern colors, expanded argument ceilings, and allocation paths expose no CSS; legacy-global direct splats retain their former unsupported path. The test-first focused Debug gate stopped at 21/23 steps and 556/558 tests on the former undefined-module boundary before final focused native Debug/ReleaseSafe passed 23/23 steps and 558/558 tests and full compiler Debug/ReleaseSafe passed 60/60 steps and 1053/1053 tests. Evaluator no-emission cross-compiles pass for Windows x64 GNU and Linux ARM64 GNU in both modes; native contract 12/12 plus check, provider/reference/product 151/151, package policy 6/6 plus check, runner selftest 59/59, repository-wide formatting, JavaScript syntax, JSON parsing, and diff gates pass. Sibling probes designate deterministic legacy-global direct `grayscale()` splats next while missing-channel and modern-color semantics and nondeterministic `sass:string.unique-id()` remain separate and unavailable. Production reachability, native graduation, release state, and external systems are unchanged.

`NSASS-011-COLOR-LEGACY-COMPLEMENT-DIRECT-SPLAT-EXPANSION` is verified at local implementation checkpoint `259f974e4a6c48af87e98a032adc7d091f8c4730`. Fresh Dart Sass 1.101.0 probes establish one-color legacy-global direct `complement()` comma-list, keyword-map, scalar, bracketed-list, and positional/keyword argument-list splats; later-map override of an earlier named binding; positional/map duplicate rejection; once-only source order; SCSS/indented-Sass behavior; legacy RGB/HSL/HWB plus transparent and alpha results; color typing; and one global-built-in deprecation per successful source call. Native dispatch selects the reflected path's evaluated-splat expansion, canonical bounded binder, global callable identity, and shared complement kernel only for global calls containing a syntactic splat; ordinary global calls and module-owned direct calls retain their prior paths. Empty/overfull splats, unknown or non-string map keys, duplicates, invalid values, direct missing-channel constructors, modern colors, optional interpolation spaces, expanded argument ceilings, and allocation paths expose no CSS. The test-first focused Debug gate stopped at 21/23 steps and 555/557 tests on the former `SplatUnsupported` boundary before final focused native Debug/ReleaseSafe passed 23/23 steps and 557/557 tests and full compiler Debug/ReleaseSafe passed 60/60 steps and 1052/1052 tests. Evaluator no-emission cross-compiles pass for Windows x64 GNU and Linux ARM64 GNU in both modes; native contract 12/12 plus check, provider/reference/product 151/151, package policy 6/6 plus check, runner selftest 59/59, repository-wide formatting, JavaScript syntax, JSON parsing, and diff gates pass. Sibling probes designate deterministic direct `sass:color.grayscale()` splats next while legacy-global grayscale splats retain a separate warning and CSS-filter boundary; optional complement interpolation spaces and nondeterministic `sass:string.unique-id()` remain separate and unavailable. Production reachability, native graduation, release state, and external systems are unchanged.

`NSASS-011-COLOR-MODULE-COMPLEMENT-DIRECT-SPLAT-EXPANSION` is verified at local implementation checkpoint `deba79a5561c2e739496aaf23713009790208a27`. Fresh Dart Sass 1.101.0 probes establish direct `sass:color.complement()` default/alias/unprefixed access; ordinary/named calls; comma-list, keyword-map, scalar, bracketed-list, and positional/keyword argument-list splats; later-map override of an earlier named binding; positional/map duplicate rejection; once-only source order; SCSS/indented-Sass behavior; legacy RGB/HSL/HWB plus transparent and alpha results; color typing; and zero warnings. Native dispatch recognizes only a loaded color-module member and selects the reflected path's evaluated-splat expansion, canonical bounded binder, module callable identity, and shared complement kernel. Empty/overfull splats, unknown or non-string map keys, duplicates, invalid values, direct missing-channel constructors, modern colors, expanded argument ceilings, and allocation paths expose no CSS; legacy-global direct splats retain their former unsupported path, and optional interpolation-space semantics remain explicitly unsupported and separate. The test-first focused Debug gate stopped at 554/556 on the former undefined-module boundary before final focused native Debug/ReleaseSafe passed 23/23 steps and 556/556 tests and full compiler Debug/ReleaseSafe passed 60/60 steps and 1051/1051 tests. Evaluator no-emission cross-compiles pass for Windows x64 GNU and Linux ARM64 GNU in both modes; native contract 12/12 plus check, provider/reference/product 151/151, package policy 6/6 plus check, runner selftest 59/59, repository-wide formatting, JavaScript syntax, JSON parsing, and diff gates pass. Sibling probes designate the deterministic one-color legacy-global direct `complement()` splat boundary next while all optional interpolation-space semantics and nondeterministic `sass:string.unique-id()` remain separate and unavailable. Production reachability, native graduation, release state, and external systems are unchanged.

`NSASS-011-COLOR-LEGACY-ADJUST-HUE-DIRECT-SPLAT-EXPANSION` is verified at local implementation checkpoint `a866db43330a0e0afdf0b5dd7dae483a4caa8a20`. Fresh Dart Sass 1.101.0 probes establish legacy-global direct `adjust-hue()` positional-list and keyword-map splats, direct-plus-map binding, later-map override, once-only source order, SCSS/indented-Sass behavior, legacy RGB/HSL/HWB plus alpha results, admitted angle units, negative/wrapped angles, color typing, and the provider warning boundary. Native dispatch selects evaluated-splat expansion, the canonical bounded binder, global callable identity, and the shared hue-adjustment kernel only for global calls containing a syntactic splat; ordinary global calls retain their prior path, direct module calls including splats remain unavailable, and the existing normalized diagnostic path is unchanged. Empty/short/overfull splats, unknown or non-string map keys, positional/map duplicates, deferred/compound angles, expanded argument ceilings, modern colors, and allocation paths expose no CSS; the existing native missing-channel and compound-unit boundaries remain deliberately narrower and fail closed rather than broadening this slice. The test-first focused Debug gate stopped at 553/555 on the former `SplatUnsupported` boundary before final focused native Debug/ReleaseSafe passed 23/23 steps and 555/555 tests and full compiler Debug/ReleaseSafe passed 60/60 steps and 1050/1050 tests. Preprocessor-root no-emission cross-compiles pass for Windows x64 GNU and Linux ARM64 GNU in both modes; native contract 12/12 plus check, provider/reference/product 151/151, package policy 6/6 plus check, runner selftest 59/59, formatting, syntax, JSON, and diff gates pass. Sibling probes designate deterministic direct `sass:color.complement()` splats next while legacy-global complement splats retain their separate warning boundary and nondeterministic `sass:string.unique-id()` remains unavailable. Production reachability, native graduation, release state, and external systems are unchanged.

`NSASS-011-COLOR-LEGACY-DESATURATE-DIRECT-SPLAT-EXPANSION` is verified at local implementation checkpoint `ada67d37ac757e6f447a7910439f317bc3587be6`. Fresh Dart Sass 1.101.0 probes establish exact legacy-global direct `desaturate()` positional-list and keyword-map splats, direct-plus-map binding, later-map override, once-only source order, SCSS/indented-Sass behavior, legacy RGB/HSL/HWB plus alpha results, unitless/unitful amounts, color typing, and one global-built-in plus one color-function deprecation per successful call. Native dispatch selects evaluated-splat expansion, the canonical bounded binder, global callable identity, and the shared desaturation kernel only for global calls containing a syntactic splat; ordinary global calls retain their prior path, and direct module calls including splats remain unavailable. Empty/short/overfull splats, unknown or non-string map keys, positional/map duplicates, negative/out-of-range amounts, expanded argument ceilings, modern colors, deferred values, and allocation paths expose no CSS; the existing native missing-channel boundary remains deliberately narrower and fail-closed rather than broadening this slice. The test-first focused Debug gate stopped at 552/554 on the former `SplatUnsupported` boundary before final focused native Debug/ReleaseSafe passed 23/23 steps and 554/554 tests and full compiler Debug/ReleaseSafe passed 60/60 steps and 1049/1049 tests. Preprocessor-root no-emission cross-compiles pass for Windows x64 GNU and Linux ARM64 GNU in both modes; native contract 12/12 plus check, provider/reference/product 151/151, package policy 6/6 plus check, runner selftest 59/59, formatting, syntax, JSON, and diff gates pass. Sibling probes designate deterministic legacy-global `adjust-hue()` direct splats next while direct `sass:color.adjust-hue()` remains a provider error and nondeterministic `sass:string.unique-id()` remains unavailable. Production reachability, native graduation, release state, and external systems are unchanged.

`NSASS-011-COLOR-LEGACY-SATURATE-DIRECT-SPLAT-EXPANSION` is verified at local implementation checkpoint `1d4e0f1864d48133180ae9022a3fa78efc2a6c98`. Fresh Dart Sass 1.101.0 probes establish exact legacy-global direct `saturate()` one-argument positional-list and keyword-map CSS-filter splats with no warnings, deferred filter preservation, two-argument color list/map splats, direct-plus-map binding, later-map override, once-only source order, SCSS/indented-Sass behavior, legacy RGB/HSL/HWB plus alpha results, unitless/unitful amounts, color typing, and one global-built-in plus one color-function deprecation per successful color call. Native dispatch selects evaluated-splat expansion, the canonical bounded binder, global callable identity, and the shared saturate kernel only for global calls containing a syntactic splat; ordinary global calls retain their prior path, and direct module calls including splats remain unavailable. Empty/short/overfull splats, unknown or non-string map keys, positional/map duplicates, negative/out-of-range color amounts, expanded argument ceilings, modern colors, deferred color amounts, and allocation paths expose no CSS; the existing native missing-channel boundary remains fail-closed rather than broadening this slice. The test-first focused Debug gate stopped at 551/553 on the former `SplatUnsupported` boundary before final focused native Debug/ReleaseSafe passed 23/23 steps and 553/553 tests and full compiler Debug/ReleaseSafe passed 60/60 steps and 1048/1048 tests. Preprocessor-root no-emission cross-compiles pass for Windows x64 GNU and Linux ARM64 GNU in both modes; native contract 12/12 plus check, provider/reference/product 151/151, package policy 6/6 plus check, runner selftest 59/59, formatting, syntax, JSON, and diff gates pass. Sibling probes designate deterministic legacy-global `desaturate()` direct splats next while direct `sass:color.desaturate()` remains a provider error and nondeterministic `sass:string.unique-id()` remains unavailable. Production reachability, native graduation, release state, and external systems are unchanged.

`NSASS-011-COLOR-LEGACY-DARKEN-DIRECT-SPLAT-EXPANSION` is verified at local implementation checkpoint `7a7808a47b6b3c88c19d21be3bd56993ce9d00e9`. Fresh Dart Sass 1.101.0 probes establish exact legacy-global direct `darken()` positional-list and keyword-map splats, direct-plus-map binding, later-map override, once-only source order, SCSS/indented-Sass behavior, legacy RGB/HSL/HWB plus alpha results, unitless/unitful amounts, color typing, and one global-built-in plus one color-function deprecation per successful call. Native dispatch selects the reflected path's evaluated-splat expansion, canonical binder, global callable identity, and bounded legacy darken kernel only for global calls containing a syntactic splat; ordinary global calls retain their prior path, and direct module calls including splats remain unavailable. Empty/short/overfull splats, unknown or non-string map keys, positional/map duplicates, negative/out-of-range amounts, expanded argument ceilings, modern colors, deferred values, and allocation paths expose no CSS; the existing native missing-channel boundary remains fail-closed rather than broadening this slice. The test-first focused Debug gate stopped at 550/552 on the former `SplatUnsupported` boundary before final focused native Debug/ReleaseSafe passed 23/23 steps and 552/552 tests and full compiler Debug/ReleaseSafe passed 60/60 steps and 1047/1047 tests. Preprocessor-root no-emission cross-compiles pass for Windows x64 GNU and Linux ARM64 GNU in both modes; native contract 12/12 plus check, provider/reference/product 151/151, package policy 6/6 plus check, runner selftest 59/59, formatting, syntax, JSON, and diff gates pass. Sibling probes designate deterministic legacy-global `saturate()` direct splats next while direct `sass:color.saturate()` remains a provider error and nondeterministic `sass:string.unique-id()` remains unavailable. Production reachability, native graduation, release state, and external systems are unchanged.

`NSASS-011-COLOR-LEGACY-LIGHTEN-DIRECT-SPLAT-EXPANSION` is verified at local implementation checkpoint `511e47b06af9bf349704e938ea5abd46d302fca6`. Fresh Dart Sass 1.101.0 probes establish exact legacy-global direct `lighten()` positional-list and keyword-map splats, direct-plus-map binding, later-map override, once-only source order, SCSS/indented-Sass behavior, legacy RGB/HSL/HWB plus alpha results, unitless/unitful amounts, color typing, and one global-built-in plus one color-function deprecation per successful call. Native dispatch selects the reflected path's evaluated-splat expansion, canonical binder, global callable identity, and bounded legacy lighten kernel only for global calls containing a syntactic splat; ordinary global calls retain their prior path, and direct module calls including splats remain unavailable. Empty/short/overfull splats, unknown or non-string map keys, positional/map duplicates, negative/out-of-range amounts, expanded argument ceilings, modern and missing-channel colors, deferred values, and allocation paths expose no CSS. The test-first focused Debug gate stopped at 549/551 on the former `SplatUnsupported` boundary before final focused native Debug/ReleaseSafe passed 23/23 steps and 551/551 tests and full compiler Debug/ReleaseSafe passed 60/60 steps and 1046/1046 tests. Evaluator no-emission cross-compiles pass for Windows x64 GNU and Linux ARM64 GNU in both modes; native contract 12/12 plus check, provider/reference/product 151/151, package policy 6/6 plus check, runner selftest 59/59, formatting, syntax, JSON, and diff gates pass. Sibling probes designate deterministic legacy-global `darken()` direct splats next while direct `sass:color.darken()` remains a provider error and nondeterministic `sass:string.unique-id()` remains unavailable. Production reachability, native graduation, release state, and external systems are unchanged.

`NSASS-011-COLOR-LEGACY-MIX-DIRECT-SPLAT-EXPANSION` is verified at local implementation checkpoint `98fa215aed394dd3254bb951c752afa7daa8c53f`. Fresh Dart Sass 1.101.0 probes establish exact legacy-global direct `mix()` positional-list and keyword-map splats, direct-plus-map binding, later-map override, once-only source order, SCSS/indented-Sass behavior, legacy RGB/HSL/HWB plus alpha results, color typing, and one global-built-in deprecation per successful call. Native dispatch selects the reflected path's evaluated-splat expansion, canonical binder, global callable identity, and bounded legacy mix kernel only for global calls containing a syntactic splat; ordinary global calls and module-owned direct calls retain their prior paths. Empty/short splats, the excluded fourth modern-method argument, unknown or non-string map keys, positional/map duplicates, unitless weights, expanded argument ceilings, modern methods, wide-gamut colors, deferred values, and allocation paths expose no CSS. The test-first focused Debug gate stopped at 548/550 on the former `SplatUnsupported` boundary before final focused native Debug/ReleaseSafe passed 23/23 steps and 550/550 tests and full compiler Debug/ReleaseSafe passed 60/60 steps and 1045/1045 tests. Evaluator no-emission cross-compiles pass for Windows x64 GNU and Linux ARM64 GNU in both modes; native contract 12/12 plus check, provider/reference/product 151/151, package policy 6/6 plus check, runner selftest 59/59, formatting, syntax, JSON, and diff gates pass. Sibling probes designate deterministic legacy-global `lighten()` direct splats next while direct `sass:color.lighten()` remains a provider error and nondeterministic `sass:string.unique-id()` remains unavailable. Production reachability, native graduation, release state, and external systems are unchanged.

`NSASS-011-COLOR-MODULE-MIX-DIRECT-FUNCTION-INVOCATION` is verified at local implementation checkpoint `fa477b4ec940b79a307b2f9148d0f06bdf3a2777`. Fresh Dart Sass 1.101.0 probes establish exact direct `sass:color.mix()` default/alias/unprefixed access, positional/named/list/map-splat binding, later-map override, once-only source order, SCSS/indented-Sass behavior, legacy RGB/HSL/HWB plus alpha results, color typing, and zero warnings. Native dispatch admits only a loaded color-module namespace and reuses the reflected path's evaluated-splat expansion, canonical binder, module callable identity, and bounded legacy mix kernel while retaining the separate global path. Empty/short/overfull splats, unknown or non-string map keys, duplicates, unitless weights, expanded argument ceilings, modern methods, wide-gamut colors, deferred values, and allocation paths expose no CSS. The test-first focused Debug gate stopped at 547/549 on the former unavailable module boundary before final focused native Debug/ReleaseSafe passed 23/23 steps and 549/549 tests and full compiler Debug/ReleaseSafe passed 60/60 steps and 1044/1044 tests. Evaluator no-emission cross-compiles pass for Windows x64 GNU and Linux ARM64 GNU in both modes; native contract 12/12 plus check, provider/reference/product 151/151, package policy 6/6 plus check, runner selftest 59/59, formatting, syntax, JSON, and diff gates pass. Sibling probes retain direct `sass:color.lighten()` as a provider error, legacy-global direct mix splats as a separate pending boundary, and nondeterministic `sass:string.unique-id()` as unavailable. Production reachability, native graduation, release state, and external systems are unchanged.

`NSASS-011-COLOR-MODULE-BLACKNESS-DIRECT-FUNCTION-INVOCATION` is verified at local implementation checkpoint `4cb8ea397ffd5a0c7549e9d0518bce2b0b4811f7`. Fresh Dart Sass 1.101.0 probes establish exact direct `sass:color.blackness()` default/alias/unprefixed access, positional/named/list/map-splat binding, later-map override, once-only source order, SCSS/indented-Sass behavior, percentage typing, one color-function deprecation per successful call, and the plain-CSS boundary for an unowned bare call. Native dispatch admits only a loaded color-module namespace and reuses the reflected path's evaluated splat expansion, canonical binder, module callable identity, bounded legacy-color kernel, and normalized warning. Empty/overfull splats, unknown or non-string map keys, duplicates, expanded argument ceilings, modern colors, deferred values, and allocation paths expose no CSS. The test-first focused Debug gate stopped at 546/548 on the former unavailable module boundary before final focused native Debug/ReleaseSafe passed 23/23 steps and 548/548 tests and full compiler Debug/ReleaseSafe passed 60/60 steps and 1043/1043 tests. Evaluator no-emission cross-compiles pass for Windows x64 GNU and Linux ARM64 GNU in both modes; native contract 12/12 plus check, provider/reference/product 151/151, package policy 6/6 plus check, runner selftest 59/59, formatting, syntax, JSON, and diff gates pass. A fresh sibling probe confirms nondeterministic `sass:string.unique-id()` remains unavailable. Production reachability, native graduation, release state, and external systems are unchanged.

`NSASS-011-COLOR-MODULE-WHITENESS-DIRECT-FUNCTION-INVOCATION` is verified at local implementation checkpoint `c97dd52966eb08bbc4184ae1c5aaad9d9de3279b`. Fresh Dart Sass 1.101.0 probes establish exact direct `sass:color.whiteness()` default/alias/unprefixed access, positional/named/list/map-splat binding, later-map override, once-only source order, SCSS/indented-Sass behavior, percentage typing, one color-function deprecation per successful call, and the plain-CSS boundary for an unowned bare call. Native dispatch admits only a loaded color-module namespace and reuses the reflected path's evaluated splat expansion, canonical binder, module callable identity, bounded legacy-color kernel, and normalized warning. Empty/overfull splats, unknown or non-string map keys, duplicates, expanded argument ceilings, modern colors, deferred values, and allocation paths expose no CSS. The test-first focused Debug gate stopped at 545/547 on the former unavailable module boundary before final focused native Debug/ReleaseSafe passed 23/23 steps and 547/547 tests and full compiler Debug/ReleaseSafe passed 60/60 steps and 1042/1042 tests. Evaluator no-emission cross-compiles pass for Windows x64 GNU and Linux ARM64 GNU in both modes; native contract 12/12 plus check, provider/reference/product 151/151, package policy 6/6 plus check, runner selftest 59/59, formatting, syntax, JSON, and diff gates pass. Direct `sass:color.blackness()` and nondeterministic `sass:string.unique-id()` remain unavailable. Production reachability, native graduation, release state, and external systems are unchanged.

`NSASS-011-STRING-LEGACY-INSERT-DIRECT-SPLAT-EXPANSION` is verified at local implementation checkpoint `c4babb6decd0394ddcd28a3801f6e3c5674151fa`. Fresh Dart Sass 1.101.0 probes establish exact legacy-global direct `str-insert()` three-item positional-list and keyword-map splats, direct-plus-map binding, later-map override, quoted/unquoted/escaped/interpolated strings, Unicode-scalar/combining/ZWJ insertion, once-only source order, SCSS/indented-Sass behavior, and one global-built-in deprecation warning per successful call. Native dispatch selects the reflected path's evaluated splat expansion, canonical binder, global callable ownership, and bounded insert kernel only for legacy insert calls containing a syntactic splat; ordinary legacy calls and every sibling direct built-in retain their prior paths. Empty, short, and overfull splats, unknown or non-string map keys, positional/map duplicates, expanded argument ceilings, invalid values, deferred calculations, and allocation paths expose no CSS. The test-first focused Debug gate stopped at 544/546 on the former `SplatUnsupported` boundary before final focused native Debug/ReleaseSafe passed 23/23 steps and 546/546 tests and full compiler Debug/ReleaseSafe passed 60/60 steps and 1041/1041 tests. Evaluator no-emission cross-compiles pass for Windows x64 GNU and Linux ARM64 GNU in both modes; native contract 12/12 plus check, provider/reference/product 151/151, package policy 6/6 plus check, runner selftest 59/59, formatting, syntax, JSON, and diff gates pass. Nondeterministic `sass:string.unique-id()` remains unavailable. Production reachability, native graduation, release state, and external systems are unchanged.

`NSASS-011-STRING-LEGACY-SLICE-DIRECT-SPLAT-EXPANSION` is verified at local implementation checkpoint `9a8400d0275098d2d60a40695074a43a4c3b1c5b`. Fresh Dart Sass 1.101.0 probes establish exact legacy-global direct `str-slice()` two- and three-item positional-list and keyword-map splats, direct-plus-map binding, later-map override, quoted/unquoted/escaped/interpolated strings, Unicode-scalar/combining/ZWJ slicing, negative indexes, once-only source order, SCSS/indented-Sass behavior, and one global-built-in deprecation warning per successful call. Native dispatch selects the reflected path's evaluated splat expansion, canonical binder, global callable ownership, and bounded slice kernel only for legacy slice calls containing a syntactic splat; ordinary legacy calls and every sibling direct built-in retain their prior paths. Empty, short, and overfull splats, unknown or non-string map keys, positional/map duplicates, expanded argument ceilings, invalid values, deferred calculations, and allocation paths expose no CSS. The test-first focused Debug gate stopped at 543/545 on the former `SplatUnsupported` boundary before final focused native Debug/ReleaseSafe passed 23/23 steps and 545/545 tests and full compiler Debug/ReleaseSafe passed 60/60 steps and 1040/1040 tests. Evaluator no-emission cross-compiles pass for Windows x64 GNU and Linux ARM64 GNU in both modes; native contract 12/12 plus check, provider/reference/product 151/151, package policy 6/6 plus check, runner selftest 59/59, formatting, syntax, JSON, and diff gates pass. Legacy `str-insert()` direct splats plus nondeterministic `sass:string.unique-id()` remain unavailable. Production reachability, other direct splats, native graduation, release state, and external systems are unchanged.

`NSASS-011-STRING-LEGACY-INDEX-DIRECT-SPLAT-EXPANSION` is verified at local implementation checkpoint `0d7a23e9090b839fda504bdc2ddc2b139bf88241`. Fresh Dart Sass 1.101.0 probes establish exact legacy-global direct `str-index()` two-item positional-list and keyword-map splats, direct-plus-map binding, later-map override, quoted/unquoted/escaped/interpolated strings, Unicode-scalar indexing, nullable misses, once-only source order, SCSS/indented-Sass behavior, and one global-built-in deprecation warning per successful call. Native dispatch selects the reflected path's evaluated splat expansion, canonical binder, global callable ownership, and bounded index kernel only for legacy index calls containing a syntactic splat; ordinary legacy calls and every sibling direct built-in retain their prior paths. Empty, short, and overfull splats, unknown or non-string map keys, positional/map duplicates, expanded argument ceilings, invalid values, deferred calculations, and allocation paths expose no CSS. The test-first focused Debug gate stopped at 542/544 on the former `SplatUnsupported` boundary before final focused native Debug/ReleaseSafe passed 23/23 steps and 544/544 tests and full compiler Debug/ReleaseSafe passed 60/60 steps and 1039/1039 tests. Evaluator no-emission cross-compiles pass for Windows x64 GNU and Linux ARM64 GNU in both modes; native contract 12/12 plus check, provider/reference/product 151/151, package policy 6/6 plus check, runner selftest 59/59, formatting, syntax, JSON, and diff gates pass. Legacy `str-slice()` and `str-insert()` direct splats plus nondeterministic `sass:string.unique-id()` remain unavailable. Production reachability, other direct splats, native graduation, release state, and external systems are unchanged.

`NSASS-011-STRING-LEGACY-LENGTH-DIRECT-SPLAT-EXPANSION` is verified at local implementation checkpoint `4b142d4ef46dcbbe2bff6aa7aebf464b2553efba`. Fresh Dart Sass 1.101.0 probes establish exact legacy-global direct `str-length()` positional-list and keyword-map splats, later-map override, quoted/unquoted/escaped/interpolated strings, Unicode-scalar length, once-only source order, SCSS/indented-Sass behavior, and one global-built-in deprecation warning per successful call. Native dispatch selects the reflected path's evaluated splat expansion, canonical binder, global callable ownership, and bounded length kernel only for legacy length calls containing a syntactic splat; ordinary legacy calls and every sibling direct built-in retain their prior paths. Empty and overfull splats, unknown or non-string map keys, positional/map duplicates, expanded argument ceilings, non-string values, deferred calculations, and allocation paths expose no CSS. The test-first focused Debug gate stopped at 541/543 on the former `SplatUnsupported` boundary before final focused native Debug/ReleaseSafe passed 23/23 steps and 543/543 tests and full compiler Debug/ReleaseSafe passed 60/60 steps and 1038/1038 tests. Evaluator no-emission cross-compiles pass for Windows x64 GNU and Linux ARM64 GNU in both modes; native contract 12/12 plus check, provider/reference/product 151/151, package policy 6/6 plus check, runner selftest 59/59, formatting, syntax, JSON, and diff gates pass. Nondeterministic `sass:string.unique-id()` remains unavailable. Production reachability, other direct splats, native graduation, release state, and external systems are unchanged.

`NSASS-011-STRING-LEGACY-LOWER-CASE-DIRECT-SPLAT-EXPANSION` is verified at local implementation checkpoint `6a746e79e78fb74f1db591cab21df27da93819ce`. Fresh Dart Sass 1.101.0 probes establish exact legacy-global direct `to-lower-case()` positional-list and keyword-map splats, later-map override, quoted/unquoted/escaped/interpolated strings, ASCII-only conversion with Unicode bytes preserved, once-only source order, SCSS/indented-Sass behavior, and one global-built-in deprecation warning per successful call. Native dispatch selects the reflected path's evaluated splat expansion, canonical binder, global callable ownership, and bounded lower-case kernel only for legacy lower-case calls containing a syntactic splat; ordinary legacy calls and every sibling direct built-in retain their prior paths. Empty and overfull splats, unknown or non-string map keys, positional/map duplicates, expanded argument ceilings, and allocation paths expose no CSS. The test-first focused Debug gate stopped at 540/542 on the former `SplatUnsupported` boundary before final focused native Debug/ReleaseSafe passed 23/23 steps and 542/542 tests and full compiler Debug/ReleaseSafe passed 60/60 steps and 1037/1037 tests. Evaluator no-emission cross-compiles pass for Windows x64 GNU and Linux ARM64 GNU in both modes; native contract 12/12 plus check, provider/reference/product 151/151, package policy 6/6 plus check, runner selftest 59/59, formatting, syntax, JSON, and diff gates pass. Nondeterministic `sass:string.unique-id()` remains unavailable. Production reachability, other direct splats, native graduation, release state, and external systems are unchanged.

`NSASS-011-STRING-LEGACY-UPPER-CASE-DIRECT-SPLAT-EXPANSION` is verified at local implementation checkpoint `ecd81b59cae2d0f718a5d439d652527fc730acab`. Fresh Dart Sass 1.101.0 probes establish exact legacy-global direct `to-upper-case()` positional-list and keyword-map splats, later-map override, quoted/unquoted/escaped/interpolated strings, ASCII-only conversion with Unicode bytes preserved, once-only source order, SCSS/indented-Sass behavior, and one global-built-in deprecation warning per successful call. Native dispatch selects the reflected path's evaluated splat expansion, canonical binder, global callable ownership, and bounded upper-case kernel only for legacy upper-case calls containing a syntactic splat; ordinary legacy calls and every sibling direct built-in retain their prior paths. Empty and overfull splats, unknown or non-string map keys, positional/map duplicates, expanded argument ceilings, and allocation paths expose no CSS. The test-first focused Debug gate stopped at 539/541 on the former `SplatUnsupported` boundary before final focused native Debug/ReleaseSafe passed 23/23 steps and 541/541 tests and full compiler Debug/ReleaseSafe passed 60/60 steps and 1036/1036 tests. Evaluator no-emission cross-compiles pass for Windows x64 GNU and Linux ARM64 GNU in both modes; native contract 12/12 plus check, provider/reference/product 151/151, package policy 6/6 plus check, runner selftest 59/59, formatting, syntax, JSON, and diff gates pass. Nondeterministic `sass:string.unique-id()` remains unavailable. Production reachability, other direct splats, native graduation, release state, and external systems are unchanged.

`NSASS-011-STRING-LEGACY-QUOTE-DIRECT-SPLAT-EXPANSION` is verified at local implementation checkpoint `c6d36205f7b38d7e4cc5936cb0d25b36cf14aa2c`. Fresh Dart Sass 1.101.0 probes establish exact legacy-global direct `quote()` positional-list and keyword-map splats, later-map override, quoted/unquoted/escaped/interpolated strings, once-only source order, SCSS/indented-Sass behavior, and one global-built-in deprecation warning per successful call. Native dispatch selects the reflected path's evaluated splat expansion, canonical binder, global callable ownership, and bounded quote kernel only for legacy quote calls containing a syntactic splat; ordinary legacy calls and every sibling direct built-in retain their prior paths. Empty and overfull splats, unknown or non-string map keys, expanded argument ceilings, and allocation paths expose no CSS. The test-first focused Debug gate stopped at 538/540 on the former `SplatUnsupported` boundary before final focused native Debug/ReleaseSafe passed 23/23 steps and 540/540 tests and full compiler Debug/ReleaseSafe passed 60/60 steps and 1035/1035 tests. Evaluator no-emission cross-compiles pass for Windows x64 GNU and Linux ARM64 GNU in both modes; native contract 12/12 plus check, provider/reference/product 151/151, package policy 6/6 plus check, runner selftest 59/59, formatting, syntax, JSON, and diff gates pass. Nondeterministic `sass:string.unique-id()` remains unavailable. Production reachability, other direct splats, native graduation, release state, and external systems are unchanged.

`NSASS-011-STRING-LEGACY-UNQUOTE-DIRECT-SPLAT-EXPANSION` is verified at local implementation checkpoint `c3c4f74d2e935f7c4fdb656f6f7e472f5127dd61`. Fresh Dart Sass 1.101.0 probes establish exact legacy-global direct `unquote()` positional-list and keyword-map splats, later-map override, quoted/escaped/interpolated strings, once-only source order, SCSS/indented-Sass behavior, and one exact global-built-in deprecation warning per successful call. Native dispatch selects the reflected path's evaluated splat expansion, canonical binder, global callable ownership, and bounded unquote kernel only for legacy unquote calls containing a syntactic splat; ordinary legacy calls and every sibling direct built-in retain their prior paths. Empty and overfull splats, unknown or non-string map keys, expanded argument ceilings, and allocation paths expose no CSS. The test-first focused Debug gate stopped at 537/539 on the former `SplatUnsupported` boundary before final focused native Debug/ReleaseSafe passed 23/23 steps and 539/539 tests and full compiler Debug/ReleaseSafe passed 60/60 steps and 1034/1034 tests. Evaluator no-emission cross-compiles pass for Windows x64 GNU and Linux ARM64 GNU in both modes; native contract 12/12 plus check, provider/reference/product 151/151, package policy 6/6 plus check, runner selftest 59/59, formatting, syntax, JSON, and diff gates pass. Nondeterministic `sass:string.unique-id()` remains unavailable. Production reachability, other direct splats, native graduation, release state, and external systems are unchanged.

`NSASS-011-STRING-MODULE-UNQUOTE-DIRECT-SPLAT-EXPANSION` is verified at local implementation checkpoint `ef4eae861d60678a7f1579a242b6d82e0da2d725`. Fresh Dart Sass 1.101.0 probes establish exact module-owned direct `sass:string.unquote()` default/alias/unprefixed access, one-item positional-list and keyword-map splats, later-map override, quoted/unquoted/escaped/interpolated strings, once-only source order, SCSS/indented-Sass behavior, and zero warnings. Native module-owned direct unquote dispatch alone now reuses the reflected path's evaluated splat expansion, canonical binder, and bounded unquote kernel; legacy-global unquote direct splats retain their raw unsupported boundary. Empty and overfull splats, unknown or non-string map keys, positional/map duplicates, expanded argument ceilings, and allocation paths expose no CSS. The test-first focused Debug gate stopped at 536/538 with both new tests at the former unsupported boundary before final focused native Debug/ReleaseSafe passed 23/23 steps and 538/538 tests and full compiler Debug/ReleaseSafe passed 60/60 steps and 1033/1033 tests. Evaluator no-emission cross-compiles pass for Windows x64 GNU and Linux ARM64 GNU in both modes; native contract 12/12 plus check, provider/reference/product 151/151, package policy 6/6 plus check, runner selftest 59/59, formatting, syntax, JSON, and diff gates pass. Nondeterministic `sass:string.unique-id()` remains unavailable. Production reachability, other direct dispatch, native graduation, release state, and external systems are unchanged.

`NSASS-011-STRING-MODULE-QUOTE-DIRECT-SPLAT-EXPANSION` is verified at local implementation checkpoint `dcb2576b1e3ef21f19d3507f0270d9704d8f7d8e`. Fresh Dart Sass 1.101.0 probes establish exact module-owned direct `sass:string.quote()` default/alias/unprefixed access, one-item positional-list and keyword-map splats, later-map override, quoted/unquoted/escaped/interpolated strings, once-only source order, SCSS/indented-Sass behavior, and zero warnings. Native module-owned direct quote dispatch alone now reuses the reflected path's evaluated splat expansion, canonical binder, and bounded quote kernel; legacy-global quote direct splats retain their raw unsupported boundary. Empty and overfull splats, unknown or non-string map keys, positional/map duplicates, expanded argument ceilings, and allocation paths expose no CSS. The test-first positive and negative filtered Debug gates each stopped 0/1 at the former unsupported boundary before final focused native Debug/ReleaseSafe passed 23/23 steps and 537/537 tests and full compiler Debug/ReleaseSafe passed 60/60 steps and 1032/1032 tests. Evaluator no-emission cross-compiles pass for Windows x64 GNU and Linux ARM64 GNU in both modes; native contract 12/12 plus check, provider/reference/product 151/151, package policy 6/6 plus check, runner selftest 59/59, formatting, syntax, JSON, and diff gates pass. Nondeterministic `sass:string.unique-id()` remains unavailable. Production reachability, other direct splats, native graduation, release state, and external systems are unchanged.

`NSASS-011-STRING-MODULE-LOWER-CASE-DIRECT-SPLAT-EXPANSION` is verified at local implementation checkpoint `a2e73a6156345418ee20b8c4abc8113dbda74dee`. Fresh Dart Sass 1.101.0 probes establish exact module-owned direct `sass:string.to-lower-case()` default/alias/unprefixed access, one-item positional-list and keyword-map splats, later-map override, ASCII-only conversion with Unicode, combining-mark, and ZWJ bytes preserved, once-only source order, SCSS/indented-Sass behavior, and zero warnings. Native module-owned direct lower-case dispatch alone now reuses the reflected path's evaluated splat expansion, canonical binder, and bounded case-conversion kernel; legacy-global lower-case direct splats retain their raw unsupported boundary. Empty and overfull splats, unknown or non-string map keys, positional/map duplicates, expanded argument ceilings, and allocation paths expose no CSS. The test-first filtered Debug gate stopped 0/1 at the former unsupported boundary before final focused native Debug/ReleaseSafe passed 23/23 steps and 536/536 tests and full compiler Debug/ReleaseSafe passed 60/60 steps and 1031/1031 tests. Evaluator no-emission cross-compiles pass for Windows x64 GNU and Linux ARM64 GNU in both modes; native contract 12/12 plus check, provider/reference/product 151/151, package policy 6/6 plus check, runner selftest 59/59, formatting, syntax, JSON, and diff gates pass. Nondeterministic `sass:string.unique-id()` remains unavailable. Production reachability, other direct splats, native graduation, release state, and external systems are unchanged.

`NSASS-011-STRING-MODULE-UPPER-CASE-DIRECT-SPLAT-EXPANSION` is verified at local implementation checkpoint `d9133550a6233e833fda40f3b728784c1b69a4d4`. Fresh Dart Sass 1.101.0 probes establish exact module-owned direct `sass:string.to-upper-case()` default/alias/unprefixed access, one-item positional-list and keyword-map splats, later-map override, ASCII-only conversion with Unicode, combining-mark, and ZWJ bytes preserved, once-only source order, SCSS/indented-Sass behavior, and zero warnings. Native module-owned direct upper-case dispatch alone now reuses the reflected path's evaluated splat expansion, canonical binder, and bounded case-conversion kernel; legacy-global upper-case and module-owned lower-case direct splats retain their raw unsupported boundaries. Empty and overfull splats, unknown or non-string map keys, positional/map duplicates, expanded argument ceilings, and allocation paths expose no CSS. The test-first positive and negative filtered Debug gates each stopped 0/1 at the former unsupported boundary before final focused native Debug/ReleaseSafe passed 23/23 steps and 535/535 tests and full compiler Debug/ReleaseSafe passed 60/60 steps and 1030/1030 tests. Evaluator no-emission cross-compiles pass for Windows x64 GNU and Linux ARM64 GNU in both modes; native contract 12/12 plus check, provider/reference/product 151/151, package policy 6/6 plus check, runner selftest 59/59, formatting, syntax, JSON, and diff gates pass. Nondeterministic `sass:string.unique-id()` remains unavailable. Production reachability, other direct splats, native graduation, release state, and external systems are unchanged.

`NSASS-011-STRING-MODULE-INSERT-DIRECT-SPLAT-EXPANSION` is verified at local implementation checkpoint `d11b4f7af81c54628db488edb79a0a6d00d359ce`. Fresh Dart Sass 1.101.0 probes establish exact module-owned direct `sass:string.insert()` default/alias/unprefixed access, three-item positional-list and keyword-map splats, direct-plus-map binding, provider-compatible later-map override, Unicode-scalar insertion, once-only source order, SCSS/indented-Sass behavior, and zero warnings. Native module-owned direct insert dispatch alone now reuses the reflected path's evaluated splat expansion, canonical binder, and bounded string kernel; legacy-global `str-insert()` retains its raw unsupported direct-splat boundary and other direct string functions retain their former binders. Empty, short, and overfull splats, unknown or non-string map keys, positional/map duplicates, expanded argument ceilings, and allocation paths expose no CSS. The test-first focused Debug gate stopped at 532/534 on the former unsupported boundary before final focused native Debug/ReleaseSafe passed 23/23 steps and 534/534 tests and full compiler Debug/ReleaseSafe passed 60/60 steps and 1029/1029 tests. Evaluator no-emission cross-compiles pass for Windows x64 GNU and Linux ARM64 GNU in both modes; native contract 12/12 plus check, provider/reference/product 151/151, package policy 6/6 plus check, runner selftest 59/59, formatting, syntax, JSON, and diff gates pass. Nondeterministic `sass:string.unique-id()` remains unavailable. Production reachability, legacy-global and other direct splats, native graduation, release state, and external systems are unchanged.

`NSASS-011-STRING-MODULE-SLICE-DIRECT-SPLAT-EXPANSION` is verified at local implementation checkpoint `55680fc088ce31b981094849e2418f3fca525da5`. Fresh Dart Sass 1.101.0 probes establish exact module-owned direct `sass:string.slice()` default/alias/unprefixed access, two- and three-item positional-list and keyword-map splats, direct-plus-map binding, provider-compatible later-map override, Unicode-scalar slicing, once-only source order, SCSS/indented-Sass behavior, and zero warnings. Native module-owned direct slice dispatch alone now reuses the reflected path's evaluated splat expansion, canonical binder, and bounded string kernel; legacy-global `str-slice()` retains its raw unsupported direct-splat boundary and other direct string functions retain their former binders. Empty, short, and overfull splats, unknown or non-string map keys, positional/map duplicates, expanded argument ceilings, and allocation paths expose no CSS. The test-first focused Debug gate stopped at 531/533 on the former unsupported boundary before final focused native Debug/ReleaseSafe passed 23/23 steps and 533/533 tests and full compiler Debug/ReleaseSafe passed 60/60 steps and 1028/1028 tests. Evaluator no-emission cross-compiles pass for Windows x64 GNU and Linux ARM64 GNU in both modes; native contract 12/12 plus check, provider/reference/product 151/151, package policy 6/6 plus check, runner selftest 59/59, formatting, syntax, JSON, and diff gates pass. Nondeterministic `sass:string.unique-id()` remains unavailable. Production reachability, legacy-global and other direct splats, native graduation, release state, and external systems are unchanged.

`NSASS-011-STRING-MODULE-INDEX-DIRECT-SPLAT-EXPANSION` is verified at local implementation checkpoint `980608175ef020af6cc2a107cc5e085a93e4051f`. Fresh Dart Sass 1.101.0 probes establish exact module-owned direct `sass:string.index()` default/alias/unprefixed access, positional-list and keyword-map splats, direct-plus-map binding, provider-compatible later-map override, Unicode-scalar indexing, once-only source order, SCSS/indented-Sass behavior, and zero warnings. Native module-owned direct index dispatch alone now reuses the reflected path's evaluated splat expansion, canonical binder, and bounded string kernel; legacy-global `str-index()` and other direct string functions retain their former raw binder. Empty, short, and overfull splats, unknown or non-string map keys, positional/map duplicates, expanded argument ceilings, and allocation paths expose no CSS. The test-first focused Debug gate stopped at 530/532 on the former unsupported boundary before final focused native Debug/ReleaseSafe passed 23/23 steps and 532/532 tests and full compiler Debug/ReleaseSafe passed 60/60 steps and 1027/1027 tests. Root-module no-emission cross-compiles pass for Windows x64 GNU and Linux ARM64 GNU in both modes; native contract 12/12 plus check, provider/reference/product 151/151, package policy 6/6 plus check, runner selftest 59/59, formatting, syntax, JSON, and diff gates pass. Direct `string.slice()` splats and nondeterministic `sass:string.unique-id()` remain unavailable. Production reachability, splats for other functions, native graduation, release state, and external systems are unchanged.

`NSASS-011-STRING-MODULE-LENGTH-DIRECT-SPLAT-EXPANSION` is verified at local implementation checkpoint `9f5a82c266abd3b2e6df83a907ad00ba67cc42a2`. Fresh Dart Sass 1.101.0 probes establish exact module-owned direct `sass:string.length()` default/alias/unprefixed access, positional-list and keyword-map splats, provider-compatible later-map override, once-only source order, SCSS/indented-Sass behavior, and zero warnings. Native module-owned direct length dispatch alone now reuses the reflected path's evaluated splat expansion, canonical binder, and bounded string kernel; legacy-global `str-length()` and other direct string functions retain their former raw binder. Empty and overfull splats, unknown or non-string map keys, positional/map duplicates, expanded argument ceilings, and allocation paths expose no CSS. The test-first focused Debug gate stopped at 529/531 on the former unsupported boundary before final focused native Debug/ReleaseSafe passed 23/23 steps and 531/531 tests and full compiler Debug/ReleaseSafe passed 60/60 steps and 1026/1026 tests. Evaluator no-emission cross-compiles pass for Windows x64 GNU and Linux ARM64 GNU in both modes; native contract 12/12 plus check, provider/reference/product 151/151, package policy 6/6 plus check, runner selftest 59/59, formatting, syntax, JSON, and diff gates pass. Nondeterministic `sass:string.unique-id()` remains unavailable. Production reachability, splats for other functions, native graduation, release state, and external systems are unchanged.

`NSASS-011-STRING-SPLIT-DIRECT-SPLAT-EXPANSION` is verified at local implementation checkpoint `3a409222cc9c7bdb3d7066e07be46f0f354a3d7f`. Fresh Dart Sass 1.101.0 probes establish exact direct positional-list and keyword-map splats for the already-graduated `sass:string.split()` callable, including direct-plus-map binding, provider-compatible later-map override, once-only source order, SCSS/indented-Sass behavior, and zero warnings. Native direct split dispatch alone now reuses the reflected path's evaluated splat expansion, canonical binder, and bounded shared kernel; other direct built-ins remain unchanged. Empty and overfull splats, unknown or non-string map keys, expanded argument ceilings, and allocation paths expose no CSS. The test-first focused Debug gate stopped at 529/530 with `SplatUnsupported` before final focused native Debug/ReleaseSafe passed 23/23 steps and 530/530 tests and full compiler Debug/ReleaseSafe passed 60/60 steps and 1025/1025 tests. Evaluator no-emission cross-compiles pass for Windows x64 GNU and Linux ARM64 GNU in both modes; native contract 12/12 plus check, provider/reference/product 151/151, package policy 6/6 plus check, runner selftest 59/59, formatting, syntax, JSON, and diff gates pass. Nondeterministic `sass:string.unique-id()` remains unavailable. Production reachability, splats for other functions, native graduation, release state, and external systems are unchanged.

`NSASS-011-META-STRING-SPLIT-FUNCTION-INVOCATION` is verified at local implementation checkpoint `791b0dc60d6b1d8b019aacfdb26c04ebfa74d763`. Fresh Dart Sass 1.101.0 probes establish exact direct/reflected `sass:string.split()` identity, aliases and unprefixed access, the closed legacy-global boundary, reflected positional/named/list/map-splat binding, once-only source order, bracketed comma-list metadata, quoted/unquoted item typing, decoded non-overlapping matching, Unicode/combining/ZWJ/escape behavior, empty/consecutive/leading/trailing/absent separators, empty-input and empty-separator semantics, and nullable fuzzy-positive integer limits with ignored units. Native direct and `meta.call()` dispatch share one bounded kernel and canonical binders; invalid binding/value/module/limit, deferred calculations, resource ceilings, and allocation paths expose no CSS. The test-first gate stopped at unavailable split dispatch before final kernel tests passed 5/5, filtered evaluator tests passed 3/3, focused native Debug/ReleaseSafe passed 23/23 steps and 529/529 tests, and full compiler Debug/ReleaseSafe passed 60/60 steps and 1024/1024 tests. Root-module no-emission cross-compiles pass for Windows x64 GNU and Linux ARM64 GNU in both modes; native contract 12/12 plus check, provider/reference/product 151/151, package policy 6/6 plus check, runner selftest 59/59, formatting, syntax, JSON, and diff gates pass. Nondeterministic `sass:string.unique-id()` remains unavailable. Production reachability, broader Sass semantics, native graduation, release state, and external systems are unchanged.

`NSASS-011-META-COLOR-IS-POWERLESS-FUNCTION-INVOCATION` is verified at local implementation checkpoint `b380a51d1dba6bf53fe0fc6803a07af4cd64c75c`. Fresh Dart Sass 1.101.0 probes establish exact direct/reflected `sass:color.is-powerless()` callable identity, aliases and unprefixed access, the closed legacy-global boundary, positional/named/list/map-splat binding, once-only source order, boolean typing, quoted case-sensitive channels, and exact HSL, HWB, LCH, and OKLCH hue-powerlessness semantics across all 15 represented spaces. Native direct and `meta.call()` dispatch share one allocation-free typed kernel with the provider's absolute `5e-12` boundary; already-represented missing modern chroma is stored zero, while unsupported legacy missing-channel constructors remain fail-closed. Invalid binding/value/module/channel, deferred values, argument ceilings, and allocation paths expose no CSS. The test-first focused Debug gate stopped exactly at the absent kernel entry point before final direct evaluator Debug/ReleaseSafe passed 388/388, focused native modes passed 23/23 steps and 525/525 tests, and full compiler modes passed 60/60 steps and 1020/1020 tests. Root-module no-emission cross-compiles pass for Windows x64 GNU and Linux ARM64 GNU in both modes; native contract 12/12 plus check, provider/reference/product 151/151, package policy 6/6 plus check, runner selftest 59/59, formatting, syntax, JSON, and diff gates pass. Sibling oracle probing leaves nondeterministic `sass:string.unique-id()` unavailable and designates deterministic `sass:string.split()` next. Production reachability, broader missing-channel parsing, native graduation, release state, and external systems are unchanged.

`NSASS-011-META-COLOR-SAME-FUNCTION-INVOCATION` is verified at local implementation checkpoint `cfd27ca492060707bb71a3b5837e9f51ce2b871d`. Fresh Dart Sass 1.101.0 probes establish exact direct and reflected `sass:color.same()` callable identity, aliases and unprefixed access, the closed legacy-global boundary, positional/named/list/map-splat binding, once-only source order, boolean typing, SCSS/indented-Sass behavior, same-space comparison, different-space XYZ D65 conversion, missing-channel zero substitution including alpha, exact fuzzy boundaries, and retained powerless HSL/LCH hue significance across all 15 represented spaces. Native direct and `meta.call()` dispatch share one allocation-free typed comparison kernel while the existing Sass `==` implementation remains unchanged. Invalid binding/value/module, deferred values, argument ceilings, unavailable `is-powerless`, and allocation paths fail without CSS. The test-first focused Debug gate stopped at 520/521 exactly at the absent direct dispatch before final direct evaluator Debug/ReleaseSafe passed 386/386, focused native modes passed 23/23 steps and 522/522 tests, and full compiler modes passed 60/60 steps and 1017/1017 tests. Root-module no-emission cross-compiles pass for Windows x64 GNU and Linux ARM64 GNU in both modes; native contract 12/12 plus check, provider/reference/product 151/151, package policy 6/6 plus check, runner selftest 59/59, formatting, syntax, JSON, and diff gates pass. Production reachability, analogous missing-channel propagation, native graduation, release state, and external systems are unchanged.

`NSASS-011-META-COLOR-CHANNEL-FUNCTION-INVOCATION` is verified at local implementation checkpoint `2a68222f92072f031e788f5fb762ec5cc2b2afa9`. Fresh Dart Sass 1.101.0 probes establish exact direct/reflected `sass:color.channel()` callable identity, aliases and unprefixed access, the closed legacy-global boundary, positional/named/list/map-splat binding, once-only source order, numeric typing, quoted case-sensitive escaped/interpolated channel names, nullable case-insensitive target spaces, XYZ aliasing, and exact unitless/degree/percentage results for stored and converted colors across all 15 represented spaces. Native direct and `meta.call()` dispatch share the canonical evaluated binder, quoted-string decoder, and allocation-free conversion core. Missing-channel values remain fail-closed rather than introducing the provider's unimplemented zero substitution and analogous-channel propagation. Invalid binding/value/module/channel/space, deferred values, argument and temporary ceilings, missing channels, and allocation paths fail without CSS; `same` and `is-powerless` remain unavailable. The test-first focused Debug gate stopped at 518/519 exactly at the absent direct dispatch before final direct evaluator Debug/ReleaseSafe passed 384/384, focused native modes passed 23/23 steps and 519/519 tests, and full compiler modes passed 60/60 steps and 1014/1014 tests. Root-module no-emission cross-compiles pass for Windows x64 GNU and Linux ARM64 GNU in both modes; native contract 12/12 plus check, provider/reference/product 151/151, package policy 6/6 plus check, runner selftest 59/59, formatting, syntax, JSON, and diff gates pass. Production reachability, missing-channel semantics, native graduation, release state, and external systems are unchanged.

`NSASS-011-META-COLOR-TO-GAMUT-FUNCTION-INVOCATION` is verified at local implementation checkpoint `cc31b78381cb7da18f48877c8632db4b891c459d`. Fresh Dart Sass 1.101.0 probes establish exact direct/reflected `sass:color.to-gamut()` callable identity, aliases and unprefixed access, the closed legacy-global boundary, positional/named/list/map-splat binding, once-only source order, color typing, mandatory exact mapping methods, nullable case-insensitive targets, XYZ aliasing, and exact bounded and unbounded results. Native direct and `meta.call()` dispatch share the canonical evaluated binder and allocation-free gamut kernel; bounded targets use channel clipping or deterministic local-MINDE OKLCH chroma reduction before conversion back to the original space, alpha is preserved, and perceptual/XYZ targets are unchanged. Missing-channel inputs and local legacy powerless-hue results remain fail-closed rather than losing provider missing-channel semantics. Invalid binding/value/module/space/method, deferred values, argument ceilings, missing channels, powerless results, and allocation paths fail without CSS. The test-first focused Debug gate stopped at the absent kernel entry point before final direct evaluator Debug/ReleaseSafe passed 382/382, focused native modes passed 23/23 steps and 517/517 tests, and full compiler modes passed 60/60 steps and 1012/1012 tests. Evaluator cross-compiles pass for Windows x64 GNU and Linux ARM64 GNU in both modes; native contract 12/12 plus check, provider/reference/product 151/151, package policy 6/6 plus check, runner selftest 59/59, formatting, syntax, JSON, and diff gates pass. Production reachability, implicit gamut mapping elsewhere, broader missing-channel semantics, native graduation, release state, and external systems are unchanged.

`NSASS-011-META-COLOR-IS-IN-GAMUT-FUNCTION-INVOCATION` is verified at local implementation checkpoint `f5be9899409242b3fa777236d328d928954b7223`. Fresh Dart Sass 1.101.0 probes establish exact direct/reflected `sass:color.is-in-gamut()` callable identity, aliases and unprefixed access, the closed legacy-global boundary, positional/named/list/map-splat binding, once-only source order, boolean typing, nullable target behavior, case-insensitive target names, XYZ aliasing, and exact bounded-space results and fuzzy boundaries across all 15 represented spaces. Native direct and `meta.call()` dispatch share the canonical evaluated binder and allocation-free color conversion/predicate core; RGB-family bounds exclude hue and alpha, perceptual/XYZ spaces are unbounded, stored missing channels are zero, and cross-space missing-channel conversion remains fail-closed without gamut mapping. Invalid binding/value/module/target, deferred values, argument ceilings, missing-channel conversion, and allocation paths fail without CSS. The test-first focused Debug gate stopped at the absent kernel entry point before final direct evaluator Debug/ReleaseSafe passed 380/380, focused native modes passed 23/23 steps and 514/514 tests, and full compiler modes passed 60/60 steps and 1009/1009 tests. Evaluator cross-compiles pass for Windows x64 GNU and Linux ARM64 GNU in both modes; native contract 12/12 plus check, provider/reference/product 151/151, package policy 6/6 plus check, runner selftest 59/59, formatting, syntax, JSON, and diff gates pass. Production reachability, gamut mapping, broader missing-channel semantics, native graduation, release state, and external systems are unchanged.

`NSASS-011-META-COLOR-IS-MISSING-FUNCTION-INVOCATION` is verified at local implementation checkpoint `8500ed90300887e0df75eee1bc75aea711d856af`. Fresh Dart Sass 1.101.0 probes establish exact direct/reflected `sass:color.is-missing()` callable identity, aliases and unprefixed access, the closed legacy-global boundary, positional/named/list/map-splat binding, escaped/interpolated quoted channel names, once-only source order, boolean typing, and SCSS/indented-Sass behavior. Native direct and `meta.call()` dispatch share the canonical evaluated binder and query the existing stored missing mask using only the exact case-sensitive channels valid for each of all 15 represented spaces. Invalid binding/value/module/channel, deferred values, argument and temporary ceilings, and allocation paths fail without CSS; missing-channel parsing/substitution, conversion, and gamut behavior remain unchanged. The test-first Debug gate stopped at direct evaluator 376/378 and focused native 509/511 before final direct evaluator Debug/ReleaseSafe passed 378/378, focused native modes passed 23/23 steps and 511/511 tests, and full compiler modes passed 60/60 steps and 1006/1006 tests. Evaluator cross-compiles pass for Windows x64 GNU and Linux ARM64 GNU in both modes; native contract 12/12 plus check, provider/reference/product 151/151, package policy 6/6 plus check, runner selftest 59/59, formatting, syntax, JSON, and diff gates pass. Production reachability, color parsing/substitution/conversion/gamut semantics, native graduation, release state, and external systems are unchanged.

`NSASS-011-META-COLOR-IS-LEGACY-FUNCTION-INVOCATION` is verified at local implementation checkpoint `a0c1b5f85c07ea01033512042990629d1c7b5878`. Fresh Dart Sass 1.101.0 probes establish exact direct/reflected `sass:color.is-legacy()` callable identity, aliases and unprefixed access, the closed legacy-global boundary, positional/named/list/map-splat binding, once-only source order, boolean typing, and SCSS/indented-Sass behavior. Native direct and `meta.call()` dispatch share the canonical typed binder and classify RGB, HSL, and HWB as legacy while all represented modern spaces are non-legacy, including converted and already-represented modern missing-channel colors without substitution. Invalid binding/value/module, deferred values, argument ceilings, and allocation paths fail without CSS; legacy missing-channel constructor parsing remains unchanged. The test-first Debug gate stopped 1/2 at the absent callable before final direct evaluator Debug/ReleaseSafe passed 376/376, focused native modes passed 23/23 steps and 509/509 tests, and full compiler modes passed 60/60 steps and 1004/1004 tests. Evaluator cross-compiles pass for Windows x64 GNU and Linux ARM64 GNU in both modes; native contract 12/12 plus check, provider/reference/product 151/151, package policy 6/6 plus check, runner selftest 59/59, formatting, syntax, JSON, and diff gates pass. Production reachability, missing-channel parsing/substitution, gamut semantics, native graduation, release state, and external systems are unchanged.

`NSASS-011-META-COLOR-TO-SPACE-FUNCTION-INVOCATION` is verified at local implementation checkpoint `c9470334dcbf6d00217a4cc3392e92ef30b829f7`. Fresh Dart Sass 1.101.0 probes establish exact direct/reflected `sass:color.to-space()` callable identity, alias and unprefixed access, the closed legacy-global boundary, positional/named/list/map-splat binding, once-only source order, SCSS/indented-Sass behavior, case-insensitive target identifiers, XYZ canonicalization, and exact conversion into all 15 represented spaces with alpha preservation. Native direct and `meta.call()` dispatch share the canonical typed binder and allocation-free conversion core; computed-color provenance survives nested identity conversion and retains provider serialization. Invalid binding/value/module, quoted or unknown targets, deferred values, missing channels, argument ceilings, and allocation paths fail without CSS; missing-channel substitution and gamut mapping remain unavailable. The test-first baseline stopped at direct evaluator 373/374 and focused native 506/507 before final direct evaluator Debug/ReleaseSafe passed 374/374, focused native modes passed 23/23 steps and 507/507 tests, and full compiler modes passed 60/60 steps and 1002/1002 tests. Evaluator cross-compiles pass for Windows x64 GNU and Linux ARM64 GNU in both modes; native contract 12/12 plus check, provider/reference/product 151/151, package policy 6/6 plus check, runner selftest 59/59, formatting, syntax, JSON, and diff gates pass. Production reachability, missing-channel and gamut semantics, native graduation, release state, and external systems are unchanged.

`NSASS-011-META-COLOR-SPACE-FUNCTION-INVOCATION` is verified at local implementation checkpoint `96a04da2b35ad93e8f7a2ef1666cef9da8dddded`. Fresh Dart Sass 1.101.0 probes establish exact direct/reflected `sass:color.space()` callable identity, alias and unprefixed access, the closed legacy-global boundary, positional/named/list/map-splat binding, once-only source order, zero-warning SCSS/indented-Sass behavior, unquoted string typing, and all 15 represented stored color-space identities with canonical `xyz` for XYZ D65. Native direct and `meta.call()` dispatch share one typed color-only implementation and canonical binder without conversion or gamut mapping. Invalid binding/value/module, deferred values, missing channels, argument ceilings, and allocation paths fail without CSS; `to-space` remains unavailable. The test-first filtered Debug gate stopped 1/2 at the absent callable before final direct evaluator Debug/ReleaseSafe passed 372/372, focused native modes passed 23/23 steps and 505/505 tests, and full compiler modes passed 60/60 steps and 1000/1000 tests. Evaluator cross-compiles pass for Windows x64 GNU and Linux ARM64 GNU in both modes; native contract 12/12 plus check, provider/reference/product 151/151, package policy 6/6 plus check, runner selftest 59/59, formatting, syntax, JSON, and diff gates pass. Production reachability, color conversion, missing-channel and gamut semantics, native graduation, release state, and external systems are unchanged.

`NSASS-011-META-MODULE-FUNCTIONS-FUNCTION-INVOCATION` is verified at local implementation checkpoint `6ce2d4344b9a68de7f2950f54467ace15413d0c5`. Fresh Dart Sass 1.101.0 probes establish exact direct/reflected built-in module function enumeration, module/global identity and warning ownership, aliases and unprefixed access, positional/named/list/map-splat binding, once-only source order, SCSS/indented-Sass behavior, and the complete ordered 116-reference maps across all seven built-in modules. One centralized native inventory resolves only loaded built-in namespaces, charges each bounded operation and entry, and owns the result map without filesystem access or user modules. The nine newly enumerable color functions and two newly enumerable string functions without implemented semantics remain non-invocable through direct and reflected calls. Invalid binding/value/namespace, argument and collection ceilings, unsupported calls, and allocation paths fail without CSS. The test-first Debug gate stopped at direct evaluator 368/370 and focused native 501/503 before final direct evaluator Debug/ReleaseSafe passed 370/370, focused native modes passed 23/23 steps and 503/503 tests, and full compiler modes passed 60/60 steps and 998/998 tests. Evaluator cross-compiles pass for Windows x64 GNU and Linux ARM64 GNU in both modes; native contract 12/12 plus check, provider/reference/product 151/151, package policy 6/6 plus check, runner selftest 59/59, formatting, syntax, JSON, and diff gates pass. Production reachability, unsupported function invocation, dynamic loading, native graduation, release state, and external systems are unchanged.

`NSASS-011-META-MODULE-VARIABLES-FUNCTION-INVOCATION` is verified at local implementation checkpoint `5acabc98ed7e691553b5a901d12251b04e7fae40`. Fresh Dart Sass 1.101.0 probes establish exact direct/reflected built-in module variable enumeration, module/global identity and warning ownership, aliases and unprefixed access, positional/named/list/map-splat binding, once-only source order, SCSS/indented-Sass behavior, empty maps for six built-in modules, and the ordered seven-value `sass:math` map. Native dispatch resolves only loaded built-in namespaces, charges one bounded static inventory, owns the result map, and carries IEEE precision provenance through cloning and later arithmetic. Invalid binding/value/namespace, argument and collection ceilings, and allocation paths fail without CSS. The test-first filtered Debug gate stopped 0/2 at the absent callable before final direct evaluator Debug/ReleaseSafe passed 368/368, focused native modes passed 23/23 steps and 501/501 tests, and full compiler modes passed 60/60 steps and 996/996 tests. Numeric units pass 8/8; evaluator cross-compiles pass for Windows x64 GNU and Linux ARM64 GNU in both modes; native contract 12/12 plus check, provider/reference/product 151/151, package policy 6/6 plus check, runner selftest 59/59, formatting, syntax, JSON, and diff gates pass. Production reachability, module-function enumeration, dynamic loading, native graduation, release state, and external systems are unchanged.

`NSASS-011-META-MODULE-MIXINS-FUNCTION-INVOCATION` is verified at local implementation checkpoint `69d011060418ab0d322f4cea3d7ba67369a45f91`. Fresh Dart Sass 1.101.0 probes establish exact direct/reflected built-in module mixin enumeration, module/global identity and warning ownership, aliases and unprefixed access, positional/named/list/map-splat binding, once-only source order, SCSS/indented-Sass behavior, empty non-mixin module maps, and ordered `sass:meta` `load-css`/`apply` references. Native dispatch resolves only loaded built-in namespaces, charges one bounded static inventory, and owns the result map without resolution or user-module access. Invalid binding/value/namespace, argument and collection ceilings, and allocation paths fail without CSS. The test-first Debug baseline stopped at direct evaluator 364/366 and focused native 497/499 before final Debug/ReleaseSafe each passed 23/23 steps and 499/499 tests. Direct evaluator coverage passes 366/366 in both modes; full compiler Debug/ReleaseSafe each pass 60/60 steps and 994/994 tests; evaluator cross-compiles pass for Windows x64 GNU and Linux ARM64 GNU in both modes; native contract 12/12 plus check, provider/reference/product 151/151, package policy 6/6 plus check, runner selftest 59/59, formatting, syntax, JSON, and diff gates pass. Production reachability, dynamic loading, remaining module enumeration, native graduation, release state, and external systems are unchanged.

`NSASS-011-META-APPLY-MIXIN-APPLICATION` is verified at local implementation checkpoint `dc169186fb6572a28876fe66ec99f3bd4f391329`. Fresh Dart Sass 1.101.0 probes establish reflected `sass:meta` `apply` application through default, aliased, and unprefixed members with positional/list/map-splat forwarding, multiple apply hops, once-only evaluation, parameterized captured content, SCSS/indented-Sass behavior, and no warnings. Native dispatch evaluates one bounded argument list, iteratively consumes only the exact `meta.apply` identity with one operation-budget charge per hop, allocates the terminal forwarded arguments once, and delegates to the existing user-mixin executor. Invalid nested targets, argument limits, and allocation paths fail without CSS; reflected `meta.load-css` stays closed without filesystem or resolver access. The test-first Debug baseline stopped at direct evaluator 362/364 and focused native 495/497 before final Debug/ReleaseSafe each passed 23/23 steps and 497/497 tests. Direct evaluator coverage passes 364/364 in both modes; full compiler Debug/ReleaseSafe each pass 60/60 steps and 992/992 tests; evaluator cross-compiles pass for Windows x64 GNU and Linux ARM64 GNU in both modes; native contract 12/12 plus check, provider/reference/product 151/151, package policy 6/6 plus check, runner selftest 59/59, formatting, syntax, JSON, and diff gates pass. Production reachability, dynamic loading, native graduation, release state, and external systems are unchanged.

`NSASS-011-META-USER-MIXIN-APPLICATION` is verified at local implementation checkpoint `bb14daa08475bb69bf124de687dd4d038641f137`. Fresh Dart Sass 1.101.0 probes establish user-mixin application through default, aliased, and unprefixed `sass:meta` members with positional/named target binding, target positional/named/list/map-splat/rest forwarding, once-only evaluation, parameterized captured content, SCSS/indented-Sass behavior, and no warnings. Native dispatch resolves only loaded built-in module mixin members, evaluates one complete argument list, separates the mixin reference, and delegates remaining arguments plus content to the existing user-mixin executor. Invalid targets, binding, namespace, content, built-in target, argument-limit, and allocation paths fail without CSS; built-in recursion and `meta.load-css()` stay closed. The test-first Debug baseline stopped at direct evaluator 360/362 and focused native 493/495 before final Debug/ReleaseSafe each passed 23/23 steps and 495/495 tests. Direct evaluator coverage passes 362/362 in both modes; full compiler Debug/ReleaseSafe each pass 60/60 steps and 990/990 tests; evaluator cross-compiles pass for Windows x64 GNU and Linux ARM64 GNU in both modes; native contract 12/12 plus check, provider/reference/product 151/151, package policy 6/6 plus check, runner selftest 59/59, formatting, syntax, JSON, and diff gates pass. Production reachability, native graduation, release state, and external systems are unchanged.

`NSASS-011-META-REFLECTED-IF-FUNCTION-INVOCATION` is verified at local implementation checkpoint `c11b932d2a5e8fca0f044533c12b3290fcf3fc98`. Fresh Dart Sass 1.101.0 probes establish that the special legacy `if` function reference is callable through `meta.call()` with one namespaced/aliased/legacy-global identity, exact existence/type/inspection behavior, positional/named/list/map-splat binding, eager once-only evaluation, Sass truthiness, the one legacy-global construction warning, and no direct legacy-`if()` deprecation in SCSS and indented Sass. Native dispatch recognizes only the reserved callable identity, binds the three evaluated arguments canonically, consumes a bounded operation, and returns the selected already-owned value without changing direct lazy `if()` behavior. Missing, extra, unknown, duplicate, eager undefined-variable, argument-limit, and allocation paths fail without CSS. The test-first Debug baseline stopped at direct evaluator 359/360 and focused native 492/493 before final Debug/ReleaseSafe each passed 23/23 steps and 493/493 tests. Direct evaluator coverage passes 360/360 in both modes; full compiler Debug/ReleaseSafe each pass 60/60 steps and 988/988 tests; evaluator cross-compiles pass for Windows x64 GNU and Linux ARM64 GNU in both modes; native contract 12/12 plus check, provider/reference/product 151/151, package policy 6/6 plus check, runner selftest 59/59, formatting, syntax, JSON, and diff gates pass. Production reachability, native graduation, release state, and external systems are unchanged.

`NSASS-011-META-GET-MIXIN-FUNCTION-INVOCATION` is verified at local implementation checkpoint `f41b05a57050b1052044552090441a5943531b41`. Fresh Dart Sass 1.101.0 probes establish callable module-owned and legacy-global `get-mixin` function references, module alias/underscore/unprefixed identity and global distinction, positional/named/list/map-splat binding, once-only evaluation, explicit-null local lookup, user and built-in mixin resolution, the exact legacy warning owner, SCSS/indented Sass behavior, and closed invalid-binding boundaries. Native dispatch uses the canonical evaluated binder and existing mixin-reference constructor without reevaluating arguments or enabling mixin application. The test-first Debug baseline stopped at direct evaluator 357/358 and focused native 490/491 before final Debug/ReleaseSafe each passed 23/23 steps and 491/491 tests. Direct evaluator coverage passes 358/358 in both modes; full compiler Debug/ReleaseSafe each pass 60/60 steps and 986/986 tests; evaluator cross-compiles pass for Windows x64 GNU and Linux ARM64 GNU in both modes; native contract 12/12 plus check, provider/reference/product 151/151, package policy 6/6 plus check, runner selftest 59/59, formatting, syntax, JSON, and diff gates pass. Production reachability, native graduation, release state, and external systems are unchanged.

`NSASS-011-META-GET-FUNCTION-FUNCTION-INVOCATION` is verified at local implementation checkpoint `34dda1527039755c198a06098c47ff1ef015f5c7`. Fresh Dart Sass 1.101.0 probes establish callable module-owned and legacy-global `get-function` references, module alias/underscore/unprefixed identity and global distinction, positional/named/list/map-splat binding, once-only evaluation, module resolution, returned-callable invocation, the exact legacy warning owner, SCSS/indented Sass behavior, and closed invalid-binding boundaries. Native dispatch uses the canonical evaluated binder and existing `get-function` constructor without reevaluating arguments or widening `$css: true` and reflected `if` behavior. The test-first Debug baseline stopped at direct evaluator 355/356 and focused native 488/489 before final Debug/ReleaseSafe each passed 23/23 steps and 489/489 tests. Direct evaluator coverage passes 356/356 in both modes; full compiler Debug/ReleaseSafe each pass 60/60 steps and 984/984 tests; evaluator cross-compiles pass for Windows x64 GNU and Linux ARM64 GNU in both modes; native contract 12/12 plus check, provider/reference/product 151/151, package policy 6/6 plus check, runner selftest 59/59, formatting, syntax, JSON, and diff gates pass. Production reachability, native graduation, release state, and external systems are unchanged.

`NSASS-011-META-CALL-FUNCTION-INVOCATION` is verified at local implementation checkpoint `d733a1c1c921e4d8577a7c8ea3c4883892d49f85`. Fresh Dart Sass 1.101.0 probes establish callable module-owned and legacy-global `call` references, module alias/escaped/unprefixed identity and global distinction, positional/list-splat forwarding, once-only evaluation, the exact legacy warning owner, SCSS/indented Sass behavior, and closed invalid-binding boundaries. Native dispatch reuses the existing evaluated `meta.call()` implementation without reevaluating arguments. The test-first Debug baseline stopped at direct evaluator 353/354 and focused native 486/487 before final Debug/ReleaseSafe each passed 23/23 steps and 487/487 tests. Direct evaluator coverage passes 354/354 in both modes; full compiler Debug/ReleaseSafe each pass 60/60 steps and 982/982 tests; evaluator cross-compiles pass for Windows x64 GNU and Linux ARM64 GNU in both modes; native contract 12/12 plus check, provider/reference/product 151/151, package policy 6/6 plus check, runner selftest 59/59, formatting, syntax, JSON, and diff gates pass. Production reachability, native graduation, release state, and external systems are unchanged.

`NSASS-011-META-CONTENT-EXISTENCE-FUNCTION-INVOCATION` is verified at local implementation checkpoint `73a406d6c6be48cc43486f814c599557b2b3ba1d`. Fresh Dart Sass 1.101.0 probes establish callable module-owned and legacy-global `content-exists` references, module alias identity and global distinction, exact current-mixin true/false results, named `$function`, empty splats, once-only evaluation, SCSS/indented Sass behavior, and closed context/argument boundaries. Native dispatch reuses the existing direct context-sensitive implementation with canonical zero-argument binding and normalized legacy warnings. The test-first Debug baseline stopped at direct evaluator 351/352 and focused native 484/485 before final Debug/ReleaseSafe each passed 23/23 steps and 485/485 tests. Direct evaluator coverage passes 352/352 in both modes; full compiler Debug/ReleaseSafe each pass 60/60 steps and 980/980 tests; evaluator cross-compiles pass for Windows x64 GNU and Linux ARM64 GNU in both modes; native contract 12/12 plus check, provider/reference/product 151/151, package policy 6/6 plus check, runner selftest 59/59, formatting, syntax, JSON, and diff gates pass. Production reachability, native graduation, release state, and external systems are unchanged.

`NSASS-011-META-CALC-FUNCTION-REFERENCE-REJECTION` is verified at local implementation checkpoint `c949fa80c8dcd243db02df9fed7e609b0268411f`. Fresh Dart Sass 1.101.0 probes establish that `calc()` is directly evaluable but not a Sass function reference: module and legacy existence queries return false, `meta.get-function("calc")` fails, static arithmetic reduces, deferred CSS remains preserved, and only the legacy existence query warns. Native SCSS and indented Sass match those exact bytes and diagnostics. The test-first Debug baseline stopped at direct evaluator 349/350 and focused native 482/483 before final Debug/ReleaseSafe each passed 23/23 steps and 483/483 tests. Direct evaluator coverage passes 350/350 in both modes; full compiler Debug/ReleaseSafe each pass 60/60 steps and 978/978 tests; evaluator cross-compiles pass for Windows x64 GNU and Linux ARM64 GNU in both modes; native contract 12/12 plus check, provider/reference/product 151/151, package policy 6/6 plus check, runner selftest 59/59, formatting, syntax, JSON, and diff gates pass. Production reachability, native graduation, release state, and external systems are unchanged.

`NSASS-011-META-COLOR-IE-HEX-STR-FUNCTION-INVOCATION` is verified at local implementation checkpoint `9b1657baf849add764ca4941310abf3b2cc9b083`. Fresh Dart Sass 1.101.0 probes match native SCSS and indented-Sass `meta.call()` invocation plus direct qualified module invocation for the one shared global/module `ie-hex-str` reference, including global/default/aliased/unprefixed identity, exact required `$color` positional/named binding, list/map splats, transparent and alpha-bearing legacy colors, all twelve represented modern spaces through exact in-gamut fixtures, uppercase `#AARRGGBB` string output, once-only source order, and zero warnings. Invalid bindings and values, missing channels, unloaded modules, argument limits, and allocation failures expose no CSS. Provider-accepted missing-channel substitution and out-of-gamut gamut mapping remain deliberately ungraduated; native typed out-of-gamut values fail closed instead of clipping. The test-first Debug baseline stopped at direct evaluator 347/349 and focused native 479/482 before final Debug/ReleaseSafe each passed 23/23 steps and 482/482 tests. Direct evaluator coverage passes 349/349 in both modes; full compiler Debug/ReleaseSafe each pass 60/60 steps and 977/977 tests; evaluator cross-compiles pass for Windows x64 GNU and Linux ARM64 GNU in both modes; native contract 12/12 plus check, provider/reference/product 151/151, package policy 6/6 plus check, runner selftest 59/59, formatting, syntax, JSON, and diff gates pass. Production reachability, native graduation, release state, and external systems are unchanged.

`NSASS-011-META-COLOR-FADE-OUT-FUNCTION-INVOCATION` is verified at local implementation checkpoint `b3e747ed50fb1918aee87867636d76a8a7df9e5d`. Fresh Dart Sass 1.101.0 probes match native SCSS and indented-Sass `meta.call()` invocation for the distinct legacy-global `fade-out` reference plus constructible-but-removed module-owned `sass:color` references, including global/default/aliased/unprefixed identity, exact required `$color`/`$amount` positional/named binding, list/map splats, transparent and alpha-bearing legacy colors, RGB conversion for HSL/HWB, simple unit-bearing magnitudes, clamping, color typing, once-only source order, and both normalized legacy warnings. Module invocation, direct qualified calls, missing channels, modern spaces, compound/deferred/out-of-range amounts, invalid arguments, unavailable modules, argument limits, and allocation failures expose no CSS; `ie-hex-str` remains unavailable. The test-first Debug baseline stopped at direct evaluator 346/347 and focused native 479/480 before final Debug/ReleaseSafe each passed 23/23 steps and 480/480 tests. Direct evaluator coverage passes 347/347 in both modes; full compiler Debug/ReleaseSafe each pass 60/60 steps and 975/975 tests; evaluator cross-compiles pass for Windows x64 GNU and Linux ARM64 GNU in both modes; native contract 12/12 plus check, provider/reference/product 151/151, package policy 6/6 plus check, runner selftest 59/59, formatting, syntax, JSON, and diff gates pass. Production reachability, native graduation, release state, and external systems are unchanged.

`NSASS-011-META-COLOR-TRANSPARENTIZE-FUNCTION-INVOCATION` is verified at local implementation checkpoint `fef4fe6e91a1f858e839bc77b9d2c36993fd0205`. Fresh Dart Sass 1.101.0 probes match native SCSS and indented-Sass `meta.call()` invocation for the distinct legacy-global `transparentize` reference plus constructible-but-removed module-owned `sass:color` references, including global/default/aliased/unprefixed identity, exact required `$color`/`$amount` positional/named binding, list/map splats, transparent and alpha-bearing legacy colors, RGB conversion for HSL/HWB, simple unit-bearing magnitudes, clamping, color typing, once-only source order, and both normalized legacy warnings. Module invocation, direct qualified calls, missing channels, modern spaces, compound/deferred/out-of-range amounts, invalid arguments, unavailable modules, argument limits, and allocation failures expose no CSS; `fade-out` remains unavailable. The test-first Debug baseline stopped at direct evaluator 344/345 and focused native 477/478 before final Debug/ReleaseSafe each passed 23/23 steps and 478/478 tests. Direct evaluator coverage passes 345/345 in both modes; full compiler Debug/ReleaseSafe each pass 60/60 steps and 973/973 tests; evaluator cross-compiles pass for Windows x64 GNU and Linux ARM64 GNU in both modes; native contract 12/12 plus check, provider/reference/product 151/151, package policy 6/6 plus check, runner selftest 59/59, formatting, syntax, JSON, and diff gates pass. Production reachability, native graduation, release state, and external systems are unchanged.

`NSASS-011-META-COLOR-FADE-IN-FUNCTION-INVOCATION` is verified at local implementation checkpoint `1d58bb2f596ff110eb05040732799aa55cf85ab3`. Fresh Dart Sass 1.101.0 probes match native SCSS and indented-Sass `meta.call()` invocation for the distinct legacy-global `fade-in` reference plus constructible-but-removed module-owned `sass:color` references, including global/default/aliased/unprefixed identity, exact required `$color`/`$amount` positional/named binding, list/map splats, transparent and alpha-bearing legacy colors, RGB conversion for HSL/HWB, simple unit-bearing magnitudes, clamping, color typing, once-only source order, and both normalized legacy warnings. Module invocation, direct qualified calls, missing channels, modern spaces, compound/deferred/out-of-range amounts, invalid arguments, unavailable modules, argument limits, and allocation failures expose no CSS; `transparentize` remains unavailable. The test-first Debug baseline stopped at direct evaluator 342/343 and focused native 475/476 before final Debug/ReleaseSafe each passed 23/23 steps and 476/476 tests. Direct evaluator coverage passes 343/343 in both modes; full compiler Debug/ReleaseSafe each pass 60/60 steps and 971/971 tests; evaluator cross-compiles pass for Windows x64 GNU and Linux ARM64 GNU in both modes; native contract 12/12 plus check, provider/reference/product 151/151, package policy 6/6 plus check, runner selftest 59/59, formatting, syntax, JSON, and diff gates pass. Production reachability, native graduation, release state, and external systems are unchanged.

`NSASS-011-META-COLOR-OPACIFY-FUNCTION-INVOCATION` is verified at local implementation checkpoint `6975bdba4a4de61e3dd93bf4d56970cfe7c1cdda`. Fresh Dart Sass 1.101.0 probes match native SCSS and indented-Sass `meta.call()` invocation for the legacy-global `opacify` reference plus distinct constructible-but-removed module-owned `sass:color` references, including global/default/aliased/unprefixed identity, exact required `$color`/`$amount` positional/named binding, list/map splats, transparent and alpha-bearing legacy colors, RGB conversion for HSL/HWB, simple unit-bearing magnitudes, clamping, color typing, once-only source order, and both normalized legacy warnings. Module invocation, direct qualified calls, missing channels, modern spaces, compound/deferred/out-of-range amounts, invalid arguments, unavailable modules, argument limits, and allocation failures expose no CSS; `fade-in` remains unavailable. The test-first Debug baseline stopped at direct evaluator 339/341 and focused native 472/474 before final Debug/ReleaseSafe each passed 23/23 steps and 474/474 tests. Direct evaluator coverage passes 341/341 in both modes; full compiler Debug/ReleaseSafe each pass 60/60 steps and 969/969 tests; evaluator cross-compiles pass for Windows x64 GNU and Linux ARM64 GNU in both modes; native contract 12/12 plus check, provider/reference/product 151/151, package policy 6/6 plus check, runner selftest 59/59, formatting, syntax, JSON, and diff gates pass. Production reachability, native graduation, release state, and external systems are unchanged.

`NSASS-011-META-COLOR-INVERT-FUNCTION-INVOCATION` is verified at local implementation checkpoint `0ed5532273788ceca9d04cace2aaeb009490739b`. Fresh Dart Sass 1.101.0 probes match native SCSS and indented-Sass `meta.call()` invocation for distinct legacy-global and module-owned `sass:color` `invert` references, including global/default/aliased/unprefixed constructibility and identity, exact required `$color` plus optional `$weight` positional/named binding, list/map splats, transparent and alpha-bearing legacy colors, RGB/HSL/HWB weighted results, color-versus-filter typing, once-only source order, and exact global-color, global-filter, module-color, and module-filter warning ownership. Global numeric/deferred and module numeric filters match the oracle; module deferred filters, explicit `$space`, missing channels, modern spaces, deprecated non-percentage weight coercion, invalid arguments, unavailable modules, direct qualified calls, argument limits, and allocation failures expose no CSS. The test-first Debug baseline stopped at direct evaluator 337/339 and focused 470/472 before final Debug/ReleaseSafe each passed 23/23 steps and 472/472 tests. Direct evaluator coverage passes 339/339 in both modes; full compiler Debug/ReleaseSafe each pass 60/60 steps and 967/967 tests; evaluator cross-compiles pass for Windows x64 GNU and Linux ARM64 GNU in both modes; native contract 12/12 plus check, provider/reference/product 151/151, package policy 6/6 plus check, runner selftest 59/59, formatting, syntax, JSON, and diff gates pass. Production reachability, native graduation, release state, and external systems are unchanged.

`NSASS-011-META-COLOR-GRAYSCALE-FUNCTION-INVOCATION` is verified at local implementation checkpoint `d3c20f913a6e509601c99753eca639ee0b3dd3b2`. Fresh Dart Sass 1.101.0 probes match native SCSS and indented-Sass `meta.call()` invocation for distinct legacy-global and module-owned `sass:color` `grayscale` references, including global/default/aliased/unprefixed constructibility and identity, exact single-`$color` positional/named binding, list/map splats, transparent and alpha-bearing legacy colors, RGB/HSL/HWB results, color-versus-filter typing, once-only source order, and exact global-color, global-filter, module-color, and module-filter warning ownership. The shared color core now preserves authored HSL hue while canonicalizing HWB grayscale to hue zero. Global numeric/deferred and module numeric filters match the oracle; module deferred filters, missing channels, modern spaces, invalid arguments, unavailable modules, direct qualified calls, argument limits, and allocation failures expose no CSS. The test-first Debug baseline stopped at direct evaluator 336/337 and focused 469/470 before final Debug/ReleaseSafe each passed 23/23 steps and 470/470 tests. Direct evaluator coverage passes 337/337 in both modes; full compiler Debug/ReleaseSafe each pass 60/60 steps and 965/965 tests; evaluator cross-compiles pass for Windows x64 GNU and Linux ARM64 GNU in both modes; native contract 12/12 plus check, provider/reference/product 151/151, package policy 6/6 plus check, runner selftest 59/59, formatting, syntax, JSON, and diff gates pass. Production reachability, native graduation, release state, and external systems are unchanged.

`NSASS-011-META-COLOR-COMPLEMENT-FUNCTION-INVOCATION` is verified at local implementation checkpoint `1d3344ee8d91b7aa0268feef3a5a11b32e6d5e51`. Fresh Dart Sass 1.101.0 probes match native SCSS and indented-Sass `meta.call()` invocation for distinct legacy-global and module-owned `sass:color` `complement` references, including global/default/aliased/unprefixed constructibility and identity, exact single-`$color` positional/named binding, list/map splats, transparent and alpha-bearing legacy colors, RGB/HSL/HWB result emission, color typing, once-only source order, and exact global-warning versus warning-free module ownership. Missing/extra/unknown/duplicate/empty-splat, invalid and deferred values, missing channels, unavailable modules, all twelve modern color spaces, unsupported `$space`, direct qualified calls, argument limits, and allocation failures expose no CSS; provider-accepted modern interpolation, selected missing channels, direct qualified invocation, `grayscale`, and every other unmeasured callable remain unchanged. The test-first Debug baseline stopped at direct evaluator 334/335 and focused 467/468 before final Debug/ReleaseSafe each passed 23/23 steps and 468/468 tests. Direct evaluator coverage passes 335/335 in both modes; full compiler Debug/ReleaseSafe each pass 60/60 steps and 963/963 tests; evaluator cross-compiles pass for Windows x64 GNU and Linux ARM64 GNU in both modes; native contract 12/12 plus check, provider/reference/product 151/151, package policy 6/6 plus check, runner selftest 59/59, formatting, syntax, JSON, and diff gates pass. Production reachability, native graduation, release state, and external systems are unchanged.

`NSASS-011-META-COLOR-ADJUST-HUE-FUNCTION-INVOCATION` is verified at local implementation checkpoint `66a2b14ee0c4cb1a6da7bd9c5ab02407efd2790c`. Fresh Dart Sass 1.101.0 probes match native SCSS and indented-Sass `meta.call()` invocation for the legacy-global `adjust-hue` reference, including global/default/aliased/unprefixed constructibility and identity, the constructible-but-removed module reference, exact `$color`/`$degrees` positional/named binding, list/map splats, transparent and alpha-bearing legacy colors, RGB/HSL/HWB result emission, degree/gradian/radian/turn conversion, unitless and deprecated simple-unit magnitude, negative and wrapped angles, color typing, once-only source order, and both normalized legacy deprecations. Missing/extra/unknown/duplicate/empty-splat, invalid and deferred values, compound/calculation angles, missing channels, unavailable modules, all twelve modern color spaces, argument limits, and allocation failures expose no CSS; module-owned/direct-qualified invocation, provider-accepted missing-channel behavior, the provider's extra deprecated-unit diagnostic, `complement`, and every other unmeasured callable remain unchanged. The test-first Debug baseline stopped at direct evaluator 332/333 and focused 465/466 before final Debug/ReleaseSafe each passed 23/23 steps and 466/466 tests. Direct evaluator coverage passes 333/333 in both modes; full compiler Debug/ReleaseSafe each pass 60/60 steps and 961/961 tests; evaluator cross-compiles pass for Windows x64 GNU and Linux ARM64 GNU in both modes; native contract 12/12 plus check, provider/reference/product 151/151, package policy 6/6 plus check, runner selftest 59/59, formatting, syntax, JSON, and diff gates pass. Production reachability, native graduation, release state, and external systems are unchanged.

`NSASS-011-META-COLOR-DESATURATE-FUNCTION-INVOCATION` is verified at local implementation checkpoint `1b5bca4883251127775b06e4181e83eb227b9d88`. Fresh Dart Sass 1.101.0 probes match native SCSS and indented-Sass `meta.call()` invocation for the legacy-global `desaturate` reference, including global/default/aliased/unprefixed constructibility and identity, the constructible-but-removed module reference, exact required `$color`/`$amount` positional/named binding, list/map splats, transparent and alpha-bearing legacy colors, RGB/HSL/HWB result emission, simple-unit numeric magnitude, inclusive range, color typing, once-only source order, and both normalized legacy deprecations. Missing/extra/unknown/duplicate/empty-splat, invalid and deferred values, compound/calculation/out-of-range amounts, missing channels, unavailable modules, all twelve modern color spaces, argument limits, and allocation failures expose no CSS; module-owned/direct-qualified invocation, provider-accepted compound-unit and missing-channel behavior, `adjust-hue`, and every other unmeasured callable remain unchanged. The test-first Debug baseline stopped at saturation core 12/13, direct evaluator 330/331, and focused 462/464 before final Debug/ReleaseSafe each passed 23/23 steps and 464/464 tests. Direct evaluator coverage passes 331/331 in both modes; full compiler Debug/ReleaseSafe each pass 60/60 steps and 959/959 tests; evaluator cross-compiles pass for Windows x64 GNU and Linux ARM64 GNU in both modes; native contract 12/12 plus check, provider/reference/product 151/151, package policy 6/6 plus check, runner selftest 59/59, formatting, syntax, JSON, and diff gates pass. Production reachability, native graduation, release state, and external systems are unchanged.

`NSASS-011-META-COLOR-SATURATE-FUNCTION-INVOCATION` is verified at local implementation checkpoint `c73dae0bcd723d1179ff98e3f7f878c771085367`. Fresh Dart Sass 1.101.0 probes match native SCSS and indented-Sass `meta.call()` invocation for the legacy-global `saturate` reference, including global/default/aliased/unprefixed constructibility and identity, the constructible-but-removed module reference, warning-free one-argument CSS-filter preservation, exact two-argument `$color`/`$amount` positional/named binding, filter and legacy list/map splats, transparent and alpha-bearing legacy colors, RGB/HSL/HWB result emission, simple-unit numeric magnitude, inclusive range, color/string result typing, once-only source order, and both normalized legacy deprecations. Missing/extra/unknown/duplicate/empty-splat, invalid filter/color/amount values, compound/calculation/out-of-range legacy amounts, missing channels, unavailable modules, all twelve modern color spaces, argument limits, and allocation failures expose no CSS; module-owned/direct-qualified invocation, provider-accepted compound-unit and missing-channel behavior, `desaturate`, and every other unmeasured callable remain unchanged. The test-first Debug baseline stopped at direct evaluator 328/329 and focused 461/462 before final Debug/ReleaseSafe each passed 23/23 steps and 462/462 tests. Direct evaluator coverage passes 329/329 in both modes; full compiler Debug/ReleaseSafe each pass 60/60 steps and 957/957 tests; evaluator cross-compiles pass for Windows x64 GNU and Linux ARM64 GNU in both modes; native contract 12/12 plus check, provider/reference/product 151/151, package policy 6/6 plus check, runner selftest 59/59, formatting, syntax, JSON, and diff gates pass. Production reachability, native graduation, release state, and external systems are unchanged.

`NSASS-011-META-COLOR-DARKEN-FUNCTION-INVOCATION` is verified at local implementation checkpoint `cd97b75e2f166dfaa713ab8fda93e3eb5c1ce8ea`. Fresh Dart Sass 1.101.0 probes match native SCSS and indented-Sass `meta.call()` invocation for the legacy-global `darken` reference, including global/default/aliased/unprefixed constructibility and identity, the constructible-but-removed module reference, exact `$color`/`$amount` positional/named binding, list/map splats, transparent and alpha-bearing legacy colors, RGB/HSL/HWB result identity, simple-unit numeric magnitude, inclusive range, color typing, once-only source order, and both normalized deprecations. Missing/extra/unknown/duplicate/empty-splat, invalid/deferred values, compound/calculation/out-of-range amounts, missing channels, unavailable modules, all twelve modern color spaces, argument limits, and allocation failures expose no CSS; module-owned/direct-qualified invocation, provider-accepted compound-unit and missing-channel behavior, `saturate`, and every other unmeasured callable remain unchanged. The test-first Debug baseline stopped at direct evaluator 326/327 and focused 459/460, and the first implementation replay caught HWB result-space drift before final Debug/ReleaseSafe each passed 23/23 steps and 460/460 tests. Direct evaluator coverage passes 327/327 in both modes; full compiler Debug/ReleaseSafe each pass 60/60 steps and 955/955 tests; evaluator cross-compiles pass for Windows x64 GNU and Linux ARM64 GNU in both modes; native contract 12/12 plus check, provider/reference/product 151/151, package policy 6/6 plus check, runner selftest 59/59, formatting, syntax, JSON, and diff gates pass. Production reachability, native graduation, release state, and external systems are unchanged.

`NSASS-011-META-COLOR-LIGHTEN-FUNCTION-INVOCATION` is verified at local implementation checkpoint `e0ca902b07114c6c363c35d2ef8d66a4f7aa8f6a`. Fresh Dart Sass 1.101.0 probes match native SCSS and indented-Sass `meta.call()` invocation for the legacy-global `lighten` reference, including global/default/aliased/unprefixed constructibility and identity, the constructible-but-removed module reference, exact `$color`/`$amount` positional/named binding, list/map splats, transparent and alpha-bearing legacy colors, RGB/HSL/HWB conversion, simple-unit numeric magnitude, inclusive range, color typing, once-only source order, and both normalized deprecations. Missing/extra/unknown/duplicate/empty-splat, invalid/deferred values, compound/calculation/out-of-range amounts, missing channels, unavailable modules, all twelve modern color spaces, argument limits, and allocation failures expose no CSS; module-owned/direct-qualified invocation, provider-accepted missing-channel behavior, `darken`, and every other unmeasured callable remain unchanged. The test-first Debug baseline stopped at direct evaluator 324/325 and focused 457/458 before final Debug/ReleaseSafe each passed 23/23 steps and 458/458 tests. Direct evaluator coverage passes 325/325 in both modes; full compiler Debug/ReleaseSafe each pass 60/60 steps and 953/953 tests; evaluator cross-compiles pass for Windows x64 GNU and Linux ARM64 GNU in both modes; native contract 12/12 plus check, provider/reference/product 151/151, package policy 6/6 plus check, runner selftest 59/59, formatting, syntax, JSON, and diff gates pass. Production reachability, native graduation, release state, and external systems are unchanged.

`NSASS-011-META-COLOR-MIX-FUNCTION-INVOCATION` is verified at local implementation checkpoint `9531731042e9338c92ed3a32769a68922a0e7901`. Fresh Dart Sass 1.101.0 probes match native SCSS and indented-Sass `meta.call()` invocation for distinct legacy-global and module-owned `sass:color` `mix` references at the evidence-closed legacy-color boundary, including default/aliased/unprefixed constructibility and identity, exact `$color1`/`$color2`/optional percentage `$weight` positional/named binding, list/map splats, transparent and alpha-bearing colors, RGB/HSL/HWB conversion, color typing, once-only source order, and exact global-warning versus warning-free module ownership. Missing/extra/unknown/duplicate/empty-splat, invalid and deferred values, non-percentage or out-of-range weights, `$method`, missing channels, unavailable modules, all twelve modern color spaces, argument limits, and allocation failures expose no CSS; direct qualified `color.mix()`, unit coercion, modern interpolation, and every unmeasured callable remain unchanged. The test-first Debug baseline stopped at direct evaluator 322/323 and focused 455/456 before final Debug/ReleaseSafe each passed 23/23 steps and 456/456 tests. Direct evaluator coverage passes 323/323 in both modes; full compiler Debug/ReleaseSafe each pass 60/60 steps and 951/951 tests; evaluator cross-compiles pass for Windows x64 GNU and Linux ARM64 GNU in both modes; native contract 12/12 plus check, provider/reference/product 151/151, package policy 6/6 plus check, runner selftest 59/59, formatting, syntax, JSON, and diff gates pass. Production reachability, native graduation, release state, and external systems are unchanged.

`NSASS-011-META-COLOR-BLACKNESS-FUNCTION-INVOCATION` is verified at local implementation checkpoint `426f6d0443dd6a8ed48bc49a54271227258d3398`. Fresh Dart Sass 1.101.0 probes match native SCSS and indented-Sass `meta.call()` invocation for the module-owned `sass:color` `blackness` reference and the absent legacy global, including default/aliased/unprefixed constructibility and identity, exact single-`$color` positional/named binding, list/map splats, transparent/RGB/HSL/HWB legacy inputs, percentage result units, number typing, once-only source order, and exact normalized module deprecations. Missing/extra/unknown/duplicate/empty-splat, invalid and deferred values, unsupported `$space`, absent global, unavailable modules, all twelve modern color spaces, argument limits, and allocation failures expose no CSS; direct qualified color calls and every unmeasured callable family remain unchanged. The test-first Debug baseline stopped at direct evaluator 320/321 and focused 453/454 before final Debug/ReleaseSafe each passed 23/23 steps and 454/454 tests. Direct evaluator coverage passes 321/321 in both modes; full compiler Debug/ReleaseSafe each pass 60/60 steps and 949/949 tests; evaluator cross-compiles pass for Windows x64 GNU and Linux ARM64 GNU in both modes; native contract 12/12 plus check, provider/reference/product 151/151, package policy 6/6 plus check, runner selftest 59/59, formatting, syntax, JSON, and diff gates pass. Production reachability, native graduation, release state, and external systems are unchanged.

`NSASS-011-META-COLOR-WHITENESS-FUNCTION-INVOCATION` is verified at local implementation checkpoint `e811cf5596382a6d1bf980d2d3605f549ba066c3`. Fresh Dart Sass 1.101.0 probes match native SCSS and indented-Sass `meta.call()` invocation for the module-owned `sass:color` `whiteness` reference and the absent legacy global, including default/aliased/unprefixed constructibility and identity, exact single-`$color` positional/named binding, list/map splats, transparent/RGB/HSL/HWB legacy inputs, percentage result units, number typing, once-only source order, and exact normalized module deprecations. Missing/extra/unknown/duplicate/empty-splat, invalid and deferred values, unsupported `$space`, absent global, unavailable modules, every modern color, argument limits, and allocation failures expose no CSS; module-owned `blackness` is the next explicit invocation boundary and direct qualified color calls remain unchanged. The resumed test-first Debug gate caught the unhandled callable-only dispatch enum before evaluator execution while other focused artifacts passed 133/133. Final Debug/ReleaseSafe each pass 23/23 steps and 452/452 tests; direct evaluator coverage passes 319/319 in both modes; full compiler Debug/ReleaseSafe each pass 60/60 steps and 947/947 tests; evaluator cross-compiles pass for Windows x64 GNU and Linux ARM64 GNU in both modes; native contract 12/12 plus check, provider/reference/product 151/151, package policy 6/6 plus check, runner selftest 59/59, formatting, syntax, JSON, and diff gates pass. Production reachability, native graduation, release state, and external systems are unchanged.

`NSASS-011-META-COLOR-LIGHTNESS-FUNCTION-INVOCATION` is verified at local implementation checkpoint `e8d0bccb48be9bffbbb7ec4cf5636686be32e3d8`. Fresh Dart Sass 1.101.0 probes match native SCSS and indented-Sass `meta.call()` invocation for the distinct legacy-global and module-owned `sass:color` `lightness` references, including global/default/aliased/unprefixed constructibility and identity, exact single-`$color` positional/named binding, list/map splats, transparent/RGB/HSL/HWB legacy inputs, percentage result units, number typing, once-only source order, and exact normalized global/module deprecations. Missing/extra/unknown/duplicate/empty-splat, invalid and deferred values, unsupported `$space`, unavailable modules, every modern color, argument limits, and allocation failures expose no CSS; module-owned `whiteness` is the next explicit invocation boundary and direct qualified color calls remain unchanged. The test-first Debug baseline stopped at direct evaluator 316/317 and focused 449/450 before final Debug/ReleaseSafe each passed 23/23 steps and 450/450 tests. Direct evaluator coverage passes 317/317 in both modes; full compiler Debug/ReleaseSafe each pass 60/60 steps and 945/945 tests; evaluator cross-compiles pass for Windows x64 GNU and Linux ARM64 GNU in both modes; native contract 12/12 plus check, provider/reference/product 151/151, package policy 6/6 plus check, runner selftest 59/59, formatting, syntax, JSON, and diff gates pass. The SDK selector was removed; production reachability, native graduation, release state, and external systems are unchanged.

`NSASS-011-META-COLOR-SATURATION-FUNCTION-INVOCATION` is verified at local implementation checkpoint `b93536d9c849bee403a21de5f4a3b36a836088da`. Fresh Dart Sass 1.101.0 probes match native SCSS and indented-Sass `meta.call()` invocation for the distinct legacy-global and module-owned `sass:color` `saturation` references, including global/default/aliased/unprefixed constructibility and identity, exact single-`$color` positional/named binding, list/map splats, transparent/RGB/HSL/HWB legacy inputs, percentage result units, number typing, once-only source order, and exact normalized global/module deprecations. Missing/extra/unknown/duplicate/empty-splat, invalid and deferred values, unsupported `$space`, unavailable modules, every modern color, argument limits, and allocation failures expose no CSS; `lightness` is the next explicit invocation boundary and direct qualified color calls remain unchanged. The test-first Debug baseline stopped at direct evaluator 314/315 and focused 447/448 before final Debug/ReleaseSafe each passed 23/23 steps and 448/448 tests. Direct evaluator coverage passes 315/315 in both modes; full compiler Debug/ReleaseSafe each pass 60/60 steps and 943/943 tests; evaluator cross-compiles pass for Windows x64 GNU and Linux ARM64 GNU in both modes; native contract 12/12 plus check, provider/reference/product 151/151, package policy 6/6 plus check, runner selftest 59/59, formatting, syntax, JSON, and diff gates pass. The SDK selector was removed; production reachability, native graduation, release state, and external systems are unchanged.

`NSASS-011-META-COLOR-HUE-FUNCTION-INVOCATION` is verified at local implementation checkpoint `f1ac3a34035a83ee3860d8d3f47315782bfd1c26`. Fresh Dart Sass 1.101.0 probes match native SCSS and indented-Sass `meta.call()` invocation for the distinct legacy-global and module-owned `sass:color` `hue` references, including global/default/aliased/unprefixed constructibility and identity, exact single-`$color` positional/named binding, list/map splats, transparent/RGB/HSL/HWB legacy inputs, degree result units, number typing, once-only source order, and exact normalized global/module deprecations. Missing/extra/unknown/duplicate/empty-splat, invalid and deferred values, unsupported `$space`, unavailable modules, every modern color, argument limits, and allocation failures expose no CSS; `saturation` is the next explicit invocation boundary and direct qualified color calls remain unchanged. The test-first Debug baseline stopped at direct evaluator 312/313 and focused 445/446 before final Debug/ReleaseSafe each passed 23/23 steps and 446/446 tests. Direct evaluator coverage passes 313/313 in both modes; full compiler Debug/ReleaseSafe each pass 60/60 steps and 941/941 tests; evaluator cross-compiles pass for Windows x64 GNU and Linux ARM64 GNU in both modes; native contract 12/12 plus check, provider/reference/product 151/151, package policy 6/6 plus check, runner selftest 59/59, formatting, syntax, JSON, and diff gates pass. The SDK selector was removed; production reachability, native graduation, release state, and external systems are unchanged.

`NSASS-011-META-COLOR-OPACITY-FUNCTION-INVOCATION` is verified at local implementation checkpoint `ba0c30787f204c432ad1030ef3ad23fa83e250d5`. Fresh Dart Sass 1.101.0 probes match native SCSS and indented-Sass `meta.call()` invocation for the distinct legacy-global and module-owned `sass:color` `opacity` references, including global/default/aliased/unprefixed constructibility and identity, exact single-`$color` positional/named binding, list/map splats, every native color space, number typing, once-only source order, and the CSS-filter number/deferred-value collision. Legacy color calls retain one normalized global warning, module colors remain warning-free, global filter values preserve exact CSS without warnings, and module numeric values preserve exact leading-zero CSS with one normalized deprecation. Missing/extra/unknown/duplicate/empty-splat, invalid values, module deferred values, unavailable modules, argument limits, and allocation failures expose no CSS; `hue` is the next explicit invocation boundary and direct qualified color calls remain unchanged. The test-first Debug baseline stopped at direct evaluator 310/311 and focused 443/444 before final Debug/ReleaseSafe each passed 23/23 steps and 444/444 tests. Direct evaluator coverage passes 311/311 in both modes; full compiler Debug/ReleaseSafe each pass 60/60 steps and 939/939 tests; evaluator cross-compiles pass for Windows x64 GNU and Linux ARM64 GNU in both modes; native contract 12/12 plus check, provider/reference/product 151/151, package policy 6/6 plus check, runner selftest 59/59, formatting, syntax, JSON, and diff gates pass. The SDK selector was removed; production reachability, native graduation, release state, and external systems are unchanged.

`NSASS-011-META-COLOR-ALPHA-FUNCTION-INVOCATION` is verified at local implementation checkpoint `98028ac57e90747505ac1c8a91e231e0455db37d`. Fresh Dart Sass 1.101.0 probes match native SCSS and indented-Sass `meta.call()` invocation for the distinct legacy-global and module-owned `sass:color` `alpha` references, including global/default/aliased/unprefixed constructibility and identity, exact single-`$color` positional/named binding, list/map splats, transparent/RGB/HSL/HWB alpha preservation, number typing, once-only source order, and the exact warning-free module versus global-built-in warning split. Missing/extra/unknown/duplicate/empty-splat, invalid values, unsupported `$space`, unavailable modules, modern colors, argument limits, and allocation failures expose no CSS; `opacity` is the next explicit invocation boundary and direct qualified color calls remain unchanged. The test-first Debug baseline stopped at direct evaluator 308/309 and focused 441/442 before final Debug/ReleaseSafe each passed 23/23 steps and 442/442 tests. Direct evaluator coverage passes 309/309; full compiler Debug/ReleaseSafe each pass 60/60 steps and 937/937 tests; evaluator cross-compiles pass for Windows x64 GNU and Linux ARM64 GNU in both modes; native contract 12/12 plus check, provider/reference/product 151/151, package policy 6/6 plus check, runner selftest 59/59, formatting, syntax, JSON, and diff gates pass. The SDK selector was removed; production reachability, native graduation, release state, and external systems are unchanged.

`NSASS-011-META-COLOR-BLUE-FUNCTION-INVOCATION` is verified at local implementation checkpoint `916f5703debba1ba93613ce3e33d9163e03ca1b7`. Fresh Dart Sass 1.101.0 probes match native SCSS and indented-Sass `meta.call()` invocation for the distinct legacy-global and module-owned `sass:color` `blue` references, including global/default/aliased/unprefixed constructibility and identity, exact single-`$color` positional/named binding, list/map splats, transparent/RGB/HSL/HWB conversion, integer rounding, number typing, once-only source order, and exact normalized global/module deprecations. Missing/extra/unknown/duplicate/empty-splat, invalid values, unsupported `$space`, unavailable modules, modern colors, argument limits, and allocation failures expose no CSS; `alpha` is the next explicit invocation boundary and direct qualified color calls remain unchanged. The test-first Debug baseline stopped at direct evaluator 306/307 and focused 439/440 before final Debug/ReleaseSafe each passed 23/23 steps and 440/440 tests. Direct evaluator coverage passes 307/307; full compiler Debug/ReleaseSafe each pass 60/60 steps and 935/935 tests; evaluator cross-compiles pass for Windows x64 GNU and Linux ARM64 GNU in both modes; native contract 12/12 plus check, provider/reference/product 151/151, package policy 6/6 plus check, runner selftest 59/59, formatting, syntax, JSON, and diff gates pass. The SDK selector was removed; production reachability, native graduation, release state, and external systems are unchanged.

`NSASS-011-META-COLOR-GREEN-FUNCTION-INVOCATION` is verified at local implementation checkpoint `9a8fb9f504cb66cd0d0215a86c68004d9cce1145`. Fresh Dart Sass 1.101.0 probes match native SCSS and indented-Sass `meta.call()` invocation for the distinct legacy-global and module-owned `sass:color` `green` references, including global/default/aliased/unprefixed constructibility and identity, exact single-`$color` positional/named binding, list/map splats, transparent/RGB/HSL/HWB conversion, integer rounding, number typing, once-only source order, and exact normalized global/module deprecations. Missing/extra/unknown/duplicate/empty-splat, invalid values, unsupported `$space`, unavailable modules, modern colors, argument limits, and allocation failures expose no CSS; `blue` is the next explicit invocation boundary and direct qualified color calls remain unchanged. The test-first Debug baseline stopped at direct evaluator 304/305 and focused 437/438 before final Debug/ReleaseSafe each passed 23/23 steps and 438/438 tests. Direct evaluator coverage passes 305/305; full compiler Debug/ReleaseSafe each pass 60/60 steps and 933/933 tests; evaluator cross-compiles pass for Windows x64 GNU and Linux ARM64 GNU in both modes; native contract 12/12 plus check, provider/reference/product 151/151, package policy 6/6 plus check, runner selftest 59/59, formatting, syntax, JSON, and diff gates pass. The SDK selector was removed; production reachability, native graduation, release state, and external systems are unchanged.

`NSASS-011-META-COLOR-RED-FUNCTION-INVOCATION` is verified at local implementation checkpoint `b6a30329099b40c9dd88616f9d3b868e42fa2c8a`. Fresh Dart Sass 1.101.0 probes match native SCSS and indented-Sass `meta.call()` invocation for the distinct legacy-global and module-owned `sass:color` `red` references, including global/default/aliased/unprefixed constructibility and identity, exact single-`$color` positional/named binding, list/map splats, transparent/RGB/HSL/HWB conversion, integer rounding, number typing, once-only source order, and exact normalized global/module deprecations. Missing/extra/unknown/duplicate/empty-splat, invalid values, unsupported `$space`, unavailable modules, modern colors, argument limits, and allocation failures expose no CSS; `green` is the next explicit invocation boundary and direct qualified color calls remain unchanged. The test-first Debug baseline stopped at direct evaluator 302/303 and focused 435/436 before final Debug/ReleaseSafe each passed 23/23 steps and 436/436 tests. Direct evaluator coverage passes 303/303; full compiler Debug/ReleaseSafe each pass 60/60 steps and 931/931 tests; evaluator cross-compiles pass for Windows x64 GNU and Linux ARM64 GNU in both modes; native contract 12/12 plus check, provider/reference/product 151/151, package policy 6/6 plus check, runner selftest 59/59, formatting, syntax, JSON, and diff gates pass. The SDK selector was removed; production reachability, native graduation, release state, and external systems are unchanged.

`NSASS-011-META-COLOR-FUNCTION-INVOCATION` is verified at local implementation checkpoint `17f02a7798fc59e872b49fb4817e35e8f1253eb0`. Fresh Dart Sass 1.101.0 probes match native SCSS and indented-Sass `meta.call()` invocation for the global `color` reference, including the measured `sass:color` existence-versus-construction asymmetry, global identity across unprefixed import, warning-free reflection, exact single-`$description` binding, positional/named use, list/map splats, all predefined spaces, `xyz-d65` normalization, unitless/percentage/static-variable channels, signed and out-of-range channels, alpha clamping, `none`, color result typing, and once-only source order. Module-owned reference lookup, deferred variables/calculations, and invalid binding/value/list/unit/space/limit/allocation cases fail closed; global `red` is the next explicit invocation boundary. The test-first Debug baseline stopped at direct evaluator 300/301 and focused 433/434 before final Debug/ReleaseSafe each passed 23/23 steps and 434/434 tests. Direct evaluator coverage passes 301/301; full compiler Debug/ReleaseSafe each pass 60/60 steps and 929/929 tests; evaluator cross-compiles pass for Windows x64 GNU and Linux ARM64 GNU in both modes; native contract 12/12 plus check, provider/reference/product 151/151, package policy 6/6 plus check, runner selftest 59/59, formatting, syntax, JSON, and diff gates pass. The SDK selector was removed; production reachability, native graduation, release state, and external systems are unchanged.

`NSASS-011-META-COLOR-OKLCH-FUNCTION-INVOCATION` is verified at local implementation checkpoint `282ac11f8c7a56b80db551001c6ec9f7343ce6ae`. Fresh Dart Sass 1.101.0 probes match native SCSS and indented-Sass `meta.call()` invocation for the global `oklch` reference, including the measured `sass:color` existence-versus-construction asymmetry, global identity across unprefixed import, warning-free reflection, exact single-`$channels` binding, positional/named use, list/map splats, unitless/percentage lightness and chroma, unitless/angle hue, negative channels, alpha, clamping, hue normalization, `none`, color result typing, and once-only source order. Module-owned reference lookup, deferred variables/calculations, and invalid binding/value/unit/list/limit/allocation cases fail closed; generic `color()` is the next explicit invocation boundary. The test-first Debug baseline stopped at direct evaluator 298/299 and focused 431/432 before final Debug/ReleaseSafe each passed 23/23 steps and 432/432 tests. Direct evaluator coverage passes 299/299; full compiler Debug/ReleaseSafe each pass 60/60 steps and 927/927 tests; evaluator cross-compiles pass for Windows x64 GNU and Linux ARM64 GNU in both modes; native contract 12/12 plus check, provider/reference/product 151/151, package policy 6/6 plus check, runner selftest 59/59, formatting, syntax, JSON, and diff gates pass. The SDK selector was removed; production reachability, native graduation, release state, and external systems are unchanged.

`NSASS-011-META-COLOR-OKLAB-FUNCTION-INVOCATION` is verified at local implementation checkpoint `02c13b2de245c28ae55cc84646b1a0f49e92bf2d`. Fresh Dart Sass 1.101.0 probes match native SCSS and indented-Sass `meta.call()` invocation for the global `oklab` reference, including the measured `sass:color` existence-versus-construction asymmetry, global identity across unprefixed import, warning-free reflection, exact single-`$channels` binding, positional/named use, list/map splats, unitless/percentage lightness and axes, negative axes, alpha, clamping, `none`, color result typing, and once-only source order. Module-owned reference lookup, deferred variables/calculations, and invalid binding/value/unit/list/limit/allocation cases fail closed; `oklch` is the next explicit invocation boundary. The test-first Debug baseline stopped at direct evaluator 296/297 and focused 429/430 before final Debug/ReleaseSafe each passed 23/23 steps and 430/430 tests. Direct evaluator coverage passes 297/297; full compiler Debug/ReleaseSafe each pass 60/60 steps and 925/925 tests; evaluator cross-compiles pass for Windows x64 GNU and Linux ARM64 GNU in both modes; native contract 12/12 plus check, provider/reference/product 151/151, package policy 6/6 plus check, runner selftest 59/59, formatting, syntax, JSON, and diff gates pass. The SDK selector was removed; production reachability, native graduation, release state, and external systems are unchanged.

`NSASS-011-META-COLOR-LCH-FUNCTION-INVOCATION` is verified at local implementation checkpoint `cef787d4b7ecd48228c7f5db95043eb3104451ab`. Fresh Dart Sass 1.101.0 probes match native SCSS and indented-Sass `meta.call()` invocation for the global `lch` reference, including the measured `sass:color` existence-versus-construction asymmetry, global identity across unprefixed import, warning-free reflection, exact single-`$channels` binding, positional/named use, list/map splats, unitless/percentage lightness and chroma, unitless/angle hue, alpha, clamping, normalization, `none`, color result typing, and once-only source order. Module-owned reference lookup, deferred variables/calculations, and invalid binding/value/unit/list/limit/allocation cases fail closed; `oklab` is the next explicit invocation boundary. The test-first Debug baseline stopped at direct evaluator 294/295 and focused 427/428 before final Debug/ReleaseSafe each passed 23/23 steps and 428/428 tests. Direct evaluator coverage passes 295/295; full compiler Debug/ReleaseSafe each pass 60/60 steps and 923/923 tests; evaluator cross-compiles pass for Windows x64 GNU and Linux ARM64 GNU in both modes; native contract 12/12 plus check, provider/reference/product 151/151, package policy 6/6 plus check, runner selftest 59/59, formatting, syntax, JSON, and diff gates pass. The SDK selector was removed; production reachability, native graduation, release state, and external systems are unchanged.

`NSASS-011-META-COLOR-LAB-FUNCTION-INVOCATION` is verified at local implementation checkpoint `ff4a17b1742932155817bfa0226718d98958df98`. Fresh Dart Sass 1.101.0 probes match native SCSS and indented-Sass `meta.call()` invocation for the global `lab` reference, including the measured `sass:color` existence-versus-construction asymmetry, global identity across unprefixed import, warning-free reflection, exact single-`$channels` binding, positional/named use, list/map splats, unitless/percentage and negative channels, alpha, clamping, `none`, color result typing, and once-only source order. Module-owned reference lookup, deferred variables/calculations, and invalid binding/value/unit/list/limit/allocation cases fail closed; `lch` is the next explicit invocation boundary. The test-first Debug baseline stopped at direct evaluator 292/293 and focused 425/426 before final Debug/ReleaseSafe each passed 23/23 steps and 426/426 tests. Direct evaluator coverage passes 293/293; full compiler Debug/ReleaseSafe each pass 60/60 steps and 921/921 tests; evaluator cross-compiles pass for Windows x64 GNU and Linux ARM64 GNU in both modes; native contract 12/12 plus check, provider/reference/product 151/151, package policy 6/6 plus check, runner selftest 59/59, formatting, syntax, JSON, and diff gates pass. The SDK selector was removed; production reachability, native graduation, release state, and external systems are unchanged.

`NSASS-011-META-COLOR-HWB-FUNCTION-INVOCATION` is verified at local implementation checkpoint `ec1b4ebcec969462700db3c6f00c85445676994f`. Fresh Dart Sass 1.101.0 probes match native SCSS and indented-Sass `meta.call()` invocation for the distinct global and module-owned `sass:color` `hwb` references at the measured strict-percentage, non-deferred boundary, including default/aliased/unprefixed module identity, warning-free invocation, exact single-`$channels` binding, positional/named use, list/map splats, angle units, percentage alpha, normalization, finite out-of-range channels, color result typing, and once-only source order. The shared HWB constructor preserves out-of-range whiteness/blackness until canonical sum normalization and serializes normalized grayscale with hue zero. Deprecated arbitrary-unit hue coercion, deferred calculations, `none`, arithmetic-shaped negative channels, and invalid binding/value/unit/list/limit/allocation cases fail closed; `lab` reflection is the next explicit measurement boundary. The test-first Debug baseline stopped at direct evaluator 289/291 and focused 422/424 before final Debug/ReleaseSafe each passed 23/23 steps and 424/424 tests. Direct evaluator coverage passes 291/291; full compiler Debug/ReleaseSafe each pass 60/60 steps and 919/919 tests; native preprocessor cross-compiles pass for Windows x64 GNU and Linux ARM64 GNU in both modes; native contract 12/12 plus check, provider/reference/product 151/151, package policy 6/6 plus check, runner selftest 59/59, formatting, syntax, JSON, and diff gates pass. The SDK selector was removed; production reachability, native graduation, release state, and external systems are unchanged.

`NSASS-011-META-COLOR-HSLA-FUNCTION-INVOCATION` is verified at local implementation checkpoint `223f4a8ace953ec2ca58f0ffb379083f3357ebf8`. Fresh Dart Sass 1.101.0 probes match native SCSS and indented-Sass `meta.call()` invocation for the global-only `hsla` reference at the measured strict-percentage, non-deferred boundary, including identity across unprefixed `sass:color` import, distinct identity from global `hsl`, warning-free invocation, positional/named channels, optional alpha, angle/percentage channels, percentage alpha, space/slash channel lists, list/map splats, omitted alpha, hue normalization, alpha clamping, out-of-range HSL preservation, color result typing, and once-only source order. Deprecated unit coercions, deferred calculations, `none`, and invalid binding/value/unit/list/module/limit/allocation cases fail closed; global `hwb` invocation is the next explicit boundary. The test-first Debug baseline stopped at direct evaluator 287/289 and focused 420/422 before final Debug/ReleaseSafe each passed 23/23 steps and 422/422 tests. Direct evaluator coverage passes 289/289; full compiler Debug/ReleaseSafe each pass 60/60 steps and 917/917 tests; native preprocessor cross-compiles pass for Windows x64 GNU and Linux ARM64 GNU in both modes; native contract 12/12 plus check, provider/reference/product 151/151, package policy 6/6 plus check, runner selftest 59/59, formatting, syntax, JSON, and diff gates pass. The SDK selector was removed; production reachability, native graduation, release state, and external systems are unchanged.

`NSASS-011-META-COLOR-HSL-FUNCTION-INVOCATION` is verified at local implementation checkpoint `e467854a148916fd94b8f658e61a7638e252f145`. Fresh Dart Sass 1.101.0 probes match native SCSS and indented-Sass `meta.call()` invocation for the global-only `hsl` reference at the measured strict-percentage, non-deferred boundary, including identity across unprefixed `sass:color` import, warning-free invocation, positional/named channels, optional alpha, angle/percentage channels, space/slash channel lists, list/map splats, hue normalization, alpha clamping, out-of-range HSL preservation, color result typing, and once-only source order. Deprecated unit coercions, deferred calculations, `none`, and invalid binding/value/unit/list/module/limit/allocation cases fail closed; global `hsla` invocation is the next explicit boundary. The test-first Debug baseline stopped at direct evaluator 285/287 and focused 418/420 before final Debug/ReleaseSafe each passed 23/23 steps and 420/420 tests. Direct evaluator coverage passes 287/287; full compiler Debug/ReleaseSafe each pass 60/60 steps and 915/915 tests; native preprocessor cross-compiles pass for Windows x64 GNU and Linux ARM64 GNU in both modes; native contract 12/12 plus check, provider/reference/product 151/151, package policy 6/6 plus check, runner selftest 59/59, formatting, syntax, JSON, and diff gates pass. The SDK selector was removed; production reachability, native graduation, release state, and external systems are unchanged.

`NSASS-011-META-COLOR-RGBA-FUNCTION-INVOCATION` is verified at local implementation checkpoint `5b54b41f5d9d93a8d954d316ac4e063058e3c50d`. Fresh Dart Sass 1.101.0 probes match native SCSS and indented-Sass `meta.call()` invocation for the global-only `rgba` reference at the measured non-deferred boundary, including identity across unprefixed `sass:color` import, warning-free invocation, positional/named channels, omitted and explicit alpha, percentages, legacy RGB/HSL/HWB color-plus-alpha overloads, space/slash channel lists, list/map splats, clamping, color result typing, and once-only source order. Invalid binding, value, unit, overload, callable, module, argument-limit, and allocation-failure cases fail closed; deferred calculations remain deliberately unavailable and global `hsl` invocation is the next explicit boundary. The test-first Debug baseline stopped at direct evaluator 283/285 and focused 416/418 before final Debug/ReleaseSafe each passed 23/23 steps and 418/418 tests. Direct evaluator coverage passes 285/285; full compiler Debug/ReleaseSafe each pass 60/60 steps and 913/913 tests; native preprocessor cross-compiles pass for Windows x64 GNU and Linux ARM64 GNU in both modes; native contract 12/12 plus check, provider/reference/product 151/151, package policy 6/6 plus check, runner selftest 59/59, formatting, syntax, JSON, and diff gates pass. The SDK selector was removed; production reachability, native graduation, release state, and external systems are unchanged.

`NSASS-011-META-COLOR-RGB-FUNCTION-INVOCATION` is verified at local implementation checkpoint `16f0b141cdb2bcb665824a44c6ffefe75bd891ec`. Fresh Dart Sass 1.101.0 probes match native SCSS and indented-Sass `meta.call()` invocation for the global-only `rgb` reference at the measured non-deferred boundary, including identity across unprefixed `sass:color` import, warning-free invocation, positional/named and percentage channels, optional alpha, legacy RGB/HSL/HWB color-plus-alpha overloads, space/slash channel lists, list/map splats, clamping, color result typing, and once-only source order. Invalid binding, value, unit, overload, calculation, callable, argument-limit, and allocation-failure cases fail closed; module-qualified construction remains rejected and global `rgba` invocation remains unavailable. The test-first Debug baseline stopped at direct evaluator 281/283 and focused 414/416 before final Debug/ReleaseSafe each passed 23/23 steps and 416/416 tests. Direct evaluator coverage passes 283/283; full compiler Debug/ReleaseSafe each pass 60/60 steps and 911/911 tests; native preprocessor cross-compiles pass for Windows x64 GNU and Linux ARM64 GNU in both modes; native contract 12/12 plus check, provider/reference/product 151/151, package policy 6/6 plus check, runner selftest 59/59, formatting, syntax, JSON, and diff gates pass. The SDK selector was removed; production reachability, native graduation, release state, and external systems are unchanged.

`NSASS-011-META-COLOR-SCALE-FUNCTION-INVOCATION` is verified at local implementation checkpoint `c62a1176753805a8cd4087fd1e80e220d4f62b8b`. Fresh Dart Sass 1.101.0 probes match native SCSS and indented-Sass `meta.call()` invocation for module-owned `sass:color` `scale` and the legacy-global `scale-color` reference, including default/custom/aliased/unprefixed identity, distinct legacy ownership, the normalized legacy warning, one positional `$color`, measured optional named channel and `$space` binding, list/map splats, positive/negative RGB scaling, HSL, HWB, Lab, LCH, display-p3, alpha, unchanged identity, named reordering, result type/inspection, and once-only source order. Missing, extra positional, unknown, duplicate, empty-splat, invalid color/space/channel/calculation/function, percentage-unit/range, hue, argument-limit, and allocation-failure cases fail closed; reflected global `rgb` invocation remains unavailable. The test-first Debug baseline stopped at direct evaluator 278/281 and focused 411/414 before final Debug/ReleaseSafe each passed 23/23 steps and 414/414 tests. Direct evaluator coverage passes 281/281; full compiler Debug/ReleaseSafe each pass 60/60 steps and 909/909 tests; native preprocessor cross-compiles pass for Windows x64 GNU and Linux ARM64 GNU in both modes; native contract 12/12 plus check, provider/reference/product 151/151, package policy 6/6 plus check, runner selftest 59/59, formatting, syntax, JSON, and diff gates pass. The SDK selector and validation caches were removed; production reachability, native graduation, release state, and external systems are unchanged.
`NSASS-011-META-COLOR-CHANGE-FUNCTION-INVOCATION` is verified at local implementation checkpoint `94c5d3a6859068148dfba9095f40ed96d399ab50`. Fresh Dart Sass 1.101.0 probes match native SCSS and indented-Sass `meta.call()` invocation for module-owned `sass:color` `change` and the legacy-global `change-color` reference, including default/custom/aliased/unprefixed identity, distinct legacy ownership, the normalized legacy warning, one positional `$color`, measured optional named channel and `$space` binding, list/map splats, RGB unitless/percentage channels, HSL, HWB, Lab, LCH, display-p3, alpha, omitted channels, named reordering, result type/inspection, and once-only source order. Missing, extra positional, unknown, duplicate, empty-splat, invalid color/space/channel/calculation/function, out-of-range alpha, argument-limit, and allocation-failure cases fail closed; `color.scale` is the next explicit unavailable boundary. The test-first Debug baseline stopped at direct evaluator 276/278 and focused 409/411 before final Debug/ReleaseSafe each passed 23/23 steps and 411/411 tests. Direct evaluator coverage passes 278/278; full compiler Debug/ReleaseSafe each pass 60/60 steps and 906/906 tests; evaluator cross-compiles pass for Windows x64 GNU and Linux ARM64 GNU in both modes; native contract 12/12 plus check, provider/reference/product 151/151, package policy 6/6 plus check, runner selftest 59/59, formatting, syntax, JSON, and diff gates pass. The SDK selector and validation caches were removed; production reachability, native graduation, release state, and external systems are unchanged.
`NSASS-011-META-COLOR-ADJUST-FUNCTION-INVOCATION` is verified at local implementation checkpoint `8d3450a2173af3b6eb103cb104d03487f727a906`. Fresh Dart Sass 1.101.0 probes match native SCSS and indented-Sass `meta.call()` invocation for module-owned `sass:color` `adjust` and the legacy-global `adjust-color` reference, including default/custom/aliased/unprefixed identity, distinct legacy ownership, the normalized legacy warning, one positional `$color`, measured optional named channel and `$space` binding, list/map splats, RGB unitless/percentage channels, HSL, HWB, Lab, LCH, display-p3, alpha, omitted channels, named reordering, result type/inspection, and once-only source order. Missing, extra positional, unknown, duplicate, empty-splat, invalid color/space/channel/calculation/function, argument-limit, and allocation-failure cases fail closed; `color.change` is the next explicit unavailable boundary. The test-first Debug baseline stopped at direct evaluator 273/275 and focused 406/408 before final Debug/ReleaseSafe each passed 23/23 steps and 408/408 tests. Direct evaluator coverage passes 275/275; full compiler Debug/ReleaseSafe each pass 60/60 steps and 903/903 tests; evaluator cross-compiles pass for Windows x64 GNU and Linux ARM64 GNU in both modes; native contract 12/12 plus check, provider/reference/product 151/151, package policy 6/6 plus check, runner selftest 59/59, formatting, syntax, JSON, and diff gates pass. The SDK selector was removed; production reachability, native graduation, release state, and external systems are unchanged.

`NSASS-011-META-STRING-LOWER-CASE-FUNCTION-INVOCATION` is verified at local implementation checkpoint `6678b6ba69ac4ca1b5ae63eab2e67f2f3015bfd3`. Fresh Dart Sass 1.101.0 probes match native SCSS and indented-Sass `meta.call()` invocation for module-owned `sass:string` `to-lower-case` and the legacy-global reference, including default/custom/unprefixed/underscore-normalized identity, distinct legacy ownership, the normalized legacy warning, positional/named `$string` binding, one-item list/map splats, ASCII-only conversion, preserved Unicode scalars, combining marks and ZWJ sequences, quoted/unquoted values, hexadecimal/simple escapes, empty strings, interpolation, result quoting/type, and once-only source order. Missing, extra, unknown, duplicate, empty-splat, invalid value/calculation, temporary, argument-limit, and allocation-failure cases fail closed; direct `string.to-lower-case()` remains calculation-closed and `color.adjust` is the next explicit unavailable boundary. The test-first Debug baseline stopped at direct evaluator 269/272 and focused 402/405 before final Debug/ReleaseSafe each passed 23/23 steps and 405/405 tests. Direct evaluator coverage passes 272/272; full compiler Debug/ReleaseSafe each pass 60/60 steps and 900/900 tests; evaluator cross-compiles pass for Windows x64 GNU and Linux ARM64 GNU in both modes; native contract 12/12 plus check, provider/reference/product 151/151, package policy 6/6 plus check, runner selftest 59/59, formatting, syntax, JSON, and diff gates pass. The SDK selector was removed; production reachability, native graduation, release state, and external systems are unchanged.

`NSASS-011-META-STRING-UPPER-CASE-FUNCTION-INVOCATION` is verified at local implementation checkpoint `cd8fde7b14db8bffbdefe8d893f7f0b1655c72e9`. Fresh Dart Sass 1.101.0 probes match native SCSS and indented-Sass `meta.call()` invocation for module-owned `sass:string` `to-upper-case` and the legacy-global reference, including default/custom/unprefixed/underscore-normalized identity, distinct legacy ownership, the normalized legacy warning, positional/named `$string` binding, one-item list/map splats, ASCII-only conversion, preserved Unicode scalars, combining marks and ZWJ sequences, quoted/unquoted values, hexadecimal/simple escapes, empty strings, interpolation, result quoting/type, and once-only source order. Missing, extra, unknown, duplicate, empty-splat, invalid value/calculation, temporary, argument-limit, and allocation-failure cases fail closed; direct `string.to-upper-case()` now also rejects a deferred calculation. The test-first Debug baseline stopped at direct evaluator 265/269 and focused 398/402 before final Debug/ReleaseSafe each passed 23/23 steps and 402/402 tests. Direct evaluator coverage passes 269/269; full compiler Debug/ReleaseSafe each pass 60/60 steps and 897/897 tests; evaluator cross-compiles pass for Windows x64 GNU and Linux ARM64 GNU in both modes; native contract 12/12 plus check, provider/reference/product 151/151, package policy 6/6 plus check, runner selftest 59/59, formatting, syntax, JSON, and diff gates pass. The SDK selector was removed; lower-case reflection remains unavailable, and production reachability, native graduation, release state, and external systems are unchanged.

`NSASS-011-META-STRING-INSERT-FUNCTION-INVOCATION` is verified at local implementation checkpoint `1b55197b115c333bf943e06a78655a8506923d99`. Fresh Dart Sass 1.101.0 probes match native SCSS and indented-Sass `meta.call()` invocation for module-owned `sass:string` `insert` and the legacy-global `str-insert` reference, including default/custom/unprefixed identity, distinct legacy ownership, the normalized legacy warning, positional/named `$string`/`$insert`/`$index` binding, three-item list/map splats, ASCII and Unicode scalar insertion, combining/ZWJ sequences, quoted/unquoted values, hexadecimal/simple escapes, mixed quoting, negative/zero/out-of-range clamping, empty strings, interpolation, source-string result quoting/type, and once-only source order. Missing, extra, unknown, duplicate, empty-splat, invalid string/value/index/calculation, temporary, argument-limit, and allocation-failure cases fail closed; direct `string.insert()` now also rejects a deferred calculation in either string operand. The test-first Debug baseline stopped at direct evaluator 262/266 and focused 395/399 before final Debug/ReleaseSafe each passed 23/23 steps and 399/399 tests. Direct evaluator coverage passes 266/266; full compiler Debug/ReleaseSafe each pass 60/60 steps and 894/894 tests; evaluator cross-compiles pass for Windows x64 GNU and Linux ARM64 GNU in both modes; native contract 12/12 plus check, provider/reference/product 151/151, package policy 6/6 plus check, runner selftest 59/59, formatting, syntax, JSON, and diff gates pass. The SDK selector was removed; production reachability, native graduation, release state, and external systems are unchanged.

`NSASS-011-META-STRING-SLICE-FUNCTION-INVOCATION` is verified at local implementation checkpoint `591a5f330c134594c16de1c53ee1f32e6691dd98`. Fresh Dart Sass 1.101.0 probes match native SCSS and indented-Sass `meta.call()` invocation for module-owned `sass:string` `slice` and the legacy-global `str-slice` reference, including default/custom/unprefixed identity, distinct legacy ownership, the normalized legacy warning, positional/named `$string`/`$start-at`/optional `$end-at` binding, three-item list/map splats, ASCII and Unicode scalar slicing, combining/ZWJ sequences, quoted/unquoted values, hexadecimal/simple escapes, omitted-end behavior, negative/zero/reversed/out-of-range clamping, interpolation, result quoting/type, and once-only source order. Missing, extra, unknown, duplicate, empty-splat, invalid string/value/index/calculation, temporary, argument-limit, and allocation-failure cases fail closed; direct `string.slice()` now also rejects a deferred-calculation string. The test-first Debug baseline stopped at direct evaluator 259/263 and focused 392/396 before final Debug/ReleaseSafe each passed 23/23 steps and 396/396 tests. Direct evaluator coverage passes 263/263; full compiler Debug/ReleaseSafe each pass 60/60 steps and 891/891 tests; evaluator cross-compiles pass for Windows x64 GNU and Linux ARM64 GNU in both modes; native contract 12/12 plus check, provider/reference/product 151/151, package policy 6/6 plus check, runner selftest 59/59, formatting, syntax, JSON, and diff gates pass. The SDK selector was removed; production reachability, native graduation, release state, and external systems are unchanged.

`NSASS-011-META-STRING-INDEX-FUNCTION-INVOCATION` is verified at local implementation checkpoint `cc9a58ad2ec062afc9cfb3e9d8c3700a89a79b82`. Fresh Dart Sass 1.101.0 probes match native SCSS and indented-Sass `meta.call()` invocation for module-owned `sass:string` `index` and the legacy-global `str-index` reference, including default/custom/unprefixed identity, distinct legacy ownership, the normalized legacy warning, positional/named `$string`/`$substring` binding, two-item list/map splats, ASCII and Unicode scalar positions, combining/ZWJ sequences, quoted/unquoted values, hexadecimal/simple escapes, empty substrings, null misses, interpolation, number/null result types, and once-only source order. Missing, extra, unknown, duplicate, empty-splat, null, boolean, number, list, map, color, calculation in either operand, temporary, argument-limit, and allocation-failure cases fail closed; direct `string.index()` now also rejects either deferred-calculation operand. The test-first Debug baseline stopped at direct evaluator 256/260 and focused 389/393 before final Debug/ReleaseSafe each passed 23/23 steps and 393/393 tests. Direct evaluator coverage passes 260/260; full compiler Debug/ReleaseSafe each pass 60/60 steps and 888/888 tests; evaluator cross-compiles pass for Windows x64 GNU and Linux ARM64 GNU in both modes; native contract 12/12 plus check, provider/reference/product 151/151, package policy 6/6 plus check, runner selftest 59/59, formatting, syntax, JSON, and diff gates pass. The SDK selector was removed; production reachability, native graduation, release state, and external systems are unchanged.

`NSASS-011-META-STRING-LENGTH-FUNCTION-INVOCATION` is verified at local implementation checkpoint `0853efb093df6aab26f32ffea85e34de9a84e9d4`. Fresh Dart Sass 1.101.0 probes match native SCSS and indented-Sass `meta.call()` invocation for module-owned `sass:string` `length` and the legacy-global `str-length` reference, including default/custom/unprefixed identity, distinct legacy and global-list ownership despite inspected-name collisions, the normalized legacy warning, positional/named `$string` binding, one-item list/map splats, ASCII and Unicode scalar counting, combining/ZWJ sequences, quoted/unquoted values, hexadecimal/simple escapes, empty strings, interpolation, numeric result type, and once-only source order. Missing, extra, unknown, duplicate, empty-splat, null, boolean, number, list, map, color, calculation, temporary, argument-limit, and allocation-failure cases fail closed; direct `string.length()` now also rejects the deferred-calculation representation, and the top-level escape splitter consumes hexadecimal terminator whitespace without regressing escaped plain-CSS function evaluation. The test-first Debug baseline stopped at direct evaluator 254/257 and focused 387/390 before final Debug/ReleaseSafe each passed 23/23 steps and 390/390 tests. Direct evaluator coverage passes 257/257; full compiler Debug/ReleaseSafe each pass 60/60 steps and 885/885 tests; evaluator cross-compiles pass for Windows x64 GNU and Linux ARM64 GNU in both modes; native contract 12/12 plus check, provider/reference/product 151/151, package policy 6/6 plus check, runner selftest 59/59, formatting, syntax, JSON, and diff gates pass. The SDK selector was removed; production reachability, native graduation, release state, and external systems are unchanged.

`NSASS-011-META-STRING-UNQUOTE-FUNCTION-INVOCATION` is verified at local implementation checkpoint `33bd709eb410f402f0f75637446dc25fc8f545bb`. Fresh Dart Sass 1.101.0 probes match native SCSS and indented-Sass `meta.call()` invocation for module-owned `sass:string` `unquote` and the legacy-global `unquote` reference, including default/custom/unprefixed identity, distinct global ownership with identical inspected spelling, the normalized legacy warning, positional/named `$string` binding, one-item list/map splats, quoted/unquoted strings, escaped-space normalization, interpolation, unquoted serialization, result type, and once-only source order. Missing, extra, unknown, duplicate, empty-splat, null, boolean, number, list, map, color, calculation, temporary, argument-limit, invalid-generated-CSS, and allocation-failure cases fail closed; direct `string.unquote()` now also rejects the deferred-calculation representation without widening another string function. Dart Sass's invalid `foo"bar` quote-removal output is deliberately rejected by the recovery-disabled ZigCSS core rather than admitted as CSS. The test-first Debug baseline stopped at direct evaluator 252/254 and focused 385/387 before final Debug/ReleaseSafe each passed 23/23 steps and 387/387 tests. Direct evaluator coverage passes 254/254; full compiler Debug/ReleaseSafe each pass 60/60 steps and 882/882 tests; evaluator cross-compiles pass for Windows x64 GNU and Linux ARM64 GNU in both modes; native contract 12/12 plus check, provider/reference/product 151/151, package policy 6/6 plus check, runner selftest 59/59, formatting, syntax, JSON, and diff gates pass. The SDK selector was removed; production reachability, release state, and external systems are unchanged.

`NSASS-011-META-STRING-QUOTE-FUNCTION-INVOCATION` is verified at local implementation checkpoint `4428e957b9b0e444f44c88bfcd5593d4aa43e2b5`. Fresh Dart Sass 1.101.0 probes match native SCSS and indented-Sass `meta.call()` invocation for module-owned `sass:string` `quote` and the legacy-global `quote` reference, including default/custom/unprefixed identity, distinct global ownership with identical inspected spelling, the normalized legacy warning, positional/named `$string` binding, one-item list/map splats, quoted/unquoted strings, escaped content, interpolation, quoted serialization, result type, and once-only source order. Missing, extra, unknown, duplicate, empty-splat, null, boolean, number, list, map, color, calculation, temporary, argument-limit, and allocation-failure cases fail closed; direct `string.quote()` now also rejects the deferred-calculation representation without widening another string function. The test-first Debug baseline stopped at direct evaluator 248/251 and focused 381/384 before final Debug/ReleaseSafe each passed 23/23 steps and 384/384 tests. Direct evaluator coverage passes 251/251; full compiler Debug/ReleaseSafe each pass 60/60 steps and 879/879 tests; evaluator cross-compiles pass for Windows x64 GNU and Linux ARM64 GNU in both modes; native contract 12/12 plus check, provider/reference/product 151/151, package policy 6/6 plus check, runner selftest 59/59, formatting, syntax, JSON, and diff gates pass. The SDK selector was removed; production reachability, release state, and external systems are unchanged.

`NSASS-011-META-SELECTOR-REPLACE-FUNCTION-INVOCATION` is verified at local implementation checkpoint `98d4bc318414f04dd810fc320130a79890b0c1f4`. Fresh Dart Sass 1.101.0 probes match native SCSS and indented-Sass `meta.call()` invocation for module-owned `sass:selector` `replace` and the legacy-global `selector-replace` reference, including default/custom/unprefixed identity, false global `replace` reflection without an unprefixed import, true and distinct legacy reflection, the normalized legacy warning, positional/named `$selector`/`$original`/`$replacement` binding, list/map splats, string/existing-selector/list values, interpolation, replacement-only results, exact serialization, and once-only source order. Extend's adjacent `$extendee`/`$extender` names and all other missing, extra, unknown, duplicate, incomplete-splat, invalid-value/parent/malformed/unsupported-native-complex-replacement, selector-count/byte, temporary, argument-limit, and allocation-failure cases fail closed. The test-first Debug baseline stopped at direct evaluator 244/248 and focused 377/381 before final Debug/ReleaseSafe each passed 23/23 steps and 381/381 tests. Direct evaluator coverage passes 248/248; full compiler Debug/ReleaseSafe each pass 60/60 steps and 876/876 tests; evaluator cross-compiles pass for Windows x64 GNU and Linux ARM64 GNU in both modes; native contract 12/12 plus check, provider/reference/product 151/151, package policy 6/6 plus check, runner selftest 59/59, formatting, syntax, JSON, and diff gates pass. The SDK selector was removed; production reachability, release state, and external systems are unchanged.

`NSASS-011-META-SELECTOR-EXTEND-FUNCTION-INVOCATION` is verified at local implementation checkpoint `bff2eed61a7756244b076b8a3d72cb3ba3503377`. Fresh Dart Sass 1.101.0 probes match native SCSS and indented-Sass `meta.call()` invocation for module-owned `sass:selector` `extend` and the legacy-global `selector-extend` reference, including default/custom/unprefixed identity, false global `extend` reflection without an unprefixed import, true and distinct legacy reflection, the normalized legacy warning, positional/named `$selector`/`$extendee`/`$extender` binding, list/map splats, string/existing-selector/list values, interpolation, retained-original extension results, exact serialization, and once-only source order. Missing, extra, unknown, duplicate, incomplete-splat, invalid-value/parent/malformed/unsupported-native-extension, selector-count/byte, temporary, argument-limit, and allocation-failure cases fail closed; module-owned `selector.replace` and every other remaining selector callable family remain unavailable. The test-first Debug baseline stopped at direct evaluator 241/245 and focused 374/378 before final Debug/ReleaseSafe each passed 23/23 steps and 378/378 tests. Direct evaluator coverage passes 245/245; full compiler Debug/ReleaseSafe each pass 60/60 steps and 873/873 tests; evaluator cross-compiles pass for Windows x64 GNU and Linux ARM64 GNU in both modes; native contract 12/12 plus check, provider/reference/product 151/151, package policy 6/6 plus check, runner selftest 59/59, formatting, syntax, JSON, and diff gates pass. The SDK selector was removed; production reachability, release state, and external systems are unchanged.

`NSASS-011-META-SELECTOR-NEST-FUNCTION-INVOCATION` is verified at local implementation checkpoint `955d44b11807b9b4a6602e7411785d2db4f460f5`. Fresh Dart Sass 1.101.0 probes match native SCSS and indented-Sass `meta.call()` invocation for module-owned `sass:selector` `nest` and the legacy-global `selector-nest` reference, including default/custom/unprefixed identity, false global `nest` reflection without an unprefixed import, true and distinct legacy reflection, the normalized legacy warning, one-or-more positional binding, list splats, string/existing-selector/list values, interpolation, Cartesian lists, repeated/chained parent substitution, leading combinators, exact serialization, and once-only source order. Missing, keyword/map-splat, invalid-value/parent/malformed-selector, selector-count/byte, temporary, argument-limit, and allocation-failure cases fail closed; module-owned `selector.extend`, `selector.replace`, and every other remaining selector callable family remain unavailable. The test-first Debug baseline stopped at direct evaluator 238/242 and focused 371/375 before final Debug/ReleaseSafe each passed 23/23 steps and 375/375 tests. Direct evaluator coverage passes 242/242; full compiler Debug/ReleaseSafe each pass 60/60 steps and 870/870 tests; evaluator cross-compiles pass for Windows x64 GNU and Linux ARM64 GNU in both modes; native contract 12/12 plus check, provider/reference/product 151/151, package policy 6/6 plus check, runner selftest 59/59, formatting, syntax, JSON, and diff gates pass. The SDK selector was removed; production reachability, release state, and external systems are unchanged.

`NSASS-011-META-SELECTOR-APPEND-FUNCTION-INVOCATION` is verified at local implementation checkpoint `6adfbcc5ac5ba3b58f987295eac70807f6d2f87e`. Fresh Dart Sass 1.101.0 probes match native SCSS and indented-Sass `meta.call()` invocation for module-owned `sass:selector` `append`, including default/custom/unprefixed identity, distinct legacy list `append` ownership and warning, one-or-more positional arguments, list splats, string/existing-selector/list values, interpolation, variadic composition, exact serialization, and once-only callable/argument source order. Missing, keyword/map-splat, invalid-value, parent/leading/malformed-selector, selector-count/byte, temporary, argument-limit, and allocation-failure cases fail closed; module-owned `selector.nest` and every other remaining selector callable family remain unavailable. The test-first Debug baseline stopped at direct evaluator 235/239 and focused 368/372 before final Debug/ReleaseSafe each passed 23/23 steps and 372/372 tests. Direct evaluator coverage passes 239/239; full compiler Debug/ReleaseSafe each pass 60/60 steps and 867/867 tests; evaluator cross-compiles pass for Windows x64 GNU and Linux ARM64 GNU in both modes; native contract 12/12 plus check, provider/reference/product 151/151, package policy 6/6 plus check, runner selftest 59/59, formatting, syntax, JSON, and diff gates pass. The SDK selector was removed; production reachability, release state, and external systems are unchanged.

`NSASS-011-META-SELECTOR-UNIFY-FUNCTION-INVOCATION` is verified at local implementation checkpoint `85c7dfcccc8ffa353c4a074d3589b7a9f44fed5b`. Fresh Dart Sass 1.101.0 probes match native SCSS and indented-Sass `meta.call()` invocation for module-owned `sass:selector` `unify` and the legacy-global `selector-unify` reference, including default/custom/unprefixed identity, false global `unify` reflection without an unprefixed import, true and distinct legacy reflection, the normalized legacy warning, positional/named `$selector1`/`$selector2` binding, list/map splats, string/existing-selector/list values, interpolation, selector-list/null results, exact serialization, and once-only source order. Missing, extra, unknown, duplicate, invalid-value, parent/malformed/unsupported-native-unification, selector-count/byte, temporary, argument-limit, and allocation-failure cases fail closed; module-owned `selector.append` and every other remaining selector callable family remain unavailable. The test-first Debug baseline stopped at direct evaluator 232/236 and focused 365/369 before final Debug/ReleaseSafe each passed 23/23 steps and 369/369 tests. Direct evaluator coverage passes 236/236; full compiler Debug/ReleaseSafe each pass 60/60 steps and 864/864 tests; evaluator cross-compiles pass for Windows x64 GNU and Linux ARM64 GNU in both modes; native contract 12/12 plus check, provider/reference/product 151/151, package policy 6/6 plus check, runner selftest 59/59, formatting, syntax, JSON, and diff gates pass. The SDK selector was removed; production reachability, release state, and external systems are unchanged.

`NSASS-011-META-SELECTOR-IS-SUPERSELECTOR-FUNCTION-INVOCATION` is verified at local implementation checkpoint `a4ad37237ebd119ac38065be9dd862be65c72087`. Fresh Dart Sass 1.101.0 probes match native SCSS and indented-Sass `meta.call()` invocation for module-owned `sass:selector` `is-superselector` and the legacy-global reference, including default/custom/unprefixed identity, true and distinct global reflection, the normalized legacy warning, positional/named `$super`/`$sub` binding, list/map splats, string/existing-selector/list values, interpolation, true/false relations, and once-only source order. Missing, extra, unknown, duplicate, invalid-value, parent/malformed/unsupported-relation, selector-count/byte, temporary, argument-limit, and allocation-failure cases fail closed; module-owned `selector.unify` and every other remaining selector callable family remain unavailable. The test-first Debug baseline stopped at direct evaluator 230/233 and focused 363/366 before final Debug/ReleaseSafe each passed 23/23 steps and 366/366 tests. Direct evaluator coverage passes 233/233; full compiler Debug/ReleaseSafe each pass 60/60 steps and 861/861 tests; evaluator cross-compiles pass for Windows x64 GNU and Linux ARM64 GNU in both modes; native contract 12/12 plus check, provider/reference/product 151/151, package policy 6/6 plus check, runner selftest 59/59, formatting, syntax, JSON, and diff gates pass. The SDK selector was removed; production reachability, release state, and external systems are unchanged.

`NSASS-011-META-SELECTOR-SIMPLE-SELECTORS-FUNCTION-INVOCATION` is verified at local implementation checkpoint `964d2163caa92213d589f8ff7f0addde1fb16005`. Fresh Dart Sass 1.101.0 probes match native SCSS and indented-Sass `meta.call()` invocation for module-owned `sass:selector` `simple-selectors` and the legacy-global reference, including default/custom/unprefixed identity, true and distinct global reflection, the normalized legacy warning, positional/named `$selector` binding, list/map splats, string/existing-selector values, interpolation, exact simple-selector ordering and serialization, item types, and once-only source order. Missing, extra, unknown, duplicate, invalid-value, complex/list/parent-selector, selector-count/byte, temporary, argument-limit, and allocation-failure cases fail closed; module-owned `selector.is-superselector` and every other remaining selector callable family remain unavailable. The focused test-first Debug baseline stopped at 359/363 before final Debug/ReleaseSafe each passed 23/23 steps and 363/363 tests. Direct evaluator coverage passes 230/230; full compiler Debug/ReleaseSafe each pass 60/60 steps and 858/858 tests; evaluator cross-compiles pass for Windows x64 GNU and Linux ARM64 GNU in both modes; native contract 12/12 plus check, provider/reference/product 151/151, package policy 6/6 plus check, runner selftest 59/59, formatting, syntax, JSON, and diff gates pass. The SDK selector was removed; production reachability, release state, and external systems are unchanged.

`NSASS-011-META-SELECTOR-PARSE-FUNCTION-INVOCATION` is verified at local implementation checkpoint `7064ec8dea362750409ee57600407012c088c9d8`. Fresh Dart Sass 1.101.0 probes match native SCSS and indented-Sass `meta.call()` invocation for module-owned `sass:selector` `parse`, including default/custom/unprefixed identity, false global reflection, positional/named `$selector` binding, list/map splats, string/list/existing-selector values, interpolation, normalized serialization, leading combinators, and once-only source order. Missing, extra, unknown, duplicate, invalid-value, malformed-parent, selector-count/byte, temporary, argument-limit, and allocation-failure cases fail closed; module-owned `selector.simple-selectors` and every other remaining selector callable family remain unavailable. The focused test-first Debug baseline stopped at 357/360 before final Debug/ReleaseSafe each passed 23/23 steps and 360/360 tests. Direct evaluator coverage passes 227/227; full compiler Debug/ReleaseSafe each pass 60/60 steps and 855/855 tests; evaluator cross-compiles pass for Windows x64 GNU and Linux ARM64 GNU in both modes; native contract 12/12 plus check, provider/reference/product 151/151, package policy 6/6 plus check, runner selftest 59/59, formatting, syntax, JSON, and diff gates pass. The SDK selector was removed; production reachability, release state, and external systems are unchanged.

`NSASS-011-META-MATH-RANDOM-FUNCTION-INVOCATION` is verified at local implementation checkpoint `c217d28f3e8618885fb78b38d7ff0a60f79ba1d6`. Fresh Dart Sass 1.101.0 probes match native SCSS and indented-Sass `meta.call()` invocation for module-owned `sass:math` `random` and the legacy-global reference, including default/custom/unprefixed ownership, true and distinct global reflection, normalized legacy and unit warnings, omitted/null and positional/named limits, list/map/empty splats, map override behavior, the full positive 32-bit bound, exact once-only source order, ranges, and selected binding failures. Four identical-source oracle compilations produced four distinct outputs; native replay remains deliberately deterministic through its existing source-seeded stream. Decimal, zero, negative, oversized, string, dynamic, extra, unknown, duplicate, argument-limit, and allocation-failure cases fail closed; selector and every other remaining callable family remain unavailable. The focused test-first Debug baseline stopped at 354/357 before final Debug/ReleaseSafe each passed 23/23 steps and 357/357 tests. Direct evaluator coverage passes 224/224; full compiler Debug/ReleaseSafe each pass 60/60 steps and 852/852 tests; evaluator cross-compiles pass for Windows x64 GNU and Linux ARM64 GNU in both modes; native contract 12/12 plus check, provider/reference/product 151/151, package policy 6/6 plus check, runner selftest 59/59, formatting, syntax, JSON, and diff gates pass. The SDK selector was removed; production reachability, release state, and external systems are unchanged.

`NSASS-011-META-MATH-MAX-FUNCTION-INVOCATION` is verified at local implementation checkpoint `5a23c9828094ef08dbde62a355d56a2f8ae8dce3`. Fresh Dart Sass 1.101.0 probes match native SCSS and indented-Sass `meta.call()` invocation for module-owned `sass:math` `max` and the legacy-global reference, including default/custom/unprefixed ownership, true and distinct global reflection, normalized legacy warnings, one-or-more positional values, list splats, converted and mixed unitless/unitful values, selected-argument unit spelling, and once-only source order. Empty, keyword, map-splat, string, list, incompatible-unit, argument-limit, and allocation-failure cases fail closed; module-owned `math.random` and every other remaining callable family remain unavailable. The focused test-first Debug baseline stopped at 350/354 before final Debug/ReleaseSafe each passed 23/23 steps and 354/354 tests. Direct evaluator coverage passes 221/221; full compiler Debug/ReleaseSafe each pass 60/60 steps and 849/849 tests; evaluator cross-compiles pass for Windows x64 GNU and Linux ARM64 GNU in both modes; native contract 12/12 plus check, provider/reference/product 151/151, package policy 6/6 plus check, runner selftest 59/59, formatting, syntax, JSON, and diff gates pass. The SDK selector was removed; production reachability, release state, and external systems are unchanged.

`NSASS-011-META-MATH-MIN-FUNCTION-INVOCATION` is verified at local implementation checkpoint `1a009fdbfea524d55015f7c70508ac79767c3090`. Fresh Dart Sass 1.101.0 probes match native SCSS and indented-Sass `meta.call()` invocation for module-owned `sass:math` `min` and the legacy-global reference, including default/custom/unprefixed ownership, true and distinct global reflection, normalized legacy warnings, one-or-more positional values, list splats, converted and mixed unitless/unitful values, selected-argument unit spelling, and once-only source order. Empty, keyword, map-splat, string, list, incompatible-unit, argument-limit, and allocation-failure cases fail closed; module-owned `math.max` and every remaining callable family remain unavailable. The focused test-first Debug baseline stopped at 347/351 before final Debug/ReleaseSafe each passed 23/23 steps and 351/351 tests. Direct evaluator coverage passes 218/218; full compiler Debug/ReleaseSafe each pass 60/60 steps and 846/846 tests; evaluator cross-compiles pass for Windows x64 GNU and Linux ARM64 GNU in both modes; native contract 12/12 plus check, provider/reference/product 151/151, package policy 6/6 plus check, runner selftest 59/59, formatting, syntax, JSON, and diff gates pass. The SDK selector was removed; production reachability, release state, and external systems are unchanged.

`NSASS-011-META-MATH-HYPOT-FUNCTION-INVOCATION` is verified at local implementation checkpoint `29bcfede3ff977272fdae4725b46c367b74ed1a9`. Fresh Dart Sass 1.101.0 probes match native SCSS and indented-Sass `meta.call()` invocation for module-owned `sass:math` `hypot`, including default/custom/unprefixed ownership, false global reflection, one-or-more positional values, list splats, unit conversion, first-argument unit spelling, and once-only source order. Empty, keyword, map-splat, string, list, mixed-unit, and incompatible-unit calls fail closed; module-owned `math.min` and every remaining callable family remain unavailable. The focused test-first Debug baseline stopped at 344/348 before final Debug/ReleaseSafe each passed 23/23 steps and 348/348 tests. Direct evaluator coverage passes 215/215; full compiler Debug/ReleaseSafe each pass 60/60 steps and 843/843 tests; evaluator cross-compiles pass for Windows x64 GNU and Linux ARM64 GNU in both modes; native contract 12/12 plus check, provider/reference/product 151/151, package policy 6/6 plus check, runner selftest 59/59, formatting, syntax, JSON, Bash, and diff gates pass. The SDK selector was removed; production reachability, release state, and external systems are unchanged.

`NSASS-011-META-MATH-UNITLESS-FUNCTION-INVOCATION` is verified at local implementation checkpoint `934ad1f5efced95621cc0163cdaf73aacc9536ab`. Fresh Dart Sass 1.101.0 probes match native SCSS and indented-Sass `meta.call()` invocation for `sass:math` `is-unitless`, including default/custom/unprefixed module ownership, the true legacy `unitless` origin, canonical positional/named `$number` binding, list/map splats, dimensionless/unitful boolean results, and once-only source order. Missing, extra, unknown, duplicate, string, and list bindings fail closed; `math.unit` and every remaining callable family remain unavailable. The focused test-first Debug baseline stopped at 312/314 before final Debug/ReleaseSafe each passed 23/23 steps and 314/314 tests. Direct evaluator coverage passes 181/181; full compiler Debug/ReleaseSafe each pass 60/60 steps and 809/809 tests; evaluator cross-compiles pass for Windows x64 GNU and Linux ARM64 GNU in both modes; native contract 10/10 plus check, provider/reference/product 151/151, package policy 6/6 plus check, runner selftest 58/58, formatting, syntax, JSON, and diff gates pass. The SDK selector was removed; production reachability, release state, and external systems are unchanged.

`NSASS-011-META-MATH-COMPATIBILITY-FUNCTION-INVOCATION` is verified at local implementation checkpoint `f6216b905538cae8ee9f572c37fd280f18a08a6c`. Fresh Dart Sass 1.101.0 probes match native SCSS and indented-Sass `meta.call()` invocation for `sass:math` `compatible`, including default/custom/unprefixed module ownership, the true legacy `comparable` origin, canonical positional/named `$number1`/`$number2` binding, list/map splats, known/custom/compound unit compatibility, boolean results, and once-only source order. Missing, extra, unknown, duplicate, string, and list bindings fail closed; `math.is-unitless` and every remaining callable family remain unavailable. The focused test-first Debug baseline stopped at 310/312 before final Debug/ReleaseSafe each passed 23/23 steps and 312/312 tests. Direct evaluator coverage passes 179/179; full compiler Debug/ReleaseSafe each pass 60/60 steps and 807/807 tests; evaluator cross-compiles pass for Windows x64 GNU and Linux ARM64 GNU in both modes; native contract 10/10 plus check, provider/reference/product 151/151, package policy 6/6 plus check, runner selftest 58/58, formatting, syntax, JSON, and diff gates pass. The SDK selector was removed; production reachability, release state, and external systems are unchanged.

`NSASS-011-META-MATH-UNARY-FUNCTION-INVOCATION` is verified at local implementation checkpoint `798a3d8359b539c69abc4b396cf9c019fc9a6752`. Fresh Dart Sass 1.101.0 probes match native SCSS and indented-Sass `meta.call()` invocation for `abs`, `ceil`, `floor`, `round`, and `percentage`, including default/custom/unprefixed module ownership, true legacy-global origins, canonical positional/named `$number` binding, list/map splats, numeric units, percentage conversion, and once-only source order. Missing, extra, unknown, duplicate, non-number, and unitful-percentage bindings fail closed; `math.compatible` and every remaining callable family remain unavailable. The focused test-first Debug baseline stopped at 307/310 before final Debug/ReleaseSafe each passed 23/23 steps and 310/310 tests. Direct evaluator coverage passes 177/177; full compiler Debug/ReleaseSafe each pass 60/60 steps and 805/805 tests; evaluator cross-compiles pass for Windows x64 GNU and Linux ARM64 GNU in both modes; native contract 10/10 plus check, provider/reference/product 151/151, package policy 6/6 plus check, runner selftest 58/58, formatting, syntax, JSON, and diff gates pass. The SDK selector was removed; production reachability, release state, and external systems are unchanged.

`NSASS-011-META-EXISTENCE-FUNCTION-INVOCATION` is verified at local implementation checkpoint `272dc9fa7c52e01bb4ca598175d4038df1ea323a`. Fresh Dart Sass 1.101.0 probes match native SCSS and indented-Sass `meta.call()` invocation for all five `sass:meta` existence-query references, including default/custom/unprefixed module ownership, true legacy-global origins, independent feature deprecation, canonical positional/named/optional binding, list/map splats, lexical call-site scope, normalized names, loaded-module reflection, and once-only source order. Missing, extra, unknown, type-invalid, and unloaded-module bindings fail closed; math and every other callable family remain unavailable. The focused test-first Debug baseline stopped at 305/308 before final Debug/ReleaseSafe each passed 23/23 steps and 308/308 tests. Direct evaluator coverage passes 175/175; full compiler Debug/ReleaseSafe each pass 60/60 steps and 803/803 tests; evaluator cross-compiles pass for Windows x64 GNU and Linux ARM64 GNU in both modes; native contract 10/10 plus check, provider/reference/product 151/151, package policy 6/6 plus check, runner selftest 58/58, formatting, syntax, JSON, and diff gates pass. The SDK selector was removed; production reachability, release state, and external systems are unchanged.

`NSASS-011-META-CALCULATION-FUNCTION-INVOCATION` is verified at local implementation checkpoint `8b103a797938e52fc583cec5d6c0875f9b3c6880`. Fresh Dart Sass 1.101.0 probes match native SCSS and indented-Sass `meta.call()` invocation for the exact module-owned `sass:meta` `calc-name` and `calc-args` references, including default/custom/unprefixed ownership, positional/named `$calc` binding, list/map splats, retained calculation values, and once-only source order. Missing, extra, unknown, duplicate, and non-calculation bindings fail closed; meta existence and every other callable family remain unavailable. The focused test-first Debug baseline stopped at 304/306 before final Debug/ReleaseSafe each passed 23/23 steps and 306/306 tests. Direct evaluator coverage passes 173/173; full compiler Debug/ReleaseSafe each pass 60/60 steps and 801/801 tests; evaluator cross-compiles pass for Windows x64 GNU and Linux ARM64 GNU in both modes; native contract 10/10 plus check, provider/reference/product 151/151, package policy 6/6 plus check, runner selftest 58/58, formatting, syntax, JSON, and diff gates pass. The SDK selector was removed; production reachability, release state, and external systems are unchanged.

`NSASS-011-META-CONTENT-ACCEPTANCE-FUNCTION-INVOCATION` is verified at local implementation checkpoint `45593411e739f4837fbb4aecc8f63cd13e4f9f14`. Fresh Dart Sass 1.101.0 probes match native SCSS and indented-Sass `meta.call()` invocation for the exact module-owned `sass:meta` `accepts-content` reference, including default/custom/unprefixed ownership, positional/named `$mixin` binding, list/map splats, once-only source order, user mixins, and built-in `load-css`/`apply` mixins. Missing, extra, unknown, and non-mixin bindings fail closed; meta calculation and every other callable family remain unavailable. The focused test-first Debug baseline stopped at 303/305 before final Debug/ReleaseSafe each passed 23/23 steps and 305/305 tests. Full compiler Debug/ReleaseSafe each pass 60/60 steps and 800/800 tests; evaluator cross-compiles pass for Windows x64 GNU and Linux ARM64 GNU in both modes; native contract 10/10 plus check, provider/reference/product 151/151, package policy 6/6 plus check, runner selftest 58/58, formatting, syntax, JSON, and diff gates pass. The SDK selector was removed; production reachability, release state, and external systems are unchanged.

`NSASS-011-META-KEYWORDS-FUNCTION-INVOCATION` is verified at local implementation checkpoint `ad4ac970dfd6a095ba3c7c6b18d3100f7eb74080`. Fresh Dart Sass 1.101.0 probes match native SCSS and indented-Sass `meta.call()` invocation for `sass:meta` `keywords`, including exact custom and unprefixed module ownership, the callable-only legacy-global origin, positional/named `$args`, named `$function`, list/map/argument-list splats, rest forwarding, normalized keyword spelling, shared keyword-consumption state, and once-only source order. Missing, extra, unknown, or non-argument-list binding, non-callables, mixins, every other unsupported built-in family, special `if()`, expanded limits, direct callable CSS serialization, cancellation, and every allocation failure expose no partial CSS. Direct evaluator coverage passes 171/171; focused native Debug/ReleaseSafe each pass 23/23 with 304/304 tests; full compiler Debug/ReleaseSafe each pass 60/60 with 799/799 tests; and Windows x64 GNU plus Linux ARM64 GNU compile-only evaluator gates pass in both modes. Native contract 10/10 plus check, complete provider/reference/product coverage 151/151, package policy 6/6 plus check, autonomous-runner 58/58, repository Zig formatting, JavaScript/JSON syntax, and diff checks pass. Both native Sass rows remain private and production-unreachable, all unmeasured callable families remain closed, the release interlock remains closed, the temporary SDK selector was removed, and no external action occurred.

`NSASS-011-META-INSPECTION-FUNCTION-INVOCATION` is verified at local implementation checkpoint `85d89299ec761c54d9e69c4c8d0e1e5107bc1e56`. Fresh Dart Sass 1.101.0 probes match native SCSS and indented-Sass `meta.call()` invocation for `sass:meta` `inspect` and `type-of`, including exact custom and unprefixed module ownership, both legacy-global origins, positional/named binding, list/map/argument-list splats, rest forwarding, nested callable/value inspection, and once-only source order. Missing, extra, or unknown binding, non-callables, mixins, every other built-in family, special `if()`, expanded limits, direct callable CSS serialization, cancellation, and every allocation failure expose no partial CSS. Direct evaluator coverage passes 169/169; focused native Debug/ReleaseSafe each pass 23/23 with 302/302 tests; full compiler Debug/ReleaseSafe each pass 60/60 with 797/797 tests; and Windows x64 GNU plus Linux ARM64 GNU compile-only evaluator gates pass in both modes. Native contract 10/10 plus check, complete provider/reference/product coverage 151/151, package policy 6/6 plus check, autonomous-runner 58/58, repository Zig formatting, JavaScript/JSON syntax, and diff checks pass. Both native Sass rows remain private and production-unreachable, all unmeasured callable families remain closed, the release interlock remains closed, the temporary SDK selector was removed, and no external action occurred.

`NSASS-011-META-MAP-MUTATION-FUNCTION-INVOCATION` is verified at local implementation checkpoint `3b559d6defb98f206ecab459a3a87ae09ba9c306`. Fresh Dart Sass 1.101.0 probes match native SCSS and indented-Sass `meta.call()` invocation for immutable `sass:map` `merge`, `remove`, `set`, `deep-merge`, and `deep-remove`, including exact custom and unprefixed module ownership, both legacy-global origins, positional/measured named binding, list/map/argument-list splats, rest forwarding, nested key paths, immutable order, and once-only source order. Invalid binding, unsupported variadic keywords, non-callables, mixins, other built-in families, special `if()`, expanded limits, direct callable CSS serialization, cancellation, and every allocation failure expose no partial CSS. Direct evaluator coverage passes 167/167; focused native Debug/ReleaseSafe each pass 23/23 with 300/300 tests; full compiler Debug/ReleaseSafe each pass 60/60 with 795/795 tests; and Windows x64 GNU plus Linux ARM64 GNU compile-only evaluator gates pass in both modes. Native contract 10/10 plus check, complete provider/reference/product coverage 151/151, package policy 6/6 plus check, autonomous-runner 58/58, repository Zig formatting, JavaScript/JSON syntax, and diff checks pass. Both native Sass rows remain private and production-unreachable, every other unsupported callable family remains closed, the release interlock remains closed, the temporary SDK selector was removed, and no external action occurred.

`NSASS-011-META-MAP-QUERY-FUNCTION-INVOCATION` is verified at local implementation checkpoint `9822b9cb5811cae8a0c533e0ec52da529d495bf4`. Fresh Dart Sass 1.101.0 probes match native SCSS and indented-Sass `meta.call()` invocation for read-only `sass:map` `get`, `has-key`, `keys`, and `values`, including exact custom and unprefixed module ownership, all four legacy-global origins, positional/named binding, list/map/argument-list splats, rest forwarding, variadic key paths, null/missing behavior, and once-only source order. Invalid binding, variadic keywords, non-callables, mixins, map mutations, other built-in families, special `if()`, expanded limits, direct callable CSS serialization, cancellation, and every allocation failure expose no partial CSS. Direct evaluator coverage passes 165/165; focused native Debug/ReleaseSafe each pass 23/23 with 298/298 tests; full compiler Debug/ReleaseSafe each pass 60/60 with 793/793 tests; and Windows x64 GNU plus Linux ARM64 GNU compile-only evaluator gates pass in both modes. Native contract 10/10 plus check, complete provider/reference/product coverage 151/151, package policy 6/6 plus check, autonomous-runner 58/58, repository Zig formatting, JavaScript/JSON syntax, and diff checks pass. Both native Sass rows remain private and production-unreachable, map mutation and every other unsupported callable family remain closed, the release interlock remains closed, the temporary SDK selector was removed, and no external action occurred.

`NSASS-011-META-LIST-FUNCTION-INVOCATION` is verified at local implementation checkpoint `198aa1bb809820bd260b56fd18ddd7e825e79ac7`. Fresh Dart Sass 1.101.0 probes match native SCSS and indented-Sass `meta.call()` invocation for the complete `sass:list` function-reference family, including custom and unprefixed module ownership, nine legacy-global origins, positional/named binding, omitted `join()` defaults, list/map/argument-list splats, rest forwarding, keyword-rest consumption, variadics, and once-only source order. Invalid binding, variadic keywords, non-callables, mixins, other built-in families, special `if()`, expanded limits, direct callable CSS serialization, cancellation, and every allocation failure expose no partial CSS. Direct evaluator coverage passes 163/163; focused native Debug/ReleaseSafe each pass 23/23 with 296/296 tests; full compiler Debug/ReleaseSafe each pass 60/60 with 791/791 tests; and Windows x64 GNU plus Linux ARM64 GNU compile-only evaluator gates pass in both modes. Native contract 10/10 plus check, complete provider/reference/product coverage 151/151, package policy 6/6 plus check, autonomous-runner 58/58, repository Zig formatting, JavaScript/JSON syntax, and diff checks pass. Both native Sass rows remain private and production-unreachable, every other built-in-reference family and mixin application remain closed, the release interlock remains closed, the temporary SDK selector was removed, and no external action occurred.

`NSASS-011-META-USER-FUNCTION-INVOCATION` is verified at local implementation checkpoint `da8c09f44c25f52d8838fb58e62f50679aea763e`. Fresh Dart Sass 1.101.0 probes match native SCSS and indented-Sass `meta.call()` invocation of declaration-stable user-function references across default/custom/unprefixed namespaces, the legacy global warning, defaults, positional/named binding, list/map/argument-list splats, rest forwarding, keyword-rest consumption, nested calls, and exact once-only source order. Non-callables, mixin references, built-in and special-function references, invalid target binding, expanded limits, direct callable CSS serialization, cancellation, and every allocation failure expose no partial CSS. Direct evaluator coverage passes 161/161; focused native Debug/ReleaseSafe each pass 23/23 with 294/294 tests; full compiler Debug/ReleaseSafe each pass 60/60 with 789/789 tests; and Windows x64 GNU plus Linux ARM64 GNU compile-only evaluator gates pass in both modes. Native contract 10/10 plus check, complete provider/reference/product coverage 151/151, package policy 6/6 plus check, autonomous-runner 58/58, repository Zig formatting, JavaScript/JSON syntax, and diff checks pass. Both native Sass rows remain private and production-unreachable, built-in function invocation and mixin application remain closed, the release interlock remains closed, the temporary SDK selector was removed, and no external action occurred.

`NSASS-011-META-CALLABLE-INSPECTION` is verified at local implementation checkpoint `4850e7efe53491d8fac7099092ba2accfb6aadcc`. Fresh Dart Sass 1.101.0 probes match native SCSS and indented-Sass inspection for user and built-in function/mixin references, normalized aliases, legacy-global and loaded-module naming, the special `if()` reference, all seven module naming families, and nested collection punctuation. Invalid encoded identities, direct callable CSS serialization, callable invocation, temporary exhaustion, cancellation, and every allocation failure expose no partial CSS. Direct evaluator coverage passes 158/158; focused native Debug/ReleaseSafe each pass 23/23 with 291/291 tests; full compiler Debug/ReleaseSafe each pass 60/60 with 786/786 tests; and Windows x64 GNU plus Linux ARM64 GNU compile-only evaluator gates pass in both modes. Native contract 10/10 plus check, complete provider/reference/product coverage 151/151, package policy 6/6 plus check, autonomous-runner 58/58, repository Zig formatting, JavaScript/JSON syntax, and diff checks pass. Both native Sass rows remain private and production-unreachable, the release interlock remains closed, the temporary SDK selector was removed, and no external action occurred.

`NSASS-011-META-FUNCTION-REFERENCES` is verified at local implementation checkpoint `d41e1ab83cc2d9dc540e0aaeed2b217443f1afca`. Fresh Dart Sass 1.101.0 probes match native SCSS and indented-Sass output for declaration-stable user function references, exact once-only `$name`/`$css`/`$module` evaluation order, false/null Sass selection, normalized and escaped names, module aliases, unprefixed members, global-versus-module identity, and the special legacy `if()` reference. Truthy `$css` plain-CSS references, missing or invalid lookup, callable invocation/inspection, direct CSS serialization, temporary exhaustion, and every allocation failure expose no partial CSS. Direct evaluator coverage passes 157/157; focused native Debug/ReleaseSafe each pass 23/23 with 290/290 tests; full compiler Debug/ReleaseSafe each pass 60/60 with 785/785 tests; and Windows x64 GNU plus Linux ARM64 GNU compile-only evaluator gates pass in both modes. Native contract 10/10 plus check, complete provider/reference/product coverage 151/151, package policy 6/6 plus check, autonomous-runner 58/58, repository Zig formatting, JavaScript/JSON syntax, and diff checks pass. Both native Sass rows remain private and production-unreachable, the release interlock remains closed, the existing isolated SDK selector changed no tracked or system state, and no external action occurred.

`NSASS-011-LEGACY-IF-DUAL-SPLAT` is verified at local implementation checkpoint `c23672df4b232031e39afc46f824cc6c6943f144`. Fresh Dart Sass 1.101.0 probes match native SCSS and indented-Sass output for the exact adjacent terminal positional-rest plus keyword-rest pair, including first-rest/second-rest/direct evaluation order, scalar/list/map/argument-list first-rest expansion, strict string-keyed map second-rest expansion, direct/first/second keyword precedence, exact key spelling, and the absence of `misplaced-rest`. Non-map keyword rest values, invalid keys/bindings, expanded limits, nonterminal pairs, three or more splats, and every allocation failure expose no partial CSS. Focused native Debug/ReleaseSafe each pass 23/23 with 287/287 tests; full compiler Debug/ReleaseSafe each pass 60/60 with 782/782 tests; and Windows x64 GNU plus Linux ARM64 GNU compile-only evaluator gates pass in both modes. Native contract 10/10 plus check, complete provider/reference/product coverage 151/151, package policy 6/6 plus check, autonomous-runner 58/58, repository Zig formatting, JavaScript/JSON syntax, and diff checks pass. Both native Sass rows remain private and production-unreachable, the release interlock remains closed, the existing isolated SDK selector changed no tracked or system state, and no external action occurred.

`NSASS-011-LEGACY-IF-MISPLACED-REST` is verified at local implementation checkpoint `a1d57820be76f5ed2f3ac05e5303e7247b3b1e6a`. Fresh Dart Sass 1.101.0 probes match native SCSS and indented-Sass output for one non-final scalar/list/map rest argument, direct positional and named arguments, rest-first side effects, direct-before-expanded binding, separate `misplaced-rest`/`if-function` warning order, and the combined omitted-warning summary. Exact two terminal splats remain fail-closed. Focused native Debug/ReleaseSafe each pass 23/23 with 286/286 tests; full compiler Debug/ReleaseSafe each pass 60/60 with 781/781 tests; and Windows x64 GNU plus Linux ARM64 GNU compile-only evaluator gates pass in both modes. Native contract 10/10 plus check, complete provider/reference/product coverage 151/151, package policy 6/6 plus check, autonomous-runner 58/58, repository Zig formatting, JavaScript/JSON syntax, and diff checks pass. Both native Sass rows remain private and production-unreachable, the release interlock remains closed, the temporary SDK selector was removed, and no external action occurred.

`NSASS-011-LEGACY-IF-FINAL-SPLAT` is verified at local implementation checkpoint `dad88d5df9e34e1f0978bc9c97ad689b6ff788ef`. Fresh Dart Sass 1.101.0 probes match native SCSS and indented-Sass output for final comma/space-list, scalar, exact-map, and rest-argument-list splats; tests pin rest-first then eager direct-argument evaluation, exact map-key binding and override order, the existing `if-function` warning throttle, and the unchanged lazy behavior of splat-free calls. Invalid bindings/keys, eager failures, expanded limits, non-final/multiple unsupported forms, cancellation ownership, and every allocation failure expose no partial CSS. Focused native Debug/ReleaseSafe each pass 23/23 with 285/285 tests; full compiler Debug/ReleaseSafe each pass 60/60 with 780/780 tests; and Windows x64 GNU plus Linux ARM64 GNU compile-only evaluator gates pass in both modes. Native contract 10/10 plus check, complete provider/reference/product coverage 151/151, package policy 6/6 plus check, autonomous-runner 58/58, repository Zig formatting, JavaScript/JSON syntax, and diff checks pass. Both native Sass rows remain private and production-unreachable, the release interlock remains closed, and no external action occurred.

`NSASS-011-PLAIN-CSS-FUNCTION-ARGUMENTS` is verified at local implementation checkpoint `be99154df9ffd94bd6fca95bef7598520e043fba`. Fresh Dart Sass 1.101.0 probes match native SCSS and indented-Sass output for arithmetic, module and user calls, nested calls, splats, interpolated and escaped names, null/empty/trailing arguments, `var()` and data-URL preservation, and source-order evaluation. Callable values, maps, keywords, known deferred wrappers, function-argument exhaustion, temporary exhaustion, cancellation, and allocation failures fail closed. Direct evaluator coverage passes 148/148; focused native Debug/ReleaseSafe each pass 23/23 with 281/281 tests; full compiler Debug/ReleaseSafe each pass 60/60 with 776/776 tests; and Windows x64 GNU plus Linux ARM64 GNU compile-only evaluator gates pass in both modes. Native contract 10/10 plus check, canonical Sass 28/28, five-language product 30/30, package policy 6/6 plus check, autonomous-runner 58/58, repository Zig formatting, JavaScript/JSON syntax, and diff checks pass. Both native Sass rows remain private and production-unreachable, the release interlock remains closed, and no external action occurred.

`NSASS-011-META-CONTENT-ACCEPTANCE` is verified at local implementation checkpoint `7da73bb7e17dd26859ff6a5b35d1bd0f23452041`. Exact Dart Sass 1.101.0 probes match the native SCSS and indented-Sass results for stable old/new user-mixin references, direct and nested content declarations, default/custom/unprefixed namespaces, one-time keyword binding, and the exact built-in `load-css` false / `apply` true contract. Direct evaluator Debug/ReleaseSafe pass 146/146; focused native Debug/ReleaseSafe each pass 23/23 with 279/279 tests; full compiler Debug/ReleaseSafe each pass 60/60 with 774/774 tests; and Windows x64 GNU plus Linux ARM64 GNU compile-only evaluator gates pass in both modes. Native contract 10/10 plus check, canonical Sass 28/28, five-language product 30/30, package policy 6/6 plus check, autonomous-runner 58/58, repository Zig formatting, JavaScript/JSON syntax, and diff checks pass. Both native Sass rows remain private and production-unreachable, the release interlock remains closed, and no external action occurred. The sibling scan's nested callable-serialization gap is recorded above and remains outside this checkpoint.

`NATIVE-005` is verified at implementation `90947e6`. Its regression suite was red while `src/preprocessor/evaluator.zig` was absent, then twelve evaluator cases passed in Debug and ReleaseSafe; combined native foundation coverage is 55/55. Transactional staging, normalized UTF-8/LF emission, UTF-16 positions, operations/loops/calls/diagnostic/map/output ceilings, deterministic replay, resolver fact ownership, late phase cancellation, explicit abort, core diagnostic rejection, independent validated CSS/map ceilings, terminal state, caller mutation/deinit, and every allocation failure are executable. Exact Zig 0.15.2 Debug and ReleaseSafe each pass 46/46 steps and 550/550 tests. Native contract schema v2 binds four source/test inventories and passes 8/8 plus check; workflow, dependency, Zig-package, preprocessor-package, documentation 7/7 plus check, formatting, and diff gates pass. Exact-main Build `29614839695` passed Test Suite `87997549492` and all five native hosts: Linux ARM64 `87997549514`, macOS x64 `87997549521`, Windows x64 `87997549531`, macOS ARM64 `87997549541`, and Linux x64 `87997549557`. All four preprocessor adapters remain `reference-only`; native release readiness remains false; no provider, production dependency, public capability, version, tag, release, or package changed. `NSASS-010` is active next.

`PRE-009` is verified at source anchor `5260028`, archive-binding checkpoint `376ace8`, and exact-main ledger head `d026b9b`. The exact version, source archive, Homebrew build, package/SBOM, native release metadata, direct archive, offline npm lifecycle, canonical providers, public API, core, site, documentation, audit, editor, compatibility, transform, benchmark-policy, syntax, formatting, and deterministic-output gates pass. Exact-main Build `29555992594` passed Test Suite `87808031910` plus all five architecture-matched native archive/install/offline/five-language/API jobs (`87808031913`, `87808031923`, `87808031930`, `87808031932`, `87808031940`). Documentation/Pages `29555992555` passed build `87808031885` and deploy `87808092086`; the public homepage serves the deployed five-language title and metadata from `index-Ei4aLU8z.js`. Candidate `0.5.0-rc.1` remains technically verified reference evidence, but the owner cancelled its publication before tag creation in favor of ADR-013's self-contained native target; no tag, package, release, or signed provenance was created.

### PRE-008 local evidence reproduced by hosted closure

The source package contract accepts CSS plus canonical SCSS, indented Sass, Less, and Stylus, while arbitrary project plugins, custom functions/importers, JavaScript evaluation, and other executable extension points remain outside the default trust boundary. The exact format matrix passes eight adapter rows, four canonical provider implementations, four combined-CLI probes, five rejected extension probes, and independent CSS Modules evidence. The complete preprocessor/product gate passes 151/151, including all five exact website source/output examples. Exact Zig 0.15.2 Debug and ReleaseSafe each pass 38/38 steps and 495/495 tests. The site passes 138/138 plus its production build; desktop and 390 px browser QA pass with no horizontal document overflow after the mobile containment regression. Documentation integrity passes 7/7 over 37 Markdown files, 68 fences (53 executable; nine site-rendered), 48 links, and 24 routes. Capability status is exact at 25 rows/24 gates; the package validator passes 6/6 over 22 runtime sources, 28 archive files, 86 locked dependencies, and five native targets. Wrapper, workflow, version, release-metadata, dependency, prefix, compatibility, transform, generated-state, and diff gates pass; root/docs/VS Code production audits are 0/0/0. npm `0.4.0-rc.3` remains the published CSS-only release, and no `0.5.x` publication action has occurred. Hosted Build and Documentation/Pages reproduced these bytes before `PRE-009` began.

### Historical validation records

`PRE-006` is implemented locally and awaiting hosted verification. The production package now exports only the launcher, `zigcss/api`, and its manifest; its exact 28-file allowlist includes the bounded product/core/preprocessor runtime, generated SPDX 2.3 SBOM, third-party notices, README, and licenses but excludes corpora, tests, development tools, and release preload. Exact production pins are `sass` 1.101.0, `less` 4.6.7, `stylus` 0.64.0, and `image-size` 0.5.5 under Node >=20.19.0. One deterministic validator owns all 22 runtime sources, five external runtime imports, the complete 86-package transitive/optional lock closure, canonical registry URLs and SHA-512 integrity, five reviewed licenses, the sole optional `@parcel/watcher` lifecycle, the exact reviewed `glob` 10.5.0 deprecation exception, the linked docs consumer lock, five native targets, archive/installed inventories, size ceilings, workflow gates, and npm provenance. The canonical provider child disables HTTP(S), net/TLS, UDP, DNS, `fetch`, and WebSocket before request/provider execution. The final local verification passes 147/147 combined preprocessor tests; exact Zig 0.15.2 Debug and ReleaseSafe each pass 38/38 steps and 495/495 tests; the package validator passes 6/6; policy gates pass 79/79; workflow/version/release/dependency/prefix/capability/format/compatibility/transform checks pass; JavaScript syntax, Zig formatting, actionlint 1.7.12, generated-state, and diff checks pass. Documentation integrity passes 7/7 over 37 Markdown files/67 fences/52 links/24 routes, while the site passes 137/137 plus production build. A lifecycle-disabled clean install produces a valid production graph; `npm ls --omit=dev --all` is clean; full and root/docs/VS Code production audits report zero findings. The exact pack is 28 files, 90,761 packed bytes, and 400,960 unpacked bytes. A real ReleaseFast aarch64-macOS archive then passed direct execution, lifecycle-disabled clean installation, a second offline postinstall, canonical SCSS/indented Sass/Less/Stylus compilation, public API use, and bounded 1,183-entry/19,120,920-byte installed state. No public capability row or release version changed. `PRE-006` remains `IMPLEMENTED_PENDING_CI` until the integrated main-branch workflow reproduces the archive/install/offline/API smoke on all five declared targets; `PRE-008` follows only after that proof.

`PRE-005` is verified at implementation checkpoint `09dc46d`. The npm product boundary now exposes one frozen string/file result contract for CSS, SCSS, indented Sass, Less, and Stylus while keeping provider execution in the exact bounded `zigcss-preprocessor-v1` host. A second four-byte big-endian `zigcss-core-v1` boundary invokes only the installed ZigCSS binary's hidden exact argument, strips its environment, rejects links/extra args/stderr/malformed frames/wrong IDs/timeouts/cancellation, and admits no generated CSS until recovery-disabled parsing and emission succeed. Source maps compose through both stages; diagnostics and provider plus CSS dependency facts remain owned; every failure has no CSS. The strict product CLI detects exact extensions, supports explicit syntax for stdin, confined entry/load-path files, stdout or atomic output, inline maps, deterministic path-derived collision names, and compile-before-write mixed batches. Its queue is capped at 4,096 inputs and eight workers with first-failure cancellation and argument-order commits. Watch compiles its single stable root read directly, tracks bounded deduplicated dependencies across every admitted syntax, records failed states before waiting, preserves the last good file, recovers after dependency restoration, and re-raises SIGINT/SIGTERM only after owned child cleanup. Twenty-nine product tests cover the real core, root API, all syntax/map/import paths, strict negative ownership, mixed determinism, installed launcher/file/stdin behavior, confined load paths, collision naming, worker bounds, no-output failures, every watch row, unchanged errors, exact snapshot ownership, cancellation, and signals. Together with 40 common and 77 canonical adapter cases, the distinct preprocessor gate is 146/146. Exact Zig 0.15.2 Debug and ReleaseSafe each pass 38/38 steps and 495/495 tests. Workflow 5/5 and check, dependency 4/4 and check, version 7/7 and check, release metadata 6/6 and check, prefix 4/4, capability 7/7, Zig package 7/7, Node wrapper 3/3, the eight-format matrix, 21-feature compatibility oracle, and all ten transform differentials pass. Documentation integrity passes 7/7 over 36 Markdown files/67 fences/52 links/24 routes; the site passes 137/137 plus production build. JavaScript syntax, repository Zig formatting, generated-state, and diff checks pass; live production audits are 0/0/0. The npm dry run remains five files/38,030 unpacked bytes and excludes `api.mjs`, the host, providers, supervisors, and corpora, so current rc.3 publication behavior and unavailable public rows do not change. Expected environment-only retries used the pinned SDK wrapper, temporary npm cache, localhost permission for the docs server, registry access for audits, and an absolute Zig path for prefix regeneration. `PRE-006` is active next; publication remains unauthorized.

`STYLUS-012` is verified at implementation checkpoint `d184914`. Official Stylus 0.64.0 revision `1086c6c1fbd7a7fd0ce9ad94f6cf4a62fc79a6e9`, tree `295654c66ed48ff47a4aeca95b1935edf7a612ba`, archive SHA-256 `140a722893f0f05b501a01ce1ff27042b6d7c0769ad746f4b5d04429ec8d429e`, and MIT license are pinned. The reproducible corpus owns 346 cases/816 files: 326 official successes and 20 project-owned failures match exact CSS or normalized error/source/location/dependency contracts under eight-way determinism. Twenty-nine official cases are explicitly excluded for executable hooks/custom resolvers, the undocumented prefix option, or legacy output rejected by strict Lightning CSS. Every admitted success parses with recovery disabled, round-trips twice through ZigCSS, and reparses independently. The corpus exposed recursive/directory-glob and package `.styl` fallback gaps; the implementation now uses exact patched `glob` 10.5.0 only as a bounded library iterator, validates confined static roots, rejects traversal and link escapes, caps depth/matches, sorts results, and routes every match through `PRE-003` without shell or child-process authority. Stylus passes 23/23 and the complete preprocessor matrix passes 117/117. Exact Zig 0.15.2 Debug and ReleaseSafe each pass 36/36 steps and 491/491 tests; main builds pass 3/3, package consumers pass 6/6 with 2/2 tests, examples pass 7/7 with 1/1 tests, and benchmark smoke passes 3/3 without archiving a result. Format, compatibility, all ten transform differentials, workflow, version, release metadata, dependency, prefix, capability, every benchmark gate, Zig package, Node wrapper, npm publication, release smoke/consumers, VS Code 13/13 with the exact five-file/120,473-byte VSIX, autodevelop 47/0, actionlint 1.7.12, and Neovim 0.11.7/0.12.4 pass. Documentation passes 137/137 plus production build; integrity covers 36 Markdown files, 67 fences/55 executable/10 site-rendered, 52 links, and 24 routes. JavaScript syntax, repository Zig formatting, generated-state, exact corpus regeneration, scoped authored-file whitespace, and diff checks pass. A clean `npm ci --ignore-scripts` installs 81 packages; production audits are 0/0/0 and the full audit is zero. The npm dry run remains exactly five files/36,484 unpacked bytes and excludes the private host, providers, importers, and corpora. Expected environment-only retries used the recorded absolute Neovim, pinned Zig, local-loopback, actionlint-label, and temporary npm-cache configurations; no product regression occurred. Public SCSS, Sass, Less, and Stylus remain unavailable; `PRE-005` is active.

`STYLUS-010-HARDENING` is verified at checkpoint `b8f3b64`. A red source-level invariant first proved the provider had not disabled Stylus 0.64.0's module-global parser cache. The renderer now sets `cache: false`; focused Stylus tests pass 10/10 and protocol tests pass 16/16, with JavaScript syntax, documentation integrity, and diff checks green. This closes cross-request AST/source retention before `STYLUS-011` introduces dependency parsing.

`STYLUS-010` is verified at implementation checkpoint `fc0c7bf`. Exact `stylus` 0.64.0/MIT and its complete 47-package closure are lock-integrity, license, engine, and lifecycle reviewed; a clean `npm ci --ignore-scripts` installs 81 packages and audits all 82 with zero findings. The internal provider admits one exact package-owned standard-library import under a stable virtual entry, but no request-controlled filesystem authority: relative/absolute/CSS/URL/network imports, load paths, file helpers, direct or aliased plugins, and injected renderer hooks fail without CSS. Console helpers become owned diagnostics. Exact expanded/compressed CSS, Source Map v3 source/content ownership, normalized CSS-free parse errors, pre/post cancellation, the 1,000-diagnostic ceiling, 12-way determinism, and real framed-host success pass. The combined preprocessor suite passes 104/104: ten Stylus plus 94 Sass/Less/host/resolver/map tests. Exact Zig 0.15.2 Debug and ReleaseSafe each pass 36/36 steps and 491/491 tests; main builds pass 3/3, package consumers pass 6/6 with 2/2 tests, examples pass 7/7 with 1/1 tests, and benchmark smoke passes 3/3 without archiving a result. Format, compatibility, all ten transform differentials, workflow, version, release metadata, dependency, prefix, capability, every benchmark policy, Zig package, Node wrapper, npm publication, release smoke/consumers, VS Code 13/13 with the exact five-file/120,473-byte VSIX, Neovim 0.11.7/0.12.4, and autodevelop 47/0 pass. Documentation passes 137/137 plus production build; integrity passes 7/7 over 35 Markdown files, 66 fences/54 executable/10 site-rendered, 52 links, and 24 routes. JavaScript syntax, repository Zig formatting, generated-state, and diff checks pass. Production audits are 0/0/0 and the full 136-dependency audit is zero. The npm dry run remains exactly five files/36,484 unpacked bytes and excludes all private preprocessor implementation. Public Stylus remains unavailable; `STYLUS-011` is active.

`LESS-012` is verified at implementation checkpoint `e1f6e3d`. Official Less 4.6.7 revision `8ae2cc3bfa79f0718ad6fe5f263a1d6819fe9d5c` is pinned as 88 reviewed cases/217 checksum-owned files with retained Apache-2.0 evidence: 68 successful fixtures match exact canonical CSS and 20 negative fixtures match exact formatted parser/evaluator errors. Normalized diagnostic, source, and explicit dependency order agree; eight bounded workers reproduce every adapter result. All successful outputs parse through pinned Lightning CSS with recovery disabled, round-trip twice through ZigCSS, and reparse independently. The new cross-parent import regression proves ordinary imports deduplicate by canonical URL while ancestry-sensitive `(multiple)` branches stay distinct. Less passes 27/27 and the combined preprocessor suite passes 94/94. Exact Zig 0.15.2 Debug and ReleaseSafe each pass 36/36 steps and 491/491 tests; main builds, package consumers, complete examples, and benchmark smoke pass in both required modes. Format, compatibility, all ten transform differentials, workflow, version, release metadata, dependency, prefix, capability, every benchmark gate, Zig package, Node wrapper, npm publication, release smoke/consumers, autodevelop 47/0, VS Code 13/13, and both supported Neovim versions pass. Documentation passes 137/137 plus production build; integrity passes 7/7 over 35 Markdown files, 66 fences/54 executable/10 site-rendered, 52 links, and 24 routes. JavaScript syntax, repository Zig formatting, scoped authored-file whitespace, exact corpus regeneration, and the five-file/36,375-byte npm dry run pass. Immutable official fixture bytes retain upstream whitespace under manifest checksum/inventory ownership. Production audits are 0/0/0 and the full dependency audit is zero. Public SCSS, Sass, and Less remain unavailable until product/package graduation; `STYLUS-010` is active.

`LESS-011` is verified at implementation checkpoint `b447c7f`. Every Less stylesheet import and supported synchronous asset read is confined to one serialized `PRE-003` session, while canonical load-path/entry-relative precedence, extension inference, nested and absolute-inside-root resolution, reference/inline/optional/multiple/forced Less/CSS semantics, URL rewriting, warnings, dependencies, and provider Source Maps remain owned and deterministic. Standard `data-uri()` and image dimension helpers consume resolver-owned bytes through exact `image-size` 0.5.5; JavaScript/plugins, schemes/network, aliases, escapes, links, unreadable/special/unstable files, invalid stylesheet UTF-8, cycles, ancestry/byte/count/read exhaustion, and more than 1,000 diagnostics fail closed without partial CSS or internal sentinel disclosure. The combined preprocessor suite passes 90/90: 23 Less, 11 resolver, 16 host, 13 metadata/map, and 27 Sass tests. Exact Zig 0.15.2 Debug and ReleaseSafe each pass 36/36 steps and 491/491 tests; both main builds, both package-consumer modes, both example modes, and benchmark smoke pass. Format reports eight adapters with seven removed and one limited; compatibility remains 21 features/18 outputs/four rejections and all ten transform differentials pass. Workflow 5/5, version 7/7, release metadata 6/6, dependency 4/4, prefix 4/4, capability 7/7, every benchmark corpus/tool/output/mode/statistics/archive/publication gate, package 7/7, wrapper 3/3, autodevelop 47/0, publication 7/7, release smoke 5/5, release consumers 17/17, VS Code 13/13 with its exact five-file/120,473-byte VSIX, Neovim 0.11.7/0.12.4, Zig package 7/7, JavaScript/Bash/formatting/generated-state/diff checks, and clean install all pass. Documentation passes 137/137 plus production build; integrity passes 7/7 over 34 Markdown files, 65 fences/53 executable/10 site-rendered, 52 internal links, and 24 routes. Full and production audits are zero; the npm dry run is exactly five files/36,375 unpacked bytes and excludes every internal preprocessor implementation. The initial docs server, registry audit, and npm-pack attempts were retried only for sandbox loopback/DNS/cache permissions; their authorized reruns were green. Public Less remains unavailable until the independent `LESS-012` conformance corpus and later product/package graduation gates pass.

`LESS-010` is verified at implementation checkpoint `061cc10`. The exact `less` 4.6.7/Apache-2.0 provider and all 15 transitive/optional packages are lockfile-integrity and license reviewed; `npm ci --ignore-scripts` reproduced 35 total packages with zero findings. Ten focused regressions prove canonical expanded/compressed rendering, maps, normalized failures, pre/post cancellation, exact request closure, repeated/12-way parallel and framed-host determinism, no public admission, and default-deny filesystem/network/JavaScript/plugin authority. Adjacent, absolute, optional, inline, HTTP(S), `data-uri`, image, and load-path probes fail without CSS or execution; plain CSS imports emit without provider I/O. The combined preprocessor suite passes 75/75. Exact Zig 0.15.2 Debug and ReleaseSafe each pass 36/36 steps and 491/491 tests. The format matrix, 21-feature/18-output/four-rejection compatibility gate, ten transform differentials, workflow, dependency, capability, prefix, version, release metadata/smoke/consumer, package, wrapper, VS Code, Neovim 0.11.7 and 0.12.4, autodevelop 47/0, JavaScript, formatting, generated-state, and diff gates pass. Documentation passes 137/137 plus production build; integrity passes 7/7 over 34 Markdown files, 65 fences/53 executable/10 site-rendered, 52 internal links, and 24 routes. Production audits are 0/0/0; the npm dry run remains exactly five files/36,348 unpacked bytes and excludes the internal provider. Public Less remains unavailable until imports, conformance, and later product/package graduation; `LESS-011` is next.

`SASS-012` is verified at implementation checkpoints `219f373`, `9f5112f`, and `405916d`. The repository pins official Sass-spec commit `1b03109a6205c8cff146defeae8488094b147c88` as 80 reviewed cases/175 exact files, with source archive SHA-256 `a7918374582d19cb4e403411a888c91499a7286f021b6f91278219f471b76790`, tree `2e2c5127e220ccc6fc1cbeef7b74f79fbadcb32c`, selection and manifest ownership, and retained upstream MIT notice. Forty-one SCSS and nineteen indented-Sass successes match exact Dart Sass 1.101.0 bytes; thirteen SCSS and seven indented-Sass failures match exact canonical behavior. All successes reparse and round-trip twice through ZigCSS and reparse through Lightning CSS 1.30.1 with recovery disabled; direct diagnostic/source/dependency order, eight-way determinism, and the 1,000-diagnostic fail-closed ceiling pass. The combined preprocessor suite passes 65/65. Exact Zig 0.15.2 Debug and ReleaseSafe each pass 36/36 steps and 491/491 tests. The format matrix, 21-feature/18-output/four-rejection compatibility gate, ten transform differentials, workflow, capability, prefix, dependency, version, release metadata/smoke/consumer, package, wrapper, VS Code, Neovim 0.11.7 and 0.12.4, syntax, generated-state, and diff gates pass. Documentation passes 137/137 plus production build; integrity passes 7/7 over 34 Markdown files, 65 fences/53 executable/10 site-rendered, 52 internal links, and 24 routes. Production audits are 0/0/0; the npm dry run remains exactly five files/36,246 unpacked bytes and excludes every private preprocessor/corpus file. Public SCSS and Sass remain unavailable until the later product/package graduation gates; `LESS-010` is next.

Main-integration repairs `b18657e`, `c22c1db`, `32fa523`, `dfe6d4e`, `0a4c102`, `712058f`, `b382807`, `9595e4c`, `27cd262`, and `918b2b7` were driven by failed GitHub runs `29225800907`, `29225800919`, `29225800317`, `29225798601`, `29226557568`, `29226557612`, `29226868223`, `29226868221`, and `29227098324`, not synthetic assumptions. Local red evidence covered the main-branch runner guard, invalid job-level `runner.temp`, ARM64 runner-map drift, unsupported Windows `cwd_dir`, platform-specific `IsDir` planning, one remaining direct watch child, a Linux early-close `BrokenPipe`, shallow source ancestry, an absent empty adapter directory in a fresh checkout, and Git Bash shadowing Windows' ZIP-capable native archive reader. The repaired head passes supervisor 47/47; archive 5/5/check; release metadata 5/5/check; release smoke 5/5; consumers 17/17; workflow 5/5/check; version 6/6/check; capability 7/7/check; format inventory 2/2 plus the complete matrix; pinned actionlint 1.7.12 with the reviewed custom runner label; a live checksum-bound Homebrew source build/smoke; repeated Debug and ReleaseSafe 36/36 with 491/491; docs 126/126 plus build; documentation integrity 7/7 over 31 files/60 fences/42 links/24 routes; a five-file/46,453-byte npm dry run; repository-wide Zig formatting, Bash syntax, JavaScript syntax, ZIP list/stream, and diff checks. Hosted run `29251213253` verifies `CI-004`, `CI-006`, `CI-007`, and `CI-008`: all five native jobs and every full Test Suite step passed. After workflow-based Pages was explicitly enabled, dispatch `29358869275` on exact head `e2eef58` passed the documentation build and deployment jobs, and the enforced-HTTPS public site returned HTTP 200. The absent controlled benchmark runner/archive is the sole remaining external gate.

`BENCH-007` publication implementation is checkpointed at `509e2ef`. Seven focused regressions and the exact check prove a byte-identical no-claims fallback, complete archive-only deterministic Markdown, report/provenance/artifact-link tamper rejection, bounded Markdown/HTML escaping, create-only output, repository-confined committed raw data, generated-byte drift failure, and mandatory build/scheduled workflow order. The real ReleaseFast statistics gate retained and revalidated 43 x 20 observations before removing the temporary report; the mode smoke accepted three API, three allocator-memory, and one throughput observation only after output validation. The complete benchmark dependency chain passes all six corpus, five toolchain, six output, five mode, six statistics, five archive, and seven publication regressions plus exact checks. Debug and ReleaseSafe each pass 491/491 across 36/36, both native builds pass 3/3, and package consumers pass 6/6 with 2/2 tests while complete examples pass 7/7 with 1/1 in both modes. Compatibility remains 21 features/18 outputs/four rejections; all ten transform differentials, eight-format, prefix, package, wrapper, version, release-metadata, dependency, workflow, capability, documentation, JSON/JavaScript/YAML, and diff gates pass. Documentation is 126/126 plus the production build and 31-file/60-fence/42-link/24-route integrity gate. All three production audits are zero; npm is five files/45.8 kB; repository formatting debt is exactly ten inherited Zig files. No scheduled workflow ran and no controlled archive or public benchmark result exists, so the current capability remains unavailable and performance claims remain withdrawn pending external scheduled evidence.

`BENCH-006` is verified at implementation checkpoint `2e4b56e`. Five archive regressions prove exact scheduling/runner/retention/cleanup/build-gate policy, full 43-series report admission, create-only manifest generation, CLI revalidation, source/run/hardware binding, and tamper/inventory/symlink/platform rejection. Five workflow-policy regressions and the exact gate own four workflows, eight least-privilege jobs, and 26 immutable action references. The complete BENCH-001 through BENCH-006 chain passes: six corpus, five toolchain, six output, five mode, six statistics, and five archive tests plus every exact check; the real BENCH-005 gate again retained and validated all 860 temporary raw samples before removing its report. Debug and ReleaseSafe each pass 491/491 across 36/36, both builds pass 3/3, and the validated in-process mode smoke passes. Independent compatibility, ten transform, eight-format, prefix, package, wrapper, version, release-metadata, dependency, capability, documentation, syntax, and diff gates pass. Documentation is 126/126 plus the production build and 31-file/60-fence/41-link/24-route integrity gate. All three production audits report zero vulnerabilities; npm remains five files/45.2 kB; repository formatting debt remains exactly ten inherited Zig files. No GitHub workflow was dispatched and no artifact, archive, result, comparison, or public claim was published; `BENCH-007` is next.

`BENCH-005` is verified at implementation checkpoint `454f0a1`. The closed contract requires 20 raw observations for each of 43 ordered series and rejects missing, duplicate, reordered, zero-duration, over-range, altered-statistic, schema, symlink, environment, executable, corpus, build-mode, and workflow drift. A real ReleaseFast run retained 180 cold-CLI and 180 warm-CLI durations across three tools and three corpora, 60 ZigCSS direct-API durations, 420 allocator-field values, and 20 two-pass throughput values; every CSS output passed the existing recovery-free or typed-structural oracle before admission. Exact median, nearest-rank p95, and population variance were recomputed from all 860 raw samples. Six statistics, five mode, six output, five toolchain, six corpus, and five workflow-policy regressions plus their exact checks pass. Debug and ReleaseSafe remain 491/491 across 36/36, both builds and the benchmark smoke pass, and all independent CSS/transform/format/prefix/package/wrapper/dependency/capability/version/release-metadata/documentation/audit gates pass. Documentation remains 126/126 plus the production build; npm remains five files/45.0 kB; a fresh 75-file package builds in both modes; focused format/diff checks pass and formatting debt remains exactly 10 inherited files. The validated report was temporary and removed; no sample, statistic, archive, comparison, or public claim was published. `BENCH-006` is next.

`BENCH-004` is verified at implementation checkpoint `6d6dcdb`. Five focused regressions plus the exact check prove one closed cold-CLI, warm-CLI, in-process-API, allocator-memory, and throughput contract; exact three-tool comparability is limited to identical new-process CLI boundaries, while direct API/memory/throughput metrics remain ZigCSS-only. The mode gate admits 9 cold and 9 warm CLI samples only after Lightning CSS 1.30.1 recovery-free semantic validation. Debug and ReleaseSafe each pass 491/491 across 36/36, both builds pass 3/3, and both Zig mode smokes accept three direct-API latency samples, three allocator-memory observations, and one two-pass throughput sample only after typed structural output validation. Six corpus, five toolchain, six output, five mode, 21-feature/18-output/four-rejection compatibility, ten transform, eight-format, five workflow, four dependency, seven Zig-package, two wrapper, four prefix, seven capability, six version, and seven documentation-integrity tests plus their exact checks pass. Documentation remains 126/126 plus the production build; production audits remain 0/0/0; npm remains five files/44.8 kB unpacked; JavaScript/JSON/format/diff checks pass and formatting debt remains exactly 10 inherited files. No raw duration, memory result, statistic, comparison, archive, or public claim was retained or published; `BENCH-005` is next.

`BENCH-003` is verified at implementation checkpoint `b78bcbb`. Six focused regressions and the exact check prove a closed output-acceptance manifest, pinned recovery-disabled Lightning CSS 1.30.1 semantic canonicalization, positive-duration admission only after validation, manifest/symlink and workflow-order failure, and removal of the active legacy timing implementation. Exact bounded invocations produce nine accepted outputs across ZigCSS, esbuild 0.28.1, Lightning CSS CLI 1.30.1, and the 95-byte, 12,383-byte, and 198,366-byte corpora; empty, malformed, truncated, changed, extra, warning-bearing, oversized, failed, or escaping output cannot become a sample. The Zig smoke independently compiles through the public facade, reparses through the rebuilt typed pipeline, requires structural equivalence, completes 3/3, and reports only input/output bytes. Debug and ReleaseSafe each pass 491/491 across 36/36 and both builds pass 3/3 with the recorded macOS 15.4 SDK isolation. Corpus/toolchain, 21-feature/18-output/four-rejection compatibility, ten transform differentials, eight-format, workflow/dependency/Zig-package/capability/version/prefix/wrapper, and production-audit gates pass. Documentation integrity passes 7/7, docs pass 126/126 and build, JSON/JavaScript/diff checks pass, npm remains five files/44.7 kB unpacked, and repository formatting debt is exactly 10 inherited files. No timing sample, raw result, comparison, or public claim was accepted or published; `BENCH-004` is next.

`BENCH-002` is verified at implementation checkpoint `e025d2b`. Five focused regressions plus the exact check prove a closed manifest for esbuild 0.28.1 and Lightning CSS CLI 1.30.1, synchronized exact root pins, checksum-bound direct and 36 optional platform archives, one host-matched regular native executable per tool, exact runtime identity, workflow ordering, and removal of the former `npx`/global-install/random-input runner and two archived timing files. A clean lifecycle-disabled install reproduced both tools with zero reported vulnerabilities. Debug and ReleaseSafe each pass 489/489 across 34/34 and both builds pass 3/3 using the recorded macOS 15.4 SDK isolation. The benchmark smoke completes 3/3 over exact 95-byte, 12,383-byte, and 198,366-byte corpora; six corpus tests, the 21-feature/18-output/four-rejection compatibility gate, ten transform differentials, eight-format matrix, five workflow-policy tests, four dependency-policy tests, seven Zig-package tests, capability/version checks, production audits, JavaScript/JSON/diff checks, and the unchanged 11-file formatting-debt check pass. Documentation passes 126/126 and builds; its integrity gate passes 7/7 over 31 Markdown files, 60 fences/50 executable, 41 links, and 24 routes. The npm dry run remains five files/44,509 unpacked bytes. No sample was accepted, no result was published, and no external state was changed; `BENCH-003` is next.

`BENCH-001` is verified at implementation checkpoint `9035398`. Six focused regressions and the read-only generator check prove three exact `v1` corpora totaling 210,844 bytes, 2,177 qualified rules, and 10,885 declarations; each file parses with recovery disabled under pinned Lightning CSS 1.30.1, and content, manifest, extra-file, symlink, hash, and active-embedding drift fail closed. The Zig benchmark smoke consumes the exact 95-byte, 12,383-byte, and 198,366-byte inputs and reports those actual sizes. Debug and ReleaseSafe each pass 489/489 across 34/34, both builds pass 3/3, and the compatibility, transform, workflow, dependency, Zig-package, documentation-integrity, YAML/JSON/JavaScript, formatting, and diff gates pass. Documentation remains 126/126 plus the production build; the npm dry run remains five files/44,275 unpacked bytes. Timing samples remain invalid for publication until `BENCH-002` through `BENCH-007` close competitor, output, mode, statistics, environment, and archival requirements. No external state was changed.

`REL-005` is verified at implementation checkpoint `db4ab56`. Four focused regressions prove the exact five-target native runner map, closed CLI arguments, two-URL-only offline npm lifecycle preload, and required build/tag workflow ordering. The host-native aarch64-macOS ReleaseFast binary was archived with generated SHA-256/SPDX metadata, extracted and executed for exact version/CSS/warning output, then packed as the exact npm surface and installed through the real postinstall; the installed binary matched source bytes and the wrapper produced the same exact output with no residue. The workflow makes those same checks mandatory on x86_64/aarch64 Linux, x86_64/aarch64 macOS, and x86_64 Windows before attestation. Debug and ReleaseSafe each pass 489/489 across 34/34. Sixteen release-consumer, five release-metadata, five workflow-policy, six version, seven capability, and four dependency-policy tests plus all associated checks pass. Documentation passes 126/126 and builds; its integrity gate covers 31 Markdown files, 60 fences/50 executable, 41 links, and 24 routes. Production audits remain 0/0/0, workflow policy owns 22 pinned actions, capability metadata owns 20 rows/18 gates, YAML/JavaScript/diff checks pass, and npm remains five files/44,011 unpacked bytes. No tag, artifact, release, npm package, image, formula, deployment, or other external state was created.

`REL-003` is verified at implementation checkpoint `1d3a47a`. Sixteen hermetic consumer regressions cover the exact npm mappings/manifest/archive/target/cleanup contract, Docker's full five-asset inventory and non-root one-binary image policy, and Homebrew's immutable commit URL, nonempty exact digest, Zig 0.15 build dependency, no-head/no-second-download rule, and CI order. The Homebrew live gate downloaded checkpoint `3fada2359ab1fa262b782a33e9ab2a7bab2c46ca` under strict HTTPS/redirect/time/size limits, matched SHA-256 `2fc630a41af5b5fef1d1e4db551604deac048c1b02ff249d5abbb061ebd5c906`, validated its confined regular-file archive and `0.4.0-rc.1` identity, built it from isolated caches with Zig 0.15.2, and exact-checked version, minified CSS, and the experimental warning. The prior Docker live gate retains its aarch64-linux ReleaseFast build, canonical five-file fixture, no-network run, configuration/architecture/layer inspection, and cleanup evidence. Debug and ReleaseSafe each pass 489/489 across 34/34 and both builds pass 3/3. Six version, five release-metadata, five workflow-policy, four dependency-policy, seven package, two wrapper, seven capability, and all 16 consumer tests pass; Ruby and JavaScript syntax, release/workflow/version/capability checks, and diff checks pass. Production audits remain 0/0/0. Documentation passes 126/126 and builds; its integrity gate passes 7/7 over 31 Markdown files, 60 fences/50 executable, 41 links, and 24 routes. The npm dry run remains five files/43,856 unpacked bytes. No tag, tap, artifact, release, package, image, or formula was published; `REL-005` is next.

Operator launcher follow-up `OPS-005` is checkpointed at `5edccab` with clean-tree selftest correction `a29696b`. Live evidence rejected launchd before model execution, removed its retrying job, proved detached-screen Git access across command boundaries, and proved a real paused supervisor remained active and clean before graceful stop. The final clean-tree gate passes 31/31 runner assertions, 3/3 operations tests, Bash/diff checks, 126/126 docs tests, the production docs build, documentation integrity, and a five-file/43,702-byte npm dry run. Production compiler and release-consumer code remain unchanged.

Operator follow-up `OPS-004` is checkpointed at `c880ff8`. The push boundary passes 31/31 hermetic assertions, including a real temporary bare-remote push/readback and production-mode rejection of an unapproved remote. The operations suite passes 3/3; Bash and diff checks, 126/126 docs tests, the production docs build, the 31-file/61-fence/51-executable/41-link/24-route documentation-integrity gate, approved-origin doctor preflight, and a five-file/43,583-byte npm dry run pass. Production compiler and `REL-003` consumer code are unchanged by this operator package; the release evidence below remains applicable.

`REL-003`'s npm consumer slice is checkpointed at `6ac89dc`. Ten hermetic regressions prove exact agreement with all five `REL-002` targets, strict platform pairing, canonical manifest parsing, streamed download bounds, checksum failure cleanup, archive-entry confinement, target-header validation, symlink rejection, atomic late replacement, and one lifecycle/CI gate. Five release-metadata tests plus its workflow check, six version tests plus its check, five workflow-policy tests plus its check, two wrapper tests, seven package tests, 126/126 docs tests and the production build, JavaScript/diff checks, and a five-file/43,295-byte npm dry run pass. Production Zig and editor code are unchanged, so the prior full evidence below remains applicable outside the touched release/package surfaces. At that checkpoint Docker and Homebrew were next; Docker is now checkpointed at `d31ce95`, leaving Homebrew.

Milestones 0, 1, 2, 3, 4, 5, and 6 are `PASS`; Milestone 7 is active and `REL-003` is in progress through Docker checkpoint `d31ce95`; operator tooling `OPS-002` is checkpointed at `e10d7e7`. Five release-metadata regressions prove deterministic bounded SPDX/checksum generation, exact target identity, tamper/path/input-and-output-symlink rejection, local Sigstore subject/predicate binding, and closed workflow drift failure; five workflow-policy tests now own 21 immutable action invocations and the exact attestation permissions. The release matrix defines five targets with exactly five assets each, two signed attestations, and two required GitHub cryptographic verifications before upload. A real aarch64-macOS ReleaseFast archive regenerated and rechecked its metadata; independent `shasum -a 256 -c` accepted both archive and SBOM, and the JSON reported SPDX-2.3/0.4.0-rc.1/BINARY/SHA256. No tag, signature, artifact upload, release, or publication occurred. Debug and ReleaseSafe pass 489/489 across 34/34. The fixed-model single-lane runner passes 24 hermetic shell assertions and three operations tests without a model call or tracked mutation; its doctor verifies Codex 0.142.5, ready saved authentication, the approved model/effort, isolated branch, and confined ignored state. Docs pass 126/126 and build. VS Code passes 13/13 and packages its exact five-file/120,473-byte prerelease VSIX; real Neovim 0.11.7 and 0.12.4 integrations pass. All five Linux/macOS/Windows targets inspect correctly after both ReleaseSafe and ReleaseFast builds. Live production audits remain zero across root, docs, and VS Code. Documentation integrity remains seven tests over 31 Markdown files, 61 fences/51 executable, 41 internal links, and 24 routes; capability metadata contains 20 rows and 17 gates. Exact BCD generation, seven package tests, two wrapper tests, the eight-format matrix, 21-feature/18-output/four-rejection CSS gate, ten transform differentials, benchmark execution, Formula/shell/workflow/JSON/JavaScript syntax, generated-state, exact release tag, stale-version, and diff checks pass. Root npm is five files/43,295 unpacked bytes. Fresh package `zigcss-0.4.0-rc.1-Bz3pcRE3GwBpTveEW6YWkZKglcWtvMQXabl0HzJptI_p` contains 75 files/1,937,408 disk bytes, builds 3/3 without mutation, and its external dependency consumer passes 6/6 steps with 2/2 tests in Debug and ReleaseSafe; the committed consumer and build example also pass both modes. Repository formatting debt remains exactly 12 inherited files. `REL-003` remains active only for Homebrew verification.

## DEP-001 validation snapshot

Milestones 0, 1, 2, 3, 4, 5, and 6 are `PASS`; Milestone 7 is active and `DEP-001` is checkpointed at `d5b2d2b`. Four dependency-policy tests prove exactly three npm manifest/version-3-lock pairs, exact direct pins with one reviewed local edge, rejection of unowned locks/ranges, consistent npm audit v2 counts, high/critical failure, and exact bounded Dependabot rendering/workflow order. Live lock-only production audits report root 0, docs 0, and VS Code 0 vulnerabilities, including 0 high and 0 critical on each graph. Weekly version-update proposals cover the three npm directories, GitHub Actions, and the root Docker ecosystem under per-entry limits without credentials, target override, assignment, or automerge. Debug and ReleaseSafe pass 489/489 across 34/34. Docs pass 124/124 and build. Documentation integrity remains seven tests across 31 Markdown files, 59 fences, 41 links, and 24 site routes; capability metadata remains 19 rows and uses 15 evidence gates. VS Code passes 13/13 and packages its exact five-file/120,473-byte temporary VSIX; both real Neovim smokes pass. Exact BCD generation, seven package tests, two wrapper tests, the eight-format matrix, 21-feature/18-output/four-rejection CSS gate, ten transform differentials, benchmark smoke, workflow/Dependabot/JSON/JavaScript syntax, generated-state, and diff checks pass. Root npm is five files/26,564 unpacked bytes. Fresh isolated package `zigcss-0.3.0-Bz3pcSAyGwADM0J9-GHXsywUvbTT7o9F_73bo1zFatyI` remains 75 files/1,937,408 disk bytes, builds 3/3 in both modes, and its dependency consumer passes 6/6 steps with 2/2 tests in Debug and ReleaseSafe. Repository formatting debt remains exactly 12 inherited files. `REL-004` is active next.

## DOC-002 validation snapshot

Milestones 0, 1, 2, 3, 4, 5, and 6 are `PASS`; Milestone 7 is active and `DOC-002` is checkpointed at `b247067`. The documentation-integrity gate covers 31 tracked Markdown files, 59 code/presentation fences (49 executable and three rendered-site examples), 41 internal Markdown links, and 24 literal site routes. Seven tests prove fence/reference/heading parsing, canonical/symlink path confinement, closed policy paths, invalid Bash/JSON/CSS/Lua/Vim rejection, and the complete live repository. Two canonical Zig examples compile and run with exact output contracts in both safety modes; all ordinary/module CSS examples compile and reparse independently; real clean Neovim 0.11.7 and 0.12.4 parse the Lua/Ex examples. Debug and ReleaseSafe pass 489/489 across 34/34. Docs pass 123/123 and build. VS Code passes 13/13 and packages its exact five-file/120,473-byte temporary VSIX; both Neovim LSP smokes remain green. Capability metadata remains 19 rows and now uses all 14 evidence gates. Exact BCD generation, seven package tests, two wrapper tests, the eight-format matrix, 21-feature/18-output/four-rejection CSS gate, ten transform differentials, benchmark smoke, workflow/JSON/JavaScript syntax, and root/docs/extension production audits pass. Root npm is five files/26,233 unpacked bytes. Fresh isolated package `zigcss-0.3.0-Bz3pcdIxGwCc1tlYjTLFyVk6XNxbG9yAkxTbeaWggdlS` remains 75 files/1,937,408 disk bytes, builds 3/3 in both modes, and its dependency consumer passes 6/6 steps with 2/2 tests in Debug and ReleaseSafe. Generated-state/diff checks pass and repository formatting debt remains exactly 12 inherited files. `DEP-001` is active next.

## DOC-001 validation snapshot

Milestones 0, 1, 2, 3, 4, 5, and 6 are `PASS`; Milestone 7 is active and `DOC-001` is checkpointed at `8317d09`. Canonical capability metadata contains 19 unique rows backed by all 13 executable evidence gates; seven generator tests prove schema closure, real npm/Zig commands, repository-confined canonical anchors including symlink rejection, deterministic replacement, marker authority, current editor/LSP boundaries, and exact two-table generation. Four docs-level metadata tests and six feature-page tests prove the same rows drive both published Markdown surfaces and the React UI with rendered inline code; docs pass 121/121 and build. Debug and ReleaseSafe remain 489/489 across 30/30 because production Zig is unchanged. VS Code passes 13/13 and produces its exact five-file/120,473-byte temporary VSIX; Neovim passes four static gates and real 0.11.7/0.12.4 smokes. Exact BCD generation, seven package tests, two wrapper tests, the eight-format matrix, 21-feature/18-output/four-rejection CSS gate, ten transform differentials, benchmark smoke, workflow/JSON/JavaScript syntax, and root/docs/extension production audits pass. Root npm is five files/25,939 unpacked bytes. Fresh isolated package `zigcss-0.3.0-Bz3pcW8tGwBkfoEoLEjkUVh-xoK142KGmr0MldSKGVOM` remains 75 files/1,933,312 disk bytes after validation, builds 3/3 in both modes, and its dependency consumer passes 6/6 steps with 2/2 tests in Debug and ReleaseSafe. Generated-state and diff checks pass; repository formatting debt remains exactly 12 inherited files. `DOC-002` is active next.

## NEOVIM-001 validation snapshot

Milestones 0, 1, 2, 3, 4, 5, and 6 are `PASS`; Milestone 7 is active with `DOC-001` next. Debug and ReleaseSafe each pass 489/489 tests across 30/30 steps. The final LSP composition covers bounded sequential framing, malformed-request recovery, lifecycle/version/cancellation rules, >1 MiB synchronized Unicode CSS, exact UTF-16 diagnostics/navigation, syntax-aware deterministic open-document indexes, resource budgets, and repeated allocator-clean state. The VS Code package passes 13/13 tests and produces an exact five-file/120,473-byte temporary VSIX with no binary or residue. The built-in CSS-only Neovim config passes four static gates and real isolated smokes on exact 0.11.7 and current 0.12.4: relative command rejection, one `[absolute zigcss, --lsp]` client, UTF-16/full sync, pull diagnostics plus seven editor methods, six absent methods, an empty compiler-backed full report, exact property hover, and clean shutdown. Docs pass 115/115 and build. Exact BCD generation, seven package tests, two wrapper tests, the eight-format matrix, 21-feature/18-output/four-rejection CSS gate, ten transform differentials, benchmark smoke, workflow/script syntax, and root/docs/extension production audits pass. Root npm is five files/25,652 bytes. Fresh package `zigcss-0.3.0-Bz3pcVgtGwC43k52obcDIv9gmTtqNfcInX351qH46Vq1` contains 75 files/1,933,312 disk bytes, builds 3/3 in both modes, and its dependency consumer passes 6/6 steps with 2/2 tests in Debug and ReleaseSafe. The unchanged production implementation retains the five-target artifact inspection evidence. Generated-state, diff, and focused formatting gates pass; repository formatting debt remains exactly 12 inherited files.

## VSCODE-001 validation snapshot

Milestones 0, 1, 2, 3, 4, and 5 are `PASS`; Milestone 6 is active and `VSCODE-001` is checkpointed at `76765e2`. The production compiler/LSP is unchanged: Debug and ReleaseSafe each pass 489/489 tests across 30/30 steps, and the prior five-target inspection remains applicable. The VS Code extension installs from an exact clean lockfile, passes 13/13 discovery/manifest/workflow tests, and packages an exact five-file 120,473-byte pre-release VSIX that includes license notices but no native executable or generated residue. Runtime executable discovery and launch arguments fail closed under absolute-path, executable-permission, entry-count, length, and NUL boundaries; CSS-only activation is disabled in untrusted and virtual workspaces. Docs pass 111/111 and build. Seven Zig-package tests, two npm-wrapper tests, exact BCD generation, the eight-format matrix, 21-feature/18-output/four-rejection CSS gate, and all ten transform differentials pass. Root, docs, and extension production audits report zero vulnerabilities; the root npm dry run remains five files/25,142 bytes. Final fresh package `zigcss-0.3.0-Bz3pccorGwDJKFEnRD0pbnqB5CEiZKMJc2q_IlWe4w9k` contains 75 allowlisted files/1,933,312 disk bytes, builds 3/3 in Debug and ReleaseSafe, and its dependency consumer passes 6/6 steps with 2/2 tests in both modes. JSON/JavaScript syntax, generated-state, diff, and focused formatting gates pass; repository formatting debt remains exactly 12 inherited files. `NEOVIM-001` is active next.

## LSP-007 validation snapshot

Milestones 0, 1, 2, 3, 4, and 5 are `PASS`; Milestone 6 is active and `LSP-007` is checkpointed at `fc6cc22`. Debug and ReleaseSafe each pass 489/489 tests across 30/30 steps (45 executable units, 20 LSP units, four transport units, two standalone position units, three standalone index units, 353 library/core, four external API, 57 CLI integration, one helper unit). Child transcripts prove bounded sequential framing, malformed-JSON recovery, notification silence, lifecycle/version rules, exact UTF-16 non-BMP navigation, complete recoverable compiler diagnostic pulls, advertised syntax-aware capabilities, deterministic multi-document symbols/navigation/rename, a >1 MiB Unicode document with compiler/index requests, exact close invalidation, and both exit statuses. Unit tests cover every handler response, both position-conversion directions, typed/opaque index semantics, 64 repeated Unicode index lifecycles, transactional state/resource limits, standard diagnostic/editor schemas, balanced full document sync, safe CSS rename serialization, and exhaustive allocation failure in allocating paths. Path-package consumer/example evidence remains green from the unchanged production implementation. Format validation proves seven removed/one limited-native adapters; stable CSS remains 21 features/18 outputs/four rejections and all ten transform differentials pass. Five release architectures inspect correctly. Fresh package `zigcss-0.3.0-Bz3pcTYqGwClVRaT1SNHl9UaASqZOMRzpD97q90LY2S7` contains 75 allowlisted files/1,929,216 disk bytes, builds 3/3 in both modes, and passes 4/4 external-consumer tests in both modes. Seven package tests, two wrapper tests, four prefix tests/exact generation, benchmark smoke, VS Code TypeScript, clean root/docs production audits, 107/107 docs/build, and a five-file/24,626-byte npm dry run pass. Focused formatting, generated-state, and diff checks pass; repository formatting debt remains exactly 12 inherited files. `VSCODE-001` is active next.
