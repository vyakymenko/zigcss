# ZigCSS 0.6 stable release status

ZigCSS 0.6.0 is the published stable release: all five machine rows are native-graduated, and its non-prerelease GitHub Release's 15 attested archive/checksum/SBOM subjects plus 10 Sigstore bundle files, immutable npm `latest` version with SLSA provenance, preserved `next`, and anonymous five-syntax install are verified. GitHub releases 372291445 (`0.6.0`) and 369856953 (`0.6.0-rc.2`) predate Immutable Releases and both read back `immutable: false`; their protected tags and immutable npm versions remain closed historical evidence.

Active source candidate 0.7.0-rc.1 is selected in `release/next-release.json` but is not published. Its `candidateReady` interlock remains `false` until all seven pre-tag gates pass; published stable identity remains 0.6.0 throughout that work.

5 of 8 admission gates now carry recorded evidence: candidate selection, active-version synchronization, native artifact reproducibility, complete local validation, and documentation/site validation. The local gate includes exact Nix 2.35.2 sandboxed `aarch64-linux` flake check, build, install checks, and run inside an ephemeral digest-pinned container. Hosted Build, exact `origin/main` integration, and tag-workflow publication remain pending.

The active source version may advance without rewriting that closed publication record or the verified Homebrew formula; active package/CLI synchronization and published-stable evidence are validated as separate fail-closed identities.

## Current contract

<!-- capability-status:start -->
| Surface | Status | Current behavior |
|---|---|---|
| .css parsing and emission | Experimental, matrix-tested | Strict file/stdin and file/stdout modes delegate to `zigcss.compile`; version, syntax, options, and 0/1/2 exit statuses are executable contracts. |
| Zig compile API | Experimental, consumer-tested | `zigcss.compile` retains the stable CSS facade; the explicit `zigcss.experimental_native` namespace returns owned CSS, composed maps, located diagnostics, and confined dependency facts for the four native differential frontends. |
| Programmatic Node.js API | Experimental, package-tested | The Unreleased source package root exposes typed CommonJS and ESM `compile`, `compileSync`, `compileFile`, `compileFileSync`, `detectSyntax`, and `ZigCssCompileError` APIs for all five syntaxes. Each bounded call canonicalizes source/root paths, uses one selected native process, and returns immutable CSS, parsed maps, diagnostics, and dependencies; optimizer and explicit target-prefix options, async timeout, `AbortSignal` cancellation, a 4 MiB sync frame cap, allocation-bounded native response serialization, request-ID-preserving operational failure envelopes, and a confined source-checkout binary path are verified with zero production dependencies. Direct use requires the freshly built binary from the same checkout; published or repacked 0.6.0 is not claimed because its immutable stable binary predates `zigcss-node-v1`. A strict TypeScript 7.0.2 consumer compiles the complete root and adapter export surface plus negative immutability and closed-option assertions. GitHub Actions requires the lifecycle-disabled offline local-tarball matrix to install one exact archive through npm, pnpm 11.25.0, Yarn Classic 1.22.22, Yarn Modern 4.9.4 in both `node-modules` and default Plug'n'Play modes, and Bun 1.4.0. All six execution variants check both package binaries and every adapter export without silently supplying a native binary. The five `node_modules` variants use ordinary CommonJS, ESM, and declaration package resolution and compile the strict TypeScript consumer. The default-PnP route uses Yarn's loader for CommonJS and ESM export resolution, verifies exact import/require declaration bytes, and path-maps those files for a condition-specific strict compile; exact unpatched TypeScript 7.0.2 without a Yarn SDK or patch is expected to report `TS2307` for no-paths PnP package resolution, so native TypeScript PnP resolution is not claimed. Projects that require it should follow [Yarn's official SDK guidance](https://yarnpkg.com/getting-started/editor-sdks); SDK generation and editor integration remain outside this gate. That route additionally proves `preferUnplugged`, no `node_modules`, confined Yarn and Corepack state, and controlled release-independent local-fixture recovery. |
| Zig package metadata | Experimental, consumer-tested | Package `zigcss` 0.7.0-rc.1 declares minimum Zig 0.15.2 and a minimal source allowlist. CI copies only that allowlist through `zig fetch` into a fresh confined cache, rejects symlinks or oversized inventory, and compiles the external package consumer against the fetched copy rather than the full checkout. This active source candidate is unpublished; stable delivery remains 0.6.0. |
| Zig build helper | Experimental, consumer-tested | `@import("zigcss").helpers.addCssCompile` uses declared lazy inputs and generated outputs with a host compiler artifact; unavailable CLI features have no helper path. |
| Nix flake source build | Experimental, repository-local gated | The current checkout exposes default Nix `packages`, `apps`, and `checks` for `x86_64-linux`, `aarch64-linux`, `x86_64-darwin`, and `aarch64-darwin`. One commit- and `narHash`-pinned nixpkgs input supplies exact Zig 0.15.2; an explicit source allowlist and bounded version, CSS, and SCSS install checks fail closed. The GitHub Actions matrix is configured to validate before the pinned installer action and build and run each native Unix output with exact Nix 2.35.2. Bootstrap and input or substitute acquisition require network access; the versioned installer script itself is not content-addressed; runner images and the experimental flake CLI remain mutable interfaces. This is repository-local source-build evidence only, with no nixpkgs or registry publication, binary cache, offline install, Windows output, NixOS or Home Manager module, or stable-release delivery claim. |
| Native plugins | Experimental, trusted/library-only | Explicit `.experimental` options run borrowed `plugin.`-namespaced Zig callbacks through bounded deterministic pass plans; there is no stable ABI, sandbox, CLI, or HTTP path. |
| --minify | Experimental | Compacts emitted whitespace without enabling AST transforms and remains independent of `--optimize`. |
| Output planning | Unreleased, safety boundary verified | The current Unreleased CLI rejects output/depfile aliases of every entry and discovered native dependency before any single/watch write or batch commit, including symlink and hard-link identities, plus duplicate final destinations. Batch names are deterministic and its cross-task dependency union is validated before the first commit; every file destination is atomically replaced. Current-source single-file `--depfile` emits the authored CSS target plus canonical entry and only native dependencies actually read, with bounded Make/Ninja escaping and CSS committed last as the success marker. Published stable 0.6.0 has no `--depfile` option and predates this hardened commit contract. |
| --profile | Experimental, measured | For native CSS only, reports one monotonic public compile total, actual stage intervals, and allocator-requested allocation, live, and retained metrics from the owned API result; preprocessor routes reject profiling. |
| Verified optimizer preset | Experimental, Unreleased and acceptance-gated | In the current Unreleased CLI, CSS, SCSS, indented Sass, Less, and Stylus `--optimize` requests run exactly seven verified analysis, cleanup, and semantic passes to a bounded byte-stable fixed point; preprocessor inputs enter only after native frontend evaluation, source maps remain incompatible, and separately authorized classes remain excluded. Published stable 0.6.0 does not provide this all-syntax optimizer contract. |
| Dead-code and critical-CSS extraction | Experimental, library/test-only | Two bounded passes use complete class/ID inventories and require separate experimental plus extraction authority; stable CLI flags remain unavailable. |
| Target prefix rewrite | Experimental, Unreleased all-syntax CLI/API verified | In the current Unreleased source, an explicit canonical target query lets `--autoprefix`, both Zig APIs, and the current-checkout programmatic Node API run one verified eight-feature property, value, selector, and at-rule pass for CSS, SCSS, Sass, Less, and Stylus, composing separately with source maps or optimization; this is not a general autoprefixer. Published stable 0.6.0 rejects the CLI prefix flags. |
| Source maps | Experimental, Unreleased all-syntax CLI/API verified | The current Unreleased CLI embeds deterministic inline maps for CSS, SCSS, Sass, Less, and Stylus across file, stdin, watch, and bounded batch routes. Both Zig APIs and the current-checkout programmatic Node API return separate maps, with each native frontend map composed through the core. UTF-16 lookup uses a sparse byte-stride index whose storage remains bounded even for newline-dense input. Published stable 0.6.0 rejects CSS CLI Source Maps and predates this unified all-syntax contract. |
| Browser target queries | Experimental, Unreleased all-syntax CLI/API verified | In the current Unreleased source, `--browsers`, both Zig APIs, and the current-checkout programmatic Node API accept only a strict explicit-minimum grammar over six browsers backed by pinned BCD 8.0.0 data; there are no defaults, aliases, dynamic market-share forms, network lookups, or configuration discovery. Published stable 0.6.0 rejects the CLI browser-query flag. |
| CSS Modules | Experimental, Zig-library-only native subset | Explicit `.syntax = .css_modules` provides source-specific class names, functional scope, plain-class composition references and dependencies, and local values with owned results; CLI and LSP exposure remain unavailable. |
| SCSS input | Native graduated verified | The published 0.6.0 stable native release and `zigcss.experimental_native` compile SCSS with confined imports, owned diagnostics, deterministic maps, and bounded batch/watch behavior. Dart Sass 1.101.0 development oracle does not run during compilation. Plugins, custom functions/importers, and project code remain outside the contract. |
| Indented Sass input | Native graduated verified | The published 0.6.0 stable native release and `zigcss.experimental_native` compile indented Sass with confined imports, owned diagnostics, deterministic maps, and bounded batch/watch behavior. Dart Sass 1.101.0 development oracle does not run during compilation. Plugins, custom functions/importers, and project code remain outside the contract. |
| Less input | Native graduated verified | The published 0.6.0 stable native release and `zigcss.experimental_native` compile Less with confined imports, bounded data helpers, owned diagnostics, deterministic maps, and bounded batch/watch behavior. Less 4.9.0 development oracle over frozen 4.6.7 baseline does not run during compilation. Its development host uses the shared bounded PNG/GIF/JPEG/SVG dimension parser with no direct `image-size` dependency. JavaScript, plugins, custom functions, and project code remain outside the contract. |
| Stylus input | Native graduated verified | The published 0.6.0 stable native release and `zigcss.experimental_native` compile Stylus with confined imports, bounded helpers/globs, owned diagnostics, deterministic maps, and bounded batch/watch behavior. Stylus 0.64.0 development oracle does not run during compilation. Plugins, custom functions/hooks, and project code remain outside the contract. |
| JavaScript build-tool adapters | Experimental, adapter-tested | The current source package exposes typed Vite, Rollup, esbuild, and Bun plugins plus one shared Webpack/Rspack raw loader. Deterministic protocol-fixture tests retain closed map, diagnostic, identity, asset, dependency-registration, failure, and concurrency contracts. After the native Debug suite builds `zig-out/bin/zigcss`, real Vite, Rollup, esbuild, pinned Bun 1.4.0, Webpack 5.110.2, and Rspack 2.2.2 builds all compile SCSS through that exact current-checkout binary; Bun deliberately makes no native-import watch claim. A pinned Next.js 16.3.4 Turbopack build reuses only `zigcss/webpack` for one condition-confined global SCSS entry and proves exact entry/import source contents, dependency-only persistent-cache invalidation, package-subpath resolution, and native diagnostics offline. Next.js 16.2+ module types are required; this source-checkout gate does not claim CSS Modules, indented Sass, Less, Stylus, arbitrary SCSS globs, a `zigcss/turbopack` export, a general Turbopack plugin, or wider framework support. A separate pinned SvelteKit 2.70.3, Svelte 5.57.0, and Vite 8.2.2 source-checkout gate routes one external `.module.scss` file through `zigcss/vite` and proves client, SSR, and static-prerender CSS Module bindings plus exact entry/partial-source-map content under a deny-network host build. It does not claim embedded `<style lang="scss">` blocks, Svelte preprocessors, framework HMR or watch invalidation, arbitrary adapters or deployment targets, or general SvelteKit compatibility. A real Parcel 2.16.4 local-transformer example proves maps, diagnostics, imported-file invalidation, caching, and native execution, but Parcel's naming rule prevents a `zigcss/parcel` export and no separate transformer package is published. Exact Next.js Webpack, Astro, and Nuxt host proofs are tracked as separate current-source capability rows. TypeScript 7.0.2 strictly compiles every public adapter subpath and negative closed-option assertions. Bazel, Nx, other framework adapters—including Angular—CSS-in-JS, PostCSS-like, Tailwind-like, arbitrary provider plugins, and trusted-project-code adapters remain outside the contract. |
| Next.js Webpack host example | Experimental, pinned host-tested | A pinned Next.js 16.3.4 current-source Webpack gate reuses `zigcss/webpack` through one exact-project-file `enforce: 'pre'` rule for global `app/styles.scss`, while exact Sass 1.101.0 remains only the downstream parser. It warms a private npm cache, repeats `npm ci` offline, and performs `next build --webpack` production builds with the freshly built checkout binary on an exact Node 24.20.0 LTS CI host while blocking public Node network entry points, including loopback listeners and Unix sockets. Its recorded Node boundary rejects direct unsafe process/network bindings, Worker and cluster escapes, and every child process except exact Next workers and the canonical staged ZigCSS `--internal-node-v1` invocation. It proves prerendered HTML, emitted CSS, package-subpath resolution, an unchanged persistent-cache hit with zero native invocations, and then a dependency-only warm rebuild that changes `_tokens.scss` from `#639` to `#7fff00` and invokes native ZigCSS again. The JavaScript preload is not an OS sandbox and trusts the exact lock-pinned host and native addons. The final Next.js/Webpack CSS does not retain the original ZigCSS source-map chain, so source-map delivery is not claimed. This is not stable 0.6.0 adapter delivery and does not claim CSS Modules, indented Sass, Less, Stylus, arbitrary SCSS globs, development HMR or watch invalidation, a `zigcss/next` export, a dedicated framework adapter, other Next.js/Webpack versions, or general Next.js compatibility. |
| Astro host example | Experimental, pinned host-tested | A pinned Astro 7.2.10 current-source gate registers `zigcss/vite` through Astro's Vite configuration and performs a cached-offline, deny-network static production build with the freshly built checkout binary on an exact Node 24.20.0 LTS CI host. Its child-process boundary admits only the canonical staged ZigCSS protocol process and one lock-pinned esbuild 0.28.2 service. It proves one external SCSS CSS Module has the same scoped binding in rendered HTML and emitted JavaScript, consumes one native Sass partial, rebases one fingerprinted SVG asset, and retains exact composed source-map content. This is not stable 0.6.0 adapter delivery and does not claim embedded Astro styles, HMR or watch invalidation, SSR adapters, every renderer, framework aliases, or a `zigcss/astro` export. |
| Nuxt host example | Experimental, pinned host-tested | A pinned Nuxt 4.5.2 current-source gate registers `zigcss/vite` through Nuxt's Vite configuration and performs a cached-offline, deny-network production build with the freshly built checkout binary on an exact Node 24.20.0 LTS CI host. Its child-process boundary admits only the canonical staged ZigCSS protocol process and Nitro's lock-pinned esbuild 0.28.2 service. It proves one external SCSS CSS Module across the client bundle, Nitro server bundle, and prerender output, consumes one native Sass partial, and rebases one emitted SVG asset. The exact native SCSS map chain is retained in Nuxt's intermediate Vite output, while the public output retains client JavaScript maps; no public production CSS map or runtime SSR request is claimed. This is not stable 0.6.0 adapter delivery and does not claim embedded Nuxt styles, modules, HMR or watch invalidation, other builders, deployment presets, or a `zigcss/nuxt` export. |
| LSP | Experimental, stress-tested | Bounded framing, JSON-RPC lifecycle, full sync, UTF-16 positions, pull diagnostics, and syntax-aware open-document features pass large-document, Unicode, malformed-request, leak, and editor-integration gates. |
| VS Code extension | Experimental, package-tested | The closed historical proof keeps Marketplace-compatible package version 0.6.0 mapped to core 0.6.0-rc.2. The current source extension is Marketplace-compatible package version 0.7.0 mapped to core 0.7.0-rc.1 and packages only with the pre-release marker; its exact lockfile, CSS-only trust boundaries, deterministic executable discovery, 13 tests, and five-file VSIX are verified; no binary is bundled or published. |
| Neovim configuration | Experimental, integration-tested | The CSS-only built-in config resolves one trusted absolute executable and passes real Neovim 0.11.7 and 0.12.4 command, capability, diagnostic, hover, rejection, and shutdown smokes; no plugin or binary is bundled. |
| Release archives | Experimental, published and native-smoke-gated | Protected historical tag `v0.6.0-rc.2` produced one verified GitHub prerelease with five architecture-matched archives and 25 exact assets; that release predates Immutable Releases and reads back `immutable: false`. Stable tag `v0.6.0` independently produced the matching non-prerelease 25-asset GitHub Release and npm `latest` package with provenance; GitHub Release 372291445 also reads back `immutable: false`, while both published npm versions remain immutable. Those files are 15 unique attested subjects plus 10 Sigstore bundle containers. Future tag archives use normalized single-entry bytes from the committed epoch and must match the exact five digests carried in the npm integrity inventory before metadata or attestation. Hosted admission records a pre-admission Build and CodeQL checkpoint without embedding the candidate contract's own commit; the Release workflow revalidates same-commit Build and default-setup CodeQL for Actions, JavaScript/TypeScript, and Ruby against the exact runtime tag commit with clean error/warning status and zero open alerts under `security-events: read`. Repository ruleset 22261144 (`Protect release tags`) must read back active for target `tag`, including `refs/tags/v*`, with exact `update` and `deletion` rules, no bypass actors, and no current-user bypass. The `create-release` job waits on GitHub Environment `immutable-release` (ID 21234930544), whose sole required reviewer is `vyakymenko` (ID 7300673), with self-review allowed, administrator bypass disabled, only deployment pattern `v*` of type `tag` (policy ID 59095548), and no stored secrets. An administrator live-reads Immutable Releases as `enabled=true` and the release-tag ruleset before tag creation; once the workflow is waiting, the required reviewer repeats both live readbacks immediately before approval. The current release terminal is draft-first: it rejects published reused identities, resumes only an authenticated exact-tag draft targeting the same commit, confines overwrite to expected draft assets, verifies all 25 local and GitHub asset names, sizes, and SHA-256 digests before publication, then requires the published release to read back `immutable: true` and pass GitHub's cryptographic release attestation before npm publication begins. `v0.7.0-rc.1` must be the first true immutable GitHub Release. A terminal non-success tag workflow must transition to schema 3 `publication-failed`, record exact workflow, GitHub, and npm surface evidence, and permanently close that tag/package identity before a new candidate version is selected. |
| Performance benchmark report | Unavailable | The BENCH-007 generator accepts only a committed, provenance-bound scheduled archive from machine-attested Linux x64 bare metal and renders its 43 complete series deterministically; VM, container, or ambiguous host evidence fails closed, and no archive is selected, so the current report contains no timing, ranking, or ratio claim. |
| Public compile API and playground | Disabled | Public compile routes return HTTP 503 until bounded process and request isolation is implemented. |
<!-- capability-status:end -->

This table, the repository README, and the site feature page consume `docs/src/data/capabilities.json`. Every row names executable evidence gates, and the capability generator rejects unknown statuses, missing anchors, duplicate surfaces, and generated-table drift.

## Current boundaries

The source-built binary accepts the closed `--syntax <css|scss|sass|less|stylus>` set. CSS routes through the stable native `zigcss.compile` facade. SCSS, indented Sass, Less, and Stylus route through self-contained native Zig parser/evaluators, then complete generated CSS enters the same recovery-disabled core. The JavaScript command launcher only locates and invokes that binary. The current `Unreleased` source package additionally exposes a typed programmatic Node.js API that invokes the same selected confined native binary and hosts no language semantics. A native frontend or generated-CSS failure returns no partial result. The already-published 0.6.0 package is immutable and is not retroactively claimed to contain this new API.

Single-file, watch, and multi-file batch compilation retain explicit file/stdin and file/stdout modes. Watch compiles one owned root snapshot and tracks native confined dependencies. CLI admission caps requests at 4,096 patterns and 16,384 expanded inputs, bounds path and glob-component bytes, uses iterative wildcard matching, and byte-sorts matches before indexed batch planning. Batch mode uses at most eight workers; the first failure closes the queue and only complete success reaches argument-ordered atomic writes. `--version` stays synchronized to package metadata, every option is non-repeatable, and exit statuses distinguish success/info (`0`), compilation/I/O failure (`1`), and usage/configuration failure (`2`). Deterministic output names disambiguate colliding stems with normalized-path hashes and cap basenames at 128 bytes. Each destination is atomic, while a multi-file batch is not claimed as one filesystem transaction. The typed CSS boundary is listed in the [CSS compatibility matrix](/guide/css-compatibility); native frontend boundaries are listed in [format compatibility](/guide/format-compatibility).

The experimental LSP transport owns each request body independently, accepts sequential HTTP-style header blocks terminated by exact CRLF pairs, and exits cleanly at a frame boundary on EOF. Headers are bounded to 8 KiB and each dynamically allocated body to 16 MiB. Missing, duplicate, non-decimal, overflowing, oversized, malformed, or truncated `Content-Length` framing is rejected before JSON handling. A real child-process transcript covers a request above the former 8 KiB ceiling followed by a second request.

Each framed body is parsed by the standard Zig JSON parser and every reply is emitted through `std.json.Stringify`; string IDs, dynamic URI keys, replacement text, diagnostic messages, and hover Markdown are escaped rather than interpolated. String, number, null, and arbitrary-precision numeric IDs retain their JSON type. Malformed JSON returns `-32700`, an invalid request envelope returns `-32600`, an unknown method returns `-32601`, and invalid method parameters return `-32602`. A malformed framed request does not terminate the server, and the next frame is processed normally.

The server now follows the LSP 3.17 initialize/shutdown/exit lifecycle. Valid notifications never produce response frames; pre-initialize notifications are dropped, pre-initialize requests return `-32002`, and `initialize` succeeds only once. The advertised `textDocumentSync` capability is exactly `{ openClose: true, change: 1 }`; open requires complete client-owned text, changes require one full replacement at a strictly increasing 32-bit version, duplicate/stale/incremental/unopened notifications do not mutate state, and close releases the owned URI/text. `$/cancelRequest` IDs are validated and the notification is safely ignored because this server deliberately finishes each request synchronously before reading the next frame, so no request can be in flight when cancellation is read. `shutdown` returns `null` without exiting, subsequent requests return `-32600`, and `exit` terminates with status 0 only after shutdown or status 1 otherwise.

Initialization explicitly advertises `positionEncoding: utf-16`, the LSP 3.17 default and mandatory encoding. One shared converter maps request positions to UTF-8 byte offsets and compiler byte spans back to zero-based UTF-16 code-unit ranges. It recognizes LF, CRLF, and lone CR without permitting a position inside CRLF, counts astral scalars as two units, clamps an overlong character to the line end, and rejects negative, overflowing, invalid UTF-8, or mid-surrogate positions as invalid parameters. Unit tests cover both conversion directions and malformed boundaries; an executable transcript proves that an astral prefix and CRLF still return the exact UTF-16 definition range.

The server advertises `diagnosticProvider` with identifier `zigcss`, no inter-file dependencies, and no workspace diagnostics. The standard singular `textDocument/diagnostic` request compiles the currently synchronized CSS bytes through the same external `zigcss.compile` facade as the CLI and returns a `kind: full` report. Each owned compiler diagnostic preserves authored order, exact message, `CSS`/`API` code, mapped severity, and a span-derived UTF-16 range; recoverable input can therefore return multiple useful problems rather than the inherited parser's first error. Valid CSS returns an empty full report. The optional matching identifier and string `previousResultId` are validated, but no result ID is issued and every response remains full until a cache has measured value. The prototype plural method is rejected with `-32601`, unopened documents or malformed provider parameters return `-32602`, and operational compiler failures return `-32803`. Removed, library-only, and unknown formats never fall back to CSS: they receive one `CSS0009` diagnostic.

Initialization now advertises hover, completion, document symbols, workspace symbols, definition, references, and rename. Every feature lazily builds a bounded per-version index from the rebuilt typed CSS pipeline; a successful full change invalidates the old index and close frees it. Cross-document operations inspect only currently open CSS documents, sort by URI and then source order, and never scan the filesystem, load imports, or infer an unopened project graph. Persistent state defaults to at most 4,096 documents, 256 MiB of synchronized text, 16 MiB of aggregate URI bytes, 64 KiB per URI, and 128 MiB of allocator-measured cached index storage. Workspace requests admit at most a 256-byte query and 100,000 results; every editor response is conservatively preflighted against a 16 MiB escaped-JSON budget before serialization. Each index independently caps symbols, properties, declaration contexts, opaque spans, individual decoded names, total owned name bytes, allocation bytes, and completion scan work. Opens and changes cross-check their replacement totals before allocation or mutation, so a rejected update retains the previous version and index state; an index that would exceed its 32 MiB document or aggregate workspace budget is destroyed before a request-failed response.

The index recognizes typed class and ID selectors (including parsed selector-list pseudo arguments), custom-property declarations and `@property` definitions, `var()` references, keyframe definitions, and unambiguous `animation-name` references. String, comment, and URL contents are opaque except for grammar-defined quoted keyframe names; ambiguous animation shorthands and invalid recovered animation-name lists are not guessed. Class and ID selectors represent external DOM names, so they support hover, symbols, references, and syntax-only rename but deliberately return no stylesheet definition. Custom properties and keyframes support open-document definition/reference/rename. Completion offers only a closed property-name list in declaration-name contexts and returns an exact UTF-16 `textEdit`; values, selectors, strings, and comments receive no speculative items. Rename validates the symbol kind, rejects NUL or malformed UTF-8 names, and emits safely serialized CSS identifier spelling. Distributable editor packaging remains owned by the editor packages.

The final protocol stress transcript starts with malformed JSON and an invalid request envelope, then initializes the real executable, synchronizes more than 1 MiB of CSS, and proves exact Unicode custom-property definition and property-hover ranges across an astral prefix, CRLF, and non-ASCII identifiers. The same document returns a compiler-backed empty diagnostic report, close makes the next positional request invalid, shutdown returns null, exit succeeds, notifications stay silent, every response frame is consumed exactly, and response output remains below 64 KiB. A separate allocator-checked test repeats Unicode open/index/full-change/re-index/close ownership 64 times and requires document, text, URI, and allocator-measured index state to return to zero after every cycle. Together with exhaustive frame, JSON, state, and editor allocation-failure tests, this completes `LSP-007`.

The experimental VS Code workspace extension maps core 0.6.0-rc.2 to Marketplace-compatible package version 0.6.0 because VS Marketplace accepts only `major.minor.patch`; the verified `--pre-release` package marker preserves the channel. It installs reproducibly from its exact npm lockfile, activates only for file-backed CSS, contributes no competing preprocessor languages, launches no unused filesystem watcher, and is disabled in untrusted and virtual workspaces. An empty server-path setting checks a future extension-local `bin` directory and then only absolute `PATH` entries; an explicit setting must be a verified absolute executable or a bare command resolved through those entries. Relative paths never execute from the current workspace, POSIX candidates require executable permission, and Windows lookup uses `Path` plus `PATHEXT`. Thirteen cross-platform discovery, real-file, manifest, version, and workflow tests pass. Pinned TypeScript and esbuild produce one CommonJS runtime; pinned official `vsce` 3.9.2 admits exactly `package.json`, README, project license, third-party dependency notices, and `dist/extension.js`, then creates a temporary pre-release VSIX whose ZIP signature and bounded size are checked before cleanup. Hosted CI uses exact Node 24.20.0 LTS, `npm ci --ignore-scripts`, tests, and the package check; the extension's declared local source-test floor remains Node.js 22 or later. No native binary enters the VSIX, no registry/marketplace action is performed, and publication remains unauthorized.

The Neovim integration uses the built-in configuration API available in supported Neovim 0.11.7+ rather than the deprecated `require('lspconfig').…setup()` framework. The runtime file attaches only to CSS, supports a `.git` workspace root or a single file, and sets the completion omnifunc only after the real client advertises completion. `ZIGCSS_LSP_PATH` must name an absolute regular executable; otherwise at most 1,024 absolute `PATH` directories are searched (with bounded `PATHEXT` handling on Windows) before the exact `--lsp` argument is appended. Empty/relative entries are ignored and an invalid explicit path fails closed without fallback. CI downloads the official Neovim 0.11.7 minimum and current 0.12.4 Linux archives, verifies both pinned SHA-256 digests, and uses isolated XDG directories. Each real headless client opens CSS, attaches one ZigCSS process, negotiates UTF-16/full sync, checks pull diagnostics plus seven editor methods and six absent methods, completes a `color` hover, and shuts down cleanly. The configuration bundles neither `nvim-lspconfig` nor a native binary.

The build registers `src/lib.zig` as the external `zigcss` module. A separate consumer test imports that name rather than testing from inside the root, then calls the owned high-level compile facade. Results retain CSS, optional map bytes, source names and line/column locations for diagnostics, ordered decoded top-level `@import` dependencies, optional module-export state, and optional profile metrics after compilation cleanup. Duplicate imports remain ordered; malformed or dynamic import preludes do not become guessed dependencies; count/owned-byte limits fail with a structured resource diagnostic and no partial facts. Profile memory values measure allocator-requested bytes rather than RSS, and retained bytes describe the still-owned result at return.

The explicit `zigcss.experimental_native` namespace admits exactly SCSS, indented Sass, Less, and Stylus. It owns source bytes, root paths, CSS, composed maps, diagnostics, dependencies, and watch snapshots across compiler cleanup. Every root path must be an absolute admitted directory; imports stay confined, limits and cancellation are bounded, and failures return no partial facts. The namespace remains deliberately separate from the stable CSS facade and does not imply executable plugin parity. The `NATIVE-009` published candidate `0.6.0-rc.2` is verified on the GitHub prerelease and npm `next` channels.

## Programmatic Node.js API boundary

The current `Unreleased` package root maps CommonJS `require('zigcss')` to `api.cjs`, ESM `import ... from 'zigcss'` to a real `api.mjs` facade, and TypeScript consumers to `api.d.ts`. The closed export set is `compile`, `compileSync`, `compileFile`, `compileFileSync`, `detectSyntax`, and `ZigCssCompileError`. Direct checkout use requires a current `zig build -Doptimize=ReleaseFast`; only a regular non-symlink `zig-out/bin/zigcss` beneath a checkout with the exact source markers is admitted, while an installed package admits only its regular packaged binary. The immutable stable 0.6.0 archive predates `zigcss-node-v1`, so neither the published package nor a repacked 0.6.0 source tarball is claimed to run this Unreleased API. String compilation defaults to CSS unless an explicit syntax or recognizable source path selects SCSS, indented Sass, Less, or Stylus; file compilation infers only `.css`, `.scss`, `.sass`, `.less`, `.styl`, and `.stylus`. A string `sourcePath` may name a virtual file, but its parent must exist and is realpath-canonicalized. Every explicit root must be an existing directory and is canonicalized before transport, and at least one root must contain the entry path; file APIs resolve the real input file and derive its canonical parent root. This prevents directory aliases such as macOS `/tmp` from creating different logical confinement identities.

Each compile call owns one private invocation of the selected source-built or packaged binary over the bounded length-framed `zigcss-node-v1` stdin/stdout protocol. It uses no shell and no temporary source file. Requests carry one source, absolute logical source path, one to sixteen resolved import roots, pretty or minified output, optional source maps, optional fixed-point optimization, and an optional explicit browser-minimum query. The native side validates the exact frame, schema, request ID, local paths, option combinations, syntax, and canonical query before compilation. Source maps and fixed-point optimization remain mutually exclusive.

Successful calls return a deeply immutable snapshot containing CSS, a parsed source-map object or `null`, diagnostics, and dependency facts. A compile failure raises `ZigCssCompileError` with immutable diagnostics and no partial CSS. Malformed, truncated, oversized, mismatched, invalid-map, ill-formed Unicode, or ill-typed diagnostic/dependency replies fail closed. Source input is capped at 10 MiB and encoded requests at 64 MiB. The native encoder enforces its 128 MiB response ceiling while serializing, before transferring ownership of the buffer, including for escape-heavy content. Expected browser-query, root, path, resource, compilation, and response-limit failures reached after schema admission return a bounded protocol envelope with the original request ID; malformed transport/schema and allocation failure remain process errors. Asynchronous response frames are capped at 128 MiB; synchronous calls use a stricter 4 MiB framed-output buffer so the shared `spawnSync` ceiling cannot admit a 128 MiB stderr flood. stderr is capped at 4 MiB in both modes. Timeouts are bounded from 1 ms through 120 seconds; asynchronous calls also accept `AbortSignal` and terminate their private process on cancellation, while synchronous calls reject signals.

This is a generic compiler API, not an incremental service. The experimental build-tool adapters described below are separate public subpaths that delegate to it; the package-root API itself exposes no watch mode, shared cache, plugin execution, provider fallback, private transport, network lookup, or arbitrary project-code hook. Starting one native process per call keeps ownership and cancellation isolated; persistent worker or incremental-cache claims require separate evidence.

`CompileOptions` admits stable CSS plus an explicit experimental `.css_modules` tag, pretty/minified output, separate maps, the closed optimizer preset, the verified target-prefix pass with a borrowed canonical query, explicitly experimental trusted native plugins, bounded dependency/module metadata, and opt-in profiling. Targets without prefixing, prefixing without targets, forged queries, optimized source maps, active plugin source maps, and CSS Modules combined with optimizer/prefix/plugins return `API0001` diagnostics without CSS. An unavailable timer returns the operational `ProfilingUnavailable` error. Stable CSS returns no module exports; successful CSS Modules results own first-authored class/value entries and nested composition references. Module dependencies remain facts rather than loads.

The Zig manifest pins package identity, active project version 0.7.0-rc.1, and minimum Zig 0.15.2. Its allowlist contains only the build entry point, manifest, supported build helper, compiler sources, README, and license. A path dependency compiles the public module. A second mandatory CI gate creates a fresh confined `zig fetch` cache, verifies the copied tree's exact top-level inventory, bounds, and absence of symlinks, and compiles the same external consumer against that cache copy. Tests, docs, wrappers, and editor files are not exported; the source manifest intentionally claims no remote URL/hash.

The dependency's build module exports `helpers.addCssCompile`. It configures an ordinary cached run step during graph construction, accepts one declared input `LazyPath`, creates one generated CSS output path, and exposes only the stable CLI's optimize/minify choices. A downstream `CheckFile` proves exact output, repeated runs are cached, and changing only the input bytes reruns compilation. Output names are bounded portable `.css` basenames. The caller must supply a host-runnable ZigCSS artifact; source maps, prefix/target options, alternate formats, arbitrary arguments, and batch directories remain absent. The complete `examples/build-integration` project proves separate host/application targets, library import, CSS check/install, executable compilation, and API tests in Debug and ReleaseSafe CI jobs.

The root package, documentation site, Next.js Turbopack example, SvelteKit example, Astro example, Nuxt example, and VS Code extension are seven independently locked npm surfaces with version-3 lockfiles and exact direct pins. The local Parcel example is an eighth exact package manifest: it must stay dependency-free and script-free, while its Parcel 2.16.4 toolchain remains owned by the root lockfile; any additional package manifest or lockfile fails the inventory. All four framework-host examples keep their host packages development-only and have zero production dependencies. CI audits all seven production lock graphs, then the complete root development graph—including Parcel—then the complete documentation and VS Code build graphs, and finally the four full pinned Next.js, SvelteKit, Astro, and Nuxt host locks, with any high or critical finding treated as a failure. The documentation deployment repeats its complete build-graph audit and may publish only the exact commit from the workflow file `.github/workflows/build.yml`, whose exact display name is `Build`, after a successful same-repository push on `main`; the checked-out source identity must still equal that triggering SHA before deployment. Pull requests, `development`, failed builds, manual dispatches, a same-named workflow at another path, a forked head repository, and any checkout drift cannot deploy Pages. Every documentation and release job has a validated finite timeout. The root full audit explicitly covers the development-only Less 4.9.0 forward oracle over the frozen 4.6.7 native baseline. A closed Dependabot v2 policy opens bounded weekly version-update pull requests for the seven independently locked npm directories, GitHub Actions, and the root Docker ecosystem; the root-bound Parcel manifest receives dependency updates through the root directory. It contains no registry credentials, target-branch override, reviewer/assignee authority, or automerge path; dependency changes must pass the ordinary pull-request gates.

Every emitted HTML page puts one exact, browser-supported meta Content Security Policy before any resource-bearing element, so that policy applies on the canonical GitHub Pages site. GitHub Pages owns its HTTP response headers; the site therefore does not claim COOP, CORP, `Permissions-Policy`, `X-Frame-Options`, or CSP `frame-ancestors` there. Those response-only controls are exact, tested guarantees of the optional static documentation container. The unfiltered common `Build` workflow installs the locked documentation graph and runs the complete documentation test suite for every `main` or `development` push and pull request, including changes outside `docs/` that its repository-wide assertions read.

Every future tag-triggered publication is gated before npm identity, packing, artifacts, attestations, or publication authority by bounded GitHub API proofs that the exact tagged commit has a successful same-repository `main` push run of the exact `Build` workflow at `.github/workflows/build.yml` and clean same-commit default-setup CodeQL analysis in the Actions, JavaScript/TypeScript, and Ruby categories, with no error or warning status and zero open CodeQL alerts under `security-events: read`.

All four GitHub Actions workflows deny token permissions by default. Their twelve jobs declare only the access they use: ordinary build, test, native-package-evidence, documentation, benchmark, npm-preflight, and anonymous public-delivery jobs receive read-only repository contents; the Pages deploy job receives Pages and OIDC write access; the release build job receives attestation and OIDC write access in addition to read-only contents; the npm publish job receives read-only contents plus OIDC for package provenance; and the release-creation job alone receives repository-content write access. All 47 action references are pinned to reviewed full commit SHAs with exact release comments, including the Bun setup action used by the package-manager and adapter gates and the Nix installer action used only by non-Windows build rows. A dependency-free gate owns the complete workflow, job, permission, and action inventory, rejects alternate YAML indirection, and runs before dependency installation; weekly Dependabot updates remain review-only.

Published 0.6.0 and its closed historical RC each carry a verified five-file set for every native target: the architecture-checked archive, a SHA-256 manifest, a deterministic SPDX 2.3 SBOM, a SLSA v1 provenance attestation bundle, and an SPDX SBOM attestation bundle. Their 25 release files comprise 15 unique attested subjects and 10 bundle containers. Both historical GitHub releases read back `immutable: false`; their exact npm versions remain immutable. The current `Unreleased` source additionally owns a dependency-free archive creator that emits one normalized executable entry in deterministic tar.gz or ZIP bytes from the committed `sourceDateEpoch`, plus a strict `native-integrity.json` inventory fixing the version, epoch, five filenames, and SHA-256 digests. Future tags must reproduce those committed digests before metadata or attestation begins. The bounded manifest/SBOM generator and local bundle parser recheck exact names, digests, predicate types, signer workflow, ref, source commit, and SBOM bytes. These future-release integrity additions do not retroactively describe the published 0.6.0 npm package, which predates the independent npm-carried inventory.

The ordinary build matrix and tag matrix both use architecture-matched hosts for all five targets. Before upload or attestation, each archive is extracted and executed for exact version and compilation output. A development build stages an isolated allowlisted npm package copy whose integrity manifest changes only the current target digest, then exercises real postinstall against those same preloaded local asset bytes; the repository trust file is never rewritten. A tag release instead consumes the exact npm archive packed once by preflight, and its postinstall must match the already committed digest without a bypass. Unexpected HTTPS, target/header mismatch, package inventory drift, checksum failure, byte mismatch, or lifecycle residue fails the job. That instrumented fixture proves deterministic package behavior; it is not a promise that a normal registry installation or recovery command can download release assets offline.

The repository Homebrew formula builds stable 0.6.0 from one immutable commit archive, verifies its exact source SHA-256, and selects the pinned Zig 0.15 toolchain path. CI repeats those exact source-download, digest, build, version, and CSS-compilation operations in a bounded formula-equivalent smoke; it does not claim to execute a mutable Homebrew install transaction. The formula is usable from a checkout, but no public tap is claimed.

The current checkout additionally carries a repository-local Nix flake for `x86_64-linux`, `aarch64-linux`, `x86_64-darwin`, and `aarch64-darwin`. One commit- and `narHash`-pinned nixpkgs input supplies exact Zig 0.15.2, an explicit allowlist confines source admission, and bounded version, CSS, and SCSS install checks back its default package, app, and check outputs. The ordinary build matrix is configured to validate the static contract before a full-SHA `cachix/install-nix-action` reference, install exact Nix 2.35.2 without enabling KVM or adding the runner user to Nix `trusted-users`, enable sandboxing, and check, build, and run the current native output without registries or lock updates. This describes configured CI, not a claim that a new hosted run has already completed.

The Nix route still has external bootstrap boundaries: installer, input, and substitute acquisition require network access; the versioned installer script is not content-addressed; GitHub-hosted runner images are mutable; and flakes remain an experimental Nix interface. It is repository-local source-build evidence, not nixpkgs or registry publication, a binary cache, an offline installer, a Windows output, a NixOS or Home Manager module, or stable-release delivery.

## Unreleased package installation recovery boundary

npm, pnpm, Yarn, and Bun may disable dependency lifecycle scripts or require package-specific approval. In the current source package, a suppressed lifecycle leaves no native executable: the launcher does not search ambient paths or start a fallback compiler and names the planned `zigcss-install` recovery binary. The published stable 0.6.0 package does not expose that binary or the independent npm-carried integrity inventory; stable users must approve its normal install script and reinstall. No published npm version currently exposes the new recovery command.

The current contract's normal installation and `zigcss-install` first read the exact `native-integrity.json` intended for a future npm package, then retrieve the bounded target checksum manifest over HTTPS. Its archive digest must match the independent npm-carried digest before the archive is downloaded; the archive must then match the same SHA-256, contain one exact executable, and expose the expected target header before atomic placement. Each download has byte, redirect, two-minute total-deadline, and 30-second inactivity bounds. The signed Sigstore bundles remain separately verifiable evidence rather than an automatic installer claim. Therefore the future production recovery binary is not an offline installer. CI has two isolated deny-network harnesses: release smokes serve the exact selected release assets, while the default-PnP matrix generates a local host fixture and changes only the temporary installed copy's selected digest. Neither creates a user-facing offline mode; production deployments that forbid downloads must carry an already verified binary in the package's expected `bin` location or build from source.

The current source package's separate manager matrix packs it once, blocks registry access, disables lifecycle scripts, and installs those same bytes through npm, pnpm 11.25.0, Yarn Classic 1.22.22, Yarn Modern 4.9.4 in both `node-modules` and default Plug'n'Play modes, and Bun 1.4.0. All six execution variants are mandatory in GitHub Actions; an unavailable non-npm CLI is only an explicit local-run skip. Every variant verifies both command shims, the exact missing-binary recovery result, the installed five-target integrity inventory, zero runtime dependencies, and all adapter exports. The five `node_modules` variants use ordinary CommonJS, ESM, and declaration package resolution and compile the strict TypeScript consumer. The default-PnP route uses Yarn's loader for CommonJS and ESM export resolution, verifies exact import/require declaration bytes, and path-maps those files for a condition-specific strict compile. Exact unpatched TypeScript 7.0.2 without a Yarn SDK or patch is expected to report `TS2307` for no-paths PnP package resolution, so native TypeScript PnP resolution is not claimed. Projects that require it should follow [Yarn's official SDK guidance](https://yarnpkg.com/getting-started/editor-sdks); SDK generation and editor integration remain outside this gate. The PnP route additionally proves `preferUnplugged`, a writable project-local `.yarn/unplugged` package, no `node_modules`, confined Yarn and Corepack state, and controlled local-fixture recovery that neither depends on a published release nor mutates the exact packed archive.

## Experimental native plugin boundary

Native plugins reuse the immutable pass contract; the inherited mutable `PluginRegistry` is not public. Definitions, metadata strings, request/dependency slices, callbacks, and `user_data` are borrowed only during synchronous `compile`, while all AST output stays in the compilation arena and no plugin pointer enters `CompileResult`. IDs must use the isolated `plugin.` namespace. Only requested IDs and their plugin dependencies execute, after the optional optimizer fixed point; a target prefix pass joins the same final plan. Dependencies take precedence, then ready passes are ordered by phase, ascending priority, and bytewise stable ID.

The outer `.experimental` tag does not grant transform authority by itself. `PluginPolicy` still defaults to verified analysis only; experimental maturity and each output-changing safety class require their own exact booleans. Invalid registries or policy return owned `API0002` diagnostics before callbacks or dependency collection. Callback/validator failure rolls back the compiler root and plan diagnostics, returns owned `API0003` without CSS, and retains already discovered import facts. Caller-owned `user_data` side effects are not reversible. Warnings survive only when the whole plan succeeds.

This is trusted in-process Zig code, not a sandbox or stable ABI. A panic, nontermination, process exit, undefined behavior, or malicious mutation cannot be recovered. Plugins remain unavailable from the recovery CLI and disabled public compiler routes. The complete source contract is recorded in `docs/adr/ADR-011-native-plugin-contract.md`.

Property-specific values are generally preserved as component trees rather than fully validated semantic values. Browser computed-style validation, broader compatibility data, optimized source-map composition, filesystem-backed LSP workspace loading, editor publication/binary distribution, CSS Modules ID/keyframe scoping and external value/ICSS resolution, other alternate formats, and public untrusted compilation remain later gates. Independent syntax acceptance is evidence, not a complete browser-semantics guarantee.

## Verified optimizer boundary

For CSS, `--optimize` invokes one static preset directly. For SCSS, indented Sass, Less, and Stylus, the native frontend first evaluates the source into complete CSS; recovery-disabled core parsing succeeds before the same preset runs. The preset contains duplicate-declaration analysis, empty-rule cleanup, numeric math folding, typed color/zero shortening, margin shorthand synthesis, adjacent at-rule merge, and adjacent selector-rule merge. Every definition is verified, validator-backed, recursively nested, order-preserving, and independently acceptance-tested. The preset policy grants only cleanup and semantic rewrite; target compatibility, extraction, experimental passes, reorder authority, custom-property resolution, and logical-to-physical conversion have no path into it.

Because one proof-carrying rewrite can expose another safe candidate after emission, a changed root enters bounded parse-transform-emit rounds until an exact no-op root or two consecutive byte sequences match. A complete plan that returns the exact input root stops before fixed-point emit/reparse work. The 32-round limit fails without partial output. File, stdin, watch, and bounded batch routes share that behavior across all five syntaxes. Its combined pretty/minified fixture covers cross-pass candidates, fallback and importance order, custom/logical preservation, nested groups, unsupported values, exact byte idempotence, independent Lightning CSS canonical semantics, and size reduction. Fixed-point source-map composition is not yet exposed, so the CLI rejects combining `--source-map` with `--optimize`; profiling remains CSS-only.

## Experimental extraction boundary

The library exposes distinct conservative dead-code and critical-CSS passes. A non-null class or ID category is exhaustive for the declared domain; null is unknown and cannot prove absence. Dead-code mode requires a closed document snapshot. Critical mode requires a closed selector-matching render tree that includes every related node a combinator can inspect—not merely a list of preferred names from a larger live page.

Only direct positive class and ID requirements participate. A style rule is removed only when every selector-list alternative contains a required token absent from a complete category. Type/attribute evidence, functional pseudo arguments, possible selector alternatives, imports, descriptors, keyframes, custom-property dependencies, layers, and other non-style rules remain authored. Inventories are owned, canonical, bounded, and conservative across ASCII case variants.

Both modes remain experimental and library/test-driver only. They require `allow_extraction` and `allow_experimental`, pass independent Lightning CSS selector projection in pretty and minified modes, and are never reached by `--optimize` or `--critical-*`.

## Verified target-prefix boundary

The CLI and both Zig APIs accept comma-separated explicit minimums such as `chrome >= 120, firefox >= 115, safari >= 17.2`. The closed browser set is `chrome`, `edge`, `firefox`, `safari`, `ios_safari`, and `ie`. Queries are lowercase, contain no defaults, and do not implement Browserslist market-share, recency, negation, alias, or configuration syntax. In the CLI, `--autoprefix` requires exactly one `--browsers <query>` and the query option requires `--autoprefix`; malformed or noncanonical input is a usage error with its byte offset and failure kind.

Compatibility facts are generated from the exact `@mdn/browser-compat-data` 8.0.0 lock for a reviewed eight-feature property/value/selector/at-rule subset. The verified library pass emits only complete, unannotated closed forms, keeps vendor declarations before the authored standard, separates vendor selector rules, clones prefixed keyframes, retains manual vendor policy, and produces deterministic source maps. A modern Chrome/Edge/Firefox query is an exact no-op; the reviewed legacy query materially changes output and passes independent Lightning CSS projection in both modes.

This is still not general autoprefixing. Partial, annotated, unsupported, mixed-selector, functional-pseudo, descriptor, recovered, and conflicting/manual cases default to no rewrite. The source-built CLI parses the query once, shares that immutable value across file, stdin, watch, and bounded batch work, runs the verified prefix pass after optional fixed-point optimization, and can compose prefix-generated segments into deterministic source maps. Missing pairs and forged API queries fail before frontend or resolver work, with no partial CSS.

Use published stable 0.6.0 only for rows that explicitly say they are published or stable-native-graduated. Rows labeled current, source-checkout, or Unreleased describe this repository snapshot rather than the npm release; an experimental label alone does not imply that a capability is unpublished. Evaluate representative project stylesheets before production adoption; unsupported ecosystem hooks fail closed rather than silently falling back.

## Tooling integration boundary

Published 0.6.0 remains the immutable CLI-launcher package. The current `Unreleased` source adds typed experimental adapters at six explicit package subpaths. Vite and Rollup use generated sibling CSS identities so downstream CSS processing keeps the source directory, exact query/fragment suffix, and CSS Module suffix. Host-facing maps are independent mutable clones while the public Node API retains deeply immutable result snapshots. Host diagnostics use the required UTF-16 columns for Rollup/Vite and byte offsets for esbuild. esbuild selects `css` or `local-css`, embeds ZigCSS maps, and returns native watch files. Bun uses the documented `onLoad` contract; because that result contract has no `watchFiles` field, Bun native-import watch invalidation is not claimed. Webpack and Rspack share one async raw loader with strict rightmost-loader ordering, native dependency registration, diagnostics, maps, and result metadata; a dependency-mutation watch rebuild remains unclaimed. Deterministic protocol-fixture tests retain the closed failure, map, diagnostic, identity, asset, dependency-registration, and concurrency contracts. After the native Debug suite produces the current checkout binary, real Vite, Rollup, esbuild, Webpack 5.110.2, Rspack 2.2.2, and pinned Bun 1.4.0 builds all compile native SCSS end to end. A pinned Next.js 16.3.4 Turbopack build reuses `zigcss/webpack` for one condition-confined global SCSS entry and proves offline output, exact entry/import maps, persistent-cache invalidation, subpath resolution, and diagnostics with that same native binary. A separate pinned Next.js 16.3.4 Webpack gate uses one exact-project-file `enforce: 'pre'` rule, repeats its exact lockfile install offline, blocks public Node network entry points during `next build --webpack`, traces the staged native protocol process, proves an unchanged cache hit with zero native invocations, and then proves dependency-only invalidation changes the CSS and invokes ZigCSS again. Its JavaScript preload is not an OS sandbox and trusts the exact lock-pinned host/native addons; the final stylesheet does not retain the original ZigCSS source-map chain. A pinned SvelteKit 2.70.3/Svelte 5.57.0/Vite 8.2.2 example routes one external CSS Module through `zigcss/vite` and proves client, SSR, prerender, module hashing, native partials, and exact maps under a deny-network host build. A pinned Astro 7.2.10 static example routes one external CSS Module through `zigcss/vite` and proves a shared HTML/client binding, native partial, fingerprinted asset, and exact maps after a cached-offline install and during a deny-network build. A pinned Nuxt 4.5.2 example uses the same Vite adapter and proves its external module across client, Nitro SSR bundle, and prerender output plus a native partial and rebased asset during a deny-network production build; its native CSS map chain is retained only in `.nuxt` intermediate output, while public client JavaScript maps remain verified. A separate local Parcel 2.16.4 transformer example passes real maps, diagnostic, imported-file invalidation, cache, and native-binary smokes.

| Host | Import path | Evidence boundary |
|---|---|---|
| Vite | `zigcss/vite` | Experimental plugin; real Vite build plus resolution/load, map, diagnostic, query, module-identity, asset-rebasing, and dependency checks. |
| Rollup | `zigcss/rollup` | Experimental plugin; real Rollup build before a downstream CSS consumer, which projects must still provide. |
| esbuild | `zigcss/esbuild` | Experimental plugin; real esbuild bundle for assets/CSS Modules plus inline-map, diagnostic, and watch-file checks. |
| Bun | `zigcss/bun` | Experimental plugin; strict contract test plus a real pinned Bun 1.4.0 CI build. |
| Webpack | `zigcss/webpack` | Experimental async raw loader; real Webpack 5.110.2 build plus focused map, failure, metadata, warning, and dependency checks. |
| Rspack | `zigcss/rspack` | Same experimental loader core; real Rspack 2.2.2 build plus the shared focused contract checks. |
| Next.js Turbopack | Reused `zigcss/webpack` in `examples/next-turbopack` | Source-checkout-only, condition-confined global SCSS example. Next.js 16.2+ module types are required; the gate pins Next.js 16.3.4 and React/ReactDOM 19.2.4 and verifies exact maps, dependency-only persistent-cache rebuilds, diagnostics, and package resolution offline. |
| Next.js Webpack | Reused `zigcss/webpack` in `examples/next-turbopack` | Source-checkout-only global SCSS example with exact Next.js 16.3.4, Sass 1.101.0 as the downstream parser, one exact-project-file pre-loader, cached-offline installation, public Node network entry points blocked, staged-native tracing, an unchanged zero-invocation cache hit, and dependency-only invalidation. The preload is not an OS sandbox; the final CSS does not retain the original ZigCSS map chain. |
| SvelteKit | Reused `zigcss/vite` in `examples/sveltekit` | Source-checkout-only external `card.module.scss` example. The gate pins SvelteKit 2.70.3, Svelte 5.57.0, Vite 8.2.2, and adapter-static 3.0.10 and verifies client, SSR, static prerender, CSS Module hashing, a native partial, and exact maps during a deny-network host build. |
| Astro | Reused `zigcss/vite` in `examples/astro` | Source-checkout-only external `card.module.scss` example. The exact Astro 7.2.10 static gate repeats its lockfile install from an isolated cache in offline mode, denies network access during the host build, and verifies the same CSS Module binding in HTML and client JavaScript, a native partial, a fingerprinted SVG asset, and exact composed maps. |
| Nuxt | Reused `zigcss/vite` in `examples/nuxt` | Source-checkout-only external `card.module.scss` example. The exact Nuxt 4.5.2 deny-network gate verifies the binding in client, Nitro SSR bundle, and prerender output, a native partial, and a rebased SVG asset. Exact native CSS maps remain in `.nuxt` intermediate output; public client JavaScript maps are present, but a public production CSS map and runtime SSR request are not claimed. |
| Parcel | Local `examples/parcel` transformer | Real Parcel 2.16.4 integration with maps, diagnostics, dependency invalidation, caching, and a native-binary CI smoke. Parcel's naming rule prevents a `zigcss/parcel` transformer subpath, and no separately published `parcel-transformer-*` package is claimed. |

A minimal Vite configuration is:

```text
import { defineConfig } from 'vite'
import zigcss from 'zigcss/vite'

export default defineConfig({
  plugins: [zigcss({ sourceMap: true, maxWorkers: 4 })],
})
```

Rollup, esbuild, and Bun use the same `zigcss()` factory from their respective subpaths. Webpack and Rspack configure their subpath as the rightmost source loader, after `css-loader` in the left-to-right declaration. All adapters accept the closed `format`, `sourceMap`, `optimize`, `browsers`, `rootPaths`, `timeoutMs`, and `maxWorkers` option set. Source maps cannot be combined with fixed-point optimization.

Where the host exposes a dependency API, only confined native preprocessor imports are converted into host watch dependencies; Bun is the explicit exception because its current `onLoad` result has no watch-file channel. Authored CSS `@import` remains in the emitted CSS for the downstream host CSS layer, avoiding guessed resolution. The adapters bound concurrent native processes but provide no persistent service or incremental cache. The two Next.js modes are deliberately narrower than a framework adapter: they claim no CSS Modules, indented Sass, Less, Stylus, arbitrary SCSS glob, development HMR/watch invalidation, `zigcss/turbopack` or `zigcss/next` export, dedicated framework adapter, other Next.js/Webpack versions, general Turbopack plugin, or wider Next.js compatibility, and they remain source-checkout-only until a future release ships the current native protocol. [Next.js documents the loader output module types introduced in 16.2](https://nextjs.org/docs/app/api-reference/config/next-config-js/turbopack#module-types), the [`--webpack` build mode](https://nextjs.org/docs/app/api-reference/cli/next#next-build-options), and its [custom Webpack hook](https://nextjs.org/docs/app/api-reference/config/next-config-js/webpack). The SvelteKit example is equally narrow: it claims only one external `.module.scss` file with client, SSR, and prerender evidence at the pinned versions; embedded `<style lang="scss">`, Svelte preprocessors, framework HMR/watch invalidation, arbitrary adapters or deployment targets, and general SvelteKit compatibility remain unclaimed. [SvelteKit documents its adapter surface here](https://svelte.dev/docs/kit/adapters). The Astro example claims only one external CSS Module in an exact Astro 7.2.10 static build; embedded styles, HMR/watch invalidation, SSR adapters, every renderer, framework aliases, and `zigcss/astro` remain unclaimed. The Nuxt example claims only one external CSS Module in the exact Nuxt 4.5.2 client, Nitro bundle, and prerender build; it does not claim a public production CSS map, runtime SSR request, embedded styles, modules, HMR/watch invalidation, other builders or presets, or `zigcss/nuxt`. Both remain current-source-checkout proofs rather than stable 0.6.0 delivery. No PostCSS adapter is shipped because a regular PostCSS plugin sees input only after the CSS parser. A separately published Parcel transformer or framework-specific adapter, Bazel rule, Nx executor, and Angular integration remain unclaimed; internal or transitive use of a covered builder does not establish broader framework compatibility.

Outside JavaScript hosts, the CLI's single-file `--depfile` primitive emits one bounded Make/Ninja-compatible rule over the canonical entry and native imports actually read. The same file is consumable by Make and Ninja rules and by CMake or Meson custom commands; `examples/build-systems` contains minimal configurations. Authored CSS imports remain downstream facts, and Bazel remains unclaimed because a correct rule still needs explicitly declared sources and a toolchain contract.

The ordinary build workflow's Test job uses exact Node 24.20.0 LTS, runs the exact six-variant npm/pnpm/Yarn Classic/Yarn Modern node-modules/Yarn Modern PnP/Bun recovery matrix and strict TypeScript consumer after the seven owned production dependency audits, one complete root development audit, the complete documentation and VS Code build-graph audits, and the four full pinned Next.js, SvelteKit, Astro, and Nuxt host-lock audits. The audit order is production graphs, root development graph, documentation and VS Code build graphs, then the four host locks. After the native Debug suite builds and tests `zig-out/bin/zigcss`, the adapter, Next.js Turbopack, Next.js Webpack, SvelteKit, Astro, Nuxt, Parcel, and dependency-file build-system suites receive that exact binary in that order; CI proves native end-to-end builds for every pinned JavaScript host, dependency-only warm rebuilds in both Next.js modes, an unchanged zero-invocation Next.js Webpack cache hit, a Webpack build with public Node network entry points blocked, deny-network SvelteKit client/SSR/prerender, cached-offline Astro static, and Nuxt client/Nitro/prerender builds, a real Parcel transformation, and a real ZigCSS depfile driving GNU Make through clean, no-op, and native-dependency-change rebuilds. Delivery, declarations, or integration drift therefore fails the same pull-request build rather than relying on documentation claims alone.

## Project direction

The current source recovery program replaces the byte-oriented parser found in the published stable CLI with a tokenizer, source model, syntax tree, standards-oriented parser, and semantics-preserving emitter. This Unreleased compiler enables the closed evidence-backed optimizer preset and the separately configured verified target-prefix pass while other gated or inherited transform paths remain unavailable. Public performance comparisons remain withdrawn because no controlled scheduled archive has been selected; BENCH-007 can render only a committed archive whose report, source/run provenance, hardware fingerprint, raw statistics, and artifact link all revalidate.

- [Build from source](/guide/build-from-source)
- [Builder integrations](/guide/builder-integrations)
- [Recovery CLI](/guide/recovery-cli)
- [CSS compatibility](/guide/css-compatibility)
- [CSS Modules subset](/guide/css-modules)
